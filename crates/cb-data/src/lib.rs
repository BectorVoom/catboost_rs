#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing))]
//! `cb-data` — Pool, dataset, and quantization layer. Stub only; the real
//! `Pool`/quantization surface is realized in Phase 2 (D-05).
