[package]
name = "cb-core"
version = "0.1.0"
edition = "2021"
description = "Shared error types and core primitives for catboost-rs"

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
# NOTE: `anyhow` is intentionally absent (D-14 structural ban). Do not add it.
