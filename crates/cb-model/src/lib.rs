#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing))]
//! `cb-model` — model serialization and SHAP. Stub only; the serialize/SHAP
//! surface is realized in Phase 4 (D-05).
