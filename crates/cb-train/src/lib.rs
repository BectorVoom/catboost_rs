#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing))]
//! `cb-train` — the gradient-boosting training loop. Stub only; the boosting
//! loop is realized in Phase 3 (D-05).
