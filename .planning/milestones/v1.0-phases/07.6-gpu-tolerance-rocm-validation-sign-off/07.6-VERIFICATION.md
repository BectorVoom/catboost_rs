---
phase: 07.6-gpu-tolerance-rocm-validation-sign-off
verified: 2026-06-21T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
mvp_mode: true
---

# Phase 7.6: GPU Tolerance, rocm Validation & Sign-off — Verification Report

**Phase Goal:** The GPU-06 tolerance is established empirically on the in-env gfx1100 GPU, documented, and signed off by the user; `rocm` is locked in as the sole GPU oracle gate with GPU tests running on `rocm` (GPU-03), closing the Phase 7 umbrella.
**Verified:** 2026-06-21T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Verification Posture (Validation/Sign-off Phase, D-06)

This is a measurement/sign-off phase. A verifier subagent has NO GPU access and CANNOT run
the `rocm` suite — that is the documented, expected division of labor (D-06), NOT a gap. The
full `rocm` suite run (75 passed / 0 failed on gfx1100), the `[GPU-06 EVIDENCE]` capture, and
the user ε=1e-4 sign-off were discharged by the **orchestrator/human in-env** this session and
recorded in the SUMMARYs + `07.6-GPU-TOLERANCE.md`. This verifier confirms the **artifacts and
recorded evidence** (harness file, module wiring, signed-off doc content, REQUIREMENTS flips,
CI policy, additive boundary) via non-GPU `grep`/`test -f`/`cargo build` — and ran the
non-GPU compile (`cargo build -p cb-backend --no-default-features --features cpu --tests`) to
prove the module builds under a non-GPU backend. `cargo test --features rocm` was NOT attempted.

## Goal Achievement (MVP User-Story Outcome)

The phase goal decomposes into the 4 ROADMAP Success Criteria, all verified below. The
outcome — "GPU-06 tolerance empirically established, documented, signed off; rocm locked as
sole GPU gate (GPU-03); Phase 7 umbrella closed" — is observably true in the codebase.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | rocm-vs-Rust-CPU divergence (incl. N≥30 run-to-run variance) measured across families on gfx1100; concrete ε proposed with headroom over observed_max+3σ (SC-1) | ✓ VERIFIED | `gpu_tolerance.rs` (806 lines): `gpu06_variance_with_stddev_reports_evidence` runs `VARIANCE_RUNS=32` (N≥30 floor, line 64), computes `mean_variance_stddev` via `cb_core::sum_f64` (D-08), emits `observed_max_plus_3sigma`. `gpu06_per_family_aggregation_reports_evidence` + `gpu06_end_to_end_leaf_values_report_evidence` cover 6 families. Doc §1 derivation: observed_max=7.451e-9, 3σ=0.0 → ε=1e-4. In-env run recorded 6 family `[GPU-06 EVIDENCE]` lines (SUMMARY 01). |
| 2 | User signs off concrete ε, recorded as vs the Rust CPU path (not C++ oracle), looser than 1e-5 (SC-2 / D-04) | ✓ VERIFIED | `07.6-GPU-TOLERANCE.md` §1: "ε = 1e-4 … human-signed-off (D-7.6-07)"; §2: "vs the Rust CPU path — NOT the C++ oracle"; §1: "strictly looser than 1e-5 (D-04) → 1e-4". REQUIREMENTS row: "ε=1e-4, vs Rust CPU path". `checkpoint:decision gate=blocking-human` in PLAN-02; SUMMARY-02 confirms auto-mode did not flow past. |
| 3 | rocm suite is the authoritative GPU gate, green in-env, NEVER in CI; wgpu/cuda remain compile-gated stubs (SC-3 / D-06/D-07) | ✓ VERIFIED | Doc §4 records authoritative invocation + never-in-CI policy. `grep -ri "features rocm" .github/` → only doc-comments (ci.yml:36-41), NO active step. Stale CI step removed (commit `70a046d` exists). SUMMARY-02: rocm 75/0 green, wgpu+cuda build RC=0 (orchestrator-discharged in-env). |
| 4 | wave32-native validation scope documented; explicit wave64 (CDNA/MI) gap NOT closed (SC-4 / D-09) | ✓ VERIFIED | Doc §3: "In scope (validated): gfx1100 (RDNA3), wave32-native"; "Explicit, documented gap (NOT closed here): wave64 (CDNA/MI-series)". `wave64` ×6, `wave32` present in doc. Header: "wave32-native". |
| 5 | GPU-06 measurement harness exists, declared in kernels.rs, builds under every backend; no cb-train dep; cb-core/cb-model/cb-compute byte-unchanged (additive boundary) | ✓ VERIFIED | `gpu_tolerance.rs` exists (806 lines, 11 `GPU-06 EVIDENCE`, 3 `gpu06_*` tests). `#[cfg(test)] mod gpu_tolerance;` at kernels.rs:2695 (plain `#[cfg(test)]`). NO cb-train in Cargo.toml (grep clean; only comments in source). `cargo build -p cb-backend --no-default-features --features cpu --tests` recompiled gpu_tolerance.rs RC=0. `git status` clean for cb-core/cb-model/cb-compute. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-backend/src/kernels/gpu_tolerance.rs` | GPU-06 evidence harness, ≥120 lines, contains "GPU-06 EVIDENCE" | ✓ VERIFIED | 806 lines (SUMMARY said 772; +34 from WR-01 skip-guard fix — benign). 11 `GPU-06 EVIDENCE` strings; 3 `gpu06_*` tests; `max_divergence`, `mean_variance_stddev`, `expected_atomic_finalize_path`, inline CPU refs all present. |
| `crates/cb-backend/src/kernels.rs` | `mod gpu_tolerance` declaration | ✓ VERIFIED | Line 2695 `mod gpu_tolerance;` under `#[cfg(test)]` (line 2694), with doc-comment. |
| `07.6-GPU-TOLERANCE.md` | signed-off doc, ≥40 lines, contains "epsilon" | ✓ VERIFIED | 150 lines. Covers all 5 D-7.6-06 content requirements: signed-off ε+derivation, vs-Rust-CPU-path, wave32/wave64, rocm-only/never-CI, per-family evidence table incl. end-to-end leaf rows. |
| `.planning/REQUIREMENTS.md` | GPU-03/GPU-06 flipped to complete | ✓ VERIFIED | Line 83 `[x] GPU-03`; line 86 `[x] GPU-06`; status rows 184/187 → "Complete (Phase 7.6)" / "Complete (Phase 7.6) — ε=1e-4, vs Rust CPU path". |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `gpu_tolerance.rs` | `crate::gpu_runtime` | launch seams over SelectedRuntime | ✓ WIRED | `use crate::gpu_runtime::{...}` (line 56); drives `launch_der_binary/unary`, `launch_block_reduce_atomic_f64`, `grow_oblivious_tree`, `AtomicFinalizePath`. |
| `gpu_tolerance.rs` | `cb_core::sum_f64` | CPU parity baseline, transcribed inline (never cb-train) | ✓ WIRED | `use cb_core::sum_f64` (line 54); 4 usages; cb-train absent from Cargo.toml. |
| `kernels.rs` | `gpu_tolerance.rs` | `#[cfg(test)] mod gpu_tolerance;` | ✓ WIRED | kernels.rs:2694-2695. |
| `07.6-GPU-TOLERANCE.md` | `[GPU-06 EVIDENCE]` numbers | per-family evidence table transcribed from in-env console | ✓ WIRED | §5 table matches SUMMARY-01 numbers (score_split 7.451e-9, all others 0.0; reduce HostSumFallback stddev=0.0). |
| `REQUIREMENTS.md` | Phase 7.6 | GPU-03/GPU-06 status flip with phase ref | ✓ WIRED | Rows 184/187 reference "Phase 7.6". |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Harness module compiles under non-GPU (cpu) backend | `cargo build -p cb-backend --no-default-features --features cpu --tests` (forced recompile of gpu_tolerance.rs) | Compiled cb-backend, RC=0 | ✓ PASS |
| No active `features rocm` step under `.github/` | `grep -ri "features rocm" .github/` | only ci.yml:36-41 doc-comments | ✓ PASS |
| No cb-train dependency in cb-backend | `grep -in "cb-train\|cb_train" crates/cb-backend/Cargo.toml` | none | ✓ PASS |
| CI-removal + sign-off commits exist | `git log --oneline -1 70a046d` / `a6d65e1` | both found | ✓ PASS |
| Full rocm suite (75/0) + ε sign-off | (orchestrator/human in-env — NO subagent GPU, D-06) | recorded in SUMMARYs + doc | ? DISCHARGED IN-ENV |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| GPU-03 | 07.6-02 | rocm/HIP backend validated on AMD hardware; GPU tests run on rocm | ✓ SATISFIED | rocm 75/0 green in-env (gfx1100); doc §4 authoritative gate; REQUIREMENTS line 83 `[x]` + "Complete (Phase 7.6)" |
| GPU-06 | 07.6-01, 07.6-02 | Documented GPU tolerance vs Rust CPU path with sign-off | ✓ SATISFIED | ε=1e-4 signed off, doc §1/§2/§5; harness emits evidence; REQUIREMENTS line 86 `[x]` + "ε=1e-4, vs Rust CPU path" |

All requirement IDs from PLAN frontmatter (GPU-06 in 07.6-01; GPU-03+GPU-06 in 07.6-02) are
accounted for in REQUIREMENTS.md. No orphaned requirements for Phase 7.6 — the ROADMAP Phase
7.6 requirement list is exactly {GPU-03, GPU-06}, both satisfied and flipped.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | No TBD/FIXME/XXX debt markers in modified files | — | gpu_tolerance.rs is test-only; cb-train mentions are intentional landmine-avoidance comments |

No debt markers, no stubs, no hollow data flow. The hard-coded `channel=f64(rocm/gfx1100)`
println text (REVIEW IN-01) is REPORTING text, not load-bearing logic; the WR-01 skip-guard
(`gpu_backend_active()`) prevents the CPU-vs-CPU false-pass it was flagged for.

### Code Review Disposition

`07.6-REVIEW.md` reported 0 critical, 3 warnings, 3 info. **WR-01** (Tests A/C lacked a
GPU-runtime guard → CPU-resolved false-pass) is FIXED: both `gpu06_per_family_aggregation`
(line 486-493) and `gpu06_end_to_end_leaf_values` (line 710-717) now early-`return` via
`if !gpu_backend_active()` (helper at line 155-157), skipping rather than fake-passing under
cpu. WR-02 (abs-OR-rel disjunction), WR-03 (latent fixture underflow, unreachable), and the
3 INFO items are accepted/deferred — none block the goal (test-harness reporting bounds, not
the signed-off gate; the gate is the single ε=1e-4 in the doc).

### Human Verification Required

None outstanding for THIS verifier. The two human checkpoints in PLAN-02 (full rocm suite
run + ε sign-off) were already discharged by the orchestrator/human in-env this session
(recorded: rocm 75/0 green, user signed off ε=1e-4). Per the phase's documented D-06 division
of labor, no further human verification is requested.

### Gaps Summary

None. All 5 must-haves verified; all 4 ROADMAP Success Criteria met; both requirement IDs
(GPU-03, GPU-06) satisfied and flipped to Complete (Phase 7.6). The harness exists, is wired
under `#[cfg(test)]`, builds under a non-GPU backend, carries no cb-train dependency, and
leaves cb-core/cb-model/cb-compute byte-unchanged (additive boundary). The signed-off
tolerance doc covers every required content area. The Phase 7 umbrella's last two requirements
close. The GPU-side run + sign-off were discharged in-env by the orchestrator/human (the
expected, documented arrangement — not a gap).

---

_Verified: 2026-06-21T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
