---
phase: 07.6-gpu-tolerance-rocm-validation-sign-off
plan: 02
subsystem: GPU-06 epsilon sign-off + GPU-03 rocm gate documentation
tags: [gpu-03, gpu-06, rocm, tolerance, sign-off, gfx1100, wave32, epsilon]
requires:
  - "07.6-01: [GPU-06 EVIDENCE] per-family observed_max + 3σ numbers (gfx1100, f64 channel)"
provides:
  - "07.6-GPU-TOLERANCE.md — the signed-off GPU tolerance / validation document (ε=1e-4)"
  - "REQUIREMENTS.md GPU-03 + GPU-06 flipped to Complete (Phase 7.6) — Phase 7 umbrella closed"
affects:
  - "Closes the Phase 7 umbrella's last two open requirements (GPU-03, GPU-06)"
tech-stack:
  added: []
  patterns:
    - "Proposal → hard human sign-off (D-7.6-07) → documentation/gate, no auto-decide past the epsilon checkpoint"
    - "Single global epsilon as the gate; per-family table as supporting evidence (D-7.6-03)"
key-files:
  created:
    - .planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md
  modified:
    - .planning/REQUIREMENTS.md
decisions:
  - "Signed-off ε = 1e-4 (vs Rust CPU path, D-04) — one clean order looser than the CPU 1e-5 bar, ~5 orders above the global observed max 7.451e-9"
  - "Bit-exact-vs-conservative tension preserved deliberately: D-04 mandates looser-than-1e-5 despite ~bit-exact observations (unmeasured losses / atomic contention / wave64 headroom)"
  - "rocm is the sole authoritative GPU gate, never in CI (D-06); wgpu/cuda compile-gated stubs only (D-07)"
  - "wave32-native scope on gfx1100; wave64 (CDNA/MI) gap explicitly documented, NOT closed (D-09)"
metrics:
  duration: ~10min
  completed: 2026-06-21
  tasks: 3
  files: 2
---

# Phase 07.6 Plan 02: GPU-06 Epsilon Sign-off & GPU-03 Gate Closure Summary

Turned Plan 01's REPORTED divergence measurements into a SIGNED-OFF gate: the user signed off **ε = 1e-4** (vs the Rust CPU path, D-04), it is written with its full derivation + scope into the new `07.6-GPU-TOLERANCE.md`, and REQUIREMENTS GPU-03/GPU-06 are flipped to Complete — closing the Phase 7 umbrella's last two open requirements.

## What was built

- **`.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md`** (NEW, 150 lines) recording, per D-7.6-06:
  1. The **signed-off ε = 1e-4** and its derivation — roundUp to a clean value strictly looser than 1e-5 over `observed_max + 3σ = 7.451e-9`, with the bit-exact-vs-conservative tension documented.
  2. That it is measured **vs the Rust CPU path, NOT the C++ CPU oracle** (D-04).
  3. The **wave32-native scope** on gfx1100 (RDNA3) and the **explicit wave64 (CDNA/MI) gap** this phase does NOT close (D-09).
  4. The **rocm-only / never-in-CI** gate policy (D-06) + authoritative invocation `cargo test -p cb-backend --no-default-features --features rocm`; wgpu/cuda as compile-gated stubs (D-07); note that the stale rocm CI step was removed this session (commit 70a046d).
  5. The **per-family observed-divergence evidence table** (single global ε is the gate, NOT per-family thresholds, D-7.6-03), including the end-to-end leaf-value rows (7.5 REPORTED → now SIGNED-OFF) and the 75 passed / 0 failed rocm-suite note.
- **`.planning/REQUIREMENTS.md`** — GPU-03 `[ ]`→`[x]` + status row → "Complete (Phase 7.6)"; GPU-06 status row → "Complete (Phase 7.6) — ε=1e-4, vs Rust CPU path" (its checkbox was already `[x]`). No other requirement altered.

## VERIFICATION sign-off content

- **Signed-off epsilon:** ε = 1e-4, vs the Rust CPU path, strictly looser than 1e-5 (D-04). Human-signed-off (D-7.6-07); auto-mode did NOT flow past the epsilon decision checkpoint.
- **Authoritative rocm gate (GPU-03):** full `cb-backend` rocm suite GREEN in-env on gfx1100 — **75 passed / 0 failed** via `cargo test -p cb-backend --no-default-features --features rocm`. wgpu + cuda compile-gated stubs build RC=0. No `features rocm` under `.github/` (stale CI step removed this session, commit 70a046d).
- **Scope:** wave32-native validation on gfx1100 (RDNA3); the wave64 (CDNA/MI-series) gap is explicitly documented and NOT closed here (D-09).
- **Discharge note (D-06):** the rocm suite run + the epsilon sign-off were discharged by the **orchestrator/human in-env**, NOT by a verifier subagent (a verifier subagent has no GPU access).
- **Global empirical headroom:** observed max abs divergence across ALL families = **7.451e-9** (score_split); signed-off ε = 1e-4 sits ~5 orders of magnitude above it.

## Deviations from Plan

None — Task 3 executed exactly as written. (Tasks 1 and 2 were the blocking-human checkpoints, discharged by the orchestrator/human in-env before this continuation: Task 1 = rocm suite 75/0 green + wgpu/cuda build + no-rocm-in-CI; Task 2 = ε=1e-4 user sign-off.)

## Self-Check: PASSED

- `.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md` — FOUND (150 lines; "rust cpu path", "wave64", per-family evidence table, ε=1e-4 all present)
- `.planning/REQUIREMENTS.md` — GPU-03 `[x]` + "Complete (Phase 7.6)"; GPU-06 row "Complete (Phase 7.6) — ε=1e-4, vs Rust CPU path"
- Commit a6d65e1 (Task 3) — FOUND
