---
phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor
plan: 05
subsystem: compute-backend-train-oracle
tags: [loss-03, multiquantile, k-independent-quantile-dims, exact-leaf, alpha-vec, per-stage-oracle, phase-close]

# Dependency graph
requires:
  - phase: 06.2-04
    provides: "Wave-2 multilabel N-dim spine: dim-major approx[d*n+i] boosting loop, per-dimension diagonal/scalar leaf path, multi-dim split score, leaf-major cb-model serialization, dim-major->leaf-major / dim-major->object-major oracle transposes, approx_dimension on the Model; D-04 dim=1 byte-identity holding through Wave 2"
  - phase: 06.1-03
    provides: "the scalar Quantile{alpha,delta} der (quantile_der1, error_functions.cpp:453-478) + the Exact weighted-alpha-quantile leaf (exact_leaf_delta, already alpha-general — D-6.1-05), reused VERBATIM per dimension"
provides:
  - "Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 } — K INDEPENDENT Quantile dimensions (D-6.2-05); approx_dimension = alpha.len(); per-object target; fully separable (no softmax coupling)"
  - "cb-backend: compute_multiquantile_gradients — per-dimension der reusing launch_quantile_f64 with alpha[d] (the scalar Quantile launcher), der2 = 0; dispatched in compute_gradients alongside the multiclass arms (per-object target path)"
  - "cb-train: loss_approx_dimension MultiQuantile = alpha.len(); validate_leaf_method gates MultiQuantile to Exact; compute_leaf_deltas gains a dim_index param threading alpha[dim_index] into the UNCHANGED exact_leaf_delta (leaf.rs git-diff-clean)"
  - "cb-compute: Loss::validate MultiQuantile arm (each alpha[k] in [0,1], delta >= 0, non-empty alpha; match self for the non-Copy Vec); the Copy-drop from 06.2-01 pays off — no second cross-crate refactor"
  - "one frozen offline fixture (multiquantile/, catboost 1.2.10, alpha=[0.1,0.5,0.9], leaf_estimation_method:Exact); multiquantile_oracle_test.rs per-stage oracle (Splits/LeafValues/StagedApprox/RAW Predictions) <=1e-5"
  - "RAW per-quantile predictions via the loss-agnostic RawFormulaVal path (predict.rs UNCHANGED — identity, no link transform)"
affects: []  # Phase-close plan — LOSS-03 multi member closed; no downstream plan in 6.2

# Tech tracking
tech-stack:
  added: []  # NO new crate — pure reuse of the 6.1 scalar quantile der + Exact leaf per dimension
  patterns:
    - "P-ADDLOSS instantiated for a SEPARABLE multi-output regression loss with a Vec<f64> param: the der reuses the existing scalar kernel launcher per dimension with alpha[d]; the Exact leaf reuses the existing alpha-general optimizer per dimension with alpha[dim_index]; the ONLY structural change is a dim_index selector on compute_leaf_deltas. No new der/leaf math, no new helper in loss.rs."
    - "MultiQuantile target plumbing: PER-OBJECT target (length n) — every dimension predicts a quantile of the SAME scalar target, so it rides the existing target.len()==n branch and loss_approx_dimension (alpha.len()), NOT the multilabel object-count-from-features / dim-major-target path."

key-files:
  created:
    - "crates/cb-train/tests/multiquantile_oracle_test.rs — MultiQuantile per-stage oracle (Splits/LeafValues/StagedApprox/RAW Predictions) <=1e-5; dim-major->leaf-major + dim-major->object-major transposes; raw per-object target loader"
    - "crates/cb-oracle/fixtures/multiquantile/{model.json,staged.npy,predictions.npy,config.json} — frozen catboost 1.2.10 MultiQuantile fixture (alpha=[0.1,0.5,0.9], Exact leaf)"
  modified:
    - "crates/cb-compute/src/runtime.rs — Loss::MultiQuantile variant + Loss::validate arm; match *self -> match self for the non-Copy Vec"
    - "crates/cb-backend/src/cpu_runtime.rs — compute_multiquantile_gradients + dispatch in compute_gradients; defensive arm in the single-dim scalar path"
    - "crates/cb-backend/src/cpu_runtime_test.rs — 4 unit tests (per-dim der == quantile_der1 + der2==0; dim=1 == scalar Quantile; alpha-dim mismatch error)"
    - "crates/cb-train/src/boosting.rs — loss_approx_dimension MultiQuantile arm; validate_leaf_method Exact gate; compute_leaf_deltas dim_index param threading alpha[dim_index]; autolr_target_type arm"
    - "crates/cb-train/src/boosting_test.rs — 2 leaf-level unit tests (per-dim alpha threading; alpha=[0.7] dim-0 == scalar Quantile{0.7} leaf) + dim_index plumbing"
    - "crates/cb-train/src/metrics.rs — EvalMetric::for_loss MultiQuantile arm"
    - "crates/cb-oracle/generator/gen_fixtures.py — gen_multiquantile + gen_multiquantile_only + --multiquantile-only + main() wiring + MULTIQUANTILE path/MULTIQUANTILE_ALPHAS"
    - "test-literal ripple: 3 cb-train exhaustive Loss-match arms (autolr_e2e_test / slice_first_oracle_test / leaf_methods_oracle_test)"

key-decisions:
  - "MultiQuantile = K INDEPENDENT Quantile dimensions (D-6.2-05): NO new der/leaf math. The der is the scalar quantile gradient kernel (launch_quantile_f64) reused VERBATIM per dimension with that dimension's alpha[d] (compute_multiquantile_gradients), der2 = 0. The leaf is the 6.1 Exact weighted-alpha-quantile optimizer (exact_leaf_delta, already alpha-general — D-6.1-05) reused VERBATIM per dimension with alpha[dim_index]. leaf.rs is git-diff-clean. The only structural change is a dim_index: usize param on compute_leaf_deltas (the per-d outer-loop index) so the Exact arm can select alpha[dim_index]; every other loss ignores it."
  - "The MultiQuantile target stays PER-OBJECT length n (unlike the multilabel losses' DIM-MAJOR target dim*n). Every quantile dimension predicts a quantile of the SAME scalar target, so MultiQuantile rides the existing target.len()==n branch in compute_gradients and loss_approx_dimension(=alpha.len()) in train — it does NOT use the multilabel object-count-from-features / per-dimension-target plumbing. This is the key structural difference from Wave 2 and made the wiring a pure additive arm."
  - "Loss::validate changed from `match *self` to `match self`: the MultiQuantile { alpha: Vec<f64>, .. } variant carries an owned non-Copy Vec, so the place cannot be matched by value. The scalar-parameter arms now bind their f64 fields by reference (default binding modes) and dereference at use. The Copy-drop on Loss in 06.2-01 (surfaced early precisely for this variant) means this is the ONLY refactor MultiQuantile forces — no second cross-crate by-value-Loss ripple."
  - "MultiQuantile predictions are RAW per-quantile (identity — no link transform, D-6.2-05). predict.rs needed NO change: the existing apply_multiclass_prediction RawFormulaVal arm transposes the dim-major approx to object-major (n, dim) regardless of loss/kind, which IS the MultiQuantile prediction. The oracle test reuses it with an arbitrary MultiClassKind (the RawFormulaVal arm ignores kind)."
  - "MultiQuantile gated to LeafMethod::Exact (validate_leaf_method, Pitfall 3): the upstream single-host-CPU default is the useExact override (catboost_options.cpp:289-301) — the weighted alpha[d]-quantile leaf per dimension; der2 = 0 means Newton/Gradient/Simple have no defined optimizer. The fixture pins leaf_estimation_method:Exact + leaf_estimation_iterations:1. Validation: each alpha[k] finite in [0,1], delta finite >= 0, non-empty alpha (T-6.2-03, typed CbError, no panic)."

patterns-established:
  - "Separable multi-output loss with a Vec<f64> param = the scalar der kernel reused per dimension with param[d] + the scalar leaf optimizer reused per dimension with param[dim_index] (a single dim_index selector on the leaf solver); no new numerical surface."

requirements-completed: [LOSS-02, LOSS-03]  # LOSS-02 already closed in 06.2-04 (re-affirmed); LOSS-03 MultiQuantile multi-output member CLOSED here -> LOSS-03 scalar+multi matrix COMPLETE

# Metrics
duration: ~16min
completed: 2026-06-16
---

# Phase 6.2 Plan 05: Wave-3 MultiQuantile (LOSS-03 multi-output) Summary

**Implemented `Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 }` as K INDEPENDENT Quantile dimensions on the Wave-0 N-dim spine (D-6.2-05) — `approx_dimension = alpha.len()`, fully separable with NO softmax coupling and NO new der/leaf math: each dimension `d` reuses the scalar quantile gradient kernel (`launch_quantile_f64`) VERBATIM with its own level `alpha[d]` (`der2 = 0`) and the 6.1 Exact weighted-`alpha[d]`-quantile leaf (`exact_leaf_delta`, already alpha-general — D-6.1-05) VERBATIM, threaded via a new `dim_index` selector on `compute_leaf_deltas` (`leaf.rs` git-diff-clean). The target stays PER-OBJECT (unlike the multilabel dim-major target), predictions are RAW per-quantile (identity — `predict.rs` unchanged), and the loss is gated to the Exact leaf method (Pitfall 3) with each `alpha[k] ∈ [0,1]` / `delta >= 0` validated. One frozen catboost 1.2.10 fixture (`alpha=[0.1,0.5,0.9]`, Exact) drives a per-stage oracle (Splits / LeafValues / StagedApprox / RAW Predictions) `<= 1e-5`, passing on the first run; the full scalar + multiclass + multilabel + MultiQuantile `cb-train --tests` suite stays green — closing LOSS-03 (scalar + multi) and completing Phase 6.2.**

## Performance

- **Duration:** ~16 min
- **Completed:** 2026-06-16
- **Tasks:** 2 `tdd="true"` tasks
- **Commits:** 2 task commits + 1 docs commit

## Accomplishments

- **Task 1 (variant + per-dim der + per-dim Exact leaf threading):** `Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 }` (runtime.rs) + `Loss::validate` arm (each `alpha[k]` finite ∈ `[0,1]`, `delta` finite `>= 0`, non-empty alpha; `match self` for the non-Copy Vec; T-6.2-03). `compute_multiquantile_gradients` (cpu_runtime.rs) reuses `launch_quantile_f64` per dimension with `alpha[d]`, `der2 = 0`, dispatched in `compute_gradients` (per-object target). `loss_approx_dimension` = `alpha.len()`; `validate_leaf_method` gates MultiQuantile to Exact (Pitfall 3); `compute_leaf_deltas` gains a `dim_index` param threading `alpha[dim_index]` into the SAME `exact_leaf_delta` (`leaf.rs` UNCHANGED). 4 backend der unit tests (per-dim der == `quantile_der1` + `der2 == 0`; dim=1 == scalar Quantile; alpha-dim mismatch error) + 2 cb-train leaf unit tests (per-dim alpha threading; `alpha=[0.7]` dim-0 leaf == scalar `Quantile{0.7}` leaf).
- **Task 2 (predict + fixture + oracle + phase close):** RAW per-quantile predictions ride the existing loss-agnostic `RawFormulaVal` transpose (`predict.rs` needed NO change). `gen_multiquantile` (+ `--multiquantile-only`) generates the frozen fixture offline (catboost 1.2.10, `MultiQuantile:alpha=0.1,0.5,0.9`, `leaf_estimation_method:Exact`, `leaf_estimation_iterations:1`, score_function L2, RAW `numeric_tiny` target, thread_count 1). `multiquantile_oracle_test.rs` gates Splits, LeafValues (dim-major→leaf-major transpose), StagedApprox (dim-major→object-major), and RAW Predictions — all `<= 1e-5`, passing first run. REQUIREMENTS (LOSS-03 traceability), ROADMAP (4 success criteria + Wave-3 plan + Phase 6.2 header), and STATE advanced — Phase 6.2 CLOSED.

## Verification — ALL GREEN

- `cargo test -p cb-compute --lib` — **81 passed**.
- `cargo test -p cb-backend` — **25 passed** (incl. the 4 new MultiQuantile der unit tests).
- `cargo test -p cb-train --lib` — **143 passed** (incl. the 2 new MultiQuantile leaf unit tests).
- `cargo test -p cb-train --test multiquantile_oracle_test` — **1 passed**: Splits / LeafValues / StagedApprox / RAW Predictions all `<= 1e-5`, no `#[ignore]`, no relaxed tolerance.
- `cargo test -p cb-train --tests` — **0 failures across all 35 integration test binaries** (the D-04 scalar oracle suite + Wave-1 `multiclass_oracle_test` + Wave-2 `multilabel_oracle_test` + the new `multiquantile_oracle_test` all green; 5 pre-existing `#[ignore]` in overfit/regularization are unrelated D-11 items).
- `cargo test -p cb-model --lib` — **15 passed**; `--tests` 0 failures.
- `crates/cb-compute/src/leaf.rs` — `git diff --stat` empty (the Exact leaf is reused VERBATIM per dimension).

## Task Commits

1. **Task 1: Loss::MultiQuantile variant + per-dim quantile der + per-dim Exact leaf threading** — `c48f6d0` (feat)
2. **Task 2: identity predictions, MultiQuantile fixture, per-stage oracle, phase close** — see final commit list (feat)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] non-exhaustive `match Loss` after adding `Loss::MultiQuantile`**
- **Found during:** Task 1 (adding the variant).
- **Issue:** 2 production sites (`metrics.rs::for_loss`, `boosting.rs::autolr_target_type`) and 3 cb-train test files (`autolr_e2e_test`, `slice_first_oracle_test`, `leaf_methods_oracle_test`) match exhaustively on `Loss`, so the new variant broke compilation.
- **Fix:** Added `Loss::MultiQuantile { .. }` arms (RMSE eval surface / Unknown auto-LR target / regression-target — none exercised by the scalar paths; the test arms only satisfy exhaustiveness). Also changed `Loss::validate` from `match *self` to `match self` (the non-Copy Vec cannot be matched by value).
- **Files:** `crates/cb-compute/src/runtime.rs`, `crates/cb-train/src/{metrics.rs,boosting.rs}`, `crates/cb-train/tests/{autolr_e2e_test,slice_first_oracle_test,leaf_methods_oracle_test}.rs`.
- **Committed in:** `c48f6d0`.

### Plan-action clarification (not a deviation)

- **`predict.rs` needed NO change.** The plan's Task-2 action says "ensure MultiQuantile predictions are the RAW per-quantile approx (identity — reuse `RawFormulaVal`)". The existing `apply_multiclass_prediction` `RawFormulaVal` arm already transposes the dim-major approx to object-major `(n, dim)` independently of loss/kind, which IS the MultiQuantile prediction. No new arm or `MultiClassKind` was required; the oracle test calls it with an arbitrary kind (ignored by the RawFormulaVal arm). This is the intended D-6.2-05 "identity, no link transform" behavior, achieved by reuse.

**Total deviations:** 1 auto-fixed Rule-3 blocking ripple (variant exhaustiveness). No fixture-design gotcha this wave (the per-object target has no cross-feature split-score tie, unlike Wave 2). No scope creep, no architectural Rule-4 escalation.

## Known Stubs

None. The variant, the per-dim der (reusing the scalar quantile kernel with `alpha[d]`), the per-dim Exact leaf threading (`alpha[dim_index]` into the unchanged `exact_leaf_delta`), the RAW predictions, and the frozen fixture are all fully wired and oracle-locked.

## Threat Flags

None — no new network/auth/file surface. The MultiQuantile params are guarded by `Loss::validate` (each `alpha[k] ∈ [0,1]`, `delta >= 0`, non-empty alpha; T-6.2-03, typed `CbError`, no panic); the backend rejects an `alpha.len() != approx_dimension` mismatch with a typed `CbError` (no panic). The der/leaf are the oracle-locked 6.1 `quantile_der1` / `exact_leaf_delta` reused verbatim (T-6.2-03b accept — no new numerical surface; `der2 = 0` so no division). No new dependency (T-6.2-SC no-op).

## Issues Encountered

- gsd-tools CLI absent (as in 06.2-01..04) — STATE.md / ROADMAP.md / REQUIREMENTS.md updated MANUALLY.

## Next Phase Readiness

- **LOSS-03 (scalar + multi) is CLOSED**: the 6.1 scalar regression matrix + this MultiQuantile multi-output member. **Phase 6.2 is COMPLETE** — all 5 plans shipped, all 4 ROADMAP success criteria met (N-dim refactor + D-04 re-lock + MultiClass/OneVsAll/MultiLogloss/MultiCrossEntropy + MultiQuantile, each per-stage oracle `<= 1e-5`).
- The MultiQuantile pattern (separable Vec-param loss = scalar der kernel per dim + scalar leaf optimizer per dim with a `dim_index` selector) is a reusable template for future per-dimension-parametric multi-output losses.
- No blockers. NEXT: `/gsd-transition` or `/gsd-plan-phase 6.3` (ranking losses & metrics).

## Self-Check: PASSED

- All created files present on disk (`multiquantile/` fixture with model.json+staged.npy+predictions.npy+config.json; `multiquantile_oracle_test.rs`).
- Task-1 commit present in git history (`c48f6d0`); Task-2 + docs commits recorded below.
- Gates green: cb-compute 81, cb-backend 25, cb-train --lib 143, cb-train --test multiquantile_oracle_test 1, cb-train --tests 0 failures (D-04 scalar + Wave-1 multiclass + Wave-2 multilabel + new multiquantile), cb-model lib 15 + --tests 0 failures.
- `leaf.rs` git-diff-clean (Exact leaf reused verbatim per dimension — D-6.1-05).
- No new crate dependency (pure reuse of the 6.1 scalar quantile der + Exact leaf; T-6.2-SC no-op).
- No `unwrap()` in the new production code (checked-index per-dimension gather, typed `CbError` validation).

---
*Phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor*
*Completed: 2026-06-16*
