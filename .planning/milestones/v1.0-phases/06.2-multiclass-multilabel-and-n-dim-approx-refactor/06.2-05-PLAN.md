---
phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor
plan: 05
type: execute
wave: 3
depends_on: ["06.2-04"]
files_modified:
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-backend/src/cpu_runtime.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-model/src/predict.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/fixtures/multiquantile
  - crates/cb-train/tests/multiquantile_oracle_test.rs
autonomous: true
requirements: [LOSS-02, LOSS-03]
must_haves:
  truths:
    - "MultiQuantile = K independent Quantile dimensions: each dimension d uses the scalar quantile der with alpha[d] and the Exact weighted-alpha-quantile leaf path (exact_leaf_delta, reused verbatim per dimension)"
    - "approx_dimension = alpha.len(); the per-quantile alpha values are a Vec<f64> param on Loss::MultiQuantile (Loss::Variant{params} pattern)"
    - "MultiQuantile passes per-stage oracle <=1e-5: splits, leaf values, staged N-dim approx, and identity predictions vs upstream catboost 1.2.10; leaf_estimation_method:Exact pinned"
    - "alpha[k] in [0,1] and delta >= 0 are validated; the Loss is non-Copy now that a Vec<f64>-carrying variant exists (the 06.2-01 Copy-drop pays off — no second refactor)"
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 } variant + per-alpha validation"
      contains: "MultiQuantile"
    - path: "crates/cb-train/tests/multiquantile_oracle_test.rs"
      provides: "MultiQuantile per-stage oracle <=1e-5 (K independent Exact quantile dims)"
      min_lines: 40
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "exact_leaf_delta"
      via: "compute_leaf_deltas Exact arm threads alpha[d] per dimension"
      pattern: "MultiQuantile|exact_leaf_delta|alpha"
---

<objective>
Wave 3: implement **MultiQuantile** (LOSS-03 multi-output member, relocated from 6.1) on the N-dim foundation — closing the LOSS-03 scalar+multi matrix. Per D-6.2-05, MultiQuantile = **K independent Quantile dimensions**: it reuses the scalar `Quantile` der (`error_functions.cpp:453-478`) AND the 6.1 Exact weighted-α-quantile leaf path (`exact_leaf_delta`, already α-general — D-6.1-05) **verbatim per dimension**, with no softmax coupling. The per-quantile `alpha` values are a `Vec<f64>` parameter on the loss variant (the `Loss::Variant { params }` pattern, D-6.1-03). `approx_dimension` = `alpha.len()`.

This is the variant that forced the `Copy` drop on `Loss` in Plan 06.2-01 — that early ripple now pays off (no second cross-crate refactor). The leaf method is **Exact** (the single-host CPU default for MultiQuantile, `catboost_options.cpp:289-301` — Pitfall 3), `der2 = 0` per dimension.

Each stage passes its oracle ≤1e-5 vs upstream catboost 1.2.10 (`thread_count=1`). MultiQuantile predictions are RAW (identity transform — no link).

Entry condition: Plan 06.2-04 complete (Wave 2 multilabel oracles green; D-04 still holding).
Output: `Loss::MultiQuantile { alpha: Vec<f64>, delta }`, the per-dim quantile der + per-dim Exact leaf threading, identity predictions, one frozen offline fixture, the per-stage oracle, and the final REQUIREMENTS/ROADMAP/STATE updates closing Phase 6.2.

NOTE: gsd-tools CLI ABSENT — STATE/ROADMAP/REQUIREMENTS updates MANUAL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-CONTEXT.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-RESEARCH.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-PATTERNS.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-VALIDATION.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-04-SUMMARY.md
@.planning/phases/06.1-regression-loss-matrix/06.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loss::MultiQuantile variant + per-dim quantile der + per-dim Exact leaf threading</name>
  <read_first>
    - 06.2-RESEARCH.md Code Examples "MultiQuantile der (K independent quantile dims — separable)" (error_functions.cpp:453-478; der[idx] = |arg|<delta ? 0 : weight*(arg>0 ? alpha[idx] : -(1-alpha[idx])); der2 = 0) ; "approx_dimension derivation" (approx_dimension.cpp:17-19 = alpha.size())
    - 06.2-RESEARCH.md Pitfall 3 (MultiQuantile leaf method = Exact on single-host CPU, NOT Gradient; der2=0)
    - 06.2-PATTERNS.md § runtime.rs (Loss::Variant{params}; MultiQuantile carries alpha: Vec<f64>; the non-Copy note — already resolved in 06.2-01; Quantile validate arm precedent at runtime.rs:223), § loss.rs (quantile_der1 reused verbatim per dim, loss.rs:166), § leaf.rs (exact_leaf_delta reused verbatim, leaf.rs:165), § boosting.rs::compute_leaf_deltas (Exact arm already threads (alpha,delta) — boosting.rs:773; MultiQuantile passes alpha[d] per dim) + validate_leaf_method (add MultiQuantile to the Exact-eligible set, boosting.rs:223-ish)
    - crates/cb-compute/src/runtime.rs:85 (Quantile variant), :184/:223 (validate) ; crates/cb-compute/src/loss.rs:166 (quantile_der1) ; crates/cb-compute/src/leaf.rs:165 (exact_leaf_delta) ; crates/cb-train/src/boosting.rs:692 (validate_leaf_method), :730/:773 (compute_leaf_deltas Exact arm with the (quantile_alpha, quantile_delta) match)
    - crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs (the scalar Quantile oracle — the per-dim degenerate reference)
  </read_first>
  <behavior>
    - For each quantile dimension idx: der[idx*n+i] = (|target_i - approx[idx*n+i]| < delta) ? 0 : weight*(arg>0 ? alpha[idx] : -(1-alpha[idx])); der2 = 0 (error_functions.cpp:453-478) — i.e. the scalar quantile_der1 applied per dimension with alpha[idx].
    - approx_dimension = alpha.len(); each dimension is an independent quantile (no coupling).
    - The Exact leaf path (exact_leaf_delta) is reused verbatim per dimension, threading alpha[d] (the weighted alpha[d]-quantile of that dimension's leaf residuals); leaf.rs UNCHANGED (D-6.1-05 reuse).
    - At alpha.len()==1 with alpha[0]=0.7, MultiQuantile reduces to the scalar Quantile{0.7} path (the same leaf values / staged approx as the 6.1 wave3 fixture) — a degenerate-equivalence anchor.
    - alpha[k] in [0,1] and delta >= 0 validated (typed CbError); MultiQuantile gated to the Exact leaf method.
  </behavior>
  <action>
    In `crates/cb-compute/src/runtime.rs`: add `Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 }` (the `Loss::Variant { params }` pattern; `Vec<f64>` is now fine — `Loss` is non-Copy since 06.2-01). Add a `Loss::validate` arm validating each `alpha[k]` finite and ∈ `[0,1]` and `delta` finite and `>= 0` (clone the Quantile arm at `runtime.rs:223`; typed `CbError::OutOfRange`, no panic). In `crates/cb-compute/src/loss.rs`: MultiQuantile reuses `quantile_der1` (`loss.rs:166`) per dimension — add a thin `multiquantile` dispatch looping `for idx in 0..alpha.len()` calling `quantile_der1(approx[idx*n+i], target_i, alpha[idx], delta)`, `der2 = 0` (RESEARCH Pattern 2 — no new der math). In `crates/cb-backend/src/cpu_runtime.rs`: add the `MultiQuantile` arm looping per-dimension over dim-major slices reusing the existing quantile kernel launcher (the `der2 = vec![0.0; …]` precedent at `:504`). In `crates/cb-train/src/boosting.rs`: derive `approx_dimension = alpha.len()`; in `compute_leaf_deltas` (`:730`) the Exact arm (`:773`) already threads `(quantile_alpha, quantile_delta)` — extend its `match loss` to supply `alpha[d]` per dimension into the SAME `exact_leaf_delta` (`leaf.rs` UNCHANGED). Extend `validate_leaf_method` (`:692`) to add `Loss::MultiQuantile { .. }` to the Exact-eligible set. All accumulation via `cb_core::sum_f64` (D-08). No `unwrap()` in production.
  </action>
  <verify>
    <automated>cargo test -p cb-compute --lib && cargo test -p cb-train --lib</automated>
  </verify>
  <acceptance_criteria>
    - `Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 }` exists (grep `crates/cb-compute/src/runtime.rs`); `crates/cb-compute/src/leaf.rs` is UNCHANGED vs HEAD (`git diff --stat crates/cb-compute/src/leaf.rs` shows no change — the Exact leaf is reused verbatim).
    - `cargo test -p cb-compute --lib` exits 0 including a unit test asserting the per-dim MultiQuantile der equals `quantile_der1` applied per dimension, and `der2 == 0`.
    - `cargo test -p cb-train --lib` exits 0; a unit/integration assertion that MultiQuantile with `alpha=[0.7]` produces the same leaf values as scalar `Quantile{0.7}` (the degenerate-equivalence anchor).
  </acceptance_criteria>
  <done>Loss::MultiQuantile variant + per-dim quantile der + per-dim Exact leaf threading land (leaf.rs unchanged); lib tests green; alpha=[0.7] reduces to scalar Quantile{0.7}.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identity predictions, offline MultiQuantile fixture, per-stage oracle, and close Phase 6.2</name>
  <read_first>
    - crates/cb-model/src/predict.rs:70 (apply_prediction_type), :73 (RawFormulaVal identity) — MultiQuantile predictions are RAW (no link transform)
    - 06.2-PATTERNS.md § predict.rs (identity for MultiQuantile) + § gen_fixtures.py (per-loss generator; pin leaf_estimation_method:Exact + the isolating params) + § oracle tests (clone wave3_quantile_loss_oracle_test.rs — the RAW-staged, no-Predictions-stage shape; here Predictions == raw per-quantile)
    - crates/cb-oracle/generator/gen_fixtures.py (the quantile generator that produced quantile_alpha07) ; crates/cb-train/tests/multilabel_oracle_test.rs (Wave-2 N-dim oracle clone with the staged transpose already handled)
    - 06.2-RESEARCH.md A4 (staged transpose) ; § Validation Architecture (per-stage oracle map, MultiQuantile row)
    - .planning/REQUIREMENTS.md (LOSS-02 row line 52, LOSS-03 traceability line 169 — to mark complete) ; .planning/ROADMAP.md § Phase 6.2 (Plans list + success criteria to check off)
  </read_first>
  <behavior>
    - MultiQuantile Probability/RawFormulaVal prediction = the raw per-quantile approx (identity; no link transform), shape (n, K).
    - One frozen offline fixture (multiquantile) from catboost 1.2.10 (.venv, thread_count=1, leaf_estimation_method:Exact, the isolating params, a K=2 or 3 alpha vector) drives a per-stage oracle: Splits, LeafValues, StagedApprox (N-dim), Predictions all <=1e-5.
    - The full scalar + multiclass + multilabel oracle suites stay green (D-04 holds through Wave 3 — the final wave).
    - REQUIREMENTS.md LOSS-02 + LOSS-03 multi rows and ROADMAP.md Phase 6.2 success criteria are marked complete; STATE.md advanced.
  </behavior>
  <action>
    In `crates/cb-model/src/predict.rs`: ensure MultiQuantile predictions are the RAW per-quantile approx (identity — reuse `RawFormulaVal` at `:73` over the N-dim buffer; NO softmax/sigmoid). Keep dim=1 byte-identical. In `crates/cb-oracle/generator/gen_fixtures.py`: add `gen_multiquantile` (clone the existing quantile generator) training catboost 1.2.10 with `loss_function='MultiQuantile:alpha=<a0>,<a1>,...'` (e.g. `alpha=0.3,0.7` or `0.1,0.5,0.9`), `leaf_estimation_method:'Exact'` (Pitfall 3), `boosting_type:'Plain'`, `bootstrap_type:'No'`, `random_strength:0`, `thread_count:1`, fixed seed, depth 2, 5 iterations, lr 0.1, trained on the RAW `numeric_tiny` target (full real line — the 6.1 quantile fixture precedent). Save `model.json` (splits + LEAF-MAJOR leaf_values), `staged.npy` (`staged_predict(RawFormulaVal)`, record layout per A4), `predictions.npy` (raw per-quantile). Run OFFLINE via `.venv/bin/python` and COMMIT under `crates/cb-oracle/fixtures/multiquantile/`.

    Create `crates/cb-train/tests/multiquantile_oracle_test.rs` (separate file — source/test separation MANDATORY; clone `multilabel_oracle_test.rs`): train via `train` under `Loss::MultiQuantile{alpha, delta}`, `compare_stage(Stage::Splits | LeafValues | StagedApprox)` vs the fixture `<=1e-5`, plus an assertion that the raw predictions match `predictions.npy` `<=1e-5`. Handle the staged transpose (A4). NO `#[ignore]`, NO weakened tolerance.

    Finally, close Phase 6.2 MANUALLY (gsd-tools absent): mark `LOSS-02` complete and the `LOSS-03` MultiQuantile portion complete in `.planning/REQUIREMENTS.md`; check off the four Phase 6.2 success criteria + fill the Plans list in `.planning/ROADMAP.md`; advance `.planning/STATE.md` (position → Phase 6.2 complete, log the decisions). Run the full per-stage oracle suite one final time as the phase gate.
  </action>
  <verify>
    <automated>cargo test -p cb-model --lib && cargo test -p cb-train --test multiquantile_oracle_test && cargo test -p cb-train --tests</automated>
  </verify>
  <acceptance_criteria>
    - The fixture `crates/cb-oracle/fixtures/multiquantile/` exists with `model.json` + `staged.npy` + `predictions.npy` (generated offline via catboost 1.2.10).
    - `cargo test -p cb-train --test multiquantile_oracle_test` exits 0 — Splits, LeafValues, StagedApprox, and raw Predictions all `<=1e-5` (no `#[ignore]`, no relaxed tolerance).
    - `cargo test -p cb-train --tests` exits 0 — the FULL scalar + multiclass + multilabel + MultiQuantile oracle suite is green (D-04 held through every wave; the Phase 6.2 gate).
    - `.planning/REQUIREMENTS.md` shows LOSS-02 and LOSS-03-MultiQuantile marked complete; `.planning/ROADMAP.md` Phase 6.2 success criteria checked and Plans list filled.
  </acceptance_criteria>
  <done>MultiQuantile identity predictions + frozen fixture + per-stage oracle land <=1e-5; the full phase oracle suite is green; REQUIREMENTS/ROADMAP/STATE closed — Phase 6.2 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MultiQuantile params → training | `alpha: Vec<f64>` and `delta` are caller-supplied loss params; out-of-range values are the input-validation surface (V5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.2-03 | Denial of Service / bad model | `Loss::validate` MultiQuantile | mitigate | Validate each `alpha[k]` finite ∈ `[0,1]` and `delta` finite `>= 0` before training (clone the Quantile arm); typed `CbError::OutOfRange` (no panic, no `unwrap`/`indexing_slicing` in production); checked indexing on the dim-major buffer keyed by `alpha.len()`. |
| T-6.2-03b | Tampering | per-dim quantile der / Exact leaf | accept | The der is the oracle-locked 6.1 `quantile_der1` and `exact_leaf_delta` reused verbatim per dimension (leaf.rs unchanged) — no new numerical surface; der2=0 (no division). |
| T-6.2-SC | Tampering | cargo install | accept | No package installs (pure host reuse of 6.1 quantile der + Exact leaf; RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- Per-crate runs (disk pressure): `cargo test -p cb-compute --lib`, `-p cb-model --lib`, `-p cb-train --test multiquantile_oracle_test`, `-p cb-train --tests` (final phase gate).
- Oracle floor (D-6.2-06): Python-reachable per-stage ≤1e-5, NO C++ instrumentation.
- D-08: all new accumulation via `cb-core::sum_f64`; `crates/cb-compute/src/leaf.rs` git-diff-clean (Exact leaf reused verbatim).
</verification>

<success_criteria>
- `Loss::MultiQuantile { alpha: Vec<f64>, delta }` implemented as K independent Quantile dimensions reusing the scalar quantile der + the Exact weighted-α-quantile leaf per dimension (leaf.rs unchanged).
- MultiQuantile passes per-stage oracle ≤1e-5 (splits/leaves/staged/raw predictions); `leaf_estimation_method:Exact` pinned.
- `alpha=[0.7]` reduces to scalar `Quantile{0.7}` (degenerate-equivalence anchor).
- The full scalar + multiclass + multilabel + MultiQuantile oracle suite is green (D-04 held through every wave).
- Phase 6.2 closed: REQUIREMENTS (LOSS-02 + LOSS-03 multi), ROADMAP success criteria, STATE advanced.
</success_criteria>

<output>
Create `.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-05-SUMMARY.md` when done.
</output>
