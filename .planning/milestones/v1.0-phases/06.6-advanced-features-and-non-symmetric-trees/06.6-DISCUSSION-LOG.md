# Phase 6.6: Advanced Features & Non-Symmetric Trees - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 6.6-advanced-features-and-non-symmetric-trees
**Areas discussed:** Sequencing & risk gate, Grow-policy engine scope, Advanced fstr depth & oracle, Monotone + penalty mechanics

---

## Sequencing & risk gate

| Option | Description | Selected |
|--------|-------------|----------|
| Symmetric features first, then engine gate | Ship monotone + penalties (ride existing symmetric grower) first; non-symmetric engine as its own multi-wave gate; advanced fstr + feature selection last | ✓ |
| Non-symmetric engine first gate | Make the second tree engine the first gate (everything blocks on it), like 6.5 tokenizer-first | |
| fstr/importance first | Land advanced fstr + LossFunctionChange first since FEAT-05 depends on importance | |

**User's choice:** Symmetric features first, then engine gate

| Option | Description | Selected |
|--------|-------------|----------|
| Block on fstr completion | FEAT-05 is LAST — consumes PVC/LossFunctionChange/Shap importance, can't be oracle-locked until those exist | ✓ |
| Parallel with stubbed importance | Start recursive-elimination loop early against existing PVC/Interaction, wire LossFunctionChange/Shap later | |
| You decide | Let planner derive ordering from dependency graph | |

**User's choice:** Block on fstr completion
**Notes:** Resulting order — symmetric features → non-symmetric engine gate → advanced fstr → FEAT-05 (last).

---

## Grow-policy engine scope

| Option | Description | Selected |
|--------|-------------|----------|
| Unified leaf-wise engine, policy-dispatched | One non-symmetric grower (mirrors leafwise_scoring.cpp) with queue/expansion parameterized by policy | ✓ |
| Separate grower per policy | Three distinct growth functions; more code, higher divergence risk | |
| You decide | Let researcher/planner pick after reading leafwise_scoring.cpp | |

**User's choice:** Unified leaf-wise engine, policy-dispatched

| Option | Description | Selected |
|--------|-------------|----------|
| Parallel variant, oblivious path untouched | Non-symmetric tree as separate TreeVariant reusing existing TNonSymmetricTree bindings; symmetric path byte-identical | ✓ |
| Unify into one traversal | Refactor oblivious into general node-traversal; cleaner but risks regressing locked oblivious oracles | |
| You decide | Defer in-memory representation to planner | |

**User's choice:** Parallel variant, oblivious path untouched

---

## Advanced fstr depth & oracle

| Option | Description | Selected |
|--------|-------------|----------|
| Seed-match strict, instrument if needed | Treat SAGE like 6.3 randomized losses — reproduce RNG seeding via instrumented trainer for ≤1e-5 | ✓ |
| Structural/weakened tolerance w/ documented gap | Fall back to statistical/structural oracle with documented weakened tolerance (6.4 D-6.4-06 precedent) | |
| Defer SAGE if not reachable in 1.2.10 | Defer if EFstrType::SageValues not exposed in Python API; confirm reachability in research | |

**User's choice:** Seed-match strict, instrument if needed
**Notes:** Fallbacks ordered in CONTEXT D-6.6-11 — (a) seed-match, (b) documented-weakened, (c) defer; never fabricate. Research must confirm SageValues reachability first.

| Option | Description | Selected |
|--------|-------------|----------|
| Extend to non-symmetric, oracle ≥1 case | Generalize TreeSHAP/fstr recursion to non-symmetric trees; oracle-lock ≥1 non-symmetric case | ✓ |
| Oblivious-only fstr this phase | All advanced fstr on oblivious only; non-symmetric fstr deferred (leaves parity gap) | |
| You decide | Let research determine feasibility first | |

**User's choice:** Extend to non-symmetric, oracle ≥1 case

---

## Monotone + penalty mechanics

| Option | Description | Selected |
|--------|-------------|----------|
| Symmetric (oblivious) only, verify upstream support | Monotone on existing oblivious grower only; note non-symmetric monotone deferred if upstream supports it | |
| Both symmetric and non-symmetric | Implement monotone for both engines; only justified if upstream supports monotone under Lossguide/Depthwise/Region | ✓ |
| You decide | Let research determine supported combinations first | |

**User's choice:** Both symmetric and non-symmetric
**Notes:** CAVEAT flagged to user — research MUST confirm upstream actually supports monotone + non-symmetric; if not, escalate-don't-weaken (no fabricated fixture), scope to supported engines. Non-symmetric monotone depends on the engine gate landing first.

| Option | Description | Selected |
|--------|-------------|----------|
| Python-reachable strict, generated fixtures | Penalty params are public in 1.2.10 — generate fixtures, oracle-lock ≤1e-5 against Python (D-06); instrument only if internals not reachable | ✓ |
| Instrument-first | Go straight to instrumented trainer for penalty internals | |
| You decide | Defer oracle-sourcing split to research | |

**User's choice:** Python-reachable strict, generated fixtures

---

## Claude's Discretion

- Wave/plan granularity within each gate (sequencing principles fixed; granularity flexible).
- Exact in-memory shape of the non-symmetric tree variant (enum vs parallel array).
- Recursive-feature-elimination loop structure and per-importance-backend plan splits.
- Whether monotone-symmetric and penalties are one plan/wave or split.

## Deferred Ideas

- GPU kernels for grow policies / monotone / penalties / fstr → Phase 7 (CubeCL).
- Python/PyO3 surface for these params + fstr types → Phase 8.
- SAGE → deferred only if EFstrType::SageValues unreachable in 1.2.10.
- Non-symmetric monotone → deferred only for engine/policy combinations upstream doesn't support.
