---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 05
type: execute
wave: 5
depends_on: ["06.6-03", "06.6-04"]
files_modified:
  - crates/cb-model/src/apply.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-model/tests/non_symmetric_oracle_test.rs
autonomous: true
requirements: [FEAT-06]
must_haves:
  truths:
    - "A non-symmetric (Depthwise/Lossguide) model applies via the pointer-walk traversal and predicts ≤1e-5 vs catboost 1.2.10 (full per-stage gate: LeafValues, StagedApprox, Predictions)."
    - "The trained non-symmetric model round-trips through .cbm AND model.json and re-applies ≤1e-5 (D-6.6-02 — round-trip is PART of the gate)."
    - "The oblivious apply path is BYTE-IDENTICAL (gated on TreeVariant — D-6.6-05)."
    - "The pointer-walk uses i64 arithmetic with bounds-checked node access and validates index<node_count on load (no panic on crafted input)."
  artifacts:
    - path: "crates/cb-model/src/apply.rs"
      provides: "leaf_index_nonsym() pointer-walk traversal gated on TreeVariant"
      contains: "leaf_index_nonsym"
    - path: "crates/cb-model/tests/non_symmetric_oracle_test.rs"
      provides: "full per-stage + .cbm/json round-trip oracle ≤1e-5 (now green)"
  key_links:
    - from: "crates/cb-model/src/apply.rs"
      to: "crates/cb-model/src/model.rs"
      via: "predict_raw_one branches on TreeVariant: Oblivious→existing leaf_index_for | NonSymmetric→leaf_index_nonsym"
      pattern: "TreeVariant::NonSymmetric|leaf_index_nonsym"
---

<objective>
Gate-B close (FEAT-06, D-6.6-02): implement the non-symmetric APPLY (pointer-walk traversal) + non-symmetric leaf VALUES, then turn the full `non_symmetric_oracle_test` green — per-stage ≤1e-5 AND `.cbm`/json round-trip (the round-trip is PART of the gate, not a follow-on). The oblivious apply path stays byte-identical, gated on `TreeVariant` (D-6.6-05).

Purpose: Complete the non-symmetric engine so train→apply→cbm/json round-trip is oracle-locked ≤1e-5 (SC-3). The apply is a simple flat-node pointer walk: `index = TreeStartOffsets[t]; loop { diff = (val>=splitIdx)?RightSubtreeDiff:LeftSubtreeDiff; index += diff; if diff==0 break } leaf = NonSymmetricNodeIdToLeafId[index]` (`evaluator_impl.cpp:726-742`). Leaf VALUES use the same fold machinery as oblivious (Open Question 1 — escalate to instrumented trainer ONLY if the draw stream diverges; splits were locked in 06.6-04).
Output: `leaf_index_nonsym()` in `apply.rs` gated on `TreeVariant`; non-symmetric leaf-value estimation wired in `boosting.rs`; the full per-stage + round-trip oracle green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- `apply.rs`: `leaf_index_nonsym(...)` pointer-walk traversal; `predict_raw_one`/`predict_raw_multi_cat` branch on `TreeVariant` (oblivious arm = existing `leaf_index_for`, byte-identical).
- `boosting.rs`: non-symmetric leaf-value estimation on the fold machinery (Gradient/Newton per loss), producing the `leaf_values`/`leaf_weights` for the node graph.
- `non_symmetric_oracle_test.rs`: full per-stage gate + `.cbm`/json round-trip now GREEN.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Non-symmetric pointer-walk apply gated on TreeVariant + non-symmetric leaf-value estimation</name>
  <files>crates/cb-model/src/apply.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-model/src/apply.rs:196-239 (`passes_split` exhaustive match 196-201; `leaf_index_for` 208-215 forward-bit; `predict_raw_one` 225-239 per-tree accumulation + `bias` once + Σ via `sum_f64`)
    - catboost-master/catboost/libs/model/cpu/evaluator_impl.cpp:710-756 (scalar `CalcNonSymmetricTrees` pointer walk: `index=TreeStartOffsets[t]; loop{ step=stepNodes[index]; diff=(featureVal>=splitIdx)?RightSubtreeDiff:LeftSubtreeDiff; index+=diff; if diff==0 break } leaf=NodeIdToLeafId[index]`; TerminalMarker==0)
    - catboost-master/catboost/libs/model/tree_to_string.cpp (non-symmetric step-node traversal reference)
    - crates/cb-train/src/boosting.rs (the leaf-estimation site producing leaf_values/leaf_weights for the grown tree — the non-symmetric node graph needs per-leaf delta estimation analogous to the oblivious path; reuse the existing Gradient/Newton/Exact leaf machinery keyed by the node→leaf partition)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Pattern 1 pointer-walk; Open Question 1 — leaf-value RNG/draw-order parity, escalate to instrumented trainer only if diverges; Pitfall 5 note re: leaf weights attach via node_id_to_leaf_id)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md (apply.rs analog — `leaf_index_nonsym` skeleton, checked `.get`, i64 arithmetic, validate index<node_count)
  </read_first>
  <action>
    In apply.rs add `fn leaf_index_nonsym(model, tree: &NonSymmetricTree, features, cat_values) -> usize` transcribing the pointer walk (evaluator_impl.cpp:726-742): start `index = 0` for the tree's local node array (per-tree node arrays, not a global offset, unless the model stores a global TreeStartOffsets — match whatever 06.6-03 decoded), loop reading `step_nodes[index]` and the split at that node, `diff = if passes_split(...) { right_subtree_diff } else { left_subtree_diff }`, `index = (index as i64 + diff as i64) as usize`, break when `diff == 0` (TerminalMarker), then `leaf = node_id_to_leaf_id[index]`. ALL access via `.get(...)` (no `indexing_slicing`); `i64` arithmetic; validate `index < node_count` (reject malformed graphs — Security). Reuse the existing `passes_split` (apply.rs:196-201) for the per-node split test verbatim.

    Branch `predict_raw_one` (and the multiclass `predict_raw_multi_cat`) per-tree accumulation on `TreeVariant`: `Oblivious(_)` → the EXISTING `leaf_index_for` call verbatim (byte-identical, D-6.6-05); `NonSymmetric(t)` → `leaf_index_nonsym(...)` then read `leaf_values[leaf]`. `bias` is still added exactly once via the seeded `out`; Σ over trees via `sum_f64` (D-08).

    In boosting.rs estimate non-symmetric leaf VALUES on the same fold machinery as oblivious: after `leaf_wise_grower` (06.6-04) produces the structure, compute per-leaf deltas using the existing Gradient/Newton/Exact leaf functions keyed by the node→leaf partition (`node_id_to_leaf_id`). Reuse the existing leaf-estimation code path; do NOT fork the leaf formula (Open Question 1: the leaf-wise grower shares the fold machinery). If the leaf-value draw stream diverges from upstream, ESCALATE to the persistent instrumented trainer (`/tmp/cb_build313` + clang-18) per Open Question 1 — never weaken tolerance or `#[ignore]`. All sums via `cb_core::sum_f64`. NO embedded tests.
  </action>
  <verify>
    <automated>cargo test -p cb-model --lib 2>&1 | tail -6; cargo test -p cb-train --lib 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-model` and `-p cb-train` succeed; `leaf_index_nonsym` exists with checked `.get`/i64 arithmetic/`index<node_count` validation.
    - `predict_raw_one`/`predict_raw_multi_cat` branch on `TreeVariant`; the oblivious arm is the literal existing `leaf_index_for` call.
    - Non-symmetric leaf-value estimation reuses the existing Gradient/Newton/Exact machinery (no forked leaf formula).
    - All existing cb-model + cb-train lib + oblivious oracle tests stay green (D-6.6-05).
    - No `mod tests`/`#[cfg(test)]` added to apply.rs/boosting.rs.
  </acceptance_criteria>
  <done>Non-symmetric pointer-walk apply + leaf-value estimation land, gated on TreeVariant; oblivious apply byte-identical; cb-model/cb-train lib + oblivious oracles green.</done>
</task>

<task type="auto">
  <name>Task 2: Full non-symmetric per-stage + .cbm/json round-trip oracle GREEN ≤1e-5</name>
  <files>crates/cb-model/tests/non_symmetric_oracle_test.rs</files>
  <read_first>
    - crates/cb-model/tests/non_symmetric_oracle_test.rs (the splits-first harness from 06.6-03; SPLITS green from 06.6-04 — now turn LeafValues/StagedApprox/Predictions + round-trip green)
    - crates/cb-model/tests/apply_oracle_test.rs (round-trip pattern: save_cbm/load_cbm + save_json/load_json then re-predict and compare)
    - crates/cb-oracle/src/{fixture,compare}.rs (Stage enum + compare_stage)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (D-6.6-02 — round-trip is PART of the gate; `.cbm` round-trips bit-exact, verified max-abs-diff 0.0)
  </read_first>
  <action>
    Complete the Depthwise + Lossguide tests in `non_symmetric_oracle_test.rs`: assert the FULL per-stage gate (Splits | LeafValues | StagedApprox | Predictions) ≤1e-5 via `compare_stage` against the committed catboost 1.2.10 fixtures, then a round-trip: `save_cbm`→`load_cbm` re-predict equality (bit-exact / ≤1e-5) AND `save_json`→`load_json` (via the `"trees"` parser from 06.6-03) re-predict equality. Both Depthwise and Lossguide must pass. None `#[ignore]`d, no tolerance weakened, no fabricated fixture.

    If leaf VALUES diverge while SPLITS pass (Open Question 1 draw-stream divergence), do the instrumented-trainer escalation in Task 1 — this task only turns the test green once the math matches. Record any escalation in the SUMMARY. Region + non-symmetric-monotone remain escalated gaps (no test added; documented).
  </action>
  <verify>
    <automated>cargo test -p cb-model --test non_symmetric_oracle_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-model --test non_symmetric_oracle_test` passes ALL tests (Depthwise + Lossguide), none `#[ignore]`d.
    - Full per-stage gate ≤1e-5 vs catboost 1.2.10; `.cbm` AND model.json round-trip re-predict ≤1e-5.
    - No tolerance weakening anywhere; fixtures are real catboost 1.2.10 output.
  </acceptance_criteria>
  <done>The non-symmetric engine (Depthwise + Lossguide) is oracle-locked end-to-end: train→apply→cbm/json round-trip ≤1e-5 vs catboost 1.2.10. FEAT-06 / SC-3 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.cbm`/JSON model file → non-symmetric apply | The pointer-walk consumes `step_nodes`/`node_id_to_leaf_id` indices loaded from a model file; a crafted file must not cause OOB, panic, infinite loop, or unbounded work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-12 | Denial of Service | `leaf_index_nonsym` pointer walk | mitigate | `i64` arithmetic; `.get(...)` checked node access; validate `index < node_count`; bound the loop (reject `diff` that revisits/escapes) — no panic, no infinite loop on crafted input |
| T-06.6-13 | Tampering | `index += diff` node-walk arithmetic | mitigate | `i64` per upstream; reject out-of-range resulting index → typed error, never index a slice unchecked |
| T-06.6-14 | Tampering | per-tree Σ leaf values + bias | mitigate | Σ via `cb_core::sum_f64`; bias added exactly once (Pitfall 6) |
</threat_model>

<verification>
- `cargo test -p cb-model --lib` and `cargo test -p cb-train --lib` green (oblivious apply byte-identical).
- `cargo test -p cb-model --test non_symmetric_oracle_test` fully green (Depthwise + Lossguide, per-stage + round-trip), none ignored.
- No-regression locks: `cargo test -p cb-model --test apply_oracle_test` + `fstr_oracle_test` + `shap_oracle_test` green.
- Grep: oblivious arm of `predict_raw_one` is the literal existing call; no `mod tests` added.
</verification>

<success_criteria>
FEAT-06 (SC-3) complete: Lossguide + Depthwise non-symmetric trees train, apply via pointer-walk, and round-trip through `.cbm`/json — all oracle-locked ≤1e-5 vs catboost 1.2.10. Oblivious path byte-identical. Region + non-symmetric-monotone recorded as escalated gaps (guarded, not built).
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-05-SUMMARY.md` when done.
</output>
