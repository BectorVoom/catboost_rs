---
phase: 06.6-advanced-features-and-non-symmetric-trees
verified: 2026-06-18T12:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "SC-3 / FEAT-06: Rust leaf-wise grower → save_cbm → load_cbm → predict ≤1e-5 (CR-02 sentinel mismatch fixed; CR-01 checked u16 step diffs)"
  gaps_remaining: []
  regressions: []
---

# Phase 06.6: Advanced Features and Non-Symmetric Trees — Verification Report

**Phase Goal:** FEAT-04 feature/per-object penalties on the oblivious grower, oracle ≤1e-5; FEAT-03 monotone constraints OBLIVIOUS-ONLY via verbatim PAVA leaf-delta projection, oracle ≤1e-5; Alternative grow policies Lossguide/Depthwise produce TRUE non-symmetric trees — full train + non-symmetric apply + .cbm/json round-trip oracle-locked ≤1e-5; Advanced fstr — ShapInteractionValues, PredictionDiff, SAGE — plus MODEL-03 LossFunctionChange importance, match upstream ≤1e-5; FEAT-05 recursive feature selection by PredictionValuesChange/LossFunctionChange/ShapValues, selected/eliminated set oracle-locked.

**Verified:** 2026-06-18T12:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 06.6-09, single partial SC-3 gap closed)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FEAT-04: feature_weights / first_feature_use_penalties / per_object_feature_penalties oracle-locked ≤1e-5 vs catboost 1.2.10; no-penalty default path byte-identical (SC-1 penalty portion) | VERIFIED | `cargo test -p cb-train --test penalty_oracle_test` → 3 passed. FeaturePenalties struct + penalize() + is_noop() in tree.rs:1619-1695. Four BoostParams penalty fields with *_default() in boosting.rs:315-349. |
| 2 | FEAT-03: monotone constraints oblivious-only via PAVA leaf-delta projection (calc_monotonic_leaf_deltas), oracle ≤1e-5; non-symmetric + Region guards typed CbError; empty-constraints path byte-identical (SC-1 monotone portion) | VERIFIED | `cargo test -p cb-train --test monotone_oracle_test` → 3 passed (0 ignored). calc_monotonic_leaf_deltas in cb-compute/src/leaf.rs:751; validate_grow_policy guard in boosting.rs:1331. |
| 3 | FEAT-06 SC-3: Lossguide/Depthwise produce TRUE non-symmetric trees — full train + non-symmetric apply + .cbm/json round-trip oracle-locked ≤1e-5; Region guard typed CbError; Rust-grower-trained model saves to .cbm and reloads within ≤1e-5 | VERIFIED | `cargo test -p cb-model --test non_symmetric_oracle_test` → 3 passed; `cargo test -p cb-train --test non_symmetric_grower_oracle_test` → 1 passed; `cargo test -p cb-model --test non_symmetric_grower_roundtrip_oracle_test` → 1 passed (NEW — grower→from_trained→save_cbm→load_cbm→predict_raw ≤1e-5). tree.rs:1143 confirmed `vec![u32::MAX; node_count]`; explicit `node_id_to_leaf_id[id] = u32::MAX` in Interior arm at line 1155; checked `u16::try_from` at lines 1163+1171; no bare `as u16` in finalization loop. |
| 4 | MODEL-03 LossFunctionChange importance (D-12) + MODEL-05 advanced fstr: ShapInteractionValues, PredictionDiff all ≤1e-5; non-symmetric PVC/Interaction/SHAP generalized ≥1 oracle-locked case; SAGE deterministic structural surrogate per pre-authorized D-6.6-11 fallback (b), documented — not silently dropped (SC-4) | VERIFIED | `cargo test -p cb-model --test fstr_oracle_test` → 6 passed; `cargo test -p cb-model --test advanced_fstr_oracle_test` → 6 passed; `cargo test -p cb-model --test shap_oracle_test` → 4 passed. SAGE deferral documented in shap.rs:1207-1227. |
| 5 | FEAT-05: select_features by RecursiveByShapValues + RecursiveByPredictionValuesChange / RecursiveByLossFunctionChange produces selected/eliminated partition matching catboost 1.2.10 (SC-2) | VERIFIED | `cargo test -p cb-train --test feature_selection_oracle_test` → 2 passed. feature_selection.rs confirmed (select_features + EFeaturesSelectionAlgorithm + FeatureSelectionResult + ImportanceRanker). |

**Score:** 5/5 truths verified

### Gap Closure — SC-3 / FEAT-06 (plan 06.6-09)

The single partial gap from the initial verification (CR-02 sentinel mismatch causing `save_cbm` to return `ModelError::SchemaVersion` on a Rust-trained non-symmetric model) is now closed. Evidence:

| Check | Result |
|-------|--------|
| `tree.rs:1143` initializes to `vec![u32::MAX; node_count]` | CONFIRMED |
| `BuiltNode::Interior` arm sets `node_id_to_leaf_id[id] = u32::MAX` explicitly (line 1155) | CONFIRMED |
| `u16::try_from` replaces bare `as u16` for step-node child diffs (lines 1163, 1171) | CONFIRMED |
| No bare `as u16` in finalization loop (lines 1130–1210) | CONFIRMED (grep returns no output) |
| `crates/cb-model/tests/non_symmetric_grower_roundtrip_oracle_test.rs` exists, contains `save_cbm`, `load_cbm`, `from_trained`, `grow_policy: EGrowPolicy::Depthwise` | CONFIRMED |
| Test is NOT `#[ignore]`d; tolerance is 1e-5 | CONFIRMED |
| `cb-backend` and `cb-compute` under `[dev-dependencies]` in `crates/cb-model/Cargo.toml` | CONFIRMED (lines 33, 36) |
| `cargo test -p cb-model --test non_symmetric_grower_roundtrip_oracle_test` | 1 passed, 0 failed, 0 ignored |
| No regressions: `cargo test -p cb-train --test non_symmetric_grower_oracle_test` | 1 passed |
| No regressions: `cargo test -p cb-train --lib` | 228 passed |
| No regressions: `cargo test -p cb-model --test non_symmetric_oracle_test` | 3 passed |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-train/src/tree.rs` | leaf_wise_grower finalization: u32::MAX interior sentinel + checked u16 diffs | VERIFIED | line 1143: `vec![u32::MAX; node_count]`; line 1155: explicit Interior sentinel; lines 1163+1171: `u16::try_from` |
| `crates/cb-model/tests/non_symmetric_grower_roundtrip_oracle_test.rs` | NEW grower→save→load→predict ≤1e-5 oracle test | VERIFIED | Exists; not #[ignore]d; trains Depthwise, saves via save_cbm, reloads via load_cbm, asserts ≤1e-5 |
| `crates/cb-model/Cargo.toml` | cb-backend + cb-compute in [dev-dependencies] | VERIFIED | Lines 33 and 36 confirmed |
| `crates/cb-train/src/boosting.rs` | BoostParams penalty/monotone/grow_policy fields + dispatchers | VERIFIED (carried from initial) | Feature penalty fields 315-349; monotone_constraints 350-366; EGrowPolicy + grow_policy 83-133; validate_grow_policy 1331 |
| `crates/cb-compute/src/leaf.rs` | calc_monotonic_leaf_deltas + build_monotonic_linear_orders | VERIFIED (carried from initial) | calc_monotonic_leaf_deltas at 751; build_monotonic_linear_orders at 556 |
| `crates/cb-model/src/model.rs` | NonSymmetricTree + TreeVariant + from_trained lift | VERIFIED (carried from initial) | NonSymmetricTree at 113; TreeVariant at 143; from_trained non-sym lift at 301 |
| `crates/cb-model/src/apply.rs` | leaf_index_nonsym + TreeVariant branch in predict_raw_one | VERIFIED (carried from initial) | leaf_index_nonsym at 234; TreeVariant branch at predict_raw_one 273-301 |
| `crates/cb-model/src/cbm.rs` | non-symmetric encode/decode wiring | VERIFIED (encode path now correctly driven by u32::MAX sentinel) | distinct_leaves filter at cbm.rs:200 counts only ids != u32::MAX; grower models now serialized correctly |
| `crates/cb-model/src/fstr.rs` | loss_function_change + non-sym PVC/Interaction recursion | VERIFIED (carried from initial) | loss_function_change at 481; non-sym PVC/Interaction confirmed |
| `crates/cb-model/src/shap.rs` | shap_interaction_values + prediction_diff + sage_values + non-sym TreeSHAP | VERIFIED (carried from initial) | All four backends confirmed; SAGE deferral documented |
| `crates/cb-train/src/feature_selection.rs` | select_features + 3-variant algorithm enum | VERIFIED (carried from initial) | select_features at 157; EFeaturesSelectionAlgorithm at 46 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| tree.rs leaf_wise_grower finalization | cbm.rs:200 distinct_leaves filter | node_id_to_leaf_id interior slots == u32::MAX | VERIFIED | vec![u32::MAX; node_count] init + explicit Interior arm assignment; cbm.rs:200 counts only ids != u32::MAX |
| non_symmetric_grower_roundtrip_oracle_test.rs | cb_model::{save_cbm, load_cbm} | train via Depthwise → from_trained → save_cbm → load_cbm → predict_raw | VERIFIED | Pattern `save_cbm(&cb_model` confirmed in test; test passes with 1 passed |
| boosting.rs BoostParams | tree.rs leaf_wise_grower | grow_policy dispatch + FeaturePenalties threading | VERIFIED (carried from initial) | train_inner dispatch on EGrowPolicy confirmed |
| boosting.rs monotone_constraints | leaf.rs calc_monotonic_leaf_deltas | PAVA post-pass | VERIFIED (carried from initial) | validate_monotone_constraints gates; PAVA called in diagonal leaf branch |
| apply.rs predict_raw_one | model.rs TreeVariant | branch on variant | VERIFIED (carried from initial) | Oblivious arm = literal leaf_index_for; NonSymmetric arm = leaf_index_nonsym |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| FEAT-06 grower→save→load→predict ≤1e-5 (new gap-closure test) | `cargo test -p cb-model --test non_symmetric_grower_roundtrip_oracle_test` | 1 passed, 0 failed, 0 ignored | PASS |
| FEAT-06 grower SPLITS oracle (no regression) | `cargo test -p cb-train --test non_symmetric_grower_oracle_test` | 1 passed, 0 failed, 0 ignored | PASS |
| cb-train lib tests (no regression) | `cargo test -p cb-train --lib` | 228 passed, 0 failed, 0 ignored | PASS |
| FEAT-06 non-symmetric full oracle (upstream-loaded, no regression) | `cargo test -p cb-model --test non_symmetric_oracle_test` | 3 passed, 0 failed, 0 ignored | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| FEAT-03 | 06.6-02 | Monotone constraints oblivious-only, PAVA, oracle ≤1e-5 | SATISFIED | monotone_oracle_test 3/3 green; calc_monotonic_leaf_deltas in leaf.rs; REQUIREMENTS.md marked [x] |
| FEAT-04 | 06.6-01 | Feature/per-object penalties, oblivious grower, oracle ≤1e-5 | SATISFIED | penalty_oracle_test 3/3 green; FeaturePenalties in tree.rs; REQUIREMENTS.md marked [x] |
| FEAT-05 | 06.6-08 | Recursive feature selection by ShapValues/PVC/LFC | SATISFIED | feature_selection_oracle_test 2/2 green; feature_selection.rs; REQUIREMENTS.md marked [x] |
| FEAT-06 | 06.6-03/04/05 + 06.6-09 | Lossguide/Depthwise non-symmetric trees, oracle ≤1e-5; full grower→save→load→predict round-trip | SATISFIED | Non-sym apply oracle 3/3; grower SPLITS oracle 1/1; grower round-trip oracle 1/1 (NEW); REQUIREMENTS.md marked [x] |
| MODEL-03 | 06.6-06 | LossFunctionChange importance (D-12), oracle ≤1e-5 | SATISFIED | fstr_oracle_test 6/6 green; loss_function_change in fstr.rs; REQUIREMENTS.md marked [x] |
| MODEL-05 | 06.6-07 | ShapInteractionValues, PredictionDiff, SAGE; non-sym SHAP | SATISFIED (SAGE: structural surrogate per D-6.6-11 fallback b, documented) | advanced_fstr_oracle_test 6/6; shap_oracle_test 4/4; REQUIREMENTS.md marked [x] |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| boosting.rs | 3949-3980 | `trees.last()` for eval-set update (always None for non-sym path) | Warning (CR-03) — LATENT | Eval-set metrics / early stopping / use_best_model silently disabled for non-sym; does not affect training accuracy or oracle parity |
| cbm.rs | 181-185 | CTR split serialized as index 0 | Warning (CR-04) — LATENT | Unreachable with current training (CTR only on oblivious); should return typed error rather than silent corruption if ever reached |
| shap.rs | 699-709 | No early return after terminal-node SHAP update | Warning (WR-04) — LATENT | Latent risk for unbounded recursion if hot_coefficient==1.0 exactly; oracle tests pass ≤1e-5 |
| json.rs | unflatten_non_symmetric | .cbm → json → .cbm is not identity-preserving | Warning (WR-02) — LATENT | One-sided halt nodes expand differently; predictions are apply-equivalent; documented in 06.6-05-SUMMARY.md |

Note: CR-02 (the blocking anti-pattern from the initial verification) is now FIXED. CR-01 (bare `as u16` truncation) is now FIXED (replaced by `u16::try_from` with `CbError::OutOfRange`). The four remaining items above are classified LATENT / outside-fixture-scope per the initial verification and remain out of scope for this phase.

### Human Verification Required

None — all findings are code-verifiable. All must-haves confirmed via static code inspection and live test execution.

### Gaps Summary

No gaps. The single partial gap from the initial verification (SC-3 / FEAT-06 grower→save→load→predict path broken by CR-02 sentinel mismatch) is now fully closed by plan 06.6-09. All five roadmap success criteria are satisfied:

- SC-1 (FEAT-03 monotone + FEAT-04 penalties): VERIFIED (initial)
- SC-2 (FEAT-05 recursive feature selection): VERIFIED (initial)
- SC-3 (FEAT-06 non-symmetric full round-trip including grower→save→load→predict): VERIFIED (gap closure 06.6-09)
- SC-4 (MODEL-03/MODEL-05 advanced fstr): VERIFIED (initial)

Remaining latent items (CR-03, CR-04, WR-02, WR-04) are outside the oracle fixture scope and do not affect parity for the tested params. They are tracked for future hardening.

---

_Verified: 2026-06-18T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
