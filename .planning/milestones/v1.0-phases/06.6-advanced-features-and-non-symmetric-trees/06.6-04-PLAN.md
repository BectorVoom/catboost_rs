---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 04
type: execute
wave: 4
depends_on: ["06.6-02", "06.6-03"]
files_modified:
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-model/src/model.rs
  - crates/cb-model/tests/non_symmetric_oracle_test.rs
  - crates/cb-compute/tests/monotone_oracle_test.rs
  - crates/cb-oracle/tests/fixtures/non_symmetric/depthwise_simplest/.gitkeep
autonomous: true
requirements: [FEAT-06]
must_haves:
  truths:
    - "BEFORE the grower is committed, the simplest-Depthwise fixture (random_strength=0, no bootstrap, no CTR) is generated from catboost 1.2.10 and locked as a failing-harness SPLITS gate, so a draw-stream divergence is caught early (RESEARCH §Open Questions RESOLVED Q1; D-6.6-11 escalate-don't-weaken fallback named)."
    - "grow_policy=Depthwise builds a level-order non-symmetric tree whose SPLITS match upstream catboost 1.2.10 (Open Question 1 — splits locked first)."
    - "grow_policy=Lossguide builds a best-gain priority-queue non-symmetric tree whose SPLITS match upstream, replicating the upstream queue-penalty NON-recompute (Pitfall 4)."
    - "grow_policy=SymmetricTree dispatch is the LITERAL existing oblivious call — byte-identical (D-6.6-05)."
    - "from_trained lifts the non-symmetric grower output into the TreeVariant::NonSymmetric variant; the oblivious lift path is untouched."
    - "The non-symmetric-monotone typed-error guard assertion deferred by Plan 06.6-02 (because grow_policy did not yet exist) is ENABLED here once grow_policy lands, and monotone_oracle_test passes asserting the typed-error rejection for monotone × {Lossguide,Depthwise}."
  artifacts:
    - path: "crates/cb-train/src/tree.rs"
      provides: "leaf_wise_grower(policy) producing a non-symmetric node graph (Depthwise + Lossguide)"
      contains: "leaf_wise_grower"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "grow_policy/max_leaves/min_data_in_leaf BoostParams fields + grow_policy dispatch"
      contains: "grow_policy"
    - path: "crates/cb-model/src/model.rs"
      provides: "from_trained lift of the non-symmetric grower output"
      contains: "NonSymmetric"
    - path: "crates/cb-compute/tests/monotone_oracle_test.rs"
      provides: "enabled non-symmetric-monotone typed-error guard assertion (deferred from 06.6-02), now green"
      contains: "monotone"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/tree.rs"
      via: "grow_policy match dispatches SymmetricTree→existing | Lossguide|Depthwise→leaf_wise_grower"
      pattern: "leaf_wise_grower|grow_policy"
---

<objective>
Gate-B core (FEAT-06, D-6.6-04): build ONE policy-parameterized leaf-wise grower producing TRUE non-symmetric trees for Lossguide + Depthwise (Region OUT — CPU-unimplemented upstream, escalated gap). Mirrors upstream's shared leaf-wise scoring core with the split queue / expansion strategy parameterized by policy — NOT three separate growers. Lock SPLITS first (Open Question 1) before leaf values — and lock the SIMPLEST-Depthwise SPLITS as a failing-harness gate BEFORE the grower is even written, so any draw-stream divergence is discovered early (RESEARCH §"Open Questions (RESOLVED)" Q1).

Purpose: The load-bearing structural item. Upstream uses one shared scoring core (`leafwise_scoring.cpp`) + two policy growers (`GreedyTensorSearchDepthwise`, `GreedyTensorSearchLossguide`) dispatched in `GreedyTensorSearch`. The SymmetricTree dispatch arm stays the literal existing oblivious call (byte-identical, D-6.6-05). Leaf VALUES + apply round-trip e2e land in 06.6-05; this plan locks the STRUCTURE. It ALSO closes the deferred Plan-02 non-symmetric-monotone guard now that `grow_policy` exists.
Output: an early simplest-Depthwise SPLITS preflight gate (Task 1); `leaf_wise_grower(policy)` in `tree.rs` (node graph, reuses `score_candidate`/`select_best_candidate`/candidate enumeration verbatim); `grow_policy`/`max_leaves`/`min_data_in_leaf` BoostParams + dispatch; `from_trained` lift into `TreeVariant::NonSymmetric`; the enabled non-symmetric-monotone guard assertion in `monotone_oracle_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- `crates/cb-oracle/tests/fixtures/non_symmetric/depthwise_simplest/`: the simplest isolating Depthwise fixture (no CTR, no bootstrap, random_strength=0, thread_count=1, pinned seed) committed for the early SPLITS preflight gate.
- `tree.rs`: `leaf_wise_grower(policy, ...)` producing a non-symmetric node graph; Depthwise (level-order queue) + Lossguide (best-gain priority queue) expansion strategies behind one core; new optional `GrownTree` fields (`step_nodes`/`node_id_to_leaf_id` or a `variant` enum) following the existing empty-parallel-vector pattern.
- `boosting.rs`: `grow_policy: EGrowPolicy`-equivalent (SymmetricTree | Lossguide | Depthwise), `max_leaves: usize`, `min_data_in_leaf: usize` BoostParams fields (+ paired `*_default()`); `train_inner` grower dispatch `match params.grow_policy`.
- `model.rs`: `from_trained` lift of the non-symmetric grower output into `TreeVariant::NonSymmetric`.
- `monotone_oracle_test.rs`: the non-symmetric-monotone typed-error guard assertion deferred by Plan 06.6-02 is ENABLED here (now that `grow_policy` exists) and passes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EARLY simplest-Depthwise SPLITS preflight — generate fixture, lock SPLITS as a failing-harness gate BEFORE the grower</name>
  <files>crates/cb-oracle/tests/fixtures/non_symmetric/depthwise_simplest/.gitkeep, crates/cb-model/tests/non_symmetric_oracle_test.rs</files>
  <read_first>
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (§"Open Questions (RESOLVED)" Q1 — the non-symmetric draw-stream parity question; simplest isolating Depthwise fixture: no CTR, no bootstrap, random_strength=0, thread_count=1; D-6.6-11 escalate-don't-weaken discipline; instrumented-trainer escalation path `/tmp/cb_build313` + clang-18)
    - crates/cb-model/tests/non_symmetric_oracle_test.rs (the splits-first harness from 06.6-03 — add the simplest-Depthwise SPLITS preflight here, alongside the existing Depthwise/Lossguide stubs)
    - crates/cb-oracle/src/{fixture,compare,model_json}.rs (Stage enum + compare_stage + the "trees" parser from 06.6-03)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md (D-6.6-11 — escalate, never weaken; never loosen tolerance)
  </read_first>
  <action>
    BEFORE writing any grower code (Task 2/3), generate the SIMPLEST possible Depthwise fixture OFFLINE from `.venv` catboost 1.2.10 to isolate the draw-stream parity question (RESEARCH §"Open Questions (RESOLVED)" Q1): `m.fit(Pool(X,y), grow_policy="Depthwise", random_strength=0, bootstrap_type="No"`-equivalent (no bootstrap)`, thread_count=1, random_seed=<pinned>, max_depth=<small>)` with NO categorical features (no CTR). Capture model.json (nested "trees") + `.cbm`. Commit under `crates/cb-oracle/tests/fixtures/non_symmetric/depthwise_simplest/`.

    In `non_symmetric_oracle_test.rs` add a `depthwise_simplest_splits` preflight test that locks SPLITS (integer-exact / ≤1e-5 via `compare_stage`) against this fixture as the FIRST hard non-symmetric gate. This test is EXPECTED TO FAIL at the end of THIS task (the grower lands in Task 2) — it is the failing-harness gate that catches a draw-stream divergence the instant the grower is wired, not mid-grower. Do NOT `#[ignore]` it; the task's automated verify asserts it COMPILES (`--no-run`).

    Document, as a comment in the test and in the SUMMARY, the escalation fallback: if the simplest-Depthwise SPLITS diverge once the grower lands, the draw stream differs from upstream and the resolution is to ESCALATE to the persistent instrumented trainer (`/tmp/cb_build313` + clang-18) per D-6.6-11 — NEVER loosen tolerance, NEVER `#[ignore]`, NEVER fabricate. This empirical preflight is the early-warning realization of RESEARCH Open Question 1.
  </action>
  <verify>
    <automated>cargo test -p cb-model --test non_symmetric_oracle_test --no-run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The simplest-Depthwise fixture (model.json + .cbm; random_strength=0, no bootstrap, no CTR, thread_count=1, pinned seed) is committed under `crates/cb-oracle/tests/fixtures/non_symmetric/depthwise_simplest/`; real catboost 1.2.10 output, no fabrication.
    - `non_symmetric_oracle_test.rs` contains a `depthwise_simplest_splits` preflight test locking SPLITS first; it COMPILES (`--no-run`) and is NOT `#[ignore]`d (it fails until Task 2's grower lands, by design).
    - The instrumented-trainer escalation fallback (D-6.6-11, escalate-don't-weaken, never loosen tolerance) is documented in the test comment and SUMMARY, referencing RESEARCH §"Open Questions (RESOLVED)" Q1.
  </acceptance_criteria>
  <done>The simplest-Depthwise SPLITS preflight gate and its committed fixture exist BEFORE the grower, so a draw-stream divergence is caught the moment the grower is wired; escalation fallback documented.</done>
</task>

<task type="auto">
  <name>Task 2: grow_policy BoostParams + dispatch + leaf_wise_grower(policy) producing a non-symmetric node graph</name>
  <files>crates/cb-train/src/tree.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-train/src/tree.rs:196-216 (`GrownTree` — already carries parallel optional vectors `ctr_splits`/`level_kinds` empty for searches that don't use them; add `step_nodes`/`node_id_to_leaf_id` the same way so existing callers compile unchanged)
    - crates/cb-train/src/tree.rs:526-595 (`greedy_tensor_search_oblivious_perturbed` 526 + `select_level_plain` 573 — the fixed-depth level-loop the leaf-wise grower replaces with a policy queue; candidate enumeration 582-595 = float features ascending × borders ascending through `score_candidate`, REUSE verbatim)
    - crates/cb-train/src/tree.rs:274-285 (`select_best_candidate` strict-`>` first-wins — DO NOT change; reuse for per-leaf best split)
    - crates/cb-train/src/boosting.rs:88-340 (BoostParams field + `*_default()` convention) and boosting.rs:2859 (`train_inner` grower dispatch site `greedy_tensor_search_oblivious_with_ctr(...)`) and boosting.rs:971 (`validate_leaf_method` — typed-CbResult up-front rejection pattern)
    - catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp:1509-1700 (`GreedyTensorSearchDepthwise`: `for curDepth { for leaf in level { CalculateStats; if gain<1e-9 continue; AddSplit; push left/right } }`; children appended (left,right), id%2 even/odd subtract-trick)
    - catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp:1806-1938 (`GreedyTensorSearchLossguide`: max-priority-queue of (leaf,gain,best_candidate,stats); pop best-gain, split, push children; stop on empty OR leaf_count>=max_leaves; gain<1e-9 + leafDepth<max_depth gate; LINES 1875-1882 — queued candidates NOT recomputed when a feature first becomes used: REPLICATE the non-recompute exactly, Pitfall 4)
    - catboost-master/catboost/private/libs/algo/enums.h (`EGrowPolicy { SymmetricTree, Lossguide, Depthwise, Region }`)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md (tree.rs grower skeleton + "RESEARCH-specific gotchas" + GrownTree empty-parallel-vector precedent)
  </read_first>
  <action>
    Add a `grow_policy` enum field to BoostParams (`SymmetricTree | Lossguide | Depthwise`; Region exists in the enum for parity but is REJECTED by the guard from 06.6-02 — no grower arm), plus `max_leaves: usize` (`max_leaves_default()` per upstream) and `min_data_in_leaf: usize`. Doc-comment + paired `*_default()` per the convention; cite upstream defaults; "Pinned EXPLICITLY, never auto-selected".

    In tree.rs add `leaf_wise_grower(policy, ...)` producing a non-symmetric node graph (flat node array → `step_nodes` `(left_subtree_diff,right_subtree_diff)` + `node_id_to_leaf_id`). ONE core parameterized by `policy`:
    - Depthwise: level-order queue — `for cur_depth in 0..max_depth { for leaf in current_level { compute per-leaf stats; best = select_best_candidate(per-leaf candidates); if gain < 1e-9 continue; add split; push left,right to next level } }` (greedy_tensor_search.cpp:1509-1700).
    - Lossguide: a max-priority-queue keyed on per-leaf best gain — pop best-gain leaf, split it, enqueue its two children's best candidates; stop when queue empty OR `leaf_count >= max_leaves`; gate on `gain < 1e-9` and `leaf_depth < max_depth` (greedy_tensor_search.cpp:1806-1938). REPLICATE the upstream queue-penalty NON-recompute (lines 1875-1882) exactly — penalties applied at ENQUEUE time only, never recomputed when a feature first becomes used (Pitfall 4).

    Reuse `score_candidate`, the candidate enumeration (tree.rs:582-595), and `select_best_candidate` (strict-`>`, tree.rs:274-285) VERBATIM — only the queue/expansion differs by policy. Add the new node-graph fields to `GrownTree` following the existing empty-parallel-vector precedent (tree.rs:196-216) so the oblivious grower leaves them empty and existing callers compile unchanged. All reductions via `cb_core::sum_f64` (D-08); `.get(...)` access; NO embedded tests.

    In `train_inner` (boosting.rs:2859) wrap the grower call in `match params.grow_policy { SymmetricTree => <the LITERAL existing greedy_tensor_search_oblivious_with_ctr(...) call, UNCHANGED>, Lossguide | Depthwise => leaf_wise_grower(policy, ...) }`. The SymmetricTree arm MUST be byte-identical (D-6.6-05). Lock SPLITS first per Open Question 1 — leaf VALUES + apply are 06.6-05; emit the structure (splits + node graph) here. Once this grower lands, Task 1's `depthwise_simplest_splits` preflight should pass; if it diverges, escalate per D-6.6-11 (instrumented trainer) — do NOT weaken.
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-train` succeeds; `leaf_wise_grower` exists with Depthwise + Lossguide expansion; `grow_policy`/`max_leaves`/`min_data_in_leaf` fields + `*_default()` exist.
    - `train_inner` dispatches on `grow_policy`; the SymmetricTree arm is the literal existing call (grep the existing call text is preserved).
    - Lossguide enqueue-time-only penalty replication is implemented (no recompute on first-use); a comment cites greedy_tensor_search.cpp:1875-1882.
    - All existing cb-train lib tests stay green (SymmetricTree path byte-identical, D-6.6-05).
    - No `mod tests`/`#[cfg(test)]` in tree.rs/boosting.rs.
  </acceptance_criteria>
  <done>The policy-parameterized leaf-wise grower builds non-symmetric node graphs for Depthwise + Lossguide; grow_policy dispatch added with the SymmetricTree arm byte-identical; cb-train lib suite green.</done>
</task>

<task type="auto">
  <name>Task 3: from_trained lift into TreeVariant::NonSymmetric + splits-first oracle lock + ENABLE the deferred non-symmetric-monotone guard assertion</name>
  <files>crates/cb-model/src/model.rs, crates/cb-compute/tests/monotone_oracle_test.rs</files>
  <read_first>
    - crates/cb-model/src/model.rs:165-211 (`from_trained` lifting: float splits → `ModelSplit::Float`, CTR splits pushed; the oblivious lift to leave untouched)
    - crates/cb-model/src/model.rs:106-116 (`ObliviousTree`) + the `NonSymmetricTree`/`TreeVariant` added in 06.6-03
    - crates/cb-train/src/tree.rs:196-216 (`GrownTree` non-symmetric fields produced by Task 2)
    - crates/cb-model/tests/non_symmetric_oracle_test.rs (the splits-first harness from 06.6-03 + Task 1's simplest-Depthwise preflight — this task turns the SPLITS assertions green)
    - crates/cb-compute/tests/monotone_oracle_test.rs (the monotone harness from 06.6-02 — the non-symmetric-monotone typed-error guard assertion was DEFERRED there because `grow_policy` did not yet exist; ENABLE it now)
    - crates/cb-train/src/boosting.rs (the escalated-gap guard from 06.6-02 rejecting monotone_constraints × non-symmetric grow_policy — now reachable since grow_policy lands in Task 2)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Open Question 1 — splits locked before leaf values)
  </read_first>
  <action>
    Extend `from_trained` (model.rs:165-211) to lift the non-symmetric grower output (`GrownTree` with `step_nodes`/`node_id_to_leaf_id` populated) into `TreeVariant::NonSymmetric`, mapping its splits to `ModelSplit::Float`/`ModelSplit::Ctr` exactly as the oblivious lift does, and carrying `step_nodes`/`node_id_to_leaf_id`. Branch on whether the grown tree is oblivious or non-symmetric (the grower from Task 2 sets the discriminator). The oblivious lift path stays byte-identical (D-6.6-05).

    Run the `non_symmetric_oracle_test.rs` harness and confirm the SPLITS stage now passes ≤1e-5 / integer-exact for the `depthwise_simplest` preflight (Task 1) AND for both full Depthwise and Lossguide (Open Question 1 — splits locked first). LeafValues/StagedApprox/Predictions and the round-trip will turn green in 06.6-05; if leaf values already happen to match, that is fine, but the HARD gate for THIS plan is SPLITS. Do NOT `#[ignore]`, do NOT weaken tolerance. If splits diverge, escalate to the persistent instrumented trainer per D-6.6-11 / Open Question 1 (draw-stream divergence) rather than weaken.

    ENABLE the deferred non-symmetric-monotone guard assertion in `crates/cb-compute/tests/monotone_oracle_test.rs`: now that `grow_policy` exists (Task 2), wire/uncomment the assertion that constructing/fitting with `monotone_constraints` under a non-symmetric `grow_policy` ({Lossguide, Depthwise}) returns a typed `CbError` (the escalated-gap guard from 06.6-02, `boosting.rs`). The test must PASS (`cargo test -p cb-compute --test monotone_oracle_test` green). Do NOT fabricate any non-symmetric-monotone fixture (no upstream ground truth — D-6.6-07); only the typed-error rejection is asserted. Keep the self-contained Region guard assertion (already in 06.6-02) intact.
  </action>
  <verify>
    <automated>cargo test -p cb-model --test non_symmetric_oracle_test 2>&1 | tail -15; cargo test -p cb-compute --test monotone_oracle_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `from_trained` lifts the non-symmetric grower output into `TreeVariant::NonSymmetric`; oblivious lift unchanged.
    - The non_symmetric_oracle_test SPLITS assertions pass for the simplest-Depthwise preflight AND for both Depthwise and Lossguide (integer-exact / ≤1e-5 vs catboost 1.2.10).
    - The previously-deferred non-symmetric-monotone guard assertion is now ENABLED and passing: `cargo test -p cb-compute --test monotone_oracle_test` is green, asserting the typed-error rejection for monotone × {Lossguide,Depthwise}; no fabricated fixture, no `#[ignore]`.
    - No tolerance weakened, no `#[ignore]`; the existing cb-model lib + oblivious oracle tests stay green.
  </acceptance_criteria>
  <done>Non-symmetric grower output is lifted into the canonical model and its tree STRUCTURE (splits + node graph) is oracle-locked vs catboost 1.2.10 for the simplest-Depthwise preflight + Depthwise + Lossguide; the deferred non-symmetric-monotone typed-error guard is enabled and green; leaf values + apply round-trip pending 06.6-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training params → grower | `max_leaves`/`min_data_in_leaf`/`grow_policy` validated; Region rejected (guard from 06.6-02); monotone × non-symmetric rejected (guard from 06.6-02, enabled here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-09 | Denial of Service | leaf-wise node-graph construction | mitigate | `max_leaves`/`max_depth` bound the queue; `.get(...)` access; no unbounded growth |
| T-06.6-10 | Tampering | per-leaf stat / gain summation | mitigate | Reductions via `cb_core::sum_f64` (D-08) — split-score parity demands exact order |
| T-06.6-11 | Input Validation | grow_policy=Region / monotone×non-symmetric | mitigate | Typed `CbError` (guards from 06.6-02; non-symmetric-monotone guard assertion enabled in Task 3) — no grower arm, no fabricated structure |
</threat_model>

<verification>
- `cargo test -p cb-train --lib` green (SymmetricTree path byte-identical).
- `cargo test -p cb-model --test non_symmetric_oracle_test` — simplest-Depthwise preflight + Depthwise + Lossguide SPLITS stage green; none ignored.
- `cargo test -p cb-compute --test monotone_oracle_test` green — the deferred non-symmetric-monotone typed-error guard assertion is enabled and passing.
- Grep: SymmetricTree dispatch arm is the literal existing call; Lossguide non-recompute comment cites lines 1875-1882; no `mod tests` added.
</verification>

<success_criteria>
The unified leaf-wise grower (Lossguide + Depthwise) produces true non-symmetric trees whose SPLITS match catboost 1.2.10; the simplest-Depthwise SPLITS were locked as a failing-harness preflight BEFORE the grower (early draw-stream divergence detection, RESEARCH Open Question 1); SymmetricTree dispatch is byte-identical; Region and monotone×non-symmetric are rejected (escalated gaps, the latter's deferred guard assertion now enabled and green). The structural half of FEAT-06 (SC-3) is locked; leaf values + apply round-trip complete in 06.6-05.
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-04-SUMMARY.md` when done.
</output>
