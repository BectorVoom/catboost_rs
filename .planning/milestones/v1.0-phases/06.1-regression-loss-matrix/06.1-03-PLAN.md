---
phase: 06.1-regression-loss-matrix
plan: 03
type: execute
wave: 3
depends_on: ["06.1-02"]
files_modified:
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-compute/src/loss_test.rs
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/cpu_runtime.rs
  - crates/cb-backend/src/cpu_runtime_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/boosting_test.rs
  - crates/cb-train/src/metrics.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/fixtures/quantile_alpha07/
  - crates/cb-oracle/fixtures/quantile_alpha05_mae/
  - crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs
autonomous: false
requirements: [LOSS-03]

must_haves:
  truths:
    - "A user can train with loss_function=Quantile:alpha=0.7 and the Exact leaf uses the weighted 0.7-quantile (not the hardcoded 0.5) — oracle <=1e-5"
    - "MAE is expressible as Quantile{alpha:0.5,delta:1e-6} and the MAE Exact oracle stays <=1e-5 (MAE == Quantile alpha=0.5)"
    - "The Exact leaf path threads the active loss's (alpha,delta) instead of the hardcoded QUANTILE_ALPHA/QUANTILE_DELTA"
    - "All prior oracles (existing ~40 + Wave-1 + Wave-2) stay green"
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "Loss::Quantile{alpha,delta} variant (defaults 0.5 / 1e-6)"
      contains: "Quantile"
    - path: "crates/cb-compute/src/loss.rs"
      provides: "quantile_der1/2 generalizing mae_der1 to arbitrary alpha"
      contains: "pub fn quantile_der1"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "compute_leaf_deltas Exact branch threads loss (alpha,delta), not hardcoded constants"
      contains: "exact_leaf_delta"
    - path: "crates/cb-oracle/fixtures/quantile_alpha07/config.json"
      provides: "upstream Quantile alpha=0.7 Exact-leaf fixture"
      contains: "alpha=0.7"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "cb_compute::leaf::exact_leaf_delta"
      via: "compute_leaf_deltas passes the active loss alpha/delta (parameterized, not QUANTILE_ALPHA/QUANTILE_DELTA)"
      pattern: "exact_leaf_delta\\(.*alpha"
    - from: "crates/cb-compute/src/runtime.rs"
      to: "crates/cb-train/src/boosting.rs"
      via: "Loss::Quantile{alpha,delta} destructured to feed the Exact leaf"
      pattern: "Loss::Quantile"
---

<objective>
Land the **quantile family** — `Quantile{alpha, delta}` generalizing the existing `Loss::Mae` (MAE == Quantile alpha=0.5) — oracle-locked <=1e-5 vs upstream catboost 1.2.10, with the Exact leaf using the weighted **alpha-quantile** for arbitrary alpha. Wave 3 of the family-wave structure (D-6.1-02), and the final scalar-matrix wave.

Purpose: Completes the LOSS-03 scalar matrix. The der1/der2 is a trivial generalization of the existing `mae_der1` deadzone; the ONE genuinely-new-code item (RESEARCH Pattern 3) is **threading the active loss's (alpha, delta) into the Exact leaf call** — today `compute_leaf_deltas` hardcodes `QUANTILE_ALPHA`/`QUANTILE_DELTA` (boosting.rs:721). `cb_compute::leaf::exact_leaf_delta` is ALREADY alpha-general (leaf.rs:165; tested at alpha=0/0.5/weighted) — so this is wiring, not new algorithm. Re-expressing MAE through Quantile must keep the existing MAE Exact oracle byte-stable.
Output: 1 new `Loss::Quantile{alpha,delta}` variant, quantile_der1/2 + kernel, the Exact-alpha threading wiring, 2 fixtures (alpha=0.7 + the MAE-equivalent alpha=0.5 anchor), 1 passing oracle test.

Decisions honored: D-6.1-01 (MultiQuantile excluded — relocated to 6.2), D-6.1-02 (Wave 3 of the family-wave structure), D-6.1-03 (Loss::Quantile{alpha,delta} enum-param pattern), D-6.1-05 (reuse the alpha-general Exact leaf — exact_leaf_delta unchanged).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-regression-loss-matrix/06.1-CONTEXT.md
@.planning/phases/06.1-regression-loss-matrix/06.1-RESEARCH.md
@.planning/phases/06.1-regression-loss-matrix/06.1-PATTERNS.md
@crates/cb-compute/src/loss.rs
@crates/cb-train/src/boosting.rs
@.planning/phases/06.1-regression-loss-matrix/06.1-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**New `Loss` enum variant (`cb-compute::runtime::Loss`):**
- `Quantile { alpha: f64, delta: f64 }` (defaults alpha=0.5, delta=1e-6 — error_functions.h:468-469). MAE == Quantile{0.5, 1e-6}.

**New der1/der2 fns (`cb-compute::loss`):**
- `quantile_der1(approx,target,alpha,delta)` — generalizes `mae_der1` deadzone to arbitrary alpha; `quantile_der2` == `0`.
  (The existing `mae_der1` stays as-is or delegates to `quantile_der1(.., QUANTILE_ALPHA, QUANTILE_DELTA)` — MAE byte-stable.)

**New kernel (`cb-backend::kernels`):** `quantile_gradient_kernel<F: Float>` (alpha/delta as length-1 Array<F>); der2=0 → constant-vec in the dispatch arm.

**Wiring change (the one real-code item — RESEARCH Pattern 3):** `cb-train::boosting::compute_leaf_deltas` Exact branch threads the active loss's (alpha, delta) through to `exact_leaf_delta` instead of the hardcoded `QUANTILE_ALPHA`/`QUANTILE_DELTA` (boosting.rs:721). `exact_leaf_delta` (leaf.rs:165) is UNCHANGED — already alpha-general.

**Dispatch-site extensions:** `cb-backend::cpu_runtime::compute_gradients`, `cb-train::metrics::for_loss`, `cb-train::boosting::autolr_target_type`.

**New oracle fixtures:** `fixtures/quantile_alpha07/{config.json,model.json,staged.npy}` (alpha=0.7, leaf_estimation_method:Exact, iterations:1) + `fixtures/quantile_alpha05_mae/{config.json,model.json,staged.npy}` (alpha=0.5 — the MAE-equivalence anchor proving Quantile{0.5}==MAE).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 — generate Quantile alpha=0.7 + alpha=0.5(MAE-equiv) Exact-leaf fixtures (failing oracle scaffold)</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/fixtures/quantile_alpha07/, crates/cb-oracle/fixtures/quantile_alpha05_mae/, crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs</files>
  <read_first>
    - crates/cb-oracle/generator/gen_fixtures.py (numeric_tiny loader + save_model(json)/staged_predict pattern)
    - crates/cb-oracle/fixtures/leaf_methods/exact/config.json (the MAE-Exact fixture — the precise precedent: loss_function MAE, leaf_estimation_method Exact, iterations:1; the alpha=0.5 fixture is this scenario re-expressed as Quantile:alpha=0.5)
    - crates/cb-train/tests/leaf_methods_oracle_test.rs (the existing Exact-leaf oracle wiring to imitate)
    - .planning/phases/06.1-RESEARCH.md (Wave 3 table; Pitfall 5 der2=0+Newton; Exact-alpha free-reuse finding)
  </read_first>
  <what-built>
    Two scenarios via `.venv/bin/python` (catboost==1.2.10, thread_count=1) on the numeric_tiny corpus:
    - **quantile_alpha07**: `loss_function="Quantile:alpha=0.7"`, `leaf_estimation_method="Exact"` (so the Exact weighted-0.7-quantile leaf is exercised — the alpha!=0.5 path), `leaf_estimation_iterations:1`, depth 2, 5 iters, lr 0.1, l2 3.0, bootstrap No, random_strength 0, score_function L2, boost_from_average false, seed 0. config.json + model.json + staged.npy; stages=[Splits, LeafValues, StagedApprox].
    - **quantile_alpha05_mae**: `loss_function="Quantile:alpha=0.5"`, `leaf_estimation_method="Exact"`, iterations:1, identical isolating params. This is the MAE-equivalence anchor: Quantile{0.5} must reproduce the existing leaf_methods/exact (MAE) model bit-for-bit within <=1e-5.
    The oracle test `wave3_quantile_loss_oracle_test.rs` loads both + the existing leaf_methods/exact MAE fixture, and compare_stage (RED until Tasks 2-3).
  </what-built>
  <action>
    Binary fixtures MUST be generated offline with `.venv/bin/python crates/cb-oracle/generator/gen_fixtures.py` (only `.venv` has catboost==1.2.10). Pin `leaf_estimation_method:"Exact"` explicitly (Quantile defaults to Gradient upstream but the existing MAE oracle uses Exact and the Exact-alpha path is the thing under test — Pitfall 2; RESEARCH Wave 3 note). This is a `checkpoint:human-verify` (runs the .venv interpreter). If `.venv` catboost cannot run in-env, STOP and surface — do not fabricate bytes, #[ignore], or weaken the oracle.
  </action>
  <how-to-verify>
    1. Run `.venv/bin/python crates/cb-oracle/generator/gen_fixtures.py`.
    2. Confirm `fixtures/quantile_alpha07/model.json` + `fixtures/quantile_alpha05_mae/model.json` parse + carry 5 trees.
    3. Spot-check: quantile_alpha05_mae model.json leaf_values match the existing leaf_methods/exact (MAE) model.json within <=1e-5 (the MAE==Quantile{0.5} sanity, confirms the fixture is correctly configured).
    4. `cargo test -p cb-train --test wave3_quantile_loss_oracle_test` COMPILES and FAILS RED.
  </how-to-verify>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/quantile_alpha07/model.json && test -f crates/cb-oracle/fixtures/quantile_alpha05_mae/model.json</automated>
  </verify>
  <acceptance_criteria>
    - Both Quantile fixtures exist + committed; each config.json pins leaf_estimation_method:"Exact" + leaf_estimation_iterations:1 + thread_count:1 + isolating params.
    - quantile_alpha05_mae model leaf_values match the existing leaf_methods/exact (MAE) model <=1e-5 (MAE==Quantile{0.5} confirmed at fixture level).
    - `wave3_quantile_loss_oracle_test.rs` compiles + fails RED with NO #[ignore], standard 1e-5 tolerance.
  </acceptance_criteria>
  <done>Quantile alpha=0.7 + alpha=0.5(MAE-equiv) fixtures generated and committed; wave3 oracle test compiles and fails RED.</done>
  <resume-signal>Type "approved" once both Quantile fixtures are generated and the oracle test compiles+fails RED, or describe the blocker.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quantile der1/der2 + Loss::Quantile variant + kernel + dispatch</name>
  <files>crates/cb-compute/src/runtime.rs, crates/cb-compute/src/loss.rs, crates/cb-compute/src/loss_test.rs, crates/cb-backend/src/kernels.rs, crates/cb-backend/src/cpu_runtime.rs, crates/cb-backend/src/cpu_runtime_test.rs</files>
  <read_first>
    - crates/cb-compute/src/loss.rs (mae_der1/der2 loss.rs:161 — the EXACT deadzone Quantile generalizes; QUANTILE_ALPHA/QUANTILE_DELTA consts loss.rs:150-152; focal_der1 loss.rs:117 parametric-arg signature)
    - crates/cb-compute/src/runtime.rs (Loss enum runtime.rs:47; Focal{..} parametric precedent runtime.rs:63; PartialEq-only derive runtime.rs:46)
    - crates/cb-backend/src/kernels.rs (mae_gradient_kernel kernels.rs:60 deadzone if-as-statement; focal_gradient_kernel kernels.rs:104 length-1 Array<F> param)
    - crates/cb-backend/src/cpu_runtime.rs (Loss::Mae constant-der2 arm cpu_runtime.rs:231 — Quantile copies)
    - catboost-master/catboost/private/libs/algo_helpers/error_functions.h:457-498 (TQuantileError, alpha/delta defaults at :468-469)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (MANDATORY before kernel code)
  </read_first>
  <behavior>
    - `quantile_der1(approx,target,alpha,delta)`: val=target-approx; `|val|<delta ? 0 : (val>0 ? alpha : -(1-alpha))`; `quantile_der2` == `0`. (error_functions.h:457-498) — at alpha=0.5,delta=1e-6 it equals `mae_der1`/`mae_der2`.
    - Unit tests: quantile_der1 at alpha=0.7 (positive residual -> 0.7, negative -> -0.3, |val|<delta -> 0); and the equivalence `quantile_der1(a,t,0.5,1e-6) == mae_der1(a,t)` at sample points.
  </behavior>
  <action>
    Add `Loss::Quantile { alpha: f64, delta: f64 }` to runtime.rs copying the Focal parametric shape; doc-comment cites error_functions.h:457-498 + the 0.5/1e-6 defaults (:468-469); keep PartialEq-only (NO Eq). Transcribe `quantile_der1`/`quantile_der2` into loss.rs in the rmse_der1 citation style, generalizing the existing `mae_der1` deadzone to take `alpha`/`delta` args. Re-express the existing `mae_der1`/`mae_der2` to delegate to `quantile_der1(approx,target,QUANTILE_ALPHA,QUANTILE_DELTA)` (or leave mae_der1 byte-identical and assert equivalence in tests) — MAE behavior MUST stay byte-stable so the existing leaf_methods/exact oracle does not move. Add `quantile_gradient_kernel<F: Float>` (alpha/delta as length-1 Array<F>; mae_gradient_kernel deadzone precedent); der2=0. Extend `compute_gradients` (cpu_runtime.rs:212) with a `Loss::Quantile { alpha, delta } =>` arm: launch the gradient kernel + fill a constant-0 der2 vec (Mae constant-der2 precedent). On ANY CubeCL build error, load cubecl_error_guideline.md FIRST.
  </action>
  <verify>
    <automated>cargo test -p cb-compute loss</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-compute` and `cargo test -p cb-backend` exit 0.
    - `grep -n "pub fn quantile_der1" crates/cb-compute/src/loss.rs` returns the fn; a unit test asserts `quantile_der1(a,t,0.5,1e-6) == mae_der1(a,t)`.
    - MAE der1/der2 behavior unchanged (existing cb-compute/loss tests still pass).
    - Loss enum stays PartialEq-only (no Eq); range guard `0<=alpha<=1`, `delta>=0` -> typed CbError; no `unwrap()`; tests in loss_test.rs.
  </acceptance_criteria>
  <done>Quantile{alpha,delta} variant + der1/der2 (generalizing MAE) + kernel + dispatch; MAE byte-stable; cb-compute/cb-backend green.</done>
</task>

<task type="auto">
  <name>Task 3: Thread loss (alpha,delta) into the Exact leaf + metric/autolr dispatch → Wave-3 oracle GREEN</name>
  <files>crates/cb-train/src/boosting.rs, crates/cb-train/src/boosting_test.rs, crates/cb-train/src/metrics.rs, crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/src/boosting.rs (compute_leaf_deltas Exact branch boosting.rs:709-723 — the HARDCODED QUANTILE_ALPHA/QUANTILE_DELTA at boosting.rs:721 to parameterize; the QUANTILE_ALPHA/QUANTILE_DELTA import boosting.rs:30; autolr_target_type match boosting.rs:637; params.loss access pattern)
    - crates/cb-compute/src/leaf.rs (exact_leaf_delta leaf.rs:165 — ALREADY alpha-general, signature `exact_leaf_delta(residuals,weights,alpha,delta)`; DO NOT modify — RESEARCH free-reuse finding)
    - crates/cb-compute/src/leaf_test.rs (leaf_test.rs:106-119 — proves exact_leaf_delta works at alpha=0/0.5/weighted)
    - crates/cb-train/src/metrics.rs (for_loss exhaustive match metrics.rs:59)
    - crates/cb-train/tests/loss_oracle_test.rs (train->compare_stage scaffold)
  </read_first>
  <action>
    The ONE real-code item (RESEARCH Pattern 3): in `compute_leaf_deltas` (boosting.rs:667), the `LeafMethod::Exact` branch (boosting.rs:709-723) currently calls `exact_leaf_delta(r, w, QUANTILE_ALPHA, QUANTILE_DELTA)` with the hardcoded constants. Parameterize it to thread the active loss's (alpha, delta): destructure `params.loss` — for `Loss::Quantile { alpha, delta }` pass those values, for `Loss::Mae` pass `(QUANTILE_ALPHA, QUANTILE_DELTA)` (== the prior behavior, so MAE Exact is byte-identical), and for any non-quantile loss the Exact branch keeps the existing default (preserve current behavior — these losses are pinned to Newton/Gradient anyway). `exact_leaf_delta` (leaf.rs:165) is UNCHANGED — it is already alpha-general. Extend `autolr_target_type` (boosting.rs:637) and `metrics::for_loss` (metrics.rs:59) for `Loss::Quantile{..}` (TargetType::Unknown; for_loss -> the Quantile/RMSE-arm default, mirroring `Loss::Mae`). Add a boosting_test.rs unit test asserting the Exact branch passes alpha=0.7 for `Loss::Quantile{alpha:0.7,..}` (falsifiable: a regression to the hardcoded 0.5 fails it). Wire the Wave-3 oracle: train under Quantile{0.7} and Quantile{0.5}, assert compare_stage <=1e-5 for Splits/LeafValues/StagedApprox; additionally assert the Quantile{0.5} model matches the existing leaf_methods/exact (MAE) fixture <=1e-5 (MAE==Quantile{0.5}). Flip RED->GREEN. `rg "match .*loss|Loss::" crates/*/src` for exhaustive coverage; any new sum via cb_core::sum_f64.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test wave3_quantile_loss_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train --test wave3_quantile_loss_oracle_test` exits 0 — Quantile{0.7} and Quantile{0.5} pass Splits/LeafValues/StagedApprox <=1e-5; Quantile{0.5} == leaf_methods/exact (MAE) <=1e-5.
    - `grep -n "QUANTILE_ALPHA" crates/cb-train/src/boosting.rs` shows the Exact branch no longer UNCONDITIONALLY hardcodes it — it is now threaded from params.loss (the constant is used only as the Mae default).
    - boosting_test.rs has a unit test proving the Exact branch threads alpha=0.7 (not 0.5) for Loss::Quantile{0.7}.
    - `cargo test -p cb-train` green — existing ~40 + Wave-1 + Wave-2 oracles unchanged (MAE Exact byte-stable; leaf.rs untouched).
    - exact_leaf_delta in leaf.rs is UNCHANGED (free reuse — no diff to leaf.rs).
  </acceptance_criteria>
  <done>Quantile family GREEN <=1e-5 with alpha-threaded Exact leaf; MAE==Quantile{0.5} confirmed; leaf.rs unchanged; all prior oracles green. LOSS-03 scalar matrix complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hyperparameter -> Loss::Quantile | alpha outside [0,1] or delta<0 yields an ill-defined quantile der/leaf |
| loss (alpha,delta) -> exact_leaf_delta | A mis-threaded alpha (e.g. stuck at 0.5) silently computes the wrong quantile leaf — a parity bug, not a crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1.03-01 | Tampering | Quantile alpha outside [0,1] / delta<0 | mitigate | Range-check 0<=alpha<=1, delta>=0 in the Loss constructor (error_functions.h:479-480) -> typed CbError; no unwrap/panic |
| T-06.1.03-02 | Spoofing (silent parity bug) | Exact branch threading the wrong alpha (regression to hardcoded 0.5) | mitigate | boosting_test.rs unit test asserts alpha=0.7 is threaded for Loss::Quantile{0.7}; the alpha=0.7 oracle <=1e-5 fails if the leaf uses 0.5; the alpha=0.5 anchor proves MAE byte-stability |
| T-06.1.03-03 | Tampering | NaN leaf from degenerate weighted quantile | accept | exact_leaf_delta already guards degenerate/empty leaves (leaf.rs:165, tested leaf_test.rs:106-119); compare_stage NaN-rejects |
| T-06.1.03-SC | Tampering | package installs | mitigate | No new external packages (RESEARCH Package Legitimacy Audit: N/A) — internal code + pinned .venv catboost==1.2.10 generator |
</threat_model>

<verification>
- `cargo test -p cb-compute` / `-p cb-backend` — Quantile der1/der2 + kernel green; MAE==Quantile{0.5} unit equivalence.
- `cargo test -p cb-train --test wave3_quantile_loss_oracle_test` — Quantile{0.7}/{0.5} per-stage <=1e-5 + MAE equivalence.
- `cargo test -p cb-train` — existing ~40 + Wave-1 + Wave-2 oracles green.
- Verify per-crate (disk pressure, Memory).
</verification>

<success_criteria>
- Quantile{alpha,delta} trains and matches upstream <=1e-5 per-stage with the Exact weighted-alpha-quantile leaf (SC#1 — Wave 3 share; LOSS-03 scalar matrix complete).
- MAE == Quantile{0.5} confirmed (existing MAE Exact oracle byte-stable).
- der1/der2 transcribed+cited; the Exact-alpha threading is the only real-code change; leaf.rs unchanged (free reuse, SC#2).
- All prior oracles green; no behavior change to existing losses (SC#3).
</success_criteria>

<output>
Create `.planning/phases/06.1-regression-loss-matrix/06.1-03-SUMMARY.md` when done.
</output>
