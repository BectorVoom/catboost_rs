---
phase: 6.1
slug: regression-loss-matrix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-16
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` + `cb-oracle` `compare_stage` ≤1e-5 harness; frozen JSON fixtures from catboost 1.2.10 (`.venv`, `thread_count=1`) |
| **Config file** | none — per-crate `Cargo.toml` test targets; oracle fixtures under `crates/cb-oracle/` |
| **Quick run command** | `cargo test -p cb-compute` (der1/der2 self-oracles for the loss under change) |
| **Full suite command** | `cargo test -p cb-compute -p cb-train -p cb-oracle` (per-loss per-stage oracles + existing ~40 scalar oracles stay green) |
| **Estimated runtime** | ~per-crate; full oracle pass minutes (disk-pressure: verify per-crate, see memory) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p cb-compute` (the loss/der self-oracle)
- **After every plan wave:** Run the per-wave loss oracle + `cargo test -p cb-train -p cb-oracle`
- **Before `/gsd-verify-work`:** Full suite green AND existing ~40 scalar oracles (Phases 3–5) still green (SC#3)
- **Max feedback latency:** per-crate test runtime

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by planner — one row per loss × {der self-oracle, per-stage train oracle}) | — | — | LOSS-03 | — / — | N/A | unit + oracle | `cargo test -p cb-compute` / `cargo test -p cb-oracle` | ❌ W0 (fixtures) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Per-loss frozen oracle fixtures generated from catboost 1.2.10 (`thread_count=1`, `leaf_estimation_method` + `leaf_estimation_iterations:1` pinned per fixture) — splits/leaves/staged-approx + final prediction.
- [ ] Per-loss der1/der2 self-oracle reference values transcribed from `error_functions.{h,cpp}`.
- [ ] MSLE metric fixture (eval_metric only — D-6.1-06; no training oracle).

*Existing `cb-oracle` infrastructure covers the harness; only new per-loss fixtures are Wave-0 work.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| (none expected) | LOSS-03 | — | All parity behaviors have automated per-stage oracles |

*All phase behaviors have automated verification (oracle-driven).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (per-loss fixtures)
- [ ] No watch-mode flags
- [ ] Feedback latency < per-crate test runtime
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
