---
phase: 06.1-regression-loss-matrix
plan: 02
subsystem: training
tags: [poisson, tweedie, mape, msle, exp-link, loss-functions, gradient-boosting, cubecl, oracle-parity]

# Dependency graph
requires:
  - phase: 06.1-regression-loss-matrix
    plan: 01
    provides: "Loss enum + der1/der2 transcription pattern, generics-float kernel + cpu_runtime dispatch, per-stage oracle harness, ParamKernel single-scalar launcher"
  - phase: 03-scalar-boosting
    provides: "Newton/Gradient leaf methods, per-stage oracle harness (compare_stage), EvalMetric eval surface"
  - phase: 04-model-inference
    provides: "cb_model::apply_prediction_type / PredictionType::Exponent (oracle-locked exp(RawFormulaVal))"
provides:
  - "Loss::{Poisson, Tweedie{variance_power}, Mape} — the positive-domain / link regression losses"
  - "6 transcribed+cited der1/der2 fns in cb-compute::loss (Poisson inline-exp, Tweedie exp-in-der, MAPE der2=0)"
  - "5 generics-float #[cube] kernels (poisson grad+hess, tweedie grad+hess, mape grad) + cpu_runtime dispatch arms"
  - "EvalMetric::Msle — MSLE as an eval-metric ONLY (D-6.1-06; NOT a Loss variant)"
  - "Tweedie (1,2) range-guard arm in Loss::validate"
  - "wave2_positive_loss_oracle_test (Poisson raw+exp / Tweedie raw / MAPE per-stage <=1e-5) + msle_metric_oracle_test (MSLE metric <=1e-5)"
affects: [06.1-03-quantile-losses, regression-loss-matrix]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inline-link der (Poisson exp / Logloss sigmoid precedent): compute exp() INLINE on the RAW approx in the der; additive raw update; Exponent predict transform — NO exp-approx storage"
    - "exp-in-der raw-approx loss (Tweedie): exp lives inside the der formula directly; the prediction is the RAW approx (NO Exponent transform)"
    - "Metric-only loss member (MSLE): EvalMetric variant + eval arm with no Loss variant and no for_loss default — selected only via explicit eval_metric"

key-files:
  created:
    - "crates/cb-oracle/fixtures/{poisson,tweedie,mape}/ - Wave-2 training fixtures"
    - "crates/cb-oracle/fixtures/msle_metric/ - MSLE eval-metric fixture (RMSE-trained model + MSLE history)"
    - "crates/cb-train/tests/wave2_positive_loss_oracle_test.rs - Poisson/Tweedie/MAPE per-stage oracle"
    - "crates/cb-train/tests/msle_metric_oracle_test.rs - MSLE metric oracle"
  modified:
    - "crates/cb-compute/src/runtime.rs - Loss::{Poisson,Tweedie,Mape} variants + Tweedie (1,2) validate arm"
    - "crates/cb-compute/src/loss.rs - poisson_der1/2 (inline exp), tweedie_der1/2 (exp-in-der), mape_der1/2 (der2=0)"
    - "crates/cb-backend/src/kernels.rs - poisson_gradient/hessian, tweedie_gradient/hessian, mape_gradient #[cube] kernels"
    - "crates/cb-backend/src/cpu_runtime.rs - PoissonGradient/MapeGradient binary arms, launch_poisson_hessian, ParamKernel::Tweedie, 3 dispatch arms"
    - "crates/cb-train/src/metrics.rs - EvalMetric::Msle + eval arm + for_loss extension"
    - "crates/cb-oracle/generator/gen_fixtures.py - gen_wave2_positive_losses + gen_msle_metric + --wave2-only entrypoint"

key-decisions:
  - "Open Q1 RESOLVED empirically (catboost 1.2.10): Poisson StagedApprox(RawFormulaVal) is RAW approx; Predictions = exp(raw), matching the default predict to 8.88e-16 (== PredictionType::Exponent). StagedApprox oracle compares RAW; Predictions applies Exponent (A2 / Pitfall 4)."
  - "Tweedie predictions are RAW (A4 confirmed): the default predict equals RawFormulaVal; the exp lives inside the der, so NO Exponent transform is applied."
  - "MSLE is metric-only (D-6.1-06): added to EvalMetric ONLY, with NO Loss variant and NO for_loss default. The msle_metric fixture trains RMSE with eval_metric=MSLE and captures the per-iteration eval history; there is no MSLE-as-objective model (it throws upstream)."
  - "Positive-target transform y_pos = y - min(y) + 1.0 (additive shift recorded per fixture) — Poisson/Tweedie/MAPE require a strictly-positive target; the Rust oracle reproduces the identical positive label column from config.json target_shift."
  - "Poisson/Tweedie pin leaf_estimation_iterations:1 (override upstream Poisson default 10 — Pitfall 3); MAPE pins Gradient (der2=0 so Newton is undefined — Pitfall 5)."

patterns-established:
  - "Inline-link der equivalence extended from Logloss sigmoid to Poisson exp: raw approx storage + inline exp() der + Exponent predict, no exp-approx machinery"
  - "ParamKernel reused for Tweedie (variance_power as length-1 device Array<F>), keeping the kernel generic over F: Float"

requirements-completed: [LOSS-03]

# Metrics
duration: 12min
completed: 2026-06-16
---

# Phase 06.1 Plan 02: Positive-Domain / Link Regression Losses Summary

**Poisson (exp-link), Tweedie{variance_power}, and MAPE land end-to-end on the scalar boosting loop — der1/der2 transcribed from error_functions.h, five generics-float CubeCL kernels, Poisson's raw-approx + inline-exp + Exponent-predict equivalence (the Logloss sigmoid precedent) — each oracle-locked <=1e-5 per-stage against upstream catboost 1.2.10; MSLE implemented as an eval-metric only (D-6.1-06), oracle-locked as a metric <=1e-5.**

## Performance

- **Duration:** ~12 min
- **Started (Wave-0 commit):** 2026-06-16T05:15:58Z
- **Completed:** 2026-06-16T05:28:00Z
- **Tasks:** 4/4
- **Files modified/created:** 31 (7 production source, 2 oracle tests, 16 fixture artifacts, 1 generator, 5 test-helper extensions overlapping)

## Accomplishments
- Three positive-domain `Loss` variants (`Poisson`, `Tweedie{variance_power}`, `Mape`) wired through the full pipeline: enum -> der1/der2 -> kernel -> dispatch -> per-stage oracle.
- `wave2_positive_loss_oracle_test` GREEN: Poisson raw StagedApprox + exp Predictions (via production `PredictionType::Exponent`), Tweedie raw predictions, MAPE Splits/LeafValues/StagedApprox — all <=1e-5.
- Resolved **Open Q1** empirically: Poisson StagedApprox(RawFormulaVal) is RAW; Predictions = `exp(raw)` matches the default predict to 8.88e-16. Recorded the finding in `poisson/config.json`.
- Confirmed **A4**: Tweedie predictions are RAW (no Exponent) — the link is internal to the der.
- `EvalMetric::Msle` added as a **metric-only** member (no Loss variant, no `for_loss` default); `msle_metric_oracle_test` GREEN at <=1e-5 against upstream per-iteration MSLE eval history.
- The Wave-1 smooth-loss oracles (4/4) plus the full existing cb-train suite (lib 139 + every existing oracle integration suite) stay green — no behavior change to existing losses.

## Task Commits

1. **Task 1: Wave-0 fixtures + failing oracle tests (RED)** - `fa4e664` (test)
2. **Task 2: der1/der2 + Loss variants + kernels + dispatch** - `bb3202f` (feat)
3. **Task 3: Poisson exp-link predict + leaf/metric dispatch -> Wave-2 GREEN** - `2a39193` (feat)
4. **Task 4: MSLE eval-metric (metric-only) -> MSLE oracle GREEN** - `d554828` (feat)

## Files Created/Modified
- `crates/cb-compute/src/runtime.rs` - `Loss::Poisson` (non-param, inline exp-link), `Loss::Tweedie{variance_power}` (param, exp-in-der raw), `Loss::Mape` (non-param, der2=0); Tweedie `(1,2)` range-guard arm in `Loss::validate`. Loss stays PartialEq-only.
- `crates/cb-compute/src/loss.rs` - `poisson_der1/2` (`target - approx.exp()` / `-approx.exp()`, inline), `tweedie_der1/2` (`exp` inside the der over raw approx), `mape_der1/2` (`sign/max(1.0,|t|)` / `0`), each with its exact `error_functions.h:<line>` citation.
- `crates/cb-compute/src/loss_test.rs` - Self-oracle unit tests for all 6 new der fns (Poisson exp at known approx, Tweedie at p=1.5, MAPE divisor boundary + sign + tie).
- `crates/cb-compute/src/lib.rs` - Re-exports for the 6 new der fns.
- `crates/cb-backend/src/kernels.rs` - `poisson_gradient_kernel`/`_hessian_kernel` (`F::exp` inline), `tweedie_gradient_kernel`/`_hessian_kernel` (length-1 `Array<F>` variance_power), `mape_gradient_kernel` (if-as-statement sign branch, `F::max`/`F::abs`).
- `crates/cb-backend/src/cpu_runtime.rs` - `BinaryKernel::{PoissonGradient,MapeGradient}`, `launch_poisson_hessian` (unary), `ParamKernel::Tweedie`, three `compute_gradients` dispatch arms (MAPE der2=0 vec).
- `crates/cb-backend/src/cpu_runtime_test.rs` - Host-reference der1/der2 tests for Poisson/Tweedie/MAPE.
- `crates/cb-train/src/boosting.rs` - `autolr_target_type` arm for the three new variants (-> `TargetType::Unknown`).
- `crates/cb-train/src/metrics.rs` - `for_loss` extension (Poisson/Tweedie/MAPE -> RMSE eval surface); `EvalMetric::Msle` + the eval arm (`mean_w((log(1+approx)-log(1+target))^2)`, sum via `cb_core::sum_f64`, log-domain guard -> typed `CbError`).
- `crates/cb-train/src/metrics_test.rs` - MSLE hand-computed (unweighted/weighted/exact-match), log-domain-violation error, `for_loss`-never-MSLE.
- `crates/cb-oracle/generator/gen_fixtures.py` - `gen_wave2_positive_losses` (poisson/tweedie/mape) + `gen_msle_metric`, positive-target transform helper, Open-Q1 empirical probe, `--wave2-only` entrypoint.
- `crates/cb-oracle/fixtures/{poisson,tweedie,mape,msle_metric}/` - committed frozen fixtures (RUN-ONCE / D-12).
- `crates/cb-train/tests/wave2_positive_loss_oracle_test.rs`, `crates/cb-train/tests/msle_metric_oracle_test.rs` - the two Wave-2 oracles, authored GREEN.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Three existing oracle-test `match loss` helpers broke at compile time**
- **Found during:** Task 3 (`cargo test -p cb-train` failed: non-exhaustive `match loss` in `leaf_methods_oracle_test.rs`, `slice_first_oracle_test.rs`, `autolr_e2e_test.rs`).
- **Issue:** Adding the three `Loss` variants made test-side exhaustive matches (loss -> target column, loss -> TargetType) non-exhaustive — the same pattern as Wave-1's deviation 3.
- **Fix:** Extended each match — Poisson/Tweedie/MAPE map to `load_regression_target()` (regression family) and `TargetType::Unknown` (not in the auto-LR table). No oracle assertion weakened.
- **Files modified:** the three test files above.
- **Commit:** `2a39193`

No architectural changes (Rule 4) were required. No auto-fixed bugs (Rule 1) or auto-added critical functionality (Rule 2) beyond the threat-register mitigations already specified in the plan (Tweedie range guard, MSLE log-domain guard) — both were implemented as planned.

## Authentication Gates

None — fixture generation ran offline against the in-repo `.venv` (catboost 1.2.10), as designed.

## Verification

- `cargo test -p cb-compute` — 66 passed (incl. 6 new der self-oracles).
- `cargo test -p cb-backend` — 16 passed (incl. 3 new Poisson/Tweedie/MAPE kernel host-reference tests).
- `cargo test -p cb-train --test wave2_positive_loss_oracle_test` — 3/3 GREEN (<=1e-5; Poisson raw StagedApprox + exp Predictions via Exponent, Tweedie raw, MAPE per-stage), no `#[ignore]`, tolerance unweakened.
- `cargo test -p cb-train --test msle_metric_oracle_test` — 1/1 GREEN (per-iteration MSLE <=1e-5).
- `cargo test -p cb-train --test wave1_smooth_loss_oracle_test` — 4/4 GREEN (Wave-1 unchanged).
- `cargo test -p cb-train` — full suite green (lib 139 + every existing oracle integration suite).
- `cargo build -p catboost-rs` — workspace top crate compiles.
- Per disk pressure (Memory), verification was per-crate; no `cargo test --workspace` run.

## Acceptance Criteria

- [x] Poisson / Tweedie{p=1.5} / MAPE each train and match upstream <=1e-5 per-stage incl. Poisson exp-link Predictions via `PredictionType::Exponent` (SC#1 Wave-2 share).
- [x] MSLE implemented as `eval_metric` only, oracle-locked as a metric, NOT a trainable Loss (SC#1 MSLE clause, D-6.1-06); `grep Msle` returns nothing in `runtime.rs`, the EvalMetric variant + eval arm in `metrics.rs`.
- [x] 6 der1/der2 transcribed + cited from error_functions.h; Poisson computes `approx.exp()` inline (no exp-approx storage); Tweedie uses exp INSIDE the der and is NOT routed to Exponent; MSLE sum routes through `cb_core::sum_f64` (SC#2).
- [x] Loss enum stays PartialEq-only; Tweedie range guard rejects `variance_power` outside `(1,2)`; MSLE log-domain guard returns typed `CbError`; no `unwrap()` in new production code.
- [x] Tests live in `*_test.rs` files (source/test separation); no inline `#[cfg(test)]` in production source.
- [x] Every exhaustive `Loss` match site handles the three new variants; no `_ =>` catch-all hiding a missed site.
- [x] Existing + Wave-1 oracles green; no behavior change to existing losses (SC#3).

## Threat Surface Scan

No new security-relevant surface beyond the plan's `<threat_model>`. The three planned `mitigate` dispositions were implemented:
- T-06.1.02-02 (Tweedie variance_power range): `Loss::validate` rejects outside `(1,2)` -> typed `CbError`.
- T-06.1.02-03 (MSLE log domain): `EvalMetric::Msle.eval` guards `1+approx`/`1+target > 0` -> typed `CbError`, never NaN/panic.
- T-06.1.02-01 (Poisson exp overflow): the positive-target fixture keeps the raw approx in range; `compare_stage` NaN-rejects any overflow.
- T-06.1.02-04 (MAPE divisor): `max(1.0, |target|) >= 1.0` by construction — division is always safe (accept).

## Self-Check: PASSED

- All modified/created production files exist on disk (runtime.rs, loss.rs, kernels.rs, cpu_runtime.rs, metrics.rs; poisson/tweedie/mape/msle_metric fixtures).
- All four task commits found in git history (`fa4e664`, `bb3202f`, `2a39193`, `d554828`).
- `poisson_der1` + `Loss::Poisson` confirmed in production source; `Msle` confirmed absent from runtime.rs and present in metrics.rs.
- SUMMARY.md created at `.planning/phases/06.1-regression-loss-matrix/06.1-02-SUMMARY.md`.
