---
phase: 06.5-text-and-embedding-features
plan: 05
type: execute
wave: 5
depends_on: ["06.5-04"]
files_modified:
  - crates/cb-compute/src/lda_linalg.rs
  - crates/cb-compute/src/lda_linalg_test.rs
  - crates/cb-compute/src/embedding_calcers.rs
  - crates/cb-compute/src/embedding_calcers_test.rs
  - crates/cb-compute/src/lib.rs
  - crates/cb-train/src/estimated/online_embedding.rs
  - crates/cb-train/src/estimated/online_embedding_test.rs
  - crates/cb-train/src/estimated/estimated_features.rs
  - crates/cb-train/src/estimated/mod.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/tests/lda_oracle_test.rs
autonomous: false
requirements: [FEAT-02]
user_setup: []

must_haves:
  truths:
    - "An instrumented-GT spike dumps the upstream LDA projection matrix + intermediate scatter + per-document projected features, and the hand-rolled-f32 vs LAPACK/nalgebra decision follows from REAL divergence numbers (not assumption) — any external linear-algebra crate is gated behind a blocking checkpoint:human-verify"
    - "LDA incremental scatter (per-class IncrementalCloud) + generalized symmetric eigen projection + cblas_sgemv project are transcribed verbatim, computed as an IOnlineFeatureEstimator over the TFold learn permutation (D-03)"
    - "LDA projected features match upstream ≤1e-5; if exact f32 reproduction is provably infeasible a documented-tolerance escalation gets human sign-off (escalate-don't-weaken — never silently weaken)"
  artifacts:
    - path: "crates/cb-compute/src/lda_linalg.rs"
      provides: "f32 GEMM + generalized symmetric eigensolver (hand-roll or vetted LAPACK binding)"
    - path: "crates/cb-compute/src/embedding_calcers.rs"
      provides: "LDA LinearDA calcer (scatter, projection, project)"
      contains: "lda"
    - path: "crates/cb-train/src/estimated/online_embedding.rs"
      provides: "per-fold ordered prefix loop for LDA (mirror ctr/online.rs)"
  key_links:
    - from: "crates/cb-compute/src/embedding_calcers.rs"
      to: "crates/cb-compute/src/lda_linalg.rs"
      via: "CalculateProjection (ssygst/ssyev analog) + sgemv project"
      pattern: "lda_linalg"
    - from: "crates/cb-oracle/tests/lda_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/embedding_calcers/LDA/ + instrumented projection dump"
      via: "compare_stage ≤1e-5 / projection-matrix compare"
      pattern: "compare"
---

<objective>
First embedding calcer — LDA (Linear Discriminant Analysis), spike-led because it is the PRIMARY FEAT-02 landmine (A1). LDA solves a generalized symmetric eigenproblem via single-precision (f32) LAPACK (ssygst_/ssyev_); the upstream LDA fixture itself rounds to 2 decimals, so bit-stability across BLAS/LAPACK implementations is uncertain. The first task is an instrumented-GT spike that drives the hand-rolled-f32 vs LAPACK-binding decision from REAL divergence numbers.

Purpose: LDA is the harder of the two embedding calcers' numerics but lands before KNN (the highest-risk). The eigensolver has no workspace analog — the decision (hand-roll f32 vs vetted LAPACK crate vs documented tolerance) MUST follow from instrumented measurements, not a guess. Any external linear-algebra crate is gated behind a blocking checkpoint:human-verify (A3, package legitimacy).
Output: lda_linalg.rs (eigensolver) + LDA calcer + online-embedding prefix wiring + LDA per-stage + projection-matrix oracle ≤1e-5 (or a human-signed-off documented tolerance).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
</context>

## Phase Goal

**As a** Rust developer embedding catboost-rs, **I want to** train models with text and embedding features whose encodings match upstream catboost 1.2.10 to within 1e-5, **so that** existing CatBoost text/embedding pipelines port over with verifiable parity.

## Acceptance Criteria
- [ ] LDA projection matrix + projected features compared vs instrumented GT; eigensolver decision driven by real divergence numbers
- [ ] LDA computed as an online estimator over the TFold learn permutation (D-03)
- [ ] LDA per-stage oracle ≤1e-5, or a human-signed-off documented tolerance (escalate-don't-weaken)

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: LDA instrumented-GT SPIKE + eigensolver decision (hand-roll f32 vs LAPACK crate vs documented tolerance)</name>
  <read_first>
    - catboost-master/catboost/private/libs/embedding_features/lda.cpp (CalculateProjection: ssygst_/ssyev_ f32, trailing-rows projection, IncrementalCloud scatter, cblas_sgemm/sgemv — lines 10-205, TRANSCRIPTION SOURCE + the spike target)
    - catboost-master/catboost/private/libs/embedding_features/lda.h (IncrementalCloud, ProjectionMatrix)
    - catboost-master/catboost/private/libs/feature_estimator/embedding_feature_estimators.cpp (ProjectionDim default = min(nClasses-1, dim-1), reg default 0.00005; fixtures reg=0.05 likelihood=true — lines 22-27)
    - crates/cb-oracle/fixtures/embedding_calcers/LDA/ + the instrumented LDA projection/scatter dump (Plan 01, D-07)
    - crates/cb-compute/src/pairwise_scoring.rs (ANALOG: closest matrix-stat reduction-style sibling for sum_f64/checked-access; NO existing eigensolver)
    - .planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md (Pitfall 1, Open Question 1, Package Legitimacy Audit, A1/A3)
  </read_first>
  <decision>How to reproduce the upstream f32 LAPACK generalized symmetric eigensolver (ssygst_+ssyev_) for LDA's CalculateProjection within ≤1e-5 on the projected feature values</decision>
  <context>
    Run the instrumented spike FIRST: dump the upstream projection matrix, intermediate scatter matrices, and per-document projected features from the Plan-01 D-07 instrumentation. Implement a candidate hand-rolled f32 path and measure ACTUAL divergence vs the dump. Only then choose. Do NOT add any external crate before this measurement. The upstream LDA fixture rounds to 2 decimals, so per-stage parity must be judged on the instrumented per-document projected feature values, not the rounded learn-error metric.
  </context>
  <options>
    <option id="hand-roll-f32">
      <name>Hand-rolled f32 GEMM + generalized symmetric eigensolver</name>
      <pros>Zero new dependency (D-05 spirit); full summation-order control via sum_f64; small matrices (e.g. 50x50); no platform/BLAS variance</pros>
      <cons>Reproducing f32 ssyev sign/ordering/low-bits exactly is hard; eigenvector sign + degenerate-eigenvalue ordering freedom</cons>
    </option>
    <option id="lapack-crate">
      <name>Vetted LAPACK/nalgebra crate behind checkpoint:human-verify</name>
      <pros>Reproduces f32 ssyev arithmetic closely if it binds the same routine</pros>
      <cons>New dependency (A3 — MUST be cargo-search + source-verified + human-approved before any Cargo.toml edit); platform variance; nalgebra is f64 != upstream f32</cons>
    </option>
    <option id="documented-tolerance">
      <name>Documented LDA-specific tolerance escalation (human sign-off)</name>
      <pros>Honest if exact f32 reproduction is provably infeasible (upstream itself rounds to 2 decimals)</pros>
      <cons>Weakens the ≤1e-5 bar for LDA only; requires explicit human sign-off; never silent</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — spike measurement; the chosen path's oracle is gated in Task 2/3. The spike produces a divergence report compared against fixtures/embedding_calcers/LDA/ + the instrumented projection dump.</automated>
    <human-check>Divergence numbers from the hand-roll-f32 candidate vs the instrumented projection/feature dump are presented; the eigensolver path is chosen from real measurements; if a LAPACK/nalgebra crate is selected it is cargo-search + source-verified first</human-check>
  </verify>
  <resume-signal>Select: hand-roll-f32, lapack-crate (name the verified crate), or documented-tolerance (with rationale)</resume-signal>
  <acceptance_criteria>A divergence report (hand-roll-f32 candidate vs instrumented LDA projection + per-document features) exists; the chosen path is recorded with the measured numbers; any external crate is verified on crates.io + source repo before selection</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LDA linear algebra + LinearDA calcer (per chosen eigensolver path)</name>
  <read_first>
    - crates/cb-compute/src/lda_linalg.rs + embedding_calcers.rs (being created)
    - catboost-master/catboost/private/libs/embedding_features/lda.cpp (IncrementalCloud::Update f32 GEMM rank update; TotalScatterCalculation; betweenMatrix diag += reg; CalculateProjection; sgemv project — lines 10-205, TRANSCRIPTION SOURCE)
    - catboost-master/catboost/private/libs/embedding_features/embedding_calcers.h (LDA calcer, FeatureCount = ProjectionDim (+nClasses if probs))
    - crates/cb-compute/src/score.rs (ANALOG: sum_f64 routing + module-doc-with-cpp-line style, lines 1-85)
    - crates/cb-core/src/reduction.rs (sum_f64 line 32)
    - crates/cb-compute/src/lib.rs (module + pub use + _test sibling wiring)
  </read_first>
  <behavior>
    - lda_linalg: f32 GEMM (small dims) + generalized symmetric eigen via the Task-1-chosen path; eigenvector trailing-rows = projection
    - lda calcer Compute: proj = ProjectionMatrix * embed (sgemv); width = ProjectionDim (+nClasses if computeProbabilities)
    - IncrementalCloud per class: BaseCenter running mean (shifted), ScatterMatrix rank updates flushed via GEMM
    - dim-mismatch embedding vector -> typed CbError (CB_ENSURE analog, helpers.h:21)
  </behavior>
  <action>
    Create lda_linalg.rs implementing the Task-1-chosen eigensolver + f32 GEMM/GEMV, transcribed verbatim from lda.cpp (D-04). Create the LDA arm in embedding_calcers.rs: per-class IncrementalCloud (BaseCenter shifted running mean, ScatterMatrix), TotalScatterCalculation, betweenMatrix = Σ_c w_c*classScatter_c with diagonal += RegParam (default 0.00005; fixtures 0.05), CalculateProjection (trailing ProjectionDim rows of the eigenvector matrix), Compute project via sgemv. Output width = ProjectionDim (+nClasses if computeProbabilities — Pitfall 5). ProjectionDim default = min(nClasses-1, dim-1). Float reductions via sum_f64; the documented upstream-order scatter cells (IncrementalCloud::Update GEMM rank cells) use raw += per D-04. Module-doc cites lda.cpp:line. Checked .get(..), typed CbError, no unwrap/index. Tests in lda_linalg_test.rs + embedding_calcers_test.rs siblings.
  </action>
  <verify>
    <automated>cargo test -p cb-compute lda_linalg embedding_calcers::lda</automated>
  </verify>
  <done>LDA linalg + calcer math unit-tested (eigensolver matches the spike-validated reference); width exact; sum_f64 routing with documented raw-+= scatter cells; lib lint clean</done>
  <acceptance_criteria>cargo test -p cb-compute lda_linalg embedding_calcers::lda exits 0; cargo clippy -p cb-compute --lib 0 indexing_slicing/unwrap_used in the new files; if a crate was added it appears in Cargo.toml only after Task-1 human-verify</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Online-embedding prefix wiring (LDA) + LDA per-stage + projection oracle</name>
  <read_first>
    - crates/cb-train/src/estimated/online_embedding.rs (being created)
    - crates/cb-train/src/ctr/online.rs (THE ANALOG: read-before-update prefix loop lines 284-317, error discipline 163-180)
    - crates/cb-train/src/estimated/online_text.rs (from Plan 04 — the sibling online prefix to mirror for embedding)
    - crates/cb-train/src/fold.rs (Fold.permutation, lines 181-256)
    - catboost-master/catboost/private/libs/feature_estimator/base_embedding_feature_estimator.h (online embedding estimation base, target + classificationTarget — TRANSCRIPTION SOURCE)
    - catboost-master/catboost/private/libs/feature_estimator/embedding_feature_estimators.cpp (estimator takes target + classificationTarget; regression -> nullptr)
    - crates/cb-train/src/estimated/estimated_features.rs (from Plans 03/04 — extend for embedding columns)
    - crates/cb-oracle/src/compare.rs (compare_stage line 84)
    - crates/cb-oracle/fixtures/embedding_calcers/LDA/ + instrumented projection/feature dump (Plan 01)
  </read_first>
  <action>
    Create estimated/online_embedding.rs mirroring online_text.rs / ctr/online.rs (read-before-update prefix over &fold.permutation; LDA incremental scatter is updated AFTER computing the projected feature for the current document — leakage control, D-03). Embedding estimators take target + classificationTarget (regression -> nullptr per embedding_feature_estimators.cpp). Extend estimated_features.rs to route embedding columns (pool.embedding_features(), no schema change) through this estimator -> projected floats -> existing quantizer. Create crates/cb-oracle/tests/lda_oracle_test.rs: full 4-stage gate (Splits|LeafValues|StagedApprox|Predictions) + borders + the instrumented projection-matrix / per-document projected-feature compare ≤1e-5 vs fixtures/embedding_calcers/LDA/ + the dump. If the Task-1 decision was documented-tolerance, the test asserts the human-signed-off tolerance (NOT ≤1e-5) with the rationale cited in-file; otherwise ≤1e-5. NO #[ignore], NO silent weakening. cb-train non-embedding path byte-identical. Tests in online_embedding_test.rs sibling for the prefix-order unit.
  </action>
  <verify>
    <automated>cargo test -p cb-train estimated::online_embedding && cargo test -p cb-oracle --test lda_oracle_test</automated>
  </verify>
  <done>LDA computed online over fold.permutation; LDA per-stage + projection oracle ≤1e-5 (or human-signed-off documented tolerance, cited in-file); no #[ignore]; cb-train non-embedding path byte-identical</done>
  <acceptance_criteria>cargo test -p cb-oracle --test lda_oracle_test exits 0 (≤1e-5 or the documented tolerance, 0 ignored); cargo test -p cb-train --lib exits 0 (no regression); cargo clippy -p cb-train --lib 0 indexing_slicing/unwrap_used in online_embedding.rs</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External LAPACK/nalgebra crate (if chosen) | Supply-chain: new dependency must be source-verified before Cargo.toml edit (A3) |
| Pool embedding column (Vec<Vec<Vec<f32>>>) → LDA scatter | Embedding-dim mismatch / empty class could index past matrices |
| TFold learn permutation → online LDA estimator | Out-of-range permutation index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-05-01 | Denial of Service (panic) | LDA scatter/project on dim-mismatch / empty class / bad permutation index | mitigate | Bounds-checked .get(..); CB_ENSURE dim-equality analog -> typed CbError (helpers.h:21); no unwrap/index (INFRA-02, V5) |
| T-06.5-05-02 | Integrity (parity) | f32 eigensolver reproducibility | mitigate | Spike-driven decision from instrumented divergence numbers; raw-+= only on documented upstream-order scatter cells; sum_f64 elsewhere (D-04) |
| T-06.5-05-SC | Tampering | optional LAPACK/nalgebra crate install | mitigate | checkpoint:human-verify (Task 1) BEFORE any Cargo.toml edit: cargo search + crates.io + source-repo verification (A3); [ASSUMED] until verified; never auto-approved |
</threat_model>

<verification>
- LDA per-stage + projection oracle ≤1e-5 (or human-signed-off documented tolerance, no #[ignore])
- Eigensolver decision recorded with measured divergence numbers
- cb-train non-embedding path byte-identical
</verification>

<success_criteria>
First embedding calcer landed: LDA projected features match upstream ≤1e-5 (or an honest, human-signed-off documented tolerance), computed leakage-controlled online over the TFold permutation. The online-embedding seam now exists for KNN (Plan 06).
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-05-SUMMARY.md` when done
</output>
