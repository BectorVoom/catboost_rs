---
phase: 06.5-text-and-embedding-features
plan: 04
type: execute
wave: 4
depends_on: ["06.5-03"]
files_modified:
  - crates/cb-compute/src/text_calcers.rs
  - crates/cb-compute/src/text_calcers_test.rs
  - crates/cb-train/src/estimated/online_text.rs
  - crates/cb-train/src/estimated/online_text_test.rs
  - crates/cb-train/src/estimated/estimated_features.rs
  - crates/cb-train/src/estimated/mod.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/tests/naive_bayes_oracle_test.rs
  - crates/cb-oracle/tests/bm25_oracle_test.rs
autonomous: true
requirements: [FEAT-01]
user_setup: []

must_haves:
  truths:
    - "NaiveBayes (multinomial log-prob, class/token priors 0.5, softmax) and BM25 (k=1.5, b=0.75, truncate=1e-3) are computed as IOnlineFeatureEstimators over the TFold learn permutation with read-before-update prefix discipline (D-03, no leakage), reusing the Phase-5 fold permutation"
    - "NaiveBayes output width = numClasses>2 ? numClasses : 1; BM25 output width = numClasses (Pitfall 5) — widths matched exactly"
    - "NaiveBayes and BM25 encodings match upstream ≤1e-5 per-stage AND at the per-prefix-position intermediate (instrumented), closing SC-2"
  artifacts:
    - path: "crates/cb-train/src/estimated/online_text.rs"
      provides: "per-fold ordered prefix loop for NaiveBayes/BM25 (mirror ctr/online.rs)"
    - path: "crates/cb-compute/src/text_calcers.rs"
      provides: "NaiveBayes log_prob+softmax and BM25 score math"
      contains: "naive_bayes"
    - path: "crates/cb-oracle/tests/naive_bayes_oracle_test.rs"
      provides: "NaiveBayes per-stage + per-prefix oracle ≤1e-5"
  key_links:
    - from: "crates/cb-train/src/estimated/online_text.rs"
      to: "crates/cb-train/src/fold.rs (Fold.permutation)"
      via: "&fold.permutation, read-before-update prefix"
      pattern: "permutation"
    - from: "crates/cb-train/src/estimated/online_text.rs"
      to: "crates/cb-compute/src/text_calcers.rs"
      via: "Compute(prefix state) THEN Update(label)"
      pattern: "naive_bayes|bm25"
---

<objective>
The shared online-text estimation slice: NaiveBayes + BM25, both target-aware IOnlineFeatureEstimators computed per-fold over the TFold learn permutation with the exact read-before-update prefix discipline (D-03 leakage control). They share the online-text seam (online_text.rs) — class frequency tables accumulated in prefix order — so they land together.

Purpose: NaiveBayes and BM25 are the two text calcers that need leakage-controlled ordered estimation (BoW did not). This slice mirrors cb-train/src/ctr/online.rs verbatim — the read-before-update prefix already validated bit-exact in Phase 5. Simplifying the online path breaks the per-stage oracle (D-03 — no shortcut).
Output: NaiveBayes + BM25 calcer math + the online-text prefix wiring + both per-stage AND per-prefix oracles ≤1e-5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
</context>

## Phase Goal

**As a** Rust developer embedding catboost-rs, **I want to** train models with text and embedding features whose encodings match upstream catboost 1.2.10 to within 1e-5, **so that** existing CatBoost text/embedding pipelines port over with verifiable parity.

## Acceptance Criteria
- [ ] NaiveBayes encoding ≤1e-5 (per-stage + per-prefix)
- [ ] BM25 encoding ≤1e-5 (per-stage + per-prefix)
- [ ] Online prefix order matches the instrumented visiting-order dump (D-03)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NaiveBayes + BM25 calcer math (double-internal, f32 output, sum_f64 reductions)</name>
  <read_first>
    - crates/cb-compute/src/text_calcers.rs (extending from Plan 03)
    - catboost-master/catboost/private/libs/text_features/naive_bayesian.cpp (LogProb + priors 0.5, lines 14-63 — TRANSCRIPTION SOURCE, D-04)
    - catboost-master/catboost/private/libs/text_features/naive_bayesian.h (BaseFeatureCount = nClasses>2?nClasses:1, lines 38-40)
    - catboost-master/catboost/private/libs/text_features/bm25.cpp (InvClassFreq + Score saturation, k=1.5/b=0.75/truncate=1e-3, lines 12-83 — TRANSCRIPTION SOURCE)
    - catboost-master/catboost/private/libs/text_features/bm25.h (BaseFeatureCount = nClasses, lines 38-40)
    - catboost-master/catboost/private/libs/text_features/helpers.h (Softmax on double then downcast, lines 33-47)
    - crates/cb-compute/src/score.rs (ANALOG: collect Vec<f64> -> sum_f64 shape, lines 49-55)
    - crates/cb-core/src/reduction.rs (sum_f64 line 32 — mandated reduction)
  </read_first>
  <behavior>
    - naive_bayes_log_prob: value = log(classSamples+0.5); per (token,count): textLen += count, value += count*log(0.5 + freq[token]); value -= textLen*log(classTokensCount adjusted) — verbatim formula
    - naive_bayes_compute: per-class log-probs -> Softmax (double) -> emit active features; width = nClasses>2?nClasses:1 (binary -> 1)
    - bm25_score: meanLen = TotalTokens/NumClasses; per (token,count): scores[c] += TruncInvClassFreq[nonZero]*Score(tf_c,k,b,meanLen,classLen); width = nClasses
    - all float reductions (textLen, value, scores[c], norms) route through cb_core::sum_f64; math accumulates in double, output stored as f32
  </behavior>
  <action>
    Extend text_calcers.rs with NaiveBayes + BM25 arms, transcribed verbatim from naive_bayesian.cpp:14-63 and bm25.cpp:12-83 (D-04). Priors: ClassPrior=TokenPrior=DEFAULT_PRIOR=0.5, SEEN_TOKENS_PRIOR=1 (NaiveBayes); k=1.5, b=0.75, truncate=1e-3 (BM25). Output widths exactly per BaseFeatureCount (Pitfall 5): NaiveBayes nClasses>2?nClasses:1, BM25 nClasses. Math in double internally, Softmax on double then downcast to f32 (helpers.h:33-47, Pitfall 6). Every float reduction via collect Vec<f64> -> cb_core::sum_f64 (D-04/D-08, never zero-seeded fold); integer class-frequency counts are EXACT integer accumulation (NOT through sum_f64). Module-doc cites .cpp:line + states the sum_f64 routing rule. Checked .get(..), typed CbError, no unwrap/index. Tests in text_calcers_test.rs sibling.
  </action>
  <verify>
    <automated>cargo test -p cb-compute text_calcers::naive_bayes text_calcers::bm25</automated>
  </verify>
  <done>NaiveBayes log-prob+softmax and BM25 score math unit-tested vs hand-derived references; widths exact; reductions via sum_f64; lib lint clean</done>
  <acceptance_criteria>cargo test -p cb-compute text_calcers::naive_bayes text_calcers::bm25 exits 0; cargo clippy -p cb-compute --lib 0 indexing_slicing/unwrap_used in text_calcers.rs</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Online-text prefix wiring (D-03) + NaiveBayes/BM25 per-stage + per-prefix oracle</name>
  <read_first>
    - crates/cb-train/src/estimated/online_text.rs (being created)
    - crates/cb-train/src/ctr/online.rs (THE ANALOG — read in full: read-before-update prefix loop lines 284-317, OnlineCtrPrefix lines 325-336, per_bucket_monotone lines 425-455, error discipline lines 44-45/163-180)
    - crates/cb-train/src/fold.rs (Fold { permutation } from create_folds, lines 181-256 — the SAME permutation; do NOT generate a new one)
    - catboost-master/catboost/private/libs/feature_estimator/base_text_feature_estimator.h (ComputeOnlineFeatures(learnPermutation) Compute-then-Update loop, lines 59-64; column-major learnFeatures[f*samplesCount+line] — TRANSCRIPTION SOURCE)
    - crates/cb-train/src/estimated/estimated_features.rs (from Plan 03 — append + quantize seam to extend for online columns)
    - crates/cb-compute/src/text_calcers.rs (NaiveBayes/BM25 from Task 1)
    - crates/cb-oracle/src/compare.rs (compare_stage line 84, compare_permutation line 128)
    - crates/cb-oracle/fixtures/text_calcers/NaiveBayes/ + .../BM25/ + the per-prefix instrumented encoding dump (Plan 01)
  </read_first>
  <behavior>
    - online_text estimator iterates &fold.permutation; for each doc: Compute the calcer output from class-frequency state accumulated from EARLIER prefix lines ONLY, write to value[doc] (object-indexed, not permutation-indexed), THEN Update (classCounts[token]+=count, ClassTotalTokens[class]+=count, ClassDocs[class]+=1)
    - the visiting order equals fold.permutation (matches the instrumented online-order dump)
    - length-mismatch / out-of-range permutation index -> typed CbError::Degenerate (mirror online.rs)
  </behavior>
  <action>
    Create estimated/online_text.rs mirroring ctr/online.rs verbatim (Pattern 1, the read-before-update prefix): for each doc in &fold.permutation (the SAME permutation CTRs use — fold.rs, do NOT generate a new one, Don't-Hand-Roll), Compute NaiveBayes/BM25 from prefix class-frequency state THEN Update with this line's class/text (base_text_feature_estimator.h:59-64). Output object-indexed value[doc] (OnlineCtrPrefix shape). Expose the per-step running state for the per-prefix monotone instrumented anchor (per_bucket_monotone shape). Integer class counts = exact integer accumulation; only the float calcer outputs go through sum_f64. Extend estimated_features.rs to route online text columns through this estimator (vs BoW's offline path) before the existing quantizer. Create crates/cb-oracle/tests/naive_bayes_oracle_test.rs + bm25_oracle_test.rs: full 4-stage gate (Splits|LeafValues|StagedApprox|Predictions) + borders + the per-prefix-position intermediate encoding compare ≤1e-5 vs the instrumented dump (to localize leakage-order bugs). NO #[ignore], NO weakened tolerance. Tests in online_text_test.rs sibling for the prefix-order unit.
  </action>
  <verify>
    <automated>cargo test -p cb-train estimated::online_text && cargo test -p cb-oracle --test naive_bayes_oracle_test --test bm25_oracle_test</automated>
  </verify>
  <done>Online prefix order == fold.permutation (matches instrumented dump); NaiveBayes + BM25 per-stage AND per-prefix oracles ≤1e-5 (no #[ignore]); cb-train non-text path byte-identical</done>
  <acceptance_criteria>cargo test -p cb-oracle --test naive_bayes_oracle_test --test bm25_oracle_test exits 0 (per-stage + per-prefix ≤1e-5, 0 ignored); cargo test -p cb-train --lib exits 0 (no regression); cargo clippy -p cb-train --lib 0 indexing_slicing/unwrap_used in online_text.rs</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TFold learn permutation index → online estimator | Out-of-range permutation index / empty group could index past document arrays |
| Pool class labels → per-class frequency tables | Malformed class label (>= numClasses) could index past the class table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-04-01 | Denial of Service (panic) | online prefix loop on bad permutation index / class label | mitigate | Bounds-checked .get(..); typed CbError::Degenerate on length/index mismatch (mirror online.rs:163-180); no unwrap/index |
| T-06.5-04-02 | Integrity (leakage/parity) | read-before-update prefix order | mitigate | Verbatim mirror of the Phase-5 bit-exact ctr/online.rs prefix; per-prefix instrumented oracle localizes any order bug |
| T-06.5-04-03 | Integrity (parity) | float-vs-double accumulators | mitigate | double-internal math, f32 output, sum_f64 reductions (Pitfall 6, D-04); integer counts exact |
| T-06.5-04-SC | Tampering | no package install | accept | No new packages; D-05 no new crate |
</threat_model>

<verification>
- NaiveBayes + BM25 per-stage AND per-prefix oracles ≤1e-5 (no #[ignore])
- Online prefix order == fold.permutation (instrumented dump match)
- cb-train non-text path byte-identical (existing suite green)
</verification>

<success_criteria>
SC-2 closed: BoW (Plan 03) + NaiveBayes + BM25 all produce upstream-matching text encodings ≤1e-5, with the online calcers leakage-controlled over the TFold learn permutation (D-03). FEAT-01 text calcers complete.
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-04-SUMMARY.md` when done
</output>
