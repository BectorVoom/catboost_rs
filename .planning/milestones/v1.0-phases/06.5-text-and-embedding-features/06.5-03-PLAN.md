---
phase: 06.5-text-and-embedding-features
plan: 03
type: execute
wave: 3
depends_on: ["06.5-02"]
files_modified:
  - crates/cb-compute/src/text_calcers.rs
  - crates/cb-compute/src/text_calcers_test.rs
  - crates/cb-compute/src/lib.rs
  - crates/cb-train/src/estimated/mod.rs
  - crates/cb-train/src/estimated/estimated_features.rs
  - crates/cb-train/src/estimated/estimated_features_test.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/tests/bow_oracle_test.rs
autonomous: true
requirements: [FEAT-01]
user_setup: []

must_haves:
  truths:
    - "BoW produces a binary presence feature per active dictionary token (target-independent, NO online wiring) matching upstream ≤1e-5"
    - "BoW's Compute walks TText and the active feature ids in lockstep BOTH sorted ascending (two-pointer merge, order is load-bearing)"
    - "BoW estimated float columns flow through the EXISTING cb-data borders/quantize/quantized_pool path into the cb-train tree search (SC-4 first slice), no parallel quantizer, no Pool schema change"
  artifacts:
    - path: "crates/cb-compute/src/text_calcers.rs"
      provides: "BoW calcer math (bag_of_words_compute)"
      contains: "bag_of_words"
    - path: "crates/cb-train/src/estimated/estimated_features.rs"
      provides: "append estimated float columns to feature layout -> quantize -> tree (SC-4 wiring)"
    - path: "crates/cb-oracle/tests/bow_oracle_test.rs"
      provides: "BoW per-stage oracle ≤1e-5"
  key_links:
    - from: "crates/cb-train/src/estimated/estimated_features.rs"
      to: "cb_data::select_borders_greedy_logsum / Pool::quantize"
      via: "estimated floats appended to float-feature layout, existing quantizer"
      pattern: "quantize"
    - from: "crates/cb-oracle/tests/bow_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/text_calcers/BoW/"
      via: "compare_stage ≤1e-5"
      pattern: "compare_stage"
---

<objective>
First calcer vertical slice: BoW (Bag-of-Words). BoW is target-INDEPENDENT (no online/ordered estimation), so it is the simplest end-to-end path — text -> digitizer (Plan 02) -> BoW binary presence -> estimated float columns -> EXISTING quantize -> tree -> per-stage oracle ≤1e-5. This task also lands the SC-4 integration seam (estimated_features.rs) that NaiveBayes/BM25/LDA/KNN reuse.

Purpose: Prove the full Pool->calcer->quantize->tree path end-to-end on the simplest calcer before adding the online-estimation complexity (Plans 04-06). After this slice, a user can train a model on a text column and get upstream-matching BoW encodings.
Output: BoW calcer math + the estimated-feature integration wiring + BoW per-stage oracle ≤1e-5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
</context>

## Phase Goal

**As a** Rust developer embedding catboost-rs, **I want to** train models with text and embedding features whose encodings match upstream catboost 1.2.10 to within 1e-5, **so that** existing CatBoost text/embedding pipelines port over with verifiable parity.

## Acceptance Criteria
- [ ] BoW binary-presence encoding ≤1e-5 vs upstream
- [ ] Estimated floats flow through the existing quantize->tree path (SC-4 first slice)
- [ ] BoW per-stage oracle green (borders/splits/leaf/staged/prediction)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BoW calcer math (lockstep binary presence over active token ids)</name>
  <read_first>
    - crates/cb-compute/src/text_calcers.rs (being created)
    - catboost-master/catboost/private/libs/text_features/bow.cpp (Compute lockstep walk, lines 7-21 — TRANSCRIPTION SOURCE, D-04)
    - catboost-master/catboost/private/libs/text_features/bow.h (FeatureCount = NumTokens)
    - crates/cb-data/src/text/text.rs (TText sorted-RLE, from Plan 02 — the input)
    - crates/cb-compute/src/score.rs (ANALOG, read 1-85: pure-math module-doc + sum_f64 routing + CbResult style)
    - crates/cb-compute/src/lib.rs (module declaration + pub use + _test sibling wiring, lines 21-79)
  </read_first>
  <behavior>
    - bag_of_words_compute(text=[(1,2),(3,1)], active_ids=[0,1,2,3]) yields [0,1,0,1] (presence, not count)
    - active_ids and text BOTH sorted ascending; two-pointer merge advances text iterator while token < active_id
    - empty text yields all-zero vector of width = active_ids.len()
    - FeatureCount == dictionary NumTokens (one binary feature per token)
  </behavior>
  <action>
    Create text_calcers.rs with the BoW arm: transcribe TBagOfWordsCalcer::Compute verbatim from bow.cpp:7-21 — walk TText and GetActiveFeatureIndices() BOTH sorted ascending (two-pointer merge, order load-bearing, Pattern §BoW); emit 0 if token absent/greater, 1 if present. BoW is the ONLY target-independent calcer (no online wiring). Output width = NumTokens (Pitfall 5). Module-doc cites bow.cpp:line + states the sum_f64 rule (BoW emits 0/1, no float reduction needed here but the module convention is documented). Checked .get(..) access, typed CbError, no unwrap/index. Tests in text_calcers_test.rs sibling (INFRA-06).
  </action>
  <verify>
    <automated>cargo test -p cb-compute text_calcers::bow</automated>
  </verify>
  <done>BoW lockstep binary-presence math unit-tested; width = NumTokens; lib lint clean</done>
  <acceptance_criteria>cargo test -p cb-compute text_calcers::bow exits 0; cargo clippy -p cb-compute --lib 0 indexing_slicing/unwrap_used in text_calcers.rs</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Estimated-feature integration wiring (SC-4 seam) + BoW per-stage oracle</name>
  <read_first>
    - crates/cb-train/src/estimated/estimated_features.rs + estimated/mod.rs (being created)
    - crates/cb-train/src/ctr/ctr_feature.rs (ANALOG: CTR features appended to feature layout)
    - crates/cb-train/src/tree.rs (ANALOG: split_score dispatch, lines 32-66 — how float features enter the search)
    - crates/cb-data/src/borders.rs (select_borders_greedy_logsum ~line 209 — the EXISTING quantizer, reuse unchanged)
    - crates/cb-data/src/quantize.rs (QuantizeParams line 42, Pool::quantize line 69)
    - crates/cb-data/src/pool.rs (text_features() line 160, n_text_features() line 130 — raw text input, no schema change)
    - crates/cb-data/src/text/digitizer.rs (from Plan 02 — text -> Vec<TText>)
    - crates/cb-compute/src/text_calcers.rs (BoW from Task 1)
    - crates/cb-oracle/tests/per_stage_oracle_test.rs (ANALOG oracle-test structure, lines 1-55)
    - crates/cb-oracle/src/compare.rs (compare_stage line 84, Stage enum)
    - crates/cb-oracle/fixtures/text_calcers/BoW/ (per-stage fixture from Plan 01)
  </read_first>
  <action>
    Create cb-train/src/estimated/estimated_features.rs: take Pool raw text columns (pool.text_features(), no schema change — Pattern §estimated_features), digitize via Plan-02 digitizer (dictionary built ONCE, offline, target-independent — Anti-Pattern "recompute dictionary per fold"), run BoW (Task 1) to produce numeric float columns, append them to the float-feature layout, then feed them through the UNCHANGED cb_data::select_borders_greedy_logsum -> Pool::quantize -> quantized_pool -> cb-train tree search (RESEARCH Don't-Hand-Roll: NO parallel quantizer, SC-4). Wire estimated/mod.rs (#[path=] copy ctr/mod.rs) + declare in cb-train/src/lib.rs. Create crates/cb-oracle/tests/bow_oracle_test.rs: full 4-stage gate (Splits|LeafValues|StagedApprox|Predictions) + borders of the estimated BoW features, compare_stage ≤1e-5 vs fixtures/text_calcers/BoW/. NO #[ignore], NO weakened tolerance. cb-train non-text path must stay byte-identical (no estimated columns -> no behavior change).
  </action>
  <verify>
    <automated>cargo test -p cb-train estimated:: && cargo test -p cb-oracle --test bow_oracle_test</automated>
  </verify>
  <done>BoW estimated floats flow through the existing quantize->tree path; BoW per-stage oracle ≤1e-5 (no #[ignore]); non-text cb-train path byte-identical</done>
  <acceptance_criteria>cargo test -p cb-oracle --test bow_oracle_test exits 0 (per-stage ≤1e-5, 0 ignored); cargo test -p cb-train --lib exits 0 (no regression); cargo clippy -p cb-train --lib 0 indexing_slicing/unwrap_used in estimated_features.rs</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pool raw text column → BoW → estimated float column → quantizer | Empty text / out-of-dictionary tokens reaching the feature layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-03-01 | Denial of Service (panic) | BoW lockstep walk / estimated-feature append on empty text or zero active tokens | mitigate | Bounds-checked .get(..); empty text -> all-zero width vector; typed CbError; no unwrap/index (INFRA-02, V5) |
| T-06.5-03-02 | Integrity (parity) | estimated-feature ordering into the feature layout | mitigate | Reuse the existing quantizer unchanged (SC-4); feature-index order transcribed from ctr_feature.rs; oracle-gated ≤1e-5 |
| T-06.5-03-SC | Tampering | no package install | accept | No new packages; D-05 no new crate |
</threat_model>

<verification>
- BoW per-stage oracle ≤1e-5 (no #[ignore])
- cb-compute text_calcers::bow + cb-train estimated:: unit suites green
- cb-train non-text path byte-identical (existing suite green)
</verification>

<success_criteria>
First calcer slice closed: BoW encodings match upstream ≤1e-5 and flow end-to-end through the existing quantize->tree path. The SC-4 estimated-feature seam now exists for the online calcers (Plans 04-07).
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-03-SUMMARY.md` when done
</output>
