---
phase: 06.5-text-and-embedding-features
plan: 08
type: execute
wave: 8
depends_on: ["06.5-04", "06.5-07"]
files_modified:
  - .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md
autonomous: true
requirements: [FEAT-01]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "The exact upstream catboost 1.2.10 path that produces the BM25 estimated-feature stored split-border scale (splits.npy ±1.24 / -0.550486 vs raw BM25 scores O(1e-3)) is located in source and transcribed — NOT another blind reproduction attempt (FEAT-01, closes the open question 06.5-04 could not answer)"
    - "An instrumented (CB_INSTRUMENT_LOG) RUN-ONCE dump of the EXACT estimated-feature values catboost feeds into estimated-column border selection for the frozen BM25 fixture exists and is recorded — proving whether the ±1.24 borders come from (a) a value transform applied to the calcer outputs before quantization, (b) a different border-selection algorithm/grid for estimated columns, or (c) multi-permutation ordered averaging of the estimated values"
    - "A binding DECISION is recorded — path A (normalization is reproducible: the transform/algorithm is identified and bounded so the implementation plan can reproduce splits.npy ≤1e-5) OR path B (the residual is proven upstream-irreducible from the source evidence, so the implementation plan must convert it to a documented, owned, signed-off scope) — with the upstream symbol/file:line evidence that justifies the choice (escalate-don't-weaken, D-07)"
  artifacts:
    - path: ".planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md"
      provides: "the source-located normalization mechanism + instrumented dump evidence + the A/B path decision gating 06.5-09"
      contains: "DECISION:"
  key_links:
    - from: ".planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md"
      to: "catboost-master/catboost/private/libs/algo/estimated_features.cpp"
      via: "transcribed file:line evidence of the estimated-feature value path that feeds border selection"
      pattern: "estimated_features.cpp"
---

<objective>
Locate, in upstream catboost 1.2.10 SOURCE (not by trial reproduction), the exact mechanism that produces the BM25 estimated-feature stored split-border scale: splits.npy holds ±1.24 / -0.550486 while the raw BM25 calcer scores are O(1e-3). Plan 06.5-04 proved exhaustively that no single learn-permutation strict prefix and no offline whole-set estimate reproduces this scale; the normalization path was never located in source. This plan ends that uncertainty by reading the source AND dumping the exact values upstream feeds into estimated-column border selection with the instrumented trainer, then records a BINDING A/B decision that gates the implementation plan 06.5-09.

Purpose: The verifier classified a bare re-deferral of the BM25 per-stage gap as a BLOCKER. The ONLY honest way forward is to first determine whether the normalization is reproducible (path A) or upstream-irreducible (path B) from EVIDENCE, not assumption. This investigation IS that gate. It commits to neither path prematurely; the implementation plan 06.5-09 branches on the recorded DECISION.
Output: BM25-NORMALIZATION-DECISION.md — the source mechanism, the instrumented dump, and the A/B decision with file:line evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
@.planning/phases/06.5-text-and-embedding-features/06.5-VERIFICATION.md
@.planning/phases/06.5-text-and-embedding-features/deferred-items.md
@.planning/phases/06.5-text-and-embedding-features/06.5-04-SUMMARY.md
@.planning/phases/06.5-text-and-embedding-features/06.5-07-SUMMARY.md
</context>

## Phase Goal

All six text and embedding calcers produce upstream-matching encodings ≤1e-5. This plan closes the ONE remaining FEAT-01 gap: the BM25 per-stage trained-model oracle (SC-2), blocked by the unlocated estimated-feature border normalization.

## Acceptance Criteria
- [ ] The estimated-feature value path that feeds border selection is transcribed from upstream source with file:line evidence
- [ ] An instrumented RUN-ONCE dump records the exact pre-quantization estimated-feature values for the BM25 fixture
- [ ] A binding A/B DECISION with evidence is written to BM25-NORMALIZATION-DECISION.md

<tasks>

<task type="auto">
  <name>Task 1: Transcribe the upstream estimated-feature value→border path from source</name>
  <read_first>
    - catboost-master/catboost/private/libs/algo/estimated_features.cpp (the ComputeOnlineFeatures / ComputeFeatures dispatch ~448-473 and the CreateObjectsDataProvider quantization wiring ~475-509 — where estimated float columns become quantized features)
    - catboost-master/catboost/private/libs/algo/fold.cpp (lines 181, 200 — GetLearnPermutationArray / permutation_count fold construction feeding the online estimator)
    - catboost-master/catboost/private/libs/feature_estimator/base_text_feature_estimator.h (lines 56-90 — ComputeOnlineFeatures writes learnFeatures[f*samplesCount+line], the RAW calcer output; confirm NO normalization happens here, already verified)
    - catboost-master/catboost/private/libs/text_features/bm25.cpp (lines 12-83 — confirm the raw score scale is O(1e-3), already verified bit-exact)
    - crates/cb-oracle/fixtures/text_calcers/BM25/splits.npy (the ±1.24 / -0.550486 borders — repeats one (−0.550486, 1.2439) pair across 5 trees), leaf_weights.npy ([7,2,0,7] depth-2), params.json (Plain, iterations=5, depth=2, lr=0.3, permutation context)
    - catboost-master/catboost/private/libs/quantization/ AND catboost-master/catboost/libs/quantization/ (the border/grid selection the quantizedFeaturesInfo applies — locate where estimated-column borders are computed; grep for CalcBordersAndNanMode, GreedyLogSum, EBorderSelectionType, and any per-feature standardization/scaling applied to the float values before border selection)
    - crates/cb-train/src/estimated/estimated_features.rs (lines 185-294 — the Rust seam: build_online_text_estimated_features feeds columns through select_borders_greedy_logsum on the RAW O(1e-3) values; this is what diverges from upstream's ±1.24)
    - crates/cb-data/src/borders.rs (line 209 — select_borders_greedy_logsum, the current Rust quantizer)
  </read_first>
  <action>
    Read the upstream estimated-feature value→quantization path end to end and transcribe, with exact file:line citations, the answer to: between the raw BM25 calcer output (learnFeatures[f*samplesCount+line], confirmed O(1e-3)) and the stored split border (splits.npy ±1.24), WHAT transforms the value scale? Investigate the three candidate mechanisms explicitly (per D-04 transcribe-first): (a) a per-feature value transform (standardization / z-score / mean-shift+scale) applied to estimated columns before or during border selection — grep the quantization path for any Mean/StdDev/Scale/Normaliz on float feature values and for how estimated features differ from raw numeric features there; (b) a different border-selection algorithm or grid configured for estimated columns vs the GreedyLogSum the Rust seam uses (check EBorderSelectionType defaults for estimated features, and whether borders are stored in a transformed space); (c) multi-permutation ordered averaging — fold.cpp permutation_count>1 means ComputeOnlineFeatures runs per permutation and the stored estimated value is an AVERAGE across permutations, which both changes the value scale AND explains the genuine depth-2 [7,2,0,7] structure that a single permutation does not produce. Record the transcribed mechanism (or the precise reason none of (a)/(b)/(c) is located) into a new file BM25-NORMALIZATION-DECISION.md under a "## Source mechanism" heading with every supporting file:line. Do NOT modify any production code in this task — this is a read+transcribe investigation. NO fenced implementation code; cite identifiers, signatures, and the storage-space of the border values.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md && grep -q '## Source mechanism' .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md && grep -Eq 'estimated_features\.cpp|fold\.cpp|quantization' .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md</automated>
  </verify>
  <done>BM25-NORMALIZATION-DECISION.md has a "## Source mechanism" section that names, with file:line evidence, the upstream value→border transform (one of (a)/(b)/(c)) OR documents precisely why each of the three candidate mechanisms is absent from the located source</done>
  <acceptance_criteria>The file exists, cites estimated_features.cpp / fold.cpp / the quantization path by file:line, and states which of value-transform / border-algorithm / multi-permutation-averaging produces the ±1.24 scale (or that none is locatable, with the searched symbols enumerated)</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instrumented RUN-ONCE dump of pre-quantization estimated-feature values + binding A/B decision</name>
  <read_first>
    - .planning/phases/06.5-text-and-embedding-features/06.5-01-PLAN.md (Wave-0: how the instrumented trainer + CB_INSTRUMENT_LOG hooks were provisioned/rebuilt — the recipe; the /tmp/cb_build313 + clang-18 toolchain)
    - $HOME/.claude/projects/-home-user-Documents-workspace-catboost-rs/memory/catboost-instrumented-trainer-build.md (sudo-free build recipe: conan/ninja/cython via uv tool; clang-18+lld-18 via apt-get download+dpkg-x; build_native.py + CB_INSTRUMENT_LOG)
    - $HOME/.claude/projects/-home-user-Documents-workspace-catboost-rs/memory/instrumented-trainer-toolchain-persists.md (the toolchain persists in /tmp — but VERIFY: ls -d /tmp/cb_build313 may be ABSENT after a /tmp clear; rebuild incrementally if so)
    - catboost-master/catboost/private/libs/algo/estimated_features.cpp (the dump insertion point identified in Task 1 — instrument the value FED INTO border selection, env-gated CB_INSTRUMENT_LOG, mirroring the existing 7 hooks from 06.5-01; vendored patches stay UNCOMMITTED, D-09/D-12)
    - crates/cb-oracle/fixtures/text_calcers/BM25/params.json (the exact training config to replay: Logloss, iterations=5, depth=2, lr=0.3, leaf_estimation_iterations=1, Plain, bootstrap=No, seed=20260618, thread_count=1, BM25 calcer, Word dictionary, Space tokenizer)
    - crates/cb-oracle/fixtures/text_embedding_inputs/texts.json + labels.npy (the frozen 16-row corpus the BM25 fixture was trained on)
    - the BM25-NORMALIZATION-DECISION.md "## Source mechanism" section from Task 1
  </read_first>
  <action>
    Confirm the instrumented catboost 1.2.10 trainer is available: if /tmp/cb_build313 is absent (a /tmp clear removed it — likely), rebuild it incrementally using the recorded sudo-free recipe (catboost-instrumented-trainer-build.md), reusing any surviving toolchain. Add an env-gated CB_INSTRUMENT_LOG hook at the estimated-feature value→border-selection insertion point identified in Task 1 (the exact value array catboost passes into the estimated-column quantizer, AFTER any normalization/averaging — i.e. the values whose borders become splits.npy). Keep the patch UNCOMMITTED (D-09/D-12). Train the BM25 fixture model ONCE with the params.json config over the frozen 16-row corpus, dumping the pre-quantization estimated-feature value array. RUN-ONCE / OFFLINE: NO fabricated fixtures, NO weakened tolerance, NO #[ignore]. Record the dumped values into BM25-NORMALIZATION-DECISION.md under "## Instrumented dump" and compare them against (i) the raw BM25 online-prefix column the Rust seam produces (O(1e-3)) and (ii) the splits.npy ±1.24 borders — establishing whether the dumped values ARE in the ±1.24 scale (confirming a value transform / averaging) or in the O(1e-3) scale (pointing the normalization to the border-storage space). Then write the BINDING decision under a "## DECISION:" heading — exactly one of: "DECISION: PATH-A" (the transform/algorithm/averaging is identified and bounded; 06.5-09 reproduces splits.npy ≤1e-5 by implementing it) WITH the precise reproduction recipe (which transform, computed from what inputs, over how many permutations, in which storage space); or "DECISION: PATH-B" (the dump proves the ±1.24 scale arises from an upstream artifact that cannot be reproduced ≤1e-5 — e.g. an internal float feature-value standardization with non-reference iteration, or multi-permutation averaging whose RNG/visiting-order is not externally recoverable) WITH the irreducibility evidence (what was dumped, why it cannot be reconstructed externally). The decision MUST be justified by the dumped numbers, not asserted. Record the necessary-and-sufficient parity contract path B would gate (which stages remain HARD ≤1e-5 — e.g. StagedApprox/Predictions, which the fixture shows are clean and reproducible) so 06.5-09 has a concrete owned-scope to implement.
  </action>
  <verify>
    <automated>grep -Eq '## Instrumented dump' .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md && grep -Eq 'DECISION: PATH-A|DECISION: PATH-B' .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md</automated>
  </verify>
  <done>The instrumented dump of the exact pre-quantization estimated-feature values for the BM25 fixture is recorded under "## Instrumented dump" and compared to both the raw O(1e-3) column and the ±1.24 borders; a single binding "DECISION: PATH-A" or "DECISION: PATH-B" is recorded with numeric justification and (for B) the necessary-and-sufficient HARD parity contract for 06.5-09 to gate</done>
  <acceptance_criteria>BM25-NORMALIZATION-DECISION.md contains "## Instrumented dump" with the dumped values, and exactly one "DECISION: PATH-A" or "DECISION: PATH-B" line whose justification references the dumped numbers; the instrumented catboost patch is present but UNCOMMITTED (git status shows catboost-master/ changes not staged); no fabricated fixtures, no #[ignore]</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instrumented catboost 1.2.10 trainer (C++) → DECISION.md | RUN-ONCE offline dump; uncommitted vendored patch must not leak into the committed tree (D-09/D-12) |
| upstream source transcription → A/B decision | a wrong reading of the value-transform path would mis-route 06.5-09; mitigated by the instrumented dump cross-checking the source claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-08-01 | Tampering | uncommitted instrumented catboost-master/ patch | mitigate | Patch stays UNCOMMITTED (D-09/D-12); the commit step lists ONLY the DECISION.md path; git status verified clean of vendored changes |
| T-06.5-08-02 | Information disclosure (false parity) | source-only reading without dump cross-check | mitigate | Task 2 dumps the EXACT pre-quantization values and forces the A/B decision to be justified by the dumped numbers, not by source assumption alone |
| T-06.5-08-03 | Repudiation (silent re-deferral) | a vague "deferred again" outcome | mitigate | The plan REQUIRES exactly one of DECISION: PATH-A / PATH-B with evidence; a bare re-deferral fails the verify gate (the verifier already rejected re-deferral as a BLOCKER) |
| T-06.5-08-SC | Tampering | no package install | accept | No new crates (D-05); the trainer rebuild uses the recorded sudo-free toolchain, no new workspace dependency |
</threat_model>

<verification>
- BM25-NORMALIZATION-DECISION.md exists with "## Source mechanism", "## Instrumented dump", and exactly one "DECISION: PATH-A" / "DECISION: PATH-B"
- The decision is justified by the instrumented dumped numbers (cross-checked against the raw O(1e-3) column and the ±1.24 borders), not asserted
- The instrumented catboost patch is present but UNCOMMITTED (D-09/D-12); commit lists only the DECISION.md
- NO fabricated fixtures, NO weakened tolerance, NO #[ignore] introduced
</verification>

<success_criteria>
The BM25 estimated-feature border-normalization mechanism is located in upstream source AND cross-checked by an instrumented RUN-ONCE dump, and a single binding A/B decision (with file:line + numeric evidence) is recorded that gates the implementation plan 06.5-09. The open question 06.5-04 could not answer ("where does the ±1.24 scale come from?") is now answered with evidence — either reproducible (A) or proven irreducible (B). No premature commitment to either path; no silent re-deferral.
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-08-SUMMARY.md` when done.
The SUMMARY MUST state the recorded DECISION (PATH-A or PATH-B) prominently so the 06.5-09 executor branches correctly.
</output>
