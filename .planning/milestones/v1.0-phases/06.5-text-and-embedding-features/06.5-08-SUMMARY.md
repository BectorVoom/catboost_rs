---
phase: 06.5-text-and-embedding-features
plan: 08
subsystem: oracle+investigation
tags: [catboost, bm25, text-features, estimated-features, border-normalization, instrumented-trainer, FEAT-01, sc-2, gap-closure, decision]

# Dependency graph
requires:
  - phase: 06.5-text-and-embedding-features
    plan: 04
    provides: BM25 calcer math + online seam ≤1e-5 (calcer-encoding); the open ±1.24 border deferral this plan resolves
  - phase: 06.5-text-and-embedding-features
    plan: 07
    provides: SC-4 mixed text+embedding join closed; the general estimated-feature grid open-question this plan disambiguates for BM25
  - phase: 06.5-text-and-embedding-features
    plan: 01
    provides: instrumented catboost 1.2.10 trainer + CB_INSTRUMENT_LOG hooks + build_instrumented_trainer.sh
provides:
  - "BINDING DECISION (PATH-A) gating 06.5-09: the BM25 ±1.24 'normalized border' does not exist — it is the default embedding calcer's border on the emb0 column; the real BM25 estimated-feature borders are O(1e-3) and already reproduced ≤1e-5"
  - "BM25-NORMALIZATION-DECISION.md — source mechanism (estimated_features.cpp:204-250 → split.cpp:45-46 → model.cpp:209, all scale-preserving) + instrumented estimated_borders dump + the A/B decision"
  - "cb_instr_estimated_borders hook added to build_instrumented_trainer.sh (vendored patch UNCOMMITTED)"
affects: [06.5-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Estimated-feature borders = BestSplit(raw calcer column) verbatim: base_text_feature_estimator.h:74-88 writes raw O(1e-3) column → estimated_features.cpp:225-237 BestSplit + SetBorders → split.cpp:45-46 reads GetBorders → model.cpp:209 stores estimatedFeatureSplit.Split — no value-transform/standardization/averaging-rescale anywhere"
    - "Fixture cross-check discipline: a per-stage .npy frozen via model._get_tree_splits() inherits the model's FULL feature set; a text-calcer fixture trained on a text+embedding pool records the EMBEDDING feature's borders, not the text calcer's (calcer_id disambiguates: 4559D4B0=BM25 text vs 96AE6D4D=emb0 embedding)"

key-files:
  created:
    - .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md
  modified:
    - crates/cb-oracle/generator/build_instrumented_trainer.sh

key-decisions:
  - "DECISION: PATH-A — the BM25 estimated-feature border 'normalization' does not exist. The committed splits.npy ±1.24/-0.550486 borders carry calcer_id=96AE6D4D (the default EMBEDDING calcer on emb0), NOT the BM25 text calcer 4559D4B0. The genuine BM25 borders are O(1e-3) (instrumented estimated_borders dump: [0.000465…,…,0.005428…]) and already reproduced ≤1e-5 by the existing Rust seam. The 06.5-04 deferral rested on a fixture mislabel: splits.npy was frozen from a text+embedding pool, so it recorded the embedding feature's borders. 06.5-09 = a fixture-correctness fix (regenerate text-only BM25 fixtures + add per-stage oracle ≤1e-5), NOT a trainer-normalization implementation."

requirements-completed: []

# Metrics
duration: ~3h 5m
completed: 2026-06-18
---

# Phase 06.5 Plan 08: BM25 estimated-feature border normalization — source + instrumented dump + A/B decision Summary

**Resolved the open question 06.5-04 could not answer ("where does the BM25 splits.npy ±1.24 / -0.550486 scale come from when raw BM25 scores are O(1e-3)?") by reading the upstream value→border path end-to-end AND dumping the exact borders the instrumented catboost 1.2.10 trainer selects: the ±1.24 borders are the default EMBEDDING calcer's splits on the `emb0` column (calcer_id `96AE6D4D…`), NOT the BM25 text calcer (`4559D4B0…`) — the BM25 fixture's `splits.npy` was frozen from a text+embedding pool and mislabels the embedding feature's borders as BM25's. The genuine BM25 estimated-feature borders are O(1e-3) and already reproduced ≤1e-5. BINDING DECISION: PATH-A (no normalization to implement; 06.5-09 regenerates text-only BM25 fixtures + per-stage oracle).**

## ⭐ DECISION (for the 06.5-09 executor): **PATH-A**

The BM25 "±1.24 normalized border" is a **fixture mislabel**, not an upstream normalization. The real BM25
estimated-feature borders are O(1e-3), reproducible ≤1e-5 by the existing
`crates/cb-train/src/estimated/estimated_features.rs` seam. **06.5-09 must:**
1. Regenerate the BM25 (text-calcer) fixtures from a **TEXT-ONLY pool** (drop `embedding_features=["emb0"]`
   from the text-calcer path in `gen_text_embedding_fixtures.py`), re-freezing `splits.npy` (will be O(1e-3),
   e.g. `0.00248965, 0.00127047`) via the instrumented single-thread trainer (RUN-ONCE).
2. Add a BM25 per-stage oracle (Splits/LeafValues/StagedApprox/Predictions ≤1e-5) — the same gate NaiveBayes
   already passes. No production-code change is expected; any residual is now a real Rule-1 bug, not deferrable.
3. HARD parity contract: BM25 text-only per-stage ≤1e-5, no stage deferred. FEAT-01 closes when green.

## Performance

- **Duration:** ~3h 5m (dominated by the from-scratch instrumented trainer rebuild — `/tmp` had been cleared)
- **Started:** 2026-06-18T01:49Z
- **Completed:** 2026-06-18T04:54Z
- **Tasks:** 2
- **Files:** 2 (1 created, 1 modified)

## Accomplishments

- **Task 1 — source transcription (`b51aa25`):** Read the estimated-feature value→border path end-to-end and
  transcribed it into `BM25-NORMALIZATION-DECISION.md` under `## Source mechanism` with file:line evidence:
  `base_text_feature_estimator.h:74-88` (raw O(1e-3) calcer column, no normalization) →
  `estimated_features.cpp:204-250` (`BestSplit` selects borders directly on the raw `values`, no transform;
  `grid_creator/binarization.{h,cpp}` + `quantization/utils.cpp` grepped for `Mean|StdDev|Standard|Normaliz|Scale`
  — none) → `split.cpp:45-46` (`GetBorders` verbatim) → `model.cpp:209` (stores `estimatedFeatureSplit.Split`,
  no export scaling). Concluded none of (a) value-transform / (b) border-algorithm / (c) multi-permutation-
  averaging — as located in source — can raise O(1e-3) inputs to ±1.24 borders, so the instrumented dump is
  the decisive cross-check.
- **Task 2 — instrumented dump + binding decision (`1849b04`):**
  - Rebuilt the instrumented catboost 1.2.10 trainer sudo-free (clang-18 toolchain survived in `/tmp`; build
    tree re-provisioned; full Release `[4300/4300]`; staged `/tmp/cb_build313/instr_pkg`).
  - Added env-gated `cb_instr_estimated_borders` hook at `estimated_features.cpp:234-237` (after `SetBorders`)
    to `build_instrumented_trainer.sh`; the vendored `catboost-master/` patch stays **UNCOMMITTED** (the
    whole subtree is untracked — D-09/D-12 satisfied).
  - RUN-ONCE replay of the exact `params.json` BM25 fit over the frozen 16-row corpus dumped: BM25 estimated-
    feature borders `[0.000465…,…,0.005428…]` (O(1e-3)); `calcer_encoding` value range `max=0.00637875`.
  - Disambiguated `splits.npy`: a text-only fit yields O(1e-3) BM25 borders (`calcer_id=4559D4B0…`) and the
    committed `model.cbm` won't even load against a text-only pool (`Feature emb0 is present in model but not
    in pool`); an embedding-present fit yields exactly `-0.550486 / 1.2439` with every split on
    `calcer_id=96AE6D4D…` (the `emb0` embedding calcer). Recorded `## Instrumented dump` + `DECISION: PATH-A`
    with numeric justification.

## Task Commits

1. **Task 1: source transcription** — `b51aa25` (docs)
2. **Task 2: instrumented dump + DECISION PATH-A** — `1849b04` (feat)

## Verification

- `BM25-NORMALIZATION-DECISION.md` exists with `## Source mechanism` (1), `## Instrumented dump` (1), and
  exactly one `## DECISION: PATH-A` (1). Cites `estimated_features.cpp`, `fold.cpp`, `quantization`,
  `split.cpp`, `model.cpp`.
- DECISION is justified by the dumped numbers (BM25 borders O(1e-3) vs committed ±1.24 = embedding calcer),
  cross-checked against the raw O(1e-3) `calcer_encoding` column and the `_get_tree_splits` calcer_id
  attribution — not asserted.
- Vendored instrumented patch UNCOMMITTED: `git ls-files catboost-master/.../estimated_features.cpp` → 0
  (the entire `catboost-master/` subtree is untracked); the Task-2 commit staged only `DECISION.md` +
  `build_instrumented_trainer.sh`, no `catboost-master/` files.
- NO fabricated fixtures, NO weakened tolerance, NO `#[ignore]`, NO Rust production changes (investigation-only).

## Decisions Made

- **PATH-A: the BM25 ±1.24 border normalization does not exist.** It is the default embedding calcer's border
  on `emb0` (`calcer_id=96AE6D4D…`), surfaced because the BM25 fixture pool included the embedding column.
  The genuine BM25 estimated-feature borders are O(1e-3), already reproduced ≤1e-5 by the existing seam.
  06.5-09 is a fixture-correctness fix (regenerate text-only BM25 fixtures + per-stage oracle), not a
  trainer-normalization implementation. PATH-B (upstream-irreducible) is explicitly falsified by the dump.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Full instrumented-trainer rebuild required (`/tmp/cb_build313` cleared)**
- **Found during:** Task 2
- **Issue:** Project memory said the instrumented `_catboost` persists in `/tmp`; in fact `/tmp/cb_build313`
  and `/tmp/cb_instr_pkg` were absent (a `/tmp` clear). Only the clang-18 toolchain (`/tmp/clang18_prefix`)
  survived.
- **Fix:** Re-ran `build_instrumented_trainer.sh` (sudo-free, idempotent), which re-provisioned the build tree
  and did a full Release build over the surviving toolchain. Staged `/tmp/cb_build313/instr_pkg`.
- **Files modified:** none (tooling only); `build_instrumented_trainer.sh` separately gained the new hook.
- **Verification:** staged `_catboost.so` (39.7 MB); RUN-ONCE replay fired all expected events.

### Plan-finding reconciliation (escalate-don't-weaken, NOT a code defect)

**2. The investigation overturned the 06.5-04 premise.** The plan framed the question as "locate the BM25
normalization transform". The evidence shows there is NO BM25 normalization — the ±1.24 belongs to a
different (embedding) feature. This is recorded as DECISION PATH-A (reproducible) with the fixture-mislabel
root cause, not as a re-deferral. The plan's verify gate (exactly one PATH-A/PATH-B with dumped-number
justification) is satisfied.

## Known Stubs

None — this is an investigation+decision plan; no production code was written. The `cb_instr_estimated_borders`
hook is a complete env-gated RUN-ONCE instrument (no-op when `CB_INSTRUMENT_LOG` unset).

## Threat Flags

None — no new network endpoints, auth paths, or trust boundaries. The three `mitigate` threats are satisfied:
T-06.5-08-01 (uncommitted patch) — `catboost-master/` subtree untracked, commit staged only DECISION.md +
the driver script; T-06.5-08-02 (false parity from source-only reading) — the A/B decision is justified by
the instrumented dumped numbers cross-checked against the raw column and the calcer_id attribution, not by
source assumption; T-06.5-08-03 (silent re-deferral) — exactly one binding DECISION: PATH-A with evidence,
no re-deferral.

## Issues Encountered

- The 06.5-04 / 06.5-07 "BM25 normalized border" and "general estimated-feature grid" deferrals are now
  understood: for BM25, the ±1.24 was never a BM25 border. The broader 06.5-07 grid question (KNN 0.5 vote
  border, BoW digitization grid) is a SEPARATE concern not touched here; this plan resolves only the BM25
  border attribution.

## Next Phase Readiness

- **06.5-09 is unblocked and re-scoped to PATH-A:** regenerate text-only BM25 (and, for hygiene,
  NaiveBayes/BoW) text-calcer fixtures from a text-only pool, then add a BM25 per-stage oracle ≤1e-5. The
  Rust seam already produces O(1e-3) borders, so no production-code change is expected; any residual becomes a
  real Rule-1 bug. FEAT-01 closes when the regenerated BM25 per-stage oracle is green.
- The instrumented trainer + `cb_instr_estimated_borders` hook are re-runnable via
  `build_instrumented_trainer.sh` for the 06.5-09 re-freeze.

## Self-Check: PASSED

- `BM25-NORMALIZATION-DECISION.md` verified on disk (177 lines) with `## Source mechanism`, `## Instrumented
  dump`, and exactly one `## DECISION: PATH-A`.
- Both task commits verified in git history (`b51aa25`, `1849b04`).
- Vendored `catboost-master/` patch confirmed untracked/uncommitted; commit staged only DECISION.md +
  build_instrumented_trainer.sh. No Rust production changes; no `#[ignore]`/weakened tolerance.
