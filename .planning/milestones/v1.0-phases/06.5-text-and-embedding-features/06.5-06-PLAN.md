---
phase: 06.5-text-and-embedding-features
plan: 06
type: execute
wave: 6
depends_on: ["06.5-05"]
files_modified:
  - crates/cb-compute/src/embedding_calcers.rs
  - crates/cb-compute/src/embedding_calcers_test.rs
  - crates/cb-compute/src/lib.rs
  - crates/cb-train/src/estimated/online_embedding.rs
  - crates/cb-train/src/estimated/online_embedding_test.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/tests/knn_oracle_test.rs
autonomous: false
requirements: [FEAT-02]
user_setup: []

must_haves:
  truths:
    - "An instrumented per-query neighbor-id SPIKE dumps the upstream HNSW neighbor lists; the implementation is EITHER an exact hand-port of library/cpp/online_hnsw OR brute-force-exact L2 validated bit-for-bit against the dump — NEVER a third-party HNSW crate (any other graph -> different neighbors -> parity fail, A2)"
    - "KNN insertion order = the per-fold learn permutation (online estimator, D-03); classification -> per-class neighbor-vote counts (width=nClasses), regression -> mean target (width=1)"
    - "KNN neighbor-vote features match upstream ≤1e-5; if HNSW exact reproduction is infeasible and brute-force does not match the dump, a documented-tolerance escalation gets human sign-off (escalate-don't-weaken)"
  artifacts:
    - path: "crates/cb-compute/src/embedding_calcers.rs"
      provides: "KNN neighbor-vote calcer (L2-squared, k-NN, per-class counts / mean)"
      contains: "knn"
    - path: "crates/cb-oracle/tests/knn_oracle_test.rs"
      provides: "KNN per-stage + neighbor-id oracle ≤1e-5"
  key_links:
    - from: "crates/cb-train/src/estimated/online_embedding.rs"
      to: "crates/cb-compute/src/embedding_calcers.rs (knn)"
      via: "insertion order = learn permutation; query k-NN from prefix-inserted vectors"
      pattern: "knn"
    - from: "crates/cb-oracle/tests/knn_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/embedding_calcers/KNN/ + instrumented neighbor-id dump"
      via: "compare_stage ≤1e-5 / neighbor-id exact compare"
      pattern: "compare"
---

<objective>
Last and highest-risk calcer — KNN (embedding k-nearest-neighbor vote). Upstream uses an online HNSW approximate index (TOnlineHnswDenseVectorIndex<float, TL2SqrDistance>) whose graph is insertion-order- and tie-break-dependent (A2). No third-party Rust HNSW crate reproduces it bit-exactly. The first task instruments per-query neighbor-id dumps, then decides between an exact hand-port of upstream online_hnsw and brute-force-exact L2 (if small fixtures make HNSW degenerate to exact).

Purpose: KNN is sequenced LAST because it carries the highest parity risk. Insertion order is already constrained to the learn permutation by online estimation (good). The neighbor set MUST be validated against instrumented dumps before relying on brute force; otherwise the exact HNSW graph must be hand-ported. A third-party HNSW crate is forbidden (parity fail).
Output: KNN calcer (hand-ported HNSW or validated brute-force) + KNN per-stage + neighbor-id oracle ≤1e-5 (or human-signed-off documented tolerance). This closes FEAT-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
</context>

## Phase Goal

**As a** Rust developer embedding catboost-rs, **I want to** train models with text and embedding features whose encodings match upstream catboost 1.2.10 to within 1e-5, **so that** existing CatBoost text/embedding pipelines port over with verifiable parity.

## Acceptance Criteria
- [ ] KNN neighbor lists validated vs instrumented dump; brute-force-vs-hand-port decision driven by real neighbor-id comparison
- [ ] KNN insertion order = learn permutation (D-03); class-vote / mean width exact
- [ ] KNN per-stage oracle ≤1e-5, or human-signed-off documented tolerance (no third-party HNSW crate)

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: KNN instrumented neighbor-id SPIKE + decision (exact HNSW hand-port vs brute-force-exact L2)</name>
  <read_first>
    - catboost-master/catboost/private/libs/embedding_features/knn.cpp (online HNSW build+query, GetNearestNeighborsAndAddItem, k default 5, opts {CloseNum,300} — lines 30-68, TRANSCRIPTION SOURCE + spike target)
    - catboost-master/catboost/private/libs/embedding_features/knn.h (TOnlineHnswDenseVectorIndex<float, TL2SqrDistance>, FeatureCount = nClasses (clf) / 1 (reg) — lines 17,94-111)
    - catboost-master/library/cpp/online_hnsw/ (the exact graph build/query to hand-port if HNSW != exact on fixtures)
    - crates/cb-oracle/fixtures/embedding_calcers/KNN/ + the instrumented per-query neighbor-id dump (Plan 01, D-07)
    - .planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md (Pitfall 2, Open Question 2, A2/A5)
  </read_first>
  <decision>How to reproduce the upstream KNN neighbor set bit-for-bit: exact hand-port of library/cpp/online_hnsw, or brute-force-exact L2 k-NN validated against the instrumented neighbor-id dump</decision>
  <context>
    Run the spike FIRST: dump the upstream per-query neighbor id lists from the Plan-01 D-07 instrumentation. Implement a brute-force-exact L2-squared k-NN (insertion order = learn permutation) and compare its neighbor ids to the dump on the actual fixture sizes. If they match (HNSW degenerate to exact at fixture scale, A5), brute force suffices; if not, hand-port the exact upstream online_hnsw graph build+query. A third-party HNSW/ANN crate is NOT an option (different graph -> different neighbors -> parity fail).
  </context>
  <options>
    <option id="brute-force-exact">
      <name>Brute-force-exact L2-squared k-NN (validated == HNSW dump)</name>
      <pros>Simple, deterministic, sum_f64-disciplined, zero dependency; matches if fixtures are non-degenerate-only above their size (A5)</pros>
      <cons>Only valid if the neighbor ids equal the instrumented HNSW dump on these fixtures — must be proven, not assumed</cons>
    </option>
    <option id="hnsw-hand-port">
      <name>Exact hand-port of library/cpp/online_hnsw (TL2SqrDistance, {CloseNum,300})</name>
      <pros>Reproduces the approximate graph bit-for-bit regardless of fixture size</pros>
      <cons>Substantial transcription of the order-/tie-break-dependent graph build; no analog in workspace</cons>
    </option>
    <option id="documented-tolerance">
      <name>Documented KNN tolerance escalation (human sign-off)</name>
      <pros>Honest if neither brute-force nor a feasible hand-port reproduces the dump ≤1e-5</pros>
      <cons>Weakens the bar for KNN only; explicit human sign-off; never silent</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — spike measurement; the chosen path's oracle is gated in Task 2. The spike produces a neighbor-id comparison report (brute-force-exact vs the instrumented HNSW dump) per query.</automated>
    <human-check>Per-query neighbor-id comparison (brute-force-exact vs instrumented HNSW dump) is presented; the path is chosen from the actual match/mismatch; no third-party HNSW crate is used</human-check>
  </verify>
  <resume-signal>Select: brute-force-exact (with the match report), hnsw-hand-port, or documented-tolerance (with rationale)</resume-signal>
  <acceptance_criteria>A per-query neighbor-id comparison report (brute-force-exact vs instrumented HNSW dump) exists; the chosen path is recorded with the match/mismatch evidence; no third-party HNSW/ANN crate appears in Cargo.toml</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KNN calcer (per chosen path) + online prefix wiring + KNN per-stage + neighbor-id oracle</name>
  <read_first>
    - crates/cb-compute/src/embedding_calcers.rs (extending from Plan 05)
    - catboost-master/catboost/private/libs/embedding_features/knn.cpp (Update: GetNearestNeighborsAndAddItem; Compute: classification result[class(neighbor)]++ / regression result[0]=mean — lines 30-68, TRANSCRIPTION SOURCE)
    - catboost-master/library/cpp/online_hnsw/ (only if hnsw-hand-port chosen in Task 1)
    - crates/cb-train/src/estimated/online_embedding.rs (from Plan 05 — extend for KNN: insertion order = learn permutation, query from prefix-inserted vectors)
    - crates/cb-train/src/ctr/online.rs (ANALOG: read-before-update prefix, error discipline 163-180)
    - crates/cb-compute/src/score.rs (ANALOG: sum_f64 routing for the L2 distance + neighbor mean)
    - crates/cb-core/src/reduction.rs (sum_f64 line 32)
    - crates/cb-oracle/src/compare.rs (compare_stage line 84, compare_permutation line 128)
    - crates/cb-oracle/fixtures/embedding_calcers/KNN/ + instrumented neighbor-id dump (Plan 01)
  </read_first>
  <behavior>
    - knn Compute (classification): neighbors = k nearest (the Task-1 path); result[class(neighbor)]++ over neighbors; width = nClasses
    - knn Compute (regression): result[0] = mean(target(neighbor)); width = 1
    - online: insertion order = &fold.permutation; query a document's k-NN from ONLY the prefix-inserted vectors (read-before-update, D-03), THEN add the current vector + its target
    - L2-squared distance + neighbor mean route through sum_f64; k > prefix size -> typed CbError (CB_ENSURE / .at() bounds analog)
  </behavior>
  <action>
    Extend embedding_calcers.rs with the KNN arm per the Task-1 decision (brute-force-exact L2-squared with sum_f64, OR the hand-ported online_hnsw graph). Transcribe Compute from knn.cpp:30-68: classification -> per-class neighbor-vote counts (width=nClasses), regression -> mean target (width=1, Pitfall 5). k default 5 (fixtures k=10/20). Extend estimated/online_embedding.rs so KNN insertion order = &fold.permutation and each document queries k-NN from prefix-inserted vectors ONLY before adding itself (leakage control, D-03). L2 distance + mean via sum_f64; checked .get(..), typed CbError on k>N / dim-mismatch, no unwrap/index. Create crates/cb-oracle/tests/knn_oracle_test.rs: full 4-stage gate (Splits|LeafValues|StagedApprox|Predictions) + borders + the per-query neighbor-id exact compare vs the instrumented dump + the class-vote feature ≤1e-5 vs fixtures/embedding_calcers/KNN/. If Task-1 chose documented-tolerance, assert the human-signed-off tolerance with rationale cited in-file; else ≤1e-5. NO #[ignore], NO third-party HNSW crate, NO silent weakening. Tests in embedding_calcers_test.rs + online_embedding_test.rs siblings.
  </action>
  <verify>
    <automated>cargo test -p cb-compute embedding_calcers::knn && cargo test -p cb-train estimated::online_embedding && cargo test -p cb-oracle --test knn_oracle_test</automated>
  </verify>
  <done>KNN neighbor ids match the instrumented dump; class-vote/mean features ≤1e-5 (or human-signed-off documented tolerance); insertion order = learn permutation; no #[ignore]; no third-party HNSW crate; cb-train non-embedding path byte-identical</done>
  <acceptance_criteria>cargo test -p cb-oracle --test knn_oracle_test exits 0 (neighbor-id exact + ≤1e-5 or the documented tolerance, 0 ignored); cargo test -p cb-train --lib exits 0 (no regression); cargo clippy -p cb-compute --lib -p cb-train --lib 0 indexing_slicing/unwrap_used in the new code; no hnsw/ANN crate in Cargo.toml</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pool embedding column → KNN distance/index | k > learn size / embedding-dim mismatch could index past arrays |
| Third-party HNSW crate | FORBIDDEN — any non-identical graph breaks parity (A2); not a supply-chain decision, a correctness ban |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-06-01 | Denial of Service (panic) | KNN query on k>N / dim-mismatch / empty prefix | mitigate | Bounds-checked .get(..); CB_ENSURE / .at() bounds analog -> typed CbError; no unwrap/index (INFRA-02, V5) |
| T-06.5-06-02 | Integrity (parity) | neighbor-graph reproducibility | mitigate | Spike-validated neighbor ids vs instrumented dump; exact hand-port or proven brute-force; sum_f64 distances |
| T-06.5-06-SC | Tampering | no package install (HNSW crate forbidden) | accept | No new packages; third-party HNSW/ANN explicitly banned for parity (A2); D-05 no new crate |
</threat_model>

<verification>
- KNN neighbor ids match the instrumented dump; class-vote/mean features ≤1e-5 (or human-signed-off documented tolerance, no #[ignore])
- No third-party HNSW/ANN crate in Cargo.toml
- cb-train non-embedding path byte-identical
</verification>

<success_criteria>
FEAT-02 complete: LDA (Plan 05) + KNN both produce upstream-matching embedding encodings ≤1e-5 (or honest human-signed-off tolerances), computed leakage-controlled online over the TFold permutation. SC-3 closed.
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-06-SUMMARY.md` when done
</output>
