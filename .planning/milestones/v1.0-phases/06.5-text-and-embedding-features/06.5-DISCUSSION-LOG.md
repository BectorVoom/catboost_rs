# Phase 6.5: Text & Embedding Features - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 6.5-text-and-embedding-features
**Mode:** `--auto` (Claude auto-selected the recommended, parity-/scope-dictated option for every gray area; no interactive prompts)
**Areas discussed:** Tokenizer scope & sequencing, Calcer online-estimation strategy, Layering/placement, Oracle ground-truth sourcing

---

## Tokenizer scope & sequencing (D-11 first risk)

| Option | Description | Selected |
|--------|-------------|----------|
| Tokenizer-first, fixture-bounded option set | Make the token/digitizer stream a bit-identical first gate; implement upstream defaults + the options the frozen fixtures exercise (ByDelimiter, lowercasing, freq dictionary) | ✓ |
| Full upstream option matrix up front | Implement every tokenizer/dictionary option (BPE, subword, custom regex) speculatively | |
| Calcers first, tokenizer approximated | Stub tokenization, focus on calcer math | |

**Auto-selected:** Tokenizer-first, fixture-bounded option set.
**Notes:** SC-1 + D-11 name the tokenizer as the load-bearing risk to nail first. Speculative options not reachable by FEAT-01/02 fixtures are deferred to avoid scope creep and untestable code. → D-01, D-02.

---

## Calcer online-estimation strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse Phase-5 ordered/fold online estimation (leakage control) | Drive NaiveBayes/BM25/LDA/KNN as `IOnlineFeatureEstimator`s over the learn permutation, like CTRs; BoW stays simple | ✓ |
| Simple (non-online) estimation for all calcers | Compute calcer features once over the full training set | |

**Auto-selected:** Reuse Phase-5 ordered/fold online estimation.
**Notes:** Verified upstream that these calcers are online + target-aware (`ComputeOnlineFeatures(learnPermutation)`, class/classificationTarget). Simplifying breaks the per-stage oracle — parity-dictated, no shortcut. → D-03, D-04.

---

## Layering / placement

| Option | Description | Selected |
|--------|-------------|----------|
| No new crate — modules in cb-data (+ wiring in cb-train) | Tokenizer/dictionary/calcer math as modules co-located with Pool + quantization; online wiring in cb-train; output flows into existing quantize→tree path | ✓ |
| New dedicated `cb-text`/`cb-features` crate | Spin up a separate crate for the calcer layer | |

**Auto-selected:** No new crate — modules.
**Notes:** Mirrors the 06.3-15 layering discipline; Pool already owns the raw text/embedding columns (DATA-01) and the quantization path already exists, so the calcer layer is additive modules, not a new crate. → D-05.

---

## Oracle ground-truth sourcing (D-07)

| Option | Description | Selected |
|--------|-------------|----------|
| Python-reachable first, C++ instrument the thin internals | catboost 1.2.10 + canondata fixtures for per-stage; reuse the persistent instrumented trainer for token stream / dictionary ids / online order | ✓ |
| Python-only | Rely solely on catboost Python surface, weaken where ground truth is thin | |
| Instrument everything | Build all ground truth from the instrumented trainer | |

**Auto-selected:** Python-reachable first, C++ instrument the thin internals.
**Notes:** Matches SC-4 + D-07. Reuses the persistent `/tmp/cb_build313` + clang-18 instrumented trainer from 06.3. Escalate-don't-weaken: no fabricated fixtures, no `#[ignore]`, no loosened tolerance. → D-06, D-07.

---

## Claude's Discretion

- Exact module split between cb-data and cb-compute for calcer primitive math.
- Whether the tokenizer-first gate is one plan or split into tokenizer + dictionary/digitizer plans (sequencing principle fixed; granularity flexible).

## Deferred Ideas

- GPU calcer kernels → Phase 7 (CubeCL).
- Python/PyO3 ingestion of text & embedding pools → Phase 8.
- Advanced features FEAT-03/04/05/06 + non-symmetric trees → Phase 6.6.
- BPE/subword tokenizers, custom dictionary types, non-fixture tokenizer options → revisit only if a later requirement needs them.
