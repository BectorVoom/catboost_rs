---
phase: 06.5-text-and-embedding-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-oracle/generator/build_instrumented_trainer.sh
  - crates/cb-oracle/generator/instrument_text_pipeline.cpp
  - crates/cb-oracle/generator/instrument_text_pipeline_README.md
  - crates/cb-oracle/generator/gen_text_embedding_fixtures.py
  - crates/cb-oracle/fixtures/text_tokenizer/
  - crates/cb-oracle/fixtures/text_calcers/
  - crates/cb-oracle/fixtures/embedding_calcers/
autonomous: false
requirements: [FEAT-01, FEAT-02]
user_setup: []

must_haves:
  truths:
    - "An instrumented catboost 1.2.10 trainer emits a CB_INSTRUMENT_LOG dump of the per-text token stream, per-dictionary token->id table, per-document TText (tokenId,count) list, per-calcer intermediate encodings, online-estimation visiting order, LDA projection matrix, and KNN per-query neighbor ids"
    - "Per-stage .npy fixtures (borders/splits/leaf/staged/prediction) for BoW/NaiveBayes/BM25/LDA/KNN are regenerated SINGLE-THREADED (thread_count=1) via .venv catboost 1.2.10, NOT consumed from -T 4 canondata"
    - "Each fixture directory carries a pinned model.json + config.json (seed, catboost_version=1.2.10, thread_count=1)"
  artifacts:
    - path: "crates/cb-oracle/generator/instrument_text_pipeline.cpp"
      provides: "CB_INSTRUMENT_LOG hooks for tokenizer/dictionary/calcer/online-order/LDA-projection/KNN-neighbor internals"
    - path: "crates/cb-oracle/generator/gen_text_embedding_fixtures.py"
      provides: "single-thread per-stage fixture generator for the 5 calcers + the tokenizer/dictionary D-01 corpus"
    - path: "crates/cb-oracle/fixtures/text_tokenizer/"
      provides: "D-01 token-stream + dictionary-id ground truth (instrumented)"
  key_links:
    - from: "crates/cb-oracle/generator/build_instrumented_trainer.sh"
      to: "/tmp/cb_build313 + clang-18 prefix"
      via: "incremental rebuild over the warm tree (re-provision sudo-free if absent)"
      pattern: "CB_INSTRUMENT_LOG"
---

<objective>
Wave 0 for Phase 6.5: stand up the D-07 instrumented ground-truth source and the single-thread per-stage fixture corpora that every later calcer slice oracles against. This is the prerequisite gate — no calcer (Plans 02-07) can assert parity until its fixtures exist and the instrumented token/dictionary/encoding/neighbor dump is captured.

Purpose: The tokenizer token stream, dictionary token->id assignment, per-calcer intermediate encodings, online-estimation permutation order, LDA projection matrix, and KNN neighbor ids are NOT cleanly Python-reachable (D-07). They require the persistent instrumented catboost 1.2.10 trainer. The per-stage .npy fixtures must be regenerated thread_count=1 (canondata is -T 4 — A6).
Output: A re-runnable instrumented trainer with text/embedding CB_INSTRUMENT_LOG hooks, a single-thread fixture generator, and frozen fixture corpora for tokenizer + 5 calcers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-VALIDATION.md
</context>

## Phase Goal

**As a** Rust developer embedding catboost-rs, **I want to** train models with text and embedding features whose encodings match upstream catboost 1.2.10 to within 1e-5, **so that** existing CatBoost text/embedding pipelines port over with verifiable parity.

(Note: the ROADMAP Phase 6.5 `**Goal:**` line is in prose form, not user-story form. Story framed here from the phase goal + FEAT-01/02 scope; surfaced for a future `/gsd mvp-phase 06.5` normalization. Does not block planning — `--auto` mode.)

## Acceptance Criteria
- [ ] Instrumented trainer rebuilds (incremental over warm `/tmp/cb_build313`, else re-provisioned sudo-free) and emits the text/embedding CB_INSTRUMENT_LOG dump
- [ ] Per-stage fixtures for BoW/NaiveBayes/BM25/LDA/KNN exist, generated thread_count=1
- [ ] Tokenizer/dictionary D-01 instrumented ground-truth corpus frozen

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Re-provision + rebuild the instrumented catboost 1.2.10 trainer with text/embedding hooks</name>
  <read_first>
    - crates/cb-oracle/generator/build_instrumented_trainer.sh (the committed sudo-free recipe — disk-gate, clang-18/lld-18 via apt-get download+dpkg -x, build_native.py driver; from 06.3-10/17)
    - crates/cb-oracle/generator/instrumented_trainer_STATUS.md (the GO record + env-gate convention)
    - crates/cb-oracle/generator/instrument_live_trainer_README.md (the CB_INSTRUMENT_LOG JSONL hook pattern)
    - catboost-master/catboost/private/libs/text_processing/tokenizer.cpp (token-stream emit point)
    - catboost-master/catboost/library/cpp/text_processing/dictionary/dictionary_builder.cpp (token->id assignment, lines 146-162)
    - catboost-master/catboost/private/libs/data_types/text.h (TText sorted-RLE, lines 155-164)
    - catboost-master/catboost/private/libs/feature_estimator/base_text_feature_estimator.h (ComputeOnlineFeatures learnPermutation loop, lines 59-64)
    - catboost-master/catboost/private/libs/embedding_features/lda.cpp (projection matrix dump point, lines 10-37)
    - catboost-master/catboost/private/libs/embedding_features/knn.cpp (per-query neighbor ids, lines 30-68)
  </read_first>
  <action>
    Re-run build_instrumented_trainer.sh: if /tmp/cb_build313 + /tmp/clang18_prefix are absent (RESEARCH Environment Availability marks /tmp cleared), the script re-provisions sudo-free; if warm, incremental rebuild. Add new env-gated (CB_INSTRUMENT_LOG-only, no-op when unset) instrumentation TUs in instrument_text_pipeline.cpp wiring into the vendored catboost-master/ sources: (a) post-split/post-lowercase token list per text (tokenizer.cpp), (b) dictionary (token-string -> id, count) table per dictionary after sort/filter/truncate (dictionary_builder.cpp), (c) per-document TText (tokenId,count) list (text.h), (d) per-calcer intermediate encoding per document (bow/naive_bayesian/bm25/lda/knn calcer Compute), (e) the learn-permutation visiting order actually used by ComputeOnlineFeatures (base_text_feature_estimator.h), (f) LDA projection matrix + intermediate scatter (lda.cpp CalculateProjection), (g) KNN per-query neighbor id lists (knn.cpp). Emit as JSONL keyed by hook name (mirror the 06.3 yeti_gumbel/leaf_der event schema). Vendored catboost-master/ instrumentation patches stay UNCOMMITTED (D-09/D-12). Document hook names + invocation in instrument_text_pipeline_README.md. OFFLINE/RUN-ONCE.
  </action>
  <what-built>The instrumented trainer rebuilt with 7 text/embedding CB_INSTRUMENT_LOG hooks; a smoke train of a CatBoostClassifier with text + embedding features emits a non-empty JSONL dump covering all 7 hook categories.</what-built>
  <how-to-verify>
    1. Run: bash crates/cb-oracle/generator/build_instrumented_trainer.sh (expect RC=0; reports disk-gate + build summary)
    2. Set CB_INSTRUMENT_LOG to a temp path, run the smoke fit inside .venv (catboost 1.2.10) with a tiny text+embedding pool, confirm the JSONL has events for token_stream / dict_ids / ttext / calcer_encoding / online_order / lda_projection / knn_neighbors
    3. Confirm git status shows catboost-master/ patches UNCOMMITTED
  </how-to-verify>
  <verify>
    <automated>MISSING — RUN-ONCE offline build; verified by the human-check below (no committable automated gate; trainer artifacts live in /tmp)</automated>
    <human-check>Trainer RC=0; JSONL dump non-empty across all 7 hook categories; catboost-master/ patches uncommitted</human-check>
  </verify>
  <resume-signal>Type "approved" when the instrumented dump is captured, or describe the build failure</resume-signal>
  <acceptance_criteria>build_instrumented_trainer.sh exits 0; CB_INSTRUMENT_LOG smoke dump contains events for all 7 hook categories; vendored patches uncommitted</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Single-thread per-stage fixture generator + frozen corpora (tokenizer + 5 calcers)</name>
  <read_first>
    - crates/cb-oracle/generator/gen_fixtures.py (existing single-thread fixture generator pattern)
    - crates/cb-oracle/generator/gen_ranking_fixtures.py (per-stage .npy + model.json + config.json freezing, 06.3 pattern)
    - crates/cb-oracle/src/fixture.rs (load_f64_vec line 21, load_config line 43, FixtureConfig schema)
    - crates/cb-oracle/src/model_json.rs (ModelJson line 168, split_borders 190, leaf_values 200, leaf_weights 212)
    - catboost-master/catboost/pytest/test.py (rotten_tomatoes fixture params: Logloss, 20 iters; LDA reg=0.05 likelihood=true; KNN k=20/10 — lines ~9921-10516)
    - catboost-master/catboost/private/libs/options/text_processing_options.h (defaults to pin: DEFAULT_DICTIONARY_BUILDER_OPTIONS, OLB layers, lines 43-49)
    - catboost-master/catboost/libs/train_lib/options_helper.cpp (data-dependent OLB = poolSize<1000?1:5, lines 394-401)
  </read_first>
  <action>
    Write gen_text_embedding_fixtures.py: build a small deterministic text+embedding pool (bound the learn-pool size and RECORD wc -l of the learn split — A4: OLB boundary is strict <1000, so verify whether the corpus yields OLB=1 or 5 and PIN it explicitly, do NOT assume). Train one catboost 1.2.10 model per calcer (BoW, NaiveBayes, BM25, LDA, KNN) thread_count=1, seed pinned, ByDelimiter tokenizer + frequency Word dictionary (+ BiGram for BoW per Pitfall 4), reproducing upstream defaults (D-02). For each, freeze per-stage .npy (borders/splits/leaf/staged/prediction) + model.json + config.json under crates/cb-oracle/fixtures/<calcer>/. Separately freeze the tokenizer/dictionary D-01 corpus (the instrumented token-stream + dict-id + TText dump from Task 1) under fixtures/text_tokenizer/. Do NOT consume -T 4 canondata directly (A6). NO fabricated values — every number comes from the live single-thread trainer.
  </action>
  <verify>
    <automated>ls crates/cb-oracle/fixtures/text_tokenizer/ crates/cb-oracle/fixtures/text_calcers/ crates/cb-oracle/fixtures/embedding_calcers/ && python3 -c "import json,glob; [json.load(open(p)) for p in glob.glob('crates/cb-oracle/fixtures/**/config.json', recursive=True)]"</automated>
  </verify>
  <done>Each of the 5 calcer fixture dirs has borders/splits/leaf/staged/prediction .npy + model.json + config.json (thread_count=1); the tokenizer D-01 corpus is frozen; the learn-pool size + pinned OLB are recorded in the generator</done>
  <acceptance_criteria>fixtures/text_tokenizer + fixtures/text_calcers/{BoW,NaiveBayes,BM25} + fixtures/embedding_calcers/{LDA,KNN} each exist with config.json carrying thread_count=1 and catboost_version=1.2.10; generator records the wc -l learn-pool size and the pinned OccurrenceLowerBound</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catboost-master/ source ← instrumentation patch | Uncommitted local patch to vendored C++; must not leak into the committed tree |
| .venv catboost ← fixture generator | Trusted offline ML library producing ground-truth numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-01 | Tampering | vendored catboost-master/ instrumentation patch | mitigate | Patches stay UNCOMMITTED (D-09/D-12); git status verified in Task 1 human-check |
| T-06.5-02 | Integrity | per-stage fixtures | mitigate | thread_count=1 single-thread regeneration (A6); no fabricated values; config.json pins seed/version |
| T-06.5-SC | Tampering | no npm/pip/cargo install in this plan | accept | No new packages; .venv catboost 1.2.10 already pinned (Phase 1) |
</threat_model>

<verification>
- Instrumented trainer rebuilds and emits a 7-category CB_INSTRUMENT_LOG dump (human-verified)
- All 5 calcer fixture corpora + the tokenizer D-01 corpus exist with thread_count=1 config.json
- Vendored catboost-master/ patches uncommitted
</verification>

<success_criteria>
The Wave-0 ground-truth source (instrumented trainer) and single-thread fixture corpora exist so every later calcer slice has a real oracle to gate against. No parity assertion happens here — this plan only produces ground truth.
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-01-SUMMARY.md` when done
</output>
