---
phase: 06.5-text-and-embedding-features
plan: 09
subsystem: oracle+fixtures
tags: [catboost, bm25, text-features, estimated-features, per-stage-oracle, fixture-correctness, FEAT-01, sc-2, gap-closure, path-a]

# Dependency graph
requires:
  - phase: 06.5-text-and-embedding-features
    plan: 08
    provides: BINDING DECISION PATH-A — the BM25 ±1.24 "normalized border" is a fixture mislabel (the default embedding calcer's border on emb0); real BM25 borders are O(1e-3) and already reproduced ≤1e-5. Mandate to regenerate text-only fixtures + add per-stage oracle.
  - phase: 06.5-text-and-embedding-features
    plan: 04
    provides: BM25 calcer math + online_text read-before-update seam ≤1e-5; the open ±1.24 border deferral this plan closes
  - phase: 06.5-text-and-embedding-features
    plan: 01
    provides: instrumented catboost 1.2.10 single-thread trainer + CB_INSTRUMENT_LOG hooks used to re-freeze the text-only fixtures
provides:
  - "BM25 per-stage oracle GREEN (Splits/LeafValues/StagedApprox/Predictions ≤1e-5, 0 ignored) — SC-2 / FEAT-01 CLOSED"
  - "BM25 text-calcer fixtures regenerated from a TEXT-ONLY pool (genuine O(1e-3) BM25 text-feature borders, calcer_id 0BDFE5…); BoW/NaiveBayes per-stage .npy byte-identical (zero regression)"
  - "gen_text_embedding_fixtures.py::_make_pool(text_only=True) — text-calcer path drops emb0 so fixtures record the text calcer's own borders"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Text-calcer fixtures MUST be frozen from a text-only pool: including embedding_features lets the embedding feature dominate the split search, and the frozen splits.npy then mislabels the embedding border as the text calcer's"
    - "BM25 Plain-mode online-tree / offline-apply contract: Splits/LeafValues come from the ONLINE-estimate tree, but StagedApprox/Predictions apply through the OFFLINE whole-set column — doc 0's online no-leakage value is 0, yet its offline value routes it to the correct leaf (the single point where BM25 differs from NaiveBayes; documented in online_text.rs)"

key-files:
  created: []
  modified:
    - crates/cb-oracle/generator/gen_text_embedding_fixtures.py
    - crates/cb-oracle/fixtures/text_calcers/BM25/
    - crates/cb-oracle/fixtures/text_calcers/BoW/model.cbm
    - crates/cb-oracle/fixtures/text_calcers/NaiveBayes/model.cbm
    - crates/cb-oracle/tests/bm25_oracle_test.rs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/06.5-text-and-embedding-features/deferred-items.md

key-decisions:
  - "Executed PATH-A (the 06.5-08 binding DECISION) as a FIXTURE-CORRECTNESS fix, not a normalization implementation. The PLAN.md prose (written before 08 concluded) assumed implementing a normalization transform; recorded a DEVIATION and followed the authoritative DECISION doc instead."
  - "No production trainer code changed — the Rust estimated_features.rs seam already produces O(1e-3) BM25 borders. The fix was entirely fixture regeneration + a new per-stage oracle. No #[ignore], no weakened tolerance, no fabricated fixtures."

requirements-completed: [FEAT-01]

# Metrics
duration: ~12m (Tasks 1-2; Task 3 = human-verify checkpoint, approved)
completed: 2026-06-18
---

# Phase 06.5 Plan 09: Close BM25 SC-2 / FEAT-01 via PATH-A fixture-correctness fix Summary

**Closed the last open Phase 6.5 gap (SC-2 / FEAT-01: BM25 per-stage parity) by executing the 06.5-08 binding DECISION PATH-A. The "BM25 ±1.24 normalized border" never existed — it was the default embedding calcer's border on `emb0`, frozen into the BM25 fixture because the fixture pool wrongly included `embedding_features=["emb0"]`. Regenerated the BM25 (and for hygiene BoW/NaiveBayes) text-calcer fixtures from a TEXT-ONLY pool via the instrumented single-thread trainer, then added the BM25 per-stage oracle. Result: 7/7 BM25 oracle tests pass, 0 ignored, all stages ≤1e-5 (StagedApprox/Predictions max err 2.9e-8). BoW/NaiveBayes per-stage arrays came back byte-identical → zero regression. No production trainer code changed.**

## Tasks completed

| Task | Name | Commit |
| ---- | ---- | ------ |
| 1 | Execute recorded path — PATH-A fixture-correctness fix (`_make_pool(text_only=True)`, regenerate BM25/BoW/NaiveBayes fixtures) | `3426145` |
| 2 | BM25 per-stage oracle + own FEAT-01/SC-2 closure (REQUIREMENTS/ROADMAP/deferred-items) | `0e87a1a` |
| 3 | Human-verify checkpoint (SC-2/FEAT-01 closure gate) | **APPROVED** by user 2026-06-18 |

## Deviation recorded

The PLAN.md `<objective>`/prose assumed implementing a BM25 normalization **transform** (it was authored before plan 06.5-08 concluded). The binding `BM25-NORMALIZATION-DECISION.md` (PATH-A) established there is **no normalization** — the symptom was a fixture mislabel. Per the execute-plan deviation protocol, the DECISION doc was treated as authoritative and the work was redirected to the fixture-correctness path. No scope was silently dropped: PATH-A fully closes the same SC-2 / FEAT-01 the original plan targeted.

## Verification (Task 3, independently confirmed by orchestrator before sign-off)

- `cargo test -p cb-oracle --test bm25_oracle_test` → **7 passed, 0 failed, 0 ignored**:
  - `bm25_oracle_splits_match_upstream` — Splits ≤1e-5 (online-estimate tree, 2 distinct splits/tree)
  - `bm25_oracle_leaf_values_match_upstream` — LeafValues ≤1e-5
  - `bm25_oracle_staged_approx_match_upstream` — StagedApprox max err 2.9e-8 (offline whole-set apply column)
  - `bm25_oracle_predictions_match_upstream` — Predictions max err 2.9e-8
  - `bm25_oracle_columns_match_closed_form`, `bm25_oracle_first_prefix_is_empty_no_leakage`, `bm25_oracle_borders_selected_through_existing_quantizer`
- Text-only BM25 borders reproduced: `0.00248965, 0.00127047, …` (O(1e-3), `calcer_id=0BDFE5…`) — the genuine BM25 text-feature borders, NOT the old ±1.24 embedding-calcer mislabel.
- No regression: `naive_bayes_oracle_test` 5/5, `bow_oracle_test` 4/4, `cb-train --lib estimated::` 31/31 — all GREEN.

## Outcome

- **FEAT-01 → Complete** (REQUIREMENTS.md): all three text calcers BoW / NaiveBayes / BM25 per-stage ≤1e-5.
- **SC-2 → CLOSED** (ROADMAP.md): text calcers produce upstream-matching encodings ≤1e-5.
- **deferred-items.md BM25 section → CLOSED** (owned, not deferred): the deferral rested on a fixture mislabel; resolved, no trainer normalization exists.

## Notes / follow-ups

- One genuine finding within PATH-A: BM25's staged/predictions apply through the offline whole-set column (the documented Plain-mode online-tree / offline-apply contract). This is test-side apply logic mirroring the serialized model — no production change.
- The vendored `catboost-master/` instrumentation patch remains UNCOMMITTED (the subtree is untracked, per D-09/D-12).
- The `gen_text_embedding_fixtures.py::catboost` import is flagged by Pyright (catboost lives in `.venv`); environment-only, not a defect.
