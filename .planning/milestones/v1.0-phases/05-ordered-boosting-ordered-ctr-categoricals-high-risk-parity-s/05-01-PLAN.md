---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-oracle/src/compare.rs
  - crates/cb-oracle/src/compare_test.rs
  - crates/cb-oracle/src/model_json.rs
  - crates/cb-oracle/src/model_json_test.rs
  - crates/cb-oracle/generator/ordered_oracle.cpp
  - crates/cb-oracle/generator/README.md
  - crates/cb-oracle/fixtures/
autonomous: true
requirements: [ORD-01, ORD-02, ORD-03, ORD-04, ORD-05]
must_haves:
  truths:
    - "compare_stage exposes Stage::Permutation (integer-exact), Stage::OnlineCtr (≤1e-5), Stage::OrderedApprox (≤1e-5)"
    - "model_json parser reads the ctr_data section of an upstream model.json (was borders-only)"
    - "ordered_oracle.cpp transcribes permutation + online CTR + body/tail + ordered approx with ZERO catboost includes and dumps the D-02 .npy stack"
    - "Purpose-built categorical fixtures exist: one-hot column, CTR column, tiny N, per-fold permutation .npy, per-object num/denom/approx .npy, per-CTR-type config"
    - "Stage::Permutation comparison rejects any index mismatch BEFORE a value stage can run (D-03 ordering)"
  artifacts:
    - path: "crates/cb-oracle/src/compare.rs"
      provides: "Stage::Permutation, Stage::OnlineCtr, Stage::OrderedApprox variants + integer-exact compare path"
      contains: "Permutation"
    - path: "crates/cb-oracle/src/model_json.rs"
      provides: "CtrDataJson serde struct + ctr_data() accessor"
      contains: "ctr_data"
    - path: "crates/cb-oracle/generator/ordered_oracle.cpp"
      provides: "transcribe-then-self-oracle standalone harness (zero catboost includes)"
      min_lines: 80
  key_links:
    - from: "crates/cb-oracle/src/compare.rs"
      to: "cb-oracle::compare_stage"
      via: "Stage match arm dispatch (integer-exact vs 1e-5)"
      pattern: "Stage::Permutation"
    - from: "crates/cb-oracle/generator/ordered_oracle.cpp"
      to: "crates/cb-oracle/fixtures/"
      via: "offline .npy dump consumed by load_f64_vec / Stage comparisons"
      pattern: "permutation_fold"
---

<objective>
Wave 0 oracle infrastructure for the high-risk parity slice. Stand up every oracle-harness gap from 05-VALIDATION.md BEFORE any dependent algorithm slice so each later slice (one-hot → permutation → Plain CTR → Ordered CTR → Ordered boosting → tensor CTR) lands against a ready comparator.

This plan implements the approved **transcribe-then-self-oracle** mechanism (CONTEXT.md DECISION REVISION 2026-06-14 + RESEARCH § "Per-Object Oracle Strategy"), NOT the superseded D-01 TU-linking (research proved those upstream TUs have no isolatable link boundary). The harness is a dependency-free transcription (zero catboost includes), mirroring the `cityhash_oracle.cpp` precedent.

Purpose: A per-object intermediate oracle is the entire point of this phase — a prefix-boundary off-by-one is exactly the bug final-prediction parity would mask. Locking the comparator + fixtures first makes every downstream divergence isolate to a single mechanism.
Output: Stage variants, ctr_data JSON parsing, the transcribed harness, and the frozen committed categorical fixtures the rest of the phase oracles against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-PATTERNS.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VALIDATION.md

@crates/cb-oracle/src/compare.rs
@crates/cb-oracle/src/model_json.rs
@crates/cb-oracle/src/fixture.rs
@crates/cb-oracle/generator/cityhash_oracle.cpp
@crates/cb-core/src/rng.rs

<interfaces>
<!-- Executor uses these directly; no codebase exploration needed. -->

From crates/cb-oracle/src/compare.rs (extend the enum + compare_stage):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Stage { Borders, Splits, LeafValues, StagedApprox, Predictions }
// compare_stage(expected, actual, stage, tol) uses the non-finite-diff guard `!(diff <= tol)`.
pub fn assert_abs_close(expected: &[f64], actual: &[f64], tol: f64) -> Result<(), OracleError>;
```
ADD: `Permutation` (integer-exact: compare via `==` on rounded/int values, NOT 1e-5),
`OnlineCtr`, `OrderedApprox` (both ≤1e-5 via existing compare path). Preserve the `!(diff <= tol)` NaN-safe guard.

From crates/cb-oracle/src/model_json.rs (extend, mirror the borders-only structs):
```rust
#[derive(Debug, Clone, PartialEq, Deserialize)]
pub struct FloatFeatureJson { pub feature_index: i64, #[serde(default)] pub borders: Vec<f64> }
pub fn float_feature_borders(&self) -> Vec<Vec<f64>> { /* accessor */ }
```
ADD a `CtrDataJson` struct over the upstream `ctr_data` hash_map (TCtrValueTable blobs: counts per bucket; interpret per static_ctr_provider.cpp:14-126) with `#[serde(default)]` so Phase-3/4 fixtures without ctr_data keep parsing (RESEARCH A5).

From crates/cb-core/src/rng.rs (the permutation seed — DO NOT re-port):
```rust
impl TFastRng64 {
    pub fn from_seed(seed: u64) -> Self;
    pub fn uniform(&mut self, bound: u64) -> u64;     // bit-exact GenUniform
    pub fn try_uniform(&mut self, bound: u64) -> CbResult<u64>;
}
```

From crates/cb-oracle/src/fixture.rs (fixture I/O the harness output feeds):
```rust
pub fn load_f64_vec(path: &Path) -> Result<Vec<f64>, OracleError>;   // reads .npy
pub struct FixtureConfig { /* seed, catboost_version, thread_count, ... */ }
pub fn load_config(path: &Path) -> Result<FixtureConfig, OracleError>;
```
</interfaces>
</context>

<artifacts_this_phase_produces>
Symbols/files this Wave 0 introduces (exclude from source-grounding drift verification — they are NEW this phase):
- `cb-oracle::Stage::Permutation`, `cb-oracle::Stage::OnlineCtr`, `cb-oracle::Stage::OrderedApprox` (enum variants in compare.rs)
- `cb-oracle::model_json::CtrDataJson` + a `ctr_data()` accessor on the model-json root
- `crates/cb-oracle/generator/ordered_oracle.cpp` (new transcribed harness)
- Fixture trees under `crates/cb-oracle/fixtures/`: `one_hot_cat/`, `plain_ctr/`, `ordered_ctr/`, `ordered_boost/`, `tensor_ctr/` (exact set named in tasks), each with `config.json` + `.npy` stack
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add Stage variants + integer-exact permutation compare path</name>
  <read_first>crates/cb-oracle/src/compare.rs, crates/cb-oracle/src/compare_test.rs, crates/cb-oracle/src/lib.rs</read_first>
  <files>crates/cb-oracle/src/compare.rs, crates/cb-oracle/src/compare_test.rs</files>
  <action>Extend the `Stage` enum with `Permutation`, `OnlineCtr`, `OrderedApprox` (per RESEARCH § "Per-Object Oracle Schema (D-02)" + 05-VALIDATION Wave-0). `OnlineCtr`/`OrderedApprox` route through the EXISTING ≤1e-5 `compare_stage` path (the `!(diff <= tol)` NaN-safe guard at compare.rs:44 is the template — preserve it). `Permutation` is integer-exact: add a comparison path that asserts element-wise equality of integer-valued indices (compare as integers, NOT at 1e-5) and returns `OracleError` on the first mismatch — this implements the D-03 linchpin lock (permutation reproduced exactly BEFORE any value stage runs). Keep the function `unwrap`/`expect`/`panic`-free (INFRA-02; tests opt out via lib.rs test cfg). No `anyhow`. Tests go in compare_test.rs (source/test separation MANDATORY) — cover: a permutation match passes, a single-index swap fails, an OnlineCtr vector within 1e-5 passes, one outside fails.</action>
  <verify>
    <automated>cargo test -p cb-oracle compare</automated>
  </verify>
  <done>Stage::{Permutation,OnlineCtr,OrderedApprox} exist; permutation compare is integer-exact and rejects any swap; OnlineCtr/OrderedApprox honor ≤1e-5; compare_test.rs covers pass+fail for both paths; no unwrap/anyhow in production.</done>
</task>

<task type="auto">
  <name>Task 2: Parse the model.json ctr_data section</name>
  <read_first>crates/cb-oracle/src/model_json.rs, crates/cb-oracle/src/model_json_test.rs, crates/cb-model/src/generated/ctr_data_generated.rs</read_first>
  <files>crates/cb-oracle/src/model_json.rs, crates/cb-oracle/src/model_json_test.rs</files>
  <action>Add a `CtrDataJson` `#[derive(Deserialize)]` struct mirroring the borders-only structs (model_json.rs:54-62 FloatFeatureJson pattern) that parses the upstream model.json `ctr_data` hash_map — the per-bucket `TCtrValueTable` counts (interpret per RESEARCH "Model-side inference apply" + static_ctr_provider.cpp:14-126: Borders→ctrHistory[1]/[0]+[1]; Counter/FeatureFreq→ctrTotal[bucket]/CounterDenominator; Mean→Sum/Count). Mark every new field `#[serde(default)]` so Phase-3/4 fixtures WITHOUT ctr_data keep parsing (RESEARCH A5 — confirmed currently borders-only). Add a `ctr_data()` accessor returning the parsed table(s) for per-CTR-type comparison via `compare_stage`. Stay `unwrap`/`expect`-free (T-03-00-01 discipline); use `thiserror` OracleError variants for any malformed-blob path, never anyhow. Tests in model_json_test.rs: a borders-only fixture still parses (default empty ctr_data); a fixture with a ctr_data block round-trips its counts.</action>
  <verify>
    <automated>cargo test -p cb-oracle model_json</automated>
  </verify>
  <done>CtrDataJson parses an upstream ctr_data hash_map; borders-only fixtures still parse (backward-compatible #[serde(default)]); ctr_data() accessor returns per-bucket counts; no unwrap/anyhow.</done>
</task>

<task type="auto">
  <name>Task 3: Transcribe ordered_oracle.cpp + generate frozen categorical fixtures</name>
  <read_first>crates/cb-oracle/generator/cityhash_oracle.cpp, crates/cb-oracle/generator/README.md, crates/cb-oracle/src/fixture.rs, .planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md</read_first>
  <files>crates/cb-oracle/generator/ordered_oracle.cpp, crates/cb-oracle/generator/README.md, crates/cb-oracle/fixtures/</files>
  <action>Create `crates/cb-oracle/generator/ordered_oracle.cpp` as a standalone, dependency-free transcription with ZERO catboost includes (copy the header-doc + build-line convention from cityhash_oracle.cpp:1-29; only `<cstdint>/<cstring>/<string>/<iostream>/<vector>/<cmath>` etc.). Transcribe VERBATIM the four small algorithms with file:line citations in comments: (a) `Shuffle` Fisher-Yates loop (shuffle.h:24-32) over a transcribed TFastRng64 — block size 1 for N<1000 (RESEARCH Open Q3); (b) online CTR read-before-increment + CalcCTR online form denom `+1` (online_ctr.cpp:168-184/300-307 + online_ctr.h:128-131); (c) body/tail prefix SelectMinBatchSize/SelectTailSize, fold_len_multiplier default 2.0 (fold.cpp:35-41,156-198); (d) ordered approximant prefix update (approx_calcer.cpp:566-600). The harness reads a committed config + input .npy and dumps the D-02 schema .npy: `permutation_fold{k}.npy [N] int32`, `ctr_good_count.npy`/`ctr_total_count.npy [N] int32` (exact integers), `ctr_value.npy [N|N,n_prior] f64`, `ordered_approx_iter{t}.npy [N] f64`. Then generate the purpose-built fixtures (D-08) OFFLINE and commit only the frozen outputs under `crates/cb-oracle/fixtures/`: build five fixture dirs `one_hot_cat/`, `plain_ctr/`, `ordered_ctr/`, `ordered_boost/`, `tensor_ctr/`. Each gets a `config.json` pinning EVERY knob explicitly per RESEARCH Pitfall 6 + config_pinning_requirement: `boosting_type` (Plain/Ordered, never auto), `simple_ctr`/`combinations_ctr` (single type + explicit priors per D-07), `one_hot_max_size`, `max_ctr_complexity`, `permutation_count`/`fold_len_multiplier`, `counter_calc_method`, `thread_count=1`, `catboost_version:"1.2.10"`, `seed`. For ORD-04 boundary (RESEARCH Pitfall 3): `one_hot_cat/` MUST contain a column of cardinality EXACTLY `one_hot_max_size` (→ one-hot) AND one of `one_hot_max_size+1` (→ CTR). Anchor the harness per RESEARCH fallback step 2: permutation cross-checked vs the Rust cb_core::TFastRng64 reproduction; final CTR vs the trained model's ctr_data blob (model.json from offline catboost==1.2.10). Self-oracle anchors documented in README.md. Generators run OFFLINE, never in CI (D-09) — only .npy/config committed.</action>
  <verify>
    <automated>test -f crates/cb-oracle/generator/ordered_oracle.cpp && ! grep -lR 'catboost/' crates/cb-oracle/generator/ordered_oracle.cpp && g++ -O2 -std=c++17 crates/cb-oracle/generator/ordered_oracle.cpp -o /tmp/ordered_oracle && for d in one_hot_cat plain_ctr ordered_ctr ordered_boost tensor_ctr; do test -f crates/cb-oracle/fixtures/$d/config.json || { echo "missing $d/config.json"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>ordered_oracle.cpp compiles with g++ -std=c++17, has ZERO catboost includes, dumps the D-02 .npy schema; all five fixture dirs exist with explicit per-knob config.json (one_hot_cat has both one_hot_max_size and +1 cardinality columns); self-oracle anchors documented; only frozen .npy/config committed (no generator runs in CI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream model.json → cb-oracle parser | untrusted-shaped JSON (ctr_data blob) crosses into the parser |
| committed .npy fixtures → Rust test reader | frozen but parsed as bytes by load_f64_vec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | model_json.rs ctr_data deserialize | mitigate | serde with `#[serde(default)]`; malformed/missing ctr_data → typed OracleError, never panic/unwrap (INFRA-02) |
| T-05-01-02 | Denial of Service | ordered_oracle.cpp (offline harness) | accept | offline generator only, never in CI; produces frozen .npy; no runtime/untrusted input path |
| T-05-01-SC | Tampering | g++ build of ordered_oracle.cpp | accept | no package installs (RESEARCH Package Legitimacy Audit: N/A); standalone transcription, zero catboost/third-party deps |
</threat_model>

<verification>
- `cargo test -p cb-oracle` green (compare + model_json suites).
- `ordered_oracle.cpp` compiles standalone with g++ -std=c++17 and has zero catboost includes.
- All five fixture dirs present with explicit per-knob config.json.
- Stage::Permutation is integer-exact; OnlineCtr/OrderedApprox ≤1e-5.
</verification>

<success_criteria>
- 05-VALIDATION.md Wave-0 checklist satisfied: Stage variants added, ctr_data parsing added, ordered_oracle.cpp transcribed, fixtures committed, D-03 ordering enforced.
- Downstream slices (05-02 … 05-06) have a ready comparator + fixtures.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-01-SUMMARY.md` when done.
</output>
