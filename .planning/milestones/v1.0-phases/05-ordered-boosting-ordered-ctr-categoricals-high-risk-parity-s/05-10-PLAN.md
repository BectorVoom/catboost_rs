---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 10
type: execute
wave: 8
depends_on: ["05-08"]
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/fixtures/ordered_boost_e2e/config.json
  - crates/cb-oracle/fixtures/ordered_boost_e2e/X.npy
  - crates/cb-oracle/fixtures/ordered_boost_e2e/y.npy
  - crates/cb-oracle/fixtures/ordered_boost_e2e/model.json
  - crates/cb-oracle/fixtures/ordered_boost_e2e/predictions.npy
  - crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs
autonomous: true
gap_closure: true
requirements: [ORD-02]
must_haves:
  truths:
    - "When boosting_type == EBoostingType::Ordered, the train() driver builds the fold set ONCE (create_folds called exactly once, BEFORE the iteration loop, continuous-stream RNG), grows each tree's STRUCTURE via the ordered split-scoring subsystem (greedy_tensor_search_oblivious_ordered, 05-08) over the learning fold's BodyTailArr, and estimates leaf VALUES on the averaging fold exactly as Plain (CalcLeafValuesSimple) — Ordered is no longer dead code in the training loop"
    - "The ordered per-iteration RNG draw accounting (fold pick + derivative seed; folds created ONCE, never per-iteration) keeps the once-created fold permutation deterministic across ALL iterations under the isolating config (random_strength=0, bootstrap_type=No) — the ordered-path portion of the D-11 multi-tree concern is resolved (no Box-Muller perturbation applies at random_strength=0)"
    - "An end-to-end ordered train→predict oracle validates final predictions through the production cb_model::predict_raw apply path ≤1e-5 vs upstream CatBoost 1.2.10 (boosting_type=Ordered) across ALL iterations/trees — the multi-tree case is NOT #[ignore]'d"
    - "The per-object intermediate ordered approximant (Stage::OrderedApprox iter-0 anchor) is reproduced by the in-training ordered path, confirming no leakage signature in train metrics (SC-2)"
    - "The Plain-path oracles (numeric slice_first, one-hot ORD-04) are unchanged — Ordered wiring does not regress the Plain branch"
  artifacts:
    - path: "crates/cb-train/src/boosting.rs"
      provides: "EBoostingType::Ordered branch in train_with_eval_sets() driving greedy_tensor_search_oblivious_ordered over create_folds (called once), leaf values on the averaging fold"
      contains: "greedy_tensor_search_oblivious_ordered"
    - path: "crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs"
      provides: "end-to-end ordered train→predict ≤1e-5 vs upstream via cb_model::predict_raw, ALL trees, no #[ignore]"
      contains: "Ordered"
    - path: "crates/cb-oracle/fixtures/ordered_boost_e2e/model.json"
      provides: "upstream catboost 1.2.10 boosting_type=Ordered trained model + predictions reference"
  key_links:
    - from: "crates/cb-train/src/boosting.rs (EBoostingType::Ordered branch)"
      to: "crates/cb-train/src/tree.rs::greedy_tensor_search_oblivious_ordered"
      via: "the ordered branch grows each tree's structure via the 05-08 ordered split-scoring subsystem"
      pattern: "greedy_tensor_search_oblivious_ordered"
    - from: "crates/cb-train/src/boosting.rs (Ordered branch)"
      to: "crates/cb-train/src/fold.rs::create_folds"
      via: "ordered path builds the fold set ONCE (learning + averaging) from the continuous-stream RNG, before the iteration loop"
      pattern: "create_folds"
---

<objective>
GAP 1 (ORD-02, FAILED) — Part 2 of 2: wire the ordered split-scoring subsystem (05-08) into the `train()` driver and lock the FULL multi-tree end-to-end ordered oracle.

Today `train_with_eval_sets()` (boosting.rs:805-818) has a `debug_assert`-only stub: `boosting_type == EBoostingType::Ordered` has ZERO effect on the training loop (05-VERIFICATION.md Gap 1, data-flow trace "HOLLOW / ORPHANED"). 05-08 built the ordered split-scoring subsystem in `tree.rs` (the structural difference Plain↔Ordered) and re-exported `greedy_tensor_search_oblivious_ordered` from cb-train's lib.rs. This plan drives it from the train loop: build the fold set ONCE via `create_folds`, grow each tree's STRUCTURE via `greedy_tensor_search_oblivious_ordered` over the learning fold, estimate leaf VALUES on the AVERAGING fold exactly as Plain (`CalcLeafValuesSimple` — leaf values are Plain-identical per the STATE.md re-scope analysis), then lock the end-to-end multi-tree oracle through the production `cb_model::predict_raw` apply path.

USER DECISION (re-plan, STATE.md 2026-06-14): the ORD-02 oracle bar is a FULL MULTI-TREE e2e HARD GATE — final predictions match upstream 1.2.10 (boosting_type=Ordered) ≤1e-5 across ALL iterations/trees, NO `#[ignore]` on the multi-tree case. The D-11 multi-tree RNG-phase drift is a `random_strength != 0` Box-Muller phenomenon; the in-scope ordered_boost isolating config pins `random_strength=0` + `bootstrap_type=No`, so NO perturbation draws occur and the once-created fold permutation (create_folds, learn_context.cpp:494-590 — folds built once at train start, never per-iteration) is deterministic across all iterations. The ordered-path portion of D-11 therefore reduces to confirming the per-iteration draw accounting (fold pick + derivative seed) does not disturb the fold permutation — straightforward at random_strength=0. (If, contrary to this analysis, a multi-tree drift surfaces at random_strength=0, the executor escalates per Rule 2 rather than weakening the must-have to single-tree.)

Purpose: closes ORD-02 end-to-end with the full multi-tree hard gate, validated through the production apply path.
Output: the `EBoostingType::Ordered` branch in `train_with_eval_sets`, the `ordered_boost_e2e/` fixture, and the multi-tree ordered oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-PATTERNS.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-08-PLAN.md

@crates/cb-train/src/boosting.rs
@crates/cb-train/src/tree.rs
@crates/cb-train/src/fold.rs
@crates/cb-train/src/permutation.rs
@crates/cb-train/tests/ordered_boost_oracle_test.rs
@crates/cb-model/src/apply.rs

<interfaces>
From crates/cb-train/src/tree.rs (the ordered subsystem built by 05-08, re-exported from cb-train lib.rs — consume, do NOT re-port):
```rust
// Scores tree splits over the learning fold's BodyTailArr (segment-summed ordered
// L2, per-segment scaled L2 = l2*(BodySumWeight/BodyFinish)); leaf_of over the
// AVERAGING-fold object order for downstream leaf-value estimation.
pub fn greedy_tensor_search_oblivious_ordered(/* signature finalized in 05-08 */) -> CbResult<GrownTree>;
```

From crates/cb-train/src/fold.rs (the fold machinery — already locked, 05-03/05-07; consume):
```rust
pub fn create_folds(n, permutation_count, permutation_needed_for_learning,
                    dynamic_body_tail, fold_len_multiplier, random_seed) -> Vec<Fold>;
pub struct Fold { pub permutation: Vec<i32>, pub body_tail_boundaries: Vec<usize>, pub is_averaging: bool }
pub fn body_tail_segments(n, multiplier) -> Vec<(usize, usize)>;
pub fn body_sum_weights(n, multiplier, weights) -> Vec<f64>;
```

From crates/cb-train/src/boosting.rs (the driver to branch — the Plain loop is ~line 886; the stub at 805-818 is replaced):
```rust
pub fn train_with_eval_sets<R: Runtime>(/* ... */) -> CbResult<Model>;
pub enum EBoostingType { Plain, Ordered }
// compute_leaf_deltas(...) — leaf VALUES on the full fold; REUSE for the averaging fold.
// accumulate_leaf_weights(...) — per-leaf weights; REUSE.
// The Plain leaf-value path (compute_leaf_deltas over weighted_der1 on the whole
// fold) is EXACTLY what the ordered path uses for leaf VALUES (averaging fold).
```

From crates/cb-model/src/apply.rs (the production apply path the e2e oracle MUST validate through — D-08 leaf-sum, bias once, checked .get):
```rust
pub fn predict_raw(model: &Model, feature_values: &[Vec<f32>]) -> Vec<f64>; // RawFormulaVal, object order
// The final-prediction ≤1e-5 assertion routes through THIS fn (not the cb-train
// staged approx) so the ordered-built model.json is validated end-to-end.
```

UPSTREAM (catboost 1.2.10), the ordered training loop:
- learn_context.cpp:494-590 InitializeFolds: `LearningFoldCount = max(1, permutation_count-1)` learning folds (BuildDynamicFold, ordered body/tail) + 1 averaging fold (BuildPlainFold), each permutation drawn IN ORDER from the single persistent `Rand` — folds created ONCE here, never per-iteration.
- greedy_tensor_search.cpp:115-117 CalcDerivativesStDevFromZero dispatches Ordered→ordered std-dev (only matters at random_strength≠0; pinned 0 here).
- approx_calcer.cpp:566-600 the historical (ordered) per-object approximant — the per-iteration in-training intermediate the OrderedApprox anchor checks.
- The leaf VALUES come from CalcLeafValuesSimple on the averaging fold (same closed-form as Plain `compute_leaf_deltas`) — the structural difference is ONLY the split scoring (05-08), not the leaf values.

ISOLATING CONFIG (crates/cb-oracle/fixtures/ordered_boost/config.json): boosting_type=Ordered, permutation_count=1, fold_len_multiplier=2.0, depth=2, iterations=5, learning_rate=0.1, l2_leaf_reg=3.0, leaf_estimation_method=Gradient, bootstrap_type=No, random_strength=0, random_seed=0, thread_count=1, loss=RMSE. With permutation_count=1 → learning_fold_count = max(1, 0) = 1 learning fold + 1 averaging fold.

ORACLE-SOURCING (D-09, the A2 residual this gap closes): the prior `ordered_boost/` fixture OMITTED input features/labels (per-object internals only). The new `ordered_boost_e2e/` fixture commits the FULL train→predict stack — `X.npy` (f32 features), `y.npy` (labels), `model.json` (upstream catboost 1.2.10 boosting_type=Ordered trained model), `predictions.npy` (upstream RawFormulaVal) — generated OFFLINE by gen_fixtures.py (pinned catboost==1.2.10, thread_count=1, never in CI), the binclf/regression precedent (gen_fixtures.py:504-544 gen_binclf_skeleton).
</interfaces>
</context>

<artifacts_this_phase_produces>
- `crates/cb-train/src/boosting.rs`: an `EBoostingType::Ordered` branch in `train_with_eval_sets()` (replacing the 805-818 stub) that builds folds ONCE via a SINGLE `create_folds(n, permutation_count, /*needed=*/true, /*dynamic=*/true, fold_len_multiplier, random_seed)` call placed BEFORE the iteration loop, per iteration grows the tree STRUCTURE via `greedy_tensor_search_oblivious_ordered` over the learning fold's `body_tail_segments` + `body_sum_weights`, then estimates leaf VALUES + weights on the AVERAGING fold via the EXISTING `compute_leaf_deltas` / `accumulate_leaf_weights` (Plain-identical), updates `approx`, records staged + the iter-0 OrderedApprox anchor. The Plain branch stays the default, unchanged.
- `crates/cb-oracle/generator/gen_fixtures.py`: a new `gen_ordered_boost_e2e()` (upstream boosting_type=Ordered train + save model.json + dump predictions, the binclf/regression precedent).
- `crates/cb-oracle/fixtures/ordered_boost_e2e/{config.json, X.npy, y.npy, model.json, predictions.npy}` (new end-to-end fixture).
- `crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs` (new): end-to-end ordered train→predict ≤1e-5 vs upstream across ALL trees (NO #[ignore]) validated through `cb_model::predict_raw`, plus the Stage::OrderedApprox iter-0 per-object anchor re-asserted under the in-training ordered path.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire greedy_tensor_search_oblivious_ordered into train_with_eval_sets() under EBoostingType::Ordered (structure on learning fold, values on averaging fold)</name>
  <read_first>crates/cb-train/src/boosting.rs, crates/cb-train/src/tree.rs, crates/cb-train/src/fold.rs, crates/cb-train/src/permutation.rs, catboost-master/catboost/private/libs/algo/learn_context.cpp, catboost-master/catboost/private/libs/algo/approx_calcer.cpp</read_first>
  <files>crates/cb-train/src/boosting.rs</files>
  <behavior>
    - With boosting_type=Plain the train loop is byte-identical to today (the existing slice_first/one-hot/leaf_methods oracles unchanged).
    - With boosting_type=Ordered: build the fold set ONCE before the iteration loop via create_folds (learning fold(s) with dynamic body/tail + averaging fold, continuous-stream RNG from random_seed). Each iteration: compute gradients; grow the tree STRUCTURE via greedy_tensor_search_oblivious_ordered over the learning fold's body_tail_segments + body_sum_weights (the 05-08 segment-summed ordered score); estimate leaf VALUES + weights on the AVERAGING fold via the existing compute_leaf_deltas / accumulate_leaf_weights (Plain-identical, CalcLeafValuesSimple); update approx; record staged.
    - `create_folds` is called EXACTLY ONCE in the Ordered path, OUTSIDE (before) the tree-iteration loop — never per-iteration; the fold permutation is fixed for the whole run.
    - For permutation_count=1 → exactly 1 learning fold + 1 averaging fold (learning_fold_count(1, true) == 1). The fold permutations are drawn ONCE at fold creation; the per-iteration fold pick + derivative-seed draws (random_strength=0, bootstrap_type=No) do not re-shuffle them.
    - The Stage::OrderedApprox iter-0 per-object anchor (the committed ordered_boost/ordered_approx_iter0.npy) is reproduced by the in-training ordered path at iteration 0 (no leakage signature).
  </behavior>
  <action>In `train_with_eval_sets` replace the `debug_assert`-only ordered stub (boosting.rs:805-818) with a real `match params.boosting_type` (or `if`) branch. For `EBoostingType::Plain` keep the existing whole-fold loop verbatim (the perturbed search + whole-fold leaf deltas — NO behavior change). For `EBoostingType::Ordered`: (1) BEFORE the iteration loop, build the fold set ONCE via a SINGLE `crate::fold::create_folds(n, params.permutation_count, true, true, params.fold_len_multiplier, params.random_seed)` call (learning permutation needed for ordered boosting; dynamic body/tail) and pick the learning fold (`!is_averaging`, first) + the averaging fold (`is_averaging`); precompute `body_tail_segments(n, params.fold_len_multiplier)` and `body_sum_weights(n, params.fold_len_multiplier, &weights)` once (they are iteration-invariant). The `create_folds` call MUST sit outside the tree-iteration loop body so it executes exactly once per `train` call (folds-built-once, learn_context.cpp:494-590). (2) Per iteration: `runtime.compute_gradients`; build the per-object `weighted_der1`; grow the tree STRUCTURE via `greedy_tensor_search_oblivious_ordered(&matrix, &weighted_der1, &weights, &learning_fold.permutation, &segments, &body_sum_weights, params.l2_leaf_reg, sum_all_weights, params.depth, n)` (pass the EXACT args the 05-08 signature defines); (3) estimate leaf VALUES via the EXISTING `compute_leaf_deltas(params.leaf_method, &grown.leaf_of, &weighted_der1, &ders.der2, &weights, &approx, target, scaled_l2, n_leaves)` on the AVERAGING-fold object order (the leaf VALUES are Plain-identical — `CalcLeafValuesSimple`), scale by `learning_rate`, and `accumulate_leaf_weights`; (4) update `approx[i] += leaf_values[leaf_of[i]]`; (5) record staged + push the ObliviousTree. RNG: at random_strength=0 + bootstrap_type=No there are no perturbation/bootstrap draws; the ordered fold permutations are fixed at creation, so the per-iteration draw accounting reduces to NOT touching the fold permutation (do NOT redraw folds per iteration). Remove the obsolete comment block claiming Ordered is intentionally unwired. All float reductions via `cb_core::sum_f64` (D-08); checked `.get` only; no `unwrap`/`expect`/`panic`/raw index; no `anyhow`.</action>
  <verify>
    <automated>cargo test -p cb-train --test ordered_boost_oracle_test && cargo test -p cb-train --test slice_first_oracle_test && cargo test -p cb-train --test one_hot_oracle_test && cargo test -p cb-train --test leaf_methods_oracle_test && test "$(grep -v '^[[:space:]]*//' crates/cb-train/src/boosting.rs | grep -c 'create_folds')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `train_with_eval_sets` branches on `params.boosting_type` to an Ordered path calling `greedy_tensor_search_oblivious_ordered` — `grep -c "greedy_tensor_search_oblivious_ordered" crates/cb-train/src/boosting.rs` ≥ 1.
    - The Ordered branch builds folds ONCE via `create_folds` and consumes `body_tail_segments` / `body_sum_weights` — `grep -E "create_folds|body_tail_segments|body_sum_weights" crates/cb-train/src/boosting.rs` matches.
    - FOLDS-BUILT-ONCE (testable): `create_folds` appears EXACTLY ONCE in production code — `grep -v '^[[:space:]]*//' crates/cb-train/src/boosting.rs | grep -c 'create_folds'` returns exactly 1, and that single call site sits OUTSIDE the tree-iteration loop body (not inside the `for iter in 0..iterations` block). The `<verify>` block enforces the count == 1; the executor confirms the placement by code review against the loop boundary.
    - The `debug_assert!`-only stub claiming "this whole-set train driver stays the Plain path" is REMOVED (`grep -c "this whole-set train driver stays the Plain path" crates/cb-train/src/boosting.rs` == 0).
    - The Plain path is unchanged: `cargo test -p cb-train --test slice_first_oracle_test` + `--test one_hot_oracle_test` + `--test leaf_methods_oracle_test` all pass; `cargo test -p cb-train --test ordered_boost_oracle_test` (5/5, the standalone approximant) still passes.
    - No NEW `unwrap`/`expect`/`panic`/`anyhow` in production boosting.rs (`grep -nE "unwrap\(\)|expect\(|panic!|anyhow" crates/cb-train/src/boosting.rs` shows no new production hits beyond pre-existing test-cfg/comment uses).
    - `cargo check --workspace --tests` clean.
  </acceptance_criteria>
  <done>boosting_type=Ordered drives greedy_tensor_search_oblivious_ordered over the learning fold for tree structure and the Plain leaf-value path on the averaging fold; folds built once (create_folds called exactly once, outside the iteration loop, grep-enforced); the Plain branch and its oracles are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the ordered_boost_e2e fixture and lock the FULL multi-tree ordered train→predict oracle ≤1e-5 through cb_model::predict_raw (no #[ignore])</name>
  <read_first>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-train/tests/slice_first_oracle_test.rs, crates/cb-train/tests/ordered_boost_oracle_test.rs, crates/cb-oracle/fixtures/ordered_boost/config.json, crates/cb-model/src/apply.rs, crates/cb-model/src/model.rs, crates/cb-oracle/src/compare.rs</read_first>
  <files>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/fixtures/ordered_boost_e2e/config.json, crates/cb-oracle/fixtures/ordered_boost_e2e/X.npy, crates/cb-oracle/fixtures/ordered_boost_e2e/y.npy, crates/cb-oracle/fixtures/ordered_boost_e2e/model.json, crates/cb-oracle/fixtures/ordered_boost_e2e/predictions.npy, crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs</files>
  <action>Add `gen_ordered_boost_e2e()` to gen_fixtures.py (the gen_binclf_skeleton precedent, :504-544): synthesize a small numeric dataset (N=30, a handful of float features, RMSE labels) pinned to the existing `ordered_boost/config.json` knobs (boosting_type=Ordered, permutation_count=1, fold_len_multiplier=2.0, depth=2, iterations=5, learning_rate=0.1, l2_leaf_reg=3.0, leaf_estimation_method=Gradient, leaf_estimation_iterations=1, bootstrap_type=No, random_strength=0, random_seed=0, thread_count=1, loss_function=RMSE, boost_from_average explicit); `model.fit(X, y)`; `model.save_model(..., format="json")` → `model.json`; `model.predict(X, prediction_type="RawFormulaVal")` → `predictions.npy` (f64); commit `X.npy` (f32 features), `y.npy` (labels), and a `config.json` mirroring `ordered_boost/config.json` with `"scenario": "ordered_boost_e2e"` and a note that it carries the FULL train→predict stack (closing the D-09 omission) and runs OFFLINE (pinned catboost==1.2.10, never in CI). Then create `crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs`: load `X.npy`/`y.npy`/`predictions.npy`, train via `cb_train::train` with `boosting_type=EBoostingType::Ordered` under the pinned config (borders computed from X via the existing border-selection path the slice_first/one_hot tests use), lift the trained model into the canonical `cb_model::Model` (via `cb_model::Model::from_trained` with the scored float-feature borders), and predict via `cb_model::predict_raw` (the production apply path — MANDATORY: the final-prediction assertion routes through `cb_model::predict_raw`, NOT the cb-train staged approx, so the ordered-built model is validated end-to-end through the production apply path). Assert `compare_stage(Stage::Predictions, &expected_predictions, &actual)` ≤1e-5 over ALL N objects (covering ALL 5 trees, NOT just tree 0; the test MUST NOT be `#[ignore]`'d). ALSO re-assert the iter-0 ordered approx per-object anchor: capture the in-training iteration-0 staged ordered approx and compare it `Stage::OrderedApprox` ≤1e-5 against the committed `ordered_boost/ordered_approx_iter0.npy` (the "no leakage signature in train metrics" gate, SC-2) — if the committed anchor's input order differs from the e2e dataset, instead assert the in-training iter-0 ordered approx reproduces the standalone `ordered_approx_delta_simple` output over the same fold (the structural no-leakage anchor) and document the choice. Float sums via sum_f64; the test file may `#![allow(clippy::unwrap_used, ...)]` for fixture loads as the sibling oracle tests do.</action>
  <verify>
    <automated>cargo test -p cb-train --test ordered_boost_e2e_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - `gen_fixtures.py` contains `gen_ordered_boost_e2e` that trains with `boosting_type="Ordered"` and saves `model.json` + `predictions.npy` + `X.npy` + `y.npy` (`grep -c "def gen_ordered_boost_e2e" crates/cb-oracle/generator/gen_fixtures.py` == 1).
    - `crates/cb-oracle/fixtures/ordered_boost_e2e/` contains `config.json`, `X.npy`, `y.npy`, `model.json`, `predictions.npy` (all committed).
    - `crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs` predicts through the PRODUCTION apply path — `grep -c "cb_model::predict_raw\|predict_raw(" crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs` ≥ 1 — and asserts final predictions ≤1e-5 vs the committed upstream `predictions.npy` via `compare_stage` over ALL objects (NOT integer-exact, NOT looser than 1e-5) — covering ALL 5 trees.
    - The final-prediction assertion uses `cb_model::predict_raw` (the production apply path), NOT the cb-train staged approx final row.
    - The test contains NO `#[ignore]` attribute (`grep -c "#\[ignore" crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs` == 0).
    - The test re-asserts the iteration-0 ordered approx per-object anchor (Stage::OrderedApprox) under the in-training ordered path.
    - `cargo test -p cb-train --test ordered_boost_e2e_oracle_test` passes.
  </acceptance_criteria>
  <done>An end-to-end ordered train→predict model matches upstream catboost 1.2.10 (boosting_type=Ordered) ≤1e-5 across ALL iterations/trees (no #[ignore]), validated through the production cb_model::predict_raw apply path, with the per-object ordered-approx no-leakage anchor reproduced by the in-training path; ORD-02 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture .npy (X/y/predictions) + model.json → Rust test loader | malformed/oversized arrays or model.json must surface typed errors, never panic / OOB-slice |
| learning fold permutation + body/tail boundaries → ordered train branch | a permutation index out of range or a boundary > n must surface CbError::Degenerate (the 05-08 subsystem guards this), never index OOB |
| per-iteration approx update (leaf_of → approx) | leaf index out of range must be a checked `.get`, never OOB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | ordered train branch fold/permutation/leaf_of bounds | mitigate | The Ordered branch consumes `create_folds` output (Fisher-Yates `< n` by construction) and the 05-08 ordered search (checked `.get`, returns Degenerate on OOR). The approx update uses checked `approx.get_mut(i)` / `leaf_values.get(leaf)` exactly as the Plain branch. No raw index. |
| T-05-10-V5 | Tampering | ordered_boost_e2e_oracle_test model.json / .npy load | mitigate | Load via `cb_model::load_json` (Phase-4 panic-free serde) + the cb-oracle `.npy` reader (bounds-checked); assert array lengths == N before comparison; no raw indexing on fixture bytes. |
| T-05-10-02 | Denial of Service | ordered fold construction (once) vs per-iteration | mitigate | Folds built ONCE before the loop (bounded by `learning_fold_count + 1`); the grep-enforced single `create_folds` call site guarantees no per-iteration unbounded allocation. Body/tail segments precomputed once (capped at n, strictly monotone). |
| T-05-10-SC | Tampering | no package installs (Rust test + offline Python generator on pinned catboost==1.2.10) | accept | RESEARCH Package Legitimacy Audit: N/A — catboost==1.2.10 already pinned/installed (D-09); no new npm/pip/cargo install task. |
</threat_model>

<verification>
- `cargo test -p cb-train --test ordered_boost_e2e_oracle_test` — end-to-end ordered ≤1e-5 vs upstream through cb_model::predict_raw, ALL trees, no #[ignore].
- `cargo test -p cb-train --test ordered_boost_oracle_test` (5/5) — standalone approximant unchanged.
- `cargo test -p cb-train --test slice_first_oracle_test --test one_hot_oracle_test --test leaf_methods_oracle_test` — Plain branch unchanged (no regression).
- `grep -v '^[[:space:]]*//' crates/cb-train/src/boosting.rs | grep -c 'create_folds'` == 1 — folds built exactly once (outside the iteration loop).
- `boosting.rs` Ordered branch calls `greedy_tensor_search_oblivious_ordered` + `create_folds` + `body_tail_segments`/`body_sum_weights`; leaf values via the existing Plain leaf path on the averaging fold.
- `cargo check --workspace --tests` clean; all float sums via `sum_f64`; no `unwrap`/`anyhow` in production.
</verification>

<success_criteria>
- ORD-02 success criterion (SC-2): `EBoostingType::Ordered` trains with exact prefix boundaries and the exact prior formula; the per-object intermediate oracle passes confirming no leakage signature in train metrics — now demonstrable because the ordered split-scoring path EXECUTES during training (closes 05-VERIFICATION.md Gap 1).
- Final ordered train→predict predictions match upstream catboost 1.2.10 (boosting_type=Ordered) ≤1e-5 across ALL iterations/trees through the production `cb_model::predict_raw` apply path, with NO `#[ignore]` (the user's full multi-tree hard gate).
- The ordered fold set is built exactly once (grep-enforced create_folds count == 1), outside the iteration loop — the folds-built-once invariant is testable, not narrative.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-10-SUMMARY.md` when done.
</output>
