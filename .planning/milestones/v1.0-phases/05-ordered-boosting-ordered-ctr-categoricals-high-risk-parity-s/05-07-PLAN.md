---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 07
type: execute
wave: 7
depends_on: []
files_modified:
  - crates/cb-oracle/generator/ordered_oracle.cpp
  - crates/cb-oracle/fixtures/ordered_ctr/permutation_fold1.npy
  - crates/cb-oracle/fixtures/ordered_ctr/config.json
  - crates/cb-train/tests/ordered_ctr_oracle_test.rs
autonomous: true
gap_closure: true
requirements: [ORD-01, ORD-03]
must_haves:
  truths:
    - "The ordered_oracle.cpp harness generates multi-fold permutations from a SINGLE continuously-advancing TFastRng64 (the upstream-faithful continuous-stream discipline matching create_folds/permutations), NOT per-fold seed+k reseeding"
    - "The committed ordered_ctr/permutation_fold1.npy is the continuous-stream second fold (permutations(30, 2, 0)[1]), integer-exact vs the production permutations()"
    - "The D-03 fold-1 gate in ordered_ctr_oracle_test.rs validates the production permutations(30, 2, 0)[1] against the regenerated fixture (integer-exact), NOT fisher_yates_permutation(30, 1)"
    - "The production permutations() continuous-stream output is integer-exact vs the regenerated upstream-faithful fixture for fold k=0 AND fold k=1"
  artifacts:
    - path: "crates/cb-oracle/generator/ordered_oracle.cpp"
      provides: "continuous-stream multi-fold permutation generation (single advancing TFastRng64 across folds)"
      contains: "GenMultiFoldPermutations"
    - path: "crates/cb-oracle/fixtures/ordered_ctr/permutation_fold1.npy"
      provides: "regenerated continuous-stream fold-1 permutation"
    - path: "crates/cb-train/tests/ordered_ctr_oracle_test.rs"
      provides: "D-03 fold-1 gate against permutations(30, 2, 0)[1]"
      contains: "permutations"
  key_links:
    - from: "crates/cb-oracle/generator/ordered_oracle.cpp"
      to: "crates/cb-train/src/permutation.rs::permutations"
      via: "continuous-stream draw order matches create_folds (single RNG across folds)"
      pattern: "continuous"
    - from: "crates/cb-train/tests/ordered_ctr_oracle_test.rs"
      to: "cb_train::permutations"
      via: "fold-1 gate validates permutations(30, 2, 0)[1] integer-exact"
      pattern: "permutations\\("
---

<objective>
GAP 3 (CR-01, BLOCKER) — Fix the multi-fold permutation oracle so the D-03 linchpin holds for ALL folds k ≥ 0, not just fold 0. The offline harness `ordered_oracle.cpp` currently seeds each fold independently as `foldSeed = seed + k` (lines 391-393), while the Rust production `permutations()` / `create_folds()` draw ALL folds from a SINGLE continuously-advancing `TFastRng64` (the upstream-faithful discipline, `learn_context.cpp` fold-creation loop over a shared `TRestorableFastRng64`). The executor adapted the fold-1 test to the harness's wrong per-fold reseeding instead of fixing the harness — so the committed `permutation_fold1.npy` does NOT correspond to upstream's second fold, and the production `permutations()` was never validated for any fold k>0.

Purpose: Closes the D-03 contract ("reproduce upstream permutations exactly") for fold k ≥ 1. After this plan, the production `permutations()` continuous-stream output is integer-exact vs an upstream-faithful fixture for BOTH folds, removing the silent self-consistency-only validation hole.
Output: corrected continuous-stream harness, regenerated `permutation_fold1.npy`, a fold-1 D-03 gate keyed on `permutations(30, 2, 0)[1]`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md

@crates/cb-train/src/permutation.rs
@crates/cb-train/src/fold.rs
@crates/cb-oracle/generator/ordered_oracle.cpp
@crates/cb-train/tests/ordered_ctr_oracle_test.rs

<interfaces>
From crates/cb-train/src/permutation.rs (the production continuous-stream generator — this is the upstream-faithful reference the harness MUST match):
```rust
// Single persistent TFastRng64::from_seed(random_seed), advancing the draw
// stream CONTINUOUSLY across folds (never reseeded per fold). Fold k consumes
// exactly n-1 draws; the next fold continues from the resulting RNG phase.
pub fn permutations(n: usize, permutation_count: usize, random_seed: u64) -> Vec<Vec<i32>>;
// One fold from an ALREADY-seeded generator (drives the persistent multi-fold RNG):
fn shuffle_in_place(n: usize, rng: &mut TFastRng64) -> Vec<i32>;
// Single-fold helper (per-fold reseed — the WRONG discipline for multi-fold; keep for fold-0 single-fold callers):
pub fn fisher_yates_permutation(n: usize, seed: u64) -> Vec<i32>;
```

From crates/cb-oracle/generator/ordered_oracle.cpp (the harness — transcribed TFastRng64 + Fisher-Yates; lines 391-393 are the per-fold reseed defect):
```cpp
struct TFastRng64 { static TFastRng64 FromSeed(ui64 seed); ui64 GenRand(); ui64 Uniform(ui64 bound); };
static std::vector<i32> FisherYatesPermutation(size_t n, ui64 seed); // re-seeds per call — single-fold only
// Current DEFECT (391-393): for k in [0,foldCount): foldSeed = seed + k; perm = FisherYatesPermutation(n, foldSeed)
```

From crates/cb-train/tests/ordered_ctr_oracle_test.rs:
```rust
const FIXTURE_SEED: u64 = 0;
const FIXTURE_N: usize = 30;
use cb_train::{calc_ctr_online, fisher_yates_permutation, ordered_ctr_per_permutation};
// Current fold-1 gate (WRONG): fisher_yates_permutation(FIXTURE_N, FIXTURE_SEED + 1)
```

The upstream-faithful continuous-stream draw order (already in permutation.rs docs / fold.rs `create_folds`): one persistent `TFastRng64::from_seed(seed)`; fold 0 drawn first (n-1 draws via the `i in 1..n: Uniform(i+1); swap(i,j)` loop), THEN fold 1 continues from the SAME generator phase — the generator is NEVER re-seeded between folds.
</interfaces>
</context>

<artifacts_this_phase_produces>
- `ordered_oracle.cpp`: new `GenMultiFoldPermutations(n, foldCount, seed)` helper that draws ALL folds from one persistent `TFastRng64` (single FromSeed, continuous GenRand stream) — replaces the per-fold `FisherYatesPermutation(n, seed+k)` reseed at lines 391-393. `FisherYatesPermutation` kept for fold-0 single-fold uses.
- regenerated `crates/cb-oracle/fixtures/ordered_ctr/permutation_fold1.npy` (the continuous-stream second fold) plus an updated `ordered_ctr/config.json` note recording the continuous-stream seeding.
- `ordered_ctr_oracle_test.rs`: a fold-1 D-03 gate keyed on `cb_train::permutations(30, 2, 0)[1]` (continuous-stream), replacing the `fisher_yates_permutation(30, 1)` per-fold reseed; fold-0 gate keyed on `permutations(30, 2, 0)[0]` (== `fisher_yates_permutation(30, 0)` for the first fold).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fix ordered_oracle.cpp to continuous-stream multi-fold seeding + regenerate permutation_fold1.npy</name>
  <read_first>crates/cb-oracle/generator/ordered_oracle.cpp, crates/cb-train/src/permutation.rs, crates/cb-train/src/fold.rs, catboost-master/catboost/private/libs/algo/learn_context.cpp, crates/cb-oracle/fixtures/ordered_ctr/config.json</read_first>
  <files>crates/cb-oracle/generator/ordered_oracle.cpp, crates/cb-oracle/fixtures/ordered_ctr/permutation_fold1.npy, crates/cb-oracle/fixtures/ordered_ctr/config.json</files>
  <action>In `ordered_oracle.cpp` replace the per-fold reseed loop (lines 391-393, `foldSeed = seed + k; FisherYatesPermutation(n, foldSeed)`) with a `GenMultiFoldPermutations(n, foldCount, seed)` helper that constructs ONE `TFastRng64::FromSeed(seed)` and runs the Fisher-Yates `for (i=1; i<n; ++i) { j = gen.Uniform(i+1); swap(v[i], v[j]); }` loop `foldCount` times over that SAME generator — the draw stream advances continuously across folds and the generator is NEVER re-seeded between folds (matching `permutation.rs::permutations` / `fold.rs::create_folds` and upstream `learn_context.cpp`'s shared `TRestorableFastRng64`). Keep `FisherYatesPermutation` for the fold-0 single-fold CTR path (line 398 — note fold 0 of the continuous stream is identical to a fresh-seed single fold, so the existing `perm0`/online-CTR derivation is unchanged). Write `permutation_fold{k}.npy` for each k from the new helper. Then regenerate the `ordered_ctr/` fixture: rebuild the harness (the existing `g++ -std=c++20 ordered_oracle.cpp` recipe — zero catboost includes, D-01 transcribe-then-self-oracle), feed the same `ordered_ctr` stdin config (N=30, foldCount≥2, seed=0) the original run used, and overwrite ONLY `permutation_fold1.npy` (fold 0 is unchanged — assert byte-identical to the existing committed `permutation_fold0.npy` before overwriting fold 1). Add a `"fold_seeding": "continuous-stream (single persistent TFastRng64 across folds, learn_context.cpp); fold1 = permutations(N,2,seed)[1]"` note to `ordered_ctr/config.json`. No catboost includes; the harness stays dependency-free (D-09 offline generator, never runs in CI).</action>
  <verify>
    <automated>cd crates/cb-oracle/generator && g++ -std=c++20 -O2 -o /tmp/ordered_oracle ordered_oracle.cpp && grep -q "GenMultiFoldPermutations" ordered_oracle.cpp && grep -vq "seed + (ui64)k" ordered_oracle.cpp && echo OK</automated>
  </verify>
  <read_first>crates/cb-oracle/generator/ordered_oracle.cpp</read_first>
  <acceptance_criteria>
    - `ordered_oracle.cpp` compiles with `g++ -std=c++20` and contains a `GenMultiFoldPermutations` helper that calls `TFastRng64::FromSeed` EXACTLY ONCE for all folds (no `seed + k` / `seed + (ui64)k` reseed remains — `grep -c "seed + (ui64)k" ordered_oracle.cpp` == 0).
    - `ordered_ctr/permutation_fold0.npy` is byte-identical before and after regeneration (fold 0 unchanged).
    - `ordered_ctr/permutation_fold1.npy` is regenerated from the continuous-stream harness and differs from the old per-fold-reseed bytes.
    - `ordered_ctr/config.json` carries a `fold_seeding` note documenting continuous-stream discipline.
    - No catboost/ include appears in `ordered_oracle.cpp` (`grep -c "catboost/" ordered_oracle.cpp` == 0).
  </acceptance_criteria>
  <done>The harness draws all folds from one persistent TFastRng64; permutation_fold1.npy is the continuous-stream second fold; config.json records the seeding; fold 0 byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Re-key the D-03 fold-1 gate to permutations(30, 2, 0)[1] and validate production continuous-stream for k=0 AND k=1</name>
  <read_first>crates/cb-train/tests/ordered_ctr_oracle_test.rs, crates/cb-train/src/permutation.rs, crates/cb-oracle/fixtures/ordered_ctr/permutation_fold0.npy, crates/cb-oracle/fixtures/ordered_ctr/permutation_fold1.npy</read_first>
  <files>crates/cb-train/tests/ordered_ctr_oracle_test.rs</files>
  <action>In `ordered_ctr_permutations_are_integer_exact_first`, import `permutations` from `cb_train` and replace the fold-1 actual derivation `fisher_yates_permutation(FIXTURE_N, FIXTURE_SEED + 1)` with the continuous-stream `permutations(FIXTURE_N, 2, FIXTURE_SEED)` — bind `let multi = permutations(30, 2, 0)`, then gate fold-0 against `multi[0]` and fold-1 against `multi[1]` (both via `compare_permutation`, integer-exact, D-03). Rewrite the function doc-comment "Fold-seeding" section: remove the per-fold-reseed rationale (the CR-01 defect) and state that BOTH folds now reproduce the production `permutations()` continuous-stream output integer-exact vs the regenerated upstream-faithful fixture — closing CR-01. Keep `fisher_yates_permutation` available for the fold-0-only single-fold gate in `ordered_ctr_per_object_counts_exact_and_value_within_tolerance` (fold 0 of the continuous stream equals a fresh-seed single fold, so that gate is unchanged), but assert in a comment that `permutations(30, 2, 0)[0] == fisher_yates_permutation(30, 0)` holds. Do NOT relax the comparator from integer-exact `compare_permutation`. No `unwrap`/`expect`/`panic` in production paths (the test file already allows them for fixture loads).</action>
  <verify>
    <automated>cargo test -p cb-train --test ordered_ctr_oracle_test</automated>
  </verify>
  <read_first>crates/cb-train/tests/ordered_ctr_oracle_test.rs</read_first>
  <acceptance_criteria>
    - `cargo test -p cb-train --test ordered_ctr_oracle_test` passes (all 3 tests green) AFTER Task 1 regenerates the fixture.
    - `ordered_ctr_permutations_are_integer_exact_first` derives fold-1 via `permutations(30, 2, 0)[1]` (the file contains `permutations(` and no longer contains `fisher_yates_permutation(FIXTURE_N, FIXTURE_SEED + 1)`).
    - Both fold-0 and fold-1 gates use `compare_permutation` (integer-exact, NOT `compare_stage` 1e-5).
    - The test asserts/notes `permutations(30, 2, 0)[0] == fisher_yates_permutation(30, 0)` (continuous-stream fold 0 == single-fold fold 0).
  </acceptance_criteria>
  <done>The fold-1 D-03 gate validates the production continuous-stream permutations(30, 2, 0)[1] integer-exact vs the regenerated fixture; CR-01 closed; the production permutations() is now validated for k=0 AND k=1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed .npy fixture → test loader | the regenerated permutation_fold1.npy is read as int32; a malformed/oversized array must not OOB-slice the comparator |
| harness stdin config → ordered_oracle.cpp | N / foldCount are read from stdin; an oversized N could overflow the permutation buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Denial of Service | ordered_oracle.cpp permutation buffer sizing from stdin N | accept | Offline generator only (D-09), never runs in CI; operator supplies the pinned N=30 config; no untrusted input path. Documented in config.json. |
| T-05-07-V5 | Tampering | ordered_ctr_oracle_test.rs .npy load + compare_permutation | mitigate | `compare_permutation` already length-checks both arrays before index-for-index `==`; the test loads via `read_npy` (bounds-checked) and asserts `len == FIXTURE_N` before comparison — no raw indexing on the fixture bytes. |
| T-05-07-SC | Tampering | no package installs (pure g++ recompile + Rust test) | accept | RESEARCH Package Legitimacy Audit: N/A — no npm/pip/cargo install; harness is dependency-free C++. |
</threat_model>

<verification>
- `g++ -std=c++20` compiles `ordered_oracle.cpp`; `GenMultiFoldPermutations` present; no `seed + (ui64)k` reseed remains.
- `cargo test -p cb-train --test ordered_ctr_oracle_test` green (3/3).
- Fold-0 fixture byte-unchanged; fold-1 fixture regenerated from the continuous stream.
- The production `permutations(30, 2, 0)` reproduces BOTH committed folds integer-exact.
- No catboost/ include in the harness; offline generator (D-09).
</verification>

<success_criteria>
- D-03 contract ("reproduce upstream permutations exactly") holds for fold k ≥ 1, not just fold 0 (closes the CR-01 BLOCKER from 05-VERIFICATION.md).
- ORD-01 multi-permutation fold machinery is validated against the upstream-faithful multi-fold seeding discipline; the production `permutations()` continuous-stream is no longer self-consistency-only for k>0.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-07-SUMMARY.md` when done.
</output>
