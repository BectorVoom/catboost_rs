---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 09
type: execute
wave: 9
depends_on: ["05-10"]
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-model/src/model.rs
  - crates/cb-model/src/apply.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/config.json
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/X_cat.npy
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/y.npy
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/predictions.npy
  - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
autonomous: true
gap_closure: true
requirements: [ORD-05]
must_haves:
  truths:
    - "The cb-model split representation gains a CTR-split variant: ObliviousTree.splits becomes Vec<ModelSplit> where ModelSplit is { Float(Split), Ctr(CtrSplit) } and CtrSplit carries { projection: TProjection, ctr_type: ECtrType, prior: Prior, target_border_idx: usize, border: f64 } — a CTR split round-trips through model.json ctr_data and is evaluated at apply time (the storage-design fork is RESOLVED, not deferred to the executor)"
    - "The train() driver's candidate generation emits tensor_ctr_candidates (SimpleCtrs + CombinationCtrs) during tree growth, respecting the max_ctr_complexity gate, and persists each chosen CTR split as a ModelSplit::Ctr in the produced ObliviousTree — tensor CTR candidates are no longer orphaned from training"
    - "A categorical dataset trains end-to-end with tensor CTRs and the resulting model (trees with CTR splits + baked ctr_data) predicts ≤1e-5 vs upstream CatBoost 1.2.10 across ALL trees (no #[ignore])"
    - "The cb-model apply path evaluates CTR-based splits by computing the projection's combined hash via calc_cat_feature_hash + fold_cat_hash (never the model's ctr_data hash_map) and looking up the baked CtrValueTable with a bounds-safe not-found→empty path"
    - "The combined projection CTR math (per-object num/denom, oracle-locked in 05-06) is reused over the combined key inside the training candidate path — not re-derived"
    - "Existing standalone tensor_ctr + one-hot + ordered oracles remain green (no regression); the Float-only ObliviousTree apply (slice_first/one-hot/leaf_methods) is byte-for-byte unchanged"
  artifacts:
    - path: "crates/cb-model/src/model.rs"
      provides: "ModelSplit enum { Float(Split), Ctr(CtrSplit) } + CtrSplit struct; ObliviousTree.splits: Vec<ModelSplit> carrying the CTR-split storage"
      contains: "ModelSplit"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "tensor CTR candidate emission wired into the train() candidate-generation / tree-growth path under max_ctr_complexity, persisting CTR splits as ModelSplit::Ctr"
      contains: "tensor_ctr_candidates"
    - path: "crates/cb-model/src/apply.rs"
      provides: "CTR-split evaluation at inference via the combined projection hash + baked ctr_data lookup over ModelSplit::Ctr"
      contains: "ctr_value_for_combined_projection"
    - path: "crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs"
      provides: "end-to-end tensor-CTR train→predict ≤1e-5 vs upstream, all trees, no #[ignore]"
      contains: "tensor"
    - path: "crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json"
      provides: "upstream catboost 1.2.10 tensor-CTR trained model + predictions reference"
  key_links:
    - from: "crates/cb-train/src/boosting.rs (candidate generation)"
      to: "crates/cb-train/src/candidates.rs::tensor_ctr_candidates"
      via: "train() emits tensor CTR candidates under max_ctr_complexity during tree growth"
      pattern: "tensor_ctr_candidates"
    - from: "crates/cb-train/src/boosting.rs (CTR tree growth)"
      to: "crates/cb-model/src/model.rs (Model CTR-split storage)"
      via: "a chosen CTR split is persisted in the ObliviousTree as ModelSplit::Ctr(CtrSplit{projection, ctr_type, prior, target_border_idx, border}) — the boosting→Model storage hop"
      pattern: "ModelSplit::Ctr"
    - from: "crates/cb-model/src/apply.rs (CTR split eval)"
      to: "crates/cb-data::calc_cat_feature_hash"
      via: "projection combined hash folds per-feature calc_cat_feature_hash via fold_cat_hash (never the ctr_data hash_map), looked up over the ModelSplit::Ctr representation"
      pattern: "calc_cat_feature_hash"
---

<objective>
GAP 2 (ORD-05, FAILED) — Wire tensor CTRs into the `train()` driver, RESOLVE the model CTR-split storage representation, and produce an end-to-end categorical train→predict model matching upstream ≤1e-5. Today `tensor_ctr_candidates` (candidates.rs) enumerates SimpleCtrs/CombinationCtrs under `max_ctr_complexity` and the combined-projection per-object CTR math is oracle-locked ≤1e-5 (05-06), BUT the candidates are NEVER emitted into the `train()` candidate-generation / tree-growth path (05-VERIFICATION.md Gap 2, data-flow trace "HOLLOW — no tensor CTR splits are generated during training"), AND there is no model representation for a CTR split (cb-model `ObliviousTree.splits` is `Vec<Split>`, float-only — `predict_raw_one` has no CTR-split branch). No full categorical-dataset train→predict model with tensor CTRs is produced, so roadmap SC-5 ("produce models matching upstream ≤1e-5 on categorical datasets") is undemonstrated end-to-end.

Purpose: closes ORD-05 end-to-end. This plan (a) RESOLVES the CTR-split storage representation in cb-model (`ModelSplit` enum + `CtrSplit` struct — finalized here, NOT left to the executor), (b) wires the training candidate generation to emit tensor CTR candidates (reusing the 05-04/05/06 combined-projection CTR math over the combined key) and persist chosen CTR splits as `ModelSplit::Ctr`, and (c) gives the cb-model apply path CTR-split evaluation (combined hash via `calc_cat_feature_hash` + baked `CtrValueTable` lookup). A new end-to-end tensor-CTR fixture (categorical X + labels + upstream model.json with ctr_data + predictions) locks final predictions ≤1e-5 across ALL trees (no #[ignore], consistent with the ORD-02 multi-tree bar) — resolving the D-09 residual via the established offline generator.
Output: the `ModelSplit`/`CtrSplit` representation in `model.rs`, tensor CTR candidate wiring in `boosting.rs`, CTR-split apply in `apply.rs`, the `tensor_ctr_e2e/` fixture, and the end-to-end tensor oracle test.

DEPENDENCY NOTE: tensor CTRs are algorithmically valid under Plain boosting too (the tensor_ctr fixture pins boosting_type=Plain). The dependency on 05-10 is to avoid `boosting.rs` merge conflicts (05-10 edits the train driver) and to inherit the resolved fold/RNG path — NOT an algorithmic requirement. This plan is wave 9 (the third of the three strictly-sequential gap plans 05-08 → 05-10 → 05-09; no two share a wave because all three touch boosting.rs / tree.rs / apply.rs), depends_on 05-10, so the boosting.rs edits stack cleanly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-PATTERNS.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-06-SUMMARY.md

@crates/cb-train/src/boosting.rs
@crates/cb-train/src/candidates.rs
@crates/cb-train/src/projection.rs
@crates/cb-model/src/model.rs
@crates/cb-model/src/apply.rs
@crates/cb-model/src/ctr_data.rs
@crates/cb-train/tests/tensor_ctr_oracle_test.rs

<interfaces>
From crates/cb-model/src/model.rs (the storage to EXTEND — CURRENT shape, float-only):
```rust
pub use cb_train::Split;                       // { feature: usize, border: f64 }
pub struct ObliviousTree { pub splits: Vec<Split>, pub leaf_values: Vec<f64>, pub leaf_weights: Vec<f64> }
pub struct Model { pub oblivious_trees: Vec<ObliviousTree>, pub bias: f64, pub float_feature_borders: Vec<Vec<f64>> }
pub fn Model::from_trained(trained: &cb_train::Model, float_feature_borders: Vec<Vec<f64>>) -> Self;
```

From crates/cb-train/src/candidates.rs + projection.rs (enumeration + combined hash — locked in 05-06; consume, do NOT re-port):
```rust
pub struct CtrCandidate { pub projection: TProjection, pub is_simple: bool }
pub fn tensor_ctr_candidates(cat_cardinalities: &[u32], one_hot_max_size: u32,
                             max_ctr_complexity: usize) -> Vec<CtrCandidate>;
pub struct TProjection { /* sorted cat_features: Vec<usize> (private), Ord+Hash */ }
pub fn enumerate_projections(cat_feature_count, max_ctr_complexity) -> Vec<TProjection>;
pub fn fold_cat_hash(running: u64, cat_hash: u32) -> u64; // ctr_provider.h:65-78 (the sign-extended (ui64)(int) cast)
// TProjection::combined_hash folds per-feature calc_cat_feature_hash via fold_cat_hash (05-06).
```

From crates/cb-model/src/ctr_data.rs (the CTR types the CtrSplit/apply consume — already committed, 05-04):
```rust
pub enum ECtrType { Borders, Buckets, BinarizedTargetMeanValue, FloatTargetMeanValue, Counter, FeatureFreq } // repr(i8)
pub struct Prior { /* num/denom */ }
pub struct CtrValueTable { /* calc_for_hash(hash, prior, shift, scale, target_border_idx) -> f64 */ }
pub struct CtrData { pub tables: BTreeMap<String, CtrValueTable> }
pub use ctr_data::{calc_inference, decode_ctr_data, CtrData, CtrTableJson, CtrValueTable, ECtrType, Prior};
```

From crates/cb-train/src/ctr (the online accumulation — REUSE over the combined key, do NOT re-derive):
```rust
pub fn online_ctr_prefix_binclf(/* read-before-increment whole-set CTR */) -> /* good/total/value */;
pub fn calc_ctr_online(count_in_class: f64, total_count: i64, prior: f64) -> f64; // +1 denom
```

From crates/cb-model/src/apply.rs (the apply path — predict_raw_one currently handles ONLY float splits; EXTEND for CTR splits over ModelSplit::Ctr):
```rust
pub fn ctr_value_for_projection(/* single-feature CTR at inference */) -> f64;
pub fn ctr_value_for_combined_projection(table, cat_values: &[&str], prior, shift, scale, target_border_idx) -> f64; // 05-06, ALREADY EXISTS
pub fn predict_raw(model: &Model, feature_values: &[Vec<f32>]) -> Vec<f64>;
fn predict_raw_one(model: &Model, features: &[f32]) -> f64; // forward-bit leaf index over tree.splits
```

RESEARCH § "Tensor / Combination CTRs" (greedy_tensor_search.cpp:491-551 AddTreeCtrs; :532-533 max_ctr_complexity gate; projection.h:138-144 GetFullProjectionLength; ctr_provider.h:65-78 CalcHash/MultiHash fold). A tensor CTR is the SAME online/ordered accumulation (05-04/05) keyed on the COMBINED projection hash. At inference the combined hash is computed from raw cat values via `cb_data::calc_cat_feature_hash` (NEVER the model's stored ctr_data hash_map — RESEARCH Anti-Pattern, 05-06 V5 discipline) and the baked per-type `CtrValueTable` is looked up with a bounds-checked not-found→empty path.

CTR-SPLIT STORAGE DESIGN (RESOLVED HERE — Blocker 3 fork closed): the cb-model split representation is extended so a CTR split is a first-class stored split, mirroring the existing tree.rs `AnySplit { Float, OneHot }` precedent. Add to crates/cb-model/src/model.rs:
```rust
// New: a stored CTR split (a (projection, CTR-type, border) test on a materialized CTR feature value).
pub struct CtrSplit {
    pub projection: cb_train::TProjection, // the combined projection (sorted cat-feature members)
    pub ctr_type: crate::ctr_data::ECtrType, // which baked CtrValueTable type to look up
    pub prior: crate::ctr_data::Prior,       // the prior used at apply (carried from the model)
    pub target_border_idx: usize,            // Buckets per-class numerator selector (default 0)
    pub border: f64,                         // the CTR-value threshold; passes when ctr_value > border
}
// New: the model split is EITHER a float threshold OR a CTR test (mirrors tree.rs AnySplit).
pub enum ModelSplit { Float(Split), Ctr(CtrSplit) }
// CHANGED: ObliviousTree.splits: Vec<Split>  ->  Vec<ModelSplit>.
```
The CTR split round-trips through model.json `ctr_data`: the baked `CtrValueTable` lives in the model's `ctr_data` (BTreeMap keyed by the CTR-base string), and `CtrSplit.projection` + `ctr_type` reconstruct that key at apply time; the per-tree split list stores the `(projection, ctr_type, prior, target_border_idx, border)` test, NOT the table itself. The existing FLOAT split path is preserved by wrapping each float split as `ModelSplit::Float(Split{..})` (no change to `Split` itself — no cross-crate literal churn). `Model::from_trained` maps the trainer's chosen splits into `Vec<ModelSplit>`.

ORACLE-SOURCING (D-09, the A2 residual this gap closes): the prior `tensor_ctr/` fixture committed only per-object CTR internals (no input cat features/labels, no trained model). The end-to-end fixture MUST commit the FULL stack — `X_cat.npy` (categorical features), `y.npy` (labels), `model.json` (upstream catboost 1.2.10 trained with simple_ctr + combinations_ctr + max_ctr_complexity, INCLUDING the baked `ctr_data` section), `predictions.npy` — generated offline by gen_fixtures.py (pinned catboost==1.2.10, thread_count=1, never in CI). model_json.rs already parses the `ctr_data` hash_map (CtrTableJson); the Rust apply consumes the baked tables.
</interfaces>
</context>

<artifacts_this_phase_produces>
- `crates/cb-model/src/model.rs`: the RESOLVED CTR-split representation — `CtrSplit { projection, ctr_type, prior, target_border_idx, border }`, `ModelSplit { Float(Split), Ctr(CtrSplit) }`, and `ObliviousTree.splits: Vec<ModelSplit>` (was `Vec<Split>`). `Model::from_trained` lifts the trainer's splits into `Vec<ModelSplit>` (float splits as `ModelSplit::Float`, CTR splits as `ModelSplit::Ctr`).
- `crates/cb-train/src/boosting.rs`: tensor CTR candidate emission wired into the candidate-generation / tree-growth path (calls `tensor_ctr_candidates` under `params.max_ctr_complexity`, feeding the combined-projection CTR feature into the split search and persisting chosen CTR splits as `ModelSplit::Ctr`). Reuses the 05-04/05 online/ordered accumulation over the combined key (no re-derivation).
- `crates/cb-model/src/apply.rs`: CTR-split evaluation in `predict_raw_one` — a `ModelSplit::Ctr` computes the projection's combined hash via `calc_cat_feature_hash` and looks up the baked `CtrValueTable` (`ctr_value_for_combined_projection`) with a bounds-safe not-found→empty path; `ModelSplit::Float` keeps the existing `value > border` path byte-for-byte.
- `crates/cb-oracle/generator/gen_fixtures.py`: a new `gen_tensor_ctr_e2e()` (upstream catboost 1.2.10 categorical train with simple_ctr + combinations_ctr + max_ctr_complexity; save model.json WITH ctr_data; dump predictions).
- `crates/cb-oracle/fixtures/tensor_ctr_e2e/{config.json, X_cat.npy, y.npy, model.json, predictions.npy}` (new end-to-end fixture).
- `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` (new): end-to-end tensor-CTR train→predict ≤1e-5 vs upstream across ALL trees (no #[ignore]), gated by D-03 permutation (if present) then per-object combined CTR ≤1e-5 then final prediction ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Resolve the CTR-split storage representation in cb-model + wire tensor_ctr_candidates into train() candidate generation</name>
  <read_first>crates/cb-model/src/model.rs, crates/cb-model/src/ctr_data.rs, crates/cb-train/src/tree.rs, crates/cb-train/src/boosting.rs, crates/cb-train/src/candidates.rs, crates/cb-train/src/projection.rs, catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp, catboost-master/catboost/libs/model/split.h</read_first>
  <files>crates/cb-model/src/model.rs, crates/cb-train/src/boosting.rs</files>
  <behavior>
    - cb-model defines `CtrSplit { projection: cb_train::TProjection, ctr_type: ECtrType, prior: Prior, target_border_idx: usize, border: f64 }` and `ModelSplit { Float(Split), Ctr(CtrSplit) }`; `ObliviousTree.splits` is `Vec<ModelSplit>`. A pure-float tree round-trips as `Vec<ModelSplit::Float(_)>` with predictions byte-identical to the old `Vec<Split>` apply.
    - `Model::from_trained` maps the trainer's chosen splits into `Vec<ModelSplit>` (float → `ModelSplit::Float`, CTR → `ModelSplit::Ctr`) — no recomputation.
    - With CTR-eligible cat features present (cardinality > one_hot_max_size), the train() candidate generation emits tensor_ctr_candidates: SimpleCtrs (max_ctr_complexity>=1) and CombinationCtrs (max_ctr_complexity>=2), respecting the gate (no candidate exceeds max_ctr_complexity).
    - A tensor CTR feature's per-object value is the 05-04/05 online accumulation keyed on the combined projection hash (combined_hash over calc_cat_feature_hash) — reused, not re-derived; a chosen CTR split is persisted in the produced ObliviousTree as `ModelSplit::Ctr(CtrSplit{..})` carrying its projection, ctr_type, prior, target_border_idx, and border.
  </behavior>
  <action>In `crates/cb-model/src/model.rs`: add `pub struct CtrSplit { pub projection: cb_train::TProjection, pub ctr_type: crate::ctr_data::ECtrType, pub prior: crate::ctr_data::Prior, pub target_border_idx: usize, pub border: f64 }` and `pub enum ModelSplit { Float(Split), Ctr(CtrSplit) }` (derive Debug, Clone, PartialEq to match `ObliviousTree`), and change `ObliviousTree.splits` from `Vec<Split>` to `Vec<ModelSplit>`. Update `Model::from_trained` to map the trainer's splits into `Vec<ModelSplit>` (each float split wrapped `ModelSplit::Float(Split{..})`; CTR splits, when present on the trained tree, mapped to `ModelSplit::Ctr(CtrSplit{..})`). Keep `Split` itself UNCHANGED (no cross-crate churn). In `crates/cb-train/src/boosting.rs` candidate generation (the tree-growth split-search path), call `crate::candidates::tensor_ctr_candidates(&cat_cardinalities, params.one_hot_max_size, params.max_ctr_complexity)` and feed each `CtrCandidate` as a candidate CTR feature into the oblivious split search (`AddTreeCtrs`, greedy_tensor_search.cpp:491-551): materialize the candidate's per-object CTR value via the 05-04/05 online accumulation keyed on `projection.combined_hash` (the combined-projection math locked in 05-06 — REUSE it; do NOT re-port the hash fold or the accumulation), score it against borders exactly like a float candidate, and when a CTR split is chosen, persist it on the trained tree so `Model::from_trained` lifts it to `ModelSplit::Ctr(CtrSplit{projection, ctr_type, prior, target_border_idx, border})`. Bound enumeration by `params.max_ctr_complexity` (the gate, :532-533). All float sums via `cb_core::sum_f64` (D-08); checked `.get` only; no `unwrap`/`expect`/`panic`/raw index; no `anyhow`. This task introduces the cross-crate split-representation change (model.rs ↔ boosting.rs ↔ apply.rs callers), so it MUST end with a workspace-wide compile check so the breakage surfaces here (NOT in the downstream e2e oracle task): run `cargo check --workspace --tests` and resolve every caller of `ObliviousTree.splits` (apply.rs, shap.rs, fstr.rs, predict.rs, cbm.rs, json.rs, and any cb-oracle consumer) to match against `ModelSplit` — apply.rs's full CTR-split evaluation lands in Task 1b, but apply.rs MUST at minimum compile against `Vec<ModelSplit>` (handle `ModelSplit::Float` as today; a `ModelSplit::Ctr` may temporarily route to the not-found→empty/`false` path until Task 1b fills it in).</action>
  <verify>
    <automated>cargo check --workspace --tests && cargo test -p cb-train --test tensor_ctr_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-model/src/model.rs` defines `ModelSplit` and `CtrSplit` and `ObliviousTree.splits: Vec<ModelSplit>` — `grep -c "enum ModelSplit" crates/cb-model/src/model.rs` == 1, `grep -c "struct CtrSplit" crates/cb-model/src/model.rs` == 1, `grep -c "Vec<ModelSplit>" crates/cb-model/src/model.rs` ≥ 1.
    - `Split` itself is UNCHANGED (`grep -c "pub struct Split" crates/cb-train/src/tree.rs` == 1, fields `feature`/`border` only).
    - `boosting.rs` calls `tensor_ctr_candidates` from the train candidate-generation path — `grep -c "tensor_ctr_candidates" crates/cb-train/src/boosting.rs` ≥ 1 — and persists chosen CTR splits as `ModelSplit::Ctr` (`grep -c "ModelSplit::Ctr" crates/cb-train/src/boosting.rs` ≥ 1, OR the mapping is in `Model::from_trained` — `grep -c "ModelSplit::Ctr" crates/cb-model/src/model.rs` ≥ 1).
    - The wiring references `params.max_ctr_complexity` and the combined-projection hash (`combined_hash` / the 05-06 combined-projection math) — no re-ported hash fold.
    - The cross-crate change COMPILES workspace-wide: `cargo check --workspace --tests` is clean (this is the gate that surfaces caller breakage BEFORE the e2e oracle task).
    - `cargo test -p cb-train --test tensor_ctr_oracle_test` (3/3, standalone per-object combined CTR) still passes — no regression.
    - No NEW `unwrap`/`expect`/`panic`/`anyhow` in production paths of model.rs / boosting.rs.
  </acceptance_criteria>
  <done>cb-model carries a first-class CTR-split representation (ModelSplit::Ctr(CtrSplit)); ObliviousTree.splits is Vec<ModelSplit>; train() emits tensor CTR candidates under max_ctr_complexity and persists chosen CTR splits as ModelSplit::Ctr; the workspace compiles with all split callers updated; the standalone tensor oracle is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Evaluate ModelSplit::Ctr in the cb-model apply path (combined projection hash + baked ctr_data lookup)</name>
  <read_first>crates/cb-model/src/apply.rs, crates/cb-model/src/model.rs, crates/cb-model/src/ctr_data.rs, crates/cb-train/src/projection.rs, crates/cb-model/tests/ctr_data_roundtrip_test.rs, catboost-master/catboost/private/libs/algo/static_ctr_provider.cpp</read_first>
  <files>crates/cb-model/src/apply.rs</files>
  <behavior>
    - cb-model `predict_raw_one` evaluates a `ModelSplit::Ctr`: compute the projection combined hash for the object's raw cat values via `calc_cat_feature_hash` + `fold_cat_hash`, look up the baked `CtrValueTable` (`ctr_value_for_combined_projection`) with the not-found→empty bounds-safe path, binarize against the split `border` (`ctr_value > border`), and route the forward-bit leaf index exactly as a float split.
    - `ModelSplit::Float` is byte-for-byte the existing `value > border` path — the slice_first/one-hot/leaf_methods Float-only apply is unchanged.
    - A combined-key bucket index out of range / not present returns the empty CTR value, never an OOB index (V5 discipline, T-05-09-V5).
  </behavior>
  <action>In `crates/cb-model/src/apply.rs` extend `passes_split` (or `predict_raw_one`'s per-split closure) so it matches on `ModelSplit`: `ModelSplit::Float(split)` keeps the existing `features.get(split.feature).is_some_and(|&v| f64::from(v) > split.border)` path byte-for-byte; `ModelSplit::Ctr(ctr_split)` gathers the object's projection-member raw cat VALUES (in the projection's sorted member order, each already in the A4 string form via `cb_data::stringify_int_category` for integer-coded values), computes the object's CTR value via `ctr_value_for_combined_projection(table, &cat_values, ctr_split.prior, shift, scale, ctr_split.target_border_idx)` where `table` is fetched from the model's `ctr_data` by the `(projection, ctr_type)` key (NEVER the model's ctr_data hash_map for the per-feature hash — RESEARCH Anti-Pattern; the per-feature hash is always `calc_cat_feature_hash`), with the not-found→empty path (V5 bounds-check: a missing combined-key bucket returns the empty value; a missing table returns the empty CTR value), then returns `ctr_value > ctr_split.border` and folds into the forward-bit leaf index exactly as a float split. The model's apply path needs access to the categorical feature columns + the `ctr_data` tables; thread them through `predict_raw` / `predict_raw_one` (extend the apply entry point to accept the categorical feature view + the model's `ctr_data`, or read them from an extended `Model` if the cat columns are model-resident) — keep the float-only `predict_raw(model, feature_values)` signature working for the existing Float-only callers (add an overload / optional cat-feature param rather than breaking the numeric apply tests). All float sums via `cb_core::sum_f64` (D-08); checked `.get` only; no `unwrap`/`expect`/`panic`/raw index; no `anyhow`.</action>
  <verify>
    <automated>cargo test -p cb-model --test ctr_data_roundtrip_test && cargo test -p cb-model --lib && cargo check --workspace --tests</automated>
  </verify>
  <acceptance_criteria>
    - `apply.rs` `predict_raw_one` matches on `ModelSplit` and evaluates `ModelSplit::Ctr` via `calc_cat_feature_hash` + `ctr_value_for_combined_projection` with a bounds-safe not-found→empty lookup — `grep -E "calc_cat_feature_hash|ctr_value_for_combined_projection" crates/cb-model/src/apply.rs` matches; `grep -c "ModelSplit::Ctr" crates/cb-model/src/apply.rs` ≥ 1; no raw indexing on the ctr_data blob.
    - `ModelSplit::Float` keeps the existing `value > border` path — the Float-only apply is byte-for-byte unchanged (slice_first/one-hot/leaf_methods oracles, exercised downstream in Task 2, still pass).
    - `cargo test -p cb-model --test ctr_data_roundtrip_test` (5/5) still passes.
    - `cargo test -p cb-model --lib` passes; `cargo check --workspace --tests` clean.
    - No NEW `unwrap`/`expect`/`panic`/`anyhow` in production apply.rs.
  </acceptance_criteria>
  <done>cb-model apply evaluates ModelSplit::Ctr via the combined projection hash + baked ctr_data with a bounds-safe not-found→empty lookup; ModelSplit::Float is unchanged; ctr_data roundtrip + cb-model lib green; workspace compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the tensor_ctr_e2e fixture and lock the FULL multi-tree tensor-CTR train→predict oracle ≤1e-5 (no #[ignore])</name>
  <read_first>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-train/tests/tensor_ctr_oracle_test.rs, crates/cb-train/tests/one_hot_oracle_test.rs, crates/cb-oracle/fixtures/tensor_ctr/config.json, crates/cb-oracle/src/model_json.rs, crates/cb-oracle/src/compare.rs, crates/cb-model/src/apply.rs</read_first>
  <files>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/fixtures/tensor_ctr_e2e/config.json, crates/cb-oracle/fixtures/tensor_ctr_e2e/X_cat.npy, crates/cb-oracle/fixtures/tensor_ctr_e2e/y.npy, crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json, crates/cb-oracle/fixtures/tensor_ctr_e2e/predictions.npy, crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs</files>
  <action>Add `gen_tensor_ctr_e2e()` to gen_fixtures.py (the gen_binclf_skeleton + cat_hash precedent): synthesize a small categorical dataset (N=30, two cat columns each with cardinality > one_hot_max_size so a genuine combination is formed — mirror `tensor_ctr/config.json`: one_hot_max_size=1, max_ctr_complexity=2, simple_ctr=["Borders:Prior=0.5"], combinations_ctr=["Borders:Prior=0.5"], permutation_count=1, fold_len_multiplier=2.0, depth=2, iterations=5, learning_rate=0.1, l2_leaf_reg=3.0, leaf_estimation_method=Gradient, bootstrap_type=No, random_strength=0, random_seed=0, thread_count=1, loss_function=Logloss); build a `Pool(X_cat, y, cat_features=[0,1])`; `model.fit(pool)`; `model.save_model(..., format="json")` → `model.json` (carries the baked `ctr_data` section); `model.predict(X_cat, prediction_type="RawFormulaVal")` → `predictions.npy` (f64); commit `X_cat.npy` (the raw categorical columns, stringify integer categories per A4), `y.npy`, and a `config.json` mirroring `tensor_ctr/config.json` with `"scenario": "tensor_ctr_e2e"` and a note that it carries the FULL train→predict stack + baked ctr_data (closing the D-09 omission), OFFLINE (pinned catboost==1.2.10, never in CI). Then create `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs`: load `X_cat.npy`/`y.npy`/`predictions.npy` + `model.json` (via `cb_oracle::model_json` / `cb_model::load_json` for the ctr_data); train via `cb_train::train` with the pinned tensor-CTR config (max_ctr_complexity=2, combinations_ctr + simple_ctr), lift to `cb_model::Model::from_trained`, predict via `cb_model::predict_raw` over the categorical apply path (the ModelSplit::Ctr evaluation from Task 1b); gate FIRST `Stage::Permutation` (D-03, if a permutation is present in the fixture), THEN per-object combined CTR `Stage::OnlineCtr` ≤1e-5 (reusing the 05-06 combined-projection anchor), THEN final prediction `compare_stage(Stage::Predictions, &expected_predictions, &actual)` ≤1e-5 vs the committed upstream `predictions.npy` over ALL objects (the final-iteration approx — covering ALL 5 trees). The test MUST NOT be `#[ignore]`'d. Float sums via sum_f64; the test may `#![allow(clippy::unwrap_used, ...)]` for fixture loads.</action>
  <verify>
    <automated>cargo test -p cb-train --test tensor_ctr_e2e_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - `gen_fixtures.py` contains `gen_tensor_ctr_e2e` that trains a categorical model with `simple_ctr` + `combinations_ctr` + `max_ctr_complexity=2` and saves `model.json` (with ctr_data) + `predictions.npy` + `X_cat.npy` + `y.npy` (`grep -c "def gen_tensor_ctr_e2e" crates/cb-oracle/generator/gen_fixtures.py` == 1).
    - `crates/cb-oracle/fixtures/tensor_ctr_e2e/` contains `config.json`, `X_cat.npy`, `y.npy`, `model.json`, `predictions.npy` (all committed).
    - `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` trains with the pinned tensor-CTR config and asserts final predictions ≤1e-5 vs the committed upstream `predictions.npy` via `compare_stage` over ALL objects (NOT looser than 1e-5) — covering ALL 5 trees — through `cb_model::predict_raw`.
    - The test contains NO `#[ignore]` attribute (`grep -c "#\[ignore" crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` == 0).
    - The test gates D-03 permutation (if present) and per-object combined CTR ≤1e-5 BEFORE the final prediction assertion.
    - `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` passes; standalone `tensor_ctr_oracle_test` (3/3) and `one_hot_oracle_test` (3/3) remain green.
  </acceptance_criteria>
  <done>An end-to-end tensor-CTR categorical train→predict model matches upstream catboost 1.2.10 ≤1e-5 across ALL trees (no #[ignore]), with D-03 + per-object combined CTR gated first, validated through the ModelSplit::Ctr apply path; ORD-05 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection combined hash → baked ctr_data CtrValueTable lookup at inference | the combined-key bucket index must be bounds-checked `< leafCount` before slicing the blob (CR-02/CR-03 hazard zone) |
| committed fixture (model.json with ctr_data blob, X_cat/y/predictions .npy) → loader | malformed/oversized ctr_data blob or arrays must surface typed ModelError, never panic / OOB-slice |
| projection enumeration ← max_ctr_complexity | unbounded combinatorial blow-up if the gate is bypassed |
| ObliviousTree.splits Vec<ModelSplit> ← all split consumers (apply/shap/fstr/predict/serde) | the enum change must be exhaustively matched by every consumer; a non-exhaustive match would mis-evaluate a CTR split |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-V5 | Tampering | apply.rs CTR-split combined-hash ctr_data lookup | mitigate | Bounds-check the combined-key bucket index `< leafCount` before indexing the baked CtrValueTable; not-found→emptyVal path (reuse 05-04/06 V5 discipline); checked `.get`, no raw index on the blob. Note CR-02 (present-but-empty bucket) / CR-03 (first-bucket stride) hazards — the lookup must not assume a present bucket; treat absent/empty as the empty CTR value. |
| T-05-09-01 | Denial of Service | candidates.rs / projection.rs combination enumeration in train() | mitigate | `GetFullProjectionLength` bounded by `max_ctr_complexity` (config, default 4) at the train() call site; checked arithmetic; no unbounded combinatorial enumeration. |
| T-05-09-02 | Tampering / DoS | model.json ctr_data blob parse (oversized blob, bad bucket index) | mitigate | Parse via the Phase-4 panic-free serde (`load_json` / model_json `CtrTableJson`); VERIFY flatbuffers accessor + bounds-check bucket index before indexing; malformed → typed `ModelError`. No `unwrap`/raw-index on untrusted model bytes (RESEARCH V5). |
| T-05-09-03 | Tampering | ModelSplit enum change across all split consumers | mitigate | The `Vec<Split>` → `Vec<ModelSplit>` change is gated by `cargo check --workspace --tests` in Task 1a — every consumer (apply/shap/fstr/predict/cbm/json) must compile against an exhaustive `match ModelSplit { Float, Ctr }`, so no consumer silently drops a CTR split. |
| T-05-09-SC | Tampering | no package installs (Rust test + offline Python generator on pinned catboost==1.2.10) | accept | RESEARCH Package Legitimacy Audit: N/A — catboost==1.2.10 already pinned (D-09); no new npm/pip/cargo install. |
</threat_model>

<verification>
- `cargo check --workspace --tests` clean — the `Vec<Split>` → `Vec<ModelSplit>` cross-crate change compiles with every split consumer updated (Task 1a gate, surfaces breakage before the e2e oracle).
- `cargo test -p cb-train --test tensor_ctr_oracle_test` (standalone combined CTR, 3/3) still green — no regression.
- `cargo test -p cb-model --test ctr_data_roundtrip_test` (5/5) still green; `cargo test -p cb-model --lib` green.
- `cargo test -p cb-train --test one_hot_oracle_test` (3/3) still green (Float-only apply unchanged).
- `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` green — end-to-end tensor CTR ≤1e-5 vs upstream through cb_model::predict_raw, all trees, no #[ignore].
- `model.rs` defines `ModelSplit`/`CtrSplit` and `ObliviousTree.splits: Vec<ModelSplit>`; `boosting.rs` emits `tensor_ctr_candidates` under max_ctr_complexity and persists `ModelSplit::Ctr`; `apply.rs` evaluates `ModelSplit::Ctr` via `calc_cat_feature_hash` + baked ctr_data with bounds-safe lookup.
- All float sums via `sum_f64`; no `unwrap`/`anyhow` in production.
</verification>

<success_criteria>
- The CTR-split storage representation is RESOLVED (ModelSplit::Ctr(CtrSplit) in cb-model, ObliviousTree.splits: Vec<ModelSplit>) — no executor-time fork; the boosting→Model→apply chain for a CTR split is explicit (key_links).
- ORD-05 success criterion (SC-5): feature combinations (SimpleCtrs/CombinationCtrs, max_ctr_complexity control) produce models matching upstream ≤1e-5 on categorical datasets — now demonstrable end-to-end because tensor CTR candidates are emitted during training, persisted as ModelSplit::Ctr, and the apply path evaluates CTR splits (closes 05-VERIFICATION.md Gap 2).
- Final tensor-CTR train→predict predictions match upstream ≤1e-5 across ALL trees, with NO `#[ignore]` (consistent with the ORD-02 multi-tree hard gate).
- Phase 5 additive ladder fully validated end-to-end: one-hot → permutation → Plain CTR → Ordered CTR → Ordered boosting (05-08/05-10) → tensor CTR (this plan), each oracle-locked AND wired into train→predict.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-09-SUMMARY.md` when done.
</output>
