---
phase: 06.3-ranking-losses-and-metrics
plan: 02
type: execute
wave: 2
depends_on: ["06.3-01"]
files_modified:
  - crates/cb-compute/src/loss.rs
  - crates/cb-compute/src/loss_test.rs
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/ranking_der.rs
  - crates/cb-compute/src/ranking_der_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/queryrmse_oracle_test.rs
  - crates/cb-train/tests/querysoftmax_oracle_test.rs
  - crates/cb-oracle/fixtures/ranking_corpus/queryrmse/.gitkeep
  - crates/cb-oracle/fixtures/ranking_corpus/querysoftmax/.gitkeep
autonomous: true
requirements: [LOSS-04]

must_haves:
  truths:
    - "QueryRMSE trains end-to-end on the grouped seam with per-group queryAvrg, producing predictions matching upstream ≤1e-5"
    - "QuerySoftMax trains end-to-end with per-group max-shifted softmax der, predictions matching upstream ≤1e-5"
    - "Both losses ride the existing pointwise leaf estimators (Gradient/Newton); no pairwise path needed"
    - "Existing scalar/N-dim oracles stay green (D-04 no-regression)"
  artifacts:
    - path: "crates/cb-compute/src/loss.rs"
      provides: "queryrmse_der + querysoftmax_der per-object inner formulas (per-group wrapper in ranking_der.rs)"
      contains: "queryrmse"
    - path: "crates/cb-train/tests/queryrmse_oracle_test.rs"
      provides: "Per-stage ≤1e-5 oracle (splits/leaf-values/staged-approx/predictions) for QueryRMSE"
      contains: "compare_stage"
    - path: "crates/cb-train/tests/querysoftmax_oracle_test.rs"
      provides: "Per-stage ≤1e-5 oracle for QuerySoftMax"
      contains: "compare_stage"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "compute_gradients_grouped"
      via: "ranking-loss der dispatch on query_info view"
      pattern: "compute_gradients_grouped"
    - from: "crates/cb-compute/src/ranking_der.rs"
      to: "cb_core::sum_f64"
      via: "per-group queryAvrg / softmax normalizer"
      pattern: "sum_f64"
---

<objective>
Land the first two deterministic per-group ranking losses — **QueryRMSE** and
**QuerySoftMax** — as complete vertical slices: der transcription → grouped-seam wiring →
boosting-loop dispatch → frozen oracle fixture → per-stage ≤1e-5 oracle green. Both are
querywise (raw approx, no pairs, pointwise leaf path), so they exercise the grouped seam
from Plan 01 without the pairwise Cholesky path. After this plan a user can train a
group-aware ranking model under QueryRMSE/QuerySoftMax and get upstream-matching predictions.

Purpose: Prove the grouped seam end-to-end on the two simplest deterministic ranking losses
(D-6.3-01 Wave A), closing the first slice of LOSS-04.
Output: queryrmse/querysoftmax der + Loss variants + grouped-seam arms + boosting dispatch +
two per-stage oracles + two frozen fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-CONTEXT.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-PATTERNS.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-01-SUMMARY.md

# Upstream transcription sources
@catboost-master/catboost/private/libs/algo_helpers/error_functions.h
@catboost-master/catboost/private/libs/algo_helpers/error_functions.cpp

# Rust seams
@crates/cb-compute/src/loss.rs
@crates/cb-compute/src/runtime.rs
@crates/cb-train/src/boosting.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QueryRMSE + QuerySoftMax der + Loss variants + grouped-seam arms</name>
  <read_first>
    crates/cb-compute/src/loss.rs:30-411 (scalar der-fn block conventions + doc-citation style),
    crates/cb-compute/src/loss.rs:424-499 (calc_softmax max-shift + softmax_ders coupled accumulation),
    crates/cb-compute/src/runtime.rs:56-258 (Loss enum + MultiQuantile owned-param precedent), :283-392 (Loss::validate → OutOfRange),
    crates/cb-compute/src/ranking_der.rs (Plan 01 grouped seam + group_reduce_weighted),
    catboost-master/catboost/private/libs/algo_helpers/error_functions.h:871-933 (QueryRMSE CalcDersForQueries + CalcQueryAvrg),
    catboost-master/catboost/private/libs/algo_helpers/error_functions.cpp:531-577 (QuerySoftMax der, max-shift, sumWTargets<=0 guard),
    catboost-master/catboost/private/libs/options/loss_description.cpp:181-209 (QuerySoftMax lambda/beta defaults)
  </read_first>
  <behavior>
    - QueryRMSE: per group, queryAvrg = Σ(target-approx)·w / Σw; der1[d]=(target[d]-approx[d]-queryAvrg)·w[d], der2[d]=-w[d]. Empty/zero-weight group → ders 0, no panic.
    - QuerySoftMax: per group, max-shift Beta·approx before exp; p=expApprox·w/Σ(expApprox·w); der1=Beta·(-sumWTargets·p + w·target), der2=Beta·sumWTargets·(Beta·p·(p-1)-LambdaReg); if sumWTargets<=0 → ders 0.
    - Loss::validate: QuerySoftMax lambda finite & >=0, beta finite & >0 → else CbError::OutOfRange (no panic).
    - All per-group reductions (queryAvrg numerator/denominator, softmax Σ) go through cb_core::sum_f64.
  </behavior>
  <files>crates/cb-compute/src/loss.rs, crates/cb-compute/src/loss_test.rs, crates/cb-compute/src/runtime.rs, crates/cb-compute/src/ranking_der.rs, crates/cb-compute/src/ranking_der_test.rs</files>
  <action>
    Add `Loss::QueryRmse` and `Loss::QuerySoftMax { lambda: f64, beta: f64 }` to the enum
    (runtime.rs), each with a full doc-comment in the existing style (der formula,
    `error_functions.{h,cpp}:line` citation, defaults, raw-approx/pointwise-leaf note) —
    mirror the `MultiQuantile` owned-param precedent (runtime.rs:250-258). Add the
    QuerySoftMax `lambda`/`beta` range checks to `Loss::validate` (mirror the Quantile/Tweedie
    arms). In `loss.rs` add the per-object inner helpers documenting the citation and returning
    UNWEIGHTED scalars where the per-group wrapper supplies the rest — QuerySoftMax MUST
    max-shift before `exp` exactly like `calc_softmax` (loss.rs:424-448; Security V5 NaN guard).
    In `ranking_der.rs` replace the Plan-01 "not yet wired" arm for these two losses with the
    per-group transcription: QueryRMSE computes `queryAvrg` per group (error_functions.h:879-933)
    then der1/der2 per object; QuerySoftMax computes the per-group softmax der
    (error_functions.cpp:531-577) with the `sumWTargets<=0 → ders 0` guard. Every per-group
    reduction routes through `cb_core::sum_f64` (group asc, doc asc). Write `loss_test.rs`
    additions + `ranking_der_test.rs` additions as the RED step: hand-computed der for a tiny
    2-group fixture vs the transcription; max-shift overflow safety; empty-group → 0; validate
    rejects bad lambda/beta. Both are raw-approx (isExpApprox==false) — no exp-stored buffer.
  </action>
  <verify>
    <automated>cargo test -p cb-compute queryrmse querysoftmax ranking_der --tests</automated>
  </verify>
  <acceptance_criteria>
    QueryRmse + QuerySoftMax{lambda,beta} in Loss enum with doc-cited der; ranking_der.rs arms
    compute per-group der matching hand-computed values; sumWTargets<=0 and empty-group guards
    return 0; validate rejects bad params; all reductions via sum_f64; unit tests green.
  </acceptance_criteria>
  <done>Both losses' der + Loss variants + grouped-seam arms land; der unit tests pass (RED→GREEN).</done>
</task>

<task type="auto">
  <name>Task 2: Boosting dispatch + per-stage oracles for QueryRMSE + QuerySoftMax</name>
  <read_first>
    crates/cb-train/src/boosting.rs:1380-1411 (train_cat / train_inner entry), :1950-1969 (der site), :869-972 (compute_leaf_deltas pointwise dispatch),
    crates/cb-train/src/query_info.rs (Plan 01 build_query_info),
    crates/cb-compute/src/runtime.rs (compute_gradients_grouped),
    crates/cb-oracle/generator/gen_ranking_fixtures.py (Plan 01 offline generator),
    crates/cb-oracle/tests/per_stage_oracle_test.rs:24-55 (compare_stage usage + load_f64_vec),
    crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs (recent per-stage loss oracle precedent)
  </read_first>
  <files>crates/cb-train/src/boosting.rs, crates/cb-train/tests/queryrmse_oracle_test.rs, crates/cb-train/tests/querysoftmax_oracle_test.rs, crates/cb-oracle/fixtures/ranking_corpus/queryrmse/.gitkeep, crates/cb-oracle/fixtures/ranking_corpus/querysoftmax/.gitkeep</files>
  <action>
    In `boosting.rs`, branch the der site (boosting.rs:1950-1969) on the loss family: when the
    loss is a ranking/querywise loss, build the grouped view once via
    `crate::query_info::build_query_info(pool group/subgroup/pairs, weights)` and call
    `runtime.compute_gradients_grouped(...)` instead of the pointwise `compute_gradients`; keep
    the existing scalar/N-dim arms BYTE-IDENTICAL (D-04). QueryRMSE/QuerySoftMax use the existing
    pointwise leaf estimators (`compute_leaf_deltas` — Gradient/Newton; no pairwise path, no
    softmax-coupled path) — confirm the upstream default `leaf_estimation_method` per loss in
    `options/` and pin it in the fixture config (RESEARCH A4). Add a small ranking-loss predicate
    (e.g. `is_grouped_loss(&Loss)`) gating the branch. Run `gen_ranking_fixtures.py --loss QueryRMSE`
    and `--loss QuerySoftMax` OFFLINE to freeze each fixture under
    `crates/cb-oracle/fixtures/ranking_corpus/{queryrmse,querysoftmax}/` (replace the `.gitkeep`
    placeholders with the real `.npy` + model.json once generated). Author
    `queryrmse_oracle_test.rs` and `querysoftmax_oracle_test.rs` that train via the public/cb-train
    path over the corpus and gate `compare_stage(Stage::Splits|LeafValues|StagedApprox|Predictions, …)`
    ≤1e-5 (mirror wave3_quantile precedent). If the `.venv` catboost cannot generate fixtures at
    author time, follow the 05-10 offline-fixture precedent: author the oracle compile-clean with
    NO `#[ignore]` and NO weakened tolerance, and the SUMMARY records the OFFLINE generation command
    as the closure step (do NOT fabricate fixtures, do NOT weaken the gate).
  </action>
  <verify>
    <automated>cargo test -p cb-train queryrmse_oracle querysoftmax_oracle --tests</automated>
  </verify>
  <acceptance_criteria>
    boosting der site dispatches ranking losses through compute_gradients_grouped on the QueryInfo
    view; scalar/N-dim arms unchanged; both oracle tests gate splits/leaf-values/staged-approx/
    predictions ≤1e-5 with no #[ignore] / no weakened tolerance; D-04 oracles stay green.
  </acceptance_criteria>
  <done>QueryRMSE + QuerySoftMax train end-to-end; per-stage oracles ≤1e-5 (or offline-fixture closure recorded); D-04 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approx/target/weights → grouped der | In-process numeric buffers; group exp-approx can overflow; zero-weight groups can divide-by-zero. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-02-01 | Tampering | QuerySoftMax exp overflow | mitigate | Max-shift Beta·approx before exp (error_functions.cpp:540-552; mirror calc_softmax) |
| T-06.3-02-02 | Denial of Service | empty group / sumWTargets<=0 / zero queryWeight division | mitigate | Guard queryCount>0 / sumWTargets>0, return 0 ders, never divide (Security V5) |
| T-06.3-02-03 | Out-of-range loss params | QuerySoftMax lambda<0 / beta<=0 | mitigate | Loss::validate → CbError::OutOfRange, no panic |
| T-06.3-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; offline generator uses present .venv catboost 1.2.10 |
</threat_model>

<verification>
- `cargo test -p cb-compute queryrmse querysoftmax ranking_der --tests` green (der + seam).
- `cargo test -p cb-train queryrmse_oracle querysoftmax_oracle --tests` green (per-stage ≤1e-5).
- `cargo test -p cb-train --tests` shows scalar/N-dim D-04 oracles green (no-regression).
- Per-crate verification; free disk before full suite.
</verification>

<success_criteria>
- QueryRMSE + QuerySoftMax train end-to-end on the grouped seam, pointwise leaf path.
- Per-stage oracles (splits/leaf-values/staged-approx/predictions) ≤1e-5 vs catboost 1.2.10 (or offline-fixture closure recorded, gate un-weakened).
- D-04 scalar/N-dim oracles green.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-02-SUMMARY.md` when done.
</output>
