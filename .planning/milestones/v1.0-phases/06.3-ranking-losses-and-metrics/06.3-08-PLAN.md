---
phase: 06.3-ranking-losses-and-metrics
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-train/src/pairwise_leaves.rs
  - crates/cb-train/src/pairwise_leaves_test.rs
  - crates/cb-oracle/generator/stochasticrank_oracle.cpp
  - crates/cb-oracle/generator/stochasticrank_centering_test.rs
autonomous: false
gap_closure: true
requirements: [LOSS-04]

must_haves:
  truths:
    - "make_zero_average in pairwise_leaves.rs routes its mean accumulation through cb_core::sum_f64 instead of a raw average += v loop (WR-01 / D-08)"
    - "stochasticrank_oracle.cpp centering aligns its accumulation order with cb_core::sum_f64 semantics (WR-03 / D-08), validated for groups > 4 docs with non-zero mu"
  artifacts:
    - path: "crates/cb-train/src/pairwise_leaves.rs"
      provides: "make_zero_average using cb_core::sum_f64(res) / n"
      contains: "sum_f64"
    - path: "crates/cb-oracle/generator/stochasticrank_oracle.cpp"
      provides: "centering accumulation aligned to sum_f64 order"
      contains: "avrg"
    - path: "crates/cb-oracle/generator/stochasticrank_centering_test.rs"
      provides: "integration test for groups > 4 docs with non-zero mu exercising accumulation-order parity"
      contains: "mu"
  key_links:
    - from: "crates/cb-train/src/pairwise_leaves.rs"
      to: "cb_core::sum_f64"
      via: "make_zero_average mean accumulation"
      pattern: "sum_f64"
---

<objective>
Close the two remaining D-08 summation-discipline WARNINGS from 06.3-VERIFICATION.md / 06.3-REVIEW.md
that are NOT in ranking_der.rs (handled by 06.3-06): **WR-01** (`make_zero_average` raw `average += v`
in `pairwise_leaves.rs`) and **WR-03** (`std::accumulate` centering vs Rust `sum_f64` in the OFFLINE
`stochasticrank_oracle.cpp` generator). Per CLAUDE.md D-08, parity-critical float accumulations route
through `cb_core::sum_f64`; the project admits no silent exception (any genuine sequential-order
requirement needs an explicit upstream line citation + a recorded policy exception, which is NOT the
chosen resolution here — we align to sum_f64).

WR-01 is a pure-Rust fix. WR-03 changes the OFFLINE generator's accumulation order, which shifts the
centered `shifted[d]` values feeding the Monte-Carlo noise stream the frozen
`stochasticrank_rng_groundtruth.jsonl` captures — so the frozen fixture must be regenerated by RUNNING
the dependency-free standalone generator (NO instrumented catboost trainer build is involved; the
generator self-oracles against cb-core::TFastRng64). Because regenerating a committed frozen fixture
must be human-confirmed, the regeneration task is a `checkpoint:human-action` and this plan is
`autonomous: false` (scope_fence directive).

Purpose: Bring make_zero_average and the stochasticrank oracle centering into D-08 compliance.
Output: sum_f64-based make_zero_average + sum_f64-order-aligned oracle centering + a >4-doc non-zero-mu
accumulation-order integration test + (human-confirmed) regenerated RNG groundtruth.

Deferral note (for verify-phase): this plan does NOT close the deferred YetiRank/YetiRankPairwise/
StochasticRank end-to-end TRAINER fixtures (truth #5) nor SC-3/D-07 instrumented-harness validation
(truth #7). It does NOT touch yetirank_oracle_test.rs / yetirank_pairwise_oracle_test.rs /
stochasticrank_oracle_test.rs absent-fixture invariant guards. LOSS-04 stays partially open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-REVIEW.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-VERIFICATION.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-04-SUMMARY.md

# Files being modified
@crates/cb-train/src/pairwise_leaves.rs
@crates/cb-oracle/generator/stochasticrank_oracle.cpp
@crates/cb-oracle/generator/instrument_ranking_rng_README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 — make_zero_average via cb_core::sum_f64</name>
  <read_first>
    .planning/phases/06.3-ranking-losses-and-metrics/06.3-REVIEW.md (WR-01 section, lines 165-190 — the exact sum_f64 fix),
    crates/cb-train/src/pairwise_leaves.rs:197-213 (make_zero_average — the raw average += v loop to replace),
    crates/cb-train/src/pairwise_leaves.rs (existing cb_core import / usage in the crate for the call pattern),
    crates/cb-train/src/pairwise_leaves_test.rs (existing 2×2 + general SPD unit tests — the zero-average invariant is already asserted; extend it)
  </read_first>
  <behavior>
    - make_zero_average(res) subtracts cb_core::sum_f64(res)/n from every element; for n==0 it is a no-op.
    - The zero-average invariant holds: after the call, sum_f64 of res is ~0 (within float epsilon).
    - The 2×2 and general SPD pairwise-leaf unit tests still pass (same numeric leaf values; sum_f64 over the small res vectors matches the prior sequential sum at these sizes).
  </behavior>
  <files>crates/cb-train/src/pairwise_leaves.rs, crates/cb-train/src/pairwise_leaves_test.rs</files>
  <action>
    In `make_zero_average` (pairwise_leaves.rs:200-213) replace the `let mut average = 0.0_f64; for &v
    in res.iter() { average += v; } average /= n as f64;` block with `let average = cb_core::sum_f64(res)
    / n as f64;` (keep the `if n == 0 { return; }` guard and the subtraction loop). Update the
    doc-comment (pairwise_leaves.rs:197-199): drop the "parity contract is the loop order" exception
    language; cite that the mean accumulation routes through cb_core::sum_f64 per D-08 (CLAUDE.md admits
    no silent summation exception). Confirm `cb_core` is already imported in the file; if only a path
    form is used elsewhere, match the existing call convention. In pairwise_leaves_test.rs add (or
    extend) an assertion that after compute_pairwise_leaf_deltas the leaf deltas sum to ~0 via
    cb_core::sum_f64 (the explicit zero-average invariant).
  </action>
  <verify>
    <automated>cargo test -p cb-train pairwise_leaves --lib --tests</automated>
  </verify>
  <acceptance_criteria>
    `grep -nv '^[[:space:]]*//' crates/cb-train/src/pairwise_leaves.rs | grep -c 'average += '`
    returns 0 (no raw accumulation in non-comment lines); `grep -n "sum_f64(res)"
    crates/cb-train/src/pairwise_leaves.rs` matches inside make_zero_average; the doc-comment no longer
    claims a loop-order exception; the 2×2 + general SPD unit tests and the zero-average invariant
    assertion pass.
  </acceptance_criteria>
  <done>make_zero_average uses cb_core::sum_f64; WR-01 D-08 violation closed; pairwise-leaf unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-03 — align stochasticrank_oracle.cpp centering with sum_f64 + >4-doc non-zero-mu test</name>
  <read_first>
    .planning/phases/06.3-ranking-losses-and-metrics/06.3-REVIEW.md (WR-03 section, lines 219-246 — sum_f64 is the SOURCE OF TRUTH; the oracle adapts to it),
    crates/cb-oracle/generator/stochasticrank_oracle.cpp:170-200 (the std::accumulate centering at line 178 + the noise/scores stream it feeds),
    crates/cb-compute/src/ranking_der.rs:832-857 (the Rust SFA centering: sum_f64(approx_slice)/count — the order WR-03 must match),
    cb-core sum_f64 definition (the documented accumulation order sum_f64 uses — confirm left-to-right vs pairwise before aligning the C++),
    crates/cb-oracle/generator/instrument_ranking_rng_README.md (the OFFLINE run command for the standalone generator; STATUS section)
  </read_first>
  <behavior>
    - The centering average in stochasticrank_oracle.cpp must be computed in the SAME accumulation order cb_core::sum_f64 uses (sum_f64 is the source of truth, D-08), then divided by count.
    - For a group with > 4 docs and non-zero mu, the centered shifted[] values (and thus every downstream noise score) match the Rust stochastic_rank_group_der centering to ≤1e-5; the 3-doc mu=0 fixture (where the sum is exactly 0) does not exercise this.
  </behavior>
  <files>crates/cb-oracle/generator/stochasticrank_oracle.cpp, crates/cb-oracle/generator/stochasticrank_centering_test.rs</files>
  <action>
    First CONFIRM the accumulation order cb_core::sum_f64 actually uses (read its definition — it is the
    D-08 single audited reduction). Then rewrite the centering at stochasticrank_oracle.cpp:178: replace
    `std::accumulate(shifted.begin(), shifted.end(), 0.0)` with an accumulation matching sum_f64's order
    (if sum_f64 is left-to-right sequential, use an explicit `double avrg = 0.0; for (double s : shifted)
    avrg += s;`; if sum_f64 is pairwise/compensated, transcribe that same reduction into the generator).
    Keep `avrg /= static_cast<double>(count);` and the subtraction loop. Add a comment citing that
    sum_f64 is the parity source of truth (D-08) and the oracle adapts to it. Author
    `crates/cb-oracle/generator/stochasticrank_centering_test.rs` (dedicated test file) exercising a
    group with > 4 docs and NON-ZERO mu: compute the centered shifted[] in Rust via the same
    sum_f64-based centering as ranking_der.rs and assert it matches the generator's centering order to
    ≤1e-5 (drive the generator via the existing offline harness/CLI, or replicate its centering formula
    in-test against sum_f64). The test must be sensitive to the accumulation order (a left-to-right vs
    pairwise mismatch on > 4 non-cancelling docs shows up).
  </action>
  <verify>
    <automated>cargo test -p cb-oracle stochasticrank_centering --tests</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "std::accumulate" crates/cb-oracle/generator/stochasticrank_oracle.cpp` returns no match on
    the centering line (line ~178); the centering comment cites sum_f64 as the D-08 source of truth;
    `crates/cb-oracle/generator/stochasticrank_centering_test.rs` exists, uses > 4 docs with non-zero mu,
    and asserts centering parity ≤1e-5; `cargo test -p cb-oracle stochasticrank_centering --tests` green.
  </acceptance_criteria>
  <done>stochasticrank_oracle.cpp centering matches sum_f64 order; WR-03 closed; >4-doc non-zero-mu accumulation-order test green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Regenerate (or confirm unchanged) the frozen stochasticrank RNG groundtruth</name>
  <files>crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl</files>
  <action>
    Task 2 changed the OFFLINE stochasticrank_oracle.cpp centering accumulation order. The frozen fixture
    `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl` is
    produced by RUNNING this dependency-free standalone generator (it self-oracles against
    cb-core::TFastRng64 — NO instrumented catboost trainer build is involved). Because the centering
    change can shift the centered shifted[] values that feed the recorded noise stream, the frozen
    groundtruth may need regeneration. This step is gated for human confirmation per the scope_fence
    directive: compile and run the standalone generator per the instrument_ranking_rng_README.md STATUS
    section, diff the regenerated jsonl against the committed one, and either confirm it is unchanged or
    review + approve committing the regenerated fixture. This is a human-action checkpoint because it
    rewrites a committed frozen fixture; the executor must NOT auto-commit a regenerated fixture without
    the confirmation below.
  </action>
  <what-built>
    Task 2 changed the OFFLINE stochasticrank_oracle.cpp centering accumulation order. The frozen fixture
    `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl` is
    produced by RUNNING this dependency-free standalone generator (it self-oracles against
    cb-core::TFastRng64 — NO instrumented catboost trainer build is involved). Because the centering
    change can shift the centered shifted[] values that feed the recorded noise stream, the frozen
    groundtruth may need regeneration.
  </what-built>
  <how-to-verify>
    1. Compile and run the standalone generator per `crates/cb-oracle/generator/instrument_ranking_rng_README.md`
       STATUS section (the dependency-free build + run command — NO catboost includes, NO trainer build).
    2. Diff the regenerated `stochasticrank_rng_groundtruth.jsonl` against the committed one.
    3. If the diff is EMPTY (the 3-doc mu=0 fixture sum is exactly 0.0, so the centering change is a
       no-op for the existing corpus): no fixture change needed — confirm "no regeneration required".
    4. If the diff is NON-EMPTY: confirm the changed lines reflect ONLY the sum_f64-order centering
       (the gauss_draw stream RNG order is unchanged) and approve committing the regenerated fixture.
    5. Confirm `cargo test -p cb-train --test stochasticrank_oracle_test` still passes (the RNG-draw-log
       gate + absent-model.json invariant), and the >4-doc centering test from Task 2 is green.
  </how-to-verify>
  <verify>
    <human-check>The regenerated stochasticrank_rng_groundtruth.jsonl diff is reviewed; either confirmed unchanged or approved + committed; stochasticrank_oracle_test still green.</human-check>
  </verify>
  <done>Frozen RNG groundtruth confirmed unchanged or regenerated under human review; stochasticrank oracle test green.</done>
  <resume-signal>Type "approved — no regeneration required" OR "approved — regenerated fixture committed", or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen RNG groundtruth fixture | A silently-changed accumulation order can shift committed fixture values without review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-08-01 | Tampering | uncontrolled regeneration of the frozen stochasticrank groundtruth | mitigate | regeneration gated behind a blocking human-action checkpoint; diff reviewed before commit |
| T-06.3-08-02 | Repudiation | D-08 violation re-introduced as an undocumented exception | mitigate | fix aligns to cb_core::sum_f64 (no exception claimed); doc-comments updated to drop loop-order wording |
| T-06.3-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure Rust edit + C++ generator edit; the generator is dependency-free (zero catboost includes) |
</threat_model>

<verification>
- `cargo test -p cb-train pairwise_leaves --lib --tests` green (WR-01 + zero-average invariant).
- `cargo test -p cb-oracle stochasticrank_centering --tests` green (WR-03 >4-doc non-zero-mu).
- `cargo test -p cb-train --test stochasticrank_oracle_test` still green (absent-fixture invariant + RNG-draw gate untouched).
- Human checkpoint confirms the frozen groundtruth either needs no change or is regenerated + reviewed.
- Per-crate verification; free disk before any broader run.
</verification>

<success_criteria>
- make_zero_average uses cb_core::sum_f64 (WR-01 closed).
- stochasticrank_oracle.cpp centering matches sum_f64 order, validated for >4 docs with non-zero mu (WR-03 closed).
- Frozen RNG groundtruth either confirmed unchanged or regenerated under human review.
- No stochasticrank oracle regression; deferred truths #5/#7 noted still open.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-08-SUMMARY.md` when done.
</output>
