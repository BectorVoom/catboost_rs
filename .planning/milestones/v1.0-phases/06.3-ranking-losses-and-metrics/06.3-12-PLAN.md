---
phase: 06.3-ranking-losses-and-metrics
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-oracle/generator/stochasticrank_oracle.cpp
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl
  - crates/cb-train/tests/grouped_weight_regression_test.rs
autonomous: false
gap_closure: true
requirements: [LOSS-04]
must_haves:
  truths:
    - "The StochasticRank RNG ground-truth fixture serializes `noise`/`score` at full f64 round-trip precision (`%.17g`), so it can actually back a ≤1e-5 gate for magnitudes >= 1 (REVIEW CR-01 BLOCKER)"
    - "The committed instrumented fixture stresses the centering path with `mu != 0` and >4 docs, not the degenerate `mu = 0` masking case (REVIEW WR-05)"
    - "The grouped_weight_regression distinguish-guard is gated on the SAME scale as its assertion (`(p*(w-1.0)).abs() > 1e-6`), removing the latent false-negative (REVIEW WR-04)"
  artifacts:
    - path: "crates/cb-oracle/generator/stochasticrank_oracle.cpp"
      provides: "17-significant-digit RNG ground-truth serialization + a non-masking mu!=0 instrumented unit"
      contains: "%.17g"
    - path: "crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl"
      provides: "Regenerated full-precision RNG ground truth (RUN-ONCE/COMMIT, human-signed)"
  key_links:
    - from: "stochasticrank_oracle.cpp"
      to: "stochasticrank_rng_groundtruth.jsonl"
      via: "snprintf(buf, \"%.17g\", noise/score) RUN-ONCE/COMMIT"
      pattern: "%.17g"
---

<objective>
Close the three trainer-INDEPENDENT fixture/test findings that gate truth #7's evidence quality and a latent test false-negative — all landing + committing regardless of the 06.3-10 build outcome.

- REVIEW CR-01 (BLOCKER): `stochasticrank_oracle.cpp:197-202` serializes `noise`/`score` via `std::to_string(double)` (~6 decimals after the point). For any score with magnitude >= 1 that leaves < 1e-5 of usable precision — structurally too coarse to certify the ≤1e-5 gate the fixture's own header claims. Fix to `%.17g` via `snprintf` and regenerate the committed JSONL.
- REVIEW WR-05: the committed instrumented unit uses `const double mu = 0.0` (line 242) — the exact masking case where the shifted sum is 0 and centering is a no-op. The fixture never stresses the centering path. Add/switch to a `mu != 0`, >4-doc unit.
- REVIEW WR-04: `grouped_weight_regression_test.rs:211` guards on `p.abs() > 1e-12` but asserts `(p*w - p).abs() > 1e-6` — the guard does not imply the assertion for `p` in `[1e-12, 1e-6]`. Gate on the same scale.

Purpose: make the StochasticRank RNG ground truth precise enough to actually back ≤1e-5 (the generator is dependency-free — g++ -std=c++20, ZERO catboost includes — so this is RUN-ONCE/COMMIT offline, NOT blocked on 06.3-10's trainer build), and remove the latent regression-test false-negative.
Output: a full-precision, non-masking regenerated RNG fixture + the corrected distinguish-guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-REVIEW.md
@crates/cb-oracle/generator/stochasticrank_oracle.cpp
@crates/cb-oracle/generator/instrument_ranking_rng_README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 + WR-05 — full-precision serialization + a non-masking mu!=0 instrumented unit</name>
  <read_first>
    - crates/cb-oracle/generator/stochasticrank_oracle.cpp lines 189-215 (the `gauss_draw`/`score` LogLine emit using `std::to_string(noise[d])` / `std::to_string(scores[d])`) and line 242 (`const double mu = 0.0;`)
    - 06.3-REVIEW.md sections CR-01 (the `snprintf(buf, sizeof(buf), "%.17g", ...)` fix shape) and WR-05 (the mu!=0, >4-doc requirement)
    - instrument_ranking_rng_README.md (the JSONL schema + the OFFLINE g++ build+run command; ZERO catboost includes)
  </read_first>
  <action>
    In `stochasticrank_oracle.cpp`:
    1. Replace the `std::to_string(noise[d])` and `std::to_string(scores[d])` substring builds in the `gauss_draw` and `score` LogLine emits with a `char buf[32]; std::snprintf(buf, sizeof(buf), "%.17g", noise[d]);` pattern (and likewise for `scores[d]`), concatenating `buf`. This matches the `%.17g` the self-oracle already uses for stderr at line 229. Apply ONLY to the f64 value fields (`noise`, `score`); integer fields (`group`/`sample`/`doc`/`order`) stay `std::to_string`.
    2. Change `const double mu = 0.0;` (line 242) to a non-masking value `mu != 0` (a small graded shift, per the README's transcription notes) AND widen the instrumented unit to > 4 docs so the Stage-1 centering subtraction is non-trivial (shifted sum != 0). Keep the single group, keep `num_estimations` minimal; the goal is a non-degenerate centering input, not a large corpus.
    3. Keep the file dependency-free (ZERO catboost includes) and the `CB_INSTRUMENT_LOG`-gated JSONL emit; the self-oracle to stderr stays.
    Do NOT regenerate the committed fixture in this task — only edit the generator source. Build it to confirm it compiles clean: `g++ -std=c++20 -O2 -Wall crates/cb-oracle/generator/stochasticrank_oracle.cpp -o /tmp/stochasticrank_oracle`.
  </action>
  <verify>
    <automated>g++ -std=c++20 -O2 -Wall crates/cb-oracle/generator/stochasticrank_oracle.cpp -o /tmp/stochasticrank_oracle && grep -q '%.17g' crates/cb-oracle/generator/stochasticrank_oracle.cpp && ! grep -q 'std::to_string(noise' crates/cb-oracle/generator/stochasticrank_oracle.cpp && ! grep -qE 'const double mu = 0\.0;' crates/cb-oracle/generator/stochasticrank_oracle.cpp</automated>
  </verify>
  <acceptance_criteria>
    Generator compiles clean (`-Wall`, zero warnings); `noise`/`score` emit via `%.17g`; no `std::to_string(noise`/`std::to_string(scores` remains; `mu` is non-zero and the unit has > 4 docs. Fixture NOT yet regenerated (Task 2 gates that).
  </acceptance_criteria>
  <done>
    `stochasticrank_oracle.cpp` serializes f64 ground truth at 17 significant digits and uses a non-masking mu!=0 / >4-doc unit; compiles clean; committed fixture untouched pending Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-04 — gate the grouped_weight_regression distinguish-guard on the assertion scale</name>
  <read_first>
    - crates/cb-train/tests/grouped_weight_regression_test.rs lines 209-219 (`if (w - 1.0).abs() > f64::EPSILON && p.abs() > EXACT_TOL { ... assert!((squared - p).abs() > 1e-6 ...) }`; `EXACT_TOL = 1e-12` at line 47)
    - 06.3-REVIEW.md section WR-04 (the `(p * (w - 1.0)).abs() > 1e-6` fix)
  </read_first>
  <action>
    In `grouped_weight_regression_test.rs` change the distinguish-guard (line ~211) from `(w - 1.0).abs() > f64::EPSILON && p.abs() > EXACT_TOL` to `(w - 1.0).abs() > f64::EPSILON && (p * (w - 1.0)).abs() > 1e-6` so the guard implies the inner assertion `(squared - p).abs() > 1e-6` (since `squared - p == p*w - p == p*(w-1)`). This removes the latent false-negative for ders `p` in `[1e-12, 1e-6]` with `w` near 1. The current corpus produces `p ~ O(0.1)` so the test still exercises every non-unit object today (no coverage loss). Cite `T-06.3-12` / REVIEW WR-04 in a one-line comment.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test grouped_weight_regression_test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    The distinguish-guard reads `(p * (w - 1.0)).abs() > 1e-6`; `grouped_weight_regression_test` stays 2/2 PASS (the CR-02 invariant still gated against pre-fix code).
  </acceptance_criteria>
  <done>
    Guard and assertion share the same scale; the regression test remains green and non-vacuous.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: RUN-ONCE regenerate + human-sign the full-precision RNG ground-truth fixture</name>
  <read_first>
    - instrument_ranking_rng_README.md (## OFFLINE build + run command; ## Captured ground truth — the RUN-ONCE/COMMIT discipline, fixtures frozen, generators run OFFLINE)
    - 06.3-08-SUMMARY.md precedent (frozen-RNG-groundtruth regeneration is human-gated; the prior round confirmed byte-identical and froze with human sign-off)
  </read_first>
  <what-built>
    Task 1 upgraded `stochasticrank_oracle.cpp` to `%.17g` serialization and a non-masking `mu != 0` / >4-doc instrumented unit. Because the precision AND the unit changed, the committed `stochasticrank_rng_groundtruth.jsonl` will now genuinely differ (unlike 06.3-08 where the mu=0 corpus made the change a no-op).
  </what-built>
  <how-to-verify>
    1. Build + run the generator OFFLINE: `g++ -std=c++20 -O2 -Wall crates/cb-oracle/generator/stochasticrank_oracle.cpp -o /tmp/stochasticrank_oracle` then `CB_INSTRUMENT_LOG=/tmp/stochasticrank_rng.jsonl /tmp/stochasticrank_oracle`.
    2. Confirm the self-oracle stderr still agrees bit-for-bit with `cb-core::TFastRng64`/`std_normal` (the README's `std_normal(seed=0)` vector is the cross-check) — the RNG transcription is unchanged; only serialization precision + the centering input changed.
    3. Diff the new `/tmp/stochasticrank_rng.jsonl` against the committed `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl`; confirm the `noise`/`score` fields now carry 17 significant digits and the values reflect the non-masking mu!=0 unit.
    4. On approval, copy the regenerated JSONL over the committed fixture.
    5. Re-run the consuming oracle: `cargo test -p cb-train --test stochasticrank_oracle_test` (the RNG-draw-log gate must stay ≤1e-5 GREEN against the new full-precision ground truth) and `cargo test -p cb-oracle stochasticrank_centering --tests`.
    Approve only after the self-oracle agreement is confirmed AND the consuming oracles pass against the regenerated fixture.
  </how-to-verify>
  <resume-signal>Type "approved — regenerated full-precision fixture committed" or describe a divergence</resume-signal>
  <acceptance_criteria>
    The committed `stochasticrank_rng_groundtruth.jsonl` carries `%.17g` precision for a non-masking mu!=0 unit; the self-oracle agrees bit-for-bit with cb-core; `stochasticrank_oracle_test` + `stochasticrank_centering` stay ≤1e-5 GREEN. RUN-ONCE/COMMIT discipline honored (generator OFFLINE, fixture frozen + human-signed).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFFLINE generator → committed fixture | A dependency-free C++ generator writes the JSONL ground truth that CI reads (never runs the generator) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-12-01 | Tampering | regenerated RNG fixture | mitigate | RUN-ONCE/COMMIT with a human sign-off checkpoint; self-oracle cross-checks the regenerated stream bit-for-bit against the oracle-locked `cb-core::TFastRng64`, so a corrupted fixture is caught at compare time |
| T-06.3-12-02 | Information Disclosure | `%.17g` precision change | accept | No sensitive data; the change increases (not decreases) ground-truth fidelity; local numerical fixture only |
| T-06.3-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs introduced; the generator builds with a stock C++20 compiler, ZERO catboost includes |

No network/auth/PII surface. No high-severity threats apply.
</threat_model>

<verification>
- Generator compiles clean (`-Wall`), serializes f64 via `%.17g`, uses mu!=0 / >4 docs.
- `grouped_weight_regression_test` 2/2 PASS with the scale-matched guard.
- Regenerated fixture: self-oracle bit-for-bit vs cb-core; `stochasticrank_oracle_test` + `stochasticrank_centering` ≤1e-5 GREEN; human-signed.
</verification>

<success_criteria>
CR-01 (BLOCKER), WR-05, and WR-04 are closed; the committed StochasticRank RNG ground truth is full-precision and non-masking; the regression-test guard matches its assertion scale — all independent of the 06.3-10 build.
</success_criteria>

<artifacts_produced>
- MODIFIED: `crates/cb-oracle/generator/stochasticrank_oracle.cpp` (`%.17g` serialization + non-masking mu!=0 / >4-doc unit)
- REGENERATED (frozen, human-signed): `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl`
- MODIFIED: `crates/cb-train/tests/grouped_weight_regression_test.rs` (scale-matched distinguish-guard)
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-12-SUMMARY.md` when done
</output>
