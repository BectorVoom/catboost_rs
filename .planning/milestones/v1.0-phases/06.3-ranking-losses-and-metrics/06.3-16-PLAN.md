---
phase: 06.3-ranking-losses-and-metrics
plan: 16
type: execute
wave: 2
depends_on: ["06.3-15"]
files_modified:
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/tree_pairwise_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/model.json
  - crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/staged.npy
  - crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/predictions.npy
  - crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs
autonomous: false
user_setup: []
requirements: [LOSS-04]
must_haves:
  truths:
    - "The greedy oblivious tree search routes split scoring through the pairwise scorer when is_pairwise_scoring(loss) is true, and through the existing pointwise/Cosine/L2 calcer otherwise"
    - "A PairLogitPairwise model.json + staged.npy + predictions.npy fixture, trained OFFLINE by catboost 1.2.10 over the frozen ranking corpus, is committed"
    - "pairlogit_pairwise_oracle_test.rs has its #[ignore] removed and passes all four per-stage compare gates (Splits|LeafValues|StagedApprox|Predictions) at ≤1e-5"
    - "The non-pairwise tree-search path (scalar/N-dim/QueryRMSE/QuerySoftMax/LambdaMart/PairLogit/YetiRank) remains byte-identical (D-04 no-regression)"
  artifacts:
    - path: "crates/cb-train/src/tree.rs"
      provides: "pairwise-scoring branch in the candidate-scoring dispatch gated on is_pairwise_scoring"
      contains: "calculate_pairwise_score"
    - path: "crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/model.json"
      provides: "frozen upstream PairLogitPairwise trained model"
    - path: "crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs"
      provides: "un-ignored per-stage ≤1e-5 oracle"
  key_links:
    - from: "crates/cb-train/src/tree.rs:split_score dispatch"
      to: "cb_compute::calculate_pairwise_score"
      via: "is_pairwise_scoring(loss) branch over the candidate's der_sums + pair_weight_statistics"
      pattern: "is_pairwise_scoring|calculate_pairwise_score"
    - from: "crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs"
      to: "ranking_corpus/PairLogitPairwise/model.json"
      via: "compare_stage(Splits|LeafValues|StagedApprox|Predictions)"
      pattern: "compare_stage"
---

<objective>
Close gap #1: wire the Plan-06.3-15 pairwise split-scorer into the greedy
oblivious tree search (gated on `is_pairwise_scoring`), train + freeze the
`PairLogitPairwise` fixture OFFLINE with catboost 1.2.10, and remove the
`#[ignore]` from `pairlogit_pairwise_oracle_test.rs` so the full per-stage ≤1e-5
gate runs.

The verification isolated the divergence precisely: PairLogitPairwise leaf VALUES
are already correct (the Cholesky leaf path in `pairwise_leaves.rs` is closed) —
the gap is SPLIT SELECTION at tree-0 split index 1 (upstream f0@1.628 vs cb-train
f1@1.816). `*Pairwise` losses score splits through the pairwise scorer, not the
pointwise der histogram. Plan 06.3-15 built the scorer; this plan connects it and
locks the result against the upstream fixture.

Purpose: deliver the FULL ≤1e-5 PairLogitPairwise oracle (one of the three
remaining LOSS-04 gaps) with no tolerance weakened and no fabricated fixture.
Output: tree-search pairwise branch + frozen fixture + un-ignored passing oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-PATTERNS.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-15-PLAN.md
@CLAUDE.md
</context>

## Artifacts this phase produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| pairwise branch in `crates/cb-train/src/tree.rs` candidate scoring | code | gated `is_pairwise_scoring(loss)`; builds per-candidate bucket_of + der_sums + pair_weight_statistics, calls `cb_compute::calculate_pairwise_score`, selects best border |
| `crates/cb-train/src/tree_pairwise_test.rs` | new test file | unit tests for the pairwise scoring branch + D-04 non-pairwise byte-identity guard |
| `crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/model.json` | fixture | frozen upstream trained model (REPLACES the absent/stale fixture) |
| `crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/staged.npy` | fixture | per-iteration staged approx |
| `crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/predictions.npy` | fixture | final raw predictions |
| `pairlogit_pairwise_oracle_per_stage` (un-ignored) | test | full 4-stage ≤1e-5 gate |
| `gen_ranking_fixtures.py` PairLogitPairwise config (if it needs the `bayesian_matrix_reg` / boosting_type=Plain pin) | generator edit | OFFLINE only |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route candidate scoring through the pairwise scorer when is_pairwise_scoring</name>
  <files>crates/cb-train/src/tree.rs, crates/cb-train/src/tree_pairwise_test.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-train/src/tree.rs:41-46 (split_score dispatch), :337-460 (assign_leaves, score_candidate, multi_dim_candidate_score, greedy_tensor_search_oblivious, the level candidate loop :531-573)
    - crates/cb-compute/src/pairwise_scoring.rs (the Plan 06.3-15 scorer: compute_der_sums, compute_pair_weight_statistics, calculate_pairwise_score)
    - catboost-master/catboost/private/libs/algo/scoring.cpp:830-850 (how upstream selects the pairwise calcer + passes l2DiagReg/bayesian_matrix_reg)
    - catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp:680-690 (scoreCalcer.Reset(new TPairwiseScoreCalcer) gating)
    - crates/cb-train/src/boosting.rs:2422-2530 (eff_weights / pairwise weight wiring + where the per-tree competitors / group_spans flow into the search)
    - crates/cb-compute/src/ranking_der.rs:49-59 (is_pairwise_scoring, is_plain_only)
  </read_first>
  <behavior>
    - When is_pairwise_scoring(loss): for each candidate float feature, compute bucket_of[doc] from the feature's borders, der_sums + pair_weight_statistics over the CURRENT leaf assignment leaf_of and the per-tree competitor/pair adjacency, then calculate_pairwise_score returns one score per border; the best (feature, border) is selected by the existing select_best_candidate first-strict-exceed rule.
    - When NOT is_pairwise_scoring: the dispatch is byte-identical to today (Cosine default / L2), proven by a regression guard.
    - The pairwise weighted derivatives fed to compute_der_sums are the SAME weighted_der1 the current pairwise path already produces (eff_weights folded), so leaf values continue to flow through pairwise_leaves.rs unchanged.
    - l2DiagReg = params.l2_leaf_reg; pairwise_bucket_weight_prior_reg = bayesian_matrix_reg default 0.1 (pinned; surfaced as a BoostParams field or a documented constant matching the fixture model.json).
  </behavior>
  <action>
    In `tree.rs` add a pairwise candidate-scoring branch reachable from the greedy oblivious level search (the candidate loop near `:531-573`): when `cb_compute::is_pairwise_scoring(&loss)` is true, for each candidate float feature build `bucket_of` via the feature borders (mirror `assign_leaves`'s per-feature bucketing), call `cb_compute::compute_der_sums` + `cb_compute::compute_pair_weight_statistics` over the current `leaf_of` and the per-tree pair adjacency, then `cb_compute::calculate_pairwise_score(...)` and emit a `Candidate` per border using the existing `Candidate`/`select_best_candidate` machinery (do NOT sort by score — keep the first-strict-exceed tie rule, tree.rs:244-250). Thread `l2_diag_reg = params.l2_leaf_reg` and `pairwise_bucket_weight_prior_reg = 0.1` (the `bayesian_matrix_reg` default from oblivious_tree_options.cpp:16) through from `boosting.rs`; document the source. Keep the non-pairwise branch (`split_score` Cosine/L2 dispatch) untouched and byte-identical (D-04). In `boosting.rs`, pass the per-tree competitor adjacency + eff_weights-folded weighted der into the search for pairwise losses (reuse the existing `uses_pairwise_weights`/`calc_pairwise_weights` plumbing at :2422-2530 — do NOT recompute pair weights). Create `tree_pairwise_test.rs` (INFRA-06 separate file; add the `#[path]` footer to tree.rs if not already present) with: a unit test that the pairwise branch selects the border maximizing `calculate_pairwise_score` over a 2-leaf hand case, and a D-04 guard asserting a non-pairwise loss produces the identical candidate ordering/score as before this change.
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib tree_pairwise && cargo test -p cb-train --test queryrmse_oracle_test --test querysoftmax_oracle_test --test lambdamart_oracle_test --test pairlogit_oracle_test --test yetirank_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p cb-train --lib tree_pairwise` passes. The five named pre-existing oracle tests (QueryRMSE/QuerySoftMax/LambdaMart/PairLogit/YetiRank) STILL pass ≤1e-5 (D-04 no-regression — the pairwise branch must not perturb non-pairwise scoring). `cargo clippy -p cb-train --lib --no-deps` shows 0 new `indexing_slicing`/`unwrap_used`.
  </acceptance_criteria>
  <done>tree search dispatches to the pairwise scorer iff is_pairwise_scoring; non-pairwise path byte-identical; all five non-pairwise oracles green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Generate + freeze the PairLogitPairwise fixture OFFLINE (catboost 1.2.10)</name>
  <files>crates/cb-oracle/generator/gen_ranking_fixtures.py, crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/model.json, crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/staged.npy, crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/predictions.npy</files>
  <read_first>
    - crates/cb-oracle/generator/gen_ranking_fixtures.py (the OFFLINE generator that builds the corpus fixtures against .venv catboost 1.2.10)
    - crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs:79-115 (base_params — the exact pins the fixture model.json must match: depth 2, 5 iters, lr 0.3, l2_leaf_reg 5, leaf_estimation_iterations 1, boosting_type Plain, bootstrap No, random_strength 0, thread_count 1, score_function Cosine→pairwise, leaf Gradient)
    - .planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md (Pitfall 2: *Pairwise forces boosting_type=Plain; IsPlainOnlyModeLoss)
    - crates/cb-oracle/fixtures/ranking_corpus/PairLogit (the sibling pointwise fixture layout to mirror)
  </read_first>
  <what-built>
    The OFFLINE generator config for `PairLogitPairwise` (loss_function='PairLogitPairwise', boosting_type='Plain', the bayesian_matrix_reg / l2_leaf_reg pins matching the test's base_params) and the three committed fixture files (model.json, staged.npy, predictions.npy) produced by running catboost 1.2.10 in the .venv over the frozen ranking corpus inputs.
  </what-built>
  <how-to-verify>
    1. Confirm `.venv` catboost is 1.2.10: `.venv/bin/python -c "import catboost; print(catboost.__version__)"` → 1.2.10.
    2. Run `gen_ranking_fixtures.py` PairLogitPairwise target OFFLINE; it must write `ranking_corpus/PairLogitPairwise/{model.json,staged.npy,predictions.npy}`.
    3. Confirm the fixture model.json's params match `pairlogit_pairwise_oracle_test.rs::base_params` (depth 2, iterations 5, learning_rate 0.3, l2_leaf_reg 5, boosting_type Plain). A mismatch means the test pins and the fixture disagree — RECONCILE before committing (escalate-don't-weaken: change the test pin to match upstream defaults, never weaken the gate).
    4. This is RUN-ONCE/COMMIT (D-08): generators run OFFLINE, fixtures frozen.
  </how-to-verify>
  <acceptance_criteria>
    The three files exist under `ranking_corpus/PairLogitPairwise/`, model.json parses via `cb_oracle::load_model_json`, and its trained params equal the test's `base_params`. Human confirms the fixture was produced by genuine catboost 1.2.10 training (not hand-authored).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the PairLogitPairwise fixture is generated by catboost 1.2.10 and committed, or describe the param reconciliation needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Un-ignore pairlogit_pairwise_oracle_test.rs and pass the full ≤1e-5 gate</name>
  <files>crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs (the whole file — remove the #[ignore] at :143 and the deferral comment block :117-142)
    - crates/cb-train/tests/pairlogit_oracle_test.rs (the closed pointwise sibling — the un-ignored 4-stage gate shape to match)
    - crates/cb-oracle/src/compare.rs (compare_stage Stage enum + ≤1e-5 tolerance)
  </read_first>
  <action>
    Remove the `#[ignore = "deferred: ..."]` attribute (line 143) and the multi-line deferral comment (lines 117-142) from `pairlogit_pairwise_oracle_test.rs`. Keep the test body as-is — it already trains under `Loss::PairLogitPairwise` and gates Splits/LeafValues/StagedApprox/Predictions via `compare_stage` at ≤1e-5 against the newly frozen fixture. If the split stage now diverges, the pairwise scorer or its tree wiring is wrong — fix the wiring/scorer (Plan 06.3-15/16), NEVER weaken the tolerance or `#[ignore]` it again (escalate-don't-weaken, D-6.3-03b). Update the doc-header comment to record CLOSURE (gap #1) with the commit that landed the scorer.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test pairlogit_pairwise_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p cb-train --test pairlogit_pairwise_oracle_test` reports `1 passed; 0 ignored` with all four `compare_stage` gates green at ≤1e-5. No `#[ignore]` remains in the file (grep). No tolerance constant changed in compare.rs (grep diff).
  </acceptance_criteria>
  <done>PairLogitPairwise per-stage oracle passes un-ignored at ≤1e-5; gap #1 (truth #4 PairLogitPairwise) CLOSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trainer tree search → pairwise scorer | candidate buckets + competitor adjacency cross from cb-train into the cb-compute scorer |
| OFFLINE generator → committed fixture | the frozen fixture is a trusted oracle ground truth; it must be genuine catboost 1.2.10 output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-16-01 | Tampering | fixture provenance | mitigate | blocking-human checkpoint confirms model.json is genuine catboost 1.2.10 training output, params reconciled against the test pins (no hand-authored fixture) |
| T-06.3-16-02 | Spoofing/Tampering | a weakened or re-ignored oracle masking a real divergence | mitigate | acceptance gate forbids `#[ignore]` and any tolerance change; escalate-don't-weaken (D-6.3-03b) |
| T-06.3-16-03 | Denial of Service | malformed candidate bucket/leaf index in the tree branch | mitigate | bounds-guard via `.get`; typed error, no panic (CLAUDE.md) |
| T-06.3-16-SC | Tampering | npm/pip/cargo installs | accept | no package installs; the generator uses the pre-existing .venv catboost 1.2.10 |
</threat_model>

<verification>
- `cargo test -p cb-train --test pairlogit_pairwise_oracle_test` → 1 passed, 0 ignored, all 4 stages ≤1e-5.
- The five non-pairwise oracles (QueryRMSE/QuerySoftMax/LambdaMart/PairLogit/YetiRank) still pass (D-04).
- `cargo test -p cb-train --lib tree_pairwise` green.
- No `#[ignore]` in pairlogit_pairwise_oracle_test.rs; no compare.rs tolerance change.
</verification>

<success_criteria>
The pairwise split-scorer is wired into the tree search behind `is_pairwise_scoring`; the PairLogitPairwise fixture is frozen from catboost 1.2.10; the oracle is un-ignored and passes the full four-stage ≤1e-5 gate; the non-pairwise path is byte-identical. Gap #1 closed.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-16-SUMMARY.md` when done.
</output>
