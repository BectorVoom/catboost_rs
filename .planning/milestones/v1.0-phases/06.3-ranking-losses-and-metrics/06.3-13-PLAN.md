---
phase: 06.3-ranking-losses-and-metrics
plan: 13
type: execute
wave: 2
depends_on: ["06.3-10"]
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/pairwise_leaves.rs
  - crates/cb-train/tests/pairlogit_oracle_test.rs
  - crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs
  - crates/cb-oracle/fixtures/ranking_corpus/PairLogit/per_leaf_der_log.jsonl
autonomous: false
gap_closure: true
requirements: [LOSS-04]
must_haves:
  truths:
    - "GAP 1 / Truth #4: PairLogit and PairLogitPairwise train end-to-end and match upstream catboost 1.2.10 ≤1e-5 per stage (Splits|LeafValues|StagedApprox|Predictions) — both oracle tests run with NO `#[ignore]` and NO weakened tolerance"
    - "The per-leaf SumDer/SumDer2 reduction the Rust pairwise-leaf path applies is the one the instrumented trainer logs (the single cross-leaf-pair leaf's effective denominator is reproduced, resolving the ~6x divergence)"
  artifacts:
    - path: "crates/cb-train/tests/pairlogit_oracle_test.rs"
      provides: "Executing per-stage PairLogit ≤1e-5 oracle (Newton, l2=3)"
      contains: "fn pairlogit_oracle_per_stage"
    - path: "crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs"
      provides: "Executing per-stage PairLogitPairwise ≤1e-5 oracle (Gradient/Cholesky, l2=5)"
  key_links:
    - from: "boosting.rs / pairwise_leaves.rs (per-leaf der2 reduction)"
      to: "PairLogit Newton leaf delta"
      via: "instrumented per-leaf SumDer2 log transcription"
      pattern: "SumDer2"
---

<objective>
Close GAP 1 (Truth #4): the PairLogit / PairLogitPairwise end-to-end ≤1e-5 oracle, currently `#[ignore]`'d on a precisely-isolated leaf-der2 parity gap.

ROOT CAUSE (deferred-items.md [06.3-03]/[06.3-09], bit-verified): at iter-0 tree-0 the `TPairLogitError` der1 leaf sums are IDENTICAL to upstream, yet the Newton leaf deltas need mutually-inconsistent per-leaf denominators — the single-object leaf holding obj10's lone cross-leaf pair (10,11) needs an effective denom ~23 from sumDer=0.5/sumDer2=-0.25 (delta 0.0064), where a plain per-object der2 sum yields ~3.75 (delta 0.04) — a ~6x gap that NO global L2/pairwise scaling reconciles. The per-leaf SumDer2 (or a per-leaf coupling the pointwise path omits) genuinely differs upstream. The bt.PairwiseWeights split-scoring wiring (06.3-09) already advanced the Splits match index 4 → 6; the residual is the LEAF-der2 reduction.

This plan requires the instrumented trainer from 06.3-10. If 06.3-10 recorded NO-GO, this plan does NOT execute the un-ignore — it stays the escalate-don't-weaken branch (oracles remain `#[ignore]`'d, no tolerance weakened, no fabricated fixture) and only the diagnosis is recorded.

Purpose: capture the upstream per-leaf SumDer/SumDer2 log for the frozen PairLogit fixture, transcribe the reduction into the Rust pairwise-leaf path, advance the Splits match to full agreement, then REMOVE `#[ignore]` from both oracle tests — the full ≤1e-5 gate runs the moment `#[ignore]` is removed.
Output: a closed Truth #4 (or a recorded, un-weakened deferral if 06.3-10 was NO-GO).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.3-ranking-losses-and-metrics/deferred-items.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-VERIFICATION.md
@crates/cb-oracle/generator/instrumented_trainer_STATUS.md
@crates/cb-train/src/pairwise_leaves.rs
@crates/cb-train/tests/pairlogit_oracle_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the instrumented per-leaf SumDer/SumDer2 log for the frozen PairLogit fixture; diagnose the upstream reduction</name>
  <read_first>
    - crates/cb-oracle/generator/instrumented_trainer_STATUS.md (the GO/NO-GO gate from 06.3-10; the `_catboost.so` path on GO)
    - crates/cb-oracle/generator/instrument_live_trainer_README.md (## RUN-ONCE invocation — dropping the instrumented `_catboost.so` into a venv-package copy + `CB_INSTRUMENT_LOG`)
    - deferred-items.md [06.3-03]/[06.3-09] (the reproduction table: leaf0 needs denom 7.47 vs our 5.5; obj10's single-pair leaf needs ~23; der1 leaf sums already match)
    - crates/cb-train/tests/pairlogit_oracle_test.rs (the frozen fixture inputs: feature columns, pairs.npy (7,2), target, base_params Newton/l2=3)
  </read_first>
  <action>
    GATE: read `instrumented_trainer_STATUS.md`. If NO-GO, STOP this plan's mutation tasks — record in the SUMMARY that GAP 1 stays deferred (escalate-don't-weaken, no `#[ignore]` removed, no tolerance weakened) and exit. If GO:
    1. Using the instrumented `_catboost.so` from 06.3-10, train the EXACT frozen PairLogit fixture config (Newton, l2=3, the committed feature columns + pairs.npy + target) with `CB_INSTRUMENT_LOG` set so the per-leaf SumDer/SumDer2 reduction events are logged for iter-0 tree-0 (and enough subsequent trees to cover the per-stage compare).
    2. Freeze the captured log under `crates/cb-oracle/fixtures/ranking_corpus/PairLogit/per_leaf_der_log.jsonl` (RUN-ONCE/COMMIT, OFFLINE).
    3. Diagnose: from the log, identify the EXACT upstream per-leaf SumDer2 reduction (whether it is a per-leaf coupling, a pairwise-weighted der2 fold, or a `AddLeafDersForQueries` der2 path that the current plain per-object der2 sum omits). Record the reduction rule and how it reproduces the obj10-leaf ~23 denominator and the leaf0 7.47 denominator.
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/generator/instrumented_trainer_STATUS.md && (grep -qi 'NO-GO' crates/cb-oracle/generator/instrumented_trainer_STATUS.md || test -f crates/cb-oracle/fixtures/ranking_corpus/PairLogit/per_leaf_der_log.jsonl)</automated>
  </verify>
  <acceptance_criteria>
    On GO: `per_leaf_der_log.jsonl` exists with per-leaf SumDer/SumDer2 events for the frozen PairLogit fixture, and the upstream reduction rule is documented (reproduces the ~23 and 7.47 denominators). On NO-GO: the plan records the un-weakened deferral and makes no mutation.
  </acceptance_criteria>
  <done>
    The upstream per-leaf der2 reduction is captured and understood (GO), or GAP 1 is recorded as an un-weakened deferral (NO-GO).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transcribe the per-leaf der2 reduction into the Rust pairwise-leaf path; un-ignore both PairLogit oracles</name>
  <read_first>
    - crates/cb-oracle/fixtures/ranking_corpus/PairLogit/per_leaf_der_log.jsonl (from Task 1; GO only)
    - crates/cb-train/src/pairwise_leaves.rs (the Cholesky leaf solve + `make_zero_average`; bit-exact vs `pairwise_leaves_calculation_ut.cpp`)
    - crates/cb-train/src/boosting.rs lines 2640-2690 (the grouped Newton leaf path; `leaf_weights_for_deltas` UNIT for grouped, the der2 consumed verbatim) and the `uses_pairwise_weights` / `eff_weights` / `scaled_l2` wiring at 2316-2457
    - crates/cb-train/tests/pairlogit_oracle_test.rs line 127-130 (the `#[ignore]` + the deferral comment) and pairlogit_pairwise_oracle_test.rs line 128-130
  </read_first>
  <behavior>
    - Test: PairLogit per-stage oracle (Newton, l2=3) — `compare_stage(Splits)` AND `compare_stage(LeafValues)` AND `compare_stage(StagedApprox)` AND `compare_stage(Predictions)` all ≤1e-5 vs the frozen catboost 1.2.10 fixture.
    - Test: PairLogitPairwise per-stage oracle (Gradient/Cholesky, l2=5) — same four stages ≤1e-5.
    - Test: at iter-0 tree-0 the obj10 single-cross-leaf-pair leaf delta matches upstream (the ~23-denominator case the diagnosis isolated).
  </behavior>
  <action>
    GO ONLY (skip on NO-GO per Task 1 gate). Transcribe the per-leaf der2 reduction rule diagnosed in Task 1 into the Rust pairwise-leaf path (`pairwise_leaves.rs` and/or the grouped Newton leaf branch in `boosting.rs` ~2640-2690), so the per-leaf SumDer2 matches the instrumented log. This is the der2 analogue of the already-correct der1 path — do NOT touch the bit-verified der1 leaf sums or the bt.PairwiseWeights split-scoring (06.3-09) or the `scaled_l2` wiring (those are correct). Route any new per-leaf accumulation through `cb_core::sum_f64` (D-08). No `unwrap()`/`expect()`/`panic` (CLAUDE.md). Source/test separation: oracle tests stay in `tests/`.
    Then REMOVE `#[ignore]` from `pairlogit_oracle_test.rs:128` and `pairlogit_pairwise_oracle_test.rs:128` and delete the deferral comment. The tests already carry the full `compare_stage(Splits|LeafValues|StagedApprox|Predictions)` wiring (175 lines each, VERIFICATION confirmed substantive) — removing `#[ignore]` runs the full ≤1e-5 gate. NO tolerance weakening (the 1e-5 bound is fixed).
    Per REVIEW IN-01: add a `leaf_weights` assertion to the PairLogit oracle now that it executes (the model's `leaf_weights` stores pairwise weight sums for `uses_pairwise_weights` losses).
  </action>
  <verify>
    <automated>cargo test -p cb-train --test pairlogit_oracle_test --test pairlogit_pairwise_oracle_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    GO: neither oracle test contains `#[ignore]`; both pass all four `compare_stage` checks ≤1e-5; the obj10-leaf behavior test passes; a `leaf_weights` assertion is present. NO-GO: tests remain `#[ignore]`'d, no tolerance changed (recorded deferral). Verify with `grep -L '#\[ignore\]'` on the two test files on the GO path.
  </acceptance_criteria>
  <done>
    PairLogit + PairLogitPairwise per-stage oracles execute ≤1e-5 with no `#[ignore]` (GO), closing Truth #4 — or stay deferred un-weakened (NO-GO).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFFLINE instrumented trainer → committed der-log fixture | The instrumented `_catboost.so` writes the per-leaf der2 log CI reads (never runs the trainer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-13-01 | Tampering | committed `per_leaf_der_log.jsonl` | mitigate | RUN-ONCE/COMMIT OFFLINE; the un-ignored oracle gates the Rust path against the frozen catboost 1.2.10 model.json ≤1e-5, so a corrupted der-log surfaces as a failing oracle, not a silent pass |
| T-06.3-13-02 | Denial of Service | new per-leaf der2 reduction | mitigate | All new accumulation via `cb_core::sum_f64`; no `unwrap`/`panic`; typed errors per CLAUDE.md |
| T-06.3-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; trainer artifact from 06.3-10's sudo-free build |

No network/auth/PII surface; no high-severity threats apply to the numerical leaf-der reduction.
</threat_model>

<verification>
- GO: `grep -c '#\[ignore\]'` on both PairLogit oracle tests == 0; both run all four stages ≤1e-5; obj10-leaf test passes; `leaf_weights` asserted.
- NO-GO: oracles remain `#[ignore]`'d; 1e-5 tolerance unchanged; deferral recorded.
- No regression in queryrmse/querysoftmax/lambdamart oracles (unaffected, but spot-check).
</verification>

<success_criteria>
GAP 1 / Truth #4 closed: PairLogit + PairLogitPairwise per-stage ≤1e-5 oracles execute with no `#[ignore]` and no weakened tolerance — or, on a 06.3-10 NO-GO, the gap stays deferred with the diagnosis recorded and nothing weakened or fabricated.
</success_criteria>

<artifacts_produced>
- NEW (GO, frozen): `crates/cb-oracle/fixtures/ranking_corpus/PairLogit/per_leaf_der_log.jsonl`
- MODIFIED (GO): `crates/cb-train/src/pairwise_leaves.rs` and/or `crates/cb-train/src/boosting.rs` (per-leaf der2 reduction)
- MODIFIED (GO): `crates/cb-train/tests/pairlogit_oracle_test.rs` + `pairlogit_pairwise_oracle_test.rs` (`#[ignore]` removed, `leaf_weights` assertion added)
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-13-SUMMARY.md` when done
</output>
