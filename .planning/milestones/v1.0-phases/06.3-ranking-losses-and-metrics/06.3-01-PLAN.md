---
phase: 06.3-ranking-losses-and-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-train/src/query_info.rs
  - crates/cb-train/src/query_info_test.rs
  - crates/cb-compute/src/ranking_der.rs
  - crates/cb-compute/src/ranking_der_test.rs
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/lib.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/generator/gen_ranking_fixtures.py
  - crates/cb-oracle/fixtures/ranking_corpus/README.md
autonomous: true
requirements: [LOSS-04]
user_setup: []

must_haves:
  truths:
    - "A grouped view (Vec<QueryInfo>) is built from a Pool's group_id/subgroup_id/pairs, with contiguous-unique group runs asserted (typed CbError, no panic)"
    - "compute_gradients_grouped slices approx/target/weights per group and returns per-object Ders, every per-group reduction routed through cb-core::sum_f64"
    - "The existing pointwise compute_gradients signature is byte-identical (no-regression on shipped scalar/N-dim oracles)"
    - "A frozen ranking corpus generator (catboost 1.2.10, thread_count=1) emits a multi-group dataset with group_id/subgroup_id and explicit pairs the Wave A-D oracles consume"
  artifacts:
    - path: "crates/cb-train/src/query_info.rs"
      provides: "QueryInfo/Competitor structs + build_query_info(pool-columns) grouped-view builder"
      contains: "struct QueryInfo"
      min_lines: 60
    - path: "crates/cb-compute/src/ranking_der.rs"
      provides: "Grouped der seam: calc_ders_for_queries dispatch over QueryInfo (per-group slicing)"
      contains: "fn calc_ders_for_queries"
      min_lines: 50
    - path: "crates/cb-oracle/generator/gen_ranking_fixtures.py"
      provides: "OFFLINE catboost 1.2.10 ranking corpus + per-loss/per-metric fixture generator"
      contains: "group_id"
  key_links:
    - from: "crates/cb-compute/src/ranking_der.rs"
      to: "cb_core::sum_f64"
      via: "per-group/per-pair reduction"
      pattern: "sum_f64"
    - from: "crates/cb-train/src/query_info.rs"
      to: "QueryInfo"
      via: "grouped view shape mirrors TQueryInfo"
      pattern: "QueryInfo"
---

<objective>
Build the grouped der seam — the design hinge of LOSS-04 — and the frozen ranking
fixture corpus every later wave consumes. This is the thinnest end-to-end ranking
foundation: a `QueryInfo`-shaped grouped view (mirroring upstream `TQueryInfo`) +
a `compute_gradients_grouped` entry point that slices approx/target/weights by
group/pair boundaries, plus the OFFLINE catboost 1.2.10 generator that produces a
multi-group ranking dataset. No individual loss math lands here — this plan delivers
the seam and corpus that Plans 02–05 attach to, with the existing pointwise locks
left byte-identical.

Purpose: Establish the single grouped seam all ranking losses funnel through (D-6.3-03),
so Waves A–D are pure per-loss transcription onto a stable structure.
Output: `query_info.rs`, `ranking_der.rs`, the `compute_gradients_grouped` sibling
seam, and `gen_ranking_fixtures.py` + a frozen ranking corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-CONTEXT.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-PATTERNS.md

# Vendored upstream transcription sources (read the cited line ranges only)
@catboost-master/catboost/private/libs/data_types/query.h
@catboost-master/catboost/private/libs/target/data_providers.cpp

# Existing Rust seams to consume / extend (read once, extract patterns)
@crates/cb-data/src/pool.rs
@crates/cb-compute/src/runtime.rs
@crates/cb-core/src/reduction.rs
@crates/cb-oracle/generator/ordered_oracle.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: QueryInfo grouped-view builder (cb-train)</name>
  <read_first>
    crates/cb-data/src/pool.rs (group_id :184, subgroup_id :190, pairs :196, Pair struct :22-27),
    catboost-master/catboost/private/libs/data_types/query.h:19-44 (TQueryInfo shape),
    catboost-master/catboost/private/libs/data_types/query.h:65 (GroupSamples contiguous+unique assertion),
    catboost-master/catboost/private/libs/target/data_providers.cpp:290-340 (explicit pairs → Competitors),
    crates/cb-train/src/lib.rs (module wiring + pub(crate) conventions),
    crates/cb-train/src/metrics.rs:117-149 (degenerate-guard → CbError pattern)
  </read_first>
  <files>crates/cb-train/src/query_info.rs, crates/cb-train/src/query_info_test.rs, crates/cb-train/src/lib.rs</files>
  <action>
    Create `query_info.rs` mirroring upstream `TQueryInfo` (D-6.3-03, per RESEARCH Pattern 1):
    define `pub struct QueryInfo { begin: usize, end: usize, weight: f64, subgroup_id: Vec<u64>, competitors: Vec<Vec<Competitor>> }`
    and `pub struct Competitor { id: usize, weight: f64 }`. Add `pub(crate) fn build_query_info(group_id: &[u64], subgroup_id: &[u64], pairs: &[Pair], weights: &[f64]) -> CbResult<Vec<QueryInfo>>`
    that scans `group_id` for contiguous equal runs (one QueryInfo per run, begin/end the
    half-open object span). Assert grouped+unique exactly as upstream `GroupSamples`
    (query.h:65): a group id reappearing after a different id is a typed
    `cb_core::CbError::Degenerate` (NOT a panic — CLAUDE.md bans unwrap/panic/indexing-slicing).
    Map explicit `Pool.pairs` into per-group `competitors[winner_local_idx]` adjacency,
    re-indexing global winner/loser_id to group-local indices (mirror data_providers.cpp:315-340);
    reject any pair whose endpoints fall in different groups or out of range with a typed error.
    Per-group `weight` defaults to the upstream group-weight convention (mean of member
    weights when present, else 1.0) — read the convention from query.h and document the
    citation in the doc-comment. Ungrouped input (empty group_id) yields one QueryInfo
    spanning all objects. Route any summation through `cb_core::sum_f64`.
    Wire `mod query_info;` into `lib.rs` and a `#[cfg(test)] #[path = "query_info_test.rs"] mod tests;`
    footer (mandatory source/test separation — INFRA-06). In `query_info_test.rs` cover:
    multi-group contiguous runs → correct begin/end/count; subgroup_id carried; explicit
    pairs → correct group-local competitors; non-contiguous group id → Degenerate error;
    cross-group pair → typed error; empty group_id → single full-span group.
  </action>
  <verify>
    <automated>cargo test -p cb-train query_info --tests</automated>
  </verify>
  <acceptance_criteria>
    build_query_info produces the correct grouped view for multi-group + subgroup + explicit-pair
    input; non-contiguous group_id and cross-group/out-of-range pairs return typed CbError (no panic);
    empty group_id → single full-span group; query_info unit tests green.
  </acceptance_criteria>
  <done>QueryInfo/Competitor + build_query_info land with contiguous-unique assertion and typed errors; unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Grouped der seam + compute_gradients_grouped (cb-compute)</name>
  <read_first>
    crates/cb-compute/src/runtime.rs (Loss enum :56, compute_gradients :469-475, Derivatives :434, Loss::validate :283),
    crates/cb-compute/src/loss.rs:478-499 (softmax_ders coupled accumulation shape),
    crates/cb-compute/src/leaf.rs:184-296 (ordered-reduction / per-leaf gather discipline),
    crates/cb-core/src/reduction.rs:32 (sum_f64), :45 (sum_f32_in_f64),
    catboost-master/catboost/private/libs/algo_helpers/error_functions.h:831-841 (CalcDersForQueries seam signature)
  </read_first>
  <files>crates/cb-compute/src/ranking_der.rs, crates/cb-compute/src/ranking_der_test.rs, crates/cb-compute/src/runtime.rs, crates/cb-compute/src/lib.rs</files>
  <action>
    Create `ranking_der.rs` as the grouped der seam mirroring upstream `IDerCalcer::CalcDersForQueries`
    (error_functions.h:831-841, RESEARCH Pattern 1). The seam takes a `GroupView` mirror of
    `QueryInfo` (re-declare a compute-tier `QueryInfo`/`Competitor` here OR accept the data as
    plain slices + group boundary spans — choose the representation that keeps cb-compute free
    of a cb-train dependency, matching the existing crate layering where cb-train depends on
    cb-compute, never the reverse; document the choice). Expose
    `pub fn calc_ders_for_queries(loss: &Loss, approx: &[f64], target: &[f64], weights: &[f64], groups: &[GroupSpan], random_seed: u64) -> CbResult<Vec<Derivatives>>`
    where `GroupSpan` carries `{ begin, end, weight, competitors }`. This plan implements ONLY
    the dispatch skeleton + per-group slicing infrastructure: for each group, slice
    `approx[begin..end]`/`target`/`weights`, and route to a per-loss arm. Provide ONE concrete
    arm now as the seam smoke-test — a trivial `Loss` arm that is already in the enum and has an
    unambiguous per-object pointwise meaning under grouping is NOT available, so instead add a
    private helper `group_reduce_weighted(slice, weights) -> f64` (the per-group normalizer all
    real losses use) routed through `cb_core::sum_f64`, and a `match loss { _ => Err(CbError::...) }`
    that returns a typed "ranking loss not yet wired" error for every variant (Plans 02–05 replace
    these arms). Add a sibling grouped entry on the Runtime path: in `runtime.rs` add
    `fn compute_gradients_grouped(...)` to the `Runtime` trait with a default impl delegating to
    `ranking_der::calc_ders_for_queries` (host-side, NO cubecl kernel — RESEARCH Architectural
    Responsibility Map; AGENTS.md: 6.3 is host reductions). Leave the existing pointwise
    `compute_gradients` signature BYTE-IDENTICAL (D-04 no-regression). Every per-group/per-pair
    sum routes through `cb_core::sum_f64` in upstream order (group asc, doc asc — RESEARCH Pitfall 4).
    Guard empty-group / zero-weight division returning 0 (Security V5 — mirror metrics.rs:145-149).
    Wire `mod ranking_der;` + `#[cfg(test)] #[path = "ranking_der_test.rs"] mod tests;`.
    In `ranking_der_test.rs` cover: per-group slicing produces correct begin/end views;
    `group_reduce_weighted` matches a hand-computed sum_f64 reduction; empty group → 0 (no panic);
    an unwired loss variant → typed error.
  </action>
  <verify>
    <automated>cargo test -p cb-compute ranking_der --tests</automated>
  </verify>
  <acceptance_criteria>
    calc_ders_for_queries slices per group correctly and routes reductions through sum_f64;
    compute_gradients_grouped exists on the Runtime trait; the existing pointwise compute_gradients
    signature is unchanged; empty group returns 0 not panic; unwired variants return typed errors;
    ranking_der unit tests green.
  </acceptance_criteria>
  <done>Grouped der seam + Runtime::compute_gradients_grouped land; pointwise path byte-identical; unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: OFFLINE ranking fixture corpus generator (cb-oracle)</name>
  <read_first>
    crates/cb-oracle/generator/gen_fixtures.py (existing offline fixture generator conventions),
    crates/cb-oracle/generator/README.md,
    crates/cb-oracle/src/compare.rs:10-31 (Stage enum: Splits/LeafValues/StagedApprox/Predictions/Permutation),
    .planning/phases/06.3-ranking-losses-and-metrics/06.3-VALIDATION.md (Wave 0 corpus spec)
  </read_first>
  <files>crates/cb-oracle/generator/gen_ranking_fixtures.py, crates/cb-oracle/fixtures/ranking_corpus/README.md</files>
  <action>
    Create `gen_ranking_fixtures.py` — an OFFLINE (RUN-ONCE/COMMIT, D-08) catboost 1.2.10
    generator producing the shared ranking corpus the Wave A–D oracles consume. Build a small
    deterministic ranking dataset: multiple groups of VARIED size (per VALIDATION Wave-0 spec),
    a `group_id` column (contiguous, unique per group), a `subgroup_id` column, an explicit
    `pairs` set (winner/loser within groups), a numeric feature matrix, and a graded relevance
    target. Pin `thread_count=1`, `depth=2`, `iterations=5`, `leaf_estimation_iterations=1`,
    a fixed `random_seed`, and `boosting_type=Plain` (the `*Pairwise` variants force Plain —
    RESEARCH; pinning Plain for the whole corpus keeps fixtures uniform). The script must accept
    a `--loss <name>` / `--metric <name>` arg and emit, per loss, the per-stage `.npy` arrays
    matching the existing Stage enum (model.json for Splits/LeafValues; staged approx; final
    predictions) plus X/y/group_id/subgroup_id/pairs inputs, under
    `crates/cb-oracle/fixtures/ranking_corpus/<name>/`. Document in
    `fixtures/ranking_corpus/README.md`: the exact corpus shape (group sizes, pair set), every
    pinned param, the catboost version (1.2.10), and the OFFLINE run command — so later waves
    regenerate identically. DO NOT run any instrumented C++ build here (that is Wave C);
    this generator uses only the Python `.venv` catboost (present). If `.venv` catboost is not
    importable at author time, the script must still be authored complete + committed and the
    README must state the offline run command (mirror the 05-10/05-09 offline-fixture precedent);
    no fixture is fabricated. Generated `.npy` fixtures for the specific losses are produced by
    Plans 02–05 invoking this generator with their loss name — this task delivers the generator
    + corpus spec + README, not every per-loss fixture.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('crates/cb-oracle/generator/gen_ranking_fixtures.py').read())" && test -f crates/cb-oracle/fixtures/ranking_corpus/README.md</automated>
  </verify>
  <acceptance_criteria>
    gen_ranking_fixtures.py parses clean, accepts --loss/--metric, pins thread_count=1/depth=2/
    iterations=5/leaf_estimation_iterations=1/Plain/fixed-seed, and emits per-stage .npy under
    ranking_corpus/<name>/; README documents corpus shape + params + offline run command.
  </acceptance_criteria>
  <done>Offline ranking corpus generator + README committed; produces per-stage fixtures for any named loss/metric.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pool group/pair columns → QueryInfo builder | In-process training input; group_id/pairs may be malformed (non-contiguous, out-of-range indices). Not network/untrusted, but a library must not panic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-01-01 | Denial of Service | build_query_info on malformed group_id/pairs | mitigate | Contiguous+unique assertion + in-range pair check return typed CbError::Degenerate; no unwrap/expect/panic/indexing_slicing (CLAUDE.md lint gate) |
| T-06.3-01-02 | Tampering | group/pair index arithmetic (local↔global remap) | mitigate | Validate winner/loser_id < n_rows and same-group before local remap; typed error on violation |
| T-06.3-01-03 | Denial of Service | empty-group / zero-weight division in group_reduce_weighted | mitigate | Guard queryCount>0 / total_weight>0, return 0 (mirror metrics.rs:145-149) |
| T-06.3-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; generator uses already-present .venv catboost 1.2.10; no new crate |
</threat_model>

<verification>
- `cargo test -p cb-train query_info --tests` green (grouped-view builder).
- `cargo test -p cb-compute ranking_der --tests` green (grouped der seam).
- `cargo test -p cb-train --tests` shows D-04 scalar/N-dim oracles still green (no-regression — pointwise compute_gradients unchanged).
- `gen_ranking_fixtures.py` parses; `ranking_corpus/README.md` present.
- Verify per-crate; free disk before any full-suite run (disk-pressure memory; 67G free now).
</verification>

<success_criteria>
- QueryInfo grouped view built from Pool columns with contiguous-unique assertion + typed errors.
- compute_gradients_grouped seam on the Runtime trait, host-side, routing every reduction through cb-core::sum_f64.
- Existing pointwise compute_gradients signature byte-identical (shipped scalar/N-dim oracles green).
- Offline ranking corpus generator + README delivering per-stage fixtures for any named loss/metric.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-01-SUMMARY.md` when done.
</output>
