---
phase: 06.3-ranking-losses-and-metrics
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-compute/src/ranking_der.rs
  - crates/cb-compute/src/ranking_der_test.rs
autonomous: true
gap_closure: true
requirements: [LOSS-04]
must_haves:
  truths:
    - "Every `cum_sum` / `cum_sum_up` / `cum_sum_low` read in `calc_dcg_metric_diff` is bounds-checked (`.get(..).copied().unwrap_or(0.0)`), consistent with the CR-01 `pos_weights` discipline two lines above (WR-02)"
    - "The 42 `clippy::indexing_slicing` panic sites in `stochastic_rank_group_der` are converted to bounds-checked accesses with NO numerical change (oracle-revalidated, DCG cumulative-stats loop)"
    - "lambdamart_oracle and stochasticrank RNG-draw oracles stay ≤1e-5 GREEN (no parity regression from the hardening)"
  artifacts:
    - path: "crates/cb-compute/src/ranking_der.rs"
      provides: "Bounds-checked DCG cumulative-stats + metric-diff reductions (no raw `[]` in the parity-critical loops)"
      contains: "stochastic_rank_group_der"
  key_links:
    - from: "ranking_der.rs:calc_dcg_metric_diff"
      to: "cum_sum / cum_sum_up / cum_sum_low"
      via: ".get(..).copied().unwrap_or(0.0)"
      pattern: "cum_sum.*get\\("
---

<objective>
Close the two trainer-INDEPENDENT `ranking_der.rs` hardening findings so they land + commit regardless of the build outcome in 06.3-10: REVIEW WR-02 (raw `[]` indexing of the prefix-sum arrays in `calc_dcg_metric_diff`, inconsistent with the bounds-checked `pos_weights` reads the CR-01 fix introduced two lines above) and the 42 pre-existing `clippy::indexing_slicing` panic sites in `stochastic_rank_group_der` (deferred-items.md [06.3-06]).

Purpose: remove latent index-panic sites from the parity-critical DCG cumulative-stats loop, matching CLAUDE.md's unchecked-index ban. This is a hardening pass on a number-exact path — it must be oracle-revalidated, NOT blind-fixed.
Output: a `ranking_der.rs` with no raw `[]` in the StochasticRank/NDCG reduction loops, with lambdamart + stochasticrank oracles still ≤1e-5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-REVIEW.md
@.planning/phases/06.3-ranking-losses-and-metrics/deferred-items.md
@crates/cb-compute/src/ranking_der.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-02 — bounds-check the cum_sum prefix-array reads in calc_dcg_metric_diff</name>
  <read_first>
    - crates/cb-compute/src/ranking_der.rs lines 899-909 (the CR-01 fix already reads `pos_weights.get(old_pos).copied().unwrap_or(0.0)` at 899-900; the very next lines 903-908 still index `cum_sum[old_pos]`, `cum_sum[new_pos]`, `cum_sum_low[old_pos/new_pos]`, `cum_sum[new_pos+1]`, `cum_sum[old_pos+1]`, `cum_sum_up[new_pos+1/old_pos+1]` with raw subscripts)
    - 06.3-REVIEW.md § WR-02 (the exact 6 raw-index sites)
  </read_first>
  <behavior>
    - Test: `calc_dcg_metric_diff` with an `old_pos`/`new_pos` at the boundary (`new_pos+1 == count`) returns the same f64 it does today (no off-by-one introduced by the `.get()` conversion).
    - Test: a graded-relevance NDCG group (ideal_dcg != 1.0) produces the identical metric diff before vs after the WR-02 change (numerically unchanged).
  </behavior>
  <action>
    In `calc_dcg_metric_diff` (ranking_der.rs ~880-909), convert the 6 raw prefix-sum subscripts in BOTH `mid_diff` arms to `cum_sum.get(idx).copied().unwrap_or(0.0)` / `cum_sum_low.get(..)` / `cum_sum_up.get(..)`, matching the `pos_weights.get(..).copied().unwrap_or(0.0)` form the CR-01 fix already uses at lines 899-900. The `cum_sum*` arrays are length `count+1` so all current reads are in-range — this is a discipline/consistency fix that yields IDENTICAL values for the present caller, removing the latent panic if a future `top`/`query_top_size` change desyncs the index range. Do NOT change the arithmetic; only the access form. Cite `T-06.3-11` and the CLAUDE.md unchecked-index ban in a one-line comment.
    Add the two RED-first behavior tests to `crates/cb-compute/src/ranking_der_test.rs` (the sibling `#[path]`-linked test file — NO inline `#[cfg(test)]` in the source, source/test separation).
  </action>
  <verify>
    <automated>cargo test -p cb-compute --lib calc_dcg_metric_diff 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    No raw `cum_sum[`/`cum_sum_up[`/`cum_sum_low[` subscript remains in `calc_dcg_metric_diff`; `grep -nE 'cum_sum(_up|_low)?\[' crates/cb-compute/src/ranking_der.rs` returns only the prefix-BUILD writes (lines ~763-773), not the metric-diff reads. The two new behavior tests pass.
  </acceptance_criteria>
  <done>
    `calc_dcg_metric_diff` reads all prefix sums via bounds-checked `.get().copied().unwrap_or(0.0)`; values are numerically identical for the present caller; new tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: clippy hardening — convert the 42 indexing_slicing sites in stochastic_rank_group_der; oracle-revalidate</name>
  <read_first>
    - crates/cb-compute/src/ranking_der.rs (the `stochastic_rank_group_der` body, fn starts at ~698, the DCG cumulative-stats loop spans ~742-829: `noise[d]`, `scores[d]`, `cum_sum[pos+1]`, `cum_sum_up[..]`, `cum_sum_low[..]`, `order[pos]`, `der1[doc_id]`, etc.)
    - deferred-items.md [06.3-06] (the 42-error baseline confirmed pre-existing on HEAD 00b299a; remediation MUST be oracle-revalidated, not blind-fixed, because it touches the parity-critical DCG cumulative-stats loop)
  </read_first>
  <action>
    Convert the direct-index accesses inside `stochastic_rank_group_der` (ranking_der.rs ~698-829) to bounds-checked forms: read sites → `.get(i).copied().unwrap_or(0.0)` (for f64 slices) / `.get(i).copied()` with an explicit typed fallback (for index slices like `order`); write sites into pre-sized `Vec`s either keep within an `if let Some(slot) = v.get_mut(i)` guard or restructure to iterator/`enumerate` forms. The numerical result must be BIT-IDENTICAL — this is purely an access-form change on in-range indices. Do NOT introduce `unwrap()`/`expect()` (CLAUDE.md ban). Where a fallback can never fire on a correct caller, prefer an iterator form over a silent `unwrap_or` so the loop intent stays clear.
    After the conversion, run `cargo clippy -p cb-compute --lib` and confirm the `indexing_slicing` count in this fn drops to 0 (it was 42 on HEAD). Then oracle-revalidate: the lambdamart per-stage oracle and the stochasticrank RNG-draw oracle must stay ≤1e-5 GREEN, and `cargo test -p cb-compute --lib` must stay at its current pass count (122/122) with no regression.
  </action>
  <verify>
    <automated>cargo clippy -p cb-compute --lib 2>&1 | grep -c 'indexing may panic' | grep -qx 0 && cargo test -p cb-compute --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    `cargo clippy -p cb-compute --lib` reports 0 `indexing may panic` diagnostics in `stochastic_rank_group_der`; `cargo test -p cb-compute --lib` passes at the prior count; `cargo test -p cb-train --test lambdamart_oracle_test` and `cargo test -p cb-train --test stochasticrank_oracle_test` stay GREEN ≤1e-5 (no parity regression).
  </acceptance_criteria>
  <done>
    Zero `indexing_slicing` panics remain in `stochastic_rank_group_der`; all reductions are bounds-checked with no numerical change; lambdamart + stochasticrank oracles unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-crate numerical refactor; no input crosses a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-11-01 | Denial of Service | `stochastic_rank_group_der` / `calc_dcg_metric_diff` index panics | mitigate | Convert raw `[]` to bounds-checked `.get(..)` so a future caller index desync returns a defined value instead of panicking (the WR-02 / clippy hardening this plan performs) |

No network/auth/PII surface. No high-severity threats apply to this numerical hardening pass.
</threat_model>

<verification>
- `grep -nE 'cum_sum(_up|_low)?\[' ranking_der.rs` returns only prefix-build writes, not metric-diff reads.
- `cargo clippy -p cb-compute --lib` → 0 `indexing may panic`.
- `cargo test -p cb-compute --lib` at prior pass count; lambdamart + stochasticrank oracles ≤1e-5.
</verification>

<success_criteria>
WR-02 and the 42 clippy indexing sites are closed in `ranking_der.rs` with provably zero numerical change (oracles ≤1e-5 unchanged), landing independently of the 06.3-10 build outcome.
</success_criteria>

<artifacts_produced>
- MODIFIED: `crates/cb-compute/src/ranking_der.rs` (bounds-checked `calc_dcg_metric_diff` + `stochastic_rank_group_der`)
- MODIFIED/NEW tests in `crates/cb-compute/src/ranking_der_test.rs` (WR-02 boundary + graded-relevance equality)
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-11-SUMMARY.md` when done
</output>
