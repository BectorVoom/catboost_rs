---
phase: 06.3-ranking-losses-and-metrics
plan: 17
type: execute
wave: 3
depends_on: ["06.3-16"]
files_modified:
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/model.json
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/staged.npy
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/predictions.npy
  - crates/cb-train/tests/yetirank_pairwise_oracle_test.rs
  - crates/cb-train/src/yetirank.rs
  - crates/cb-train/src/boosting.rs
requirements: [LOSS-04]
autonomous: false
user_setup: []
must_haves:
  truths:
    - "A YetiRankPairwise model.json + staged.npy + predictions.npy fixture, trained OFFLINE by catboost 1.2.10 over the frozen ranking corpus, is committed"
    - "yetirank_pairwise_oracle_test.rs::yetirank_pairwise_end_to_end_per_stage takes the model.json branch and passes all four per-stage compare gates at ≤1e-5 (RNG seed plumbing already closed; pairwise split-scorer from 06.3-16 now supplies the structure)"
    - "If the YetiRankPairwise fixture unmasks REVIEW WR-01 (dual RNG seeder desync), WR-02 (float-only candidate count), or WR-04 (learn_approx.get(*begin..*end).unwrap_or_default() silently dropping an out-of-range learning-fold span), that latent warning is fixed in this plan rather than re-deferred"
  artifacts:
    - path: "crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/model.json"
      provides: "frozen upstream YetiRankPairwise trained model"
    - path: "crates/cb-train/tests/yetirank_pairwise_oracle_test.rs"
      provides: "end-to-end per-stage ≤1e-5 oracle taking the present-fixture branch"
  key_links:
    - from: "crates/cb-train/tests/yetirank_pairwise_oracle_test.rs:yetirank_pairwise_end_to_end_per_stage"
      to: "ranking_corpus/yetirank_pairwise/model.json"
      via: "compare_stage(Splits|LeafValues|StagedApprox|Predictions) once model.json exists"
      pattern: "compare_stage"
    - from: "crates/cb-train/src/boosting.rs pairwise tree search"
      to: "cb_compute::calculate_pairwise_score"
      via: "is_pairwise_scoring(YetiRankPairwise) routes the per-tree re-sampled competitors through the pairwise scorer"
      pattern: "is_pairwise_scoring|calculate_pairwise_score"
---

<objective>
Close gap #2: with the pairwise split-scorer landed (06.3-15) and wired into the
tree search (06.3-16), and the YetiRankPairwise RNG seed plumbing ALREADY closed
(it shares YetiRank's `YetiRankTreeSeeder` + sampler — the RNG draw-log oracle is
green), the only remaining blocker for YetiRankPairwise was the same pairwise
SPLIT-scorer gap as PairLogitPairwise. This plan trains + freezes the
`YetiRankPairwise` fixture OFFLINE and lets the existing end-to-end test take its
present-fixture branch, passing the full four-stage ≤1e-5 gate.

It also addresses the latent REVIEW warnings the verification flagged as
"unmasked once the deferred *Pairwise fixtures land": WR-01 (dual RNG seeder
desync) and WR-02 (float-only candidate count). Because the YetiRankPairwise
fixture is the first one exercising the per-tree re-sampled pairwise competitors
THROUGH the pairwise scorer, this plan fixes whichever of those actually fires
against the new fixture rather than re-deferring.

Purpose: deliver the FULL ≤1e-5 YetiRankPairwise oracle (gap #2 of LOSS-04's
three), reusing the 06.3-16 scorer and the closed YetiRank seeder.
Output: frozen fixture + present-branch passing oracle + WR-01/WR-02/WR-04 closure if
unmasked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-PATTERNS.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-16-PLAN.md
@CLAUDE.md
</context>

## Artifacts this phase produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/model.json` | fixture | frozen upstream YetiRankPairwise trained model |
| `crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/staged.npy` | fixture | per-iteration staged approx |
| `crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/predictions.npy` | fixture | final raw predictions |
| present-fixture branch of `yetirank_pairwise_end_to_end_per_stage` (now LIVE) | test | full 4-stage ≤1e-5 gate |
| WR-01 fix in `crates/cb-train/src/yetirank.rs` (if unmasked) | code | dual-RNG seeder desync → single seeded source or typed-error on desync |
| WR-02 fix in `crates/cb-train/src/boosting.rs:1824-1827` (if unmasked) | code | candidate-feature count over all candidate features, not float-only |
| WR-04 fix in `crates/cb-train/src/yetirank.rs:2993-3010` (if unmasked) | code | out-of-range learning-fold span returns a typed `CbError` instead of `unwrap_or_default()` silently dropping the group |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Generate + freeze the YetiRankPairwise fixture OFFLINE (catboost 1.2.10)</name>
  <files>crates/cb-oracle/generator/gen_ranking_fixtures.py, crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/model.json, crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/staged.npy, crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/predictions.npy</files>
  <read_first>
    - crates/cb-oracle/generator/gen_ranking_fixtures.py (OFFLINE generator; the YetiRank target as the sibling to mirror)
    - crates/cb-train/tests/yetirank_pairwise_oracle_test.rs:157-225 (the present-fixture branch — the exact params the fixture must match: loss YetiRankPairwise{permutations:10,decay:0.85}, depth 2, 5 iters, lr 0.3, boosting_type Plain, bootstrap No, random_strength 0, random_seed 20_260_617, leaf Gradient, score Cosine→pairwise)
    - crates/cb-oracle/fixtures/ranking_corpus/yetirank (the CLOSED YetiRank fixture layout + its model.json — mirror it for the pairwise variant)
    - crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/yetirank_rng_groundtruth.jsonl (the already-committed RNG ground truth — the seed stream is closed; only structure was blocked)
  </read_first>
  <what-built>
    The OFFLINE generator config for `YetiRankPairwise` (loss_function='YetiRankPairwise', permutations 10, decay 0.85, boosting_type='Plain', random_seed 20260617, the l2/bayesian_matrix_reg pins matching the test) and the three committed fixture files produced by catboost 1.2.10 over the frozen ranking corpus.
  </what-built>
  <how-to-verify>
    1. `.venv/bin/python -c "import catboost; print(catboost.__version__)"` → 1.2.10.
    2. Run the YetiRankPairwise target of `gen_ranking_fixtures.py` OFFLINE with thread_count=1 (single-thread so blockCount=1 — the RNG seed derivation the Rust sampler reproduces).
    3. Confirm the written model.json params equal the test's params block (loss, depth, iterations, lr, boosting_type Plain, random_seed). Reconcile any mismatch by changing the TEST pin to the upstream default, never by weakening the gate.
    4. RUN-ONCE/COMMIT (D-08): frozen, generators OFFLINE.
  </how-to-verify>
  <acceptance_criteria>
    The three files exist under `ranking_corpus/yetirank_pairwise/`, model.json parses via `cb_oracle::load_model_json`, params match the test. Human confirms genuine catboost 1.2.10 single-thread training output.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the YetiRankPairwise fixture is generated by catboost 1.2.10 and committed, or describe the reconciliation needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Activate the end-to-end gate + close WR-01/WR-02/WR-04 if the fixture unmasks them</name>
  <files>crates/cb-train/tests/yetirank_pairwise_oracle_test.rs, crates/cb-train/src/yetirank.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-train/tests/yetirank_pairwise_oracle_test.rs:139-238 (the if model_json.exists() present-fixture branch already wired — it activates automatically once Task 1 commits the fixture; the else absent-fixture invariant becomes dead)
    - crates/cb-train/src/yetirank.rs:262-380 (REVIEW WR-01: YetiRankTreeSeeder.rng + boosting.rs rng both seeded from random_seed — silently desyncs on bootstrap!=No / random_strength!=0; the YetiRankPairwise fixture pins bootstrap=No/random_strength=0 so confirm it stays masked OR fix)
    - crates/cb-train/src/boosting.rs:1824-1827 (REVIEW WR-02: yetirank_n_candidate_features counts only float features)
    - .planning/phases/06.3-ranking-losses-and-metrics/06.3-VERIFICATION.md:185-187 (the WR-01/WR-02/WR-04 latent-warning records + unmask conditions)
  </read_first>
  <behavior>
    - Once model.json is committed, yetirank_pairwise_end_to_end_per_stage takes the present-fixture branch and gates Splits/LeafValues/StagedApprox/Predictions ≤1e-5; it must PASS (the scorer from 06.3-16 supplies the structure; the seeder supplies the sampled pairs).
    - If the run exposes WR-01 (a draw-count/seed desync between the seeder and the boosting rng on this fixture's config) the seeding is made single-sourced or a desync returns a typed error — verified by the gate passing AND the yetirank_pairwise_rng_draw_log_oracle staying green.
    - If WR-02 fires (the candidate-feature count under-counts on this corpus) it is corrected to count all candidate features; the YetiRank (pointwise) oracle must remain green (no regression).
    - If WR-04 is unmasked (a learning-fold span on this fixture goes out of range, currently swallowed by `learn_approx.get(*begin..*end).unwrap_or_default()` at yetirank.rs:2993-3010) the unwrap_or_default is replaced with a typed `CbError` on the out-of-range span so the group-drop is no longer silent; the gate passing proves no span actually desyncs on the frozen fixture.
    - If neither fires against this float-only fixture, document that they remain correctly masked (with the precise config reason) — do NOT churn code that the fixture cannot exercise.
  </behavior>
  <action>
    Run `cargo test -p cb-train --test yetirank_pairwise_oracle_test` after Task 1. The present-fixture branch in `yetirank_pairwise_end_to_end_per_stage` activates automatically. If all four stages pass, update the test doc-header to record gap #2 CLOSURE and confirm in the SUMMARY that WR-01/WR-02/WR-04 stay masked (state the config reason: bootstrap=No, random_strength=0, float-only corpus). If the split or staged stage diverges with a draw-count/seed symptom, fix WR-01 in `yetirank.rs` by single-sourcing the seed (or returning a typed `CbError` on a detected desync) per the VERIFICATION:185 finding; if a candidate under-count symptom appears, fix WR-02 in `boosting.rs:1824-1827` to count all candidate features; if a learning-fold span goes out of range, fix WR-04 in `yetirank.rs:2993-3010` by replacing `learn_approx.get(*begin..*end).unwrap_or_default()` with a typed `CbError` on the out-of-range span (never a silent drop). NEVER `#[ignore]` the test or weaken `compare_stage` tolerance (escalate-don't-weaken). After any WR fix, re-run the YetiRank (pointwise) + PairLogitPairwise oracles to prove no regression.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test yetirank_pairwise_oracle_test --test yetirank_oracle_test --test pairlogit_pairwise_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p cb-train --test yetirank_pairwise_oracle_test` passes all three of its tests including `yetirank_pairwise_end_to_end_per_stage` on the present-fixture branch (4 stages ≤1e-5). YetiRank (pointwise) and PairLogitPairwise oracles still pass (no regression). No `#[ignore]` added; no compare.rs tolerance change. If WR-01/WR-02/WR-04 were touched, `cargo clippy -p cb-train --lib --no-deps` shows no new warnings.
  </acceptance_criteria>
  <done>YetiRankPairwise end-to-end per-stage oracle passes at ≤1e-5 on the present-fixture branch; gap #2 (truth #5 YetiRankPairwise) CLOSED; WR-01/WR-02/WR-04 either fixed or documented as correctly masked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFFLINE generator → committed YetiRankPairwise fixture | trusted oracle ground truth; must be genuine single-thread catboost 1.2.10 output |
| YetiRankTreeSeeder rng ↔ boosting rng | two seeded streams whose desync is the WR-01 latent hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-17-01 | Tampering | fixture provenance | mitigate | blocking-human checkpoint confirms genuine catboost 1.2.10 single-thread training; params reconciled to the test |
| T-06.3-17-02 | Tampering | WR-01 dual-RNG seeder desync (silent wrong training) | mitigate | single-source the seed or return a typed error on desync; gate proves correctness or documents the mask reason |
| T-06.3-17-03 | Spoofing | re-ignored / tolerance-weakened oracle | mitigate | acceptance forbids `#[ignore]` and tolerance change (D-6.3-03b) |
| T-06.3-17-SC | Tampering | npm/pip/cargo installs | accept | no package installs; reuses .venv catboost 1.2.10 |
</threat_model>

<verification>
- `cargo test -p cb-train --test yetirank_pairwise_oracle_test` → all pass, end-to-end on present-fixture branch, 4 stages ≤1e-5.
- YetiRank (pointwise) + PairLogitPairwise oracles still green (no regression).
- WR-01/WR-02/WR-04 fixed or documented-masked with the config reason.
- No `#[ignore]`, no tolerance change.
</verification>

<success_criteria>
The YetiRankPairwise fixture is frozen from catboost 1.2.10; the end-to-end per-stage oracle passes at ≤1e-5 on the present-fixture branch reusing the 06.3-16 pairwise scorer + the closed YetiRank seeder; latent WR-01/WR-02/WR-04 are resolved or proven masked. Gap #2 closed.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-17-SUMMARY.md` when done.
</output>
