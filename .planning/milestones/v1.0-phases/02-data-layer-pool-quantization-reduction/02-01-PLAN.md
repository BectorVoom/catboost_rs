---
phase: 02-data-layer-pool-quantization-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-core/src/lib.rs
  - crates/cb-core/src/reduction.rs
  - crates/cb-core/src/reduction_test.rs
  - scripts/check-no-raw-float-sum.sh
  - .github/workflows/ci.yml
  - crates/cb-oracle/generator/gen_inputs.py
  - crates/cb-oracle/generator/gen_fixtures.py
  - Cargo.toml
  - crates/cb-data/Cargo.toml
autonomous: false
requirements: [DATA-07]
must_haves:
  truths:
    - "A single sequential f64 reduction primitive exists in cb-core and is re-exported"
    - "The reduction primitive returns the naive-sequential result (not Kahan/pairwise) on an adversarial input"
    - "A CI grep gate fails the build on raw float .sum()/.fold(0.0, +) in library crates, excluding reduction.rs and *_test.rs"
    - "Wave-0 oracle fixtures (numeric_nan dataset + borders/cat-hash/class-weight scenarios) are generated and committed with documented assumption resolutions"
  artifacts:
    - path: "crates/cb-core/src/reduction.rs"
      provides: "sum_f64 / sum_f32_in_f64 sequential reduction primitive (D-07)"
      contains: "pub fn sum_f64"
    - path: "crates/cb-core/src/reduction_test.rs"
      provides: "naive-sequential-order property test (DATA-07)"
    - path: "scripts/check-no-raw-float-sum.sh"
      provides: "D-08 CI-grep backstop banning raw float summation"
    - path: "crates/cb-oracle/fixtures/borders_quant"
      provides: "border/quantization oracle fixtures for numeric_tiny + numeric_nan"
    - path: "crates/cb-oracle/fixtures/cat_hash"
      provides: "(cat string -> ui32) hash vectors + perfect-hash bins"
    - path: "crates/cb-oracle/fixtures/class_weights"
      provides: "Balanced/SqrtBalanced auto class-weight expected outputs"
  key_links:
    - from: "crates/cb-core/src/lib.rs"
      to: "crates/cb-core/src/reduction.rs"
      via: "pub use reduction"
      pattern: "pub use reduction::"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-no-raw-float-sum.sh"
      via: "CI step invocation"
      pattern: "check-no-raw-float-sum"
---

<objective>
Establish the Phase-2 foundation slice: the single audited deterministic reduction primitive in `cb-core` (DATA-07/D-07), the CI-grep gate banning all other raw float summation (D-08), and the Wave-0 oracle fixtures that every downstream parity slice compares against — generated while resolving the open Assumptions A1–A5 (NaN sentinel in `get_borders()`, default `border_count`, integer-cat stringification, CityHash64 vectors).

Purpose: Every later Phase-2 slice (borders, quantization, weights) routes its sums through this one primitive and validates against these committed fixtures. Pinning the summation order and the oracle expectations now prevents a border/summation-order divergence from poisoning any downstream tree.
Output: `cb-core::sum_f64`/`sum_f32_in_f64`, `scripts/check-no-raw-float-sum.sh` wired into CI, and committed `borders_quant/`, `cat_hash/`, `class_weights/` fixtures plus a new `numeric_nan` input dataset.
</objective>

<artifacts_this_phase_produces>
This plan introduces the following NEW symbols/paths (excluded from source-drift verification):
- Functions: `cb_core::reduction::sum_f64`, `cb_core::reduction::sum_f32_in_f64`
- Files: `crates/cb-core/src/reduction.rs`, `crates/cb-core/src/reduction_test.rs`, `scripts/check-no-raw-float-sum.sh`
- Fixture trees: `crates/cb-oracle/fixtures/borders_quant/`, `crates/cb-oracle/fixtures/cat_hash/`, `crates/cb-oracle/fixtures/class_weights/`, `crates/cb-oracle/fixtures/inputs/numeric_nan/`
- Workspace deps added in root `Cargo.toml`: `arrow = "59.0.0"`, `polars = { version = "0.54.4", default-features = false, features = ["dtype-full"] }`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-pool-quantization-reduction/02-RESEARCH.md
@.planning/phases/02-data-layer-pool-quantization-reduction/02-PATTERNS.md
@crates/cb-core/src/lib.rs
@crates/cb-core/src/rng.rs
@crates/cb-oracle/generator/gen_fixtures.py
@scripts/check-no-anyhow.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduction primitive in cb-core (the only summation primitive)</name>
  <files>crates/cb-core/src/reduction.rs, crates/cb-core/src/reduction_test.rs, crates/cb-core/src/lib.rs</files>
  <read_first>
    - `crates/cb-core/src/rng.rs` — the in-repo parity-port utility analog (doc-comment style, per-step `//` C++ citations, explicit type discipline) per 02-PATTERNS.md "reduction.rs" assignment.
    - `crates/cb-core/src/lib.rs` — module-wiring + test-lint exemption template (Shared Pattern A).
    - `crates/cb-core/src/rng_test.rs` — one-`#[test]`-per-property test-file template (Shared Pattern B).
    - Source of truth: 02-RESEARCH.md "Reduction primitive" (binarization.cpp:803-815 `totalWeight += weight`; calc_class_weights.cpp:36-54 double per-block sum, blocks=1 under thread_count=1) and Pitfall 1 (NO Kahan/pairwise).
  </read_first>
  <behavior>
    - sum_f64(&[1e16, 1.0, -1e16]) == 0.0 (locks naive-sequential order; pairwise/Kahan would give 1.0).
    - sum_f64(&[]) == 0.0; sum_f64 of a small known vector equals the left-to-right running sum.
    - sum_f32_in_f64 accumulates f32 inputs in an f64 accumulator and returns f64.
  </behavior>
  <action>
    Create `reduction.rs` with two public infallible functions `sum_f64(values: &[f64]) -> f64` and `sum_f32_in_f64(values: &[f32]) -> f64`, each a plain left-to-right `for &v in values { acc += ... }` loop over an `acc = 0.0_f64` accumulator — sequential `f64` fold per D-09/DATA-07, NO Kahan/pairwise/`.sum()`/`.fold(0.0, +)` (this file is the single sanctioned hand-written loop). Open with a `//!` doc citing `library/cpp/grid_creator/binarization.cpp:803-815` and `private/libs/target/calc_class_weights.cpp:36-54` as the order source, and stating the contract "sequential f64 fold, thread_count=1, no compensated summation". Add `mod reduction;`, `pub use reduction::{sum_f64, sum_f32_in_f64};`, and `#[cfg(test)] mod reduction_test;` to `lib.rs` (purely additive — keep existing `error`/`rng` wiring). Create `reduction_test.rs` as a dedicated sibling test file (source/test separation, D-17; no inline `#[cfg(test)] mod` in `reduction.rs`) with one `#[test]` per behavior above, the adversarial `[1e16, 1.0, -1e16]` order-lock asserted with `assert_eq!`.
  </action>
  <verify>
    <automated>cargo test -p cb-core reduction 2>&1 | tail -5; cargo test -p cb-core reduction --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-core/src/reduction.rs` contains `pub fn sum_f64` and `pub fn sum_f32_in_f64`.
    - `crates/cb-core/src/lib.rs` contains `pub use reduction::`.
    - `cargo test -p cb-core reduction` exits 0; the order-lock test asserting `sum_f64(&[1e16, 1.0, -1e16]) == 0.0` passes.
    - `reduction.rs` contains no `#[cfg(test)]` brace block (source/test separation).
    - `cargo clippy -p cb-core --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The single sequential-f64 reduction primitive exists, is re-exported from cb-core, and a property test locks the naive-sequential order.</done>
</task>

<task type="auto">
  <name>Task 2: CI-grep gate banning raw float summation (D-08) + workspace deps</name>
  <files>scripts/check-no-raw-float-sum.sh, .github/workflows/ci.yml, Cargo.toml, crates/cb-data/Cargo.toml</files>
  <read_first>
    - `scripts/check-no-anyhow.sh` — copy wholesale; reuse the `ROOT` resolver, the `CORE_DIRS` loop with `[ -d "$dir" ] || continue` tolerance, the `*_test.rs` exclusion `case`, and the `violations`/exit-1 epilogue (02-PATTERNS.md "check-no-raw-float-sum.sh" assignment).
    - `.github/workflows/ci.yml` — the existing CI step that invokes `check-no-anyhow.sh` and `check-source-test-separation.sh`; add the new gate to the same step.
    - `Cargo.toml` — `[workspace.dependencies]` block (Shared Pattern D); add `arrow`/`polars` at the RESEARCH-verified versions.
    - `crates/cb-data/Cargo.toml` — `[lints] workspace = true` + `[dependencies]` (anyhow stays ABSENT, D-14).
  </read_first>
  <action>
    Copy `check-no-anyhow.sh` to `scripts/check-no-raw-float-sum.sh`, changing only: (a) the grep pattern to detect raw float summation — match `\.sum()` and `\.fold(0.0` / `\.fold(0.0_f` / `\.fold(0f` forms in library `*.rs`; (b) the exclusion list to skip BOTH `*_test.rs` AND `crates/cb-core/src/reduction.rs` (the sanctioned primitive — mirror how the anyhow gate excludes `*_test.rs`); (c) the violation/OK messages to reference D-08. Keep the same `CORE_DIRS` set and the `[ -d "$dir" ] || continue` missing-dir tolerance. Wire it into `.github/workflows/ci.yml` in the same CI job step that runs the existing two gates, invoked by relative path. In root `Cargo.toml` `[workspace.dependencies]`, add `arrow = "59.0.0"` and `polars = { version = "0.54.4", default-features = false, features = ["dtype-full"] }` (RESEARCH Standard Stack; latest stable per CLAUDE.md). In `crates/cb-data/Cargo.toml` add `cb-core = { path = "../cb-core" }`, `thiserror.workspace = true`, `arrow.workspace = true`, `polars.workspace = true` — do NOT add `anyhow` (D-14). Commit the regenerated `Cargo.lock` (T-01-SC supply-chain convention).
  </action>
  <verify>
    <automated>chmod +x scripts/check-no-raw-float-sum.sh; bash scripts/check-no-raw-float-sum.sh; cargo build -p cb-data 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-raw-float-sum.sh` exists, is executable, and exits 0 on the current tree (reduction.rs excluded).
    - The script greps non-test library `*.rs` for `.sum()`/`.fold(0.0` and would exit 1 on a planted violation (verify by a temporary planted-line dry run, then revert).
    - `.github/workflows/ci.yml` contains a `check-no-raw-float-sum` invocation alongside the existing gates.
    - Root `Cargo.toml` `[workspace.dependencies]` contains `arrow = "59.0.0"` and a `polars` entry pinned to `0.54.4`.
    - `cargo build -p cb-data` succeeds with the new deps; `Cargo.lock` is updated and committed.
  </acceptance_criteria>
  <done>The D-08 summation ban is enforced in CI (excluding reduction.rs), and arrow/polars are wired into the workspace for downstream ingestion plans.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Generate Wave-0 oracle fixtures + resolve Assumptions A1–A5</name>
  <files>crates/cb-oracle/generator/gen_inputs.py, crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/fixtures/inputs/numeric_nan/, crates/cb-oracle/fixtures/borders_quant/, crates/cb-oracle/fixtures/cat_hash/, crates/cb-oracle/fixtures/class_weights/</files>
  <what-built>Wave-0 oracle fixtures generated from the pinned `catboost==1.2.10` venv: a new `numeric_nan` input dataset (NaN-containing float column) and an explicit-categorical variant added to `gen_inputs.py`, plus `borders_quant/`, `cat_hash/`, and `class_weights/` expected-output fixtures added to `gen_fixtures.py`, resolving Assumptions A1–A5.</what-built>
  <read_first>
    - `crates/cb-oracle/generator/gen_inputs.py` — `_write()`/`_assert_f64()`/`gen_numeric_tiny()` template for the new datasets.
    - `crates/cb-oracle/generator/gen_fixtures.py` — the `get_borders()` -> `borders.npy` + `borders_per_feature.npy` + `config.json` extraction block (the template for the new scenarios).
    - 02-RESEARCH.md "Oracle Fixture Schema", "Open Questions" (A1–A5: NaN sentinel in get_borders, default border_count, integer-cat stringification, CityHash64 vectors), and Pitfall 3.
  </read_first>
  <action>
    Extend `gen_inputs.py` with a `numeric_nan` dataset (a float feature column containing `np.nan` entries) and an explicit-categorical dataset (categorical columns fed as explicit int/string, NOT f64-coded) following the existing `_write`/`_assert_f64` discipline (pinned seed, recorded in `config.json`). Extend `gen_fixtures.py` to emit three new scenario dirs reusing the proven `get_borders()` extraction: `borders_quant/` (borders for `numeric_tiny` + `numeric_nan`; `config.json` records `border_count`, `nan_mode`, `border_selection_type=GreedyLogSum`, and EMPIRICALLY whether `get_borders()` includes the `f32::MIN`/`MAX` NaN sentinel — resolves A1/A3); `cat_hash/` (`cat_hashes.npy` = f64-encoded `ui32` per category string via an upstream probe, `perfect_hash_bins.npy` = bin per object, plus the exact integer-cat stringification used — resolves A4); `class_weights/` (`balanced.npy`, `sqrt_balanced.npy` from CatBoost auto class-weights on a binary-label dataset). Keep BUILD-TIME-only, `thread_count=1`, pinned-seed, `_assert_f64` determinism (D-12); the generator runs on the dev machine, NOT in CI. Record the resolved value of each of A1–A5 in the relevant `config.json` and in the plan SUMMARY.
  </action>
  <how-to-verify>
    1. Run `cd crates/cb-oracle/generator && .venv/bin/python gen_inputs.py && .venv/bin/python gen_fixtures.py` (venv at `crates/cb-oracle/generator/.venv`).
    2. Confirm `crates/cb-oracle/fixtures/inputs/numeric_nan/X.npy` exists and contains NaN entries.
    3. Confirm `crates/cb-oracle/fixtures/{borders_quant,cat_hash,class_weights}/` each contain their `.npy` arrays + a `config.json`.
    4. Open each `config.json` and confirm A1–A5 are explicitly recorded (sentinel-present yes/no, border_count value, cat stringification form, hash-vector source).
  </how-to-verify>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/inputs/numeric_nan/X.npy && test -f crates/cb-oracle/fixtures/borders_quant/config.json && test -f crates/cb-oracle/fixtures/cat_hash/cat_hashes.npy && test -f crates/cb-oracle/fixtures/class_weights/balanced.npy && echo FIXTURES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-oracle/fixtures/inputs/numeric_nan/X.npy` exists with NaN entries.
    - `crates/cb-oracle/fixtures/borders_quant/config.json` records whether the NaN sentinel appears in `get_borders()` (A3) and the resolved `border_count` (A2).
    - `crates/cb-oracle/fixtures/cat_hash/{cat_hashes.npy,perfect_hash_bins.npy}` exist; the integer-cat stringification (A4) is documented.
    - `crates/cb-oracle/fixtures/class_weights/{balanced.npy,sqrt_balanced.npy}` exist.
    - The plan SUMMARY records the resolved A1–A5 values for downstream plans.
  </acceptance_criteria>
  <done>numeric_nan input + borders_quant/cat_hash/class_weights fixtures committed; A1–A5 resolved and recorded in config.json and the SUMMARY.</done>
  <resume-signal>Type "approved" once fixtures are generated, committed, and A1–A5 resolutions are recorded; or describe discrepancies.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oracle generator (dev machine) → committed fixtures | build-time-only; CI consumes frozen `.npy`/`config.json` and never runs the generator |
| reduction primitive → all downstream sums | a wrong summation order silently breaks the ≤1e-5 oracle gate everywhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | reduction primitive summation order | mitigate | Property test locks naive-sequential order (`[1e16,1.0,-1e16]==0.0`); D-08 CI-grep bans all other float summation |
| T-02-02 | Tampering | unaudited float `.sum()`/`.fold` slipping into a library crate | mitigate | `scripts/check-no-raw-float-sum.sh` fails CI, excluding only `reduction.rs` + `*_test.rs` |
| T-02-03 | Tampering | fixtures regenerated with wrong seed/thread_count → false oracle baseline | mitigate | `thread_count=1` + pinned seed recorded in every `config.json`; generator is build-time-only, fixtures committed frozen (D-12) |
| T-02-SC | Tampering | npm/pip/cargo installs (arrow/polars added to workspace) | mitigate | RESEARCH Package Legitimacy Audit verdicts all OK; no `[ASSUMED]`/`[SUS]` packages; commit pinned `Cargo.lock` |
</threat_model>

<verification>
- `cargo test -p cb-core reduction` green; order-lock property holds.
- `bash scripts/check-no-raw-float-sum.sh` exits 0; wired into CI.
- `cargo build -p cb-data` succeeds with arrow/polars present.
- All Wave-0 fixtures present with A1–A5 resolved in `config.json`.
</verification>

<success_criteria>
- DATA-07 reduction primitive exists, re-exported, order-locked.
- D-08 CI-grep gate live (reduction.rs excluded).
- Wave-0 oracle fixtures committed; Assumptions A1–A5 resolved and recorded.
- arrow 59.0.0 / polars 0.54.4 in the workspace; `Cargo.lock` committed.
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-pool-quantization-reduction/02-01-SUMMARY.md` when done, recording the resolved A1–A5 values for downstream plans.
</output>
