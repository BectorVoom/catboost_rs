---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 01
type: execute
wave: 2
depends_on: ["03-00"]
files_modified:
  - crates/cb-compute/src/lib.rs
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-compute/src/loss_test.rs
  - crates/cb-compute/src/histogram.rs
  - crates/cb-compute/src/histogram_test.rs
  - crates/cb-compute/src/score.rs
  - crates/cb-compute/src/score_test.rs
  - crates/cb-compute/src/leaf.rs
  - crates/cb-compute/src/leaf_test.rs
  - crates/cb-compute/Cargo.toml
  - crates/cb-backend/src/cpu_runtime.rs
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/lib.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/tree_test.rs
  - crates/cb-train/Cargo.toml
  - crates/cb-train/tests/slice_first_oracle_test.rs
autonomous: true
requirements: [TRAIN-01, TRAIN-02, TRAIN-03]
user_setup: []

must_haves:
  truths:
    - "A user can train a plain-boosted model of symmetric oblivious trees on the CPU for RMSE and Logloss"
    - "Per-tree splits (feature+border) match upstream catboost 1.2.10 to <=1e-5"
    - "Per-tree leaf values (Gradient estimation) match upstream to <=1e-5"
    - "Per-iteration staged approximants match upstream raw-logit/raw-value output to <=1e-5"
    - "Equal-gain split ties resolve via strict gain > bestGain, first-candidate-wins"
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "Abstract R: Runtime + F: Float traits (no cubecl dep, D-03)"
      contains: "pub trait Runtime"
    - path: "crates/cb-compute/src/loss.rs"
      provides: "RMSE + Logloss der1/der2 (elementwise, upstream-cited)"
    - path: "crates/cb-compute/src/leaf.rs"
      provides: "CalcAverage + ScaleL2Reg + Gradient leaf delta (host ordered reduce)"
    - path: "crates/cb-train/src/tree.rs"
      provides: "GreedyTensorSearchOblivious + strict first-wins tie-break"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "Plain boosting loop (iterations/learning_rate/depth/boost_from_average)"
    - path: "crates/cb-backend/src/cpu_runtime.rs"
      provides: "impl cb_compute::Runtime for the CubeCL CpuRuntime (launches kernels, returns un-reduced buffers)"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "cb_compute::Runtime (build_histograms/compute_gradients/eval_splits)"
      via: "trait method calls on R: Runtime"
      pattern: "Runtime"
    - from: "crates/cb-compute/src/histogram.rs"
      to: "cb_core::sum_f64"
      via: "ordered host-side bin-total reduction (D-02/D-05)"
      pattern: "sum_f64"
    - from: "crates/cb-train/tests/slice_first_oracle_test.rs"
      to: "fixtures/{regression_skeleton,binclf_skeleton}/{model.json,staged.npy}"
      via: "compare_stage(Stage::Splits|LeafValues|StagedApprox, …)"
      pattern: "compare_stage"
---

<objective>
Build the thinnest end-to-end train→predict slice: stand up `cb-compute`'s abstract `R: Runtime` / `F: Float` boundary (no cubecl, D-03), the `cb-backend` CpuRuntime trait impl that launches the kernels and returns un-reduced buffers, and the `cb-train` plain boosting loop that grows symmetric oblivious trees with Gradient leaf estimation — oracle-locked against BOTH RMSE (regression) and Logloss (binary classification) at simplified isolating params.

Purpose: Prove the full tree/leaf math surface and the generic compute seam in one slice, against params simple enough (D-07) that any divergence is unambiguously the tree/leaf math. This is the MVP first slice: after it, a user can actually train a model and have splits + leaves + staged approximants match upstream to ≤1e-5.
Output: `cb-compute` (runtime/loss/histogram/score/leaf), `cb-backend::cpu_runtime`, `cb-train` (boosting/tree), and a passing `slice_first_oracle` test for both losses (TRAIN-01, TRAIN-02, TRAIN-03 Gradient method).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-00-PLAN.md
@AGENTS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-compute::runtime`** — `pub trait Runtime` with coarse ops `compute_gradients(approx, target) -> (der1, der2)`, `build_histograms(bins, ders) -> per-bin scatter buffers`, `eval_splits(...)` (D-04); `pub trait Float` bound. NO `cubecl` dependency (D-03).
- **`cb-compute::loss`** — `rmse_der1/rmse_der2` (`target-approx`, `-1.0`), `logloss_der1/der2` (`p=sigmoid(approx)`, `target-p`, `-p*(1-p)`), `sigmoid` — elementwise scalars, upstream-cited (`error_functions.{h,cpp}`).
- **`cb-compute::histogram`** — host-side ordered bucket reduction over the kernel's scatter buffers via `cb_core::sum_f64`; `TBucketStats` analogue (`SumWeightedDelta`, `SumWeight`).
- **`cb-compute::score`** — L2 score calcer (`AddLeafPlain`: `rightAvrg*rightSum + leftAvrg*leftSum`), `MINIMAL_SCORE = f64::NEG_INFINITY`.
- **`cb-compute::leaf`** — `CalcAverage(sumDelta, count, scaledL2)`, `ScaleL2Reg(l2, sumAllWeights, docCount)`, Gradient leaf delta.
- **`cb-backend::cpu_runtime`** — `impl cb_compute::Runtime` launching the `#[cube]` gradient + histogram-scatter kernels on `CpuRuntime`, returning per-object/per-bin buffers UN-reduced (host folds).
- **`cb-backend::kernels`** — extend with the histogram-scatter kernel (order-independent scatter; no reduction).
- **`cb-train::tree`** — `GreedyTensorSearchOblivious` (one split per depth level, applied across the level; `2^depth` leaves), `SelectBestCandidate` (strict `gain > bestGain`, first-wins, upstream candidate order: feature index ascending, border ascending).
- **`cb-train::boosting`** — `train(...) -> CbResult<Model>`: init approx (`boost_from_average`: target mean for RMSE / 0 for Logloss), loop `iterations` × {compute_gradients → grow oblivious tree → Gradient leaf values → `approx += learning_rate * delta`}; `Model { oblivious_trees: Vec<{splits, leaf_values}>, bias }`.
- **New CbError variants** (as needed): degenerate-leaf / depth-cap (`depth ≤ 16`, upstream MaxDepth).
- **Oracle test:** `slice_first_oracle_test` gating `Stage::Splits`, `Stage::LeafValues`, `Stage::StagedApprox` for both `regression_skeleton` and `binclf_skeleton` at ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cb-compute boundary — Runtime/Float traits, loss, histogram, score, leaf</name>
  <files>crates/cb-compute/src/lib.rs, crates/cb-compute/src/runtime.rs, crates/cb-compute/src/loss.rs, crates/cb-compute/src/loss_test.rs, crates/cb-compute/src/histogram.rs, crates/cb-compute/src/histogram_test.rs, crates/cb-compute/src/score.rs, crates/cb-compute/src/score_test.rs, crates/cb-compute/src/leaf.rs, crates/cb-compute/src/leaf_test.rs, crates/cb-compute/Cargo.toml</files>
  <read_first>
    - crates/cb-core/src/lib.rs (lib-root module-tree + `pub use` + `#[cfg(test)] mod` shape to copy)
    - crates/cb-data/src/borders.rs (transcribe-upstream-with-f64-discipline + route-every-sum-through-`cb_core::sum_f64` service-module analog; source-of-truth doc-block shape)
    - crates/cb-data/src/nan_mode.rs (small pure elementwise transcription module analog for loss.rs)
    - crates/cb-core/src/reduction.rs (`sum_f64`/`sum_f32_in_f64` — the ONLY sanctioned float reduction; Shared Pattern C)
    - crates/cb-core/src/error.rs (`CbError`/`CbResult` — Shared Pattern B; add new variants here)
    - crates/cb-data/Cargo.toml (Shared Pattern A — lints.workspace, anyhow ban, dep shape; cb-compute must stay cubecl-free, D-03)
    - 03-RESEARCH.md § Code Examples (RMSE/Logloss der1/der2; `CalcAverage`/`ScaleL2Reg`; L2 `AddLeafPlain` score) and canonical_refs (`error_functions.{h,cpp}`, `online_predictor.h:112-178`, `score_calcers.cpp:20-49`)
  </read_first>
  <behavior>
    - loss: RMSE `der1(approx,target)=target-approx`, `der2=-1.0`; Logloss `p=sigmoid(approx)`, `der1=target-p`, `der2=-p*(1-p)` (raw-logit approx, Pitfall 6)
    - leaf: `CalcAverage(sumDelta,count,scaledL2)` returns `sumDelta/(count+scaledL2)` when `count>0`, else `0.0` (guard, no panic); `ScaleL2Reg(l2,sumAllW,docCount)=l2*(sumAllW/docCount)`
    - histogram: per-bin totals computed by folding the kernel's per-object scatter contributions through `cb_core::sum_f64` in canonical object order (D-05)
    - score: L2 `AddLeafPlain` accumulates `rightAvrg*rightSumWeightedDelta + leftAvrg*leftSumWeightedDelta`
  </behavior>
  <action>
    Fill `cb-compute` per RESEARCH "Recommended Project Structure". `runtime.rs`: define `pub trait Runtime` exposing coarse domain ops (`compute_gradients`, `build_histograms`, `eval_splits` per D-04) and a `pub trait Float` bound; NO `cubecl` import (D-03 — `cb-compute/Cargo.toml` stays cubecl-free, keep the anyhow-ban comment). `loss.rs`: one `#[must_use]` fn per derivative with upstream cite inline (transcribe RMSE/Logloss der1/der2 from `error_functions`). `leaf.rs`: `CalcAverage` (guarded `count>0`), `ScaleL2Reg`, Gradient leaf delta (`CalcAverage(sumDer, sumWeights, scaledL2)`) — transcribe `online_predictor.h:112-178`. `histogram.rs`: `TBucketStats` analogue + the ordered host-side bin-total reduction routing EVERY sum through `cb_core::sum_f64` (never `.sum()`/`.fold(0.0,+)` — D-08 CI-grep ban). `score.rs`: L2 `AddLeafPlain` score with `MINIMAL_SCORE = f64::NEG_INFINITY`. Open each module with a `# Source of truth` doc citing the exact upstream file+line. Wire `cb-compute/Cargo.toml` deps to `cb-core` + `cb-data` (`.workspace`/path). Write dedicated `*_test.rs` unit tests FIRST (tdd): RMSE/Logloss der values, `CalcAverage` count==0 guard returns 0.0, `ScaleL2Reg` formula, an ordered-reduction determinism check, L2 score on a hand-computed bucket. No inline `#[cfg(test)]`.
  </action>
  <verify>
    <automated>cargo test -p cb-compute loss::</automated>
    <automated>cargo test -p cb-compute</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-compute/src/runtime.rs` contains `pub trait Runtime` and a `Float` bound
    - `crates/cb-compute/Cargo.toml` contains no `cubecl` reference (D-03)
    - every float fold in `cb-compute` routes through `cb_core::sum_f64` (CI-grep `scripts/check-no-raw-float-sum.sh` passes)
    - `cargo test -p cb-compute loss::` exits 0 (RMSE `t-a`/`-1`; Logloss `t-p`/`-p(1-p)`)
    - `cargo test -p cb-compute` exits 0 (leaf guard, score, histogram reduction unit tests green)
  </acceptance_criteria>
  <done>The abstract compute boundary + the host-side loss/leaf/score/histogram math exist, cubecl-free, with every parity-critical sum routed through `cb_core::sum_f64`.</done>
</task>

<task type="auto">
  <name>Task 2: cb-backend CpuRuntime impl + histogram-scatter kernel</name>
  <files>crates/cb-backend/src/lib.rs, crates/cb-backend/src/kernels.rs, crates/cb-backend/src/cpu_runtime.rs</files>
  <read_first>
    - crates/cb-data/src/ingest/owned.rs (analog — "implement a cb-* trait for a concrete type")
    - crates/cb-backend/src/kernels.rs (the gradient kernel from Plan 00 — extend with scatter)
    - crates/cb-compute/src/runtime.rs (the `Runtime` trait to implement — from Task 1)
    - crates/cb-data/src/quantized_pool.rs (`ColumnBins::{U8,U16,U32}` typed-width SoA accessor — histogram-kernel input, P2 D-11)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Cubecl_generics.md (kernel launch + bytemuck `Bytes::from_elems` transfer)
    - 03-RESEARCH.md § Architectural Responsibility Map + Pattern 2 (kernel scatters, host reduces — D-02/D-05)
  </read_first>
  <action>
    Add a `#[cube(launch)]` histogram-scatter kernel (generics-float) that, per object, writes its gradient/hessian contribution into the per-bin slot for that object's bin — order-independent scatter ONLY, NO reduction inside the kernel (D-02/D-05 hard constraint; CI-grep raw-sum ban applies). In `cpu_runtime.rs`, `impl cb_compute::Runtime` for the CubeCL CpuRuntime (D-01 — the CubeCL seam stands up this phase): `compute_gradients` launches the gradient kernel; `build_histograms` launches the scatter kernel reading the `QuantizedPool` typed-width bins and returns the per-object/per-bin scatter buffers UN-reduced for the host to fold (Task-1 `histogram.rs` does the ordered `sum_f64`). Data transfer via `bytemuck::Pod` (`Bytes::from_elems`). Confirm the `SelectedRuntime = CpuRuntime` alias (Plan 00) is the concrete `R` used. On ANY CubeCL build error load `cubecl_error_guideline.md` first (AGENTS.md).
  </action>
  <verify>
    <automated>cargo test -p cb-backend</automated>
    <automated>cargo build -p cb-backend --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-backend/src/cpu_runtime.rs` contains `impl` of `cb_compute::Runtime`
    - the histogram-scatter kernel performs no in-kernel float reduction (grep: no `+=` accumulation into a shared bin total inside `#[cube]` bodies; scatter only)
    - `cargo build -p cb-backend --features cpu` exits 0 under deny-lints
    - `cargo test -p cb-backend` exits 0 (scatter kernel output matches a host reference)
  </acceptance_criteria>
  <done>The CubeCL CpuRuntime implements `cb_compute::Runtime`, launching elementwise gradient + scatter kernels and returning un-reduced buffers; the host owns the ordered reduction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cb-train oblivious tree growth + strict first-wins tie-break</name>
  <files>crates/cb-train/src/lib.rs, crates/cb-train/src/tree.rs, crates/cb-train/src/tree_test.rs, crates/cb-train/Cargo.toml</files>
  <read_first>
    - crates/cb-data/src/borders.rs (greedy-search-with-exact-tie-break analog for tree.rs; the WR-01 STL tie-break precedent)
    - crates/cb-compute/src/{runtime.rs,score.rs,histogram.rs} (from Task 1 — the host math the tree search orchestrates)
    - crates/cb-oracle/src/compare.rs (`Stage::Splits` — the consumer the split extractor feeds)
    - 03-RESEARCH.md § Pattern 1 (oblivious tree growth), Pitfall 1 (strict `>` tie-break, `greedy_tensor_search.cpp:948-966`); canonical_refs `greedy_tensor_search.cpp`
  </read_first>
  <behavior>
    - tie-break: on a hand-built equal-gain candidate set, `SelectBestCandidate` returns the FIRST candidate reaching the max gain (strict `gain > bestGain`), iterating feature index ascending then border ascending — never the later equal-gain candidate
    - oblivious growth: for `depth in 0..depth`, exactly ONE split is chosen and applied across the whole level; a depth-`d` tree has `d` splits and `2^depth` leaves
  </behavior>
  <action>
    Fill `cb-train::tree`. `tree.rs`: `GreedyTensorSearchOblivious` — for `depth in 0..depth` (cap `depth ≤ 16`, return `CbError` otherwise — `2^depth` overflow guard, upstream MaxDepth), call into `cb-compute` histogram/score, then `SelectBestCandidate` with strict `gain > bestGain` first-wins over the EXACT upstream candidate order (feature index ascending, border ascending within feature); apply the one chosen split across the whole level; `2^depth` leaves. Do NOT sort by score; do NOT use `>=` (Pitfall 1). Surface degenerate split candidates (no valid split, count==0) as `CbError`, never panic. Wire `cb-train/Cargo.toml`: deps `cb-core`/`cb-data`/`cb-compute`/`cb-backend` (`.workspace`/path); add `approx`/`anyhow` as dev-deps only per Shared Pattern A. Write `tree_test.rs` FIRST (tdd): the strict-first-wins tie-break unit test on a hand-built equal-gain candidate set — assert the FIRST max-gain candidate (feature index ascending, border ascending) wins and that swapping to `>=` would flip it. No inline `#[cfg(test)]`.
  </action>
  <verify>
    <automated>cargo test -p cb-train tree::tie_break</automated>
    <automated>cargo test -p cb-train tree::</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-train/src/tree.rs` uses strict `>` (no `>=`) in candidate selection; `cargo test -p cb-train tree::tie_break` exits 0 (first-wins on equal gain)
    - `depth` is capped (`≤ 16`); supplying a larger depth returns `CbError`, not a panic/overflow
    - `tree.rs` contains no `unwrap()`/`expect()`/`.sum()` on floats (deny-lints + D-08 grep pass)
    - `cargo test -p cb-train tree::` exits 0
  </acceptance_criteria>
  <done>`GreedyTensorSearchOblivious` grows symmetric oblivious trees choosing one split per level with the exact upstream strict-`>` first-wins tie-break (Pitfall 1), depth-capped against `2^depth` overflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: cb-train plain boosting loop + first-slice oracle (RMSE + Logloss)</name>
  <files>crates/cb-train/src/lib.rs, crates/cb-train/src/boosting.rs, crates/cb-train/tests/slice_first_oracle_test.rs</files>
  <read_first>
    - crates/cb-data/src/quantize.rs (top-level driver analog for boosting.rs)
    - crates/cb-data/tests/borders_oracle_test.rs (canonical "load committed fixture, compute Rust actual, gate via compare_stage" integration-test shape; fixture-path helper)
    - crates/cb-oracle/src/model_json.rs (from Plan 00 — split/leaf extractors) and crates/cb-oracle/src/compare.rs (`Stage` + `compare_stage`)
    - crates/cb-train/src/tree.rs (from Task 3 — the oblivious tree growth the loop drives)
    - crates/cb-compute/src/{runtime.rs,leaf.rs,score.rs,histogram.rs,loss.rs} (from Task 1 — the host math this loop orchestrates)
    - 03-RESEARCH.md § Pattern 1 (oblivious tree growth), Pitfall 2 (boost_from_average starting approx), Pitfall 5 (leaf L2 + iterations); canonical_refs `train_model.cpp` (CalcOptimumConstApprox)
  </read_first>
  <behavior>
    - boost_from_average: RMSE starting approx == target mean (stored as bias); Logloss starting approx == 0
    - first-slice oracle: trained model's per-tree splits (float_feature_index+border), per-tree leaf_values, and per-iteration staged approximants match the committed fixtures to <=1e-5, for BOTH regression_skeleton and binclf_skeleton
  </behavior>
  <action>
    Fill `cb-train::boosting`. `boosting.rs`: `train<R: Runtime>(pool, target, weights, params) -> CbResult<Model>` — init approx via `boost_from_average` (`CalcOptimumConstApprox`: target mean for RMSE / 0 for Logloss, Pitfall 2), loop `iterations` × {`R::compute_gradients` → grow oblivious tree via `cb-train::tree` (Task 3) → Gradient leaf values via `cb-compute::leaf` (ordered `sum_f64` over leaf members, `ScaleL2Reg` applied, `leaf_estimation_iterations=1`) → `approx[i] += learning_rate * leafDelta[leaf(i)]`}; return `Model { oblivious_trees, bias }`. Surface degenerate inputs (empty leaf, count==0) as `CbError`, never panic; no `unwrap`/`.sum()` on floats. `slice_first_oracle_test.rs` (integration, `tests/`, file-scope lint allow): train on `regression_skeleton` and `binclf_skeleton` inputs, then `compare_stage(Stage::Splits, …)`, `compare_stage(Stage::LeafValues, …)`, `compare_stage(Stage::StagedApprox, …)` against the Plan-00 fixtures at ≤1e-5 for BOTH losses — preserving the D-08 RMSE+Logloss coverage. (The dev-dep `cb-oracle` and `approx`/`anyhow` dev-deps were wired in Task 3's `cb-train/Cargo.toml`.)
  </action>
  <verify>
    <automated>cargo test -p cb-train slice_first_oracle</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train slice_first_oracle` exits 0 — Splits + LeafValues + StagedApprox match for BOTH regression_skeleton (RMSE) and binclf_skeleton (Logloss) at ≤1e-5 (D-08)
    - `boosting.rs` contains no `unwrap()`/`expect()`/`.sum()` on floats (deny-lints + D-08 grep pass)
    - the boosting loop applies `learning_rate * leafDelta` and stores the `boost_from_average` starting approx as `Model.bias`
    - `cargo test --workspace` exits 0 (wave merge gate)
  </acceptance_criteria>
  <done>A user can train RMSE + Logloss plain-boosted oblivious-tree models on the CPU and every split, leaf value, and staged approximant matches upstream catboost 1.2.10 to ≤1e-5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated `QuantizedPool` + target/weights → training core | Inputs already validated at ingestion (P2 D-06); the training core consumes trusted in-memory data. No network/auth surface (RESEARCH Security Domain: ASVS web categories N/A). |
| host ↔ CubeCL CpuRuntime | `bytemuck::Pod` buffer transfer; kernels scatter, host reduces (D-02/D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | leaf math / tree partition on degenerate input (empty leaf, count==0) | mitigate | `CalcAverage` guards `count>0` returns 0.0; deny-lints (`unwrap_used`/`indexing_slicing`); degenerate leaf returns `CbError`, never panic. |
| T-03-01-02 | Tampering | `2^depth` leaf-count integer overflow | mitigate | Cap `depth ≤ 16` (upstream MaxDepth); larger depth returns `CbError` before allocation. |
| T-03-01-03 | Tampering (parity integrity) | non-deterministic reduction breaking the ≤1e-5 oracle gate | mitigate | All histogram/leaf/score sums via `cb_core::sum_f64` in canonical object order (`thread_count=1` equivalent, D-02/D-05); kernels never reduce; CI-grep raw-float-sum ban (D-08). |
| T-03-01-04 | Tampering | wrong split chosen on equal-gain tie | mitigate | Strict `gain > bestGain` first-wins over fixed upstream candidate order (Pitfall 1); dedicated `tree::tie_break` unit test; no `>=`, no score-sort. |
</threat_model>

<verification>
- `cargo test -p cb-compute` + `cargo test -p cb-backend` green.
- `cargo test -p cb-train tree::tie_break` green (strict first-wins).
- `cargo test -p cb-train slice_first_oracle` green — Splits/LeafValues/StagedApprox ≤1e-5 for RMSE + Logloss.
- `cargo test --workspace` green (wave merge gate).
- CI raw-float-sum grep (D-08) green; `cb-compute/Cargo.toml` cubecl-free (D-03).
</verification>

<success_criteria>
- The generic `R: Runtime` / `F: Float` boundary is established in `cb-compute` (cubecl-free) with the `cpu` backend implementing it via CubeCL CpuRuntime.
- A plain boosting loop builds symmetric oblivious trees with Gradient leaf estimation; per-tree splits + leaf values + staged approximants pass ≤1e-5 vs C++ for RMSE and Logloss.
- A first end-to-end CPU train→predict cycle runs.
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-01-SUMMARY.md` when done
</output>
