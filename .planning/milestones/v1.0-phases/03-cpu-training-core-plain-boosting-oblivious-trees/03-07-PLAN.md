---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 07
type: execute
wave: 8
depends_on: ["03-06"]
files_modified:
  - crates/cb-train/src/lib.rs
  - crates/cb-train/src/autolr.rs
  - crates/cb-train/src/autolr_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-oracle/generator/gen_fixtures.py
autonomous: true
requirements: [TRAIN-08]
user_setup: []

must_haves:
  truths:
    - "Automatic learning-rate selection from dataset size matches upstream"
    - "Auto-LR is active only when learning_rate / leaf_estimation_method / leaf_estimation_iterations / l2_leaf_reg are ALL unset"
    - "The selected learning_rate matches upstream get_all_params()['learning_rate'] to <=1e-5"
    - "An end-to-end CPU train->predict cycle runs with auto-selected learning rate"
  artifacts:
    - path: "crates/cb-train/src/autolr.rs"
      provides: "TAutoLRParamsGuesser coefficient table + exp/log formula keyed by target/task/useBestModel/boostFromAverage"
      contains: "learning_rate"
    - path: "crates/cb-train/src/autolr_test.rs"
      provides: "Auto-LR formula unit test vs upstream-derived expected values"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/autolr.rs"
      via: "pre-train learning_rate guess when unset"
      pattern: "autolr"
---

<objective>
Add automatic learning-rate selection (TRAIN-08): the `TAutoLRParamsGuesser` coefficient table + `exp/log` formula keyed by `(TargetType, CPU, useBestModel, boostFromAverage)`, active only when `learning_rate` (and `leaf_estimation_method`/`leaf_estimation_iterations`/`l2_leaf_reg`) are all unset — matching upstream's selected learning rate, and confirming the first full end-to-end CPU train→predict cycle runs with the auto-selected rate.

Purpose: Final additive slice (D-10), closing TRAIN-08 and Phase-3 success criterion 5. The formula is a pure scalar; the parity check is the selected `learning_rate` matching `get_all_params()['learning_rate']`.
Output: `cb-train::autolr` with the coefficient table + formula, the pre-train guess wired into the loop, and a passing `autolr` unit test plus an end-to-end auto-LR train→predict run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-01-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-train::autolr`** — `TAutoLRParamsGuesser`: `const` coefficient table `{A,B,C,D}` keyed by `(TargetType{RMSE|Logloss}, CPU, useBestModel, boostFromAverage)`; formula `custIter = exp(C*ln(iterCount)+D)`, `defIter = exp(C*ln(1000)+D)`, `defLR = exp(A*ln(learnObjectCount)+B)`, `lr = round(min(defLR*custIter/defIter, 0.5), 6)`; active ONLY iff `learning_rate`/`leaf_estimation_method`/`leaf_estimation_iterations`/`l2_leaf_reg` all NotSet.
- **`cb-train::boosting`** — pre-train: if `learning_rate` unset (and the other gating params unset), call `autolr::guess(...)` to fill it before the loop.
- **New oracle scenario:** `autolr` (generator extended) — trains WITHOUT setting `learning_rate` (and the gating params) and persists `model.get_all_params()['learning_rate']` per target.
- **Unit test:** `autolr_test` asserting the guessed rate matches the persisted upstream value at ≤1e-5; plus an end-to-end auto-LR train→predict run.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generate the autolr oracle scenario (RMSE + Logloss)</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - catboost-master/catboost/libs/train_lib/options_helper.cpp (`TAutoLRParamsGuesser` table + formula — lines 116-262; activation gating; example coeffs)
    - crates/cb-oracle/generator/gen_fixtures.py (extend with the autolr scenario)
    - 03-RESEARCH.md § Auto Learning-Rate (Code Examples — formula + example coeffs)
  </read_first>
  <action>
    Extend `gen_fixtures.py` with an `autolr` scenario: train an RMSE and a Logloss model WITHOUT setting `learning_rate` (and without setting `leaf_estimation_method`/`leaf_estimation_iterations`/`l2_leaf_reg`, so auto-LR activates), with a fixed `iterations` and a dataset whose object count is recorded. Persist `model.get_all_params()['learning_rate']` and the relevant keying inputs (target type, `use_best_model`, `boost_from_average`, `learn_object_count`, `iterations`) into the scenario config/`.npy` for assertion. Commit. Python-reachable oracle only (D-11).
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/autolr/config.json || test -f crates/cb-oracle/fixtures/autolr/rmse/config.json</automated>
  </verify>
  <acceptance_criteria>
    - the `autolr` scenario records upstream `get_all_params()['learning_rate']` for RMSE and Logloss (committed)
    - the scenario does NOT set `learning_rate`/`leaf_estimation_method`/`leaf_estimation_iterations`/`l2_leaf_reg` (auto-LR active — grep config)
    - keying inputs (target, useBestModel, boostFromAverage, learn_object_count, iterations) are persisted
  </acceptance_criteria>
  <done>An autolr oracle scenario records upstream's auto-selected learning rate for RMSE + Logloss with all keying inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TAutoLRParamsGuesser + wire pre-train; oracle-lock; end-to-end run</name>
  <files>crates/cb-train/src/lib.rs, crates/cb-train/src/autolr.rs, crates/cb-train/src/autolr_test.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - catboost-master/catboost/libs/train_lib/options_helper.cpp (`TAutoLRParamsGuesser` table + formula + activation gating — lines 116-262)
    - crates/cb-data/src/weights.rs (pure scalar-formula utility analog — `#[must_use]` fns + `const` table style)
    - crates/cb-train/src/boosting.rs (pre-train param resolution point to wire the guess into)
    - 03-RESEARCH.md § Auto Learning-Rate (Code Examples — exact formula + example CPU coeffs: RMSE,bestModel=F,bfa=T -> {0.158,-4.287,-0.813,2.571}; Logloss,bestModel=F,bfa=F -> {0.427,-7.525,-0.917,2.63})
  </read_first>
  <behavior>
    - guess active ONLY iff learning_rate/leaf_estimation_method/leaf_estimation_iterations/l2_leaf_reg all unset
    - `lr = round(min(exp(A*ln(N)+B) * exp(C*ln(iter)+D)/exp(C*ln(1000)+D), 0.5), 6)` with table coeffs keyed by (target, useBestModel, bfa)
    - guessed rate matches upstream `get_all_params()['learning_rate']` to <=1e-5
  </behavior>
  <action>
    Create `autolr.rs` mirroring `weights.rs`' `#[must_use]` pure-fn + `const` table style: a `const` coefficient table `{A,B,C,D}` keyed by `(TargetType, CPU, useBestModel, boostFromAverage)` and a `guess(target, use_best_model, boost_from_average, learn_object_count, iter_count) -> f64` implementing `custIter=exp(C*ln(iter)+D)`, `defIter=exp(C*ln(1000)+D)`, `defLR=exp(A*ln(N)+B)`, `lr=round(min(defLR*custIter/defIter, 0.5), 6)`. In `boosting.rs`, before the loop, if `learning_rate` is unset AND the other gating params are unset, call `autolr::guess(...)` to fill `learning_rate`. No `unwrap`/panic. Write `autolr_test.rs` FIRST asserting the guessed rate against the persisted upstream values from Task 1 (and against the two RESEARCH example coeff rows) at ≤1e-5. Add an end-to-end check: train with auto-LR (no explicit learning_rate) and predict, confirming the cycle runs (Phase-3 success criterion 5).
  </action>
  <verify>
    <automated>cargo test -p cb-train autolr::</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-train/src/autolr.rs` contains a `const` coeff table keyed by (target, useBestModel, boostFromAverage) and the exp/log/round formula
    - `cargo test -p cb-train autolr::` exits 0 — guessed rate matches upstream `get_all_params()['learning_rate']` at ≤1e-5 for RMSE + Logloss
    - the guess fires ONLY when the four gating params are unset (source assertion)
    - an end-to-end auto-LR train→predict cycle runs (test asserts a non-empty model is produced)
    - `cargo test --workspace` exits 0 (phase gate)
  </acceptance_criteria>
  <done>Automatic learning-rate selection matches upstream, and a first end-to-end CPU train→predict cycle runs with the auto-selected rate (TRAIN-08; Phase-3 success criterion 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset size / params → scalar LR formula | Pure host scalar computation; no external surface (ASVS web categories N/A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Denial of Service | `ln(learn_object_count)` / `ln(iter_count)` on zero/negative count | mitigate | Guard `count > 0` before `ln` (Phase-3 already caps degenerate inputs); return `CbError` on a zero-object pool, never `ln(0)` → -inf/panic. |
| T-03-07-02 | Tampering (parity integrity) | wrong coefficient row selected | mitigate | Table keyed exactly by (target, useBestModel, boostFromAverage); unit test pins the two RESEARCH example rows + upstream-derived values. |
</threat_model>

<verification>
- `cargo test -p cb-train autolr::` green (formula matches upstream LR ≤1e-5).
- End-to-end auto-LR train→predict runs.
- `cargo test --workspace` green (phase gate); D-08 grep green.
</verification>

<success_criteria>
- Automatic learning-rate selection from dataset size matches upstream, and a first end-to-end CPU train→predict cycle runs (TRAIN-08; Phase-3 success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-07-SUMMARY.md` when done
</output>
