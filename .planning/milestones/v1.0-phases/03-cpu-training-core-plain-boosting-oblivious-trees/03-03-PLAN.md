---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 03
type: execute
wave: 4
depends_on: ["03-02"]
files_modified:
  - crates/cb-train/src/lib.rs
  - crates/cb-train/src/bootstrap.rs
  - crates/cb-train/src/bootstrap_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/bootstrap_oracle_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
autonomous: true
requirements: [TRAIN-04]
user_setup: []

must_haves:
  truths:
    - "Bootstrap/sampling supports Poisson, Bayesian, Bernoulli, MVS, and No, seeded by TFastRng64"
    - "subsample and object/group sampling units behave per upstream"
    - "Sampling draw order reproduces upstream's per-1000-element-block reseed (randSeed + blockIdx, Advance(10))"
    - "A model trained with each bootstrap type matches upstream splits + leaf values end-to-end to <=1e-5"
  artifacts:
    - path: "crates/cb-train/src/bootstrap.rs"
      provides: "Poisson/Bayesian/Bernoulli/MVS/No sample-weight generation over TFastRng64"
      contains: "TFastRng64"
    - path: "crates/cb-train/tests/bootstrap_oracle_test.rs"
      provides: "End-to-end oracle lock per bootstrap type"
  key_links:
    - from: "crates/cb-train/src/bootstrap.rs"
      to: "cb_core::TFastRng64"
      via: "per-block reseed from_seed(rand_seed + block_idx).advance(10)"
      pattern: "TFastRng64"
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/bootstrap.rs"
      via: "per-iteration Bootstrap() call producing sample weights"
      pattern: "bootstrap"
---

<objective>
Add the bootstrap/sampling subsystem (TRAIN-04): Poisson, Bayesian, Bernoulli, MVS, and No sample-weight generation seeded by the Phase-1 `TFastRng64` port, reproducing upstream's exact per-block reseed draw order; wire it into the per-iteration boosting loop and oracle-lock each bootstrap type end-to-end.

Purpose: The first slice deliberately fixed `bootstrap_type=No` (D-07) to isolate the tree/leaf math. This is the first additive-widening slice (D-10), turning the sampler on. The draw ORDER is the parity contract (Pitfall 4); MVS internal weights are not Python-observable (D-11), so MVS is validated end-to-end via splits/leaves and is the highest sampling-parity risk.
Output: `cb-train::bootstrap` with all five types, per-block reseed reproduced, and a passing `bootstrap_oracle` test per type.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-01-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-train::bootstrap`** — `Bootstrap()` dispatch over `EBootstrapType::{Poisson, Bayesian, Bernoulli, MVS, No}`; per-1000-element-block reseed `TFastRng64::from_seed(rand_seed + block_idx)` + `.advance(10)`; Bayesian weight `powf(-FastLogf(GenRandReal1()+1e-100), bagging_temperature)`; Bernoulli `Control[i] = GenRandReal1() < subsample`; Poisson draws; MVS weights; `subsample` param; object/group sampling-unit handling.
- **`cb-train::boosting`** per-iteration `Bootstrap()` call producing sample weights applied to gradients before tree growth.
- **New oracle scenarios:** `bootstrap/{poisson,bayesian,bernoulli,mvs,no}` (Plan-00 generator extended), each with the matching `bootstrap_type` + `subsample` pinned, other params at first-slice values.
- **Oracle test:** `bootstrap_oracle_test` gating `Stage::Splits` + `Stage::LeafValues` (and `StagedApprox`) per bootstrap type at ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generate per-bootstrap-type oracle scenarios</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - catboost-master/catboost/private/libs/algo/tensor_search_helpers.cpp (`Bootstrap`, `GenerateRandomWeights` per-block reseed — lines ~322-560)
    - catboost-master/catboost/private/libs/options/catboost_options.cpp (bootstrap defaults — MVS default, subsample=0.8, lines ~779-800)
    - crates/cb-oracle/generator/gen_fixtures.py (extend with five bootstrap scenarios)
    - 03-RESEARCH.md § Pitfall 4 (MVS is default, per-block reseed), Assumption A1
  </read_first>
  <action>
    Extend `gen_fixtures.py` to emit five scenarios `bootstrap/{poisson,bayesian,bernoulli,mvs,no}`, each `model.json` (+ `staged.npy`) with `bootstrap_type` set explicitly to the matching value, `subsample` pinned (e.g. 0.8 where applicable), a fixed `bagging_temperature` for the Bayesian scenario, and all other params at the first-slice simplified values (fixed `l2_leaf_reg`/`depth`/`lr`/`iterations`, `random_strength=0`, explicit `boost_from_average`, `leaf_estimation_iterations=1`, `score_function=L2`, fixed `random_seed`, `thread_count=1`). Use a dataset large enough to exercise at least two reseed blocks (>1000 objects) so the per-block reseed contract is actually tested. Commit fixtures. Python-reachable oracle only (D-11).
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/bootstrap/no/model.json && test -f crates/cb-oracle/fixtures/bootstrap/bayesian/model.json && test -f crates/cb-oracle/fixtures/bootstrap/bernoulli/model.json && test -f crates/cb-oracle/fixtures/bootstrap/poisson/model.json && test -f crates/cb-oracle/fixtures/bootstrap/mvs/model.json</automated>
  </verify>
  <acceptance_criteria>
    - all five `bootstrap/<type>/model.json` fixtures exist (committed)
    - each scenario pins its `bootstrap_type` + `subsample` and the first-slice simplified params (grep)
    - the bootstrap dataset has >1000 objects (exercises ≥2 reseed blocks)
  </acceptance_criteria>
  <done>Five committed bootstrap oracle scenarios pin each sampling type with a multi-block dataset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bootstrap/sampling over TFastRng64 with exact draw order; oracle-lock per type</name>
  <files>crates/cb-train/src/lib.rs, crates/cb-train/src/bootstrap.rs, crates/cb-train/src/bootstrap_test.rs, crates/cb-train/src/boosting.rs, crates/cb-train/tests/bootstrap_oracle_test.rs</files>
  <read_first>
    - crates/cb-core/src/rng.rs (`TFastRng64` API — `from_seed`/`advance`/`gen_rand`/uniform surface; the draw stream is parity-critical)
    - catboost-master/catboost/private/libs/algo/tensor_search_helpers.cpp (`Bootstrap`, `GenerateRandomWeights` — per-block `TRestorableFastRng64(randSeed+blockIdx); Advance(10)`, block size 1000; Bayesian weight; Bernoulli SetSampledControl)
    - catboost-master/catboost/private/libs/algo/mvs.cpp (MVS weight computation)
    - crates/cb-train/src/boosting.rs (the per-iteration loop to wire Bootstrap into)
    - crates/cb-train/tests/slice_first_oracle_test.rs (oracle-test shape to mirror)
    - 03-RESEARCH.md § Pitfall 4, § Don't Hand-Roll (use `cb_core::TFastRng64`, never `rand`)
  </read_first>
  <behavior>
    - No: every sample weight == 1.0, zero RNG draws
    - Bayesian: per block reseed `from_seed(rand_seed + block_idx).advance(10)`; weight == `(-ln(GenRandReal1()+1e-100))^bagging_temperature`
    - Bernoulli: `Control[i] = GenRandReal1() < subsample` in object order
    - bootstrap_test asserts the draw sequence for a fixed seed matches a hand-computed reference for ≥2 blocks
  </behavior>
  <action>
    Create `bootstrap.rs` with `Bootstrap()` dispatch over `EBootstrapType::{Poisson, Bayesian, Bernoulli, MVS, No}`, consuming `cb_core::TFastRng64` ONLY (never `rand`; Don't-Hand-Roll). Reproduce the per-1000-element-block reseed EXACTLY: `TFastRng64::from_seed(rand_seed + block_idx)` then `.advance(10)` per block (the block structure is part of the contract at `thread_count=1`, Pitfall 4). Implement Bayesian weight (`powf(-ln(GenRandReal1()+1e-100), bagging_temperature)` via a `GenRandReal1` helper on the RNG), Bernoulli `Control[i] = GenRandReal1() < subsample`, Poisson draws, MVS weights (transcribe `mvs.cpp`), and `subsample` + object/group sampling-unit handling. Any weight sums route through `cb_core::sum_f64`; no `unwrap`/panic. Wire `Bootstrap()` into `boosting.rs` per iteration, applying sample weights to gradients before tree growth (No → all 1.0, the first-slice path unchanged). Write `bootstrap_test.rs` FIRST: assert the Bayesian/Bernoulli draw sequences for a fixed seed across ≥2 blocks against a hand-computed reference. Add `bootstrap_oracle_test.rs` gating `Stage::Splits` + `Stage::LeafValues` per type at ≤1e-5 (MVS validated end-to-end only — internal weights not Python-observable, D-11).
  </action>
  <verify>
    <automated>cargo test -p cb-train bootstrap</automated>
    <automated>cargo test -p cb-train bootstrap_oracle</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-train/src/bootstrap.rs` dispatches all five `EBootstrapType` variants and uses `cb_core::TFastRng64` (no `rand` crate import)
    - the per-block reseed uses `rand_seed + block_idx` and `advance(10)` with block size 1000 (source assertion)
    - `cargo test -p cb-train bootstrap` exits 0 (draw-sequence unit test across ≥2 blocks)
    - `cargo test -p cb-train bootstrap_oracle` exits 0 — Splits + LeafValues match upstream per bootstrap type at ≤1e-5
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>All five bootstrap types reproduce upstream draws (exact per-block reseed order) and produce models matching upstream end-to-end to ≤1e-5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `TFastRng64` draw stream → sample weights | Host-only deterministic RNG; `TFastRng64` is explicitly NON-cryptographic — used only for parity-reproducing sampling, never for secrets (RESEARCH Security Domain V6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure (parity integrity) | mis-seeded sampler leaking unintended draw order | mitigate | Reproduce the exact `rand_seed + block_idx` + `Advance(10)` per-block reseed (Pitfall 4); draw-sequence unit test across ≥2 blocks. |
| T-03-03-02 | Tampering | `TFastRng64` wrapping-arithmetic overflow | mitigate | `TFastRng64` uses `wrapping_*` (verified Phase 1); no panic path. |
| T-03-03-03 | Misuse | RNG used for anything but sampling | mitigate | `rng.rs` documents non-crypto status; bootstrap is sampling-only; no secrets introduced. |
</threat_model>

<verification>
- `cargo test -p cb-train bootstrap` green (draw sequence across ≥2 blocks).
- `cargo test -p cb-train bootstrap_oracle` green (Splits/LeafValues ≤1e-5 per type).
- `cargo test --workspace` green.
- D-08 grep green; no `rand` crate import.
</verification>

<success_criteria>
- Bootstrap/sampling (Poisson, Bayesian, Bernoulli, MVS, No; `subsample`; object/group units) is seeded by the Phase-1 `TFastRng64` and reproduces upstream draws (TRAIN-04).
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-03-SUMMARY.md` when done
</output>
