---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/cb-backend/Cargo.toml
  - crates/cb-backend/src/lib.rs
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/kernels_test.rs
  - crates/cb-oracle/src/lib.rs
  - crates/cb-oracle/src/model_json.rs
  - crates/cb-oracle/src/model_json_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - .planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md
requirements: []   # Wave-0 PREP plan — produces fixtures/parser/kernel-spike + Nyquist sign-off only. The TRAIN-01/02/03 oracle that GATES those IDs lives in Plan 01 (slice_first_oracle). No requirement is satisfied by Wave-0 prep alone.
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A minimal #[cube] gradient kernel compiles and runs on CubeCL CpuRuntime under the workspace deny-lints"
    - "cb-oracle can parse a committed model.json into per-tree splits (float_feature_index, border) and leaf_values as Vec<f64>"
    - "The fixture generator emits regression_skeleton (RMSE) and binclf_skeleton (Logloss) training oracles with simplified isolating params"
    - "03-VALIDATION.md records nyquist_compliant: true and wave_0_complete: true once all Wave-0 assets are built and green"
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "First #[cube] gradient kernel (generics-float), order-independent elementwise"
      contains: "#[cube"
    - path: "crates/cb-oracle/src/model_json.rs"
      provides: "model.json Deserialize structs + split/leaf extractors"
      exports: ["load_model_json", "ModelJson", "ObliviousTree"]
    - path: "crates/cb-oracle/generator/gen_fixtures.py"
      provides: "Per-slice training oracle generator (save_model json + staged_predict)"
    - path: "Cargo.toml"
      provides: "cubecl 0.10.0 + bytemuck workspace deps"
      contains: "cubecl"
    - path: ".planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md"
      provides: "Wave-0 Nyquist sign-off (nyquist_compliant: true, wave_0_complete: true)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "crates/cb-backend/Cargo.toml"
      to: "cubecl 0.10.0 (features=[cpu]) + bytemuck"
      via: "dependency .workspace = true"
      pattern: "cubecl"
    - from: "crates/cb-oracle/src/model_json.rs"
      to: "fixtures/regression_skeleton/model.json"
      via: "serde_json::from_str"
      pattern: "serde_json"
---

<objective>
Stand up the Phase-3 foundation: install the CubeCL CPU compute dependency, prove the `#[cube]` seam with a minimal gradient kernel running on `CpuRuntime`, add a `model.json` parser to `cb-oracle`, and extend the fixture generator to emit the per-slice training oracles (RMSE regression + Logloss binary-clf skeletons) all later slices lock against. Finally, record the Wave-0 Nyquist sign-off in `03-VALIDATION.md`.

Purpose: De-risk the CubeCL seam (Open Q2) and create every test asset Wave-1+ slices depend on BEFORE any algorithm is written. This is the Nyquist Wave-0 PREP plan — it produces the fixtures, the parser, and the compiling kernel that the first oracle slice (Plan 01) consumes, and flips the Nyquist gate in `03-VALIDATION.md`. It satisfies NO TRAIN requirement on its own — TRAIN-01/02/03 are gated by Plan 01's `slice_first_oracle`.
Output: `cubecl`/`bytemuck` wired into `cb-backend` only (D-03); a compiling+running `#[cube]` gradient kernel; `cb-oracle::model_json`; committed `regression_skeleton`/`binclf_skeleton` training-oracle fixtures with simplified isolating params (D-07/A1/A2); `03-VALIDATION.md` with `nyquist_compliant: true` + `wave_0_complete: true`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md
@AGENTS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 0) creates:
- **Workspace deps:** `cubecl = { version = "0.10.0", features = ["cpu"] }`, `bytemuck = { version = "1", features = ["extern_crate_std"] }` in root `[workspace.dependencies]`; wired into `cb-backend` ONLY (D-03).
- **`cb-backend::kernels`** — first `#[cube]` kernel `gradient_kernel<F: Float>` (generics-float; RMSE der1 = `target − approx`), launchable on `CpuRuntime`.
- **`cb-oracle::model_json`** — `pub fn load_model_json(&Path) -> Result<ModelJson, OracleError>`; structs `ModelJson`, `ObliviousTree { leaf_values: Vec<f64>, splits: Vec<SplitJson> }`, `SplitJson { border, float_feature_index, split_index, split_type }`; extractor fns yielding `Vec<f64>` for `compare_stage(Stage::Splits|LeafValues, …)`.
- **New oracle fixture scenarios:** `regression_skeleton` (RMSE) extended + new `binclf_skeleton` (Logloss), each with `model.json` + per-iteration `staged.npy`, generated via pinned `catboost==1.2.10` `save_model(format='json')` + `staged_predict`.
- **Pinned oracle params (A1/A2/A4):** `bootstrap_type=No`, `random_strength=0`, fixed `l2_leaf_reg`/`depth`/`learning_rate`/`iterations`, explicit `boost_from_average` (true RMSE / false Logloss), `leaf_estimation_iterations=1`, `score_function=L2`, `leaf_estimation_method=Gradient`, pinned seed, `thread_count=1`.
- **Wave-0 Nyquist sign-off:** `03-VALIDATION.md` frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, with the Validation Sign-Off checkboxes checked.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install CubeCL + bytemuck workspace deps and prove the #[cube] CpuRuntime seam</name>
  <files>Cargo.toml, crates/cb-backend/Cargo.toml, crates/cb-backend/src/lib.rs, crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels_test.rs</files>
  <read_first>
    - AGENTS.md (CubeCL kernel rules — kernels use generics-float; read the manual before kernel code; on ANY build error load the error guideline FIRST)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (MANDATORY before writing kernel code)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Cubecl_generics.md (CpuRuntime usage, `features=["cpu"]`, bytemuck `Bytes::from_elems` transfer pattern)
    - crates/cb-backend/src/lib.rs (current `SelectedRuntime` cfg arms — extend the cpu arm in place)
    - crates/cb-backend/Cargo.toml (manifest comment "NO cubecl dependency yet" — must be updated)
    - crates/cb-data/Cargo.toml (Shared Pattern A — `[lints] workspace = true`, anyhow ban comment, `.workspace = true` dep shape)
    - root Cargo.toml (`[workspace.dependencies]` block to extend)
  </read_first>
  <action>
    Add to root `[workspace.dependencies]`: `cubecl = { version = "0.10.0", features = ["cpu"] }` and `bytemuck = { version = "1", features = ["extern_crate_std"] }` (versions confirmed via `cargo search`: cubecl 0.10.0, bytemuck 1.25.0 — `version = "1"` is the floor). Wire BOTH into `crates/cb-backend/Cargo.toml` via `.workspace = true` ONLY (D-03 — never into `cb-compute`); update the manifest's "NO cubecl dependency yet" comment. In `cb-backend/src/lib.rs`, keep the `wgpu`/`cuda`/`rocm` arms `= ()`; under `#[cfg(feature = "cpu")]` stand up the CubeCL CPU runtime now (D-01) — replace `SelectedRuntime = ()` with the CubeCL CPU runtime alias (`cubecl::cpu::CpuRuntime` per the manual — confirm the exact path against `Cubecl_generics.md`). Add `mod kernels;` + gated `#[cfg(test)] mod kernels_test;`. In `kernels.rs` write a minimal `#[cube(launch)] fn gradient_kernel<F: Float>(approx: &Array<F>, target: &Array<F>, der1: &mut Array<F>)` computing `der1[ABSOLUTE_POS] = target[ABSOLUTE_POS] - approx[ABSOLUTE_POS]` guarded by `if ABSOLUTE_POS < approx.len()` (RMSE der1, generics-float per AGENTS.md). This kernel does ONLY order-independent elementwise work (D-02/D-06) — it MUST NOT reduce/sum. In `kernels_test.rs` launch the kernel on `CpuRuntime`, transfer data via bytemuck, and assert per-element output equals a host-computed `target[i]-approx[i]` reference. On ANY CubeCL build error, load `cubecl_error_guideline.md` before attempting a fix (AGENTS.md hard rule); do not blind-fix. This task resolves RESEARCH Open Q2.
  </action>
  <verify>
    <automated>cargo test -p cb-backend kernels::gradient</automated>
    <automated>cargo build -p cb-backend --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-backend/src/kernels.rs` contains a `#[cube` annotated `gradient_kernel` generic over `F: Float` (grep `#[cube` matches; no hard-coded `f32`/`f64` in the kernel signature)
    - `cargo build -p cb-backend --features cpu` exits 0 under the workspace deny-lints
    - `cargo test -p cb-backend kernels::gradient` exits 0 (kernel runs on CpuRuntime, per-element output matches host reference)
    - root `Cargo.toml` `[workspace.dependencies]` contains `cubecl` and `bytemuck`; `crates/cb-compute/Cargo.toml` does NOT reference `cubecl` (D-03)
  </acceptance_criteria>
  <done>The CubeCL CPU seam is proven: a generics-float `#[cube]` gradient kernel compiles and runs on `CpuRuntime` under deny-lints, with `cubecl`/`bytemuck` wired into `cb-backend` only.</done>
</task>

<task type="auto">
  <name>Task 2: Add model.json parser to cb-oracle</name>
  <files>crates/cb-oracle/src/lib.rs, crates/cb-oracle/src/model_json.rs, crates/cb-oracle/src/model_json_test.rs</files>
  <read_first>
    - crates/cb-oracle/src/fixture.rs (analog — serde `Deserialize` structs + fallible `load_config` loader returning `Result<_, OracleError>`)
    - crates/cb-oracle/src/lib.rs (module-tree + `pub use` block + `#[cfg(test)] mod` ordering to extend)
    - crates/cb-oracle/src/compare.rs (`Stage::{Splits, LeafValues}` + `compare_stage` — the consumer the extractors feed)
    - crates/cb-oracle/fixtures/regression_skeleton/model.json (verified schema: top-level `oblivious_trees`, `scale_and_bias`, `features_info`; each tree has `leaf_values`, `leaf_weights`, `splits[{border, float_feature_index, split_index, split_type}]`)
  </read_first>
  <action>
    Create `model_json.rs` mirroring `fixture.rs`' serde + fallible-loader shape. Define `#[derive(Debug, Clone, PartialEq, Deserialize)]` structs: `ModelJson { oblivious_trees: Vec<ObliviousTree>, scale_and_bias: ... }`, `ObliviousTree { leaf_values: Vec<f64>, splits: Vec<SplitJson> }`, `SplitJson { border: f64, float_feature_index: i64, split_index: i64, split_type: String }` (field names matching the verified schema exactly). Add `pub fn load_model_json(path: &Path) -> Result<ModelJson, OracleError>` reusing `fixture.rs`' `OracleError::Io`/`OracleError::Json` variants (read_to_string + serde_json::from_str). Add extractor fns returning `Vec<f64>` suitable for `compare_stage(Stage::Splits, …)` (borders flattened in tree order) and `compare_stage(Stage::LeafValues, …)` (leaf_values flattened in tree order), plus a `bias()` accessor reading `scale_and_bias[1][0]`. Register `mod model_json;` + `#[cfg(test)] mod model_json_test;` and add the new `pub use` exports to `lib.rs`. In `model_json_test.rs` load the committed `regression_skeleton/model.json` and assert non-empty trees, correct split/leaf counts (`leaf_values.len() == 2^depth` per tree), and that `bias()` reads the `scale_and_bias=[1,[bias]]` value. No `unwrap` in production paths — return `OracleError`.
  </action>
  <verify>
    <automated>cargo test -p cb-oracle model_json</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-oracle/src/model_json.rs` exports `load_model_json`, `ModelJson`, `ObliviousTree`, `SplitJson`
    - `cargo test -p cb-oracle model_json` exits 0 (parses committed `regression_skeleton/model.json`; split/leaf extractors return `Vec<f64>` of the asserted lengths)
    - production code in `model_json.rs` contains no `unwrap()`/`expect()` (fallible paths return `OracleError`)
  </acceptance_criteria>
  <done>`cb-oracle::model_json` parses upstream `model.json` and yields per-tree splits + leaf_values + bias as `Vec<f64>` ready for `compare_stage`.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the fixture generator to emit RMSE + Logloss training oracles</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - crates/cb-oracle/generator/gen_fixtures.py (current generator — extend, preserve the run-once-commit discipline)
    - crates/cb-oracle/src/bin/write_skeleton.rs (one-off generator analog — run-once/commit/never-in-CI discipline + file-scope lint allow)
    - crates/cb-oracle/fixtures/regression_skeleton/ (existing fixture layout: config.json + model.json + .npy)
    - crates/cb-oracle/src/fixture.rs (`FixtureConfig` schema — pinned seed, thread_count=1)
    - 03-RESEARCH.md § Pitfalls 2/4/5/6, Assumptions A1/A2/A4/A5 (param pinning rationale: boost_from_average, MVS-is-default override, leaf_estimation_iterations, raw-logit staged predict), § Open Questions (RESOLVED) Q1 (score_function=L2)
  </read_first>
  <action>
    Extend `gen_fixtures.py` to emit, via the pinned `catboost==1.2.10` venv at `crates/cb-oracle/generator/.venv`: (1) the existing `regression_skeleton` RMSE scenario plus a NEW `binclf_skeleton` Logloss scenario mirroring it. For each scenario write `model.json` (`model.save_model(path, format='json')`) and per-iteration `staged.npy` (stack `model.staged_predict(X)` raw-logit outputs — `prediction_type='RawFormulaVal'` for Logloss per A5/Pitfall 6). Pin params explicitly so the slice and oracle agree (A1/A2): `bootstrap_type='No'`, `random_strength=0`, `l2_leaf_reg=3.0`, `depth` fixed (e.g. 2 — small leaf count), `learning_rate` fixed, `iterations` fixed (small), `boost_from_average=True` for RMSE / `False` for Logloss (Pitfall 2), `leaf_estimation_iterations=1` (A4/Pitfall 5), `score_function='L2'` (Open Q1 RESOLVED to L2 — simplest first-slice math), `leaf_estimation_method='Gradient'`, fixed `random_seed`, `thread_count=1`. Commit the generated fixtures. Document the run-once-commit discipline in a header comment (the generator does NOT run in CI). Do NOT add a C++ build step (D-11 — Python-reachable oracle only).
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/regression_skeleton/model.json && test -f crates/cb-oracle/fixtures/binclf_skeleton/model.json && test -f crates/cb-oracle/fixtures/binclf_skeleton/staged.npy</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-oracle/fixtures/binclf_skeleton/model.json` and `staged.npy` exist (committed)
    - `crates/cb-oracle/fixtures/regression_skeleton/staged.npy` exists (committed)
    - both scenario configs pin `bootstrap_type=No`, `random_strength=0`, explicit `boost_from_average`, `leaf_estimation_iterations=1`, `score_function=L2`, `thread_count=1` (grep the generator/config for these literals)
    - `gen_fixtures.py` carries a run-once-commit header comment and adds no C++ build step
  </acceptance_criteria>
  <done>Frozen RMSE + Logloss training-oracle fixtures (model.json + staged.npy) exist with simplified isolating params, ready for the Wave-1 first-slice oracle.</done>
</task>

<task type="auto">
  <name>Task 4: Record the Wave-0 Nyquist sign-off in 03-VALIDATION.md</name>
  <files>.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md (frontmatter `nyquist_compliant: false`/`wave_0_complete: false`; the Validation Sign-Off checkbox list; the Wave 0 Requirements list)
  </read_first>
  <action>
    This task runs LAST, only after Tasks 1–3 verify green. Edit `03-VALIDATION.md`: (1) set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`; (2) check every box in the `## Validation Sign-Off` section (`[x]`), since all Wave-0 MISSING references are now satisfied — the cubecl/bytemuck install + `#[cube]` spike (Task 1), the `model.json` parser (Task 2), and the `regression_skeleton`/`binclf_skeleton` training oracle fixtures (Task 3); (3) check the boxes in the `## Wave 0 Requirements` list that this plan completed (the generator extension for `regression_skeleton`/`binclf_skeleton`, the `model.json` parser, the `cb-backend` build spike, and the framework install) — leave later-slice scenario rows (leaf_methods/bootstrap/regularization/overfit/eval_metrics/autolr) unchecked as they belong to their own slices; (4) set `**Approval:** signed-off (Wave 0 complete)`. Make NO other content change. Do NOT mark `nyquist_compliant: true` unless Tasks 1–3 are green — this is the gate's whole point.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `wave_0_complete: true` (grep both)
    - the `## Validation Sign-Off` boxes are all `[x]`; `**Approval:**` is no longer `pending`
    - the Wave-0-owned rows in `## Wave 0 Requirements` are checked; later-slice scenarios remain unchecked
  </acceptance_criteria>
  <done>The Nyquist Wave-0 gate is recorded: `03-VALIDATION.md` shows `nyquist_compliant: true` and `wave_0_complete: true` with the sign-off boxes checked, only after the Wave-0 assets are built and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture .npy/.json → cb-oracle parser | Trusted, frozen, committed artifacts; the only "input" is project-controlled fixture data. No network/auth/external-input surface (RESEARCH Security Domain: ASVS web categories N/A). |
| host ↔ CubeCL CpuRuntime buffer | `bytemuck::Pod` data transfer in/out of kernel buffers; the kernel does order-independent elementwise work only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Denial of Service | `model_json::load_model_json`, kernel buffer indexing | mitigate | Deny-lints (`unwrap_used`/`indexing_slicing`) + `OracleError` returns on malformed JSON; kernel guards `ABSOLUTE_POS < len`. |
| T-03-00-02 | Tampering | `#[cube]` gradient kernel | mitigate | Kernel does NO float reduction (D-02/D-05) — only `target[i]-approx[i]` per element; all parity-critical sums are host-side via `cb-core::sum_f64` in later slices. CI-grep raw-float-sum ban (D-08) stays live. |
| T-03-00-SC | Tampering | `cubecl`/`bytemuck` installs | mitigate | Both packages OK per RESEARCH § Package Legitimacy Audit (cubecl: Tracel-AI/Burn, roadmap-pinned, `cargo search` confirms 0.10.0; bytemuck: ubiquitous Pod crate). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no blocking human checkpoint required. |
</threat_model>

<verification>
- `cargo build -p cb-backend --features cpu` exits 0 under deny-lints.
- `cargo test -p cb-backend kernels::gradient` exits 0.
- `cargo test -p cb-oracle model_json` exits 0.
- `regression_skeleton` + `binclf_skeleton` fixtures (model.json + staged.npy) committed.
- `crates/cb-compute/Cargo.toml` has no `cubecl` reference (D-03 guard).
- CI raw-float-sum grep gate (D-08) remains green.
- `03-VALIDATION.md` records `nyquist_compliant: true` + `wave_0_complete: true` (Task 4).
</verification>

<success_criteria>
- CubeCL CPU seam proven (Open Q2 closed): generics-float `#[cube]` gradient kernel compiles + runs on `CpuRuntime`.
- `cb-oracle::model_json` parses upstream model.json into splits/leaf_values/bias.
- Frozen RMSE + Logloss training-oracle fixtures exist with pinned simplified params (D-07/A1/A2/A4/A5).
- All Wave-1 test assets are in place AND the Nyquist Wave-0 gate is signed off in `03-VALIDATION.md`.
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-00-SUMMARY.md` when done
</output>
