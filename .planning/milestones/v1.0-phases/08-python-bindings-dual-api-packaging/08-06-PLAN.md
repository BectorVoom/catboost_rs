---
phase: 08-python-bindings-dual-api-packaging
plan: 06
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - crates/catboost-rs-py/src/lib.rs
  - crates/catboost-rs-py/tests/test_free_threaded.py
  - crates/catboost-rs-py/FREE_THREADING.md
autonomous: false
requirements: [PYAPI-06]
tags: [pyo3, free-threading, gil-safety]

must_haves:
  truths:
    - "The module declares gil_used=false (free-threaded-aware) and loads on a free-threaded interpreter"
    - "Concurrent fit/predict from multiple Python threads on the same data produces no corruption (own-before-detach holds under real free threading) — validated on python3.13t as a phase gate before /gsd-verify-work; if 3.13t is unavailable in-env, recorded as a scoped deferral with PYAPI-06 remaining code-property-validated"
    - "The custom-loss callback GIL-reentry caveat is documented (it re-enters Python during compute, incompatible with a blanket gil_used=false claim for that path)"
  artifacts:
    - path: "crates/catboost-rs-py/src/lib.rs"
      provides: "#[pymodule(gil_used=false)] flag"
      contains: "gil_used"
    - path: "crates/catboost-rs-py/tests/test_free_threaded.py"
      provides: "multi-thread buffer-safety test (run under python3.13t)"
      contains: "thread"
    - path: "crates/catboost-rs-py/FREE_THREADING.md"
      provides: "the abi3↔free-threaded deferral rationale + custom-loss reentry caveat (A2/A6)"
      contains: "free-threaded"
  key_links:
    - from: "crates/catboost-rs-py/src/lib.rs"
      to: "PyO3 free-threading"
      via: "gil_used=false module flag"
      pattern: "gil_used"
---

<objective>
Satisfy PYAPI-06 as the research-confirmed CODE property (own-before-detach + `gil_used=false`), validated on a free-threaded interpreter, with the free-threaded WHEEL explicitly deferred (CONTEXT Deferred Ideas; abi3 ⊥ free-threaded in PyO3 0.29). Set the `#[pymodule(gil_used=false)]` flag, add a multi-thread buffer-safety test exercising concurrent fit/predict, and document the abi3-vs-free-threaded deferral plus the custom-loss callback GIL-reentry caveat (A6).

This is the safety-hardening slice for PYAPI-06: the own-before-detach discipline established in 08-03 is now proven under real free threading.

Purpose: PYAPI-06 free-threaded-aware design as a validated code property.
Output: the gil_used flag, `test_free_threaded.py`, and `FREE_THREADING.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-python-bindings-dual-api-packaging/08-RESEARCH.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-03-SUMMARY.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gil_used=false flag + multi-thread buffer-safety test</name>
  <files>crates/catboost-rs-py/src/lib.rs, crates/catboost-rs-py/tests/test_free_threaded.py</files>
  <read_first>
    - crates/catboost-rs-py/src/lib.rs (the `#[pymodule]` from 08-01 — add the `gil_used=false` flag)
    - RESEARCH Free-Threading: lines 58 (PYAPI-06 satisfiable as a code property), 273-288 (own-before-detach, `py.detach`), 349 (`#[pymodule(gil_used=false)]`), Pitfall 3 lines 358-362
    - 08-03-SUMMARY (the own-before-detach call sites this test targets)
    - RESEARCH Environment lines 503, 539 (`python3.13t` may need to be built/installed separately; the test is gated on its availability)
  </read_first>
  <behavior>
    - Test (test_free_threaded.py): spawn N (≥8) Python threads each running fit+predict on its own copy AND on a shared read-only input; assert all results are finite, equal across threads for equal inputs, and the run completes without crash/corruption.
    - Test: the test SKIPs (not fails) with a clear message when running on a non-free-threaded interpreter (`sys._is_gil_enabled()` True), so the cpu/abi3 CI run does not false-pass nor false-fail — it is meaningful only under `python3.13t` (mirrors the Phase-7.5 cpu-skip-guard lesson).
    - Test: under a free-threaded interpreter, the module imports (proving gil_used=false load).
  </behavior>
  <action>Change the `#[pymodule]` in `lib.rs` to `#[pymodule(gil_used = false)]` (PyO3 0.29). In `test_free_threaded.py`, write a `test_concurrent_fit_predict` that uses `threading.Thread` (≥8 workers) over the `toy_regression`/`toy_classification` fixtures: each worker fits a fresh estimator and predicts; a second variant shares one immutable input array across workers calling predict on a single fitted model concurrently; assert finite + cross-thread equality. Guard the whole module with `pytest.mark.skipif(sys._is_gil_enabled(), reason="free-threaded interpreter (python3.13t) required for PYAPI-06 buffer-safety validation")` — SKIP, never panic, on a GIL build (so the standard cpu CI run is a clean skip, not a false pass). Document in the test how to run it: install/build a `python3.13t`, `maturin develop` against it, `python3.13t -m pytest tests/test_free_threaded.py`.</action>
  <verify>
    <automated>cd crates/catboost-rs-py && ../../.venv-py8/bin/maturin develop --features cpu 2>&1 | tail -2 && ../../.venv-py8/bin/pytest tests/test_free_threaded.py -q 2>&1 | tail -8</automated>
    <human-check>PHASE GATE (required before `/gsd-verify-work`): On a free-threaded `python3.13t` interpreter, `maturin develop --features cpu` then `python3.13t -m pytest tests/test_free_threaded.py` — the concurrent fit/predict test runs (not skipped) and passes with no corruption. (Manual because the standard test venv is a GIL build; 3.13t must be built/installed in-env.) If `python3.13t` is UNAVAILABLE in-env, record it in 08-06-SUMMARY as a SCOPED DEFERRAL: the concurrent free-threaded run is deferred, PYAPI-06 remains CODE-PROPERTY-VALIDATED (own-before-detach call sites from 08-03 + `gil_used=false` + the GIL-build skip-guard test). Do not block the phase on 3.13t availability; do block on the skip-guard test passing on the GIL build.</human-check>
  </verify>
  <done>`gil_used=false` is set; the multi-thread test runs (under 3.13t) or skips cleanly (under a GIL build) and asserts no corruption; the module loads on a free-threaded interpreter.</done>
</task>

<task type="auto">
  <name>Task 2: Document the abi3↔free-threaded deferral + custom-loss reentry caveat</name>
  <files>crates/catboost-rs-py/FREE_THREADING.md</files>
  <read_first>
    - RESEARCH lines 58, 346-350 (Pitfall 1: abi3 ignored on free-threaded builds; ship abi3 cpu wheel, defer the free-threaded wheel), 445-446 (LOSS-07 custom-loss callback re-enters the GIL during compute — incompatible with a blanket gil_used=false for that path, A6), 472 (A6)
    - 08-CONTEXT Deferred Ideas (the free-threaded wheel is deferred)
  </read_first>
  <behavior>
    - Doc check (grep gate): `FREE_THREADING.md` states (a) PYAPI-06 is satisfied as a code property (own-before-detach + gil_used=false), (b) the free-threaded WHEEL is deferred because abi3-py312 and free-threaded are mutually exclusive in PyO3 0.29, (c) the custom-loss/custom-metric Python callback re-enters the GIL during compute (per-call `Python::attach`) and is a documented exception to the fully-detached gil_used=false claim.
  </behavior>
  <action>Write `crates/catboost-rs-py/FREE_THREADING.md` covering: the abi3 ⊥ free-threaded landmine (RESEARCH Pitfall 1) and why the shipped artifact is the abi3-py312 cpu wheel with the free-threaded wheel deferred (CONTEXT Deferred); how PYAPI-06 is satisfied as a code property (own-before-detach at every ingest call site, `gil_used=false`); how to validate it (`python3.13t -m pytest tests/test_free_threaded.py`); and the custom-loss callback caveat — when a Python `custom_loss`/`custom_metric`/`eval_metric` callable is supplied, compute re-enters Python via `Python::attach` per der1/der2/eval, so for THAT path the gil_used=false guarantee is documented as serialized re-entry, not a contradiction (A6). Use grep-checkable tokens: "own-before-detach", "gil_used", "free-threaded", "custom_loss".</action>
  <verify>
    <automated>grep -v '^#' crates/catboost-rs-py/FREE_THREADING.md | grep -c -e "own-before-detach" -e "gil_used" -e "free-threaded" -e "custom_loss"</automated>
  </verify>
  <done>FREE_THREADING.md documents the deferral rationale, the code-property satisfaction of PYAPI-06, the validation command, and the custom-loss GIL-reentry caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multiple Python threads → one estimator/buffer | Concurrent access to shared input/model under free threading |
| Python callback ← Rust compute (custom-loss) | Rust re-enters Python during the detached compute loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Tampering / DoS | use-after-free of a borrowed buffer under real free threading | mitigate | own-before-detach (08-03) PROVEN by `test_free_threaded.py` concurrent fit/predict; `gil_used=false` declares the contract |
| T-08-19 | DoS | data race on a shared fitted model during concurrent predict | mitigate | Facade `Model` is `Send+Sync` (CLAUDE.md architecture); predict takes `&self` over owned/quantized data; test asserts cross-thread equality |
| T-08-20 | Tampering | custom-loss callback racing the GIL under free threading | accept | Documented caveat (A6): callback path re-enters via `Python::attach` (serialized); not a fixture-reachable default path |
</threat_model>

<verification>
- `pytest tests/test_free_threaded.py -q` skips cleanly on the GIL venv.
- PHASE GATE before `/gsd-verify-work`: (in-env) `python3.13t -m pytest tests/test_free_threaded.py` runs (not skipped) and passes — OR, if `python3.13t` is unavailable in-env, the SUMMARY records a scoped deferral and PYAPI-06 stands as a code property (own-before-detach + `gil_used=false` + GIL-build skip guard).
- `grep` gate on FREE_THREADING.md passes.
</verification>

<success_criteria>
- `gil_used=false` set; module loads free-threaded.
- Concurrent fit/predict shows no corruption (validated under 3.13t).
- The abi3-deferral and custom-loss caveat are documented.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings-dual-api-packaging/08-06-SUMMARY.md` when done. Record whether a python3.13t was available in-env and the concurrent-test result.
</output>
