# Phase 7.4: Pairwise Histogram Family - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-20
**Phase:** 7.4-Pairwise Histogram Family
**Mode:** `--auto` (no interactive prompts; every gray area auto-resolved to the recommended default)
**Areas discussed:** Variant coverage & sequencing, Kernel-family decomposition, Pairwise input/output seam, Atomic accumulation, CPU self-oracle baseline, Scan/update deferral, Verification & boundary

---

## Variant coverage & in-phase sequencing (SC-1)

| Option | Description | Selected |
|--------|-------------|----------|
| Full family, narrowest-first across plans | All variants land this phase; sequence 5/6/7-bit → 8-bit-atomics → half-byte → binary → one-hot overlay | ✓ |
| Full family, one mega-plan | Port everything in a single plan | |
| Scope-cut to one-byte only | Defer half-byte/binary/one-hot | |

**Auto-selected:** Full family, narrowest-first across plans (recommended default) → **D-7.4-01**
**Notes:** Mirrors the 7.1/7.2/7.3 incremental-porting precedent. Sequencing is plan decomposition, not a scope cut — SC-1 requires every variant on `rocm`.

---

## Kernel-family decomposition + bit-width / one-hot specialization

| Option | Description | Selected |
|--------|-------------|----------|
| Distinct families mirroring upstream; comptime bits + one-hot overlay | 5/6/7-bit share a comptime-bits template; 8-bit-atomics a distinct global-atomic family; half-byte + binary distinct; one-hot a comptime overlay | ✓ |
| Single mega-kernel with runtime branches | One kernel, runtime width/one-hot switches | |
| Monomorphize every (width × one-hot) combination by hand | Separate hand-written kernel per variant | |

**Auto-selected:** Distinct families + comptime overlay (recommended default) → **D-7.4-02**
**Notes:** Structural-parity bar (D-01). 8-bit-atomics is distinct because the line exceeds shared memory upstream — must mirror the global-atomic structure. Exact CubeCL comptime-vs-monomorphization expression is the research-flag item.

---

## Pairwise input/output seam

| Option | Description | Selected |
|--------|-------------|----------|
| Device-resident pairs + per-pair weight + cindex + partition in → histogram handle out | Distinct from the 7.2/7.3 pointwise der seam; mirrors upstream pairwise signature | ✓ |
| Reuse the 7.2 pointwise der1/der2 handle seam | Treat pairwise like pointwise | |
| Host-build pairs and upload per launch | No device-resident pairs seam | |

**Auto-selected:** Device-resident pairs + pair-weight seam (recommended default) → **D-7.4-03**
**Notes:** Pairwise accumulates over object pairs (`uint2* pairs`), not single objects — fundamentally different input from 7.3. Exact `pairs` encoding (uint2-like vs two u32 arrays) left to Claude's discretion.

---

## Atomic accumulation (D-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Shared-mem block accumulate + atomic merge (sub-8-bit); true global atomics (8-bit/one-hot); f64 finalize | Mirror upstream; reuse 7.1 atomic-finalize; D-03/D-09 honored | ✓ |
| Pure global atomics for all widths | Skip shared-mem blocking | |
| Host-ordered sum (D-02 scatter) | No on-device accumulation | |

**Auto-selected:** Shared-mem + atomic merge, global atomics for 8-bit/one-hot (recommended default) → **D-7.4-04**
**Notes:** D-03/D-09 locked by umbrella — restated, not reopened.

---

## CPU self-oracle baseline & fixtures (SC-2)

| Option | Description | Selected |
|--------|-------------|----------|
| Host pairwise histogram via ordered `sum_f64`; reference `pairwise_scoring.rs`; pairwise/ranking-loss fixture; REPORT divergence | Clone the 7.3 oracle harness into `kernels/pairwise_hist.rs` | ✓ |
| Hard-code the GPU-06 epsilon now | Sign off tolerance in 7.4 | |
| Oracle against upstream CUDA output only | No CPU baseline | |

**Auto-selected:** Host reference + CPU pairwise path, REPORT divergence (recommended default) → **D-7.4-05**
**Notes:** SC-2 requires matching the Rust CPU pairwise path including a pairwise/ranking loss. GPU-06 epsilon sign-off is 7.6's job. CPU pairwise path already exists (`cb-compute/src/pairwise_scoring.rs`).

---

## Scan/update deferral + verification & boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Defer scan/update + pairwise scorer to 7.5; per-crate verification; cb-backend-only | Deliver FILL + handle only; carry 7.3 gating/boundary | ✓ |
| Include scan/update + scorer in 7.4 | Build the full pairwise path now | |

**Auto-selected:** Defer to 7.5; carry 7.3 gating + boundary (recommended default) → **D-7.4-06 / D-7.4-07 / D-7.4-08**
**Notes:** Mirrors 7.3 deferring `Scan/Update PointwiseHistograms`. Per-crate verification (rocm run, wgpu run, cuda build-only); all coupling in `cb-backend`.

---

## Claude's Discretion

- Exact CubeCL comptime-vs-monomorphization expression of bit-width + one-hot specialization (research-flag item).
- Shared-memory pairwise-histogram line-size / blocking geometry per bit-width.
- Device encoding of the `pairs` buffer (`uint2`-like vs two parallel `u32` arrays).
- `parallelStreamCount` and `fullPass`/partition handling depth for the single-tree fill.
- Histogram-output device-buffer handle type and the precise representative fixture set.

## Deferred Ideas

- Post-fill scan/update + pairwise split scorer (`Update/BuildBinaryFeature/Scan PairwiseHistograms`, `split_pairwise.cuh`) into the device-resident grow loop — Phase 7.5.
- Empirical GPU-06 epsilon sign-off + `rocm` oracle-gate lock-in — Phase 7.6.

## Reviewed Todos (not folded)

- **"Estimated-feature stored-border-VALUE quantization-grid parity"** (score 0.4) — reviewed, NOT folded. CPU KNN/online-HNSW subsystem residual from Phase 6.5/6.6; unrelated to GPU pairwise histograms. Weak keyword-only match; folding would be cross-boundary scope creep.
