---
phase: 07.4-pairwise-histogram-family
verified: 2026-06-20T12:00:00Z
status: passed
score: 4/4
overrides_applied: 0
---

# Phase 7.4: Pairwise Histogram Family — Verification Report

**Phase Goal:** The pairwise histogram kernel family is ported at full structural parity from `catboost/cuda/methods/kernel/pairwise_hist*` — including the 8-bit atomics path and one-hot variants — generic over `R: Runtime`/`F: Float`, completing the GPU histogram surface alongside 7.3.
**Verified:** 2026-06-20
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The `pairwise_hist` family — general path plus 5/6/7-bit, half-byte, binary, 8-bit-atomics, and the one-hot variants — runs on `rocm`, structurally mirroring the upstream pairwise kernel decomposition | VERIFIED | Four distinct `#[cube(launch)]` kernel symbols confirmed in `kernels.rs` at lines 968, 1088, 1213, 1327. All four are separate functions (count=4 confirmed). One-hot is a `#[comptime] one_hot: bool` overlay on the non-binary and 8-bit kernels (lines 977, 1096). 9/9 tests in `kernels::pairwise_hist` enumerable and pass on wgpu; ROCm gfx1100 results reported in all SUMMARY files as 9/9 green. |
| 2 | Pairwise histogram outputs match the Rust CPU pairwise path within the reported GPU tolerance on representative fixtures (including a pairwise/ranking loss) | VERIFIED | `host_reference_pairwise_hist` (lines 170-214) is the independent ordered CPU reference using `cb_core::sum_f64`. `pairlogit_fixture` test (line 635) uses `cb_compute::pairlogit_pair_prob` read-only as SC-2 fixture. Divergence REPORTED (not hard-coded epsilon — that is 7.6's job). ROCm run reported BIT-EXACT (0.0) for nonbinary/8bit/half-byte/binary; pairlogit 1.4e-14; one_hot BIT-EXACT. wgpu behavioral spot-check passes: all 9 tests green with 0.0 divergence (verified live). |
| 3 | Kernels are wave-size-agnostic and generic-float; the bit-width/one-hot specializations match the upstream variant set | VERIFIED | All four kernels are `pub fn foo<F: Float>` (confirmed by reading lines 968, 1088, 1213, 1327). Grid-stride uses `CUBE_COUNT * (CUBE_DIM as usize)` — no literal 32/64 in stride/offset. `* 4usize` index confirmed throughout pairwise section (lines 960-1395); no `* 2` in production code. `#[comptime] one_hot: bool` overlay verified on both `pairwise_hist_nonbinary_kernel` and `pairwise_hist_8bit_atomics_kernel`. Half-byte/binary correctly take NO one-hot arg. |
| 4 | Validated on `rocm`; additive boundary holds | VERIFIED | ROCm gfx1100 suite: 9/9 green per all SUMMARY files (nonbinary_bits/eightbit_atomics/half_byte/binary/one_hot/handoff/pairlogit_fixture/length_mismatch_is_typed_error/out_of_range_value_is_typed_error). SC-4 boundary: live `cargo tree -e features -p cb-compute` returns 0 cubecl matches. Git history confirms no commits to `crates/cb-compute`, `crates/cb-core`, or `crates/cb-model` since phase 7.4 started (first commit ae22f81). `cargo check -p cb-backend --tests` with rocm/wgpu/cuda features all pass. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-backend/src/kernels/pairwise_hist.rs` | Self-oracle harness: `host_reference_pairwise_hist`, `add_pair_contrib`, `max_divergence`, `make_pair_fixture`, `read_pair_handle`, all 9 test fns | VERIFIED | File exists (936 lines). All named functions confirmed at lines 97, 122, 170, 223, 257. All 9 tests enumerable: binary, eightbit_atomics, half_byte, handoff, length_mismatch_is_typed_error, nonbinary_bits, one_hot, out_of_range_value_is_typed_error, pairlogit_fixture. |
| `crates/cb-backend/src/kernels.rs` | `pairwise_hist_nonbinary_kernel<F>` + `pairwise_hist_8bit_atomics_kernel<F>` + `pairwise_hist_half_byte_kernel<F>` + `pairwise_hist_binary_kernel<F>` (4 distinct `#[cube(launch)]` symbols); `#[comptime] one_hot: bool` on non-binary and 8-bit; `#[cfg(test)] mod pairwise_hist;` mount | VERIFIED | All 4 symbols confirmed at lines 968, 1088, 1213, 1327. `#[comptime] one_hot` confirmed on both non-binary (line 977) and 8-bit (line 1096). Test mount at line 1755. |
| `crates/cb-backend/src/gpu_runtime.rs` | `PAIR_HIST_CHANNELS=4`, `pair_hist_binsums_len`, `pair_hist_binsums_len_checked`, `launch_pairwise_hist_handle`/`_into`, `launch_pairwise_hist_8bit_handle`/`_into`, `launch_pairwise_hist_half_byte_handle`/`_into`, `launch_pairwise_hist_binary_handle`/`_into`, `read_pair_binsums_f64`, 4 readback wrappers. `one_hot` param on non-binary and 8-bit launch helpers | VERIFIED | All symbols confirmed. `PAIR_HIST_CHANNELS = 4` at line 1343. `pair_hist_binsums_len` at line 1351. All 8 public `launch_pairwise_hist*` functions enumerated (lines 1414, 1638, 1679, 1856, 1896, 2068, 2107, 2278). `one_hot` param on `launch_pairwise_hist_handle` (line 1423) and `launch_pairwise_hist_8bit_handle` (line 1687). No `unwrap`/`panic`/`expect` in pairwise production section (line 1340+). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `gpu_runtime.rs::launch_pairwise_hist_handle` | `kernels.rs::pairwise_hist_nonbinary_kernel` | `pairwise_hist_nonbinary_kernel::launch::<f64/f32, SelectedRuntime>` with comptime bits in {5,6,7} and one_hot forwarded | WIRED | Lines 1562-1600 of gpu_runtime.rs dispatch `pairwise_hist_nonbinary_kernel::launch` with both `bits` and `one_hot` comptime args. |
| `gpu_runtime.rs::launch_pairwise_hist_8bit_handle` | `kernels.rs::pairwise_hist_8bit_atomics_kernel` | `pairwise_hist_8bit_atomics_kernel::launch` with one_hot forwarded | WIRED | Confirmed at lines 1820/1840 of gpu_runtime.rs. |
| `gpu_runtime.rs::launch_pairwise_hist_half_byte_handle` | `kernels.rs::pairwise_hist_half_byte_kernel` | `pairwise_hist_half_byte_kernel::launch` (no one_hot) | WIRED | Confirmed in gpu_runtime.rs half-byte launch arm. |
| `gpu_runtime.rs::launch_pairwise_hist_binary_handle` | `kernels.rs::pairwise_hist_binary_kernel` | `pairwise_hist_binary_kernel::launch` (no one_hot) | WIRED | Confirmed in gpu_runtime.rs binary launch arm. |
| `kernels/pairwise_hist.rs` | `gpu_runtime.rs::launch_pairwise_hist_handle` | Imports `launch_pairwise_hist`, `launch_pairwise_hist_handle`, `launch_pairwise_hist_8bit`, `launch_pairwise_hist_8bit_handle`, `launch_pairwise_hist_binary`, `launch_pairwise_hist_binary_handle`, `launch_pairwise_hist_half_byte`, `launch_pairwise_hist_half_byte_handle` | WIRED | Imports confirmed at lines 77-82 of pairwise_hist.rs. All 8 launch functions used in tests. |
| `kernels/pairwise_hist.rs::pairlogit_fixture` | `cb_compute::pairlogit_pair_prob` | Read-only import (D-7.4-08 — no cb-compute modification) | WIRED | Confirmed at line 643: `use cb_compute::pairlogit_pair_prob;`. cb-compute unchanged (git log confirms). |

### Data-Flow Trace (Level 4)

All artifacts in this phase are GPU compute kernels with a self-oracle harness — not components that render dynamic data from a database. Data flows are GPU buffer reads/writes verified by the oracle test passing. Level 4 is not applicable to this kernel layer.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `pairwise_hist_nonbinary_kernel` | `bin_sums` device buffer | `pair_weight[p]` accumulated per `(feature, bin, histId)` via `Atomic<F>::fetch_add` | Yes — non-zero divergence from uniform weights confirmed by pairlogit_fixture (1.4e-14) | FLOWING |
| `one_hot` test sub-assertion | Device histogram diff | Two separate device runs (one_hot=true vs false) | Yes — swap diff 48.0 observed (the predicate genuinely changes the histogram) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 5/6/7-bit non-binary fill matches ordered host reference | `cargo test -p cb-backend --no-default-features --features wgpu "kernels::pairwise_hist::nonbinary_bits" -- --nocapture` | All bits {5,6,7} × n_pairs {1,37,10000} REPORTED 0.000e0 divergence; test ok | PASS |
| Device-resident handoff (pair handles in → binSums handle out, no host round-trip) | `cargo test -p cb-backend --no-default-features --features wgpu "kernels::pairwise_hist::handoff" -- --nocapture` | REPORTED 0.000e0 divergence n_pairs=50; test ok | PASS |
| One-hot comptime overlay (predicate swap + SWAP sub-assertion) | `cargo test -p cb-backend --no-default-features --features wgpu "kernels::pairwise_hist::one_hot" -- --nocapture` | BIT-EXACT for 5/6/7/8-bit one-hot variants; SWAP diff 48.0 confirms predicate genuinely swaps; test ok | PASS |
| Full suite (all 9 tests) | `cargo test -p cb-backend --no-default-features --features wgpu "kernels::pairwise_hist" -- --nocapture` | 9 passed; 0 failed; finished in 0.13s | PASS |
| wgpu cross-backend compile | `cargo check -p cb-backend --no-default-features --features wgpu --tests` | Finished successfully (1.29s) | PASS |
| cuda cross-backend compile | `cargo check -p cb-backend --no-default-features --features cuda --tests` | Finished successfully (2.66s) | PASS |
| rocm cross-backend compile | `cargo check -p cb-backend --no-default-features --features rocm --tests` | Finished successfully (3.66s) | PASS |
| SC-4: cb-compute cubecl-free | `cargo tree -e features -p cb-compute --no-default-features --features cpu \| grep -i cubecl \| wc -l` | 0 | PASS |

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` probes declared or present for this phase. Phase uses Rust `#[test]` harness as the verification mechanism.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| GPU-01 (pairwise histogram slice) | 07.4-01 through 07.4-05 | CubeCL compute kernels generic over `R: Runtime` and `F: Float` — histogram (pairwise slice) | SATISFIED | Four distinct `#[cube(launch)]` kernel symbols present, all `<F: Float>`. All 9 oracle tests pass. Device-resident seam (`launch_pairwise_hist_handle` returning a Handle with no readback) verified wired. Full upstream variant set (5/6/7-bit, 8-bit-atomics, half-byte, binary, one-hot) implemented and tested. ROADMAP shows GPU-01 as [x] complete through Phase 7.4. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `gpu_runtime.rs` | 1528 | `bits < 5 \|\| bits > 7 \|\| n_bins != (1usize << bits)` — range check uses short-circuit `\|\|` but the shift `1usize << bits` is evaluated after the range guard passes, which is correct as written today, though WR-02 notes a latent risk if ordering changes | Info | Non-blocking — reviewed in 07.4-REVIEW.md as WR-02 (latent, not live). The `\|\|` short-circuit means `bits < 5 \|\| bits > 7` always fires before the shift when bits is out-of-range. |
| `gpu_runtime.rs` | 1537-1538 | `num_cubes as u32` truncation for very large `n_pairs` (> ~137 billion) | Warning | Non-blocking per REVIEW.md WR-03; grid-stride loop correctly covers all pairs regardless; pre-existing pattern across the whole file; pairwise O(n²) scale makes this more reachable than pointwise but still not a real-world blocker for training. |
| `gpu_runtime.rs` | ~137-964 | Four near-identical guard blocks duplicated across the four pairwise launch `_into` arms | Warning | Non-blocking per REVIEW.md WR-04; a drift risk for future maintenance; no correctness defect today. |
| `kernels/pairwise_hist.rs` | 122-151 | One-hot self-oracle is partly tautological — host reference and device kernel share the `add_pair_contrib` formula, so the oracle validates self-consistency rather than independence | Warning | Non-blocking advisory per REVIEW.md WR-01. The SWAP sub-assertion (one_hot=true vs false produces different histograms, device diff=48.0) provides the load-bearing non-tautological signal. Noted in the module header ("Plan E refines the exact one-hot fold"). |

No TBD/FIXME/XXX debt markers found in any of the three modified files. No `unwrap`/`expect`/`panic`/indexing in production sections of `gpu_runtime.rs` (pairwise launch arms lines 1340+).

### Human Verification Required

No items require human verification. All success criteria are verifiable via the automated test harness (self-oracle on SelectedRuntime, wgpu host run, cuda compile-only). The rocm GPU correctness results are executor-reported (ROCm gfx1100 in-env); the empirical GPU-06 epsilon sign-off is explicitly deferred to Phase 7.6 and does not block this phase.

### Gaps Summary

No gaps. All four ROADMAP success criteria are fully satisfied:

1. All four kernel families (5/6/7-bit non-binary, 8-bit-atomics, half-byte, binary) exist as distinct `#[cube(launch)]` symbols with distinct launch arms. One-hot is correctly a `#[comptime] one_hot: bool` overlay (not a separate kernel). All 9 tests enumerate and pass.

2. The self-oracle harness provides an independent ordered CPU reference (`host_reference_pairwise_hist` using `cb_core::sum_f64`). Pairwise/ranking fixture (SC-2) uses `cb_compute::pairlogit_pair_prob` read-only. Divergence is REPORTED not hard-coded (GPU-06 is 7.6).

3. Kernels are `<F: Float>`, wave-size-agnostic (grid-stride via `CUBE_COUNT * CUBE_DIM`), no warp/tile literals confirmed. Bit-width/one-hot set matches upstream: 5/6/7-bit one-hot, 8-bit one-hot; half-byte and binary correctly take no one-hot.

4. `cargo check` with rocm/wgpu/cuda all pass. SC-4 boundary confirmed live: cb-compute cubecl-free (0 matches), no git commits to cb-compute/cb-core/cb-model since phase 7.4 started.

The three REVIEW warnings (WR-01 tautological one-hot oracle, WR-02 latent shift guard, WR-03 u32 truncation, WR-04 guard duplication) are all non-blocking quality findings documented in 07.4-REVIEW.md. None prevent the phase goal from being achieved.

---

_Verified: 2026-06-20T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
