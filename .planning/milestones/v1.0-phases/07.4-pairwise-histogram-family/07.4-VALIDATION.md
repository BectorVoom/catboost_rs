---
phase: 7.4
slug: pairwise-histogram-family
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-20
---

# Phase 7.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` (rocm-gated self-oracle), source/test separated into `crates/cb-backend/src/kernels/pairwise_hist.rs` (clone of `kernels/pointwise_hist.rs`) |
| **Config file** | none — `cargo test -p cb-backend` with the `rocm` feature; mounted via `#[cfg(test)] mod pairwise_hist;` in `kernels.rs` |
| **Quick run command** | `cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist -- --nocapture` |
| **Full suite command** | `cargo test -p cb-backend --no-default-features --features rocm` (per-crate; never full-workspace — disk pressure, D-7.4-07) |
| **Estimated runtime** | ~60 seconds (per-crate, in-env gfx1100) |

---

## Sampling Rate

- **After every task commit:** Run the touched family's quick run, e.g. `cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::nonbinary_bits -- --nocapture`
- **After every plan wave:** Run `cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist` (all pairwise families) + `--features wgpu` build+run on host (f32 channel) + `--features cuda` compile-only
- **Before `/gsd-verify-work`:** Full `cb-backend` rocm suite green + the SC-4 `cargo tree` cubecl-free gate + `git diff --stat` showing `cb-compute`/`cb-core`/`cb-model` byte-unchanged
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.4-A | A | 1 | GPU-01 / SC-1 | — | N/A (in-process GPU kernel; no external input) | unit (rocm self-oracle) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::nonbinary_bits -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-A | A | 1 | GPU-01 / SC-1 | — | N/A | unit (device-residency) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::handoff -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-A | A | 1 | SC-2 | — | N/A | unit (rocm self-oracle) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::pairlogit_fixture -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-B | B | 2 | GPU-01 | — | N/A | unit (rocm self-oracle) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::eightbit_atomics -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-C | C | 3 | GPU-01 | — | N/A | unit (rocm self-oracle) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::half_byte -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-D | D | 4 | GPU-01 | — | N/A | unit (rocm self-oracle) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::binary -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-E | E | 5 | GPU-01 / SC-3 | — | N/A | unit (rocm self-oracle) | `cargo test -p cb-backend --features rocm kernels::pairwise_hist::one_hot -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.4-* | * | * | SC-3 / SC-4 | — | wave-agnostic, generic-float, cb-compute cubecl-free | grep + `cargo tree` gate | `cargo tree -e features -p cb-compute \| grep -ci cubecl` == 0; grep kernels for warp/tile literals | partial | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/cb-backend/src/kernels/pairwise_hist.rs` — the self-oracle harness (clone `pointwise_hist.rs`: `max_divergence`, a fresh `host_reference_pairwise_hist` per-pair → **4-channel** via `cb_core::sum_f64`, edge cases empty / n_pairs=1 / n_pairs=37 / large-N, REPORT divergence, the PairLogitPairwise/YetiRankPairwise fixture from `ranking_der`). Covers GPU-01 / SC-2.
- [ ] `#[cfg(test)] mod pairwise_hist;` mount in `kernels.rs` (the `pointwise_hist`/`gradient_gpu` mount precedent).
- [ ] No new framework install — `#[test]` + `rocm` feature already in use.

*Existing infrastructure (the shipped `pointwise_hist.rs`/`gradient_gpu.rs` harness + the `cargo tree` SC-4 gate) covers the PATTERN; the pairwise-specific oracle file is the only new test artifact.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `cuda` arm correctness | GPU-01 | No NVIDIA hardware in-env (D-07) | `cargo build -p cb-backend --no-default-features --features cuda` — compile-only; correctness deferred to a CUDA host |
| Run-to-run atomic variance budget | GPU-01 / D-03 | Atomic accumulation order is non-deterministic; tolerance is REPORTED not signed off (sign-off is Phase 7.6) | Re-run the rocm self-oracle N times; observe max abs/rel divergence is stable and small |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
