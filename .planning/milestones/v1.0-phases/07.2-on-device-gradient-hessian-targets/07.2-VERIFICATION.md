---
phase: 07.2-on-device-gradient-hessian-targets
verified: 2026-06-20T17:00:00Z
status: passed
score: 4/4
overrides_applied: 0
human_verification_resolved:
  - test: "Run the full der oracle suite on rocm gfx1100: `cargo test -p cb-backend --no-default-features --features rocm kernels::gradient_gpu -- --nocapture`"
    expected: "23/23 tests pass; each test PRINTS `REPORTED max abs_div=... rel_div=...` per fixture; Focal tests report rel_div <= ~1e-8; all other families report 0.0 or <=1e-14; no test panics or is #[ignore]."
    result: "CONFIRMED LIVE by orchestrator in-env on gfx1100 (ROCm 7.1.1): `test result: ok. 23 passed; 0 failed; 0 ignored` in 1.39s, EXIT=0. Reported divergences: RMSE/Quantile/MAE/CrossEntropy 0.0; Logloss der1/der2 ≤2.22e-16 (abs) / ≤9.88e-15 (rel); Focal der1/der2 abs ≤5.55e-16, rel ≤2.31e-12; saturated-logit test all-finite. No #[ignore], no panic. Per-crate command honored the disk-pressure constraint (no workspace test)."
---

# Phase 7.2: On-Device Gradient/Hessian Targets — Verification Report

**Phase Goal:** Gradient and Hessian (der1/der2) computation and the target/derivative path run device-resident on the GPU runtimes, ported at structural parity from `catboost/cuda/targets/`, feeding the histogram kernels without host round-trips.
**Verified:** 2026-06-20T17:00:00Z
**Status:** passed
**Re-verification:** No — initial verification. SC-2 human-verification item resolved live by the orchestrator running the sanctioned per-crate GPU oracle on gfx1100 (23/23 passed, EXIT=0).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Generic-over-`R: Runtime`/`F: Float` der1/der2 kernels for in-scope losses (RMSE, Logloss/CrossEntropy, Quantile/MAE, Focal) are device-resident, structurally mirroring `catboost/cuda/targets/` dispatch-by-loss shape | VERIFIED | `DerBinaryKernel`, `DerUnaryKernel`, `DerParamKernel` enums present in `gpu_runtime.rs` lines 311-596; launch helpers launch over `SelectedRuntime` generically; kernels in `kernels.rs` are `#[cube]` generic-float (no hard-coded float type); structural mirroring confirmed via elementwise dispatch-by-loss architecture matching `pointwise_targets.cu(h)` |
| 2 | Derivative results match the Rust CPU der1/der2 path on representative fixtures within reported GPU tolerance (divergence reported, not signed off — GPU-06 epsilon is 7.6's job) | VERIFIED (live GPU run) | Orchestrator ran the sanctioned per-crate command on the live gfx1100/ROCm 7.1.1 device: `test result: ok. 23 passed; 0 failed; 0 ignored` in 1.39s (EXIT=0). Reported divergences vs the `cb-compute::loss` baseline: RMSE/Quantile/MAE/CrossEntropy = 0.0; Logloss der1/der2 abs ≤2.22e-16, rel ≤9.88e-15; Focal der1/der2 abs ≤5.55e-16, rel ≤2.31e-12; saturated-logit test all values finite. Report-not-sign-off per D-7.2-06 (no GPU-06 epsilon hard-coded). |
| 3 | Outputs stay device-resident with no host fold on the hand-off path (der1/der2 returned as device HANDLES consumable by histogram kernels) | VERIFIED | `launch_der_binary_handle`, `launch_der_unary_handle`, `launch_der_param_handle`, `const_der_handle` — all confirmed to return `Handle` with NO `read_one` call inside them (code-read of `gpu_runtime.rs` lines 338-781); `launch_der_binary_into` short-circuit at n=0 returns `client.empty(0)`, never reads; `all_losses_device_resident_handoff` test (lines 834-934) asserts every family hands der1+der2 as `Ok(handle)` with read-back deferred to test end; SC-3 contract explicitly documented in code comments |
| 4 | `cb-core`/`cb-model`/`cb-compute` byte-unchanged; `cb-compute` is cubecl-free | VERIFIED | `git diff --name-only 394bfad..HEAD -- crates/cb-core crates/cb-model crates/cb-compute` returned EMPTY (no changes); `cargo tree -e features -p cb-compute \| grep -ci cubecl` returned 0; `cargo check -p cb-backend --no-default-features --features rocm --tests` returned RC=0 in 0.13s (already-cached) |

**Score:** 4/4 truths VERIFIED (SC-1/SC-3/SC-4 static; SC-2 confirmed by live in-env gfx1100 oracle run, 23/23 passed)

### Deferred Items

Items explicitly deferred per D-7.2-01 — not gaps.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Querywise/pairwise/multiclass GPU derivative kernels (query_rmse, query_softmax, pair_logit, pfound_f, yeti_rank_pointwise, dcg, multilogit) | Phase 7.4/7.5 | D-7.2-01 explicitly defers these to sub-phases that consume them; Phase 7.4 goal covers pairwise histogram family which consumes pairwise derivatives |
| 2 | Remaining pointwise regression der kernels on GPU (LogCosh/Lq/Huber/Expectile/Poisson/Tweedie/MAPE) | Phase 7.3–7.5 | D-7.2-01 defers these as additive follow-ons on the same seam when a downstream consumer needs them |
| 3 | On-device loss `functionValue` computation | Phase 7.5 | D-7.2-02 defers until device-side metrics consumer lands (7.5 on-device loop) |
| 4 | Empirical GPU-06 epsilon sign-off | Phase 7.6 | ROADMAP Phase 7.6 success criteria 1 and 2 explicitly own the epsilon measurement and user sign-off |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-backend/src/gpu_runtime.rs` | `launch_der_binary_handle` + `launch_der_binary` + `DerBinaryKernel`; `launch_der_unary_handle` + `launch_der_unary` + `DerUnaryKernel`; `launch_der_param_handle` + `launch_der_param` + `DerParamKernel`; `const_der_handle` | VERIFIED | All symbols present and substantive (781 lines); no read_one on handle-returning paths; param_pair non-panicking; no unwrap/panic/indexing in production code; commit 1c5cc90 + 7add20e + 4047854 + 2de0dbf confirmed |
| `crates/cb-backend/src/kernels/gradient_gpu.rs` | All-backend `#[cfg(test)]` self-oracle with `max_divergence`, RMSE/Logloss/CrossEntropy/Quantile/MAE/Focal oracle tests, `all_losses_device_resident_handoff`, `cb_compute_is_cubecl_free` | VERIFIED | File exists at 954 lines; 23 `#[test]` functions present; imports `launch_der_*_handle`, `launch_der_*`, `DerBinaryKernel`, `DerUnaryKernel`, `DerParamKernel`, `const_der_handle`; `max_divergence` copied from scan.rs; report-not-sign-off pattern confirmed in every test; Plan-01 min_lines=60 satisfied (954 lines); commits a8f0f7a + 7add20e + 4047854 + 2de0dbf + 4b817ae confirmed |
| `crates/cb-backend/src/kernels.rs` | `#[cfg(test)] mod gradient_gpu;` mount (all-backend, not cpu-gated) | VERIFIED | Line 877-878: `#[cfg(test)]` / `mod gradient_gpu;` — not behind `#[cfg(all(test, feature = "cpu"))]` (unlike the gradient spike mount at line 846-847); mirrors the reduce/scan mounts |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `kernels/gradient_gpu.rs` | `gpu_runtime.rs` | `launch_der_binary` / `launch_der_binary_handle` | VERIFIED | `gradient_gpu.rs` line 23-26 imports all launch fns; called throughout (lines 79, 246, 267, 295, 318, etc.) |
| `gpu_runtime.rs` | `kernels.rs` | `gradient_kernel::launch::<f64, SelectedRuntime>` | VERIFIED | `gpu_runtime.rs` line 400; all five kernels imported at lines 41-45 and dispatched inside `launch_der_binary_into` / `launch_der_unary_into` / `launch_der_param_into` |
| `kernels/gradient_gpu.rs` | `cb-compute::rmse_der1` / `logloss_der1` / `quantile_der1` / `focal_der1` | elementwise baseline comparison in `*_baseline` helpers | VERIFIED | `rmse_der1_baseline` (line 50-56), `logloss_der1_baseline` (line 219-225), `quantile_der1_baseline` (line 412-418), `focal_der1_baseline` (line 603-609) — all call the respective `cb_compute::*_der1` fns |
| `gpu_runtime.rs` | `kernels.rs` | `quantile_gradient_kernel::launch` | VERIFIED | `gpu_runtime.rs` line 682 inside `DerParamKernel::QuantileGradient` arm |
| `gpu_runtime.rs` | `kernels.rs` | `focal_gradient_kernel::launch` / `focal_hessian_kernel::launch` | VERIFIED | `gpu_runtime.rs` lines 701, 718 inside `FocalGradient` / `FocalHessian` arms |

### Data-Flow Trace (Level 4)

Level 4 not applicable: `gradient_gpu.rs` is entirely `#[cfg(test)]` code; there is no dynamic-data-rendering component. The seam functions in `gpu_runtime.rs` are pure compute helpers (not components rendering fetched data). The data-flow is: host `&[f64]` slice → `client.create(Bytes::from_elems(...))` device buffer → kernel launch over `SelectedRuntime` → output `Handle` returned without read-back. This is confirmed substantive (not hollow) by the lack of any `read_one` on the handle-returning paths.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| cb-backend compiles with rocm + tests | `cargo check -p cb-backend --no-default-features --features rocm --tests` | RC=0 in 0.13s (cached) | PASS |
| cb-compute has no cubecl dependency | `cargo tree -e features -p cb-compute \| grep -ci cubecl` | 0 | PASS |
| cb-core/cb-model/cb-compute byte-unchanged | `git diff --name-only 394bfad..HEAD -- crates/cb-core crates/cb-model crates/cb-compute` | empty (no output) | PASS |
| gradient_gpu.rs test enumeration | `grep -c '^#\[test\]' ...gradient_gpu.rs` | 23 | PASS |
| All 6 phase commits exist and are reachable | `git log --oneline \| grep feat(07.2` | 1c5cc90, a8f0f7a, 7add20e, 4047854, 2de0dbf, 4b817ae — all present | PASS |
| Full GPU oracle run on rocm gfx1100 (23/23) | `cargo test -p cb-backend --no-default-features --features rocm kernels::gradient_gpu -- --nocapture` | CANNOT RUN (live GPU required, disk-pressure constraint per D-7.2-07) | SKIP (human needed) |

### Probe Execution

No probe scripts declared or discovered for this phase (`find scripts -path '*/tests/probe-*.sh' 2>/dev/null` — not applicable). Verification command from PLAN is the sanctioned `cargo check -p cb-backend --no-default-features --features rocm --tests` (confirmed PASS above) and the rocm test run (routed to human).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| GPU-01 (gradient/hessian + targets slice) | 07.2-01, 07.2-02, 07.2-03 | CubeCL compute kernels generic over R:Runtime/F:Float — histogram, gradient/hessian, scan, reductions | PARTIALLY SATISFIED (this phase covers grad/hess slice; histogram + scan + full-loop deferred to 7.3–7.5 per ROADMAP traceability) | All four pointwise der families device-resident; seam confirmed; REQUIREMENTS.md traceability maps GPU-01 to "Phase 7.1–7.5" with "Pending (scan/reductions 7.1, grad/hess 7.2, pointwise hist 7.3, pairwise hist 7.4, on-device loop close 7.5)" |

**Note on GPU-01 scope:** The phase requirement ID is GPU-01 but the REQUIREMENTS.md traceability explicitly maps GPU-01 to phases 7.1–7.5 collectively. Phase 7.2's contribution is the grad/hess slice only. The 07.2 plans mark `requirements-completed: [GPU-01]` in their frontmatter, but this refers to the 7.2 sub-slice of GPU-01, not full closure of GPU-01 (which requires 7.3–7.5). This is consistent with the REQUIREMENTS.md notation "Pending (grad/hess 7.2 ... on-device loop close 7.5)". No gap — this is by design.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `kernels.rs` | 88-101 | Quantile kernel `\|val\| >= delta` boundary disagrees with `cb-compute::quantile_der1`'s `\|val\| < delta` deadzone (WR-01 from review) | Warning | Measure-zero divergence at exact boundary; oracle passes on random fixtures because `\|val\| == delta` is not hit; pre-existing latent issue documented in 07.2-REVIEW.md as WR-01; NOT a 7.2 regression (the kernel predates 7.2; 7.2 only wired the launch seam, not the kernel arithmetic) |
| `gpu_runtime.rs` | 114, 185, 269, 388, 511, 667 | Unchecked `num_cubes as u32` truncation (WR-02 from review) | Warning | Unreachable at training-set sizes; latent for n > ~1.37e11 elements; documented advisory in REVIEW.md |
| `gpu_runtime.rs` | 108, 183, 385, 508, 665 | Unchecked `n * size_of::<f64>()` on 32-bit (WR-03 from review) | Warning | 64-bit only target in practice; latent on hypothetical 32-bit; documented advisory in REVIEW.md |
| `kernels.rs` | 622-633, 709-720 | `block_reduce_kernel` fallback tree-reduce implicitly assumes `CUBE_DIM_X` is a power of two (WR-04 from review) | Warning | CUBE_DIM=32 (power of two) is enforced by the compile-time assert; safe today; latent if CUBE_DIM changes; documented advisory in REVIEW.md |

**Debt marker gate:** No `TBD`, `FIXME`, or `XXX` markers found in any of the three modified files (`gpu_runtime.rs`, `kernels.rs`, `gradient_gpu.rs`). Gate: PASS.

**WR-01 classification:** Described as "a pre-existing latent edge in the authored kernel, not a 7.2 regression" in the verification guidance. The kernel body exists since before 7.2; 7.2 only added the `launch_der_param_handle` wrapper around it. The divergence is measure-zero on the oracle fixtures and is formally documented in REVIEW.md. Not a BLOCKER for the 7.2 goal — the goal is the device-residency seam and handle hand-off, not kernel arithmetic parity (the arithmetic was authored in an earlier phase). However, WR-01 should be addressed before 7.3 uses this kernel path for training.

**WR-02/WR-03/WR-04:** All advisory, unreachable at current fixture sizes. REVIEW.md documents them. No BLOCKER.

### Human Verification Required

#### 1. Full GPU Der Oracle Suite on rocm gfx1100

**Test:** Run `cargo test -p cb-backend --no-default-features --features rocm kernels::gradient_gpu -- --nocapture` from the workspace root.

**Expected:**
- 23 tests pass (0 ignored, 0 failed)
- Each test prints a `[der ... REPORTED max abs_div=X rel_div=Y]` line
- RMSE, Logloss, CrossEntropy, Quantile, MAE fixtures: abs_div and rel_div ≈ 0.0 or ≤ 1e-14
- Focal fixtures: rel_div ≤ ~1e-8 (the acknowledged higher tolerance from transcribed `ln`/`powf` chains)
- `focal_der_saturated_logit_no_nan`: all der1/der2 values finite (no NaN/Inf on ±40 logits)
- `all_losses_device_resident_handoff`: each family block reports divergence and passes assertions
- `cb_compute_is_cubecl_free`: passes (focal baselines are finite)

**Why human:** Requires the live AMD gfx1100/ROCm 7.1.1 device (`/dev/kfd` exposed in-env). `cargo check` confirms clean compilation but cannot execute GPU kernels. The disk-pressure constraint (D-7.2-07) prohibits `cargo test --workspace`; only the per-crate test command above is sanctioned.

### Gaps Summary

No gaps. All must-haves are either VERIFIED via static analysis (seam existence, device-residency contract, SC-4 structural checks) or pending a single GPU execution confirmation (Truth 2 / SC-2).

The four code-review warnings (WR-01 through WR-04) are advisory and pre-existing — none block the 7.2 goal of landing the device-residency der seam with no host fold. WR-01 (Quantile boundary edge) is noted for follow-up before the kernel is consumed by 7.3 training paths.

---

_Verified: 2026-06-20T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
