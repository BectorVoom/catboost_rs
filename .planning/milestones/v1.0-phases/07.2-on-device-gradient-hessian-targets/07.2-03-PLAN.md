---
phase: 07.2-on-device-gradient-hessian-targets
plan: 03
type: execute
wave: 3
depends_on: ["07.2-02"]
files_modified:
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/gradient_gpu.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "Focal der1 (focal_gradient_kernel) and der2 (focal_hessian_kernel), both parametric on (alpha, gamma) as length-1 Array<F>, compute device-resident over SelectedRuntime and match the cb-compute baseline on rocm"
    - "Focal der1/der2 are returned as device HANDLES with no host fold on the hand-off path (SC-3)"
    - "Focal p is clamped (the kernel already clamps to [1e-13, 1-1e-13]) so saturated logits do not produce NaN"
    - "A full-family device-residency hand-off assertion confirms every in-scope loss hands 7.3 handle-in → handles-out with no read_one on the seam (SC-3)"
    - "cb-compute / cb-core / cb-model stay byte-unchanged; cb-compute remains cubecl-free (SC-4)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "DerParamKernel::FocalGradient + DerParamKernel::FocalHessian arms (alpha/gamma length-1 Array<F>)"
      contains: "FocalGradient"
    - path: "crates/cb-backend/src/kernels/gradient_gpu.rs"
      provides: "Focal der1/der2 oracle tests (f32+f64, edge cases incl. saturated logit) + full-family device-residency hand-off test + SC-4 structural assertion"
      contains: "focal"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs"
      to: "crates/cb-backend/src/kernels.rs"
      via: "focal_gradient_kernel / focal_hessian_kernel ::launch::<f64, SelectedRuntime>"
      pattern: "focal_gradient_kernel::launch"
    - from: "crates/cb-backend/src/kernels/gradient_gpu.rs"
      to: "cb-compute::focal_der1 / focal_der2"
      via: "elementwise baseline comparison"
      pattern: "focal_der1"
---

<objective>
Third and final MVP vertical slice: extend the parametric der seam (`launch_der_param`,
landed in Plan 02) to the **Focal** der1/der2 family — the two-kernel parametric loss
(`alpha`/`gamma` as length-1 `Array<F>`, both a gradient AND a hessian kernel) — running
device-resident over `SelectedRuntime`, returned as device HANDLES (SC-3), self-oracled
against the `cb-compute::loss` baseline on `rocm` (gfx1100). Then close the phase with a
full-family device-residency hand-off assertion (every in-scope loss: RMSE / Logloss-
CrossEntropy / Quantile-MAE / Focal hands 7.3 handle-in → handles-out, no `read_one` on the
seam) and the SC-4 structural check (cb-compute cubecl-free, cb-core/cb-model unchanged).

After this plan, all four in-scope pointwise der families (D-7.2-01) compute device-resident,
each oracle-checked, completing the GPU-01 gradient/hessian + targets slice.

Purpose: Cover the Focal family + lock the full device-residency seam contract for 7.3.
Output: `DerParamKernel::FocalGradient` / `FocalHessian` arms, the Focal oracle tests, the
full-family hand-off test, and the SC-4 structural assertion.

Honors: D-7.2-01 (Focal completes the narrow start), D-7.2-03 (reuse the authored focal
kernels), D-7.2-04 (device handles), D-7.2-05 (cb-backend-only), D-7.2-06 (report-not-sign-off),
D-7.2-07 (per-crate). der stays UNWEIGHTED (A1). Focal clamp guards NaN (T-04-02-02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-on-device-gradient-hessian-targets/07.2-CONTEXT.md
@.planning/phases/07.2-on-device-gradient-hessian-targets/07.2-RESEARCH.md
@.planning/phases/07.2-on-device-gradient-hessian-targets/07.2-PATTERNS.md
@.planning/phases/07.2-on-device-gradient-hessian-targets/07.2-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan adds (on the Plan-02 `launch_der_param` seam):
- **`gpu_runtime.rs`: `DerParamKernel::FocalGradient`** arm (launches `focal_gradient_kernel`, alpha/gamma length-1 `Array<F>`).
- **`gpu_runtime.rs`: `DerParamKernel::FocalHessian`** arm (launches `focal_hessian_kernel`, alpha/gamma length-1 `Array<F>`).
- **`kernels/gradient_gpu.rs` tests:** `focal_der1_matches_cpu_baseline_*`, `focal_der2_matches_cpu_baseline_*`, `focal_der_saturated_logit_no_nan`, `focal_der_edge_cases`, `all_losses_device_resident_handoff` (full-family SC-3 assertion), `cb_compute_is_cubecl_free` (SC-4 structural note / pointer to the `cargo tree` gate).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Focal der1+der2 (two-kernel parametric launch) to the seam + oracle</name>
  <read_first>
    - crates/cb-backend/src/gpu_runtime.rs (the Plan-02 `launch_der_param_handle` / `DerParamKernel` / length-1-param pattern — add the Focal arms; the WR-05 read-back)
    - crates/cb-backend/src/cpu_runtime.rs (`launch_focal_f64` lines 232–~280 — the `alpha`/`gamma` length-1 `Array<F>` buffers + the `hessian: bool` selecting `focal_gradient_kernel` vs `focal_hessian_kernel`; `compute_gradients_one_dim` lines 478–488 — the `Loss::Focal { alpha, gamma }` arm launching BOTH der1 and der2)
    - crates/cb-backend/src/kernels.rs (`focal_gradient_kernel` lines 134–162 — args `approx, target, der1, alpha, gamma`; `focal_hessian_kernel` lines 174–204 — args `approx, target, der2, alpha, gamma`; BOTH clamp `p` to `[1e-13, 1-1e-13]` before `ln`/`powf`; both have the bounds guard)
    - crates/cb-compute/src/lib.rs (re-exports: `focal_der1`, `focal_der2`, `FOCAL_P_MIN`)
    - .planning/phases/07.2-on-device-gradient-hessian-targets/07.2-PATTERNS.md (Pattern 2 length-1 Array<F>; Security Domain — Focal NaN clamp)
  </read_first>
  <behavior>
    - Test: `launch_der_param(approx, target, DerParamKernel::FocalGradient, &[alpha, gamma])` matches `cb_compute::focal_der1` elementwise for several (alpha, gamma) (f64 rel ~1e-9).
    - Test: `DerParamKernel::FocalHessian` matches `cb_compute::focal_der2` elementwise.
    - Test: a saturated-logit fixture (e.g. approx = ±40) produces finite (non-NaN) der1/der2 (the clamp holds).
    - Test: n=37 exercises the bounds guard; both 0 and 1 targets exercise the label branch.
  </behavior>
  <action>
    In `gpu_runtime.rs`: add `DerParamKernel::FocalGradient` and `DerParamKernel::FocalHessian`
    arms to the Plan-02 `match kernel` in `launch_der_param_handle`. Both build length-1 buffers
    `alpha_handle = client.create(Bytes::from_elems(vec![params[0]]))` and `gamma_handle =
    ...vec![params[1]]` (read params via the non-panicking accessor from Plan 02 — `CbError::
    Degenerate` on a malformed slice, NO indexing). FocalGradient launches `focal_gradient_kernel::
    launch::<f64, SelectedRuntime>(&client, count, dim, ArrayArg::from_raw_parts(approx_handle, n),
    ArrayArg::from_raw_parts(target_handle, n), ArrayArg::from_raw_parts(out_handle.clone(), n),
    ArrayArg::from_raw_parts(alpha_handle, 1), ArrayArg::from_raw_parts(gamma_handle, 1))`;
    FocalHessian launches `focal_hessian_kernel` with the identical arg shape. Reuse the Plan-02
    geometry/client/read-back exactly — no duplicated geometry, no `read_one` on the handle path
    (SC-3), no `unwrap`/`panic`/indexing (D-13), no Atomic/plane. The kernels already clamp `p`
    so no host-side clamp is needed.

    In `kernels/gradient_gpu.rs`: add `focal_der1_matches_cpu_baseline_f64_non_cube_multiple`
    (n=37, e.g. alpha=0.25 gamma=2.0 — the common Focal defaults), `focal_der1_matches_cpu_baseline_f32`,
    `focal_der2_matches_cpu_baseline_f64`/`_f32`, `focal_der_saturated_logit_no_nan` (approx with
    a few ±40 entries; assert all der1/der2 are finite — `is_finite()` — and match the clamped
    `cb_compute::focal_der1`/`focal_der2`), and `focal_der_edge_cases` (empty/n=1/large-N). Mix 0
    and 1 targets to exercise the at/pt label branch. Each REPORTS divergence + asserts the f64
    ~1e-9 / f32 ~1e-3 bound.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::gradient_gpu::focal -- --nocapture 2>&1 | tail -25</automated>
  </verify>
  <done>`DerParamKernel::FocalGradient` + `FocalHessian` present; Focal der1/der2 + saturated-logit + edge-case oracle tests green on rocm (gfx1100) and print reported divergence; der1/der2 confirmed finite (no NaN) on saturated logits; rocm RC=0. On ANY CubeCL build error, load /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md BEFORE fixing.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the full-family device-residency hand-off + SC-4 structural assertion</name>
  <read_first>
    - crates/cb-backend/src/kernels/gradient_gpu.rs (the RMSE/Logloss/Quantile/Focal oracle tests + the Plan-01 `rmse_der_device_resident_handoff` shape to generalize)
    - crates/cb-backend/src/gpu_runtime.rs (the `launch_der_binary_handle` / `launch_der_unary_handle` / `launch_der_param_handle` / `const_der_handle` seam fns — assert they return handles WITHOUT read-back)
    - .planning/phases/07.2-on-device-gradient-hessian-targets/07.2-RESEARCH.md (Phase Requirements → Test Map: the `device_resident_handoff` + SC-4 rows; Open Q1 — hand 7.3 unweighted der1/der2 + reuse histogram_scatter_kernel for weight)
    - .planning/phases/07.2-on-device-gradient-hessian-targets/07.2-VALIDATION.md (the SC-4 structural row: `cargo tree -e features -p cb-compute` shows no cubecl)
  </read_first>
  <action>
    In `kernels/gradient_gpu.rs`: add `all_losses_device_resident_handoff` — for EACH in-scope
    loss family (RMSE via `launch_der_binary_handle` + `const_der_handle(-1.0,n)`; Logloss via
    `launch_der_binary_handle` + `launch_der_unary_handle`; Quantile/MAE via `launch_der_param_handle`
    + `const_der_handle(0.0,n)`; Focal via two `launch_der_param_handle` calls) assert the handle
    fns return `Ok(handle)` and that the der1 AND der2 handles are obtained WITHOUT any host fold
    inserted between them (handle-in → handles-out — the SC-3 / D-7.2-04 contract for the 7.3
    seam). Read each handle back ONCE at the end (test-only) and confirm it equals the matching
    `cb_compute` baseline. Document in a test comment that the handles handed to 7.3 are UNWEIGHTED
    (weight folded downstream by `histogram_scatter_kernel`, per the Plan-01 checkpoint + Open Q1).

    Add `cb_compute_is_cubecl_free` as a `#[test]` that documents the SC-4 structural gate (a
    comment pointing to `cargo tree -e features -p cb-compute` showing no cubecl, run in
    verification) — or, if a programmatic check is awkward, leave it as a doc-test-style note and
    rely on the verification command. The authoritative SC-4 check is the `cargo tree` command in
    `<verification>`; this test makes the intent discoverable in the oracle file.

    No edits to `kernels.rs` (the mount already exists from Plan 01) and NO edits to any
    `cb-compute`/`cb-core`/`cb-model` file (SC-4 — they stay byte-unchanged).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::gradient_gpu -- --nocapture 2>&1 | tail -30 && cargo tree -e features -p cb-compute 2>/dev/null | grep -ci cubecl</automated>
  </verify>
  <done>`all_losses_device_resident_handoff` green on rocm — every in-scope loss family hands der1+der2 device HANDLES with no host fold (SC-3); the FULL `kernels::gradient_gpu` suite passes on gfx1100; `cargo tree -e features -p cb-compute | grep -ci cubecl` returns 0 (SC-4 — cb-compute cubecl-free); `git diff --stat` shows cb-compute/cb-core/cb-model unchanged; `cargo build -p cb-backend --no-default-features --features wgpu` and `--features cuda` both RC=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host slice → device buffer | `Bytes::from_elems` copies host `&[f64]` (approx/target + the length-1 alpha/gamma buffers) into device handles; `ArrayArg::from_raw_parts(handle, len)` re-asserts each length |
| (no external/network/auth boundary) | in-process numeric GPU compute (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-07 | Tampering (numeric NaN/Inf) | Focal `ln`/`powf` on saturated logits | mitigate | `focal_gradient_kernel`/`focal_hessian_kernel` already clamp `p` to `[1e-13, 1-1e-13]` before `ln`/`powf` (T-04-02-02); the `focal_der_saturated_logit_no_nan` test asserts finite der1/der2 on ±40 logits |
| T-07.2-08 | Tampering | `from_raw_parts(out_handle, n)` over-provisioned threads on non-cube-multiple n | mitigate | Both focal kernels guard writes with `if ABSOLUTE_POS < approx.len()`; n=37 fixture exercises the idle path |
| T-07.2-09 | Tampering (length) | length-1 `alpha`/`gamma` param buffers read at index 0 | mitigate | Built as `vec![p]` (length 1), passed `from_raw_parts(handle, 1)`; host-side malformed `params` surfaces `CbError::Degenerate`, not a panic/index |
| T-07.2-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (audit: none new) |
</threat_model>

<verification>
- rocm: `cargo test -p cb-backend --no-default-features --features rocm kernels::gradient_gpu -- --nocapture` — FULL der oracle suite (RMSE/Logloss/CrossEntropy/Quantile/MAE/Focal + hand-off) green on gfx1100; prints reported divergence.
- wgpu: `cargo build -p cb-backend --no-default-features --features wgpu` RC=0 (host run of the suite optional: `cargo test -p cb-backend --no-default-features --features wgpu kernels::gradient_gpu`).
- cuda: `cargo build -p cb-backend --no-default-features --features cuda` RC=0 (compile-only, no NVIDIA).
- SC-4: `cargo tree -e features -p cb-compute | grep -ci cubecl` == 0; `git diff --stat` shows cb-compute/cb-core/cb-model unchanged.
- Per-crate only (D-7.2-07) — never `cargo test --workspace`.
</verification>

<success_criteria>
- Focal der1/der2 (parametric, two-kernel) match the cb-compute baseline device-resident on rocm; finite (no NaN) on saturated logits.
- The full in-scope der family (RMSE/Logloss/CrossEntropy/Quantile/MAE/Focal) computes device-resident, each oracle-checked, completing GPU-01's grad/hess + targets slice.
- Every loss family hands 7.3 der1+der2 device HANDLES with no host fold (SC-3).
- cb-compute is cubecl-free; cb-compute/cb-core/cb-model byte-unchanged (SC-4); wgpu builds+runs, cuda compiles.
</success_criteria>

<output>
Create `.planning/phases/07.2-on-device-gradient-hessian-targets/07.2-03-SUMMARY.md` when done.
</output>
