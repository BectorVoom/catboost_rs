---
phase: 04-model-serialization-shap-rust-api-first-full-oracle-lock
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - crates/catboost-rs/src/builder.rs
  - crates/catboost-rs/src/model.rs
  - crates/catboost-rs/src/error.rs
  - crates/catboost-rs/src/lib.rs
  - crates/catboost-rs/Cargo.toml
  - crates/catboost-rs/tests/builder_oracle_test.rs
  - crates/catboost-rs/src/error_test.rs
autonomous: true
requirements: [RAPI-01, RAPI-02]

must_haves:
  truths:
    - "CatBoostBuilder::new()...loss(...).iterations(...).depth(...).fit(&pool) returns a Model; the loss selects classification vs regression"
    - "Model exposes predict(pool, PredictionType) plus predict()/predict_proba() shorthands, and save_cbm/load_cbm/save_json/load_json/shap_values/feature_importance methods"
    - "The public CatBoostError (thiserror) carries Io/Deserialize/SchemaVersion/FeatureMismatch variants and a #[from] cb_core::CbError arm"
    - "A full numeric binclf + regression train->serialize->load->predict cycle through the public API matches upstream <=1e-5"
  artifacts:
    - path: "crates/catboost-rs/src/builder.rs"
      provides: "CatBoostBuilder (D-05) driving cb_train::train"
      contains: "CatBoostBuilder"
    - path: "crates/catboost-rs/src/model.rs"
      provides: "Model facade methods (predict/save/load/shap/importance) delegating to cb-model"
    - path: "crates/catboost-rs/src/error.rs"
      provides: "Public CatBoostError (thiserror, #[from] CbError + io)"
      contains: "CatBoostError"
  key_links:
    - from: "crates/catboost-rs/src/builder.rs"
      to: "cb_train::train"
      via: "fit(&pool) maps builder fields to BoostParams"
      pattern: "train|BoostParams|fit"
    - from: "crates/catboost-rs/src/model.rs"
      to: "cb_model"
      via: "predict/save/load/shap/importance delegation"
      pattern: "cb_model|predict_raw|save_cbm|shap_values"
    - from: "crates/catboost-rs/src/error.rs"
      to: "cb_core::CbError"
      via: "#[from] arm"
      pattern: "from.*CbError|CbError"
---

<objective>
Wrap the internal crates in the published `catboost-rs` facade: a single unified `CatBoostBuilder` (D-05) whose `loss` selects classification vs regression, a cohesive `Model` carrying `predict`/`predict_proba`/save/load/`shap_values`/`feature_importance` methods (D-06/D-07), and the public typed `CatBoostError` (thiserror, `#[from] CbError`, D-08/RAPI-02). Drive a full numeric binclf + regression train→serialize→load→predict cycle through the public API, oracle-locked ≤1e-5 — closing the phase's first full vertical slice.

Purpose: this is the user-facing surface and the end-to-end success-criterion lock (ROADMAP Phase-4 criterion 5). It composes Plans 01-04 (train, apply/predict, serialize, explain) into one ergonomic object.

Output: `catboost-rs::CatBoostBuilder`, the `Model` facade, `CatBoostError`, and the end-to-end oracle test proving the whole slice.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-CONTEXT.md
@.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-RESEARCH.md
@.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-PATTERNS.md
@crates/cb-train/src/boosting.rs
@crates/cb-core/src/error.rs
@crates/cb-oracle/src/error.rs
</context>

<artifacts_this_phase_produces>
NEW symbols this plan creates (exclude from drift verification):
- `catboost_rs::CatBoostBuilder` with `new()` + chained setters (loss, iterations, depth, learning_rate, l2_leaf_reg, ...) + `fit(&pool) -> Result<Model, CatBoostError>`
- `catboost_rs::Model` facade with methods: `predict(pool, PredictionType)`, `predict()`, `predict_proba()`, `save_cbm`, `load_cbm`, `save_json`, `load_json`, `shap_values`, `feature_importance(type)`
- `catboost_rs::CatBoostError` enum (Train(#[from] CbError), Io(#[from] io::Error), Deserialize, SchemaVersion, FeatureMismatch)
- re-exports of `PredictionType`, `FeatureImportanceType`
- new test files builder_oracle_test.rs, error_test.rs
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CatBoostBuilder + public CatBoostError + Model facade methods</name>
  <read_first>
    crates/cb-train/src/boosting.rs (BoostParams lines 57-119 — the full param surface the Builder wraps; train()/train_with_eval entry points lines 298+; Model produced)
    crates/cb-model/src/model.rs (canonical Model the facade wraps)
    crates/cb-model/src/apply.rs (predict_raw), crates/cb-model/src/predict.rs (PredictionType), crates/cb-model/src/cbm.rs + json.rs (save/load), crates/cb-model/src/shap.rs (shap_values), crates/cb-model/src/fstr.rs (FeatureImportanceType + importance fns)
    crates/cb-core/src/error.rs (variant catalog + #[error] docs)
    crates/cb-oracle/src/error.rs (lines 69-79 — #[from] io/serde dropping Clone/PartialEq; copy this exact tradeoff for the public error)
    crates/cb-compute/src/runtime.rs (Loss enum incl Plan-02 CrossEntropy/Focal — loss selects clf-vs-regression per D-05)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-RESEARCH.md (D-05 unified builder, loss determines task; D-06 predict enum core + shorthands; D-07 methods on Model; D-08 CatBoostError wraps CbError NOT Clone/PartialEq because io::Error)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-PATTERNS.md (Pattern Assignments > builder.rs, catboost-rs/src/model.rs, catboost-rs/src/error.rs — the public-error pattern code block; Shared Patterns > thiserror Error Enums)
    CLAUDE.md (Rust side uses the Builder pattern; thiserror in libraries; no anyhow; no unwrap)
  </read_first>
  <action>
    Create `crates/catboost-rs/src/error.rs` with the public `#[derive(Debug, thiserror::Error)] pub enum CatBoostError` exactly per the PATTERNS public-error block: `Train(#[from] cb_core::CbError)`, `Io(#[from] std::io::Error)`, `Deserialize(String)`, `SchemaVersion(String)`, `FeatureMismatch(String)`. Add a `#[from] cb_model::ModelError` arm (or map ModelError → Deserialize/SchemaVersion) so model-load errors surface as CatBoostError. Do NOT derive Clone/PartialEq (io::Error blocks them — D-08 rationale; document the tradeoff in a comment).

    Create `crates/catboost-rs/src/builder.rs` with `CatBoostBuilder` (D-05): `new()` + chained `#[must_use]` setters for the param surface mirroring `BoostParams` (loss, iterations, learning_rate, depth, l2_leaf_reg, random_strength, bootstrap/subsample, etc.) + `fit(&self, pool: &Pool) -> Result<Model, CatBoostError>` that maps builder fields onto `BoostParams`, calls `cb_train::train` (selecting the runtime/backend as Phase 3 established), and builds the canonical `cb_model::Model` (carrying leaf_weights + float_feature_borders). The `loss` field selects classification vs regression (D-05) — no typed Classifier/Regressor split.

    Create `crates/catboost-rs/src/model.rs` — the facade `Model` (wrap or re-export `cb_model::Model`) with methods (D-06/D-07): `predict(&self, pool, PredictionType) -> Result<Vec<f64>, CatBoostError>` (enum core), `predict(&self, pool)` and `predict_proba(&self, pool)` shorthands calling it with fixed types, `save_cbm/load_cbm/save_json/load_json` delegating to `cb_model::cbm`/`json`, `shap_values(&self, pool)` → `cb_model::shap_values`, `feature_importance(&self, FeatureImportanceType)` → `cb_model::fstr`. Wire all modules + re-export `PredictionType`/`FeatureImportanceType`/`CatBoostError` in `crates/catboost-rs/src/lib.rs` (add the test-lint-exemption header). Add `cb-train`, `cb-model`, `cb-core`, `cb-data`, `cb-compute`, `cb-backend`, `thiserror` to `crates/catboost-rs/Cargo.toml` (keep anyhow absent — D-14/D-15).

    Add `crates/catboost-rs/src/error_test.rs` (unit) asserting `CatBoostError` variants exist and `#[from] CbError` converts (RAPI-02). Wire `#[cfg(test)] mod error_test;` at the crate root.
  </action>
  <verify>
    <automated>cargo build -p catboost-rs && cargo test -p catboost-rs error && cargo clippy -p catboost-rs --lib</automated>
  </verify>
  <acceptance_criteria>
    - `CatBoostBuilder` with `new()` + `fit(&pool)` exists: `grep -nE 'struct CatBoostBuilder|fn new|fn fit' crates/catboost-rs/src/builder.rs` matches all three.
    - Model facade exposes the D-06/D-07 method set: `grep -nE 'fn predict|fn predict_proba|fn save_cbm|fn load_cbm|fn save_json|fn load_json|fn shap_values|fn feature_importance' crates/catboost-rs/src/model.rs` matches each.
    - Public error: `grep -nE 'enum CatBoostError|from.*CbError|Io|Deserialize|SchemaVersion|FeatureMismatch' crates/catboost-rs/src/error.rs` matches; CatBoostError does NOT derive Clone/PartialEq.
    - anyhow absent: `grep -rn 'anyhow' crates/catboost-rs/src crates/catboost-rs/Cargo.toml` returns nothing.
    - `cargo test -p catboost-rs error` exits 0 (RAPI-02 variant + #[from] conversion asserts).
    - `cargo clippy -p catboost-rs --lib` exits 0 (no unwrap/indexing_slicing).
  </acceptance_criteria>
  <done>The public Builder, Model facade, and CatBoostError compile clippy-clean; error variants + #[from] conversions locked.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end binclf + regression train->serialize->load->predict oracle</name>
  <read_first>
    crates/catboost-rs/src/builder.rs + model.rs (Task 1 public API)
    crates/cb-train/tests/leaf_methods_oracle_test.rs (oracle integration-test layout: fixture() helper, compare_stage usage)
    crates/cb-oracle/src/compare.rs (Stage::Predictions, compare_stage ≤1e-5 lines 70-96)
    crates/cb-data (Pool construction for the test input)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-RESEARCH.md (Validation Architecture: RAPI-01 `cargo test -p catboost-rs builder`; the success-criterion-5 end-to-end lock)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-PATTERNS.md (Shared Patterns > Oracle Integration-Test Layout — fixture() helper, integration tests under tests/)
  </read_first>
  <action>
    Add `crates/catboost-rs/tests/builder_oracle_test.rs` exercising the FULL public-API slice for BOTH a numeric binary-classification fixture and a numeric regression fixture (reuse the Phase-3 `binclf_skeleton`/`regression_skeleton` inputs + the Plan-01 prediction fixtures): (1) build a `Pool` from the fixture inputs; (2) `CatBoostBuilder::new().loss(...).iterations(...).depth(...).fit(&pool)` → Model; (3) `model.save_cbm(tmp)` then `Model::load_cbm(tmp)` → reloaded; (4) `reloaded.predict(&pool, PredictionType::RawFormulaVal)` and assert ≤1e-5 vs the upstream prediction fixture via `compare_stage(Stage::Predictions, ...)`; (5) repeat through `save_json`/`load_json`; (6) assert `predict_proba` matches the Probability fixture. Use the `fixture()` path helper from the Shared Patterns layout (adjust the crate hop). If a fixture is unavailable in-env, `#[ignore]` the oracle assertions with a documented reason but KEEP the structural round-trip asserts (fit→save→load reproduces predictions deterministically Rust-vs-Rust) as the in-env gate.
  </action>
  <verify>
    <automated>cargo test -p catboost-rs builder</automated>
  </verify>
  <acceptance_criteria>
    - `crates/catboost-rs/tests/builder_oracle_test.rs` runs the fit→save_cbm→load_cbm→predict and fit→save_json→load_json→predict cycle for BOTH binclf and regression and asserts ≤1e-5 vs upstream (or documented `#[ignore]` for the oracle leg + in-env Rust↔Rust round-trip determinism asserts); `cargo test -p catboost-rs builder` exits 0.
    - The test uses the public API only (no direct cb-train/cb-model imports for the prediction path): `grep -nE 'CatBoostBuilder|\.fit\(|\.predict|\.save_cbm|\.load_cbm|\.save_json|\.load_json|predict_proba' crates/catboost-rs/tests/builder_oracle_test.rs` matches.
    - compare_stage ≤1e-5 used for the oracle legs: `grep -n 'compare_stage' crates/catboost-rs/tests/builder_oracle_test.rs` matches.
  </acceptance_criteria>
  <done>The full numeric binclf + regression train→serialize→load→predict cycle passes through the public API ≤1e-5 (ROADMAP criterion 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (Rust caller) -> CatBoostBuilder/Model | Public API surface; errors surface as typed CatBoostError, never panic |
| load_cbm/load_json file -> facade | Delegates to cb-model deserializers (validated in Plan 03); facade maps ModelError -> CatBoostError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | malformed file via facade load_cbm/load_json | mitigate | Delegates to Plan-03 validated deserializers; ModelError mapped to CatBoostError::Deserialize/SchemaVersion — no panic crosses the public boundary |
| T-04-05-02 | Information Disclosure | predict on a Pool with wrong feature count | mitigate | FeatureMismatch variant returned (typed) when pool features != model expectation; no OOB |
| T-04-05-03 | Repudiation | none (no auth/logging surface) | accept | Numerical library; no identity/audit requirement |
</threat_model>

<verification>
- `cargo test -p catboost-rs error builder` exits 0 (oracle legs run with fixtures or documented `#[ignore]`; structural round-trip determinism runs in-env).
- Full workspace gate before /gsd-verify-work: `cargo test --workspace` (watch disk — MLIR dep; STATE.md caution).
- `grep -rn 'anyhow' crates/catboost-rs` returns nothing.
- `cargo clippy -p catboost-rs --lib` clean (no unwrap/indexing_slicing).
- Public API drives the prediction path (no internal-crate prediction imports in the integration test).
</verification>

<success_criteria>
- `CatBoostBuilder...fit(&pool) -> Model` with loss-selected task; predict enum core + shorthands; serialize/explain methods on Model (RAPI-01, D-05/D-06/D-07).
- Public typed `CatBoostError` (thiserror, #[from] CbError) across the surface (RAPI-02, D-08).
- Full numeric binclf + regression train→serialize→load→predict oracle pass ≤1e-5 (ROADMAP Phase-4 criterion 5).
</success_criteria>

<output>
Create `.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-05-SUMMARY.md` when done.
</output>
