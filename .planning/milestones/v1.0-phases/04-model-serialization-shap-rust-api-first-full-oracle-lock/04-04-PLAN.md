---
phase: 04-model-serialization-shap-rust-api-first-full-oracle-lock
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/cb-model/src/shap.rs
  - crates/cb-model/src/fstr.rs
  - crates/cb-model/src/lib.rs
  - crates/cb-model/tests/shap_oracle_test.rs
  - crates/cb-model/tests/fstr_oracle_test.rs
autonomous: true
requirements: [MODEL-04, MODEL-03]

must_haves:
  truths:
    - "Regular SHAP produces a per-object [n_features+1] matrix matching upstream <=1e-5 (trailing column = bias/expected-value term)"
    - "The local-accuracy invariant holds: sum over features (incl trailing column) of SHAP == RawFormulaVal prediction"
    - "PredictionValuesChange feature importance matches upstream <=1e-5, normalized to percentages summing to 100"
    - "Interaction feature importance (pairwise) matches upstream <=1e-5"
  artifacts:
    - path: "crates/cb-model/src/shap.rs"
      provides: "Regular TreeSHAP recursion + prepared-trees (subtree weights, mean values)"
      contains: "subtree"
    - path: "crates/cb-model/src/fstr.rs"
      provides: "PredictionValuesChange + Interaction feature importance"
      contains: "PredictionValuesChange"
  key_links:
    - from: "crates/cb-model/src/shap.rs"
      to: "Model.leaf_weights"
      via: "subtree-weight + mean-value baselines"
      pattern: "leaf_weights|subtree"
    - from: "crates/cb-model/src/shap.rs"
      to: "cb_core::sum_f64"
      via: "feature-path weight folds"
      pattern: "sum_f64"
    - from: "crates/cb-model/src/fstr.rs"
      to: "Model.leaf_weights"
      via: "leaf-pair weighted variance"
      pattern: "leaf_weights|count"
---

<objective>
Add model explainability: Regular `EShapCalcType` TreeSHAP (MODEL-04) producing the full per-object × (n_features+1) SHAP matrix with the local-accuracy invariant, and PredictionValuesChange + Interaction feature importance (MODEL-03, partial — LossFunctionChange deferred per D-12). All three consume the per-leaf weights captured in Plan 01.

Purpose: completes the explain leg of the train→serialize→load→predict/explain slice. SHAP is the strongest correctness signal (per-object, sign-sensitive — D-11). This is the hardest new code in the phase (no in-repo recursion analog); transcribe the cited upstream algorithm line-for-line.

Output: `cb-model::shap` (TreeSHAP + prepared-trees), `cb-model::fstr` (PredictionValuesChange + Interaction), and oracle tests locking the SHAP matrix (+ local accuracy) and both importance types.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-CONTEXT.md
@.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-RESEARCH.md
@.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-PATTERNS.md
@crates/cb-core/src/reduction.rs
@crates/cb-compute/src/loss.rs
</context>

<artifacts_this_phase_produces>
NEW symbols this plan creates (exclude from drift verification):
- `cb_model::shap::shap_values(model, pool) -> Vec<Vec<f64>>` (per-object [n_features+1] matrix)
- internal prepared-trees helpers: subtree-weights, mean-value-per-tree, ExtendFeaturePath/UnwindFeaturePath/UpdateShapByFeaturePath transcriptions, a feature-path Elem struct
- `cb_model::fstr::FeatureImportanceType` enum (PredictionValuesChange, Interaction)
- `cb_model::fstr::prediction_values_change(model) -> Vec<f64>` (percent, Σ=100)
- `cb_model::fstr::interaction(model) -> Vec<(usize, usize, f64)>`
- new test files shap_oracle_test.rs, fstr_oracle_test.rs
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regular TreeSHAP (prepared-trees + recursion) with local-accuracy lock</name>
  <read_first>
    crates/cb-model/src/model.rs (canonical Model: per-tree leaf_values + leaf_weights; bias; float_feature_borders)
    crates/cb-model/src/apply.rs (Plan-02 predict_raw — the RawFormulaVal target the local-accuracy invariant checks against)
    crates/cb-core/src/reduction.rs (sum_f64 lines 31-38 — every feature-path weight fold routes here)
    crates/cb-train/src/boosting.rs (lines 274-277 .and_then(|col| col.get(obj)) — the checked-index style the recursion must mirror under indexing_slicing deny)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-RESEARCH.md (Pattern 4 full: prepared-trees subtreeWeights/meanValue shap_prepared_trees.cpp:25-222 need LeafWeights; ExtendFeaturePath shap_values.cpp:44-64 [Code Example], UnwindFeaturePath:66-104 FuzzyEquals on oneFrac==0, recursion:196-320 hot/coldCoefficient, UpdateShapByFeaturePath coefficient=weightSum*(oneFrac-zeroFrac)*(leafValue-averageTreeApprox); matrix assembly:1030-1055 trailing column=Σ meanValue+bias; local-accuracy invariant D-11; A3 binFeatureCombinationClass identity for numeric-only; scale==1)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-PATTERNS.md (Pattern Assignments > shap.rs; No Analog Found > shap recursion — budget for indexing_slicing-clean recursion; Shared Patterns > Float Summation Routing + Oracle Integration-Test Layout)
  </read_first>
  <behavior>
    - Prepared-trees per tree: subtreeWeights[depth][node] = bottom-up sum of leaf_weights (leaves = leaf_weights, internal = sum of children); meanValue[dim] = (Σ leafValue·leafWeight)/subtreeWeights[0][0] = averageTreeApprox baseline.
    - ExtendFeaturePath/UnwindFeaturePath/recursion transcribed verbatim (the polynomial weight machinery); go-branch oneFrac=newOnePathsFraction, skip-branch oneFrac=0; at leaf, distribute coefficient=weightSum*(oneFrac-zeroFrac)*(leafValue-averageTreeApprox) per feature.
    - Output per object = [n_features+1]; trailing column[n_features] = Σ_trees meanValue + model bias.
    - Local accuracy: Σ over all columns (incl trailing) == predict_raw RawFormulaVal for that object (D-11 — the strongest check).
    - numeric-only ⇒ each float bin-feature maps to its own feature (combinationClass identity, A3).
  </behavior>
  <action>
    Create `crates/cb-model/src/shap.rs`. Implement prepared-trees precompute: per tree build `subtree_weights` bottom-up from `leaf_weights` and `mean_value = (Σ leafValue·leafWeight)/subtree_weights[0][0]` (RESEARCH Pattern 4 prepared-trees; all sums via `cb_core::sum_f64`). Transcribe `extend`/`unwind`/the per-leaf recursion + `update_shap_by_feature_path` VERBATIM from RESEARCH Pattern 4 + Code Example (`shap_values.cpp:44-320`), using a small `Elem { feature, zero_frac, one_frac, weight }` struct. Assemble the per-object output `[n_features+1]` with the trailing column = `Σ meanValue + bias` (`shap_values.cpp:1030-1055`). Use `FuzzyEquals`-style float compare for the `oneFrac==0` branch (RESEARCH UnwindFeaturePath). For numeric-only models treat each feature as its own combination class (A3). Use checked `.get`/`.get_mut` throughout (no indexing — the recursion is index-heavy; mirror the `.and_then(|col| col.get(obj))` discipline); route every weight fold through `cb_core::sum_f64`. Expose `#[must_use] pub fn shap_values(model: &Model, pool: ...) -> Vec<Vec<f64>>`. Wire `mod shap; pub use shap::shap_values;` into `lib.rs`.

    Add `crates/cb-model/tests/shap_oracle_test.rs`: load the Plan-01 SHAP `.npy` fixture and assert the per-object matrix vs upstream ≤1e-5 (flatten to a Vec and `compare_stage(Stage::Predictions, ...)`), AND assert the local-accuracy invariant `Σ_columns shap[obj] == predict_raw[obj]` ≤1e-5 for every object (D-11). If the fixture is unavailable in-env, `#[ignore]` the oracle case with a documented reason but KEEP the local-accuracy invariant as an in-env test on a Rust-built model (it needs no fixture).
  </action>
  <verify>
    <automated>cargo test -p cb-model shap && cargo clippy -p cb-model --lib</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-model/tests/shap_oracle_test.rs` asserts the SHAP matrix vs the upstream `.npy` fixture ≤1e-5 (or documented `#[ignore]`) AND the local-accuracy invariant `Σ shap == predict_raw` ≤1e-5 for every object (this case runs in-env without a fixture); `cargo test -p cb-model shap` exits 0.
    - shap.rs uses leaf_weights: `grep -nE 'leaf_weights|subtree' crates/cb-model/src/shap.rs` matches.
    - No raw float sum: `grep -v '^[[:space:]]*//' crates/cb-model/src/shap.rs | grep -E '\.sum\(\)|fold\(0\.0' | grep -v sum_f64` returns nothing.
    - No raw indexing / unwrap in production recursion: `grep -nE 'unwrap\(\)|expect\(' crates/cb-model/src/shap.rs` returns nothing; `cargo clippy -p cb-model --lib` exits 0 (indexing_slicing clean).
  </acceptance_criteria>
  <done>Per-object SHAP matrix matches upstream ≤1e-5 and satisfies local accuracy; recursion is indexing_slicing-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PredictionValuesChange + Interaction feature importance</name>
  <read_first>
    crates/cb-model/src/model.rs (per-tree leaf_values + leaf_weights; splits per tree give the per-level feature)
    crates/cb-model/src/shap.rs (Task 1 — reuse any prepared subtree-weight helpers if applicable)
    crates/cb-compute/src/score.rs (weighted-reduction + sum_f64 style — the closest in-repo analog)
    crates/cb-core/src/reduction.rs (sum_f64)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-RESEARCH.md (Pattern 5 CalcEffect feature_str.h:233-270: per tree per split-feature per leaf-pair (leaf, leaf^(1<<bit)) inverted>leaf, count1/count2=leaf weights, skip if either 0, avrg=(val1·c1+val2·c2)/(c1+c2), dif=(val1-avrg)²c1+(val2-avrg)²c2, res[srcFeature]+=dif, ConvertToPercents Σ=100; Pattern 6 Interaction CalcMostInteractingFeatures feature_str.cpp:190-270 + calc_fstr.cpp:314-441 pairwise weighted-variance, normalized percent, Vec<(i,j,score)>; D-12 LossFunctionChange OUT OF SCOPE)
    .planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-PATTERNS.md (Pattern Assignments > fstr.rs)
  </read_first>
  <behavior>
    - PredictionValuesChange: for each tree, for each split-feature bit, for each leaf pair (leaf, leaf^(1<<bit)) with inverted>leaf: count1=leaf_weight[leaf], count2=leaf_weight[inverted]; skip if either is 0; avrg=(val1·c1+val2·c2)/(c1+c2); dif=(val1-avrg)²·c1+(val2-avrg)²·c2; res[srcFeature]+=dif. Normalize res to percentages summing to 100.
    - Interaction: pairwise weighted-variance contribution per feature pair aggregated across trees, normalized to percent; returns Vec<(feature_i, feature_j, score)>. NOT SHAP interaction values (that is Phase 6).
  </behavior>
  <action>
    Create `crates/cb-model/src/fstr.rs`. Implement `prediction_values_change(model: &Model) -> Vec<f64>` transcribing `CalcEffect` verbatim (RESEARCH Pattern 5): iterate trees → split-feature bits → leaf pairs, using `leaf_weights` as count1/count2, the `count==0` short-circuit, the avrg/dif accumulation, then a `ConvertToPercents` helper normalizing so Σ=100 (all sums via `cb_core::sum_f64`). Implement `interaction(model: &Model) -> Vec<(usize, usize, f64)>` per RESEARCH Pattern 6 (pairwise weighted-variance, percent-normalized). Add `pub enum FeatureImportanceType { PredictionValuesChange, Interaction }`. DO NOT implement LossFunctionChange (D-12 — out of scope). Use checked access; route folds through `sum_f64`. Wire `mod fstr; pub use fstr::{FeatureImportanceType, prediction_values_change, interaction};` into `lib.rs`.

    Add `crates/cb-model/tests/fstr_oracle_test.rs`: load the Plan-01 PredictionValuesChange + Interaction `.npy` fixtures and assert ≤1e-5 (compare_stage). Assert PredictionValuesChange sums to 100 (±1e-5). If a fixture is unavailable in-env, `#[ignore]` with a documented reason and keep the Σ=100 normalization assert as the in-env gate.
  </action>
  <verify>
    <automated>cargo test -p cb-model fstr && cargo clippy -p cb-model --lib</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-model/tests/fstr_oracle_test.rs` asserts PredictionValuesChange + Interaction vs upstream fixtures ≤1e-5 (or documented `#[ignore]`) AND PredictionValuesChange Σ==100±1e-5 (in-env); `cargo test -p cb-model fstr` exits 0.
    - fstr.rs uses leaf_weights and skips count==0: `grep -nE 'leaf_weights|count' crates/cb-model/src/fstr.rs` matches.
    - LossFunctionChange is NOT implemented: `grep -n 'LossFunctionChange' crates/cb-model/src/fstr.rs` returns nothing (D-12).
    - No raw float sum: `grep -v '^[[:space:]]*//' crates/cb-model/src/fstr.rs | grep -E '\.sum\(\)|fold\(0\.0' | grep -v sum_f64` returns nothing.
    - `cargo clippy -p cb-model --lib` exits 0.
  </acceptance_criteria>
  <done>PredictionValuesChange (Σ=100) and Interaction match upstream ≤1e-5; LossFunctionChange correctly omitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model (trusted, loaded/trained) -> SHAP/fstr | Operates on an already-validated Model; leaf_weights and indices bounded by 2^depth |
| Pool -> SHAP per-object | User feature values; binarization bounded as in apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | index-heavy SHAP recursion (subtree node / feature-path indices) | mitigate | Checked `.get`/`.get_mut` throughout (indexing_slicing deny); transcribed bounds match 2^depth tree shape — no OOB |
| T-04-04-02 | Information Disclosure | division by subtree_weights[0][0]==0 (missing leaf weights) | mitigate | Plan 01 guarantees leaf_weights present; SHAP skips/guards zero-weight subtrees (upstream short-circuit), no NaN leak |
| T-04-04-03 | Information Disclosure | PredictionValuesChange divide-by-(c1+c2)==0 | mitigate | `count==0` short-circuit skips the leaf pair (verbatim upstream), no div-by-zero |
</threat_model>

<verification>
- `cargo test -p cb-model shap fstr` exits 0 (oracle cases run with fixtures or documented `#[ignore]`; local-accuracy + Σ=100 invariants run in-env regardless).
- Local-accuracy invariant `Σ shap == predict_raw` ≤1e-5 holds for every object.
- All float sums via cb_core::sum_f64; no unwrap/indexing in production.
- `grep -n 'LossFunctionChange' crates/cb-model/src` returns nothing (D-12 scope guard).
- `cargo clippy -p cb-model --lib` clean.
</verification>

<success_criteria>
- Regular SHAP matrix matches upstream ≤1e-5 with local accuracy asserted (MODEL-04, D-11).
- PredictionValuesChange (percent, Σ=100) + Interaction match upstream ≤1e-5 (MODEL-03 partial; LossFunctionChange deferred per D-12).
</success_criteria>

<output>
Create `.planning/phases/04-model-serialization-shap-rust-api-first-full-oracle-lock/04-04-SUMMARY.md` when done.
</output>
