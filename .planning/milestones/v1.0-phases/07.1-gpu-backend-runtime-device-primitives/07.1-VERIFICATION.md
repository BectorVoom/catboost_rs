---
phase: 07.1-gpu-backend-runtime-device-primitives
verified: 2026-06-20T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification: null
---

# Phase 7.1: GPU Backend Runtime & Device Primitives — Verification Report

**Phase Goal:** The `rocm`/`wgpu`/`cuda` GPU runtimes are wired into the `cb-backend` `SelectedRuntime` cfg-alias (replacing the inert `()` placeholders) with zero runtime dispatch, and the device-side scan/reduction/memory primitives that all later kernels build on land at structural parity with `catboost/cuda/cuda_lib` + `cuda_util` — wave-size-agnostic, using CUDA-style in-kernel atomic/plane reductions (D-03).
**Verified:** 2026-06-20
**Status:** PASSED
**Re-verification:** No — initial verification
**Authoritative GPU gate:** rocm gfx1100 (wave32, ROCm 7.1.1) — per D-06 / D-7.1-10

---

## Step 0: Previous Verification

None found. Initial mode.

---

## Goal Achievement

### Observable Truths (ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Each Cargo feature (`cpu`/`wgpu`/`cuda`/`rocm`) resolves `SelectedRuntime` via a single `cfg`-gated type alias, zero runtime `match`; `cb-compute` stays cubecl-free; `cb-core`/`cb-model` unchanged | VERIFIED | `lib.rs:45-65` — four `pub type SelectedRuntime = …` arms in a mutual-exclusion `not(…)` chain; no `match` over backends. `cb-compute/Cargo.toml` has no `cubecl` dep; `cb-compute/src/lib.rs:9` doc enforces it. All four `cargo build -p cb-backend --no-default-features --features X` RC=0. |
| SC-2 | `rocm` initializes and runs on in-env gfx1100; `wgpu` builds and runs on host; `cuda` compiles behind its feature gate | VERIFIED | `cargo test -p cb-backend --no-default-features --features rocm kernels::reduce -- --nocapture` → 4/4 pass (links `libamdhip64`). `cargo test -p cb-backend --no-default-features --features wgpu kernels::reduce` → 4/4 pass. `cargo build -p cb-backend --no-default-features --features cuda` RC=0 (no NVIDIA hardware, compile-only per D-07). |
| SC-3 | Generic-over-`F: Float` scan and reduction primitives run on `rocm`, are wave-size-agnostic (no warp-size constants), mirror CUDA reduction/scan structure (D-03 atomic/plane reductions) | VERIFIED | `kernels.rs:574-838` — `block_reduce_kernel`, `block_reduce_atomic_kernel`, `block_scan_kernel` all `<F: Float>` generic, no hard-coded 32/64 in any stride (strides derive from `CUBE_DIM_X`/`PLANE_DIM`). `BLOCK_REDUCE_SHMEM=32` is the comptime SharedMemory size, not a reduction stride. Hillis-Steele scan structure mirrors `inplace_scan.cuh::InplaceInclusiveScan` (D-01). Atomic-finalize sibling `block_reduce_atomic_kernel` mirrors CUDA in-kernel `fetch_add` (D-03). `cargo test -p cb-backend --no-default-features --features rocm kernels::scan` → 4/4 pass (max divergence 0.000e0). |
| SC-4 | Primitives self-oracle against the Rust CPU path; tolerance reported, not signed off (GPU-06 epsilon deferred to 7.6) | VERIFIED | `kernels/reduce.rs` and `kernels/scan.rs` compare device output to `cb-core::sum_f64` / CPU prefix-sum and `println!` max abs/rel divergence without hard-coding. Observed: reduce max rel div 9.6e-14 (f64), scan max div 0.000e0. No `assert!(… <= EPSILON)` with a pinned tolerance. |

**Score: 4/4 truths verified**

---

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| D1 | Cross-cube scan carry — `block_scan_kernel` is correct only for N ≤ CUBE_DIM (32); multi-cube carry is explicitly documented as the first forward dep | Phase 7.2/7.3 | PLAN-02 SUMMARY §"Forward Dependencies": "Cross-cube scan carry (first 7.2/7.3 dependency)". `launch_block_scan_f64` enforces the limit with a typed `CbError::Degenerate` for `n > CUBE_DIM` (not a silent wrong result). |
| D2 | f64 in-kernel-atomic non-determinism sign-off — gfx1100/HIP lacks f64 atomic-add at cubecl 0.10.0 so the `HostSumFallback` path ran; variance is 0.000e0 (deterministic fallback) | Phase 7.6 | PLAN-02 SUMMARY §"Decisions Made": "EMPIRICAL (Open Q1 RESOLVED)". Confirmed in live run: `path=HostSumFallback runs=32 ... run_to_run_spread=0.000e0`. GPU-06 epsilon sign-off is Phase 7.6's job. |
| D3 | GPU-01 grad/hess, histograms, score/split, on-device loop | Phases 7.2–7.5 | REQUIREMENTS.md: "GPU-01 \| Phase 7.1–7.5 \| Pending (scan/reductions 7.1, grad/hess 7.2, ...)". This phase owns the scan+reductions slice only. |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-backend/src/lib.rs` | `SelectedRuntime` cfg-alias arms for all four backends; module mounts widened | VERIFIED | Lines 45-65: four `pub type SelectedRuntime` arms with mutual-exclusion `not(…)` chain. Lines 18-27: `kernels` and `gpu_runtime` modules mounted under all-backends predicate. |
| `crates/cb-backend/Cargo.toml` | Per-backend cubecl feature mapping (`rocm = ["cubecl/hip"]`, etc.) | VERIFIED | Confirmed by feature tree: `cargo tree -e features -p cb-backend --no-default-features --features rocm` shows `cubecl-hip v0.10.0` / `cubecl-hip-sys v7.1.5280200`; no `cubecl feature "cpu"` in the rocm tree. |
| `crates/cb-backend/src/gpu_runtime.rs` (NEW) | `launch_block_reduce_f64`, `launch_block_scan_f64`, `launch_block_reduce_atomic_f64`, `AtomicFinalizePath`; CR-01/CR-02 guards | VERIFIED | File exists, 291 lines. CR-01 guard `if n > CUBE_DIM` at line 163. CR-02 `const _: () = assert!(CUBE_DIM <= crate::kernels::BLOCK_REDUCE_SHMEM, …)` at line 71-75. All three launch helpers present. No `unwrap`/`expect`/`panic`/indexing in production bodies (grep confirmed). |
| `crates/cb-backend/src/kernels.rs` | `block_reduce_kernel`, `block_reduce_atomic_kernel`, `block_scan_kernel`; `BLOCK_REDUCE_SHMEM`; module mounts | VERIFIED | Lines 574-838 contain all three `#[cube(launch)]` kernels. `BLOCK_REDUCE_SHMEM: usize = 32` at line 22 (`pub(crate)`). Module mounts at lines 846-869. |
| `crates/cb-backend/src/kernels/reduce.rs` (NEW) | rocm self-oracle vs `cb-core::sum_f64`; f32+f64; edge cases; both paths; atomic variant + variance report | VERIFIED | File exists (254 lines). Four tests: `block_reduce_matches_cpu_sum_f32`, `block_reduce_matches_cpu_sum_f64_non_cube_multiple`, `block_reduce_edge_cases`, `block_reduce_atomic_finalize_matches_cpu_sum_and_reports_variance`. Live run: 4/4 pass on gfx1100. |
| `crates/cb-backend/src/kernels/scan.rs` (NEW) | rocm self-oracle vs CPU prefix-sum; inclusive+exclusive; f32+f64; n=1 edge case; divergence reported | VERIFIED | File exists (185 lines). Four tests covering f64 inclusive, f64 exclusive, f32 inclusive, n=1 edge case. Live run: 4/4 pass on gfx1100. |
| `Cargo.lock` | `cubecl-hip-sys` pinned at `7.1.5280200` | VERIFIED | `grep -A3 'name = "cubecl-hip-sys"' Cargo.lock` → `version = "7.1.5280200"`. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lib.rs` | `cubecl::hip::HipRuntime` | `pub type SelectedRuntime = cubecl::hip::HipRuntime` (rocm arm, line 65) | WIRED | Live confirmed: `cargo build -p cb-backend --no-default-features --features rocm` RC=0; `cargo tree` shows `cubecl-hip v0.10.0`. |
| `gpu_runtime.rs` | `kernels::block_reduce_kernel` | `block_reduce_kernel::launch::<f64, SelectedRuntime>` at line 116 | WIRED | Grep confirmed; live reduce oracle passes. |
| `gpu_runtime.rs` | `kernels::block_scan_kernel` | `block_scan_kernel::launch::<f64, SelectedRuntime>` at line 187 | WIRED | Grep confirmed; live scan oracle passes. |
| `gpu_runtime.rs` | `kernels::block_reduce_atomic_kernel` | `block_reduce_atomic_kernel::launch::<f64, SelectedRuntime>` at line 271 | WIRED | Grep confirmed; live atomic test passes. |
| `kernels/reduce.rs` | `cb_core::sum_f64` | Oracle comparison calls `cb_core::sum_f64(&partials_f64)` and `cb_core::sum_f64(input)` | WIRED | Lines 70, 86. Live comparison asserts match. |
| `kernels/scan.rs` | CPU prefix-sum baseline | `cpu_inclusive_scan` / `cpu_exclusive_sum` helpers comparing device output to sequential Rust sum | WIRED | Lines 64-84. Live scan oracle passes with 0.000e0 divergence. |

---

### Data-Flow Trace (Level 4)

These are device-compute primitives (not React components), so the data-flow model is: host slice → device handle → kernel → read-back → comparison baseline.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `launch_block_reduce_f64` | `input: &[f64]` → device → partials | `Bytes::from_elems(input.to_vec())` → `block_reduce_kernel` → `client.read_one` → `bytemuck::cast_slice` | Yes — live test produced `device_sum=8152`, `baseline=8152` | FLOWING |
| `launch_block_scan_f64` | `input: &[f64]` → device → scan output | Same transfer pattern → `block_scan_kernel` → read-back | Yes — live test: `max abs_div=0.000e0` vs CPU baseline | FLOWING |
| `launch_block_reduce_atomic_f64` | `input: &[f64]` → device (or host fallback) → scalar sum | Capability-gated: if f64 atomic supported → `block_reduce_atomic_kernel`; else → `launch_block_reduce_f64` + `sum_f64` | Yes — live test: `baseline=5595.25`, result matches | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| rocm reduce oracle (4 tests incl. edge cases + atomic variant) | `cargo test -p cb-backend --no-default-features --features rocm kernels::reduce -- --nocapture` | 4/4 pass; max rel div 9.6e-14 (f64 N=100k), 0.000e0 (f32, n=37, n=1, atomic) | PASS |
| rocm scan oracle (inclusive, exclusive, f32, n=1) | `cargo test -p cb-backend --no-default-features --features rocm kernels::scan -- --nocapture` | 4/4 pass; max abs_div=0.000e0 for all tests | PASS |
| wgpu reduce runs on host | `cargo test -p cb-backend --no-default-features --features wgpu kernels::reduce` | 4/4 pass | PASS |
| wgpu scan runs on host | `cargo test -p cb-backend --no-default-features --features wgpu kernels::scan` | 4/4 pass | PASS |
| cpu build unbroken | `cargo build -p cb-backend --no-default-features --features cpu` | RC=0 | PASS |
| wgpu build | `cargo build -p cb-backend --no-default-features --features wgpu` | RC=0 | PASS |
| cuda compile-gate | `cargo build -p cb-backend --no-default-features --features cuda` | RC=0 | PASS |
| rocm cpu-free feature graph | `cargo tree -e features -p cb-backend --no-default-features --features rocm` | `cubecl-hip v0.10.0` present; `cubecl feature "cpu"` absent | PASS |

---

### Probe Execution

No `scripts/*/tests/probe-*.sh` files found; no probe-based verification declared in PLAN/SUMMARY. The behavioral spot-checks above serve as the equivalent verification layer.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| GPU-02 | 07.1-01 | Compile-time backend selection via Cargo features through single `cfg`-gated type alias — zero runtime dispatch | SATISFIED | `lib.rs:45-65`: four `pub type SelectedRuntime` arms; no runtime `match`. All four backend builds RC=0. REQUIREMENTS.md status: `Complete`. |
| GPU-04 | 07.1-01 | `wgpu` backend for dev machines without ROCm/CUDA | SATISFIED | `cargo test -p cb-backend --no-default-features --features wgpu kernels::reduce/scan` both 4/4. REQUIREMENTS.md status: `Complete`. |
| GPU-05 | 07.1-01 | `cuda` backend compile-gated, untested locally | SATISFIED | `cargo build -p cb-backend --no-default-features --features cuda` RC=0. REQUIREMENTS.md status: `Complete`. |
| GPU-01 | 07.1-01, 07.1-02 | CubeCL compute kernels generic over `R: Runtime` and `F: Float` — scan + reductions slice (histogram, grad/hess, full kernel surface owned by 7.2–7.5) | PARTIALLY SATISFIED (7.1 slice complete) | Scan + reduce + atomic-reduce primitives implemented, oracle-verified on gfx1100. Full GPU-01 spans 7.1–7.5; the 7.1 scan/reductions slice is the only 7.1 deliverable. REQUIREMENTS.md: "Pending (scan/reductions 7.1, grad/hess 7.2, ...)". |

**Requirement ID cross-reference against REQUIREMENTS.md:** All four IDs declared in plan frontmatter (`GPU-02`, `GPU-04`, `GPU-05`, `GPU-01`) are mapped to Phase 7 in REQUIREMENTS.md. No orphaned requirements detected.

---

### Anti-Patterns Found

Scanned all files modified or created in this phase:
- `crates/cb-backend/src/lib.rs`
- `crates/cb-backend/src/gpu_runtime.rs`
- `crates/cb-backend/src/kernels.rs`
- `crates/cb-backend/src/kernels/reduce.rs`
- `crates/cb-backend/src/kernels/scan.rs`
- `Cargo.toml` (root)

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| None | `TBD`/`FIXME`/`XXX` debt markers | — | Zero found across all phase-modified files (grep confirmed). |
| `gpu_runtime.rs` | `unwrap`/`expect`/`panic` | — | Zero in production bodies. Only in doc comment lines explaining the prohibition. |
| `kernels/reduce.rs`, `kernels/scan.rs` | `unwrap`/`indexing` | Info | Test files use `.unwrap()` / indexing — permitted under `#[cfg(test)]` (`lib.rs:1` allow). |
| `kernels.rs:22` | `BLOCK_REDUCE_SHMEM = 32` | Info | This is the permitted comptime SharedMemory size constant, not a warp-size literal in a reduction stride. The `const _: ()` coupling assertion in `gpu_runtime.rs:71-75` makes any launch-width drift a compile error (CR-02 guard). |

No blockers or warnings.

---

### Human Verification Required

None. All four success criteria are verifiable programmatically via the rocm oracle gate (live test results captured above). The wgpu host run confirms SC-2 for the non-rocm build path. No visual/UX/real-time/external-service items exist in this infrastructure phase.

---

### Gaps Summary

No gaps. All four ROADMAP.md success criteria are satisfied:

1. **SC-1 (SelectedRuntime wiring):** Verified by source inspection (`lib.rs:45-65`, zero runtime `match`) and four successful feature builds.
2. **SC-2 (runtime initialization):** Verified by live rocm oracle (8/8 tests pass on gfx1100), live wgpu run (8/8 tests pass on host), and cuda compile RC=0.
3. **SC-3 (wave-size-agnostic primitives, D-03 structure):** Verified by source inspection (no literal 32/64 in strides; `CUBE_DIM_X`/`PLANE_DIM` drives all strides; Hillis-Steele cross-plane carry mirrors `inplace_scan.cuh`; atomic sibling mirrors CUDA in-kernel atomics) and live rocm oracle runs.
4. **SC-4 (self-oracle with reported tolerance):** Verified by live rocm tests printing `REPORTED max abs_div`/`rel_div` without hard-coding a tolerance; GPU-06 epsilon explicitly deferred to 7.6.

Three items are explicitly deferred to later phases (cross-cube scan carry → 7.2/7.3; f64 atomic non-determinism sign-off → 7.6; remaining GPU-01 surface → 7.2–7.5) and are documented as forward dependencies, not scope cuts.

---

## Code Review Gate

Two BLOCKERs from `07.1-REVIEW.md` (CR-01: silent wrong result for `n > CUBE_DIM`; CR-02: OOB shmem coupling) were both fixed in commit `8a3b1eb`:

- CR-01: `gpu_runtime.rs:163-168` — typed `CbError::Degenerate` returned for `n > CUBE_DIM`.
- CR-02: `gpu_runtime.rs:71-75` — `const _: () = assert!(CUBE_DIM <= crate::kernels::BLOCK_REDUCE_SHMEM, …)` compile-time coupling.

Both fixes confirmed present via source inspection. Post-fix rocm oracles (reduce 4/4, scan 4/4) confirmed in this verification run.

Five WARNINGs (WR-01..WR-05) and three INFOs remain as advisory latent items for 7.2–7.6; none block the 7.1 phase goal (all are non-gate-reachable at this scope).

---

_Verified: 2026-06-20_
_Verifier: Claude (gsd-verifier)_
