---
phase: 07.3-pointwise-histogram-family
plan: 03
type: execute
wave: 3
depends_on: ["07.3-01"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/pointwise_hist.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "A downstream consumer can fill the 2-channel pointwise histogram for half-byte (4-bit) features on rocm, device-resident, via a SEPARATE half-byte #[cube] kernel whose shared-memory layout / load-unroll structurally mirrors `pointwise_hist2_half_byte_template.cuh` (D-7.3-02 — distinct family, NOT a comptime case of the non-binary kernel)."
    - "The half-byte fill matches the ordered host-reference 2-channel histogram within the REPORTED divergence across the edge-case fixtures (D-7.3-04)."
    - "The half-byte kernel writes into the FROZEN Plan A binSums layout via the same der-handle-in -> binSums-handle-out seam (host dispatch extended with the half-byte family branch); the seam itself is unchanged (D-7.3-01 / Pitfall 2)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "pointwise_hist2_half_byte_kernel<F> (#[cube(launch)]) — distinct half-byte shared-mem layout + atomic merge"
      contains: "pointwise_hist2_half_byte_kernel"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_pointwise_hist2_handle half-byte family branch (HalfByteFeatures dispatch) — same seam, same binSums layout"
      contains: "half_byte"
    - path: "crates/cb-backend/src/kernels/pointwise_hist.rs"
      provides: "half_byte test (4-bit fixtures, edge cases) reusing the Plan A host reference + harness"
      contains: "half_byte"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle"
      to: "crates/cb-backend/src/kernels.rs::pointwise_hist2_half_byte_kernel"
      via: "cube launch for the HalfByteFeatures policy"
      pattern: "pointwise_hist2_half_byte_kernel::launch"
---

<objective>
Add the **half-byte (4-bit)** `pointwise_hist2` fill as a **separate** `#[cube]` kernel family — `ComputeHist2HalfByte` — whose shared-memory layout and load/unroll structurally mirror `pointwise_hist2_half_byte_template.cuh` (D-7.3-02: half-byte is structurally distinct, NOT a comptime case of the non-binary kernel). It plugs into the FROZEN Plan A binSums seam (der handles in → binSums handle out) via a half-byte branch on the host dispatch, and is self-oracled on rocm.

Purpose: The third MVP slice — after completion a consumer can fill the histogram for half-byte features they could not before. The Plan A seam, binSums layout, atomic-merge building block, and oracle harness are reused; only the new half-byte kernel body and its dispatch branch are added.
Output: `pointwise_hist2_half_byte_kernel`, the half-byte host-dispatch branch, the `half_byte` oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md

@.planning/phases/07.3-pointwise-histogram-family/07.3-CONTEXT.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-RESEARCH.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-PATTERNS.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-01-PLAN.md

# Upstream parity targets (structural model — D-01)
@catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_half_byte_template.cuh
@catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_half_byte.cu
@catboost-master/catboost/cuda/methods/pointwise_kernels.cpp

# In-repo analogs (Plan A's outputs + the atomic-merge building block)
@crates/cb-backend/src/kernels.rs
@crates/cb-backend/src/gpu_runtime.rs
@crates/cb-backend/src/kernels/pointwise_hist.rs

# CubeCL authoring rules (MANDATORY)
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (on any build error)
</context>

## Phase Goal

**As a** Rust developer embedding the GPU training path, **I want to** fill the device-resident 2-channel pointwise histogram for half-byte (4-bit) features via the structurally-faithful half-byte kernel, **so that** half-byte feature groups get a correct histogram through the same frozen seam with verifiable parity.

## Acceptance Criteria
- [ ] `cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::half_byte -- --nocapture` passes on gfx1100, reporting divergence.
- [ ] Plan A `nonbinary_8bit`/`handoff` and Plan B `nonbinary_bits` tests still pass (no seam regression).
- [ ] `wgpu` build+run of `half_byte`; `cuda` compile-only succeeds.
- [ ] `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged.
- [ ] The half-byte kernel is a SEPARATE `#[cube]` fn (not a comptime branch of the non-binary kernel); no warp-size literal; generics-float.

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: half_byte oracle (4-bit fixtures) — the failing slice test</name>
  <files>crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/pointwise_hist.rs (Plan A host_reference_hist2, max_divergence, fixtures, nonbinary_8bit body — clone the shape; half-byte = up to 16 bins)
    - .planning/phases/07.3-pointwise-histogram-family/07.3-VALIDATION.md § Per-Task Verification Map (7.3-C-* row: `kernels::pointwise_hist::half_byte`)
    - catboost-master/catboost/cuda/methods/pointwise_kernels.cpp lines 46-67 (HalfByteFeatures → ComputeHist2HalfByte dispatch)
  </read_first>
  <behavior>
    - half_byte test fails initially (kernel + dispatch branch absent): build fixtures with up to 16 bins, random f32+f64 der1, non-trivial weights, edge cases n=1/n=37/large-N, compute host_reference_hist2, and (once Task 2 lands) compare device vs reference within reported bounds.
    - Reuses the Plan A host reference + harness verbatim.
  </behavior>
  <action>Add a `half_byte` test fn to `kernels/pointwise_hist.rs` generating 4-bit (≤16 bin) fixtures (random f32+f64 der1, non-trivial weights, edge cases n=1/n=37/large-N), reusing Plan A's `host_reference_hist2`/`max_divergence`/fixtures, and calling `launch_pointwise_hist2_handle`/`launch_pointwise_hist2` selecting the half-byte family. Assert device vs host within reported bounds (f32 ~1e-3 / f64 ~1e-9) and print the REPORTED divergence. Compiles but FAILS until Task 2 adds the kernel + dispatch branch (RED). Test code MAY use `.unwrap()`/indexing.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::half_byte --no-run 2>&1 | tail -15</automated>
  </verify>
  <done>`half_byte` exists, compiles, uses ≤16-bin fixtures + edge cases, and reuses the Plan A host reference/harness; it fails only because the half-byte kernel + dispatch branch are not yet present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The half-byte #[cube] fill kernel + host-dispatch branch (makes half_byte green)</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs::pointwise_hist2_nonbinary_kernel (Plan A — the AddPoint loop + Atomic<F>::fetch_add merge + wave-agnostic in-block reduce idioms to reuse; the half-byte kernel has its OWN shared-mem layout) and block_reduce_atomic_kernel lines 661-730 (the atomic-merge building block) and the HIST_SHMEM const
    - crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle (Plan A — add a half-byte family branch; binSums layout/seam FROZEN, do NOT change)
    - catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_half_byte_template.cuh + pointwise_hist2_half_byte.cu (the TPointHistHalfByte shared-mem layout + Reduce + merge — the distinct structure to mirror)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md + Cubecl_shared_memory.md + Cubecl_plane.md
  </read_first>
  <behavior>
    - half_byte oracle goes GREEN: the half-byte device histogram matches host_reference_hist2 within reported bounds for n=1/n=37/large-N; empty short-circuits with NO launch/read-back.
    - The half-byte kernel is a SEPARATE #[cube] fn with the half-byte shared-mem layout (≤16-bin working histogram), wave-agnostic reduce, and atomic merge into the FROZEN binSums layout.
    - Plan A/B tests stay green (seam unchanged).
  </behavior>
  <action>Add `pointwise_hist2_half_byte_kernel<F: Float>` to `kernels.rs` as a SEPARATE `#[cube(launch)]` fn (NOT a comptime case of the non-binary kernel — D-7.3-02). Structurally mirror `pointwise_hist2_half_byte_template.cuh`: a half-byte-specific `SharedMemory<F>` working histogram (≤16 bins × 2 channels, sized from the `HIST_SHMEM` const used-prefix), zero-init, a bounds-guarded AddPoint loop reading `cindex[indices[i]]` → 4-bit bin and adding `der1[i]`/`weight[i]` to the two channels, the wave-agnostic in-block reduce (strides from `CUBE_DIM_X`/`PLANE_DIM` — re-express upstream's half-byte `& 31`/tile constructs wave-agnostically, Pitfall 4), and the `Atomic<F>::fetch_add` cross-block merge (plain write when single-block) into the FROZEN binSums layout. Add a half-byte family branch to `launch_pointwise_hist2_handle` (mirroring the `HalfByteFeatures` dispatch) that launches this kernel and writes into the same binSums buffer — the seam and layout are FROZEN from Plan A and MUST NOT change. Generics-float; no warp-size literal; no `unwrap`/`panic`/`[]` in production. On any CubeCL build error load `cubecl_error_guideline.md` first.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::half_byte -- --nocapture && cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::nonbinary_8bit -- --nocapture</automated>
  </verify>
  <done>`half_byte` passes on rocm reporting divergence; `nonbinary_8bit` (and Plan B `nonbinary_bits`, if run) stay green; the half-byte kernel is a separate `#[cube]` fn writing the FROZEN binSums layout; no warp-size literal.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-backend gate + SC-4 boundary re-assertion for the half-byte slice</name>
  <files>crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - .planning/phases/07.3-pointwise-histogram-family/07.3-VALIDATION.md § Sampling Rate (after-wave gate)
    - crates/cb-backend/src/kernels/gradient_gpu.rs (SelectedRuntime cross-backend pattern)
  </read_first>
  <action>Confirm `half_byte` builds+runs under `wgpu` on host and compiles under `cuda`. Re-run the SC-4 boundary gate: `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `cb-compute`/`cb-core`/`cb-model` byte-unchanged. Grep the half-byte kernel for forbidden warp-size literals. Fix any backend gap inside `cb-backend` only.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features wgpu && cargo test -p cb-backend --no-default-features --features wgpu kernels::pointwise_hist::half_byte -- --nocapture && cargo build -p cb-backend --no-default-features --features cuda && test "$(cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl)" -eq 0</automated>
  </verify>
  <done>wgpu builds and runs `half_byte`; cuda compiles; `cb-compute` cubecl count 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged; no warp-size literal in the half-byte kernel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch helper → device half-byte kernel | In-process buffer lengths/shapes cross here |
| device half-byte kernel → device binSums buffer | 4-bit bin index writes into the histogram buffer |

Pure in-process numeric GPU compute; NO external input, network, auth, secrets, file parsing, or untrusted data. Honest threat surface = correctness/safety invariants.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-10 | Tampering / DoS (UB) | out-of-bounds 4-bit bin or object index in the half-byte AddPoint loop | mitigate | Device `ABSOLUTE_POS < indices.len()` idle-guard; bin index masked to 4 bits and bounded by the comptime used-prefix |
| T-07.3-11 | Tampering / DoS (UB) | length mismatch among der1/weight/cindex/indices on the half-byte path | mitigate | Reuse Plan A's `CbError::LengthMismatch` host guards (unchanged seam) |
| T-07.3-12 | DoS | empty/zero-length read-back on the half-byte path | mitigate | Empty-input short-circuit (Plan A precedent), NO launch / NO read-back |
| T-07.3-13 | DoS | `unwrap`/`panic`/indexing in the half-byte dispatch branch | mitigate | D-13 / workspace lints ban them in production; tests use the `#[cfg(test)]` allow |
| T-07.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); only a new `#[cube]` kernel + dispatch branch |

**Block-on-high:** none. A separate kernel family adds no external input surface; bounds/length/empty hazards are mitigated by the same guards as Plan A.
</threat_model>

<verification>
- rocm: `half_byte` green reporting divergence; Plan A/B tests regression-green.
- wgpu: build + run `half_byte` on host.
- cuda: compile-only.
- SC-4: cb-compute cubecl count 0; cb-compute/cb-core/cb-model byte-unchanged.
- D-09/D-7.3-02: separate kernel family; no warp-size literal; generics-float.
</verification>

<success_criteria>
- The half-byte (4-bit) fill runs device-resident on rocm via a separate structurally-faithful kernel and matches the ordered host reference within reported tolerance.
- It writes the FROZEN Plan A binSums layout through the unchanged seam.
- Separate kernel family (not a comptime branch); no warp-size literal; generics-float.
- cb-compute cubecl-free; cb-core/cb-model byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/07.3-pointwise-histogram-family/07.3-03-SUMMARY.md` when done.
</output>
