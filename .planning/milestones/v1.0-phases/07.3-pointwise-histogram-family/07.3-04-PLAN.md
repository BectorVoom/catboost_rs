---
phase: 07.3-pointwise-histogram-family
plan: 04
type: execute
wave: 4
depends_on: ["07.3-01"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/pointwise_hist.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "A downstream consumer can fill the 2-channel pointwise histogram for binary (1-bit) features on rocm, device-resident, via a SEPARATE binary #[cube] kernel whose shared-memory layout / merge structurally mirrors `pointwise_hist2_binary.cu` (D-7.3-02 — distinct family)."
    - "The binary fill matches the ordered host-reference 2-channel histogram within the REPORTED divergence across the edge-case fixtures (D-7.3-04)."
    - "With the binary family landed, the FULL pointwise_hist2 family (8/5/6/7-bit non-binary + half-byte + binary) runs on rocm through the single frozen seam — SC-1 closed for this phase; the whole-family rocm suite is green (D-7.3-01)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "pointwise_hist2_binary_kernel<F> (#[cube(launch)]) — distinct binary shared-mem layout + atomic merge"
      contains: "pointwise_hist2_binary_kernel"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_pointwise_hist2_handle binary family branch (BinaryFeatures dispatch) — same seam, same binSums layout"
      contains: "binary"
    - path: "crates/cb-backend/src/kernels/pointwise_hist.rs"
      provides: "binary test (1-bit fixtures, edge cases) + a whole-family rocm assertion reusing the Plan A host reference + harness"
      contains: "binary"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle"
      to: "crates/cb-backend/src/kernels.rs::pointwise_hist2_binary_kernel"
      via: "cube launch for the BinaryFeatures policy"
      pattern: "pointwise_hist2_binary_kernel::launch"
---

<objective>
Add the **binary (1-bit)** `pointwise_hist2` fill as the third **separate** `#[cube]` kernel family — `ComputeHist2Binary` — structurally mirroring `pointwise_hist2_binary.cu` (D-7.3-02), plugging into the FROZEN Plan A binSums seam via a binary branch on the host dispatch, self-oracled on rocm. With binary landed, the **full** `pointwise_hist2` family runs on rocm — SC-1 closed for this phase.

Purpose: The final MVP slice — after completion a consumer can fill the histogram for binary features they could not before, completing the variant set (8/5/6/7-bit non-binary + half-byte + binary). Reuses the Plan A seam, binSums layout, atomic-merge building block, and oracle harness; adds only the binary kernel body + dispatch branch and a whole-family rocm regression assertion.
Output: `pointwise_hist2_binary_kernel`, the binary host-dispatch branch, the `binary` oracle test + a whole-family rocm assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md

@.planning/phases/07.3-pointwise-histogram-family/07.3-CONTEXT.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-RESEARCH.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-PATTERNS.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-01-PLAN.md

# Upstream parity targets (structural model — D-01)
@catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_binary.cu
@catboost-master/catboost/cuda/methods/pointwise_kernels.cpp

# In-repo analogs (Plan A's outputs + the atomic-merge building block)
@crates/cb-backend/src/kernels.rs
@crates/cb-backend/src/gpu_runtime.rs
@crates/cb-backend/src/kernels/pointwise_hist.rs

# CubeCL authoring rules (MANDATORY)
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (on any build error)
</context>

## Phase Goal

**As a** Rust developer embedding the GPU training path, **I want to** fill the device-resident 2-channel pointwise histogram for binary (1-bit) features via the structurally-faithful binary kernel, **so that** the FULL pointwise_hist2 family runs on rocm through one frozen seam with verifiable parity — SC-1 satisfied.

## Acceptance Criteria
- [ ] `cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::binary -- --nocapture` passes on gfx1100, reporting divergence.
- [ ] The whole histogram suite — `cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist -- --nocapture` (nonbinary_8bit, nonbinary_bits, half_byte, binary, handoff) — is green (SC-1 / SC-2).
- [ ] `wgpu` build+run of `binary`; `cuda` compile-only succeeds.
- [ ] `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged.
- [ ] The binary kernel is a SEPARATE `#[cube]` fn; no warp-size literal; generics-float.

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: binary oracle (1-bit fixtures) — the failing slice test</name>
  <files>crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/pointwise_hist.rs (Plan A host_reference_hist2, max_divergence, fixtures, nonbinary_8bit body — clone the shape; binary = 2 bins)
    - .planning/phases/07.3-pointwise-histogram-family/07.3-VALIDATION.md § Per-Task Verification Map (7.3-D-* row: `kernels::pointwise_hist::binary`)
    - catboost-master/catboost/cuda/methods/pointwise_kernels.cpp lines 46-67 (BinaryFeatures → ComputeHist2Binary dispatch)
  </read_first>
  <behavior>
    - binary test fails initially (kernel + dispatch branch absent): build 2-bin fixtures, random f32+f64 der1, non-trivial weights, edge cases n=1/n=37/large-N, compute host_reference_hist2, and (once Task 2 lands) compare device vs reference within reported bounds.
    - Reuses the Plan A host reference + harness verbatim.
  </behavior>
  <action>Add a `binary` test fn to `kernels/pointwise_hist.rs` generating 1-bit (2-bin) fixtures (random f32+f64 der1, non-trivial weights, edge cases n=1/n=37/large-N), reusing Plan A's `host_reference_hist2`/`max_divergence`/fixtures, calling `launch_pointwise_hist2_handle`/`launch_pointwise_hist2` selecting the binary family. Assert device vs host within reported bounds (f32 ~1e-3 / f64 ~1e-9) and print the REPORTED divergence. Compiles but FAILS until Task 2 adds the kernel + dispatch branch (RED). Test code MAY use `.unwrap()`/indexing.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::binary --no-run 2>&1 | tail -15</automated>
  </verify>
  <done>`binary` exists, compiles, uses 2-bin fixtures + edge cases, and reuses the Plan A host reference/harness; it fails only because the binary kernel + dispatch branch are not yet present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The binary #[cube] fill kernel + host-dispatch branch + whole-family assertion (makes binary + suite green)</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs, crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs::pointwise_hist2_nonbinary_kernel + pointwise_hist2_half_byte_kernel (Plan A/C — the AddPoint loop + Atomic<F>::fetch_add merge + wave-agnostic reduce idioms to reuse; binary has its OWN shared-mem layout) + block_reduce_atomic_kernel lines 661-730 + the HIST_SHMEM const
    - crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle (Plan A — add a binary family branch; binSums layout/seam FROZEN, do NOT change)
    - catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_binary.cu (the binary TPointHistHalfByte-based shared-mem layout + merge — the distinct structure to mirror)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md + Cubecl_shared_memory.md + Cubecl_plane.md
  </read_first>
  <behavior>
    - binary oracle goes GREEN: the binary device histogram matches host_reference_hist2 within reported bounds for n=1/n=37/large-N; empty short-circuits with NO launch/read-back.
    - The binary kernel is a SEPARATE #[cube] fn with the binary shared-mem layout (2-bin working histogram), wave-agnostic reduce, atomic merge into the FROZEN binSums layout.
    - The whole-family rocm suite (nonbinary_8bit, nonbinary_bits, half_byte, binary, handoff) is green (SC-1).
  </behavior>
  <action>Add `pointwise_hist2_binary_kernel<F: Float>` to `kernels.rs` as a SEPARATE `#[cube(launch)]` fn (NOT a comptime case of the non-binary/half-byte kernels — D-7.3-02). Structurally mirror `pointwise_hist2_binary.cu`: a binary-specific `SharedMemory<F>` working histogram (2 bins × 2 channels, sized from the `HIST_SHMEM` used-prefix), zero-init, a bounds-guarded AddPoint loop reading `cindex[indices[i]]` → 1-bit bin and adding `der1[i]`/`weight[i]` to the two channels, the wave-agnostic in-block reduce (strides from `CUBE_DIM_X`/`PLANE_DIM` — re-express upstream's binary `& 31`/tile constructs wave-agnostically, Pitfall 4), and the `Atomic<F>::fetch_add` cross-block merge (plain write when single-block) into the FROZEN binSums layout. Add a binary family branch to `launch_pointwise_hist2_handle` (mirroring the `BinaryFeatures` dispatch) that launches this kernel and writes into the same binSums buffer — the seam and layout are FROZEN from Plan A and MUST NOT change. Add a whole-family assertion in `kernels/pointwise_hist.rs` (or confirm the module path) so the full suite `kernels::pointwise_hist` exercises all variants. Generics-float; no warp-size literal; no `unwrap`/`panic`/`[]` in production. On any CubeCL build error load `cubecl_error_guideline.md` first.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::binary -- --nocapture && cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist -- --nocapture</automated>
  </verify>
  <done>`binary` passes on rocm reporting divergence; the whole `kernels::pointwise_hist` suite (all variants + handoff) is green; the binary kernel is a separate `#[cube]` fn writing the FROZEN binSums layout; no warp-size literal.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-backend gate + final SC-4 boundary assertion (phase close)</name>
  <files>crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - .planning/phases/07.3-pointwise-histogram-family/07.3-VALIDATION.md § Sampling Rate (phase-gate row: full cb-backend rocm suite + cargo tree gate + git diff --stat)
    - crates/cb-backend/src/kernels/gradient_gpu.rs (SelectedRuntime cross-backend pattern)
  </read_first>
  <action>Confirm `binary` and the full histogram suite build+run under `wgpu` on host and compile under `cuda`. Run the final SC-4 phase gate: `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `cb-compute`/`cb-core`/`cb-model` byte-unchanged. Grep the binary kernel (and confirm all three families) for forbidden warp-size literals. Fix any backend gap inside `cb-backend` only.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features wgpu && cargo test -p cb-backend --no-default-features --features wgpu kernels::pointwise_hist -- --nocapture && cargo build -p cb-backend --no-default-features --features cuda && test "$(cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl)" -eq 0</automated>
  </verify>
  <done>wgpu builds and runs the full histogram suite; cuda compiles; `cb-compute` cubecl count 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged; no warp-size literal in any of the three kernel families; SC-1/SC-2/SC-3/SC-4 satisfied for the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch helper → device binary kernel | In-process buffer lengths/shapes cross here |
| device binary kernel → device binSums buffer | 1-bit bin index writes into the histogram buffer |

Pure in-process numeric GPU compute; NO external input, network, auth, secrets, file parsing, or untrusted data. Honest threat surface = correctness/safety invariants.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-14 | Tampering / DoS (UB) | out-of-bounds 1-bit bin or object index in the binary AddPoint loop | mitigate | Device `ABSOLUTE_POS < indices.len()` idle-guard; bin index masked to 1 bit and bounded by the comptime used-prefix |
| T-07.3-15 | Tampering / DoS (UB) | length mismatch among der1/weight/cindex/indices on the binary path | mitigate | Reuse Plan A's `CbError::LengthMismatch` host guards (unchanged seam) |
| T-07.3-16 | DoS | empty/zero-length read-back on the binary path | mitigate | Empty-input short-circuit (Plan A precedent), NO launch / NO read-back |
| T-07.3-17 | DoS | `unwrap`/`panic`/indexing in the binary dispatch branch | mitigate | D-13 / workspace lints ban them in production; tests use the `#[cfg(test)]` allow |
| T-07.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); only a new `#[cube]` kernel + dispatch branch |

**Block-on-high:** none. The binary family adds no external input surface; bounds/length/empty hazards are mitigated by the same guards as Plan A.
</threat_model>

<verification>
- rocm: `binary` green reporting divergence; the full `kernels::pointwise_hist` suite (all variants + handoff) green (SC-1/SC-2).
- wgpu: build + run the full histogram suite on host.
- cuda: compile-only.
- SC-4: cb-compute cubecl count 0; cb-compute/cb-core/cb-model byte-unchanged.
- D-09/D-7.3-02: three separate kernel families; no warp-size literal; generics-float.
</verification>

<success_criteria>
- The binary (1-bit) fill runs device-resident on rocm via a separate structurally-faithful kernel and matches the ordered host reference within reported tolerance.
- The FULL pointwise_hist2 family (8/5/6/7-bit non-binary + half-byte + binary) runs on rocm through one frozen seam — SC-1 closed.
- Three separate kernel families; no warp-size literal; generics-float.
- cb-compute cubecl-free; cb-core/cb-model byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/07.3-pointwise-histogram-family/07.3-04-SUMMARY.md` when done.
</output>
