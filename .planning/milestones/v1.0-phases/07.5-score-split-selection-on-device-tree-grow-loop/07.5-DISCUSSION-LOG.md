# Phase 7.5: Score/Split Selection & On-Device Tree-Grow Loop - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-20
**Phase:** 7.5-Score/Split Selection & On-Device Tree-Grow Loop
**Mode:** `--auto` (no interactive prompts; every gray area auto-resolved to the recommended default)
**Areas discussed:** Score/split scope, Device-resident loop orchestration (research-flag), Scan/update integration, Boundary placement, CPU cross-oracle, Non-determinism budget, MVP path scope, Verification

---

## Score-calcer + split-selection scope (SC-1)

| Option | Description | Selected |
|--------|-------------|----------|
| Port score_calcers.cuh (L2/Cosine + Solar/LOO/Sat) + ComputeOptimalSplits + pairwise scorer; narrowest-first | Mirror upstream; CPU score.rs is the oracle | ✓ |
| L2 only, defer the rest | Minimal scorer | |
| All upstream score fns incl. GPU-only ones | Beyond the CPU reference set | |

**Auto-selected:** Port the score set in CPU `score.rs` + split selection + pairwise scorer, narrowest-first (recommended default) → **D-7.5-01**
**Notes:** CPU `cb-compute/src/score.rs` (`l2_split_score`/`cosine_split_score`/`multi_dim_split_score`) is the bit-level parity oracle. Pairwise scorer consumes the 7.4 4-channel handle.

---

## Device-resident grow-loop orchestration (D-05 — RESEARCH-FLAGGED)

| Option | Description | Selected |
|--------|-------------|----------|
| Full device-resident loop, persistent buffers, minimal round-trips; mechanism → RESEARCH | Honors D-05; argmax/partition device-vs-host left to research | ✓ |
| Host-orchestrated per-kernel offload hybrid | Violates D-05 / SC-2 | |
| Partial loop (host keeps partition/argmax) | Hybrid; not the mandate | |

**Auto-selected:** Full device-resident loop; exact CubeCL orchestration → RESEARCH (recommended default) → **D-7.5-02**
**Notes:** D-05 is explicit ("not a kernel-offload hybrid with host orchestration"). The launch-graph / persistent-buffer / argmax-partition mechanism is the roadmap research-flag item.

---

## Scan/update integration (deferred from 7.3 + 7.4)

| Option | Description | Selected |
|--------|-------------|----------|
| Land Scan/Update Pointwise + Pairwise here as FILL→scorer bridge | Mirror pointwise_scores.cu / split_pairwise.cu | ✓ |
| Re-defer to a later phase | Leaves the FILL handles unconsumed | |

**Auto-selected:** Integrate the deferred scan/update here (recommended default) → **D-7.5-03**
**Notes:** These are exactly the transforms 7.3 and 7.4 deferred; consume the FROZEN 2-channel / 4-channel handles unchanged.

---

## Boundary placement & additive constraint (SC-4)

| Option | Description | Selected |
|--------|-------------|----------|
| Device loop self-contained in cb-backend; cb-core/cb-model/cb-compute byte-unchanged; cb-train read-only oracle | Carry the umbrella cubecl-in-cb-backend boundary | ✓ |
| Wire the device loop into cb-train now | Entangles trainer with cubecl | |

**Auto-selected:** Self-contained in cb-backend, cb-train read-only oracle (recommended default) → **D-7.5-04**
**Notes:** SC-4 freezes cb-core/cb-model/cb-compute (not cb-train), but the umbrella keeps all cubecl in cb-backend. A cubecl-free cb-train hook is deferred.

---

## CPU cross-oracle (SC-3)

| Option | Description | Selected |
|--------|-------------|----------|
| Cross-oracle GPU tree vs cb-train CPU path: per-tree structure exact + leaf values within reported tol; REPORT | Same fixture/seed; multi-tree + pairwise fixture | ✓ |
| Sign off GPU-06 epsilon now | That is 7.6's job | |
| Structure-only check | Misses leaf-value parity | |

**Auto-selected:** Structure-exact + leaf-value-within-tolerance, REPORT divergence (recommended default) → **D-7.5-05**
**Notes:** Oracle against `cb-train/src/tree.rs` + `cb-compute` leaf/score. GPU-06 sign-off deferred to 7.6.

---

## Non-determinism budget across a multi-tree run (RESEARCH-FLAGGED)

| Option | Description | Selected |
|--------|-------------|----------|
| Structure (integer splits) exact; leaf values within reported float tol; characterize any argmax-flip boundary | Mirror upstream determinism (f64 finalize, stable tie-break) | ✓ |
| Allow structure drift within tolerance | Weakens the parity bar | |

**Auto-selected:** Structure exact, leaf values within tolerance, REPORT the flip boundary (recommended default) → **D-7.5-06**
**Notes:** The roadmap research-flag's second item — atomic noise compounding across a multi-tree run.

---

## MVP path scope + Verification

| Option | Description | Selected |
|--------|-------------|----------|
| MVP = symmetric oblivious + Plain boosting; per-crate verification (rocm run, wgpu run, cuda build-only) | Match CPU default path; defer non-symmetric/ordered GPU | ✓ |
| Include non-symmetric/ordered GPU grow now | Beyond the MVP parity surface | |

**Auto-selected:** Symmetric-oblivious Plain MVP + per-crate verification (recommended default) → **D-7.5-07** (+ scope note in domain)
**Notes:** Non-symmetric trees / ordered-boosting GPU paths are additive follow-ons.

---

## Claude's Discretion

- Exact CubeCL device-loop orchestration (launch-graph, persistent buffer lifetimes, argmax/partition device-vs-host) — the research-flag item.
- Precise score-function subset to land in the MVP first plan beyond L2/Cosine.
- Candidate-generation device representation; leaf-value estimation kernel design.
- Representative fixture set + multi-tree count.
- Whether a cubecl-free cb-train integration hook is introduced.

## Deferred Ideas

- GPU-06 epsilon sign-off + rocm oracle-gate lock-in — Phase 7.6.
- Non-symmetric trees + ordered-boosting GPU grow path — additive follow-on.
- GPU CTR/text/embedding paths — CPU remains reference.
- cubecl-free cb-train integration hook — deferred.

## Reviewed Todos (not folded)

- **"Estimated-feature stored-border-VALUE quantization-grid parity"** (score 0.4) — reviewed, NOT folded. CPU KNN/online-HNSW subsystem residual from Phase 6.5/6.6; unrelated to GPU score/split/grow-loop. Weak keyword-only match; folding would be cross-boundary scope creep.
