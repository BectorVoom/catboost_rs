---
phase: "07.5"
fixed_at: 2026-06-21T00:00:00Z
review_path: .planning/phases/07.5-score-split-selection-on-device-tree-grow-loop/07.5-REVIEW.md
iteration: 2
findings_in_scope: 11
fixed: 11
skipped: 0
status: all_fixed
rocm_reconfirmed: true
rocm_result: "75/75 passed on gfx1100 (HipRuntime)"
---

# Phase 07.5: Code Review Fix Report

**Fixed at:** 2026-06-21
**Source review:** `.planning/phases/07.5-score-split-selection-on-device-tree-grow-loop/07.5-REVIEW.md`
**Iteration:** 2

**Summary:**
- Findings in scope: 11 (fix_scope = `all`: 6 warning + 5 info)
- Fixed: 11
- Skipped: 0

Iteration 1 fixed 7 of 11 and skipped 4 (WR-05, IN-02, IN-03, IN-04). Iteration 2
(this pass) addressed all four previously-skipped findings on explicit direction.

All fixes were applied and verified with `cargo check -p cb-backend` against the
default `cpu` feature, the `--no-default-features --features wgpu` cfg (for
backend-conditional code), and `--tests` (for the test-only changes).

**ROCm re-confirmation (iteration 2, in-env gfx1100 / HipRuntime):** the full
`cargo test -p cb-backend --no-default-features --features rocm` suite was executed
on the real GPU. This caught a REGRESSION in the iter-1 WR-01 fix: seeding the device
argmin with `F::new(f32::NEG_INFINITY)` made CubeCL emit a bare `double(-inf)` literal
that the HIP/comgr compiler rejects on gfx1100 (`use of undeclared identifier 'inf'`),
failing the `find_optimal_split` / `select_best_split` kernel JIT for all calcers —
**16/75 tests failed**. WR-01 was corrected to the HIP-safe finite `f32::MIN` sentinel
(commit `7248d38`; see the revised WR-01 entry). After the correction the suite is
**75/75 passing on gfx1100**, which also discharges the on-device numeric validation
for WR-05 and IN-02 (their tests exercise the same kernel). The earlier iter-1 kernel
fixes WR-03 and WR-04 are likewise covered by the now-passing pairwise/partition tests.

## Fixed Issues

### WR-06: `max_divergence` truncates on length mismatch under release profile

**Files modified:** `crates/cb-backend/src/kernels/score_split.rs`, `crates/cb-backend/src/kernels/grow_loop.rs`
**Commit:** `675c4d8`
**Applied fix:** Replaced the `debug_assert_eq!(device.len(), baseline.len())` (compiled
out under release) with an unconditional length check in both copies of the helper; a
mismatch now returns a sentinel `(f64::INFINITY, f64::INFINITY)` divergence so any caller
threshold check fails loudly instead of silently comparing only the common prefix.
Test-only helper; verified via `cargo check -p cb-backend --tests` (cpu).

### WR-04: `partition_update_kernel` unguarded `part_stats[leaf_of[obj]*2]` store

**Files modified:** `crates/cb-backend/src/kernels.rs`
**Commit:** `cbd6c64`
**Applied fix:** Added an in-kernel `if part * 2 + 1 < part_stats.len()` guard around the
two atomic `fetch_add` stores (matching the scan kernel's `cell < bin_sums.len()`
precedent at `kernels.rs:2131`), and corrected the kernel doc which had claimed a
host-side `leaf_of < n_parts` validation that does not actually run (`leaf_of` is produced
device-side by `partition_split_kernel` with no read-back). Pure additive defensive guard;
for depth==1 (the MVP path) the index is always in bounds so it is a no-op for the verified
oracle. **VERIFIED on-device: rocm 75/75 on gfx1100** (partition/grow-loop tests pass).

### WR-01: Finite `f32::MIN` argmin sentinel can drop a real candidate for negative-score calcers

**Files modified:** `crates/cb-backend/src/kernels.rs`
**Commits:** `3cb6b00` (iter-1, superseded), `7248d38` (iter-2 correction)
**Applied fix:** Iter-1 changed the argmin seed from `F::new(f32::MIN)` to
`F::new(f32::NEG_INFINITY)` in `find_optimal_split_kernel` and `select_best_split_kernel`
to match the CPU oracle's `f64::NEG_INFINITY` and close a latent device-vs-CPU argmin
disagreement for SolarL2/LOOL2/SatL2 candidates more negative than `f32::MIN`.

**ROCm re-confirmation FAILED this iter-1 form (16/75 tests), now corrected.** CubeCL
emits the `-inf` seed as a bare `double(-inf)` literal that the HIP/comgr compiler rejects
on gfx1100 (`error: use of undeclared identifier 'inf'`), failing the kernel JIT for every
calcer. Reverted (commit `7248d38`) to the HIP-safe finite `F::new(f32::MIN)` (~-3.4e38),
which is below every realizable split score (L2/Cosine >=0; Solar/LOO/Sat are gradient-
derived and bounded, never near -3.4e38) and below every other lane's gain (so unseen
lanes still lose the shared-memory reduction) — behaviorally identical to
`f64::NEG_INFINITY` for all reachable inputs. The host-side CPU oracle keeps
`f64::NEG_INFINITY` (CPU compiles it fine). The (unreachable) sub-`f32::MIN` tail would
need a per-lane "seen" flag threaded through the reduction; deferred as it has no reachable
effect. **VERIFIED on-device: rocm 75/75 on gfx1100.**

### WR-03 / IN-01: Pairwise winner reported f32-reduced gain; decorative device round-trip

**Files modified:** `crates/cb-backend/src/gpu_runtime.rs`
**Commit:** `b5fc434`
**Applied fix:** In `select_best_split_over_scores`, the across-block argmax now compares
and tie-breaks over the exact host-resident f64 `scores` Vec (looked up by the candidate
index each device block nominates) instead of the device-reduced `best_gains` (which run
in f32 on wgpu and could collapse two host-distinct f64 scores into one f32 tie, flipping
the split versus the CPU oracle's strict-`>` over f64). The device-reduced gains are now
explicitly a structural-only artifact (`let _ = &best_gains;`), addressing IN-01's
"decorative round-trip" observation without removing the device kernel (which the review
framed as an optional larger refactor). **VERIFIED on-device: rocm 75/75 on gfx1100**
(`grow_loop::pairwise::matches_cpu_pairwise_grow` and `score_split::pairwise` pass).

### WR-02: `launch_der_*_into` launches `f64` unconditionally; breaks on a true wgpu backend

**Files modified:** `crates/cb-backend/src/gpu_runtime.rs`
**Commit:** `62e36d4`
**Applied fix:** Took the reviewer-sanctioned typed-reject alternative. The der binary,
unary, and parametric `_into` seams are f64-typed end-to-end (handle layout,
`empty(n * size_of::<f64>())`, `cast_slice::<u8, f64>` read-backs, and the
`grow_boosting_pass_into` f64 read-back); WGSL has no `f64` type so a genuine wgpu backend
cannot JIT them. Each seam now returns a descriptive `CbError::OutOfRange` under
`#[cfg(feature = "wgpu")]` (before any handle is created) instead of surfacing an opaque
JIT crash. `#[cfg_attr(feature = "wgpu", allow(unused_variables, unreachable_code))]` keeps
the wgpu build warning-clean without affecting the rocm/cuda/cpu f64 path. Verified to
compile cleanly under BOTH `cpu` (default) and `--features wgpu`.

> Note: the full f32-channel split (mirroring the histogram seams) was deliberately NOT
> taken — it would touch the der upload, launch, read-back, AND the
> `grow_boosting_pass_into` f64 read-back across the 7.2-owned der path, is unverifiable
> in-env (no wgpu device run; rocm is f64-only), and a partial version would leave wgpu in
> a subtly-wrong state worse than an honest typed reject.

### IN-05: Scan seam implicit precondition

**Files modified:** `crates/cb-backend/src/kernels.rs`
**Commit:** `228bc5a`
**Applied fix:** Added a "# Precondition — `bin_sums` validity" section to the
`scan_update_pointwise_kernel` doc comment documenting that the kernel consumes an
already-filled `bin_sums` and relies implicitly on the fill path's `cindex` value-range
guards (in `launch_scan_update_pointwise_into`); any future external `bin_sums` caller
must enforce the same preconditions. Doc-only; verified to compile.

### WR-05: Trailing `border = n_bins-1` no-op split is scored and argmin-eligible (pointwise)

**Files modified:** `crates/cb-backend/src/kernels.rs`,
`crates/cb-backend/src/gpu_runtime.rs` (now `gpu_runtime/mod.rs`),
`crates/cb-backend/src/kernels/score_split.rs`,
`crates/cb-backend/src/kernels/grow_loop.rs`
**Commit:** `af6757e` (iteration 2)
**Applied fix:** Took the upstream-correct fix and applied it in EXACT lockstep across
device and host. The device `find_optimal_split_kernel` argmin now skips the trailing
`border == n_bins - 1` candidate (the no-op: all bins LEFT / none RIGHT, which upstream
and the pairwise path `n_splits = n_bins - 1` never enumerate) via a `border < n_bins - 1`
guard, while STILL writing `scores[c]` for that slot — so the per-candidate `scores`
buffer geometry (`n_features * n_bins`, the `feature * n_bins + bin` index decode, the
element-wise `max_divergence` oracle compare) is unchanged and no `-inf` sentinel (which
would NaN under that compare) is introduced. The host winner decode (`gpu_runtime` across-
block argmin), the host reference winner `score_split::reference_best_split`, and the
grow-loop CPU references `grow_loop::cpu_best_stump` / `cpu_best_stump_cosine` all skip the
SAME trailing border in lockstep, so device and CPU oracle agree on a real split. Updated
the kernel doc. **VERIFIED on-device: rocm 75/75 on gfx1100** — the L2/Cosine/Solar/LOO/Sat
`*_matches_cpu_oracle`, `argmin_*`, and grow-loop greedy-search tests all pass with the
trailing border excluded in lockstep on device and CPU oracle.

### IN-02: `find_optimal_split_kernel` copy-paste calcer arms

**Files modified:** `crates/cb-backend/src/kernels.rs`
**Commit:** `ba6d215` (iteration 2)
**Applied fix:** Behavior-preserving extraction of the five copy-pasted comptime calcer
arms into two shared `#[cube]` device helpers: `cb_leaf_avg` (the `count>0`-guarded
`calc_average`) and `cb_leaf_score_term` (the per-leaf additive score contribution for the
comptime-selected calcer). The kernel now calls the term helper once per leaf (left then
right, same order) and sums; the Cosine denominator (seed-first `1e-100 + avg²·weight`) is
folded in the kernel via `cb_leaf_avg`. Each `#[comptime] score_fn` branch is JIT-resolved
and every arm is transcribed verbatim from the prior inlined code (same operand order,
same guards, no `scaled_l2` for Solar/LOO/Sat), so the monomorphized kernel body computes
identical values — no numeric logic changed. Compiles under cpu, wgpu, and `--tests`.
**VERIFIED on-device: rocm 75/75 on gfx1100** — all five `*_matches_cpu_oracle` calcer
tests pass against the CPU oracle with the refactored `#[cube]` body.

### IN-03: `gpu_runtime.rs` size

**Files modified:** `crates/cb-backend/src/gpu_runtime.rs` (deleted),
`crates/cb-backend/src/gpu_runtime/mod.rs` (new),
`crates/cb-backend/src/gpu_runtime/der_seams.rs` (new),
`crates/cb-backend/src/gpu_runtime/pairwise.rs` (new)
**Commit:** `f78025f` (iteration 2)
**Applied fix:** Pure mechanical move splitting the 4473-line module into a directory
module: `mod.rs` (2043 lines — 7.1 reduce/scan primitives, 7.3 pointwise-histogram seam,
7.5 pointwise split/score + scan/update seam, partition primitives, pointwise grow-loop +
boosting drivers) plus two relocated leaf submodules `der_seams.rs` (572 lines, Phase 7.2
der1/der2 seam) and `pairwise.rs` (1898 lines, Phase 7.4/7.5 pairwise histogram fill,
scan/update, make-derivatives, split score, pairwise grow driver). Every public item is
re-exported (`pub use der_seams::*` / `pub use pairwise::*`) so the crate-internal
`crate::gpu_runtime::X` API surface is IDENTICAL and all existing `use` paths still
resolve; submodules import shared parent items via `use super::*`. The ONLY visibility
change is `launch_der_binary_into` private → `pub(crate)` (the boosting pass in `mod.rs`
calls it across the new module boundary) — still crate-internal, no external API change.
ZERO logic changes; behavior preserved by construction and fully validated by `cargo check`
(cpu, wgpu, `--tests`, wgpu `--tests`) with zero errors and zero warnings.

### IN-04: Duplicated fixture builders

**Files modified:** `crates/cb-backend/src/kernels.rs`,
`crates/cb-backend/src/kernels/test_fixtures.rs` (new),
`crates/cb-backend/src/kernels/score_split.rs`,
`crates/cb-backend/src/kernels/grow_loop.rs`
**Commit:** `4d83fbb` (iteration 2)
**Applied fix:** Added `kernels/test_fixtures.rs` with parameterized `#[cfg(test)]`
primitives (`ramp_centred`, `weight_mod5`, `cindex_feature_major`, `indices_identity`,
`competitor_pairs`) and recomposed the five distinct builders (`make_fixture`,
`make_boosting_fixture`, `make_ranking_fixture`, `make_score_fixture`,
`make_pairwise_fixture`) from them. Each primitive is a literal extraction of the
per-channel construction the builders already shared, so every builder emits byte-identical
oracle inputs — the builders keep their distinct names and distinct overall shapes (der1-
vs target-ramp, with/without a weight channel, with/without the competitor-pair list); the
genuinely-shared substructure is factored, the genuinely-distinct parts preserved (no
oracle input changed). Compiles under cpu, wgpu, and `--tests`. Behavior preserved by
construction (byte-identical channel-construction primitives).

---

_Fixed: 2026-06-21 (iteration 2; iteration 1 fixed 7/11)_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
