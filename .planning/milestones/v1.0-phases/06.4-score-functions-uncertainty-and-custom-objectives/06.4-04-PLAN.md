---
phase: 06.4-score-functions-uncertainty-and-custom-objectives
plan: 04
type: execute
wave: 4
depends_on: ["06.4-02"]
files_modified:
  - crates/cb-compute/src/custom.rs
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-backend/src/cpu_runtime.rs
  - crates/cb-train/src/metrics.rs
  - crates/catboost-rs/src/builder.rs
  - crates/cb-compute/tests/custom_objective_test.rs
autonomous: true
requirements: [LOSS-07]
must_haves:
  truths:
    - "CustomObjective (calc_ders_range mirroring Python calc_ders_range(approxes, targets, weights) -> [(der1, der2)]) and CustomMetric (evaluate -> (error, weight) + get_final_error + is_max_optimal) traits exist, return CbResult (thiserror, no unwrap/anyhow), Send+Sync (D-6.4-05)"
    - "Loss::Custom(CustomObjectiveHandle) plugs an Arc<dyn CustomObjective> into the non-Copy Loss enum via a newtype with manual Clone + Debug + Arc::ptr_eq PartialEq (workspace's first Arc<dyn> trait object)"
    - "Adding EvalMetric::Custom forces a one-time EvalMetric Copy-drop (mirrors the 6.2 Loss Copy-drop); the cross-crate by-value→borrow/clone ripple is resolved"
    - "Self-oracle: a built-in loss (Logloss and/or RMSE) reimplemented AS a Rust CustomObjective reproduces that built-in's EXISTING per-stage oracle ≤1e-5 — proves trait dispatch is faithful with no new upstream fixture"
    - "The trait is exposed on the catboost-rs builder facade designed so the Phase-8 PyO3 callback wraps the SAME trait; PyO3 is DEFERRED to Phase 8 (D-09) and NOT implemented here"
  artifacts:
    - path: "crates/cb-compute/src/custom.rs"
      provides: "CustomObjective + CustomMetric traits + CustomObjectiveHandle/CustomMetricHandle newtypes"
      contains: "trait CustomObjective"
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "Loss::Custom(CustomObjectiveHandle)"
      contains: "Custom"
    - path: "crates/cb-train/src/metrics.rs"
      provides: "EvalMetric::Custom + Copy-drop"
      contains: "Custom"
    - path: "crates/cb-compute/tests/custom_objective_test.rs"
      provides: "Logloss/RMSE-as-custom self-oracle ≤1e-5"
  key_links:
    - from: "crates/cb-backend/src/cpu_runtime.rs"
      to: "crates/cb-compute/src/custom.rs"
      via: "compute_gradients Loss::Custom(h) => h.0.calc_ders_range(...)"
      pattern: "Loss::Custom"
    - from: "crates/catboost-rs/src/builder.rs"
      to: "crates/cb-compute/src/custom.rs"
      via: "builder accepts Arc<dyn CustomObjective>/Arc<dyn CustomMetric> (Phase-8 PyO3 wraps the same trait)"
      pattern: "custom_objective|custom_metric"
---

<objective>
Wave C (LOSS-07, Rust half): define the `CustomObjective`/`CustomMetric` Rust trait pair, plug them
into the one `Loss`/`EvalMetric` dispatch via an `Arc<dyn>` newtype seam, and prove the dispatch is
faithful by SELF-ORACLE — reimplement a built-in loss (Logloss/RMSE) as a Rust custom objective and
match its EXISTING per-stage oracle ≤1e-5. End-to-end slice: a Rust user supplies der1/der2 + eval and
trains/evaluates through the custom path.

Purpose: deliver the LOSS-07 Rust trait designed for a clean Phase-8 PyO3 wrap (D-09 — Python callback
bridge DEFERRED, NOT implemented here), via the single-code-path dispatch (D-6.2-03).

Output: `crates/cb-compute/src/custom.rs` (trait pair + handles), `Loss::Custom`, `EvalMetric::Custom`
(+ the one-time `EvalMetric` Copy-drop), the `compute_gradients`/`EvalMetric::eval` dispatch arms, the
builder surface, and `crates/cb-compute/tests/custom_objective_test.rs` (self-oracle).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-CONTEXT.md
@.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-RESEARCH.md
@.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-PATTERNS.md
@crates/cb-compute/src/runtime.rs
@crates/cb-compute/src/loss.rs
@crates/cb-train/src/metrics.rs
@crates/catboost-rs/src/builder.rs
</context>

<artifacts_this_phase_produces>
- `crates/cb-compute/src/custom.rs` (NEW): `CustomObjective` + `CustomMetric` traits;
  `CustomObjectiveHandle(Arc<dyn CustomObjective>)` + `CustomMetricHandle(Arc<dyn CustomMetric>)`
  newtypes with manual `Clone` + `Debug` + `Arc::ptr_eq` `PartialEq`
- `Loss::Custom(CustomObjectiveHandle)` (`runtime.rs`)
- `EvalMetric::Custom(CustomMetricHandle)` + the one-time `EvalMetric` Copy-drop (`metrics.rs`)
- `compute_gradients` `Loss::Custom` arm (`cpu_runtime.rs`); `EvalMetric::eval` `Custom` arm (`metrics.rs`)
- builder `custom_objective(...)`/`custom_metric(...)` setters (`builder.rs`)
- `crates/cb-compute/tests/custom_objective_test.rs` (NEW — self-oracle)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RED — custom.rs trait pair + Arc<dyn> handles + failing self-oracle test</name>
  <files>crates/cb-compute/src/custom.rs, crates/cb-compute/src/runtime.rs, crates/cb-compute/tests/custom_objective_test.rs</files>
  <read_first>
    - crates/cb-compute/src/runtime.rs (`Runtime` trait `:821` Send+Sync/CbResult shape; `Loss` enum `:56` derives `Debug, Clone, PartialEq`)
    - crates/cb-compute/src/loss.rs (built-in RMSE der1=`target-approx`/der2=`-1` `:35-44`; Logloss der1=`target-p`/der2=`-p(1-p)`)
    - cb_core CbError/CbResult (thiserror; unwrap/anyhow banned)
    - crates/cb-train/tests/loss_oracle_test.rs (the EXISTING built-in Logloss/RMSE per-stage oracle the self-oracle must reproduce)
    - 06.4-RESEARCH.md Strand 3 (trait signatures + Arc<dyn> mechanism + self-oracle) + Pitfall 7
    - 06.4-PATTERNS.md "custom.rs trait pair" + "Arc<dyn> newtype in a Clone+PartialEq enum"
  </read_first>
  <action>
    Create the NEW module `crates/cb-compute/src/custom.rs` (register it in the crate's `lib.rs`). Define
    `pub trait CustomObjective: Send + Sync` with `calc_ders_range(&self, approxes: &[f64], targets: &[f64],
    weights: &[f64], ders: &mut [(f64,f64)]) -> CbResult<()>` (empty weights == all 1.0, upstream convention;
    mirrors Python `calc_ders_range(approxes, targets, weights) -> [(der1, der2)]` so the Phase-8 PyO3 wrap is
    a thin adapter — D-09, DO NOT implement PyO3). Define `pub trait CustomMetric: Send + Sync` with
    `evaluate(&self, approxes: &[f64], target: &[f64], weight: &[f64]) -> CbResult<(f64,f64)>`,
    `get_final_error(&self, error: f64, weight: f64) -> f64`, `is_max_optimal(&self) -> bool`. Define the
    newtypes `CustomObjectiveHandle(pub Arc<dyn CustomObjective>)` / `CustomMetricHandle(pub Arc<dyn CustomMetric>)`
    with `#[derive(Clone)]`, manual `PartialEq` via `Arc::ptr_eq`, and manual `Debug` ("<dyn>") per RESEARCH
    Strand 3 (use `Arc`, not `Box`). Add `Loss::Custom(CustomObjectiveHandle)` to `runtime.rs` — `Loss`'s
    `#[derive(Debug, Clone, PartialEq)]` now compiles because the field implements all three manually. Then
    write the SEPARATE self-oracle test `crates/cb-compute/tests/custom_objective_test.rs`: a test-local
    struct implementing `CustomObjective` as Logloss (der1=`target-sigmoid(approx)`, der2=`-p(1-p)`; the
    stronger der2 test) — RED now (training through `Loss::Custom` not wired yet). Add NaN/Inf der guards
    (the `Focal` `[1e-13,1-1e-13]` clamp precedent) in the trait-consumer path design. NO inline `#[cfg(test)]`.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/catboost_rs && cargo build -p cb-compute 2>&1 | tail -5; cargo test -p cb-compute --test custom_objective_test 2>&1 | grep -qE "cannot find|E0599|unresolved|Loss::Custom" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    `cb-compute` compiles with `custom.rs` + `Loss::Custom` (the `Loss` derives still work via the manual
    handle impls); the separate self-oracle test exists (Logloss-as-custom) and fails because the train
    dispatch is not wired yet.
  </acceptance_criteria>
  <done>RED: trait pair + Arc<dyn> handles + Loss::Custom compile; self-oracle test references the unwired path and fails.</done>
</task>

<task type="auto">
  <name>Task 2: EvalMetric Copy-drop + EvalMetric::Custom + compute_gradients/eval dispatch</name>
  <files>crates/cb-train/src/metrics.rs, crates/cb-backend/src/cpu_runtime.rs</files>
  <read_first>
    - crates/cb-train/src/metrics.rs (`EvalMetric` `:54` derives `Copy`; `eval` `:216` takes `self` by value; `Rmse`/`Logloss` arms `:252-279`)
    - crates/cb-backend/src/cpu_runtime.rs (`compute_gradients` N-dim/scalar dispatch arms)
    - crates/cb-compute/src/custom.rs (the handles from Task 1)
    - 06.4-RESEARCH.md Strand 3 dispatch seam + Pitfall 7 (Copy-drop ripple; mirrors 6.2 Loss Copy-drop) + leaf default Newton/1 for custom
    - 06.4-PATTERNS.md "EvalMetric::Custom + eval arm"
  </read_first>
  <action>
    Add `EvalMetric::Custom(CustomMetricHandle)` to `metrics.rs`. Because `Arc` is not `Copy`, DROP the `Copy`
    derive from `EvalMetric` (one-time mechanical refactor mirroring the 6.2 `Loss` Copy-drop, Pitfall 7);
    `eval` becomes `&self` (or callers `.clone()`); GREP the workspace for every by-value `EvalMetric` call
    site and fix the `expected EvalMetric, found &EvalMetric` ripple. Add the `eval` `Custom` arm:
    `let (err, w) = h.0.evaluate(approx, target, weight)?; Ok(h.0.get_final_error(err, w))`. In
    `cpu_runtime.rs::compute_gradients`, add `Loss::Custom(h) => h.0.calc_ders_range(...)` filling the per-object
    `(der1, der2)` buffer, then assemble `Derivatives` through the SAME scalar path the built-ins use (the rest
    of the pipeline is loss-agnostic); default the custom leaf-estimation to Newton/1-iter (matches the
    self-oracle target's default, A3). Reject non-finite ders (Tampering mitigation). All reductions via
    `cb_core::sum_f64` (D-08); no `unwrap`/`anyhow`/indexing in library code.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/catboost_rs && cargo build -p cb-train -p cb-backend 2>&1 | tail -5; cargo test -p cb-train 2>&1 | grep -iE "test result|FAILED" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    `EvalMetric` no longer derives `Copy`; `EvalMetric::Custom` + `eval` `Custom` arm + `Loss::Custom`
    `compute_gradients` arm exist; the cross-crate Copy-drop ripple is fully resolved (workspace builds);
    `cargo test -p cb-train` green (no regression).
  </acceptance_criteria>
  <done>Custom objective/metric dispatch is wired through the one Loss/EvalMetric seam; EvalMetric Copy-drop ripple resolved.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — builder surface + Logloss-as-custom self-oracle ≤1e-5</name>
  <files>crates/catboost-rs/src/builder.rs, crates/cb-compute/tests/custom_objective_test.rs</files>
  <read_first>
    - crates/catboost-rs/src/builder.rs (`loss(...)` setter `:99-103`; `score_function` field `:254`; `CatBoostBuilder` already non-Copy `:47-53`)
    - crates/cb-train/tests/loss_oracle_test.rs (the EXISTING built-in Logloss/RMSE per-stage oracle target)
    - crates/cb-compute/src/custom.rs + the Task-1 test struct
    - 06.4-RESEARCH.md Strand 3 self-oracle + Don't-Hand-Roll (Arc<dyn>) + Open Q (PyO3 deferred)
  </read_first>
  <action>
    Add builder setters `custom_objective(Arc<dyn CustomObjective>)` and `custom_metric(Arc<dyn CustomMetric>)`
    to `catboost-rs::builder` (the Rust-native facade Phase-8 PyO3 wraps the SAME trait through — DO NOT
    implement PyO3, D-09), mirroring the `loss(...)`/`score_function` setter pattern; `CatBoostBuilder` is
    already non-Copy so `Arc<dyn>` fields are consistent. Then make the Task-1 self-oracle GREEN: train the
    Logloss-as-`CustomObjective` (and optionally RMSE) through the `Loss::Custom` path (Newton/1-iter) and
    `compare_stage` the per-stage approx/leaf/prediction against the EXISTING built-in Logloss/RMSE oracle
    fixture ≤1e-5 (NO new upstream fixture — the mechanism is proven against the shipped built-in oracle).
    Add a `CustomMetric` self-oracle case (Logloss-as-metric `evaluate` matches the built-in `EvalMetric::Logloss`
    final error). Confirm the trait surface is shaped for a thin PyO3 adapter (signatures mirror the Python
    callback contract).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/catboost_rs && cargo test -p cb-compute --test custom_objective_test 2>&1 | tail -5; cargo build -p catboost-rs 2>&1 | tail -3; cargo test -p cb-compute -p cb-train 2>&1 | grep -iE "test result|FAILED" | tail -4</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p cb-compute --test custom_objective_test` PASSES: Logloss-as-custom reproduces the built-in
    Logloss per-stage oracle ≤1e-5, and the custom-metric self-oracle matches `EvalMetric::Logloss`; `catboost-rs`
    builds with the `custom_objective`/`custom_metric` setters; `cargo test -p cb-compute -p cb-train` green.
  </acceptance_criteria>
  <done>GREEN: the custom-objective/metric trait dispatch is faithful (self-oracle ≤1e-5); builder surface exposed; PyO3 deferred to Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied `dyn CustomObjective`/`dyn CustomMetric` → train/eval loop | a user trait impl runs inside the training loop; its der/eval outputs flow into leaf values. In-process only (Rust caller); the Phase-8 PyO3 bridge is out of scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.4D-01 | Denial of Service | a user `CustomObjective` panicking mid-train | mitigate | trait methods return `CbResult` (no `unwrap`/`panic!` in the dispatch path — CLAUDE.md); errors propagate via `thiserror`, never `anyhow` |
| T-06.4D-02 | Tampering | NaN/Inf der from a user objective poisoning leaf values | mitigate | reject/guard non-finite ders (the `Focal` `[1e-13,1-1e-13]` clamp precedent) before they reach leaf estimation |

**No external attack surface beyond the in-process Rust trait object:** no network, auth, or serialization;
the Python callback bridge (the only untrusted-source path) is DEFERRED to Phase 8 (D-09) and not built here.
</threat_model>

<verification>
- `cargo test -p cb-compute --test custom_objective_test` green: Logloss-as-custom reproduces the built-in Logloss per-stage oracle ≤1e-5; custom-metric matches `EvalMetric::Logloss`.
- `cargo test -p cb-compute -p cb-train` green; `catboost-rs` builds (builder setters present).
- `Loss::Custom` + `EvalMetric::Custom` compile via the `Arc<dyn>` newtype manual `Clone`/`Debug`/`Arc::ptr_eq` impls; `EvalMetric` Copy-drop ripple resolved.
- Trait methods return `CbResult`; no `unwrap`/`expect`/`panic!`/`anyhow`/indexing in the dispatch path; non-finite ders rejected.
- No PyO3 code added (D-09 — Phase 8); trait signatures mirror the Python callback contract for a thin future adapter.
</verification>

<success_criteria>
- `CustomObjective`/`CustomMetric` traits exist (CbResult, Send+Sync) and plug into the one Loss/EvalMetric dispatch via `Arc<dyn>` newtypes.
- Self-oracle proves dispatch faithful ≤1e-5 against the shipped built-in oracle (no new upstream fixture).
- `EvalMetric` Copy-drop completed; builder exposes the trait; PyO3 deferred to Phase 8 (D-09).
</success_criteria>

<output>
Create `.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-04-SUMMARY.md` when done.
</output>
