---
phase: 10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire
plan: 05
type: execute
wave: 4
depends_on: ["10-04", "10-02"]
files_modified:
  - bench/cuda_oracle.py
  - bench/README.md
  - bench/RESULTS.md
  - bench/fixtures/.gitkeep
  - crates/cb-backend/src/kernels/depth1_oracle.rs
  - crates/cb-backend/src/kernels.rs
autonomous: true
requirements: [GPUT-04, BENCH-01, BENCH-02]
must_haves:
  truths:
    - "Committed deterministic depth-1 RMSE + Logloss fixtures (Plain, fold_count=1) exist, with the Logloss CPU reference pinned to FIRST-ORDER (gradient) leaves (A1/Pitfall 3)"
    - "bench/cuda_oracle.py runs the correctness oracle as a BLOCKING gate (depth-1 device == CPU <=1e-5) BEFORE any wall-clock speed number is reported"
    - "The depth-1 device-vs-CPU wall-clock speed gate is measured on a LARGE-n dataset (~1e5-1e6 rows), warm-run/JIT-excluded, train-only (D-10-09 resolved); correctness runs on the small fixture"
    - "bench/README.md documents the human-gated Kaggle CUDA steps (build --features cuda wheel -> oracle -> speed) and bench/RESULTS.md records the human sign-off"
  artifacts:
    - path: "bench/cuda_oracle.py"
      provides: "correctness-gate-then-speed Kaggle CUDA harness (extends benchmark.py shape)"
      contains: "def"
    - path: "bench/README.md"
      provides: "Kaggle notebook steps + the human-gated external workflow"
      contains: "maturin"
    - path: "bench/RESULTS.md"
      provides: "committed human sign-off log structure for the per-phase CUDA run"
      contains: "Phase 10"
    - path: "crates/cb-backend/src/kernels/depth1_oracle.rs"
      provides: "in-env rocm depth-1 RMSE+Logloss device-vs-CPU oracle test (<=1e-5)"
      contains: "depth1"
  key_links:
    - from: "bench/cuda_oracle.py"
      to: "bench/fixtures/"
      via: "loads the committed deterministic depth-1 fixtures for the correctness gate"
      pattern: "fixtures"
    - from: "bench/cuda_oracle.py"
      to: "catboost_rs (--features cuda wheel)"
      via: "imports the built wheel; fit() drives the cb-train -> device seam path"
      pattern: "catboost_rs"
---

<objective>
Deliver GPUT-04's oracle + BENCH-01/BENCH-02: the reproducible Kaggle CUDA correctness+speed
harness (script + README, D-10-03), the committed deterministic depth-1 RMSE + Logloss fixtures
(D-10-05/08), and an in-env ROCm depth-1 oracle smoke. `bench/cuda_oracle.py` runs the
correctness oracle as a BLOCKING gate (depth-1 device == CPU ≤1e-5) BEFORE any wall-clock number
(D-10-04), then measures device-vs-CPU train-only wall-clock — warm-run/JIT-excluded, queue
drained before stopping the clock (Pitfall 6). The correctness oracle runs on the SMALL
deterministic fixture; the **speed gate runs on a LARGE-n dataset (~1e5–1e6 rows)** because
depth-1 device ≥ CPU is physically infeasible at small n and achievable only at large n
(D-10-09 RESOLVED). The Logloss fixture's CPU reference is pinned to FIRST-ORDER (gradient)
leaves — Newton der2 leaves are Phase 11 (A1/Pitfall 3). The Kaggle run is a human-gated external
step recorded in `bench/RESULTS.md`.

Purpose: Establish the authoritative GPU oracle + standing per-phase speed harness reused by
Phases 11–13, and prove the depth-1 device tree's correctness (in-env smoke now; Kaggle CUDA
authoritative).
Output: bench/ harness + README + RESULTS + fixtures + in-env depth-1 oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-RESEARCH.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-PATTERNS.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-CONTEXT.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-02-SUMMARY.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-04-SUMMARY.md
</context>

## Artifacts this plan produces

- New `bench/` directory: `cuda_oracle.py` (correctness-gate-then-speed harness),
  `README.md` (Kaggle steps + human-gated workflow), `RESULTS.md` (committed sign-off log),
  `fixtures/` (committed deterministic depth-1 RMSE + Logloss fixtures + a reference model).
- New in-env test `crates/cb-backend/src/kernels/depth1_oracle.rs` + `#[cfg(test)] mod depth1_oracle;` include.

<tasks>

<task type="auto">
  <name>Task 1: Committed depth-1 RMSE+Logloss fixtures + in-env ROCm depth-1 oracle</name>
  <files>crates/cb-backend/src/kernels/depth1_oracle.rs, crates/cb-backend/src/kernels.rs, bench/fixtures/.gitkeep</files>
  <read_first>
    - crates/cb-backend/src/kernels/grow_loop.rs (make_fixture deterministic-gen :101-156; cpu_stump_score / cpu_leaf_index transcription; LEAF_BOUND tolerance :53-56)
    - crates/cb-backend/src/kernels/session_residency.rs (the resident grow path from 10-04 to drive)
    - crates/cb-backend/src/kernels.rs (the mod-include block at :2775 — add `mod depth1_oracle;`)
    - crates/cb-backend/src/gpu_backend.rs (grow_tree_on_device override; der seam RMSE/Logloss)
    - .planning/phases/10-.../10-RESEARCH.md (Pitfall 3 :248-251 — pin Logloss to first-order leaves; A1; Depth-1 Oracle Fixtures)
    - .planning/phases/10-.../10-PATTERNS.md (bench/fixtures section :352-359; bin->border / first-order leaf note)
  </read_first>
  <action>
    Create `crates/cb-backend/src/kernels/depth1_oracle.rs` (dedicated test file; include via
    `#[cfg(test)] mod depth1_oracle;` in kernels.rs). Generate small deterministic depth-1 fixtures
    for BOTH losses (RMSE and Logloss, Plain, fold_count=1 — D-10-08) and serialize them under `bench/fixtures/`
    (format Claude's discretion — e.g. numpy `.npy`/`.csv` for X/y + a reference JSON of the expected
    split (feature, bin_id/border) and leaf values). CRITICAL: compute the Logloss fixture's CPU
    reference leaf values with FIRST-ORDER (gradient) `calc_average` — the SAME method the device
    depth-1 path uses — NOT Newton der2 (Newton leaves are Phase 11; A1/Pitfall 3); document this in
    the fixture reference. Add an in-env test that grows the depth-1 tree on device (over the resident
    session from 10-04) for each loss and asserts the device split + leaf values match the transcribed
    CPU reference ≤1e-5 (whole-dataset level-0 histogram is the exact CPU score → tight bound).
    Transcribe `cb_compute` score/leaf helpers read-only; do NOT import cb-train. Commit the fixtures
    (replace the `.gitkeep` placeholder with the real fixture files). Tests may use `unwrap`/`assert`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm depth1_oracle</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --no-default-features --features rocm depth1_oracle` passes for BOTH RMSE and Logloss depth-1 ≤1e-5 in-env. Tier: ROCm in-env smoke (non-gating; Kaggle CUDA is authoritative).
    - The Logloss fixture reference uses first-order (gradient) `calc_average` leaves (documented in the fixture), NOT Newton — so the ≤1e-5 oracle is method-consistent (A1).
    - Fixtures are committed under `bench/fixtures/` (real files, not the placeholder): `ls bench/fixtures/` shows the RMSE + Logloss fixtures + reference.
  </acceptance_criteria>
  <done>Committed deterministic depth-1 RMSE+Logloss fixtures (Logloss pinned to first-order leaves) + an in-env rocm oracle prove the depth-1 device tree matches the CPU path ≤1e-5.</done>
</task>

<task type="auto">
  <name>Task 2: bench/cuda_oracle.py + README + RESULTS (correctness-gate-then-speed, large-n speed)</name>
  <files>bench/cuda_oracle.py, bench/README.md, bench/RESULTS.md</files>
  <read_first>
    - benchmark.py (repo root — seeded numpy gen, import-guard, warm fit, time.time deltas, device-vs-CPU summary; reuse the speedup reporter)
    - benchmark_fast.py, benchmark_small.py (size-variant generators to model the large-n generator after)
    - bench/fixtures/ (the committed fixtures from Task 1)
    - .planning/phases/10-.../10-RESEARCH.md (Kaggle CUDA Harness; Pitfall 6 :262-264 ordering; Depth-1 device>CPU Feasibility :510-525 — large-n speed gate; D-10-09 RESOLVED; D-10-04 correctness-gate-first; D-10-10 baselines; A5 Kaggle image pin)
    - .planning/phases/10-.../10-PATTERNS.md (bench/cuda_oracle.py section :328-349 — BENCH ordering, two datasets, baselines)
  </read_first>
  <action>
    Create `bench/cuda_oracle.py` extending the `benchmark.py` shape. Execution order (Pitfall 6,
    MUST): (1) confirm CUDA active via `nvidia-smi` (print device; abort if absent); (2) warm ONE
    untimed fit (JIT excluded); (3) run the CORRECTNESS oracle on the SMALL committed fixture —
    depth-1 device prediction vs the CPU reference ≤1e-5 for BOTH RMSE and Logloss — as a BLOCKING
    gate that aborts before any speed number if it fails (D-10-04); (4) only then measure
    train-only wall-clock on a LARGE-n generated dataset (~1e5–1e6 rows × ≥50 features; add the
    large-n generator alongside the small fixture loader), draining CubeCL's lazy queue with a
    read-back/predict before stopping the clock; report device vs CPU (and vs official CatBoost
    `task_type='GPU'` where a comparable depth-1 config exists — D-10-10). Print a clear
    PASS/FAIL for the gate and a speedup/slowdown line for the speed measurement. Reuse the
    `benchmark.py` import-guard and reporter; no `unwrap`-equivalent silent failures (surface
    errors). Write `bench/README.md`: the human-gated Kaggle steps — in `crates/catboost-rs-py/`
    run `maturin build --release --no-default-features --features cuda`, `pip install` the wheel,
    `python bench/cuda_oracle.py`; pin a known-good Kaggle CUDA image (A5); state that ROCm in-env
    is an optional smoke convenience, NOT a gate. Write `bench/RESULTS.md`: a committed sign-off log
    table with a "Phase 10" row holding columns for the CUDA correctness verdict (≤1e-5 pass/fail),
    the depth-1 large-n device-vs-CPU wall-clock + speedup, the official-CatBoost-GPU comparison
    (where applicable), the Kaggle image/date, and a human sign-off field (left blank until the user
    runs it). Document that this RESULTS log is the per-phase record reused by Phases 11–13 (BENCH-02 standing).
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('bench/cuda_oracle.py').read()); print('syntax-ok')" && test -f bench/README.md && grep -q "maturin" bench/README.md && grep -q "Phase 10" bench/RESULTS.md</automated>
    <human-check>
    On a Kaggle CUDA notebook (no NVIDIA in-env): build the `--features cuda` wheel per bench/README.md,
    run `python bench/cuda_oracle.py`; confirm the depth-1 RMSE+Logloss correctness gate passes ≤1e-5
    BEFORE any speed number, then record the large-n device-vs-CPU wall-clock (device ≥ CPU at large n,
    D-10-09) and the official-CatBoost-GPU comparison in bench/RESULTS.md, and sign off.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `bench/cuda_oracle.py` parses cleanly (Python AST) and structurally enforces correctness-gate-BEFORE-speed (the speed block is unreachable until the oracle PASS); the speed dataset is the large-n generator, the oracle dataset is the small fixture. Tier: in-env structural check (automated); the actual CUDA correctness ≤1e-5 + speed numbers are Kaggle CUDA human-gated (recorded in RESULTS.md).
    - `bench/README.md` documents the `maturin --features cuda` wheel build + the human-gated Kaggle workflow + the ROCm-is-not-a-gate note.
    - `bench/RESULTS.md` has a "Phase 10" sign-off row with correctness verdict, large-n device-vs-CPU speedup, and a blank human sign-off field.
  </acceptance_criteria>
  <done>The Kaggle CUDA correctness+speed harness, README, and RESULTS log exist; correctness gates before speed; the speed gate targets large-n; the Kaggle run is documented as the human-gated authoritative step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle notebook → wheel | the user builds + runs the wheel on external Kaggle CUDA hardware; the harness reads committed fixtures (no untrusted network/auth input) |
| fixture file → oracle | committed deterministic fixtures feed the ≤1e-5 gate; a corrupted/mismatched fixture is the only tampering vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Repudiation (fast-but-wrong number quoted) | reporting speed before the correctness oracle passes | mitigate | the speed block is structurally unreachable until the ≤1e-5 gate PASSES (D-10-04); RESULTS records the verdict first |
| T-10-05-02 | Tampering | fixture corruption/mislabel skewing the oracle | mitigate | fixtures are committed deterministic files with a reference; the in-env oracle (Task 1) re-derives them so drift is caught |
| T-10-05-03 | Information accuracy | cold/JIT-inflated or queue-undrained timing | mitigate | warm one untimed fit; drain the lazy queue with a read-back before stopping the clock (Pitfall 6) |
| T-10-05-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed — numpy/catboost/maturin are pre-existing and audited OK (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cargo test -p cb-backend --no-default-features --features rocm depth1_oracle` green (RMSE + Logloss ≤1e-5 in-env).
- `bench/cuda_oracle.py` parses; `bench/README.md` documents the maturin/Kaggle workflow; `bench/RESULTS.md` has the Phase 10 row.
- Fixtures committed under bench/fixtures/; Logloss reference uses first-order leaves.
- Kaggle CUDA correctness ≤1e-5 (BLOCKING) + large-n speed: human-gated, recorded in RESULTS.md (out-of-band).
- `cargo test -p cb-train` still green (D-04 — CPU path unchanged).
</verification>

<success_criteria>
GPUT-04 depth-1 RMSE+Logloss ≤1e-5 is proven in-env (rocm smoke) and wired for the authoritative
Kaggle CUDA oracle; BENCH-01's reproducible script+README+fixtures+RESULTS harness exists with
correctness gating before speed; BENCH-02's standing per-phase speed check is established with the
depth-1 device-vs-CPU wall-clock measured on a large-n dataset (D-10-09 resolved). The Kaggle run
is the documented human-gated authoritative step recorded in RESULTS.md, reused by Phases 11–13.
</success_criteria>

<output>
Create `.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-05-SUMMARY.md` when done.
</output>