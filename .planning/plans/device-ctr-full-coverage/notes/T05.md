# T05 — Frozen fixture `ctr_device_buckets` — COMPLETE

Spec: DCTR-08 (support). Wave 0. Branch `feat/device-ctr-full-coverage`, worktree policy
`implementation.use_worktree = false` ⇒ executed in the primary checkout
`/home/user/Documents/workspace/catboost_rs`.

## Escalation rung reached: **rung 0** (the pinned starting point) — no escalation needed

The plan expected to reach rung 3 (assumption 4, PLAN §6). It did not. Rung 0's seed search
found a passing seed on its **second** seed.

| parameter | value | class |
|---|---|---|
| `SCENARIO` | `ctr_device_buckets` | — |
| `CARDS` | `(6,)` — ONE cat column | tunable (rung 0) |
| `N_ROWS` | 64 | tunable (rung 0) |
| `N_FLOAT` | 2 | **gate-load-bearing** (cat-only pools can never reach the device) |
| `iterations` | 5 | tunable (rung 0) |
| `SEARCH_SEEDS` | `range(24)` | tunable (rung 0) |
| `data_seed` | **1** (seeds tried: `[0, 1]`) | frozen |
| `simple_ctr` | `["Buckets:Prior=0.5"]` | pinned recipe |
| `combinations_ctr` | `[]`, `max_ctr_complexity=1` | pinned recipe — no combination projection can form |
| `border_count` | 15 | **gate-load-bearing** (R-11: `borders.len()+1 == n_bins`) |
| `one_hot_max_size` | 1 | **gate-load-bearing** (routes the cat column to CTR) |
| `permutation_count=1`, `bootstrap_type="No"`, `random_strength=0`, `boost_from_average=False`, `score_function="L2"`, `leaf_estimation_method="Gradient"`, `leaf_estimation_iterations=1`, `thread_count=1`, no `task_type` | as pinned | **gate-load-bearing** |

The ladder is **encoded in the generator itself** (`RUNGS` tuple, rungs 0→4 in the plan's
mandated order: widen seeds → cardinality 8 → second cat column `(6,5)` → iterations 10).
It stops at the first rung/seed satisfying every guard and records
`escalation_rung` / `escalation_rung_reason` / `escalation_attempts` in `config.json`. The
guard is never relaxed at any rung; if all five rungs failed the script `sys.exit`s with an
escalate-do-not-weaken message.

Because rung 0 won, `X_cat.npy` is a **single** cat column stored 1-D as `(64,) int32` —
exactly the shape T05's Red spec predicted, and the same convention as
`ctr_device_mixed/X_cat.npy`. No smoke-test shape adjustment was needed.

## Proof that BOTH `target_border_idx` values appear

`model.json` `features_info.ctrs` (read back after freezing):

```
n ctrs = 2
ctr_types           = ['Buckets']            (all descriptors, asserted)
target_border_idxs  = [0, 1]                 (top-level key on each descriptor)
  Buckets tbi=0 prior=0.5/1 elements=[{cat_feature_index: 0, combination_element: 'cat_feature_value'}] nborders=2
  Buckets tbi=1 prior=0.5/1 elements=[{cat_feature_index: 0, combination_element: 'cat_feature_value'}] nborders=2
float_features (index, n_borders) = [(0, 0), (1, 1)]   ⇒ >=1 float split
descriptor key set = ['borders','ctr_type','elements','identifier','prior_denomerator',
                      'prior_numerator','scale','shift','target_border_idx']
```

`config.json` records `"observed_target_border_idxs": [0, 1]`.

Accessor confirmed to be **top-level** on each `features_info.ctrs[i]` (PLAN assumption 2),
read with `c["target_border_idx"]` — never a defaulting `.get(..., 0)`, which would have
made the only discriminating guard in the phase silently vacuous.

### The guard is demonstrably NOT vacuous (discrimination evidence)

Rung 0 / seed **0** was rejected by exactly this guard. Measured directly against the
committed generator's own functions (read-only probe, no fixture written):

```
seed 0: n_ctr=1 types=['Buckets'] target_border_idxs=[0]    float_split=True std=0.0963   -> REJECTED
seed 1: n_ctr=2 types=['Buckets'] target_border_idxs=[0, 1] float_split=True std=0.1396   -> accepted
```

A `[0]`-only model is a real, reachable outcome at this shape, and both the generator's
assertion and the smoke test's `assert_eq!(idxs, vec![0, 1])` reject it. That is the
equivalent of a §2.5 mutation check for the one assertion that carries this task (T05 is
not on the plan's nine-item mutation-candidate list, so no production mutation was
performed — and none was possible: this task changes no production code).

## Other mandatory guards, all satisfied

- **Float split present**: `float_features[1]` carries 1 border (guard 2).
- **`predictions.std() = 0.13956912793142692 > 1e-6`** (guard 3).
- **Border freezing (R-15)**: `Pool.quantize(border_count=15)` +
  `save_quantization_borders` → full per-feature set → `borders.npy` `(2, 15) f64`; asserted
  exactly 15 borders per float feature; asserted
  `np.abs(quantized_fit_preds - raw_fit_preds).max() == 0.0` (bit-identical). `model.json`'s
  float borders are the pruned USED subset and are never fed to the trainer.
- **Overflow margin**: docstring + `config.json` field —
  `n*max(w)*max(|der1|) <= 64*1.0*1.0 = 64 << 2^33 ~= 8.6e9`, margin > 1.3e8x.
- **FROZEN marker**: `config.json` `"note": "FROZEN. Generated once by this directory's own
  gen_fixtures.py and NEVER regenerated in CI. CatBoost quantization is run-to-run
  nondeterministic on categorical routing; regenerating invalidates the <=1e-5 gate."`
  plus `"requirement": "DCTR-08"`. The reproducibility caveat is also in the generator
  docstring.
- **Corpus contamination check** kept verbatim from the combo generator (see deviation 1).

## Files added (all new; no pre-existing file modified)

Generator: `/home/user/Documents/workspace/catboost_rs/crates/cb-oracle/fixtures/ctr_device_buckets/gen_fixtures.py`

```
crates/cb-oracle/fixtures/ctr_device_buckets/gen_fixtures.py
crates/cb-oracle/fixtures/ctr_device_buckets/config.json
crates/cb-oracle/fixtures/ctr_device_buckets/model.json
crates/cb-oracle/fixtures/ctr_device_buckets/X.npy            (64, 2) f32
crates/cb-oracle/fixtures/ctr_device_buckets/X_cat.npy        (64,)   i32
crates/cb-oracle/fixtures/ctr_device_buckets/y.npy            (64,)   f64
crates/cb-oracle/fixtures/ctr_device_buckets/borders.npy      (2, 15) f64
crates/cb-oracle/fixtures/ctr_device_buckets/predictions.npy  (64,)   f64
crates/cb-oracle/tests/ctr_device_buckets_fixture_smoke_test.rs
```

Generation command (run from the repo root so CatBoost's `catboost_info/` log dir lands on
the gitignored `/catboost_info/` path instead of inside the fixture corpus):

```
python3 crates/cb-oracle/fixtures/ctr_device_buckets/gen_fixtures.py
```

Output: `rung 0 (pinned starting point): scanned 2 seed(s) — CHOSE seed 1`.

## TDD record

**Red** — `crates/cb-oracle/tests/ctr_device_buckets_fixture_smoke_test.rs`, test
`ctr_device_buckets_fixture_loads_with_both_target_borders`, written before the fixture
existed.

```
cargo test -p cb-oracle --test ctr_device_buckets_fixture_smoke_test
=> FAILED
   panicked at crates/cb-oracle/tests/ctr_device_buckets_fixture_smoke_test.rs:41:29:
   ctr_device_buckets/X.npy must load as f32 [N,F]:
   Io(Os { code: 2, kind: NotFound, message: "No such file or directory" })
```

This is **exactly** the predicted principal failure (`No such file or directory (os error 2)`).

**Green** — generator run offline against the installed `catboost==1.2.10`; same command:

```
test ctr_device_buckets_fixture_loads_with_both_target_borders ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

**Refactor** — none (new files only), per the task.

## Validation commands

| command | result |
|---|---|
| `cargo test -p cb-oracle --test ctr_device_buckets_fixture_smoke_test` | **PASS** (1 passed, 0 failed) |
| `git status --porcelain crates/cb-oracle/fixtures` | **PASS with a concurrency note** — see deviation 1 |
| `rustfmt --edition 2021 --check` on the new test | non-clean, but **identical in kind** to the shipped sibling `ctr_device_combo_fixture_smoke_test.rs` (no `rustfmt.toml` in the repo; the corpus is not rustfmt-default-clean). No action — house style matched. |
| `cargo clippy -p cb-oracle --tests` | no finding names `ctr_device_buckets`. The 5 pre-existing errors (`approximate value of f{32,64}::consts::{PI,E}` in `skeleton_oracle_test` / `stochasticrank_centering_test` / lib test, one `unwrap()` lint) are untouched by this task. |

## Deviations

1. **The generator's corpus-contamination check fired — on files this task did not
   create.** It was kept **verbatim** from the combo generator as mandated, and it rejects
   any dirty path under `crates/cb-oracle/fixtures` not containing `SCENARIO`. Concurrent
   Wave-0 agents T06/T07 were creating `ctr_device_counter/` and `ctr_device_btmv/` at the
   same time, so the check printed:

   ```
   corpus contamination — this generator touched paths outside its scenario:
       ?? crates/cb-oracle/fixtures/ctr_device_counter/
   ```

   and exited 1 **after** all artifacts and every assertion had already been written and
   passed. Verified manually that this task touched nothing outside its own directory:
   `git status --porcelain crates/cb-oracle/fixtures` now lists exactly
   `ctr_device_btmv/`, `ctr_device_buckets/`, `ctr_device_counter/` — the first and third
   belonging to T07 and T06. **No code was changed to work around this**; it is a
   parallel-execution artifact of the plan's own C-15 wave-0 parallelism and is expected to
   disappear once the coordinator commits the wave. The check remains correct for a serial
   re-run.

2. **Rung 0 sufficed** where PLAN §6 assumption 4 predicted rung 3. Recorded rather than
   worked around: `escalation_rung = 0`. The full ladder remains encoded in the generator
   for any future re-freeze.

3. **`cat_term` was defined once, not escalated.** The task specifies adapting
   `logit = 3.0*(cat_term - 0.5) + 2.0*(x[:,0] - x[:,1])` but leaves `cat_term` to the
   implementer; it is not one of the four ladder rungs. It is defined once as a **graded**
   `[0, 1]` categorical signal (a binary signal collapses the two Buckets numerators onto
   the same ordering), with the one-column form `0.8*(c0%2) + 0.2*((c0//3)%2)` and the
   two-column form mirroring `ctr_buckets_simple`'s
   `1.2*(c0%2) - 0.9*(c1%3) + 0.3*(c0//3)` rescaled onto `[0, 1]`. Only rung 0's one-column
   form was exercised. The per-object float ramp (R-14) is retained verbatim.

4. **The smoke test asserts more than the task's minimum**: on top of the shapes,
   `observed_target_border_idxs == [0, 1]`, and 15 borders per float row, it also re-derives
   the both-borders set from `model.json` itself and asserts every descriptor is `Buckets`
   and that `config["note"]` starts with `FROZEN`. This strengthens the gate and duplicates
   no other task's scope.

## Scope

Nothing outside `crates/cb-oracle/fixtures/ctr_device_buckets/` and the single new smoke
file was created or modified. No `git add` / `git commit` / any git write was run.
`crates/cb-train/src/**`, `crates/cb-compute/src/**`, and the sibling fixtures
`ctr_device_counter` / `ctr_device_btmv` were not touched.

## Rollback

Delete `crates/cb-oracle/fixtures/ctr_device_buckets/` and
`crates/cb-oracle/tests/ctr_device_buckets_fixture_smoke_test.rs`. Nothing depends on them
until T10.
