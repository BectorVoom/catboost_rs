# T00 — Consciously migrate the gate's five existing covering assertions — DONE

- **Specs**: DCTR-18 (migration clause), R-21
- **Wave**: 0. **Blocks** T01, T10, T12, T16, T19, T23.
- **Branch / tree**: `feat/device-ctr-full-coverage` @ `7c821b6` (main checkout;
  `planning/settings.json` has `implementation.use_worktree: false`, so no worktree was
  created — the plan frontmatter's `worktree-gpu-borders-shared-sample` is stale w.r.t. the
  coordinator's instruction to work in `/home/user/Documents/workspace/catboost_rs`).
- **Production behaviour changed**: **none**. `crates/cb-train/src/boosting.rs` is
  byte-identical to HEAD (`git diff -- crates/cb-train/src/boosting.rs` empty at exit).

---

## 1. What changed

Single file: `crates/cb-train/src/device_ctr_combo_config_test.rs` (+230 / −47).

| location (post-change) | change |
|---|---|
| `:19-53` | **NEW** module-doc section *"The conscious re-opening of the gate (P1 / T00 / R-21 / DCTR-18)"* — the written justification the superseded `:124-126` contract demanded. Contains all four required clauses (a)-(d), see §2. |
| `:159-168` | **NEW** `struct GateRow { label, column, expected, flips_at }`. |
| `:170-183` | **NEW** `with_type` / `with_target_border` / `with_prior_denom` column mutators (keep `covered_column` untouched). |
| `:185-213` | **NEW** doc block carrying the eight-row GATE STATE TABLE verbatim, the V-0c "simple projections on purpose" rationale, and the honest row-4 limitation. |
| `:215-275` | **NEW** `gate_state_table() -> Vec<GateRow>` — the eight rows. |
| `:277-293` | **NEW** `#[test] gate_admits_exactly_the_current_wave` — asserts on `ctr_types_are_device_covered` **directly** (imported at `:21`, unchanged), one row at a time. |
| `:295-310` | **NEW** `#[test] an_all_simple_borders_set_stays_covered` — the surviving `:144` positive (D-04) pin, now a distinct never-flipping function, deliberately kept as a **multi-column** set so it also pins the `.all(..)` fold that single-column rows cannot reach. |
| removed | `a_combination_column_set_is_NOT_device_covered_yet` (old `:116-147`) and `a_non_borders_column_still_declines` (old `:149-173`). |
| unchanged | `covered_column` (`:75-86` post-shift), `buckets`, `build`, `combination_projection_carries_every_member`, `simple_projection_is_byte_unchanged`, `an_unknown_projection_member_is_a_typed_error`, `averaging_columns_get_the_same_member_treatment`. |

Side effect (welcome): the crate's only `non_snake_case` warning
(`fn a_combination_column_set_is_NOT_device_covered_yet` — emitted on every
`cargo test -p cb-train --lib` at baseline) is gone with the retired function.

### The table as shipped

```
  row 1  simple   Borders      0  1.0  => true    flips_at: never (D-04 pin)
  row 2  combo    Borders      0  1.0  => false   flips_at: T19 (arity)
  row 3  simple   Counter      0  1.0  => false   flips_at: T12 (type)
  row 4  simple   Buckets      1  1.0  => false   flips_at: T10 (type + target border)
  row 5  simple   BTMV         0  1.0  => false   flips_at: T16 (type)
  row 6  simple   Borders      0  2.0  => false   flips_at: T01 (prior denominator)
  row 7  simple   FloatTMV     0  1.0  => false   flips_at: never (no parity surface)
  row 8  simple   FeatureFreq  0  1.0  => false   flips_at: never
```

Failure message shape (this is the contract later tasks will see):

```
row {n} ({label}): expected {expected}, got {actual}
   flips_at: {task + conjunct}
   If you deleted that conjunct, update THIS row in the SAME commit. If you did not, the
   device gate just moved by accident — which is exactly what the conscious-act contract
   in this module's doc forbids.
```

## 2. The written justification (module doc `:19-53`) — the four required clauses

- **(a)** P1 is the conscious re-opening the original comment anticipated — four conjuncts
  removed deliberately, one task per conjunct, each with its own kernel self-oracle and
  frozen upstream fixture; not a side effect of unrelated work.
- **(b)** The original's attached condition is honoured: `device_ctr_combo_fit_test` is
  un-ignored **in T19** and rewritten with a `CountingGpu` device-commit assertion
  (`grown.get() == params.iterations`), because `oblivious_trees.len() == iterations` is
  satisfied by the CPU grower too (R-8).
- **(c)** Measured evidence recorded: control `2.746e-2` FAIL → D-1 `2.082e-17` PASS, and
  `2.082e-17 != 1.388e-17` (the CPU-fallback value) is independent evidence the device path
  ran.
- **(d)** Why a table replaces per-conjunct negative tests: V-0c — the old assertions used
  `TProjection::from_features(&[0, 1])`, so each declined via the **arity** conjunct as well
  as via its named one and would go **vacuous, not red**, at T01/T10/T12.

The old sentence *"(currently `#[ignore]`d)"* survives **only** inside the block-quote of
the superseded contract, and the very next clause states that T19 removes the attribute —
so the stale reference the task flagged is corrected in place rather than left standing.

## 3. Red evidence

Per the task, this is a **§2.5 green-on-write** test: the table is authored to match today's
behaviour, so there is no conventional Red. Two things stand in for it.

**Baseline (before the rewrite)** — `cargo test -p cb-train --lib device_ctr_combo_config_tests`:
```
running 6 tests
test boosting::device_ctr_combo_config_tests::a_non_borders_column_still_declines ... ok
test boosting::device_ctr_combo_config_tests::a_combination_column_set_is_NOT_device_covered_yet ... ok
test boosting::device_ctr_combo_config_tests::simple_projection_is_byte_unchanged ... ok
test boosting::device_ctr_combo_config_tests::averaging_columns_get_the_same_member_treatment ... ok
test boosting::device_ctr_combo_config_tests::an_unknown_projection_member_is_a_typed_error ... ok
test boosting::device_ctr_combo_config_tests::combination_projection_carries_every_member ... ok
test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 378 filtered out
warning: function `a_combination_column_set_is_NOT_device_covered_yet` should have a snake case name
```

**First run after the rewrite** — green, 6 tests, no warning (functions renamed, count held
at 6: two retired, two added).

The real Red is the mutation section below.

## 4. §2.5 mutation checks — VERBATIM observed failures

Protocol followed for each: mutate production → run **only**
`cargo test -p cb-train --lib device_ctr_combo_config_tests` → record → revert → re-run
green. **No workspace-wide or device-suite command was run while any mutation was live.**
The task mandated one (row 6); two more were executed because otherwise rows 2-5 and 7-8
would have had no evidence of being wired to the real predicate.

### Mutation 1 (mandated) — delete `&& col.prior_denom == 1.0` from `boosting.rs:2364`

This is **exactly T01's Green**, so the output below doubles as T01's predicted Red.

```
thread 'boosting::device_ctr_combo_config_tests::gate_admits_exactly_the_current_wave' (587292) panicked at crates/cb-train/src/device_ctr_combo_config_test.rs:283:9:
assertion `left == right` failed: row 6 (simple/Borders/b=0/denom=2.0): expected false, got true
   flips_at: T01 — deletes `col.prior_denom == 1.0`
   If you deleted that conjunct, update THIS row in the SAME commit. If you did not, the device gate just moved by accident — which is exactly what the conscious-act contract in this module's doc forbids.
  left: true
 right: false

test result: FAILED. 5 passed; 1 failed; 0 ignored; 0 measured; 379 filtered out
```

Matches the task's predicted text (`row 6 (simple/Borders/b=0/denom=2.0): expected false, got true`)
character-for-character. Reverted; `git diff -- crates/cb-train/src/boosting.rs` empty; focused
test green again (6 passed).

### Mutation 2 (extra) — delete `&& col.ctr_type == ECtrType::Borders.as_i8()` from `boosting.rs:2362`

```
thread 'boosting::device_ctr_combo_config_tests::gate_admits_exactly_the_current_wave' (589417) panicked at crates/cb-train/src/device_ctr_combo_config_test.rs:283:9:
assertion `left == right` failed: row 3 (simple/Counter/b=0/denom=1.0): expected false, got true
   flips_at: T12 — admits Counter in the `ctr_type` conjunct
   If you deleted that conjunct, update THIS row in the SAME commit. If you did not, the device gate just moved by accident — which is exactly what the conscious-act contract in this module's doc forbids.
  left: true
 right: false

test result: FAILED. 5 passed; 1 failed; 0 ignored; 0 measured; 379 filtered out
```

Wires rows 3 / 5 / 7 / 8 (the loop aborts at the first, row 3). Reverted; green.

### Mutation 3 (extra) — delete `col.projection.is_simple() &&` from `boosting.rs:2361`

```
thread 'boosting::device_ctr_combo_config_tests::gate_admits_exactly_the_current_wave' (590656) panicked at crates/cb-train/src/device_ctr_combo_config_test.rs:283:9:
assertion `left == right` failed: row 2 (combo[0,1]/Borders/b=0/denom=1.0): expected false, got true
   flips_at: T19 — deletes `col.projection.is_simple()`
   If you deleted that conjunct, update THIS row in the SAME commit. If you did not, the device gate just moved by accident — which is exactly what the conscious-act contract in this module's doc forbids.
  left: true
 right: false

test result: FAILED. 5 passed; 1 failed; 0 ignored; 0 measured; 379 filtered out
```

Reverted; green. Row 1's positive is proved by the whole-table green (an unconditional
`false` predicate would fail row 1); row 4 is the documented limitation (§6).

**Post-mutation state verified**: `git diff -- crates/cb-train/src/boosting.rs` → **empty**.
No mutation is live.

## 5. Validation commands

| command | result |
|---|---|
| `cargo test -p cb-train --lib device_ctr_combo_config_tests` (baseline, pre-change) | **PASS** — 6 passed, 0 failed (+1 `non_snake_case` warning) |
| `cargo test -p cb-train --lib device_ctr_combo_config_tests` (post-change) | **PASS** — 6 passed, 0 failed, 0 warnings |
| `cargo test -p cb-train --lib` | **PASS** — 385 passed, 0 failed, 0 ignored |
| `cargo clippy -p cb-train --lib --profile test` | **PASS (no new lint)** — 16 warnings, only two in this file (`useless_vec` at `:104` and `:324`), **both pre-existing** in `build()` / `an_unknown_projection_member_is_a_typed_error`, neither touched by T00 |
| `cargo fmt -p cb-train -- --check` (scoped read) | file has 3 residual diffs (`:112`, `:134`, `:144`) — **all pre-existing** lines; the one diff my new code produced (`assert_eq!(actual, expected,` collapse) was applied. NB the crate has **836** pre-existing `Diff in` hunks, so `cargo fmt --check` is **not** a repo gate. |
| `git diff --stat -- crates/cb-train/src/boosting.rs` | **empty** — no production change |

Device commands (`--no-default-features --features rocm`, `run_device_tests.sh`) were
**not** run: T00 changes no production code and no device-facing file, and §2.5's isolation
rule forbids suite-wide runs during the mutation window. Regression scope per the task is
`cargo test -p cb-train --lib` only.

## 6. Deviations from the plan

1. **Extra rows 9/10 NOT added.** The task offered `simple/Buckets/b=0` and
   `simple/Borders/b=1` (both `flips_at: T10`) to resolve row 4's two-attribute ambiguity,
   calling it "low impact either way". Declined, for two reasons now recorded in the table's
   doc block: (i) appending them would either renumber the rows later tasks are specified
   against, or force a `flips_at` that is conditional on the *shape* of T10's edit
   ("global removal" vs "conditional on Buckets"), which is exactly the ambiguity the
   one-task-per-row contract is meant to exclude; (ii) T23 collapses the predicate and
   retires the table. **Consequence, stated plainly**: row 4 is the one row with no
   single-conjunct mutation proof — deleting only the type conjunct leaves it false via the
   target-border conjunct and vice versa, and the loop aborts at row 3 before reaching it
   when both are deleted. Documented verbatim in the source.
2. **Two extra mutations executed** (2 and 3 above) beyond the one mandated. Strengthens
   evidence; both reverted and verified.
3. **One rustfmt-driven cosmetic change** inside my own new assert (`actual, expected` on
   one line). Not a plan step; zero behavioural effect.
4. **Concurrency observation, not a deviation**: `git diff --stat` shows
   `crates/cb-train/src/ctr/calc_ctr_test.rs | 50 +++++` from the **parallel wave-0 T03**
   agent working in the same tree. T00 did not touch it. It is why
   `cargo test -p cb-train --lib` reports 385 rather than the 384 implied by the baseline
   filter count (T00 is net-zero on test count: two retired, two added). Coordinator note:
   the three §2.5 mutations were each live for ≲15 s while only the single focused test ran,
   but the tree is shared — if T03 ran a `cb-train` build inside one of those windows it
   would have seen a transiently-open gate. No suite-wide command was issued by T00 while
   mutated, which is the rule §2.5 states.
5. **Worktree**: none created (`planning/settings.json → implementation.use_worktree: false`,
   and the coordinator directed work into the main checkout on
   `feat/device-ctr-full-coverage`). `PLAN.md`'s frontmatter still names the stale
   `gpu-borders-shared-sample` worktree; not corrected — out of T00's file scope.

## 7. Contract handed to the later gate tasks

T01, T10, T12, T16, T19, T23 must each, **in the same commit as their conjunct deletion**,
flip exactly the rows their `flips_at` note names, and must run
`cargo test -p cb-train --lib device_ctr_combo_config_tests`. Row 1 and
`an_all_simple_borders_set_stays_covered` (D-04) may **not** be flipped by any P1 task.

**Rollback**: restore the two original functions from `a0a67ec`. Nothing depends on the
rewrite except the later tasks' one-row updates.
