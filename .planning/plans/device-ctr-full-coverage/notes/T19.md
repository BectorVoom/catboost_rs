# T19 — Combination CTR end-to-end: un-ignore, `CountingGpu`, drop the arity conjunct (DCTR-17)

**Status: DONE.** Wave 6, third and last Track D task. Branch `feat/device-ctr-full-coverage`,
primary checkout (`planning/settings.json`, no leading dot, `implementation.use_worktree ==
false`). Base `8a81c23` plus T17's and T18's uncommitted work; `git status --short` recorded
before the first edit.

**Headline**: the cb-train gate's `col.projection.is_simple()` conjunct is **deleted** — the
gate's only remaining test is the CTR-type admission list — and
`device_ctr_combo_fit_test` runs **un-ignored**, **committing to the device**
(`CountingGpu.grown == 5`) at **`max |Δpred| = 2.082e-17`**, matching PLAN §6 assumption 5's
measured expectation `≈2.082e-17` to the printed digit. The T00 gate-state table's **row 2**
flipped to `true` in the same change; rows 1 and 3-10 are untouched. D-04 held exactly on all
four pre-existing e2es. `bash ./run_device_tests.sh` **24 PASS / 0 FAIL**.

**R-20 is STILL OPEN**, and T19 *measured* that rather than assuming it — §5.

---

## 1. What changed (file:symbol)

### `crates/cb-train/src/boosting.rs`

| symbol | change |
|---|---|
| `ctr_types_are_device_covered` **body** | the `col.projection.is_simple() &&` conjunct **deleted**. The `.all(..)` closure is now a single `matches!(ECtrType::from_i8(col.ctr_type), Some(Borders \| Buckets \| BinarizedTargetMeanValue \| Counter))`. The replacement in-source comment says in prose that there is no arity conjunct and points at the doc section — it deliberately **does not spell `is_simple`**, because `gate_body()` scans the RAW body including inline comments (T10 §4's trap, now applying to my own new pin). |
| `ctr_types_are_device_covered` **doc**, the FPP-11 escalation block | **rewritten** as *"The FPP-11 escalation is RESOLVED — what was actually wrong (DCTR-15/16/17, T17-T19)"*. The localisation (trees 0-2 structurally identical incl. tree 2's `[0,1] @ 4.0`; divergence at tree 3 level 0) is **kept** — it stood up. The two suspects are dispositioned per the task text: **suspect 2 (`bucket_count` semantics) REFUTED** with the citation (`TOnlineCtrUniqValuesCounts::Count` is `ComputeReindexHash` over the LEARN range at `topSize = Max<ui64>()`, `online_ctr.cpp:696-700`, the unbounded `topSize` from `cat_feature_options.cpp:236`) — the same observed-distinct-count quantity `combine_projection_bins` returns; **suspect 1 (`eligible_max`) CONFIRMED and fixed by T18**; and the **PRIMARY cause, previously unnamed — the device had no per-level candidate eligibility gate at all** — fixed by T17, with the `AddTreeCtrs` (`greedy_tensor_search.cpp:491-551`) rule and the `crate::tree::combination_ctr_eligible` mirror spelled out. Closes with the landed numbers (`2.082e-17`, `grown == iterations`) and the R-8 warning about `oblivious_trees.len()`. |

### `crates/cb-train/tests/device_ctr_combo_fit_test.rs`

| location | change |
|---|---|
| `#[ignore = "FPP-11 ESCALATED …"]` | **REMOVED**, together with the doc paragraph carrying its rationale. That rationale was factually wrong (*"this fit runs on the CPU grower and the arm-routing assertion below would fail"* — the only routing assertions are `oblivious_trees.len() == iterations` and the empty non-symmetric/region lists, all satisfied by the CPU grower). Per the task's rollback clause it is **not** preserved verbatim anywhere; the module doc records *why* it was wrong instead. |
| `mod device` | `CountingGpu` added, **verbatim** from `device_ctr_gate_test.rs` (the canonical copy), with a keep-in-sync note. Seventh copy — see §6. |
| `run()` | the fit is driven through `CountingGpu` instead of `GpuBackend::default()`; `assert_eq!(gpu.grown.get(), params.iterations)` added **AFTER** the ≤1e-5 loop (T20 §2 — the in-source comment explains why and says not to "fix" the ordering); `println!("[device-ctr-combo-e2e] device grows = …")`. |
| `run()` — NEW assertion | a **combination vacuity guard**: ≥1 chosen CTR split must carry a ≥2-member projection. The module doc has always *claimed* the test "re-asserts the same property on the trained Rust model"; it did not. It does now, and the printed line reports the count (`8 CTR splits (3 of them ≥2-member combinations)`). This is T13 §2's rule applied to this track: `≥1 CTR split` alone is satisfied by a fit that silently degraded to simple projections, which is exactly what DCTR-17 exists to exclude. |
| kept unchanged | the `assert_ne!(create_shuffled_indices(n, seed), averaging_ctr_permutation(n, 1, seed))` structure-vs-averaging divergence guard (GLOBALS §2.2), the `n_ctr_splits >= 1` guard, the two shape assertions, `combo_params()`, and the fixture (FROZEN, §2.6 — untouched). |
| module doc | rewritten: DCTR-17 framing, the D-1/D-2 dependency, and a *"Why this test was `#[ignore]`d, and what un-ignoring it required (R-8)"* section recording the 0.01 s / `1.388e-17` CPU-fallback false pass and why a small delta is never reassurance. |

### `crates/cb-train/src/device_ctr_combo_config_test.rs` (T00's file)

* **Row 2** (`combo[0,1]/Borders/b=0/denom=1.0`) `expected: false → true`, in the SAME change as
  the conjunct deletion (T00's `flips_at` contract), with a `flips_at` note recording that the
  row varies exactly ONE attribute against row 1 and naming its device evidence. The ASCII
  table in the doc block was updated to match. **Rows 1 and 3-10 untouched.**
* Clause **(b)** of the "conscious re-opening" module doc — T00's forward promise — now reads
  as **DISCHARGED**, not scheduled: `#[ignore]` is gone, the `CountingGpu` assertion exists,
  the run is green. Clause **(c)** now cites the numbers as **re-measured on the landed code**
  (`2.082e-17`, `grown = 5`, 1.7 s) rather than off the reverted spike, and states that
  `grown`, not the delta difference, is the proof.

### `crates/cb-train/src/boosting_ctr_gate_test.rs` (T01's file, extended per its contract)

A new `DCTR-17 (T19)` section, **two tests, in the file's mandated shape**:

1. `combination_arity_is_structurally_bounded_and_carried_whole` — the structural pin. Unlike
   the other three conjunct removals this one is *not* justified by the tested attribute being
   constant (a combination column is genuinely reachable, and admitting it genuinely changes
   the scored candidate set). What makes it a **coverage** change rather than a semantic one is
   three facts, each asserted: (a) arity is bounded by `max_ctr_complexity` at ONE producer —
   `tensor_ctr_candidates` (`max_ctr_complexity == 1` ⇒ SimpleCtrs only; `2`/`3` ⇒ at least one
   combination and never longer than the bound); (b) `TProjection` keeps members sorted and
   de-duplicated, the order `combined_hash` folds in and the order `build_device_ctr_config`
   emits `member_bins`/`projection_members` in, so nothing re-sorts; (c) **source scans** — one
   `DeviceCtrColumn` construction in `boosting.rs`, exactly two `build_columns(` calls
   (structure + averaging), one `tensor_ctr_candidates(` call. (c) is T10 §1's single-producer
   pairing made executable, which only becomes load-bearing now that combinations reach the
   device. The doc states plainly what the test *cannot* say (the device's per-level candidate
   semantics — that is `cb-backend`'s `resident_combination_eligible`).
2. `the_device_gate_no_longer_reads_the_projection_arity` — `gate_body()` must not contain
   `is_simple`. Red before the deletion, green after (measured under MUT-2, §3.3).

### `crates/cb-backend/src/gpu_runtime/{mod.rs, session.rs, ctr_eligibility_test.rs}` — **comments only**

Five stale forward references written by T17/T18 in the form *"until T19 drops the cb-train
gate's arity conjunct …"* now read as fulfilled, and the R-20 paragraphs carry T19's
measurement (§5) instead of a structural claim that is no longer true. **Zero executable change
in `cb-backend`** — verified: `cargo test -p cb-backend --no-default-features --features rocm`
is `277 passed / 0 failed / 2 ignored`, identical to T18's figure.

`run_device_tests.sh` was **NOT** touched (C-8 — **T24** owns it). It already lists
`device_ctr_combo_fit_test`, so this change converts a vacuous "1 ignored" entry into a real
run without changing the array (`git diff --stat run_device_tests.sh` empty).

---

## 2. Red → Green (verbatim)

### 2.1 Baseline, captured BEFORE the first edit (D-04)

```
[device-ctr-e2e]         1 CTR splits; max |Δpred| = 4.483e-11 (bar 1e-5);  device grows = 5
[device-ctr-buckets-e2e] 8 Buckets CTR splits; chosen target_border_idx = [0]×8 (distinct [0])
[device-ctr-buckets-e2e] max |Δpred| = 2.776e-17 (bar 1e-5);                device grows = 5
[device-ctr-counter-e2e] 5 Counter CTR splits
[device-ctr-counter-e2e] max |Δpred| = 1.388e-17 (bar 1e-5);                device grows = 5
[device-ctr-btmv-e2e]    5 BTMV CTR splits; max bucket occupancy = 15
[device-ctr-btmv-e2e]    max |Δpred| = 2.776e-17 (bar 1e-5);                device grows = 5
```

and the pre-change `--ignored` run of the combo test, i.e. the false pass being retired:

```
running 1 test
[device-ctr-combo-e2e] 8 CTR splits; max |Δpred| = 1.388e-17 (bar 1e-5)
test device_ctr_combo_fit_matches_upstream_predictions ... ok
test result: ok. 1 passed; … finished in 0.01s
```

`1.388e-17` in **0.01 s** — the CPU-fallback fingerprint exactly (T20 §1), on a test whose name
and doc claim the device. `cargo test -p cb-train --lib device_ctr_combo_config_tests`: 8 passed.

### 2.2 The Red — `#[ignore]` removed and `CountingGpu` added, gate NOT yet edited

`cargo test -p cb-train --no-default-features --features rocm --test device_ctr_combo_fit_test -- --nocapture`:

```
running 1 test
[device-ctr-combo-e2e] 8 CTR splits (3 of them ≥2-member combinations); max |Δpred| = 1.388e-17 (bar 1e-5)

thread 'device_ctr_combo_fit_matches_upstream_predictions' (21463) panicked at crates/cb-train/tests/device_ctr_combo_fit_test.rs:287:9:
assertion `left == right` failed: the combination-CTR fit must COMMIT to the device: expected 5 device grows, got 0. `oblivious_trees.len() == iterations` above does not say this — the CPU oblivious grower satisfies it too (R-8), which is why this test was a false pass for as long as it was `#[ignore]`d with a wrong rationale.
  left: 0
 right: 5
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.01s
```

**Exactly the plan's predicted ``left: 0, right: 5`` on `gpu.grown.get()`.** Note the ordering
pays off immediately: the passing `max |Δpred|` line prints *before* the panic, so this single
run is itself the evidence that the ≤1e-5 bar cannot detect a fallback. Runtime 0.01 s.

### 2.3 The Green — delete the arity conjunct + flip row 2

```
running 1 test
[device-ctr-combo-e2e] 8 CTR splits (3 of them ≥2-member combinations); max |Δpred| = 2.082e-17 (bar 1e-5)
[device-ctr-combo-e2e] device grows = 5
test device_ctr_combo_fit_matches_upstream_predictions ... ok
test result: ok. 1 passed; … finished in 1.70s
```

**`max |Δpred| = 2.082e-17`** — PLAN §6 assumption 5's `≈2.082e-17` reproduced on the landed
code (it was an *expectation* from a reverted gfx1151 spike, and it is now a measurement; the
asserted bar remains `≤1e-5` plus `grown == iterations`, never the 17th digit). It is
**different** from the CPU fallback's `1.388e-17`, and the runtime moved 0.01 s → 1.70 s.
`cargo test -p cb-train --lib device_ctr_combo_config_tests`: 8 passed, row 2 green at `true`.

### 2.4 Refactor

The doc-block rewrite, T00's clause (b)/(c) update, the two new gate pins, and the five stale
`cb-backend` forward references. One assertion was corrected while writing the structural pin:
`build_columns(` matches **2** lines, not 3 — the closure's own `let build_columns = |…|`
binding carries no `(`. Observed failure (fixed by asserting the true count, not by loosening):

```
assertion `left == right` failed: … Found: ["let columns = build_columns(materialized_ctr_features, &mut group_keys)?;", "let avg_columns = build_columns(averaging_ctr_features, &mut group_keys)?;"]
  left: 2
 right: 3
```

---

## 3. §2.5 mutation evidence — both mandated mutations, plus two extras

Protocol for every one: mutate → run **only** the single focused test → record verbatim →
**revert by targeted textual edit** (T12 §5 — `git checkout <file>` was **never** run) →
re-run green. No mutation was live during any suite-wide command.
`grep -rn "MUTATION-T19" crates/ --include=*.rs` ⇒ **empty**, and `git diff --stat` on the two
`cb-backend` files was re-read after each revert and is byte-identical to the pre-mutation
figures (`mod.rs 224`, `session.rs 56`).

### 3.1 MUT-1 (mandated, R-2 / C-5 scope check) — hoist `chosen_ctr_projections` to FIT lifetime

The real hoist, not a proxy: a `chosen_ctr_projections: Vec<Vec<u32>>` field added to
`CtrSearchState` (`session.rs`) and initialised in `begin_inner`, a
`pub chosen_ctr_projections: &'a mut Vec<Vec<u32>>` field on `ResidentCtrSearch` (`mod.rs`),
wired at the single view literal, and the tree-local `let mut chosen_ctr_projections` deleted
with its three uses re-pointed at `cs.`. Isolated to `--test device_ctr_combo_fit_test`:

```
thread 'device_ctr_combo_fit_matches_upstream_predictions' (26639) panicked at crates/cb-train/tests/device_ctr_combo_fit_test.rs:270:13:
obj 0: device CTR prediction 0.006279728723751803 vs upstream -0.02117900175611364 exceeds ≤1e-5 (|Δ|=2.746e-2)
test result: FAILED. 0 passed; 1 failed; … finished in 1.68s
```

**`|Δ| = 2.746e-2`** — bit-for-bit the CONTROL-ARM number PLAN/T00 recorded for "the arity gate
simply opened with no per-level eligibility gate". Two things follow, and both are worth
carrying: (i) C-5's predicted failure mode is real and this fixture detects it — under a
fit-lifetime list, tree ≥ 1 level 0 admits a combination upstream structurally cannot have;
(ii) the control arm's `2.746e-2` and the fit-lifetime arm's `2.746e-2` coinciding is not a
coincidence — a fit-lifetime list makes the gate vacuous from tree 1 onward, which is
observationally the same as having no gate. Reverted; combo e2e green at `2.082e-17` again.

### 3.2 MUT-2 (mandated, R-8 gate-closed check) — restore `col.projection.is_simple() &&`

```
running 1 test
[device-ctr-combo-e2e] 8 CTR splits (3 of them ≥2-member combinations); max |Δpred| = 1.388e-17 (bar 1e-5)

thread 'device_ctr_combo_fit_matches_upstream_predictions' (28434) panicked at crates/cb-train/tests/device_ctr_combo_fit_test.rs:287:9:
assertion `left == right` failed: the combination-CTR fit must COMMIT to the device: expected 5 device grows, got 0. …
  left: 0
 right: 5
test result: FAILED. 0 passed; 1 failed; … finished in 0.01s
```

**Fails on `grown.get()`, NOT on the prediction bar** — which passed, at `1.388e-17`, and
printed first. That is precisely the completion evidence DCTR-17 asks for, from one run.

### 3.3 MUT-2 extras (same mutation, two more focused runs) — the two new pins discriminate

Row 2's flip is wired to the real predicate, and **only** row 2 moves:

```
the device CTR gate does not admit the set this wave pins:
row 2 (combo[0,1]/Borders/b=0/denom=1.0): expected true, got false
   flips_at: FLIPPED by T19 — deleted `col.projection.is_simple()` (DCTR-17), the LAST conjunct besides the type list. …
test result: FAILED. 7 passed; 1 failed; 0 ignored; 0 measured; 393 filtered out
```

and the new source pin:

```
test boosting::boosting_ctr_gate_tests::the_device_gate_no_longer_reads_the_projection_arity ... FAILED
… panicked at crates/cb-train/src/boosting_ctr_gate_test.rs:686:5
test result: FAILED. 12 passed; 1 failed; 0 ignored; 0 measured; 388 filtered out
```

Reverted; both modules green (`8 passed`, `13 passed`) and the combo e2e green.

### 3.4 OBS — the R-20 probe (an OBSERVATION, not a completion claim)

D-2's call site reverted to the pre-T18 unfiltered
`cs.bucket_counts.iter().copied().max().unwrap_or(1).max(1)` — T22's mutation 1, run against
the combo e2e now that combinations are reachable:

```
[device-ctr-combo-e2e] 8 CTR splits (3 of them ≥2-member combinations); max |Δpred| = 2.082e-17 (bar 1e-5)
[device-ctr-combo-e2e] device grows = 5
test result: ok. 1 passed; … finished in 1.67s
```

**Byte-identical to the filtered run in every printed quantity.** See §5. Reverted;
`grep MUTATION-T19` empty.

**Why `combination_arity_is_structurally_bounded_and_carried_whole` was not separately
mutated**: its three source-scan counts are the mutation-sensitive part and each is a
count-equality that any edit to the scanned construct changes by ±1 (the `build_columns`
miscount in §2.4 is an in-flight demonstration that the assertion is not vacuous); its
behavioural halves are `tensor_ctr_candidates`/`TProjection` invariants already
mutation-covered by `projection_test.rs` / `candidates_test.rs`. Recorded as a judgement, not
as an omission.

---

## 4. Validation commands

| command | result |
|---|---|
| `cargo test -p cb-train --lib device_ctr_combo_config_tests` (**CRITICAL-1**) | **PASS** — `8 passed; 0 failed`; **row 2 flipped, rows 1 and 3-10 unmoved** |
| `cargo test -p cb-train --lib boosting_ctr_gate_tests` | **PASS** — `13 passed; 0 failed` (T16's 11 + T19's 2) |
| `cargo check -p cb-train --no-default-features --features rocm --test device_ctr_combo_fit_test` | **PASS** |
| `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_combo_fit_test` | **PASS** — **`grown = 5`, `max \|Δpred\| = 2.082e-17`**, 8 CTR splits / 3 combinations, 1.58-1.70 s |
| `… --test device_ctr_fit_test` | **PASS** — **`4.483e-11` UNCHANGED**; `grows = 5` |
| `… --test device_ctr_buckets_fit_test` | **PASS** — **`2.776e-17` UNCHANGED**; `[0]×8`; `grows = 5` |
| `… --test device_ctr_counter_fit_test` | **PASS** — **`1.388e-17` UNCHANGED**; `grows = 5` |
| `… --test device_ctr_btmv_fit_test` | **PASS** — **`2.776e-17` UNCHANGED**; occupancy 15; `grows = 5` |
| `… --test device_ctr_type_gate_test` | **PASS** — `4 passed`; T13's square unmoved (`5/5`, `5/5`, `0/5`, `0/5`) |
| `… --test device_ctr_gate_test` | **PASS** — `2 passed` |
| `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` | **PASS** — `3 passed` |
| `cargo test -p cb-train --test ctr_mixed_simple_vs_combo_oracle_test` | **PASS** — `2 passed` |
| `bash ./run_device_tests.sh` | **PASS — 24 PASS / 0 FAIL** (23 binaries + perf lane; Poisson **10.7×** / **11.8×** across two runs, no R-13 flake). `device_ctr_combo_fit_test` now a REAL pass, not "1 ignored" |
| `cargo test -p cb-backend --no-default-features --features rocm` | **PASS** — lib `277 passed; 0 failed; 2 ignored` (**identical to T18** — T19's cb-backend diff is comments only) + `4` + `1` + `0` |
| `cargo check -p cb-backend --no-default-features --features rocm --lib --tests` | **PASS** — only the 3 pre-existing warnings |
| `cargo check -p cb-backend --no-default-features --features wgpu` | **PASS**, **0 errors**; only the pre-existing wgpu-only unused-import warnings |
| `cargo check -p cb-compute` | **PASS**, zero diagnostics |
| `cargo test -p cb-compute` | **PASS** — `206 + 5 + 1 + 3 + 9`, zero failures |
| `cargo test -p cb-train --lib` | **PASS** — **`401 passed; 0 failed`** (399 at T16/T17/T18 + T19's 2 gate pins) |
| `cargo test -p cb-train` (all targets, default cpu) | **PASS** — **110 targets**, every one `test result: ok`, zero `FAILED` |
| `cargo clippy -p cb-train --lib` | **PASS** — the only warning is the transitively-compiled `cb-backend` `duplicated_attributes` |
| `cargo clippy -p cb-backend --no-default-features --features rocm --lib --tests --keep-going` | **pre-existing FAIL, unrelated** — single `error` = `erasing_op` at `kernels/score_split.rs:374`; **18 warnings**, the same count T17/T18 recorded; **zero name any file T19 touched** (the 4 `doc list item without indentation` are in `kernels/grow_loop.rs`) |
| `git diff --stat run_device_tests.sh` | **empty** — C-8 honoured |

### Pre-existing, NOT chased (T24's list) — nothing new discovered

* `clippy::erasing_op` at `kernels/score_split.rs:374` (T08) — still the only `error` on the
  rocm clippy lane.
* `clippy::duplicated_attributes` at `gpu_runtime/mod.rs:4535`/`:4567` (T10, line-shifted by
  T18) — **unmoved by T19** (T19 inserts no code above it).
* The 12 `cb-train` integration-test clippy targets (T04) — untouched.
* Default-cpu `cb-backend --lib`: re-measured at T19 = **`227 passed; 60 failed; 2 ignored`**,
  **identical to T18's measurement**, confirming T17's 59-60 RANGE from a third observation.
  All 60 in `kernels::*`; T19's `cb-backend` diff is comments only.

---

## 5. ⚠ R-20 STATUS: **STILL OPEN** — and T19 measured it rather than assuming it

T18 could not close R-20 for a *structural* reason: while the arity conjunct stood, every
column reaching pass C had one member, so D-2's filter was the identity on every reachable
input. **T19 removes that structural excuse** — ≥2-member columns now reach pass C, and the
combo fit chooses 3 of them. So the natural question is whether D-2 now discriminates.

**Measured (§3.4): it does not, on this fixture.** Reverting D-2's call site to the unfiltered
`.max()` leaves `device_ctr_combo_fit_test` byte-identical in every printed quantity —
`2.082e-17`, `grown = 5`, 8 CTR splits of which 3 are combinations. (Plausible reason, not
verified: on `ctr_device_combo` the two cat columns' simple projections and their 2-member
combination have bucket counts whose maximum is unchanged by dropping an ineligible column at
the levels where one is ineligible; a fixture with a much larger-cardinality combination would
be needed. That is a *hypothesis* — T22 owns the measurement, not this note.)

⇒ **R-20 stays OPEN.** T22's mutation 1 remains the designated measurement and must compare the
device **split SEQUENCE**, not predictions — the coordinator's consolidated finding (T10 §2 +
T16) already constrains how. What T19 changes is the *reason* R-20 is open: no longer "the
filter is structurally inert", now "the filter is live but no committed fixture moves under
it". Both facts are mirrored in-source (the helper's doc, the pass-C comment, and
`ctr_eligibility_test.rs`'s module doc) so a later reader cannot reach the code without meeting
them. **Do not read T19's green as closing R-20**, and do not cite T18's unit tests as closure.

---

## 6. Deviations from the task text

1. **One extra production-adjacent assertion in the e2e: the combination vacuity guard.** The
   task lists the guards to keep (`assert_ne!` divergence, `n_ctr_splits >= 1`) and does not
   ask for a third. Added anyway, because the file's module doc has always claimed the test
   "re-asserts [the ≥2-member projection] property on the trained Rust model" and it did not —
   and because T13 §2 establishes that `≥1 CTR split` is exactly the guard that ships green and
   vacuous. It is the cheapest possible check (a `cat_features().len() >= 2` count over
   `ctr_splits`) and it reports its count in the printed line.
2. **Two extra §2.5 runs beyond the two mandated** (§3.3): the same MUT-2 mutation re-used to
   show that T00's row 2 and T19's new source pin each go red, so the *pins* are proved
   discriminating and not just the e2e.
3. **One extra mutation that is an OBSERVATION, not a §2.5 check** (§3.4 / §5): T22's mutation
   1 run early because T19 is the first moment it *can* be run. Recorded as "does not move",
   explicitly **not** claimed as R-20 closure. T22 still owns the split-sequence measurement.
4. **Five comment-only edits in `cb-backend`** (T17's and T18's `"until T19 …"` forward
   references and the two R-20 paragraphs). Not in the task's file list, but leaving them would
   have left three files asserting in prose that the arity conjunct still stands. Executable
   surface unchanged, proved by the identical `277 passed / 0 failed / 2 ignored`.
5. **The structural gate pin covers three facts rather than one constant.** The other three
   conjunct removals in this file rest on "the attribute is structurally constant"; arity is
   not, so the pin instead fixes the three properties that make admitting it a coverage change
   (bounded producer, sorted members, single seam producer). §1 and the test's own doc say what
   it deliberately does not prove.
6. **`build_columns(` count corrected from 3 to 2** while writing the pin (§2.4) — a
   fix-the-assertion-to-the-truth, made before the test had ever been green, not a loosening.
7. **`run_device_tests.sh` untouched** (C-8, T24 owns it) and **no new test binary added** —
   T19 un-ignores an already-registered one. `CountingGpu`'s duplication count rises to
   **SEVEN**.

---

## 7. Rollback

Restore `col.projection.is_simple() &&` as the first conjunct of the `.all(..)` closure; flip
gate-state row 2 back to `expected: false` in the same edit; re-add `#[ignore]` to
`device_ctr_combo_fit_matches_upstream_predictions` with a **corrected** rationale (the
original's is wrong and must not be restored verbatim — the honest one is *"the cb-train gate
declines non-simple projections, so this fit falls back to the CPU and the `CountingGpu`
assertion fails at `grown == 0`; the ≤1e-5 bar still passes, at `1.388e-17`"*). The doc-block
rewrite, the new gate pins, the combination vacuity guard and the `cb-backend` comment updates
are independent and may stay; T17's D-1 and T18's D-2 must **not** be reverted with this — they
are correct in their own right and reverting D-1 alone reproduces `2.746e-2` (§3.1).

---

## 8. For T21, T22, T23, T24

* **T22 owns R-20's measurement and it is now a sharper question** — §5. The combo e2e does not
  move under an un-wired D-2 even with combinations reachable.
* **T22/T21: `device_ctr_combo_fit_test` is the R-2 detector** and it is sensitive — a
  fit-lifetime `chosen_ctr_projections` costs `2.746e-2` (§3.1). Any later change to the
  eligibility list's scope has a ready-made bar.
* **T23 must now retire or update ELEVEN source-scan pins** in `boosting_ctr_gate_test.rs`
  (T01's 2 + T10's 4 + T16's 3 + T19's 2), not the nine T16 counted. T19's
  `the_device_gate_no_longer_reads_the_projection_arity` asserts on `gate_body()` text and
  `combination_arity_is_structurally_bounded_and_carried_whole` scans `boosting.rs` for
  `DeviceCtrColumn {` / `build_columns(` / `tensor_ctr_candidates(` counts — the latter is
  about `build_device_ctr_config`, not the gate, so T23's gate rewrite need not touch it.
* **T23: the gate is now a single `matches!` over `from_i8`** — its rewrite to delegate to
  `is_cpu_supported` is a near-identity transformation with no other conjunct to preserve.
* **T24**: `CountingGpu` duplication is **SEVEN**; `run_device_tests.sh` still needs the four
  new binaries registered (T10/T12/T13/T16's) — T19 adds none.
