# T21 — One-hot × CTR keeps declining, plus the surviving-clause boundary pins (DCTR-03)

**Status: DONE.** Wave 7, second of the two parallel tasks (T22 landed first; its files were
read before the first edit and its mutation windows were closed — `git status --short`
showed only T22's committed-pending work when T21 started). Branch
`feat/device-ctr-full-coverage`, primary checkout (`planning/settings.json`, no leading dot,
`implementation.use_worktree == false`). Base `657b7dd` + T22's uncommitted work. D-04
baseline captured **before the first edit**.

**Headline**: the `one_hot_bins.is_empty()` device conjunct is **still in the source** and is
now **measured** to be the live second layer, not merely asserted to be one — with the
SPEC-OH-26 rejection alone disabled the mixed pool trains at `grown == 0`, and with both
disabled it **commits at `grown == 5`**. `device_ctr_type_gate_test.rs` gains the two
remaining acceptance-9 boundary pins plus a control:

```
[device-ctr-type-gate] float-half-control: n_float=2 borders=[15, 15] trees=5 ctr_splits=5  grown=5/5
[device-ctr-type-gate] cat-only:           n_float=0 borders=[]       trees=5 ctr_splits=10 grown=0/5
[device-ctr-type-gate] borders-7:          n_float=2 borders=[7, 7]   trees=5 ctr_splits=5  grown=0/5
```

Production change: **comment only** (`boosting.rs`, `48 7`, every changed line a `//`
comment — verified by diff filter). D-04 held exactly. `bash ./run_device_tests.sh`
**24 PASS / 0 FAIL**.

---

## 1. Which clauses ACTUALLY survive in the current gate (the task asked; measured, not
## taken from the plan's snapshot)

`ctr_types_are_device_covered` (`crates/cb-train/src/boosting.rs`, `fn` at `:2509`) is now
**one conjunct**: `!cols.is_empty()` plus a single `matches!` over
`ECtrType::from_i8(col.ctr_type)` naming `{Borders, Buckets, BinarizedTargetMeanValue,
Counter}`. The type conjunct in its old `== Borders.as_i8()` form, the arity conjunct, the
`target_border_idx == 0` conjunct and `prior_denom == 1.0` are **all gone** (T01/T10/T12/
T16/T19). ⇒ **no P2/P3 boundary of this task lives inside that predicate**, which is why
every T21 pin targets a clause OUTSIDE it. Verified by reading the current source, not the
plan's snapshot.

The CTR-side exclusions that survive, and where each one now lives:

| surviving clause | site (locate by text; lines drift) | T21 pin | inverts at |
|---|---|---|---|
| `learning_folds_for_cycle == 1` | `boosting.rs`, CTR arm of `device_host_eligible` (`:4640`) | `device_ctr_gate_test::multi_permutation_ctr_declines_to_device` (re-run, §3.2) | **P3 (C-2)**, anchored `pc=4`/`seed=0` (R-17) |
| `one_hot_bins.is_empty()` | same conjunction (`:4641`) | `device_fpp_composition_test::one_hot_x_ctr_still_declines` (re-run, §3.1) | **never — retained by design (DCTR-03)** |
| the CTR type admission list | `ctr_types_are_device_covered` | `boosting_ctr_gate_test.rs` (T16/T19), `session_ctr_type_test` | P2/P3 only for GPU-only types, which stay declined |
| `eval_sets.is_empty()` | same conjunction (`:4696`) | T13's 2×2 square in this file | **P3** |
| `has_any_scorable_feature` | `boosting.rs:3500`, clause 11 of the same conjunction | **NEW** `cat_only_ctr_pool_declines_to_device` | **P2 (C-5)** |
| `col.borders.len() + 1 == n_bins` | `cb-backend` `session.rs`, BOTH `ctr_covered` closures (`:204`, `:217`) | **NEW** `non_15_border_count_ctr_pool_declines_to_device` | **P2 (C-1, R-11)** |

Two of these are **not single-layer**, which is the substantive finding of this task — §3.

---

## 2. What changed

### `crates/cb-train/src/boosting.rs` — COMMENT ONLY (`48 7`)

1. **The DCTR-03 rationale**, immediately above the `one_hot_bins.is_empty()` conjunct, as
   the task specifies: the conjunct is **UNREACHABLE-TRUE** today, with the case analysis
   over `partition_cat_columns` written out (all-one-hot ⇒ no CTR columns ⇒ the
   `materialized_ctr_features.is_empty()` arm is taken and this arm is never evaluated;
   all-CTR ⇒ `one_hot_bins` is empty; mixed ⇒ `train_inner` already returned
   `CbError::Unsupported` at SPEC-OH-26); that it is **deliberately retained** as defence in
   depth per DCTR-03 with the reason (the day the CPU gains a three-way candidate union,
   SPEC-OH-26 goes away and one-hot × CTR would reach the device with zero device testing);
   that `one_hot_x_ctr_still_declines` is its pin; and the **measured** §3.1 numbers, so a
   later reader meets the evidence rather than the claim.
2. **A stale-comment correction in the same block** (deviation 1, §6). The GDC-11 comment
   above the disjunction still said *"Anything else — multi-permutation, combination
   projections, non-Borders types, one-hot × CTR — still falls back"* and described the
   admitted set as *"simple Borders projections"*. Both were falsified by T10/T12/T16/T19.
   It now names the three conjuncts that actually survive and the negative test each carries.

**No executable change.** `git diff` on the file, filtered to non-`//` lines, is empty.

### `crates/cb-train/tests/device_ctr_type_gate_test.rs` (T13's file, T21 co-owns) — `308 4`

* `device::assert_float_half_route(tag, float_columns, borders, expect)` — the boundary-pin
  runner. Deliberately the **same recipe** as T13's committing cell (`skiptest+noeval`):
  same frozen `ctr_device_counter` learn set, same `ctr_params(SkipTest)`, same
  `train_cat_with_eval_sets` entry point, empty eval-set slice — with **exactly one**
  attribute varied. It carries T13's full non-vacuity chain (fit succeeded, all 5 trees
  grown, ≥1 CTR split, every chosen split's `ctr_type == Counter`) because `grown == 0`'s
  mirror-image hazard is identical.
* **`unmodified_float_half_commits_to_device`** — the control arm, `grown = 5`. See §3.4 for
  why it exists rather than being redundant with T13's cell.
* **`cat_only_ctr_pool_declines_to_device`** — `// P2 WILL INVERT THIS (C-5).`
* **`non_15_border_count_ctr_pool_declines_to_device`** — `// P2 WILL INVERT THIS (C-1).`
  `TRUNCATED_BORDER_COUNT = 7` (⇒ device `n_bins == 8` against the CTR columns'
  `15 + 1 == 16`), with an in-test assertion that the fixture's borders really are
  `ctr_border_count_default()` long before truncating, so the pin cannot go vacuous if the
  frozen fixture is ever re-baselined at a different width.
* **Report-before-assert ordering** in the runner (T20 §2 generalised): `grown`,
  `ctr_splits` and the tree count are PRINTED before the non-vacuity guards, so a §2.5
  mutation run yields the observable even when a guard is the assertion that fires. This
  was not cosmetic — MUT-3's first run (§3.3) failed on the vacuity guard and hid
  `grown = 5`, the number the completion criterion asks for. An in-source comment says not
  to "fix" the ordering.
* Module doc: a DCTR-03 section with the pin/clause/inversion table and the statement that
  both new pins assert the OBSERVABLE only.

### `crates/cb-train/tests/device_fpp_composition_test.rs` — DOC COMMENT ONLY (`24 0`)

The mandatory P2/P3 inversion annotations (checker MINOR-13):

* `one_hot_x_ctr_still_declines` — **`RETAINED BY DESIGN (DCTR-03) — not a P2/P3
  boundary.`**, plus why it asserts the observable and both §3.1 measurements.
* `multi_permutation_x_ctr_still_declines` — `P3 WILL INVERT THIS (C-2)`, pointing at the
  sibling pin T21 actually mutation-checked.

### `crates/cb-train/tests/device_ctr_gate_test.rs` — DOC COMMENT ONLY (`14 0`)

`multi_permutation_ctr_declines_to_device` — `P3 WILL INVERT THIS (C-2)` + the §3.2
overdetermination finding, so the next reader does not repeat the mutation and conclude the
test is broken.

### NOT changed

No fixture (`crates/cb-oracle/fixtures/**` byte-untouched — §2.6). No `run_device_tests.sh`
(**C-8 — T24 owns it**; §7). No `cb-backend` file (`git diff --stat` on
`gpu_runtime/session.rs` is empty; `mod.rs`'s diff is T22's, untouched by T21). No new test
binary — T21 adds three tests to an existing one. No production logic anywhere.

---

## 3. §2.5 mutation evidence — one recorded failure per pin, plus the complements

Protocol for every one: mutate → run **only** the single focused `--test <name>` → record
verbatim → **revert by targeted textual edit** (never `git checkout <file>`, T12 §5) →
re-run green. No mutation was live during any suite-wide command (`run_device_tests.sh`,
the package-wide `cb-backend` rocm suite and `cargo test -p cb-train` all ran after the
final revert). `grep -rn "MUTATION-T21" crates/ --include=*.rs` ⇒ **empty**, and
`git diff --stat` on every mutated file was re-read after each revert and was byte-identical
to its pre-mutation state.

### 3.1 Pin: `one_hot_x_ctr_still_declines` (the mandated mutation) — **RED, and the
### retained conjunct is MEASURED to be live**

The task mandates deleting the SPEC-OH-26 rejection **and** the `one_hot_bins.is_empty()`
conjunct together. T21 ran it as **two nested steps** so the retention argument is evidence
rather than assertion, with a temporary test-side probe printing `(ok, grown, err)` before
the existing `expect_err`.

**MUT-1a — SPEC-OH-26 alone disabled** (`if false && !one_hot_absolute.is_empty() && …`):

```
test fpp17_one_hot_x_ctr_still_declines ... MUTATION-T21 PROBE: ok=true grown=0 err=None

thread 'fpp17_one_hot_x_ctr_still_declines' (221833) panicked at crates/cb-train/tests/device_fpp_composition_test.rs:270:23:
a mixed one-hot/CTR pool must be REFUSED, not trained: (Model { oblivious_trees: [ … one_hot_absolute: [0] }, BakedCtrData { tables: [] })
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 5 filtered out; finished in 0.05s
```

⇒ the mixed pool now TRAINS, and **`grown = 0` in 0.05 s** — the CPU-fallback fingerprint
(T20 §1). The `one_hot_bins.is_empty()` conjunct is what holds it off the device. **This is
the measurement DCTR-03's retention decision rests on**, and no previously-shipped test makes
it.

**MUT-1b — BOTH disabled** (the mandated joint mutation; `&& (true || one_hot_bins.is_empty())`):

```
test fpp17_one_hot_x_ctr_still_declines ... MUTATION-T21 PROBE: ok=true grown=5 err=None

thread 'fpp17_one_hot_x_ctr_still_declines' (222705) panicked at crates/cb-train/tests/device_fpp_composition_test.rs:270:23:
a mixed one-hot/CTR pool must be REFUSED, not trained: (Model { oblivious_trees: [ObliviousTree { splits: [Split { feature: 0, border: 0.7093801498 }], ctr_splits: [], one_hot_splits: [OneHotSplit { feature: 0, value: 1 }], level_kinds: [Float(0), OneHot(0)], … } … ] })
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 5 filtered out; finished in 52.81s
```

⇒ **`grown = 5`** (the task's "fails with a non-zero `grown`") in **52.81 s** — the device
fingerprint. The mixed pool reaches the device grower, which is exactly the latent hazard
DCTR-03 retains the conjunct against. The test is **not** vacuous post-T19 and no
replacement is needed (C-18 discharged as written).

Reverted (both, textually); probe removed; `git diff --stat` on `boosting.rs` and
`device_fpp_composition_test.rs` **empty**; `--test device_fpp_composition_test` green,
`6 passed`.

### 3.2 Pin: `multi_permutation_ctr_declines_to_device` — a mutation that **PASSED**, and
### the diagnosis (T18 §3, not T17 MUT-1c)

**MUT-2a — the clause the pin claims, deleted** (`|| ((true || learning_folds_for_cycle == 1) && …`):

```
test multi_permutation_ctr_declines_to_device ... [ctr-multi-perm] device grows = 0
test single_permutation_ctr_commits_to_device ... [ctr-single-perm] device grows = 5
test result: ok. 2 passed; 0 failed; … finished in 1.99s
```

**GREEN.** Per the executor rules that means the test is blind to that clause *as stated* —
so the input was strengthened rather than the result accepted. Root cause, located in
source: `learning_folds_for_cycle` is ALSO handed to `begin_device_training` as `fold_count`
(`boosting.rs:5000`, `:5042`), and **every** backend coverage mapper independently declines
`fold_count != 1` (`cb-backend/src/gpu_runtime/session.rs:524`, `:627`, `:679`, `:731`,
`:786`).

**MUT-2b — both layers neutralised** (the host conjunct as above, **and** the two
`begin_device_training` call sites passing a literal `1` so the backend can no longer see
the fold count):

```
thread 'multi_permutation_ctr_declines_to_device' (227663) panicked at crates/cb-train/tests/device_ctr_gate_test.rs:163:9:
assertion `left == right` failed: [ctr-multi-perm] expected 0 device grows (permutation_count=4)
  left: 5
 right: 0
test result: FAILED. 1 passed; 1 failed; … finished in 27.55s
```

**RED at `left: 5, right: 0`** — the pin is discriminating for the boundary.

**MUT-2c — the complement** (host conjunct restored, backend's view of the fold count still
neutralised), driven per T18 §3 before choosing a diagnosis:

```
test multi_permutation_ctr_declines_to_device ... [ctr-multi-perm] device grows = 0
test result: ok. 2 passed; 0 failed; … finished in 2.62s
```

**GREEN.** ⇒ **Diagnosis: two mutually-redundant guards on the SAME host-computed quantity**
— either alone declines, so **no test can separate them**. This is T18 §3's mode ("a green
mutation that is NOT the T17 MUT-1c blindness mode"), not a blind test, and it is precisely
why the planner's decision to assert the observable is right for this pin too. Recorded
in-source on the test.

Reverted (all three edits, textually); `git diff --stat crates/cb-train/src/boosting.rs`
empty; `--test device_ctr_gate_test` green, `2 passed`, `grows = 0 / 5`.

### 3.3 Pin: `non_15_border_count_ctr_pool_declines_to_device` — **RED**, single-layer

**MUT-3** — `ctr_covered`'s `col.borders.len() + 1 == n_bins` neutralised in **both**
closures (`&& (true || col.borders.len() + 1 == n_bins)`, `session.rs:204` and `:217`):

```
running 1 test
test non_15_border_count_ctr_pool_declines_to_device ... [device-ctr-type-gate] borders-7: n_float=2 borders=[7, 7] trees=5 ctr_splits=0 grown=5/5

thread 'non_15_border_count_ctr_pool_declines_to_device' (231312) panicked at crates/cb-train/tests/device_ctr_type_gate_test.rs:555:9:
[borders-7] the trained model must contain ≥1 CTR split — without one this pin says nothing about CTR routing
FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 5 filtered out; finished in 15.74s
```

Two things in one run: **`grown = 5/5`** (the fit committed — the clause is the ONLY thing
declining it, so this pin is genuinely single-layer), and **`ctr_splits = 0`** — the CTR
columns' 16 bins forced into an 8-bin histogram line stop winning any candidate at all. That
is the silent degradation the shape check exists to prevent, and it is worth knowing that it
degrades silently rather than erroring.

**The first MUT-3 run reported only the panic** (the vacuity guard fired before the print),
which is what motivated the report-before-assert reordering in §2. The transcript above is
the re-run after the reorder — the mutation was still live, only the focused test was run,
and the production mutation itself was unchanged between the two runs.

Reverted textually; `git diff --stat crates/cb-backend/src/gpu_runtime/session.rs` **empty**;
`--test device_ctr_type_gate_test` green, `7 passed`.

### 3.4 Pin: `cat_only_ctr_pool_declines_to_device` — **overdetermined by four guards; the
### mutation cannot redden it, and the control is what proves it discriminating**

**MUT-4a** — the clause the pin claims (`has_any_scorable_feature` → `true || …`):

```
test cat_only_ctr_pool_declines_to_device ... [device-ctr-type-gate] cat-only: n_float=0 borders=[] trees=5 ctr_splits=10 grown=0/5
test result: ok. 1 passed; … finished in 0.01s
```

**GREEN.** Strengthened, layer by layer, each measured:

| mutation (cumulative) | site | result |
|---|---|---|
| MUT-4a `has_any_scorable_feature` → `true` | `boosting.rs:3500` | GREEN, `grown = 0`, 0.01 s |
| MUT-4b + `device_active`'s `device_n_bins > 0` neutralised | `boosting.rs:4990` | GREEN, `grown = 0`, 0.14 s |
| MUT-4c + the session `begin` preamble `n == 0 \|\| n_features == 0 \|\| n_bins == 0` neutralised | `session.rs:1681` | GREEN, `grown = 0`, 0.07 s |

The fourth layer is `ctr_covered`'s shape check again (`16 != 0` when there are no float
borders). **Driving all four was declined deliberately**: it produces a device configuration
with `n_features == 0` and `n_bins == 0`, and a red from an empty problem would say nothing
about the boundary. Diagnosis, in the same class as §3.2 but stronger: **four
mutually-redundant guards, all keyed on the single fact that a CTR pool with no float and no
one-hot column has an EMPTY device feature axis.** No single-clause mutation can redden this
pin, and that is a property of the code, not of the test.

**What proves the pin discriminating instead — and it ships**: `unmodified_float_half_commits_to_device`,
the same helper on the same recipe with the float half restored, reports **`grown = 5/5`**.
The `Route::Cpu` assertion is therefore sensitive to exactly the attribute the pin varies
and would fail the moment P2 moves the boundary. Both facts are written into the control's
doc comment so the next reader does not repeat the four mutations.

(Reverted textually after each rung; `grep -rn "MUTATION-T21"` empty; both mutated files
byte-identical to `HEAD`.)

---

## 4. Validation commands

| command | result |
|---|---|
| `cargo check -p cb-train --no-default-features --features rocm --test device_ctr_type_gate_test` | **PASS**, zero diagnostics |
| `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_type_gate_test` | **PASS** — `7 passed` (T13's 4 + T21's 3); control `5/5`, cat-only `0/5`, borders-7 `0/5` |
| `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_gate_test` | **PASS** — `2 passed`; `grows = 0` / `5` |
| `cargo test -p cb-train --no-default-features --features rocm --test device_fpp_composition_test` | **PASS** — `6 passed`; one-hot × CTR **refused outright**, verbatim message unchanged |
| `… --test device_ctr_fit_test` | **PASS** — **`4.483e-11` UNCHANGED**; `grows = 5` |
| `… --test device_ctr_buckets_fit_test` | **PASS** — **`2.776e-17` UNCHANGED**; `[0]×8`; `grows = 5` |
| `… --test device_ctr_counter_fit_test` | **PASS** — **`1.388e-17` UNCHANGED**; `grows = 5` |
| `… --test device_ctr_btmv_fit_test` | **PASS** — **`2.776e-17` UNCHANGED**; occupancy 15; `grows = 5` |
| `… --test device_ctr_combo_fit_test` | **PASS** — **`2.082e-17` UNCHANGED**; `grows = 5`; 8 splits / 3 combinations |
| `cargo test -p cb-train --lib device_ctr_combo_config_tests` (**V-0b / CRITICAL-1**) | **PASS** — `8 passed`; **no gate-state row moved** |
| `cargo test -p cb-train --lib boosting_ctr_gate_tests` | **PASS** — `13 passed`, unchanged from T19/T22 (T21's `boosting.rs` comment lines are stripped by `code_lines_mentioning`, and none sits inside `gate_body()`) |
| `bash ./run_device_tests.sh` | **PASS — 24 PASS / 0 FAIL** (23 binaries + isolated perf lane; Poisson **10.9×**, no R-13 flake) |
| `cargo test -p cb-backend --no-default-features --features rocm` | **PASS** — lib `277 passed; 0 failed; 2 ignored` (**identical to T18/T19/T22** — T21's `cb-backend` diff is empty) + `4` + `1` + `0` |
| `cargo test -p cb-train` (all targets, **default cpu**) | **PASS** — **111 targets**, every one `test result: ok`, zero `FAILED` (identical to T22 — T21 adds no binary) |
| `cargo test -p cb-train --test device_ctr_type_gate_test` (**default cpu**, the SKIP path) | **PASS** — `7 passed`, seven `SKIP …: needs rocm/cuda` lines, no panic, no `#[ignore]` |
| `cargo clippy -p cb-train --lib` | **PASS** — the only warning is the transitively-compiled pre-existing `duplicated_attributes` |
| `cargo clippy -p cb-train --no-default-features --features rocm --test device_ctr_type_gate_test` | **PASS** — same single pre-existing warning; **zero diagnostics name a T21-touched file** |
| `cargo clippy -p cb-train --no-default-features --features rocm --test device_ctr_gate_test --test device_fpp_composition_test` | **pre-existing warning only** — `type_complexity` on `device_ctr_gate_test.rs:140` `load_inputs`, verified pre-existing at `HEAD` (same line, same signature); T21's diff there is doc comments only |
| `git diff --stat run_device_tests.sh` | **empty** — C-8 honoured |
| `grep -rn "MUTATION-T21" crates/ --include=*.rs` | **empty** |
| `git diff crates/cb-train/src/boosting.rs`, filtered to non-`//` lines | **empty** — the production diff is comment-only |

**D-04 held exactly** (post-change run, byte-identical to the pre-first-edit baseline):

```
[device-ctr-e2e]         1 CTR splits; max |Δpred| = 4.483e-11 (bar 1e-5);  device grows = 5
[device-ctr-buckets-e2e] 8 Buckets CTR splits; chosen target_border_idx = [0]×8 (distinct [0])
[device-ctr-buckets-e2e] max |Δpred| = 2.776e-17 (bar 1e-5);                device grows = 5
[device-ctr-counter-e2e] 5 Counter CTR splits
[device-ctr-counter-e2e] max |Δpred| = 1.388e-17 (bar 1e-5);                device grows = 5
[device-ctr-btmv-e2e]    5 BTMV CTR splits; max bucket occupancy = 15
[device-ctr-btmv-e2e]    max |Δpred| = 2.776e-17 (bar 1e-5);                device grows = 5
[device-ctr-combo-e2e]   8 CTR splits (3 of them ≥2-member combinations); max |Δpred| = 2.082e-17; device grows = 5
```

### Pre-existing, NOT chased (T24's list) — nothing new discovered

* `clippy::erasing_op` at `kernels/score_split.rs:374` (T08) and `clippy::duplicated_attributes`
  in `gpu_runtime/mod.rs` (T10, at `:4542`/`:4574` per T22) — unmoved; T21 touches no
  `cb-backend` file.
* The 12 `cb-train` integration-test clippy targets (T04) — untouched. `type_complexity` on
  `device_ctr_gate_test.rs:140` is a **warning**, not one of those errors, and is
  pre-existing at `HEAD`.
* Default-cpu `cb-backend --lib` **59–60 range** (T17 §7) — untouched.
* `T22-OBS-1` and `T22-OBS-2` — read, out of T21's scope, **not chased** (they are unowned;
  see T22 §5).

---

## 5. What this task does NOT claim

* **It does not close R-20.** T21 ran no D-2 probe; T22's §4.1 remains the last word (still
  OPEN).
* **`grown == 0` is not attributed to a named layer by any T21 pin.** §3.2 and §3.4 measure
  exactly why that would be unsound: two of the five surviving exclusions are reached by
  more than one guard, and in both cases the guards are mutually redundant.
* **The `borders-7` and cat-only fits are self-oracles, not upstream oracles.** They assert
  routing, never predictions; no fixture was generated, regenerated or read for prediction
  comparison (§2.6 honoured — `crates/cb-oracle/fixtures/` byte-untouched).

---

## 6. Deviations from the task text

1. **A stale-comment correction in the block T21 edits** (`boosting.rs`, the GDC-11 comment
   above the CTR disjunction). Not in the task's file list, which asks only for the
   `one_hot_bins.is_empty()` rationale. It claimed *"combination projections, non-Borders
   types … still falls back"* and described the admitted set as *"simple Borders
   projections"* — both falsified by T10/T12/T16/T19, both immediately above the conjunct
   whose retention argument T21 is writing, and leaving them would have left the file
   asserting in prose the opposite of what the gate does. Comment-only.
2. **Doc-comment edits in two files the task does not list** —
   `device_fpp_composition_test.rs` and `device_ctr_gate_test.rs`. Forced by the task's own
   mandatory P2/P3 inversion annotations (checker MINOR-13), which name
   `one_hot_x_ctr_still_declines` and `multi_permutation_ctr_still_declines`; both live in
   those files, not in T21's. Doc comments only (`24 0` and `14 0`, verified by diff filter).
3. **A third shipped test, `unmodified_float_half_commits_to_device`.** The task specifies
   two pins. §3.4 measured that no single-clause mutation can redden the cat-only pin, and
   the executor rule then requires strengthening rather than accepting the green; the
   control is what makes the pin discriminating, and it must ship with the pins or the
   evidence lives only in this note. It duplicates T13's `skiptest+noeval` recipe
   deliberately — what it proves is that **T21's helper** can report `Device`, which is the
   thing the two pins depend on.
4. **`multi_permutation_ctr_still_declines` was re-run, not authored** (as the task
   instructs) — under its shipped name `multi_permutation_ctr_declines_to_device` in
   `device_ctr_gate_test.rs`. A **second** copy also already exists
   (`device_fpp_composition_test::multi_permutation_x_ctr_still_declines`); both were
   re-run, both annotated, neither duplicated.
5. **Seven mutation runs, not four** — MUT-1a/1b (the mandated joint one, split into two so
   the retention argument is measured), MUT-2a/2b/2c (the claimed clause, the composite, and
   T18 §3's complement), MUT-3, MUT-4a/4b/4c. Two of them (MUT-2a, MUT-4a) PASSED and were
   escalated rather than accepted.
6. **One test-side probe** during MUT-1 (a `println!` of `(ok, grown, err)` before the
   existing `expect_err`), removed before the revert was verified. Without it MUT-1a's
   `grown = 0` — the whole evidence for DCTR-03's retention — is invisible, because the
   existing test panics at `expect_err` first.
7. **Report-before-assert reordering** inside T21's own new helper (§2). Not requested;
   forced by MUT-3's first run hiding `grown = 5`.
8. **`run_device_tests.sh` untouched** (C-8 — T24 owns it) and **no new test binary**. T21
   adds three tests to `device_ctr_type_gate_test`, which T13 already put on T24's
   registration list. `CountingGpu`'s duplication count is **unchanged at EIGHT**.

---

## 7. Rollback

Delete the three added `#[test]`s and `device::assert_float_half_route` /
`device::float_half()` / `TRUNCATED_BORDER_COUNT` from
`crates/cb-train/tests/device_ctr_type_gate_test.rs`, and revert the four comment blocks
(`boosting.rs`, the two annotated test files, and the module doc). **The
`one_hot_bins.is_empty()` conjunct must NOT be deleted as part of any rollback** — DCTR-03
retains it, and T21's only production change was the comment explaining why. Nothing else in
the tree depends on T21.

---

## 8. For T23 and T24

* **T23 is unaffected.** T21 changes no gate expression, moves no gate-state row, and adds
  no source-scan pin: `device_ctr_combo_config_tests` `8 passed` with every row unmoved and
  `boosting_ctr_gate_tests` `13 passed`, both identical to T19/T22. T23's count of
  **eleven** source-scan pins to retire or update is unchanged. One thing to preserve: T21's
  new `boosting.rs` comments are all `//`-prefixed, which is what keeps
  `code_lines_mentioning` from counting them — a T23 rewrite that turns any of them into
  code, or moves them inside `ctr_types_are_device_covered`'s body, will move those counts.
* **T24: no new binary.** `device_ctr_type_gate_test` is already on the registration list
  (T13's). The list stays at FIVE: `device_ctr_buckets_fit_test` (T10),
  `device_ctr_counter_fit_test` (T12), `device_ctr_type_gate_test` (T13),
  `device_ctr_btmv_fit_test` (T16), `device_ctr_combo_types_diff_test` (T22). `CountingGpu`
  duplication is **EIGHT**.
* **T24 (DoD, acceptance scenario 9): all five negatives now have a passing test.**
  one-hot × CTR, multi-permutation, eval-set, cat-only and `border_count != 15` — the
  table in §1 maps each to its test and to the phase that inverts it.
* **T24 should record the overdetermination finding** (§3.2, §3.4) as a phase-level fact,
  not a T21 quirk: three of this phase's five negatives are reached by more than one guard,
  so "the mutation for the clause you CLAIM" (T13 §1) is sometimes impossible, and the
  honest substitute is a measured complement plus a control arm.
