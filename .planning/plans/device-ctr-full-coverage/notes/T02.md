# T02 — Widen the device CTR seam with per-column CTR identity — COMPLETION NOTE

- **Spec**: DCTR-01
- **Wave**: 0
- **Status**: **DONE**
- **Work location**: `/home/user/Documents/workspace/catboost_rs` (branch `feat/device-ctr-full-coverage`;
  `planning/settings.json` has `"implementation.use_worktree": false`, and the coordinator
  directed work in the main checkout — no worktree created).
- **Commits**: none. No `git add` / `git commit` / any git write was executed (coordinator
  commits per wave). The only `git` calls made were the read-only `git status`,
  `git diff --stat`, `git diff --name-only`, `git branch --show-current` and one
  `git show HEAD:<file>` used as a read (see the pre-existing-failure proof below, which
  restored the file byte-for-byte from a scratchpad copy).

---

## 1. What changed (file:line)

Three files, all additive. `git diff --stat` for this task's share:

```
crates/cb-compute/src/runtime.rs         | 36 +++          (36 insertions, 0 deletions)
crates/cb-train/src/boosting.rs          | 43 +++          (43 insertions, 0 deletions)
crates/cb-train/src/device_ctr_combo_config_test.rs  | +91 (pure append, nothing deleted)
```

(The full working-tree `git diff --stat` also lists `crates/cb-train/src/ctr/calc_ctr_test.rs`
and the earlier part of `device_ctr_combo_config_test.rs` — those are **T03** and **T00**,
Wave-0 siblings, untouched by this task.)

### 1.1 `crates/cb-compute/src/runtime.rs` — the seam (`struct DeviceCtrColumn`, `:1105`)

Three plain host fields added per `SPEC.md` §4.1, after `weight_group`. No `cubecl` import
(D-03 / T-10-04); `#[derive(Debug, Clone, Default, PartialEq)]` at `:1104` still holds
(`i8`, `u32`, `Vec<u32>` all satisfy them — verified by compile).

| line | field | doc-comment anchor |
|---|---|---|
| `:1134` | `pub ctr_type: i8` | `cb_train::ctr::ECtrType` discriminants transcribed by value (`0 Borders, 1 Buckets, 2 BinarizedTargetMeanValue, 3 FloatTargetMeanValue, 4 Counter, 5 FeatureFreq`) + `restrictions.h:20-32`, with the note that `cb-compute` may not depend on `cb-train` (T-10-04) |
| `:1140` | `pub target_border_idx: u32` | `GetTargetBorderCount` (`ctr_helper.h:35-42`); only Buckets ever yields `> 0`; states why the host conversion is checked rather than `unwrap_or(0)` |
| `:1163` | `pub projection_members: Vec<u32>` | `AddTreeCtrs`' `seenProj` subset rule (`greedy_tensor_search.cpp:491-551`) + the `baseProj.IsEmpty()` skip, and the forward reference to T17 |

**Mandatory identifier-space warning (C-12b / checker MINOR-12b)** — present as a dedicated
`# ⚠ IDENTIFIER-SPACE WARNING — not index-compatible with member_bins` doc section on
`projection_members` (`runtime.rs:1152-1162`). It states verbatim that these are **absolute**
cat-feature indices while the sibling `member_bins` is in **CTR-eligible position** space
(resolved through `eligible_absolute.iter().position(..)`, `boosting.rs:2487-2500`), that
`projection_members[k]` must never index `member_bins` nor be compared against a bin index,
and that this is safe **only** because the eligibility predicate compares `projection_members`
against other `projection_members` and never against bin space.

### 1.2 `crates/cb-train/src/boosting.rs` — `build_device_ctr_config`'s `build_columns` closure

| line | change |
|---|---|
| `:2519-2530` | comment: why the seam now carries CTR identity, and why the `usize -> u32` conversion is **checked** |
| `:2524-2529` | `let target_border_idx = u32::try_from(col.target_border_idx).map_err(\|_\| CbError::OutOfRange(..))?;` — **C-4** honoured: no `unwrap`, no silent `unwrap_or(0)` |
| `:2531-2538` | comment: `projection_members` is **NOT re-sorted** (`TProjection` keeps `cat_features` sorted+deduped by construction, `projection.rs:121-126` `from_features` / `:111-115` `single`), plus a pointer to the identifier-space warning |
| `:2539-2553` | `let projection_members = col.projection.cat_features().iter().map(\|&m\| u32::try_from(m).map_err(\|_\| CbError::OutOfRange(..))).collect::<CbResult<Vec<u32>>>()?;` — **C-4** applied to each member too |
| `:2554-2558` | **guardrail**: `if projection_members.is_empty() { return Err(CbError::Degenerate("device CTR column with an empty projection member list")); }` — the mirror of the existing empty-projection rejection at `:2477-2481`, so the production builder can never emit the `vec![]` that would make T17's subset test treat a combination as simple |
| `:2559-2568` | the literal now populates `ctr_type: col.ctr_type, target_border_idx, projection_members` |

Untouched, as required: `ctr_covered`, `build_ctr_cindex_columns`, `CtrSearchState`,
`ResidentCtrSearch`, `ctr_types_are_device_covered`. Nothing reads the new fields yet.

### 1.3 `crates/cb-train/src/device_ctr_combo_config_test.rs:355-446` — the Red test

New test function `device_ctr_column_carries_type_border_and_sorted_members`, **appended** at
the end of the file. T00's module doc, `gate_state_table()`,
`gate_admits_exactly_the_current_wave` and `an_all_simple_borders_set_stays_covered` were not
touched (the edit was a pure append onto the tail of
`averaging_columns_get_the_same_member_treatment`).

It materializes two source columns through the file's existing `covered_column` / `with_type` /
`with_target_border` / `build` helpers:
1. a **two-member combination** `TProjection::from_features(&[0, 2])`, Borders / `b = 0`;
2. a **simple Buckets** column `TProjection::from_features(&[1])` with `target_border_idx = 1`
   (the only type that ever carries `> 0`).

and asserts, for **both** the structure half and the averaging half of the emitted config:
`ctr_type == source.ctr_type`; `target_border_idx == source.target_border_idx as u32`;
`projection_members == source.projection.cat_features()` cast to `u32`;
`!projection_members.is_empty()` (the guardrail's `len() >= 1`); and
`projection_members.windows(2).all(|w| w[0] < w[1])` (strictly increasing ⇒ sorted + deduped).
A closing assertion pins that the combination row really has **2** members, so the member
assertion cannot be satisfied vacuously by a single-member column.

**No §2.5 mutation check is required for this task.** T02 is not in §2.5's nine mutation
candidates, and its test was **not** green-on-write — it failed to compile before the
production change and passed after it (§2 below).

---

## 2. Red → Green → Refactor

### 2.1 Red — VERBATIM observed compile error

`cargo test -p cb-train --lib device_ctr_combo_config_tests`, with only the test written:

```
   Compiling cb-train v0.1.0 (/home/user/Documents/workspace/catboost_rs/crates/cb-train)
error[E0609]: no field `ctr_type` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:390:21
    |
390 |                 col.ctr_type, source.ctr_type,
    |                     ^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `ctr_type` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:393:21
    |
393 |                 col.ctr_type, source.ctr_type
    |                     ^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `target_border_idx` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:398:21
    |
398 |                 col.target_border_idx, want_border,
    |                     ^^^^^^^^^^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `target_border_idx` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:401:21
    |
401 |                 col.target_border_idx
    |                     ^^^^^^^^^^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `projection_members` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:411:21
    |
411 |                 col.projection_members, want_members,
    |                     ^^^^^^^^^^^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `projection_members` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:414:21
    |
414 |                 col.projection_members
    |                     ^^^^^^^^^^^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `projection_members` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:420:22
    |
420 |                 !col.projection_members.is_empty(),
    |                      ^^^^^^^^^^^^^^^^^^ unknown field
    |
    = note: available fields are: `member_bins`, `prior`, `borders`, `bucket_count`, `weight_group`

error[E0609]: no field `projection_members` on type `&DeviceCtrColumn`
   --> crates/cb-train/src/device_ctr_combo_config_test.rs:427:21
    | ...
```

Exactly the plan's predicted Red (`E0609: no field 'ctr_type' on type 'DeviceCtrColumn'`, and
the same for the other two fields). The compiler's own
`available fields are: member_bins, prior, borders, bucket_count, weight_group` line is
independent confirmation the seam had none of the three before the change.

### 2.2 Green

```
running 7 tests
test boosting::device_ctr_combo_config_tests::an_all_simple_borders_set_stays_covered ... ok
test boosting::device_ctr_combo_config_tests::gate_admits_exactly_the_current_wave ... ok
test boosting::device_ctr_combo_config_tests::averaging_columns_get_the_same_member_treatment ... ok
test boosting::device_ctr_combo_config_tests::combination_projection_carries_every_member ... ok
test boosting::device_ctr_combo_config_tests::simple_projection_is_byte_unchanged ... ok
test boosting::device_ctr_combo_config_tests::an_unknown_projection_member_is_a_typed_error ... ok
test boosting::device_ctr_combo_config_tests::device_ctr_column_carries_type_border_and_sorted_members ... ok

test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 379 filtered out; finished in 0.00s
```

T00's `gate_admits_exactly_the_current_wave` and `an_all_simple_borders_set_stays_covered` are
green and unchanged — T02 did not move the gate.

### 2.3 Refactor

Doc comments written as specified (§1.1), including the mandatory identifier-space warning and
the "do not re-sort, and here is why" comment in `boosting.rs`. No behavioural change was made
during refactor; the focused test was re-run green afterwards.

---

## 3. Byte-identity evidence (DCTR-01's defining invariant)

Nothing reads the new fields, so the device output must be numerically unchanged.

| | command | printed line |
|---|---|---|
| **BEFORE** (clean tree, pre-edit) | `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_fit_test -- --nocapture` | `[device-ctr-e2e] 1 CTR splits; max \|Δpred\| = 4.483e-11 (bar 1e-5)` |
| **AFTER** (post-Green) | same | `[device-ctr-e2e] 1 CTR splits; max \|Δpred\| = 4.483e-11 (bar 1e-5)` |
| **AFTER (final tree state)** | same | `[device-ctr-e2e] 1 CTR splits; max \|Δpred\| = 4.483e-11 (bar 1e-5)` |

**`4.483e-11` before == `4.483e-11` after. The byte-identity invariant HOLDS.**

The AFTER run is a genuine rebuild, not a stale binary:

```
target/debug/deps/device_ctr_fit_test-c8ff9e40330ba2ea  2026-08-08 16:06:03  (test binary)
crates/cb-compute/src/runtime.rs                        2026-08-08 16:04:15  (edit)
crates/cb-train/src/boosting.rs                         2026-08-08 16:04:29  (edit)
```

`device_ctr_gate_test` is likewise unchanged: `2 passed; 0 failed` before and after
(`single_permutation_ctr_commits_to_device`, `multi_permutation_ctr_declines_to_device`).

---

## 4. Validation commands — all PASS

| # | command | result |
|---|---|---|
| 1 | `cargo check -p cb-compute` | **PASS** — `Finished dev profile in 11.86s` |
| 2 | `cargo check -p cb-backend --no-default-features --features rocm` | **PASS** — `Finished dev profile in 2.84s` |
| 3 | `cargo test -p cb-train --lib device_ctr_combo_config_tests` | **PASS** — `7 passed; 0 failed; 379 filtered out` |
| 4 | `cargo test --workspace` (and `--no-fail-fast`) | **PASS modulo a proven pre-existing failure** — see §4.1 |
| 5 | `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_fit_test` | **PASS** — `1 passed; 0 failed`, `max \|Δpred\| = 4.483e-11` |
| 6 | `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_gate_test` | **PASS** — `2 passed; 0 failed` |
| 7 | `bash ./run_device_tests.sh` | **PASS — 23/23 + perf lane** — see §4.2 |
| 8 (extra) | `cargo clippy -p cb-compute -p cb-train --all-targets` | **PASS for this task** — see §4.3 |

Every rocm invocation used `--no-default-features --features rocm` **and** an explicit
`--test <name>` (R-9 and R-10 both honoured; no package-wide rocm run was issued).

### 4.1 `cargo test --workspace` — one failing target, proven pre-existing

`cargo test --workspace --no-fail-fast` ends with **`error: 1 target failed:`**. That one target
is `cb-backend --lib`:

```
Running unittests src/lib.rs (target/debug/deps/cb_backend-1bfb68631f781ce2)
test result: FAILED. 211 passed; 60 failed; 2 ignored; 0 measured; 0 filtered out; finished in 341.07s
```

Every other target in the workspace passed, including all `cb-train` integration tests, all
`cb-oracle` oracles, and every doc-test.

The 60 failures are the known CubeCL **CPU-runtime** limitation (`cargo test --workspace` uses
default features ⇒ `cpu` backend), e.g.:

```
thread 'DSD-0-0' panicked at cubecl-cpu-0.10.0/src/compiler/visitor/operation/mod.rs:156:17:
plane_inclusive_sum(local(0).v2) is not supported on CPU.
```

**Proof of pre-existence (executed, not assumed).** `cb-backend` does not depend on `cb-train`,
so this task's only change visible to it is `cb-compute/src/runtime.rs` (36 insertions, **0
deletions** — purely additive fields). I saved my version to the scratchpad, restored the file
from `git show HEAD:crates/cb-compute/src/runtime.rs`, and re-ran the failing family:

```
$ cargo test -p cb-backend --lib kernels::sort::        # with cb-compute at pristine HEAD
test result: FAILED. 0 passed; 5 failed; 0 ignored; 0 measured; 268 filtered out
```

Identical failures with the change fully reverted ⇒ **pre-existing, not introduced by T02**.
The file was then restored byte-for-byte from the scratchpad copy (`git diff --stat` returned to
`crates/cb-compute/src/runtime.rs | 36 +++`) and `cargo test -p cb-train --lib
device_ctr_combo_config_tests` re-run green (`7 passed; 0 failed`). No workspace-wide or
suite-wide command was run while the revert was live (§2.5 isolation rule).

Corroborating: none of the 60 failing tests constructs a `DeviceCtrColumn`, and
`gpu_runtime::session_depth_gt1_test` — the module that *does* — is **not** among them.

### 4.2 `bash ./run_device_tests.sh` — 23/23 PASS + perf lane PASS

```
PASS  device_bias_gate_test              ok. 3 passed
PASS  device_exact_leaf_gate_test        ok. 4 passed
PASS  device_nonsym_bootstrap_gate_test  ok. 3 passed
PASS  device_bias_fit_test               ok. 1 passed
PASS  device_exact_leaf_fit_test         ok. 1 passed
PASS  device_ctr_combo_fit_test          ok. 0 passed; 1 ignored     (still #[ignore]d — T19 owns it)
PASS  device_nonsym_bootstrap_test       ok. 1 passed
PASS  device_ordered_fit_test            ok. 1 passed
PASS  device_fpp_composition_test        ok. 6 passed
PASS  device_nonsym_fit_test             ok. 2 passed
PASS  device_region_fit_test             ok. 1 passed
PASS  device_weighted_fit_test           ok. 1 passed
PASS  device_weighted_nonsym_fit_test    ok. 3 passed
PASS  device_ctr_fit_test                ok. 1 passed
PASS  device_ctr_gate_test               ok. 2 passed
PASS  device_weight_gate_test            ok. 4 passed
PASS  device_fold_count_gate_test        ok. 2 passed
PASS  device_gate_composition_test       ok. 3 passed
PASS  bootstrap_dev_oracle_test          ok. 1 passed
PASS  device_bootstrap_parity_test       ok. 4 passed
PASS  device_poisson_bootstrap_test      ok. 6 passed
PASS  device_one_hot_parity_test         ok. 3 passed
PASS  device_seam_test                   ok. 6 passed
PASS  [perf] kernels::poisson_bootstrap_speed_test   ok. 1 passed
        [poisson speed] n=2000000: parallel Poisson 24.0ms vs serial stream draw 252.7ms -> 10.5x
```

**23/23 PASS, 0 FAIL**, matching the `a0a67ec` baseline. The R-13 perf flake did not fire
(10.5× vs the 5× bar, in its isolated lane).

### 4.3 Lints

`cargo clippy -p cb-compute -p cb-train --all-targets`: **zero diagnostics point at any of the
three files this task changed** (verified by extracting every `--> ` location and filtering for
`runtime.rs`, `boosting.rs`, `device_ctr_combo_config_test.rs` — the filter returns nothing).

Pre-existing and untouched: `cb-compute` fails clippy on `crates/cb-compute/src/runtime_test.rs:60-62`
(`clippy::erasing_op` on `0 * k + 0`), a file not in this task's diff (`git diff --name-only`
confirms). `cb-backend` emits its known `duplicated_attributes` warning. Neither is T02's.

---

## 5. Deviations

### D-1 (MATERIAL) — C-12's table mis-classifies 4 of the 8 literal forms; only **1** literal edit was needed, not 5

The **site set** is exactly C-12's — `grep -rn "DeviceCtrColumn {" crates/ --include=*.rs`
returned the same **9 lines** (1 definition + 8 literals) at the same line numbers, so I did
**not** hit the "a site not in that table ⇒ STOP" condition and did not need to re-measure the
blast radius. What is wrong is C-12's **form** column: it marks
`session_depth_gt1_test.rs:501`, `:553`, `:630` and `runtime_ctr_averaging_test.rs:14` as
"full literal ⇒ **yes**, needs edit", but all four already end in `..DeviceCtrColumn::default()`.

Verified per site:

| site | C-12 says | **actual form on disk** | edit needed? |
|---|---|---|---|
| `cb-compute/src/runtime.rs:1105` | definition | definition | yes — the 3 fields |
| `session_depth_gt1_test.rs:501` | full literal → yes | `..DeviceCtrColumn::default()` | **no** |
| `session_depth_gt1_test.rs:553` | full literal → yes | `..DeviceCtrColumn::default()` | **no** |
| `session_depth_gt1_test.rs:629` | `..default()` → no | `..DeviceCtrColumn::default()` | no |
| `session_depth_gt1_test.rs:630` | full literal → yes | `..DeviceCtrColumn::default()` | **no** |
| `session_depth_gt1_test.rs:698` | `..default()` → no | `..DeviceCtrColumn::default()` | no |
| `session_depth_gt1_test.rs:779` | `..default()` → no | `..DeviceCtrColumn::default()` | no |
| `runtime_ctr_averaging_test.rs:14` | full literal → yes | `..DeviceCtrColumn::default()` | **no** |
| `cb-train/src/boosting.rs:2519` | full literal → yes | **full literal** (production) | **yes** |

⇒ **All 7 test-side literals use functional-update syntax and compile unchanged. Exactly one
literal — the production one — needed editing.** Both `cargo check -p cb-compute` and
`cargo check -p cb-backend --no-default-features --features rocm` pass with the two test files
untouched, which is the executable confirmation.

**Action taken: no edit to `session_depth_gt1_test.rs` or `runtime_ctr_averaging_test.rs`.**
The plan's Green step said to "mechanically extend the 5 struct literals **that need edits**";
zero test-side literals need edits, and editing them anyway would be scope expansion with no
observable effect (nothing reads the fields). This makes the diff strictly smaller than
planned; the task's "`git diff --stat` limited to …" evidence criterion is satisfied as a
proper subset.

### D-2 (FORWARD-LOOKING, for T17/T19) — the `projection_members: vec![]` re-review set is **8 sites, not 3**

C-12 flagged that the three `..Default::default()` sites would silently receive
`projection_members: vec![]` and "must be re-reviewed once T19 lands". Because **all eight**
literals are `..Default::default()`, that re-review set is now **all eight**. The one that
matters most:

> `crates/cb-backend/src/gpu_runtime/session_depth_gt1_test.rs:630-635` declares a genuine
> **2-member combination** column (`member_bins: vec![cat0, cat1]`) whose `projection_members`
> is `vec![]`. Once T17's per-level eligibility gate reads that field, this fixture will look
> like a **simple** column to the gate.

T17/T19 must give that literal (and any sibling whose `member_bins.len() >= 2`) a real member
list. This is not a defect today — nothing reads the field — and the plan's chosen guardrail
against the `vec![]` hazard on the **production** side is implemented as specified
(`boosting.rs:2554-2558`, the typed `CbError::Degenerate`), so `build_device_ctr_config` can
never emit an empty member list. Only hand-written cb-backend unit-test literals can.

### D-3 (MINOR) — the Red test also covers the **averaging** half

The plan's Red spec asks for assertions on "each emitted `DeviceCtrColumn`". The test iterates
both `cfg.columns` and `cfg.averaging.columns`, because `build_columns` is called twice and a
population applied to only one call would leave the leaf-value gather blind to the identity —
the same failure mode `averaging_columns_get_the_same_member_treatment` already guards for
`member_bins`. Strictly additive coverage; no assertion was weakened or dropped.

### D-4 (NONE) — no deviation on the constrained items

- **C-4** applied to both conversions (`target_border_idx` and every projection member) as
  checked `u32::try_from(..).map_err(|_| CbError::OutOfRange(..))?`. No `unwrap`, no `expect`,
  no `unwrap_or(0)`.
- `projection_members` is **not** re-sorted, with the comment explaining why.
- Source/test separation preserved (the test is the existing `#[path]`-mounted sibling module
  at `boosting.rs:7407-7408`; no `#[cfg(test)] mod tests` was added to any production file).
- `cb-compute` remains `cubecl`-free; `cb-backend` still does not depend on `cb-train`; no
  `unwrap`/`expect`/`panic`/raw indexing was added anywhere under `crates/cb-backend/src/**`
  (that crate was not modified at all).
- `ctr_covered`, `build_ctr_cindex_columns`, `CtrSearchState`, `ResidentCtrSearch` and
  `ctr_types_are_device_covered` are untouched.
- No git write command was executed.
