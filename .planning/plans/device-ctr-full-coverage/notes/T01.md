# T01 — Delete the provably-dead `prior_denom == 1.0` conjunct — DONE

- **Specs**: DCTR-02
- **Wave**: 1. **Depends on** T00 (done). **Blocks** T10 and T20.
- **Branch / tree**: `feat/device-ctr-full-coverage` @ `c8ecd0e` (main checkout;
  `planning/settings.json → implementation.use_worktree: false`, so no worktree —
  PLAN.md's `gpu-borders-shared-sample` frontmatter is stale, per COORDINATOR-FINDINGS).
- **Not committed**: per the coordinator's instruction, no `git add`/`git commit` was run.

---

## 1. What changed (file : symbol)

| file : symbol | change |
|---|---|
| `crates/cb-train/src/boosting.rs : ctr_types_are_device_covered` | **deleted the single conjunct `&& col.prior_denom == 1.0`**. The arity, `ctr_type` and `target_border_idx` conjuncts are **untouched** (T10/T12/T16/T19 own those). |
| `crates/cb-train/src/boosting.rs` — doc block above `ctr_types_are_device_covered` | the "Everything else … non-unit prior denominators — still declines" sentence no longer claims a prior-denominator decline; **new section "There is NO prior-denominator conjunct, deliberately (DCTR-02, T01)"** carries the `ctr_helper.cpp:50` citation verbatim (`CB_ENSURE(denom == 1.0f, "Error: CPU could use only 1 as denom for ctrs currently")`, v1.2.10), points at `CTR_PRIOR_DENOM` / the single call site / `ctr_splits_for_tree`'s hard-coded `1.0`, and states the restore-the-conjunct rule if the constant ever becomes fit-dependent. |
| `crates/cb-train/src/boosting.rs` (end of file) | **new mount** `#[cfg(test)] #[path = "boosting_ctr_gate_test.rs"] mod boosting_ctr_gate_tests;` next to the existing `device_ctr_combo_config_test.rs` mount. |
| `crates/cb-train/src/boosting_ctr_gate_test.rs` | **NEW FILE** (sibling `#[path]` module — source/test separation, no `mod tests` in production). Helpers `production_source()` / `code_lines_mentioning()` / `gate_body()`, tests `ctr_prior_denom_is_structurally_unit` and `the_device_gate_no_longer_reads_prior_denom`. Module doc states the **extension contract** for T10/T12/T16/T19/T22 (see §5). |
| `crates/cb-train/src/device_ctr_combo_config_test.rs : gate_state_table` | **row 6 only** (`simple/Borders/b=0/denom=2.0`): `expected: false → true`, `flips_at` note rewritten to `flipped by T01 …`; the ASCII table in the doc block above updated on the row-6 line. **No other row, and no other function, touched.** |

Production behaviour change: **none reachable**. `prior_denom` is `1.0` at the only
production materialization site, so no fit's routing can move — that is exactly what
`ctr_prior_denom_is_structurally_unit` pins, and the unchanged `4.483e-11` device e2e
number (§4) is the empirical confirmation.

## 2. Red — part 1 (row-6 flip), VERBATIM

`cargo test -p cb-train --lib device_ctr_combo_config_tests`, row 6 flipped, gate not yet
edited:

```
thread 'boosting::device_ctr_combo_config_tests::gate_admits_exactly_the_current_wave' (664058) panicked at crates/cb-train/src/device_ctr_combo_config_test.rs:287:9:
assertion `left == right` failed: row 6 (simple/Borders/b=0/denom=2.0): expected true, got false
   flips_at: flipped by T01 — deleted `col.prior_denom == 1.0` (DCTR-02). The deletion is a proven no-op: the only production materialization site (`boosting.rs:2237`) passes `CTR_PRIOR_DENOM = 1.0`, pinned by `boosting_ctr_gate_tests::ctr_prior_denom_is_structurally_unit`, so this row is unreachable from any real fit
   If you deleted that conjunct, update THIS row in the SAME commit. If you did not, the device gate just moved by accident — which is exactly what the conscious-act contract in this module's doc forbids.
  left: false
 right: true

test result: FAILED. 6 passed; 1 failed; 0 ignored; 0 measured; 379 filtered out
```

**Exactly T00's recorded mutation output with the sign reversed**, as the task predicted:
`row 6 (simple/Borders/b=0/denom=2.0): expected true, got false`. The flip and the
conjunct deletion are in the **same change**, per T00's contract.

## 3. Red — part 2 (the new gate-home test file), VERBATIM

**3a. Before the file existed** — `cargo test -p cb-train --lib boosting_ctr_gate_tests`:

```
running 0 tests
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 386 filtered out
```

**3b. After writing the file, gate not yet edited** — the genuine Red, from
`the_device_gate_no_longer_reads_prior_denom`:

```
thread 'boosting::boosting_ctr_gate_tests::the_device_gate_no_longer_reads_prior_denom' (667236) panicked at crates/cb-train/src/boosting_ctr_gate_test.rs:140:5:
`ctr_types_are_device_covered` still reads `prior_denom`; DCTR-02 deletes that conjunct because `CTR_PRIOR_DENOM` makes it unreachable. Body was:
fn ctr_types_are_device_covered(cols: &[crate::ctr::CtrFeatureColumn]) -> bool {
    !cols.is_empty()
        && cols.iter().all(|col| {
            // ESCALATED (FPP-11): the projection-arity conjunct is RESTORED. See the
            // "combination CTR is device-INELIGIBLE" section of this function's doc
            // comment for the measured evidence and the localisation.
            col.projection.is_simple()
                && col.ctr_type == crate::ctr::ECtrType::Borders.as_i8()
                && col.target_border_idx == 0
                && col.prior_denom == 1.0
        })

test result: FAILED. 1 passed; 1 failed; 0 ignored; 0 measured; 387 filtered out
```

**3c. Test-side bug found by its own first Red (recorded for honesty).** The first draft of
`ctr_prior_denom_is_structurally_unit` asserted the two `CTR_PRIOR_DENOM` code lines in
source order, definition first:

```
thread 'boosting::boosting_ctr_gate_tests::ctr_prior_denom_is_structurally_unit' (665787) panicked at crates/cb-train/src/boosting_ctr_gate_test.rs:117:5:
assertion `left == right` failed: the denominator must stay a hard-coded constant, never a plumbed parameter
  left: Some("CTR_PRIOR_DENOM,")
 right: Some("const CTR_PRIOR_DENOM: f64 = 1.0;")
```

The **use** (`:2237`) precedes the **definition** (`:2257`) in `boosting.rs`. That is
incidental layout, not a contract, so the assertion was changed to order-independent
`iter().any(..)` membership checks with a comment saying why. No production code was
involved in this failure.

## 4. §2.5 mutation check — MANDATORY, VERBATIM

`ctr_prior_denom_is_structurally_unit` is **green on write**, so its discriminating power
was proved by mutating production. Protocol as required: mutate → run **only** the single
focused test → record → revert → re-run green. **No workspace-wide or device-suite command
was issued while the mutation was live** (the window was one `cargo test … --lib
boosting_ctr_gate_tests::ctr_prior_denom_is_structurally_unit` invocation, ≈20 s).

**Mutation**: `boosting.rs:2257`, `const CTR_PRIOR_DENOM: f64 = 1.0;` → `= 2.0;`

```
thread 'boosting::boosting_ctr_gate_tests::ctr_prior_denom_is_structurally_unit' (670020) panicked at crates/cb-train/src/boosting_ctr_gate_test.rs:102:5:
assertion `left == right` failed: upstream ctr_helper.cpp:50 forbids denom != 1 on the CPU task type; if this constant ever becomes non-unit, the device gate's deleted `prior_denom == 1.0` conjunct must be RESTORED (see DCTR-02)
  left: 2.0
 right: 1.0

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 388 filtered out
```

**Reverted** — `grep -n "const CTR_PRIOR_DENOM" crates/cb-train/src/boosting.rs` →
`2257:const CTR_PRIOR_DENOM: f64 = 1.0;`; focused test re-run **green** (1 passed).
**No mutation is live.**

`the_device_gate_no_longer_reads_prior_denom` needed no mutation check — it was genuinely
red before the deletion (§3b).

## 5. Structuring for T10 / T12 / T16 / T19 / T22 (the file is their home too)

`boosting_ctr_gate_test.rs`'s module doc states the extension contract explicitly. Each
later gate task adds ONE section, in gate-conjunct order, with two tests:

1. `<attribute>_is_structurally_<invariant>` — the production constant / call-site pin that
   makes the deletion safe (green-on-write ⇒ §2.5 mutation mandatory); and
2. `the_device_gate_no_longer_reads_<attribute>` — built on the shared `gate_body()`
   helper, genuinely **red before** the deletion and green after.

Three reusable helpers are already in place and used: `production_source()`
(`include_str!("boosting.rs")`), `code_lines_mentioning(src, needle)` (strips `//`, `///`,
`//!` lines so a *prose* mention never counts as a use), and `gate_body()` (extracts the
gate from signature to the first column-0 `}`; panics with a rename hint if the signature
moves). Division of labour recorded in the doc: the **table** in
`device_ctr_combo_config_test.rs` says *what the gate admits*; **this file** says *why the
removal is safe*, which a routing table structurally cannot express.

`gate_body()` deliberately excludes the doc comment, so a conjunct name surviving only in
the prose above the function does not read as a surviving conjunct — relevant for T10/T12/
T16/T19, whose doc blocks will keep discussing the attributes they removed.

## 6. Validation commands — all PASS

| command | result |
|---|---|
| `cargo test -p cb-train --lib device_ctr_combo_config_tests` (baseline, pre-change) | **PASS** — 7 passed, 0 failed (7 not 6: T02 added `device_ctr_column_carries_type_border_and_sorted_members`) |
| `cargo test -p cb-train --lib device_ctr_combo_config_tests` (final) | **PASS** — 7 passed, 0 failed |
| `cargo test -p cb-train --lib boosting_ctr_gate_tests` (final) | **PASS** — 2 passed, 0 failed |
| `cargo test -p cb-train --lib` | **PASS** — **389 passed**, 0 failed, 0 ignored (CRITICAL-1 full-lib regression, green) |
| `cargo test -p cb-train --test ctr_feature_materialize_test` | **PASS** — 6 passed, 0 failed |
| `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_fit_test` | **PASS** — 1 passed; `[device-ctr-e2e] 1 CTR splits; max \|Δpred\| = 4.483e-11 (bar 1e-5)` |
| `cargo test -p cb-train --no-default-features --features rocm --test device_ctr_gate_test` | **PASS** — 2 passed (`single_permutation_ctr_commits_to_device`, `multi_permutation_ctr_declines_to_device`) |
| `cargo clippy -p cb-train --lib --profile test` | **PASS (no new lint)** — 16 warnings, the same count T00 recorded; **zero** in `boosting_ctr_gate_test.rs`, and the two `contains()` hints resolve to `estimated/estimated_features_test.rs:84,85` |
| `rustfmt --edition 2021 --check crates/cb-train/src/boosting_ctr_gate_test.rs` | **clean (exit 0)** after one fmt-driven fix inside my own `assert_eq!` |

**DCTR-01 byte identity held**: `4.483e-11` is *exactly* the number T02 recorded before and
after its change (COORDINATOR-FINDINGS). T01 did not move it, as a provable no-op must not.

R-9/R-10 traps respected: every rocm command carries `--no-default-features` **and** an
explicit `--test <name>`; no package-wide rocm run was issued.

Formatting note: `device_ctr_combo_config_test.rs` still shows rustfmt hunks at `:112`,
`:134`, `:144` (pre-existing, recorded by T00) and `:370`, `:383` (**T02's new test**, not
mine); `boosting.rs` has 46 pre-existing hunks, none in the regions T01 edited. The crate
is not rustfmt-clean at baseline, so `--check` is not a repo gate.

## 7. Deviations

1. **A second test was added beyond the task's stated one.**
   `the_device_gate_no_longer_reads_prior_denom` was not named in the task; the task
   specified only `ctr_prior_denom_is_structurally_unit` and accepted that it is
   green-on-write. It was added because it makes the task's own *Goal / observable
   completion* sentence — "`ctr_types_are_device_covered` no longer mentions `prior_denom`"
   — executable, and it gives Red part 2 a **genuine** failing state instead of relying
   solely on a mutation. Additive, no production impact, and it is the pattern the later
   gate tasks now inherit (§5). The mandated mutation check was still executed in full.
2. **`ctr_prior_denom_is_structurally_unit` proves "only one production call site" by a
   compile-time source scan** (`include_str!("boosting.rs")`). There is no value-level way
   to observe the *absence* of a second, differently-parameterised call. This is the only
   `include_str!`-on-source test in the crate (the sole precedent,
   `cb-compute/tests/depth6_reference_test.rs:37`, includes a JSON fixture). Flagged as a
   novel-for-this-repo technique, not a rule violation; the helpers are written fail-loud
   (an unfindable signature or an unbalanced body panics with a rename hint rather than
   silently passing).
3. **Test-side ordering bug**, fully recorded in §3c — the two `CTR_PRIOR_DENOM` code lines
   are use-then-definition, not definition-then-use. Fixed by making the assertion
   order-independent. No production code involved.
4. **Line numbers in the task file had shifted** as the coordinator warned, though not from
   T02: `ctr_types_are_device_covered` was still at `:2355` (T02's `+43` lines land later
   in the file, in `build_device_ctr_config`). Everything was located by symbol. The
   deleted conjunct was at `:2364` exactly as the task said; `CTR_PRIOR_DENOM` at `:2257`
   and its use at `:2237`, both as cited.
5. **Concurrency observation, not a deviation**: `git status` shows the parallel T04 agent's
   in-flight edits to `crates/cb-train/src/ctr/{ctr_feature.rs, mod.rs}` plus a new
   `ctr/ctr_feature_test.rs` — note that these are **not** the `ctr/calc_ctr.rs` /
   `calc_ctr_test.rs` pair the coordinator named. T01 touched none of them. They are why
   `cargo test -p cb-train --lib` reports 389 rather than the 386 + 2 = 388 T01 alone
   implies.
6. **No git write command was run** (no `add`, no `commit`), per the coordinator's
   instruction; the wave commit is the coordinator's.

## 8. Rollback

Re-add `&& col.prior_denom == 1.0` at the end of the `.all(..)` closure, revert row 6 of
`gate_state_table` to `expected: false` / `flips_at: "T01 — deletes …"`, and delete the new
file plus its mount. Only T22's final collapse assumes the conjunct is gone.
