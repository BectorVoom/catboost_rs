---
phase: 07.3-pointwise-histogram-family
plan: 02
type: execute
wave: 2
depends_on: ["07.3-01"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/pointwise_hist.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "A downstream consumer can fill the 2-channel pointwise histogram for 5-, 6-, and 7-bit non-binary features on rocm, device-resident, via the SAME non-binary kernel selecting the bit-count through the #[comptime] bits parameter — no runtime branch, pruned at JIT time (D-7.3-02)."
    - "Each of the 5/6/7-bit fills matches the ordered host-reference 2-channel histogram within the REPORTED divergence across the edge-case fixtures, exercised at the matching border count (D-7.3-04)."
    - "The frozen binSums layout and the der-handle-in -> binSums-handle-out seam from Plan A are reused UNCHANGED; only the comptime bits and the host dispatch entry point are extended (D-7.3-01)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "pointwise_hist2_nonbinary_kernel<F> generalized to comptime bits in {5,6,7,8} (histogram line size + used-prefix derive from bits at comptime)"
      contains: "bits"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_pointwise_hist2_handle bit-width dispatch extended to 5/6/7 (DISPATCH_ONE_BYTE shape) — same seam, same binSums layout"
      contains: "launch_pointwise_hist2_handle"
    - path: "crates/cb-backend/src/kernels/pointwise_hist.rs"
      provides: "nonbinary_bits test (5/6/7-bit fixtures across border counts) reusing the Plan A host reference + harness"
      contains: "nonbinary_bits"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle"
      to: "crates/cb-backend/src/kernels.rs::pointwise_hist2_nonbinary_kernel"
      via: "cube launch with comptime bits in {5,6,7}"
      pattern: "pointwise_hist2_nonbinary_kernel::launch"
---

<objective>
Extend the frozen Plan A non-binary slice to the remaining one-byte bit-widths — **5-, 6-, and 7-bit** — on the SAME `pointwise_hist2_nonbinary_kernel` by driving the histogram line size, shared-memory used-prefix, and unroll from the `#[comptime] bits` parameter (mirroring upstream's `ComputeHist2NonBinary<Bits>` template and the `DISPATCH_ONE_BYTE(...,5/6/7/8)` host dispatch). Self-oracle each bit-width on rocm.

Purpose: This is the second MVP slice — after completion a consumer can fill the histogram for 5/6/7-bit features they could not before, with NO new kernel family (D-7.3-02: 5/6/7/8 are comptime cases of ONE non-binary kernel; only half-byte and binary are separate families). The Plan A seam and binSums layout are reused unchanged.
Output: the generalized comptime-`bits` non-binary kernel (5/6/7/8), the extended host bit-width dispatch, the `nonbinary_bits` oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md

@.planning/phases/07.3-pointwise-histogram-family/07.3-CONTEXT.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-RESEARCH.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-PATTERNS.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-01-PLAN.md

# Upstream parity targets (structural model — D-01)
@catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_one_byte_templ.cuh
@catboost-master/catboost/cuda/methods/pointwise_kernels.cpp

# In-repo analogs (Plan A's outputs — reuse, do not re-derive the seam)
@crates/cb-backend/src/kernels.rs
@crates/cb-backend/src/gpu_runtime.rs
@crates/cb-backend/src/kernels/pointwise_hist.rs

# CubeCL authoring rules (MANDATORY)
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (on any build error)
</context>

## Phase Goal

**As a** Rust developer embedding the GPU training path, **I want to** fill the device-resident 2-channel pointwise histogram for 5-, 6-, and 7-bit non-binary features on the same kernel via a compile-time bit-count, **so that** every one-byte feature group gets a correct, JIT-specialized histogram with no runtime branch and verifiable parity.

## Acceptance Criteria
- [ ] `cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::nonbinary_bits -- --nocapture` passes on gfx1100 for bits in {5,6,7} (and still {8} regression-green), reporting per-bit divergence.
- [ ] The 8-bit `nonbinary_8bit` and `handoff` tests from Plan A still pass (no seam regression).
- [ ] `wgpu` build+run of `nonbinary_bits`; `cuda` compile-only succeeds.
- [ ] `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged.
- [ ] No warp-size literal introduced; the bit-count drives only comptime line-size/used-prefix, never a runtime branch.

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nonbinary_bits oracle (5/6/7-bit fixtures) — the failing slice test</name>
  <files>crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/pointwise_hist.rs (Plan A's host_reference_hist2, max_divergence, fixture helpers, nonbinary_8bit body — clone the body shape, vary the border/bin count per bit-width)
    - .planning/phases/07.3-pointwise-histogram-family/07.3-VALIDATION.md § Per-Task Verification Map (7.3-B-* row: `kernels::pointwise_hist::nonbinary_bits`)
    - catboost-master/catboost/cuda/methods/pointwise_kernels.cpp lines 46-67 (DISPATCH_ONE_BYTE 5/6/7/8 — bits b → up to (1<<b) borders)
  </read_first>
  <behavior>
    - nonbinary_bits test fails initially (kernel/dispatch not yet generalized): for each bits in {5,6,7}, build fixtures with up to (1<<bits) borders, random f32+f64 der1, non-trivial weights, edge cases n=1/n=37/large-N, compute host_reference_hist2, and (once Task 2 lands) compare device vs reference within reported bounds.
    - Reuses the Plan A host reference and harness verbatim — no new reduction logic.
  </behavior>
  <action>Add a `nonbinary_bits` test fn to `kernels/pointwise_hist.rs` that loops bits over {5,6,7} (keep an 8-bit assertion as a regression anchor), generating fixtures with the matching border count `(1 << bits)`, reusing Plan A's `host_reference_hist2` and `max_divergence` and the edge-case fixtures (n=1/n=37/large-N), and calling `launch_pointwise_hist2_handle`/`launch_pointwise_hist2` with the bit-width selected by the feature group. Assert device vs host within the reported bounds (f32 ~1e-3 / f64 ~1e-9) and print the per-bit REPORTED divergence. The test compiles but FAILS until Task 2 generalizes the kernel/dispatch (RED). Test code MAY use `.unwrap()`/indexing.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::nonbinary_bits --no-run 2>&1 | tail -15</automated>
  </verify>
  <done>`nonbinary_bits` exists, compiles, loops bits {5,6,7} (+8 anchor) with matching border counts, and reuses the Plan A host reference/harness; it fails (or is unsatisfied) only because the kernel/dispatch is not yet generalized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the non-binary kernel + host dispatch to comptime bits {5,6,7,8} (makes nonbinary_bits green)</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs::pointwise_hist2_nonbinary_kernel (Plan A — currently 8-bit; generalize the shared-mem used-prefix + line size to derive from `bits` at comptime; HIST_SHMEM stays the 8-bit worst-case upper bound, the USED prefix = `2 * (1 << bits)`)
    - crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle (Plan A — extend the host bit-width selection to pass 5/6/7 as well as 8; the binSums layout/seam is FROZEN — do NOT change it)
    - catboost-master/catboost/cuda/methods/kernel/pointwise_hist2_one_byte_templ.cuh (TDeclarePassInnerOuterBitsTrait<Bits-5> — the inner/outer histogram bit split driven by Bits; mirror structurally, wave-agnostically)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/comptime_specialization.md
  </read_first>
  <behavior>
    - For bits in {5,6,7,8}, the kernel allocates the SharedMemory at the fixed worst-case HIST_SHMEM but uses only the `2 * (1 << bits)` prefix (zero-init + accumulate + reduce over the used prefix only); the histogram line size and any inner/outer bit split derive from `bits` at comptime, pruned at JIT time (no runtime branch, no warp literal).
    - launch_pointwise_hist2_handle selects bits per feature group (5/6/7/8) and writes into the FROZEN binSums layout unchanged.
    - nonbinary_bits goes GREEN for {5,6,7}; nonbinary_8bit + handoff stay green.
  </behavior>
  <action>Generalize `pointwise_hist2_nonbinary_kernel` so the histogram line size and shared-memory used-prefix derive from `#[comptime] bits: u32` (used prefix = `2 * (1 << bits)`; `HIST_SHMEM` remains the fixed 8-bit worst-case const, only the prefix changes — Pitfall 3). Mirror upstream's `Bits`-driven inner/outer histogram bit split (`TDeclarePassInnerOuterBitsTrait<Bits-5>`) structurally and wave-agnostically; the zero-init, AddPoint accumulation, in-block reduce, and atomic merge all operate over the used prefix. Extend `launch_pointwise_hist2_handle` in `gpu_runtime.rs` to select `bits` in {5,6,7,8} per the feature group's border count (mirroring `DISPATCH_ONE_BYTE`), passing it as the comptime arg — the binSums layout and the der-handle-in → binSums-handle-out seam are FROZEN from Plan A and MUST NOT change. Keep generics-float; no warp-size literal; no `unwrap`/`panic`/`[]` in production. On any CubeCL build error load `cubecl_error_guideline.md` first.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::nonbinary_bits -- --nocapture && cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::nonbinary_8bit -- --nocapture && cargo test -p cb-backend --no-default-features --features rocm kernels::pointwise_hist::handoff -- --nocapture</automated>
  </verify>
  <done>`nonbinary_bits` passes on rocm for {5,6,7} with per-bit reported divergence printed; `nonbinary_8bit` and `handoff` still pass; the bit-count is a comptime arg (no runtime branch); the binSums seam is unchanged; no warp-size literal.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-backend gate + SC-4 boundary re-assertion for the bit-width slice</name>
  <files>crates/cb-backend/src/kernels/pointwise_hist.rs</files>
  <read_first>
    - .planning/phases/07.3-pointwise-histogram-family/07.3-VALIDATION.md § Sampling Rate (after-wave gate)
    - crates/cb-backend/src/kernels/gradient_gpu.rs (SelectedRuntime cross-backend pattern)
  </read_first>
  <action>Confirm `nonbinary_bits` builds+runs under `wgpu` on host and compiles under `cuda`. Re-run the SC-4 boundary gate: `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `cb-compute`/`cb-core`/`cb-model` byte-unchanged. Grep the generalized kernel for forbidden warp-size literals (`& 31`, `& 63`, `tiled_partition`, bare `32`/`64` in a stride/slice). Fix any backend gap inside `cb-backend` only.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features wgpu && cargo test -p cb-backend --no-default-features --features wgpu kernels::pointwise_hist::nonbinary_bits -- --nocapture && cargo build -p cb-backend --no-default-features --features cuda && test "$(cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl)" -eq 0</automated>
  </verify>
  <done>wgpu builds and runs `nonbinary_bits`; cuda compiles; `cb-compute` cubecl count 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged; no warp-size literal in the generalized kernel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch helper → device kernel | In-process buffer lengths/shapes + the comptime bit-count cross here; the only untrusted-shape surface |
| device kernel → device binSums buffer | Bin index (now over a `(1<<bits)` range) writes into the histogram buffer |

Pure in-process numeric GPU compute; NO external input, network, auth, secrets, file parsing, or untrusted data. Honest threat surface = correctness/safety invariants.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-07 | Tampering / DoS (UB) | bit-count > border-count mismatch → used-prefix over-read of the shared histogram | mitigate | Used prefix = comptime `2 * (1 << bits)` bounded by `HIST_SHMEM` 8-bit worst case; host dispatch selects `bits` from the feature group's actual border count; bin index device-bounds-guarded |
| T-07.3-08 | Tampering / DoS (UB) | length mismatch among der1/weight/cindex/indices at the new bit-widths | mitigate | Reuse Plan A's `CbError::LengthMismatch` host guards (unchanged seam) |
| T-07.3-09 | DoS | `unwrap`/`panic`/indexing in the extended production dispatch | mitigate | D-13 / workspace lints ban `unwrap`/`expect`/`panic`/`[]` in production; tests use the `#[cfg(test)]` allow |
| T-07.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); only a comptime generalization of an existing kernel |

**Block-on-high:** none. The bit-width generalization adds no external input surface; the only new risk is a comptime-bounded used-prefix over-read, fully mitigated by the comptime bound + device guard.
</threat_model>

<verification>
- rocm: `nonbinary_bits` green for {5,6,7} (+8 anchor); `nonbinary_8bit`/`handoff` regression-green.
- wgpu: build + run `nonbinary_bits` on host.
- cuda: compile-only.
- SC-4: cb-compute cubecl count 0; cb-compute/cb-core/cb-model byte-unchanged.
- D-09: no warp-size literal; bit-count is comptime-only.
</verification>

<success_criteria>
- 5/6/7-bit non-binary fills run device-resident on rocm via comptime `bits` on the same kernel and match the ordered host reference within reported tolerance.
- The Plan A binSums seam is reused unchanged.
- No new kernel family; no runtime bit-count branch; no warp-size literal; generics-float.
- cb-compute cubecl-free; cb-core/cb-model byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/07.3-pointwise-histogram-family/07.3-02-SUMMARY.md` when done.
</output>
