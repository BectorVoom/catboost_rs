# Phase 7.3: Pointwise Histogram Family - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-20
**Phase:** 7.3-Pointwise Histogram Family
**Mode:** auto (all gray areas auto-selected; each question auto-resolved to the recommended default — no interactive prompts)
**Areas discussed:** Variant coverage & sequencing, Bit-width specialization mechanism, Accumulation/atomic strategy, CPU self-oracle baseline & fixtures, Device-residency seam, Verification gating

---

## Variant coverage & in-phase sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Narrowest-first, full family across plans | Land general one-byte non-binary path + seam first, then 5/6/7-bit, half-byte, binary as additive plans — all in this phase (recommended) | ✓ |
| All variants in one mega-plan | Implement every bit-width specialization simultaneously | |
| Defer some variants to a follow-on | Ship only the general path now | |

**Auto-selected:** Narrowest-first, full family across plans (recommended default).
**Notes:** SC-1 requires the full family to run on rocm — sequencing is plan decomposition, not a scope cut. Mirrors the 7.1/7.2 "resist porting the whole set up front" pattern. → D-7.3-01.

---

## Bit-width specialization mechanism in CubeCL (research-flagged)

| Option | Description | Selected |
|--------|-------------|----------|
| Three kernel families + comptime bit-count | Distinct kernels for one-byte non-binary / half-byte / binary; 5/6/7/8-bit via `#[comptime]` (recommended) | ✓ |
| Single parametric runtime kernel | One kernel branching on bit-width at runtime | |
| One `#[cube]` fn per concrete variant | Six fully-separate hand-written kernels | |

**Auto-selected:** Three kernel families + comptime bit-count (recommended default).
**Notes:** Upstream's shared-mem layout/load/unroll differ structurally across the three families (D-01). Exact CubeCL comptime-vs-monomorphization expression is the roadmap RESEARCH flag — left to RESEARCH.md. → D-7.3-02.

---

## Accumulation strategy — shared-memory blocking + in-kernel atomics

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror `compute_point_hist2_loop` shmem + atomic merge | Per-block shared-mem histogram → atomic merge to global, f64 finalize (D-03), reuse 7.1 atomic-finalize primitive (recommended) | ✓ |
| Scatter + host ordered sum only | Reuse the existing D-02 scatter path (no on-device fill) | |
| Global-memory atomics only | Atomic-add straight to global bins, no shared-mem blocking | |

**Auto-selected:** Mirror upstream shmem + atomic merge (recommended default).
**Notes:** D-03/D-09 are umbrella-locked — restated, not reopened. The existing `histogram_scatter_kernel` (D-02) stays the CPU/parity baseline, not the GPU fill. → D-7.3-03.

---

## CPU self-oracle baseline & fixtures

| Option | Description | Selected |
|--------|-------------|----------|
| Host 2-channel reference via ordered `sum_f64`, report divergence | Generalize `reduce_leaf_stats` leaf→bin; report max abs/rel, no GPU-06 sign-off (recommended) | ✓ |
| Hard-code an epsilon gate now | Assert against a fixed tolerance in 7.3 | |
| C++ oracle directly | Compare against original CatBoost CUDA output | |

**Auto-selected:** Host reference + report divergence (recommended default).
**Notes:** D-7.2-06 pattern — report, don't sign off (epsilon is 7.6). Fixtures: random f32+f64, weights, edges (empty/single/non-multiple/large-N) across bit-widths. → D-7.3-04.

---

## Device-residency input/output seam

| Option | Description | Selected |
|--------|-------------|----------|
| Consume 7.2 der handles, emit device histogram handle | No host fold in; bins/indices/partition device-resident; `binSums` handle out for 7.5 (recommended) | ✓ |
| Host round-trip between der and histogram | Read der to host, refill device for histogram | |
| Fold scan/update into 7.3 | Include `ScanPointwiseHistograms` post-fill here | |

**Auto-selected:** Consume der handles, emit device histogram handle (recommended default).
**Notes:** Honors D-7.2-04 hand-off seam; weight folded here. Scan/update integration is 7.5. → D-7.3-05.

---

## Verification gating

| Option | Description | Selected |
|--------|-------------|----------|
| Per-crate (rocm run+oracle, wgpu run, cuda build-only) | Carried from D-7.2-07 under disk-pressure constraint; rocm never in CI (recommended) | ✓ |
| Full-workspace test link | Single workspace-wide test build | |

**Auto-selected:** Per-crate gating (recommended default).
**Notes:** Root disk ~100% full; full-workspace links fail. Umbrella D-06: rocm tests local/manual only. → D-7.3-06. Boundary preservation (SC-4) carried as D-7.3-07.

---

## Claude's Discretion

- Exact CubeCL comptime-vs-monomorphization expression of bit-width specialization (research-flag item).
- Per-bit-width shared-memory histogram line-size / blocking geometry, launch geometry, histogram-output handle type.
- Partition/`fullPass` handling depth for the single-tree fill (multi-part grow-loop detail deferred to 7.5).
- Precise representative fixture set.

## Deferred Ideas

- Pairwise histogram family (`pairwise_hist_*`) — Phase 7.4.
- Score/split calcers + scan/update grow-loop integration — Phase 7.5.
- Empirical GPU-06 epsilon sign-off + rocm oracle-gate lock-in — Phase 7.6.
- `pointwise_hist1` (single-channel) + `greedy_subsets_searcher` hist variant — additive follow-ons only.
