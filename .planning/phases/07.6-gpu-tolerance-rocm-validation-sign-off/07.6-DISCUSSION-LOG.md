# Phase 7.6: GPU Tolerance, rocm Validation & Sign-off - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-21
**Phase:** 7.6-gpu-tolerance-rocm-validation-sign-off
**Mode:** `--auto` (Claude auto-selected the recommended option for every area; no interactive prompts)
**Areas discussed:** Divergence measurement coverage, Run-to-run variance protocol, Epsilon structure, Epsilon headroom & derivation, rocm gate formalization, Documentation deliverable

---

## Divergence Measurement Coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Per-kernel + end-to-end (full surface) | Re-run every 7.1–7.5 self-oracle for per-family divergence AND measure GPU-trained vs CPU-trained model (structure exact, leaf values within epsilon) | ✓ |
| End-to-end only | Only compare full trained models | |
| Per-kernel only | Only aggregate existing kernel self-oracles | |

**User's choice:** [auto] Per-kernel + end-to-end (full surface) — recommended default.
**Notes:** End-to-end leaf values were REPORTED-not-signed-off in 7.5; 7.6 is where they enter the gate.

---

## Run-to-Run Variance Protocol

| Option | Description | Selected |
|--------|-------------|----------|
| N≥30 repeats, report max + stddev | Characterize D-03 atomic non-determinism with ≥30 repeats per fixture | ✓ |
| Single run | One measurement per fixture | |
| N=5 | Small repeat count | |

**User's choice:** [auto] N≥30 repeats, report max + stddev — recommended default.
**Notes:** Variance term feeds the epsilon headroom; single-run defeats the purpose of D-04's empirical epsilon.

---

## Epsilon Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Single global epsilon + evidence table | One signed-off number is the gate; per-family divergence documented as evidence | ✓ |
| Tiered per-family thresholds | Separate epsilon per kernel family | |

**User's choice:** [auto] Single global epsilon + evidence table — recommended default.
**Notes:** Matches GPU-06 wording ("a separately-stated epsilon"); tiered thresholds over-engineer a sign-off gate.

---

## Epsilon Headroom & Derivation

| Option | Description | Selected |
|--------|-------------|----------|
| Conservative, looser-than-1e-5 per D-04; document bit-exact gap | Keep D-04 headroom despite bit-exact observations; surface the tension at sign-off | ✓ |
| Tight epsilon reflecting observed bit-exactness | Set epsilon near the observed ~1e-14 | |

**User's choice:** [auto] Conservative, looser-than-1e-5 per D-04 — recommended default.
**Notes:** Observed divergence is bit-exact (0.0–~1e-14), tighter than D-03/D-04 predicted. Honor D-04's looser-than-1e-5 mandate anyway for headroom against unmeasured losses/larger fixtures/wave64; document the observed-vs-proposed gap for an informed sign-off.

---

## rocm Gate Formalization (GPU-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Documented manual per-crate rocm run, no CI | `cargo test -p cb-backend --features rocm` in-env on gfx1100, recorded as authoritative; never in GitHub Actions | ✓ |
| Wire into CI | Add a GitHub Actions GPU lane | |
| wgpu as a gate | Promote wgpu to a validation gate | |

**User's choice:** [auto] Documented manual per-crate rocm run, no CI — recommended default.
**Notes:** Standing ROCm-never-in-CI constraint (D-06); disk pressure forces per-crate not full-workspace; wgpu/cuda stay compile-gated (D-07).

---

## Documentation Deliverable

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated tolerance doc + VERIFICATION sign-off + REQUIREMENTS update | Record epsilon/derivation, vs-CPU-path framing, wave32 scope + wave64 gap, rocm-only policy; flip GPU-03/06 | ✓ |
| Minimal note only | Single line in VERIFICATION | |

**User's choice:** [auto] Dedicated tolerance doc + VERIFICATION sign-off + REQUIREMENTS update — recommended default.

---

## Claude's Discretion

- Exact fixture selection for the end-to-end GPU-vs-CPU model comparison.
- Precise repeat count above the N≥30 floor.
- Document filename/location.
- The clean rounded epsilon candidate to propose for sign-off.

## Deferred Ideas

- Wave64 (CDNA/MI-series) literal validation — documented gap, not closed here (D-09).
- Performance/perf-tuning parity beyond structural parity — follow-up.
- wgpu as a CI validation gate — not this phase (D-07).
- `estimated-feature-grid-parity.md` todo (score 0.40) — reviewed, NOT folded: CPU-side KNN/online-HNSW parity gap, unrelated to GPU sign-off.

## Note on the User Sign-off Gate

SC-2's user sign-off of the concrete epsilon is a **hard human gate during
execution/verification**, not a discuss-time decision. `--auto` captured the
*methodology* for proposing the epsilon, not the number. The plan must include
an explicit pause for the user to sign off before GPU-06 is recorded complete
(CONTEXT D-7.6-07).
