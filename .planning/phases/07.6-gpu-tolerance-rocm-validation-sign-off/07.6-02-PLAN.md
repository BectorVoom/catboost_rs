---
phase: 07.6-gpu-tolerance-rocm-validation-sign-off
plan: 02
type: execute
wave: 2
depends_on: ["07.6-01"]
files_modified:
  - .planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [GPU-03, GPU-06]
must_haves:
  truths:
    - "The full rocm suite runs green in-env on gfx1100 via `cargo test -p cb-backend --no-default-features --features rocm` and is documented as the authoritative GPU gate, never in GitHub Actions (GPU-03 / D-06)"
    - "A concrete epsilon strictly looser than the CPU path's 1e-5 is PROPOSED with headroom over observed_max+3σ, the user SIGNS OFF the number, and only the signed-off value is recorded (GPU-06 / D-7.6-07)"
    - "07.6-GPU-TOLERANCE.md records the signed-off epsilon + derivation, the 'vs Rust CPU path not C++ oracle' framing (D-04), the wave32-native scope + explicit wave64 (CDNA/MI) gap (D-09), the rocm-only/never-CI policy (D-06), and the per-family evidence table"
    - "REQUIREMENTS.md GPU-03 and GPU-06 are flipped to complete with the phase reference after sign-off"
    - "wgpu and cuda remain compile-gated stubs (assert they still build), not validation gates (D-07)"
  artifacts:
    - path: ".planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md"
      provides: "The signed-off GPU tolerance / validation document (D-7.6-06)"
      min_lines: 40
      contains: "epsilon"
    - path: ".planning/REQUIREMENTS.md"
      provides: "GPU-03 and GPU-06 flipped to complete"
      contains: "GPU-06"
  key_links:
    - from: ".planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md"
      to: "07.6-01-SUMMARY.md [GPU-06 EVIDENCE] numbers"
      via: "per-family evidence table transcribed from the in-env console output"
      pattern: "evidence"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 7.6"
      via: "GPU-03/GPU-06 status flip with phase reference"
      pattern: "GPU-0[36]"
---

<objective>
Close the Phase 7 umbrella: lock the **rocm gate (GPU-03)** by running the full
suite green in-env on gfx1100 and documenting it as the authoritative, never-in-CI
GPU oracle; **propose a concrete epsilon, obtain the hard human sign-off
(D-7.6-07)**, and write the signed-off number + derivation + scope into
`07.6-GPU-TOLERANCE.md`; then flip REQUIREMENTS GPU-03/GPU-06 to complete.

This is the **proposal → sign-off → documentation/gate** vertical slice (MVP_MODE):
after this plan, GPU-06 has a signed-off epsilon and GPU-03 has a documented
authoritative gate — the Phase 7 umbrella's last two open requirements close.
Consumes the `[GPU-06 EVIDENCE]` numbers Plan 01 produced.

Purpose: Turn Plan 01's REPORTED measurements into a SIGNED-OFF gate (SC-1→SC-4),
honoring D-04's mandate that the epsilon be looser than 1e-5 DESPITE the bit-exact
observations, with the tension surfaced for an informed human decision.
Output: `07.6-GPU-TOLERANCE.md` (signed-off doc), REQUIREMENTS.md GPU-03/GPU-06
flipped, and the VERIFICATION sign-off content for the phase verifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase 7.6 source artifacts (authoritative)
@.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-CONTEXT.md
@.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-RESEARCH.md
@.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-VALIDATION.md

# The evidence this plan consumes
@.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-01-SUMMARY.md

# Umbrella fidelity decisions (D-04 epsilon, D-06 rocm-never-CI, D-07 stubs, D-09 wave64 gap)
@.planning/phases/07-gpu-backends-via-cubecl/07-CONTEXT.md

@./CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md`
  (NEW doc) — signed-off epsilon + derivation + per-family evidence table +
  vs-Rust-CPU-path framing + wave32 scope / wave64 gap + rocm-only/never-CI policy
- `.planning/REQUIREMENTS.md` — GPU-03 and GPU-06 status lines flipped to `[x]` /
  "Complete (Phase 7.6)"
- VERIFICATION sign-off content (handed to the phase verifier / recorded per
  the summary) capturing the signed-off epsilon and the green rocm suite
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Run the full rocm suite green in-env (GPU-03 gate) + assert wgpu/cuda still build</name>
  <files>(no file edits — in-env GPU run + capture)</files>
  <what-built>
    Plan 01 landed `gpu_tolerance.rs`. This checkpoint EXERCISES the GPU-03 gate:
    the orchestrator/human runs the full `cb-backend` rocm suite in-env on gfx1100
    (a verifier subagent has NO GPU — D-06), confirms it is green and emits the
    `[GPU-06 EVIDENCE]` lines, and confirms the wgpu/cuda compile-gated stubs still
    build (D-07). This produces the GPU-03 authoritative-gate evidence AND the
    captured per-family evidence numbers that Task 2's epsilon proposal consumes.
  </what-built>
  <action>
    Orchestrator/human runs the full rocm suite in-env on gfx1100, captures the
    per-family `[GPU-06 EVIDENCE]` numbers, confirms the wgpu/cuda compile-gated
    stubs still build, and confirms no `features rocm` appears in `.github/`. This
    is a blocking-human checkpoint — a verifier subagent cannot run it (no GPU
    access, D-06). The captured numbers feed Task 2's epsilon proposal.
  </action>
  <how-to-verify>
    1. Full rocm suite (authoritative GPU gate, per-crate due to disk pressure):
       `cargo test -p cb-backend --no-default-features --features rocm -- --nocapture`
       Expected: all tests green; `[GPU-06 EVIDENCE]` lines present for every family
       (reduce, der/hess, pointwise hist, pairwise hist, score/split,
       end_to_end_leaf_values) on `channel=f64(rocm/gfx1100)`.
    2. Capture the observed numbers (observed_max_abs, stddev, observed_max+3σ,
       AtomicFinalizePath) per family — these feed Task 2.
    3. Stubs still build (D-07, not validation gates):
       `cargo build -p cb-backend --no-default-features --features wgpu`
       `cargo build -p cb-backend --no-default-features --features cuda`
       Expected: both RC=0.
    4. Confirm NEVER wired into GitHub Actions: `grep -ri "features rocm" .github/ || echo "OK: no rocm in CI"` → expect "OK".
  </how-to-verify>
  <verify><human-check>Full rocm suite green in-env; [GPU-06 EVIDENCE] lines present on channel=f64(rocm/gfx1100); wgpu+cuda build RC=0; no rocm in CI.</human-check></verify>
  <done>The full rocm suite is green in-env on gfx1100, the per-family evidence numbers are captured, wgpu/cuda compile-gated stubs still build, and no rocm gate is wired into GitHub Actions.</done>
  <resume-signal>Paste the per-family [GPU-06 EVIDENCE] numbers + confirm "rocm suite green, wgpu/cuda build, no rocm in CI", or describe failures.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Propose the concrete epsilon and obtain the hard human sign-off (D-7.6-07)</name>
  <files>(no file edits — human decision; the value is recorded in Task 3)</files>
  <decision>
    The single global GPU-06 epsilon for the rocm-vs-Rust-CPU-path gate. The
    executor PROPOSES a concrete number; the USER signs off the actual value. The
    epsilon is NOT auto-decided — auto-mode captured only the methodology
    (D-7.6-01..04), never the number.
  </decision>
  <action>
    The executor presents Task 1's observed per-family numbers (observed_max_abs +
    stddev + observed_max+3σ), proposes a concrete epsilon strictly looser than 1e-5
    with the bit-exact-vs-conservative tension made explicit (see options), and
    PAUSES for the user to sign off the actual value. Do NOT auto-decide or flow past
    this checkpoint (D-7.6-07). Only the human-signed-off value proceeds to Task 3.
  </action>
  <context>
    From Task 1's evidence: every family observed bit-exact to ~1e-14 divergence
    (far tighter than D-03/D-04 predicted), and the N≥30 variance term
    (observed_max+3σ) is at that same tiny scale. D-04 nonetheless MANDATES an
    epsilon strictly looser than the CPU path's 1e-5, measured vs the **Rust CPU
    path** (not the C++ oracle). The TENSION to surface: propose a clean value
    looser than 1e-5 (e.g. 1e-4) DESPITE the bit-exact observations, because
    (a) D-04 requires it; (b) bit-exactness may not hold for unmeasured
    losses/fixtures, larger atomic-contention fixtures, or wave64 hardware;
    (c) headroom protects the gate from future flakiness. Present the observed
    numbers, the proposed epsilon, and this rationale so the user signs an
    INFORMED number. Only after sign-off is the value recorded.
  </context>
  <options>
    <option id="propose-1e-4">
      <name>ε = 1e-4 (proposed default — clean, one order looser than 1e-5)</name>
      <pros>Honors D-04 (strictly looser than 1e-5); clean round value; ample headroom over observed_max+3σ (~1e-14) and over the f64 run-stable BOUND (1e-9); robust to future atomic-contention / unmeasured-loss flakiness.</pros>
      <cons>Far looser than the bit-exact observations — a deliberate conservatism the doc must justify.</cons>
    </option>
    <option id="user-other">
      <name>A different clean value the user specifies (must stay strictly looser than 1e-5 per D-04)</name>
      <pros>User retains full authority over the gate number.</pros>
      <cons>Must remain > 1e-5 to honor D-04; tighter choices reduce future-flakiness headroom.</cons>
    </option>
  </options>
  <verify><human-check>The recorded epsilon equals the user's signed-off value and is strictly looser than 1e-5 (D-04); auto-mode did not flow past this checkpoint.</human-check></verify>
  <done>The user has signed off a concrete epsilon value strictly looser than 1e-5; it is the only value carried into Task 3's documentation and REQUIREMENTS flip.</done>
  <resume-signal>Sign off the concrete epsilon value (e.g. "approved: 1e-4") or specify a different value strictly looser than 1e-5.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 07.6-GPU-TOLERANCE.md + flip REQUIREMENTS GPU-03/GPU-06 + VERIFICATION sign-off content</name>
  <files>.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-CONTEXT.md` D-7.6-06 (doc content requirements) + D-04/D-06/D-09 carry-forwards
    - `.planning/REQUIREMENTS.md` lines ~83 (GPU-03), ~86 (GPU-06), ~184/187 (the status-table rows) — the exact lines to flip
    - Task 1 evidence (per-family numbers) + Task 2 signed-off epsilon
  </read_first>
  <action>
    Write `.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md`
    recording, per D-7.6-06: (1) the SIGNED-OFF epsilon (the Task 2 value) and its
    derivation — `roundUp` to a clean value strictly looser than 1e-5 over
    `observed_max + 3σ` (D-7.6-04), with the bit-exact-vs-conservative tension
    documented; (2) that it is measured **vs the Rust CPU path, not the C++ CPU
    oracle** (D-04); (3) the **wave32-native validation scope** on gfx1100 and the
    **explicit, documented wave64 (CDNA/MI-series) gap** this phase does NOT close
    (D-09); (4) the **rocm-only / never-in-CI** gate policy and the authoritative
    invocation `cargo test -p cb-backend --no-default-features --features rocm`
    (D-06), with wgpu/cuda as compile-gated stubs not validation gates (D-07);
    (5) the **per-family observed-divergence evidence table** (supporting evidence,
    a SINGLE global epsilon is the gate — NOT per-family thresholds, D-7.6-03),
    transcribed from the Task 1 `[GPU-06 EVIDENCE]` numbers, including the
    end-to-end leaf-value row (the 7.5 REPORTED→now-SIGNED-OFF numbers).

    Then flip `.planning/REQUIREMENTS.md`: GPU-03 `[ ]`→`[x]` and its status-table
    row to "Complete (Phase 7.6)"; GPU-06 `[ ]`→`[x]` and its row to "Complete
    (Phase 7.6) — ε=<signed-off value>, vs Rust CPU path". Do NOT alter any other
    requirement.

    Produce the VERIFICATION sign-off content (the signed-off epsilon + the green
    rocm suite statement + the wave32/wave64 scope) in the plan SUMMARY so the phase
    verifier can record it — note that the rocm run + epsilon sign-off were
    discharged by the orchestrator/human in-env, NOT by a verifier subagent (D-06).
  </action>
  <verify>
    <automated>test -f .planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md && grep -qi "rust cpu path" .planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md && grep -qi "wave64" .planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-GPU-TOLERANCE.md && grep -q "GPU-06" .planning/REQUIREMENTS.md && grep -E "GPU-0[36].*\[x\]|x\].*GPU-0[36]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `07.6-GPU-TOLERANCE.md` exists and records: signed-off epsilon + derivation; vs-Rust-CPU-path framing (D-04); wave32 scope + explicit wave64 gap (D-09); rocm-only/never-CI policy + authoritative invocation (D-06); per-family evidence table (D-7.6-03) including the end-to-end leaf-value row
    - REQUIREMENTS.md GPU-03 and GPU-06 flipped to complete with the Phase 7.6 reference and the signed-off ε
    - VERIFICATION sign-off content captured in the SUMMARY; rocm/sign-off noted as orchestrator/human-discharged (not a subagent)
    - No other requirement altered
  </acceptance_criteria>
  <done>The signed-off epsilon + derivation + per-family evidence + wave32/wave64 scope + rocm-only gate policy are written to `07.6-GPU-TOLERANCE.md`; GPU-03/GPU-06 are flipped to complete with the signed-off ε; the Phase 7 umbrella's last two requirements close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local sign-off / documentation slice: NO network surface, NO untrusted input, NO
new production code (docs + requirements edits + an in-env GPU run). The boundary
is **measurement integrity** — that the gate number recorded reflects a real GPU
run, and that the gate is not accidentally weakened or CI-wired.

| Boundary | Description |
|----------|-------------|
| in-env rocm run ↔ recorded gate | The orchestrator/human runs the suite on gfx1100 and transcribes numbers; a wrong feature-gate or CI-wiring would corrupt the gate's authority |
| proposed ε ↔ signed-off ε | The executor proposes; only the human-signed-off value may be recorded (auto-mode must not flow past D-7.6-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.6-04 | Spoofing (fake validation channel) | the GPU-03 gate run | mitigate | Task 1 pins `--no-default-features --features rocm` and verifies `channel=f64(rocm/gfx1100)` in the evidence lines; a CPU-vs-CPU run (default `cpu`) would be bit-exact and is explicitly rejected. The doc records the exact authoritative invocation. |
| T-07.6-05 | Elevation/weakening (auto-deciding the gate) | the epsilon value | mitigate | The epsilon is a `checkpoint:decision` with `gate="blocking-human"` (autonomous: false); auto-mode cannot flow past it (D-7.6-07). Only the human-signed-off number is written to the doc / REQUIREMENTS. |
| T-07.6-06 | Tampering (CI exposure of the rocm gate) | `.github/` workflows | mitigate | Task 1 greps `.github/` for `features rocm` and expects none; the doc records the never-in-CI policy (D-06) as standing constraint. |
| T-07.6-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (docs + requirements edits + an in-env test run). No legitimacy gate needed. |

No `high`-severity classic threats. The register reflects gate-integrity for a
sign-off phase.
</threat_model>

<verification>
- Full rocm suite green in-env on gfx1100 (Task 1, orchestrator/human-discharged — D-06).
- wgpu and cuda still build (D-07 stubs unaffected).
- No `features rocm` anywhere under `.github/` (never-in-CI, D-06).
- The epsilon recorded equals the human-signed-off value (D-7.6-07); it is strictly looser than 1e-5 (D-04).
- `07.6-GPU-TOLERANCE.md` covers all five D-7.6-06 content requirements; REQUIREMENTS GPU-03/GPU-06 flipped.
- NOTE: a verifier subagent has NO GPU — the rocm run + sign-off are discharged by the orchestrator/human in-env.
</verification>

<success_criteria>
- SC-1: epsilon proposed with headroom over observed_max+3σ (from Plan 01 evidence).
- SC-2: user signs off the concrete epsilon; recorded as vs the Rust CPU path, looser than 1e-5 (D-04).
- SC-3: rocm suite is the authoritative GPU gate, green in-env, never in CI; wgpu/cuda stay compile-gated stubs (D-06/D-07).
- SC-4: wave32-native scope + explicit wave64 (CDNA/MI) gap documented (D-09).
- GPU-03 and GPU-06 flipped to complete — Phase 7 umbrella closed.
</success_criteria>

<output>
Create `.planning/phases/07.6-gpu-tolerance-rocm-validation-sign-off/07.6-02-SUMMARY.md` when done.
Record: the signed-off epsilon value, the per-family evidence table, the green
rocm suite confirmation, and that the rocm run + sign-off were
orchestrator/human-discharged in-env (not by a subagent).
</output>
