---
phase: 07.4-pairwise-histogram-family
plan: 02
type: execute
wave: 2
depends_on: ["07.4-01"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/pairwise_hist.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "The 8-bit-atomics pairwise histogram (the structurally DISTINCT global-atomics family — upstream pairwise_hist_one_byte_8bit_atomics) runs device-resident on rocm and matches the ordered host-reference 4-channel histogram within the REPORTED divergence across empty/n_pairs=1/n_pairs=37/large-N fixtures (D-7.4-02, SC-1)."
    - "The 8-bit-atomics family is a SEPARATE #[cube] kernel symbol + a SEPARATE launch arm (structural parity with upstream's separate .cu), even though the MVP body mirrors the non-binary kernel with bits=8 always using direct global atomics (the upstream per-thread cached-bin run is documented as a perf follow-up)."
    - "The 8-bit family reuses the FROZEN 4-channel weight-only binSums layout from Plan A unchanged (histLineSize = 4 * totalBinFeatures, (feature*n_bins+bin)*4 + histId); buffer length is n_features*256*4."
    - "cb-compute stays cubecl-free and cb-core/cb-model byte-unchanged; all new code lives in cb-backend (SC-4 / D-7.4-08)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "pairwise_hist_8bit_atomics_kernel<F> (#[cube(launch)], #[comptime] one_hot; bits fixed at 8, direct global Atomic<F>::fetch_add — distinct family)"
      contains: "pairwise_hist_8bit_atomics_kernel"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_pairwise_hist_8bit_handle + _into arm (256-bin, reuses the FROZEN 4-channel layout + the backend-dispatched channel + the guard block)"
      contains: "pairwise_hist_8bit_atomics_kernel::launch"
    - path: "crates/cb-backend/src/kernels/pairwise_hist.rs"
      provides: "eightbit_atomics test over SelectedRuntime (edge cases + REPORT divergence against host_reference_pairwise_hist with n_bins=256)"
      contains: "fn eightbit_atomics"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_pairwise_hist_8bit_handle"
      to: "crates/cb-backend/src/kernels.rs::pairwise_hist_8bit_atomics_kernel"
      via: "cube launch, bits=8, direct global atomics"
      pattern: "pairwise_hist_8bit_atomics_kernel::launch"
    - from: "crates/cb-backend/src/kernels/pairwise_hist.rs::eightbit_atomics"
      to: "crates/cb-backend/src/gpu_runtime.rs::launch_pairwise_hist_8bit_handle"
      via: "pair handles in -> 4-channel binSums handle out -> read back ONCE"
      pattern: "launch_pairwise_hist_8bit"
---

<objective>
Add the **8-bit-atomics** pairwise histogram family — upstream's `ComputePairwiseHistogramOneByte8BitAtomics`, a structurally DISTINCT family because a 256-bin × 4-channel line does not fit the per-block shared-memory budget, so upstream accumulates via true global atomics (D-7.4-02/04). Add it as a separate `#[cube]` kernel symbol + a separate launch arm, reusing the FROZEN 4-channel layout + seam + self-oracle harness from Plan A. Self-oracle on rocm with `n_bins = 256`.

Purpose: Closes the 8-bit slice of SC-1's variant set. The MVP body is structurally a `bits = 8` non-binary kernel that ALWAYS uses direct global `Atomic<F>::fetch_add` (the shipped 7.3 MVP already does this for every width) — but kept a DISTINCT kernel + launch arm to preserve structural parity with upstream's separate `.cu` (D-7.4-02). Upstream's per-thread `CachedBinsLeq/Ge` global-atomic-flush cache is documented as a perf follow-up over the same atomic structure.
Output: `pairwise_hist_8bit_atomics_kernel`, `launch_pairwise_hist_8bit_handle` + its `_into` arm, the `eightbit_atomics` self-oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md

@.planning/phases/07.4-pairwise-histogram-family/07.4-CONTEXT.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-RESEARCH.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-PATTERNS.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-VALIDATION.md

# The seam this plan extends (read the Plan A SUMMARY for the as-implemented 4-channel layout + seam)
@.planning/phases/07.4-pairwise-histogram-family/07.4-01-SUMMARY.md
@.planning/phases/07-gpu-backends-via-cubecl/07-CONTEXT.md

# Upstream parity target (the DISTINCT global-atomic family — D-01)
@catboost-master/catboost/cuda/methods/kernel/pairwise_hist_one_byte_8bit_atomics.cuh

# In-repo analogs (Plan A's pairwise kernel + the shipped 7.3 8-bit precedent)
@crates/cb-backend/src/kernels.rs
@crates/cb-backend/src/gpu_runtime.rs
@crates/cb-backend/src/kernels/pairwise_hist.rs

# CubeCL authoring rules (MANDATORY per AGENTS.md)
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (load on ANY CubeCL build error before fixing)
</context>

## Phase Goal

**As a** Rust developer embedding the GPU pairwise/ranking training path, **I want to** fill a device-resident 4-channel pairwise histogram for 8-bit (256-bin) features on rocm via the distinct global-atomics family, **so that** the full variant set runs device-resident with verifiable parity against the CPU pairwise path.

## Artifacts this phase produces (new symbols — exclude from drift verification)

This plan creates: `pairwise_hist_8bit_atomics_kernel` (`#[cube(launch)]`, `#[comptime] one_hot`, bits fixed at 8); `launch_pairwise_hist_8bit_handle` + its private `_into` arm in `gpu_runtime.rs`; the `eightbit_atomics` test in `kernels/pairwise_hist.rs`. It REUSES (does not recreate) the Plan A `PAIR_HIST_CHANNELS`, `pair_hist_binsums_len`, `read_pair_binsums_f64`, `host_reference_pairwise_hist`, `make_pair_fixture`, and the backend-dispatched channel machinery.

## Acceptance Criteria
- [ ] `cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::eightbit_atomics -- --nocapture` passes on gfx1100 (n_bins=256) and prints the REPORTED max abs/rel divergence.
- [ ] `pairwise_hist_8bit_atomics_kernel` is a SEPARATE `#[cube]` symbol with a SEPARATE launch arm (grep finds both `pairwise_hist_8bit_atomics_kernel` and `pairwise_hist_nonbinary_kernel` as distinct fns) — not folded into the non-binary kernel.
- [ ] `cargo build -p cb-backend --no-default-features --features wgpu` + run `eightbit_atomics` on host; `cargo build -p cb-backend --no-default-features --features cuda` compile-only.
- [ ] `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `crates/cb-compute`, `crates/cb-core`, `crates/cb-model` byte-unchanged.
- [ ] No warp/tile literal in any stride/slice offset; kernel is `<F: Float>`; binSums indexing is `* 4` (reuses the FROZEN layout — never `* 2`).

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 8-bit-atomics distinct #[cube] family + its launch arm + the eightbit_atomics self-oracle</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs, crates/cb-backend/src/kernels/pairwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs (the Plan A `pairwise_hist_nonbinary_kernel` — the body to mirror with bits fixed at 8 and global atomics always; the shipped 7.3 8-bit `pointwise_hist2_nonbinary_kernel` precedent — grid-stride direct `Atomic<F>::fetch_add`, NO shared-mem, NO warp literal)
    - crates/cb-backend/src/gpu_runtime.rs (the Plan A `launch_pairwise_hist_handle`/`_into` + `pair_hist_binsums_len` + the `launch_hist2_family!` backend-dispatched channel macro — clone an arm with bits=8, n_bins=256)
    - crates/cb-backend/src/kernels/pairwise_hist.rs (the Plan A `host_reference_pairwise_hist`, `make_pair_fixture`, `max_divergence`, `read_pair_handle`, and the `nonbinary_bits` test body — clone for `eightbit_atomics` with n_bins=256)
    - catboost-master/catboost/cuda/methods/kernel/pairwise_hist_one_byte_8bit_atomics.cuh (`TEightBitHistogram` — the per-thread CachedBinsLeq/Ge + direct global atomicAdd flush; the DISTINCT global-atomic structure. Reduce to the MVP: direct per-pair global Atomic<F>::fetch_add into the 4-channel cell, bits=8; document the cache as a perf follow-up — RESEARCH Pattern 3 / A2)
    - .planning/phases/07.4-pairwise-histogram-family/07.4-PATTERNS.md § "8-bit-atomics family"
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (before writing kernel code); load cubecl_error_guideline.md on ANY build error before fixing
  </read_first>
  <behavior>
    - `eightbit_atomics` oracle test goes GREEN with n_bins=256: device histogram matches `host_reference_pairwise_hist` (n_bins=256) within reported bounds (f32 ~1e-3, f64 ~1e-9) for n_pairs=1, n_pairs=37, large-N; empty (n_pairs=0) short-circuits with NO launch and NO read-back.
    - The 8-bit kernel is a distinct symbol using direct global atomics always (no shared-mem pre-reduce); the MVP body mirrors the non-binary kernel with bits=8.
    - The launch arm reuses the FROZEN 4-channel layout, the backend-dispatched channel, and the full guard block (LengthMismatch / OutOfRange / overflow / empty short-circuit).
  </behavior>
  <action>Add `pairwise_hist_8bit_atomics_kernel<F: Float>` to `kernels.rs` as a SEPARATE `#[cube(launch)]` fn (D-7.4-02 structural parity — keep it distinct even though the MVP body equals the non-binary kernel with `bits = 8`). Bind the SAME inputs as the Plan A kernel (`pair_i`, `pair_j`, `pair_weight`, `cindex`, `bin_sums: &Array<Atomic<F>>`, `n_features: u32`, `n_objects: u32`, `#[comptime] one_hot: bool`); fix `n_bins = comptime!(256usize)`; grid-stride over PAIRS; per pair read `oi`/`oj`/`w`, per feature read `bin1`/`bin2` (cindex stride over OBJECTS — Pitfall 3), compute `hist_id` from the comptime-pruned Compare predicate (same as Plan A), write `bin_sums[(feature * 256usize + bin) * 4usize + hist_id].fetch_add(w)` — direct global atomics ALWAYS (no shared-mem; document upstream's per-thread CachedBins cache as the perf follow-up — RESEARCH Pattern 3). All index arithmetic in `usize`; NO warp/tile literal (Pitfall 4). Add `launch_pairwise_hist_8bit_handle(pair_i, pair_j, pair_weight, cindex, n_objects, n_features, one_hot) -> CbResult<Handle>` + its private `_into` to `gpu_runtime.rs` cloning the Plan A `launch_pairwise_hist_into` with `n_bins = 256` and dispatching `pairwise_hist_8bit_atomics_kernel::launch` (reuse `pair_hist_binsums_len`, the guard block, the backend-dispatched channel, the empty short-circuit, the no-readback seam — all unchanged from Plan A). Add the `eightbit_atomics` test to `kernels/pairwise_hist.rs` cloning `nonbinary_bits` with `n_bins=256`, calling `launch_pairwise_hist_8bit_handle`, asserting against `host_reference_pairwise_hist(..., n_bins=256, ...)`, REPORT divergence, edge cases empty/n_pairs=1/n_pairs=37/large-N. Test code MAY `.unwrap()`/index; production may not. On ANY CubeCL build error, load `cubecl_error_guideline.md` BEFORE fixing.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::eightbit_atomics -- --nocapture</automated>
  </verify>
  <done>`eightbit_atomics` passes on rocm (n_bins=256), REPORTING divergence; `pairwise_hist_8bit_atomics_kernel` is a distinct `#[cube]` symbol with a distinct launch arm; the FROZEN 4-channel layout is reused (`* 4`); empty fixture does not launch or read back; no warp/tile literal.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-backend build gate + SC-4 boundary assertion</name>
  <files>crates/cb-backend/src/kernels/pairwise_hist.rs</files>
  <read_first>
    - .planning/phases/07.4-pairwise-histogram-family/07.4-VALIDATION.md § Sampling Rate ("After every plan wave")
    - crates/cb-backend/src/kernels/pairwise_hist.rs (the Plan A SelectedRuntime cross-backend property)
  </read_first>
  <action>Confirm the `eightbit_atomics` test builds + runs under `wgpu` on host (f32 channel) and compiles under `cuda` (no new code unless a backend gap surfaces — fix inside `cb-backend` only). Run the SC-4 boundary gate: `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `cb-compute`/`cb-core`/`cb-model` byte-unchanged. Grep the new kernel for warp/tile literals and `* 2` binSums indexing and confirm none.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features wgpu && cargo test -p cb-backend --no-default-features --features wgpu kernels::pairwise_hist::eightbit_atomics -- --nocapture && cargo build -p cb-backend --no-default-features --features cuda && test "$(cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl)" -eq 0</automated>
  </verify>
  <done>wgpu builds and the 8-bit oracle runs on host; cuda compiles; `cb-compute` cubecl count is 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged; no warp/tile literal or `* 2` indexing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch helper → device kernel | In-process buffer lengths/shapes cross here; no external/untrusted input |
| device kernel → device binSums buffer | 256-bin × 4-channel index derived from `cindex[feature*n_objects + oi/oj]` |

Pure in-process numeric GPU compute kernel — no network/auth/secrets/file/untrusted input. Threat surface is buffer-bounds + integer-overflow correctness (the 256-bin line makes `n_features*256*4` overflow checking load-bearing).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-07 | Tampering / DoS (UB) | mismatched pair/weight/cindex lengths or OOB pair/bin index → OOB device read/write at 256-bin scale | mitigate | Reuse the Plan A guard block: `CbError::LengthMismatch`, value-range `pair_i/j < n_objects` + `cindex < 256`, `checked_mul` on `n_features*n_objects` and `n_features*256*4` BEFORE launch; device `p < n_pairs` idle-guard |
| T-07.4-08 | DoS | `read_one` on empty/zero-length HIP buffer | mitigate | Empty-input short-circuit to `client.empty(0)`, NO launch / NO read-back (Pitfall 5) |
| T-07.4-09 | Spoofing (of a correct result) | silent all-zero histogram on wgpu (f64 atomics absent) | mitigate | Reuse the backend-dispatched f32 channel on wgpu; oracle REPORTS divergence and catches a zero buffer |
| T-07.4-10 | DoS | `unwrap`/`panic`/indexing in the production 8-bit launch helper | mitigate | D-13 lints ban them in production `gpu_runtime.rs`; tests use the `#[cfg(test)]` allow |
| T-07.4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); only a `#[cube]` kernel + launch arm + a test added to existing `cb-backend` deps |

**Block-on-high:** none expected (in-process compute kernel, no external input surface).
</threat_model>

<verification>
- rocm: `kernels::pairwise_hist::eightbit_atomics` green on gfx1100 (n_bins=256), REPORTING divergence.
- wgpu: build + run on host (f32 channel). cuda: compile-only.
- SC-4: `cargo tree` cubecl count 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged.
- D-09 / Pitfall 2: no warp/tile literal; binSums indexing `* 4` (reuses FROZEN layout).
</verification>

<success_criteria>
- 8-bit-atomics pairwise fill runs device-resident on rocm and matches the ordered host reference within reported tolerance across empty/n_pairs=1/n_pairs=37/large-N (SC-1).
- The 8-bit family is a distinct kernel + launch arm (structural parity, D-7.4-02), reusing the FROZEN 4-channel layout + seam from Plan A.
- In-kernel atomic merge (D-03); no warp/tile literal; generics-float (SC-3).
- cb-compute cubecl-free; cb-core/cb-model byte-unchanged (SC-4).
</success_criteria>

<output>
Create `.planning/phases/07.4-pairwise-histogram-family/07.4-02-SUMMARY.md` when done.
</output>
