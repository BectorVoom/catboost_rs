---
phase: 07.4-pairwise-histogram-family
plan: 04
type: execute
wave: 4
depends_on: ["07.4-03"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/pairwise_hist.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "The binary (1-bit, 2-bin) pairwise histogram family (upstream pairwise_hist_binary) runs device-resident on rocm and matches the ordered host-reference 4-channel histogram within the REPORTED divergence across empty/n_pairs=1/n_pairs=37/large-N fixtures (D-7.4-02, SC-1)."
    - "The binary family is a SEPARATE #[cube] kernel symbol + launch arm with a FIXED 2-bin line (bit-masked bins); it takes NO one-hot overlay (upstream has no binary_one_hot.cu)."
    - "The binary family reuses the FROZEN 4-channel weight-only binSums layout from Plan A unchanged (histLineSize = 4 * totalBinFeatures, (feature*2+bin)*4 + histId); the 2x2 (i leq/ge)x(j leq/ge) channel decomposition matches upstream pairwise_hist_binary.cu (the (invBin1&invBin2)|... | histId selection)."
    - "cb-compute stays cubecl-free and cb-core/cb-model byte-unchanged; all new code lives in cb-backend (SC-4 / D-7.4-08)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "pairwise_hist_binary_kernel<F> (#[cube(launch)], fixed 2-bin, bit-masked bins, no one-hot)"
      contains: "pairwise_hist_binary_kernel"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_pairwise_hist_binary_handle + _into arm (2-bin, reuses the FROZEN 4-channel layout + guard block + backend-dispatched channel)"
      contains: "pairwise_hist_binary_kernel::launch"
    - path: "crates/cb-backend/src/kernels/pairwise_hist.rs"
      provides: "binary test over SelectedRuntime (edge cases + REPORT divergence against host_reference_pairwise_hist with n_bins=2)"
      contains: "fn binary"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_pairwise_hist_binary_handle"
      to: "crates/cb-backend/src/kernels.rs::pairwise_hist_binary_kernel"
      via: "cube launch, fixed 2-bin"
      pattern: "pairwise_hist_binary_kernel::launch"
    - from: "crates/cb-backend/src/kernels/pairwise_hist.rs::binary"
      to: "crates/cb-backend/src/gpu_runtime.rs::launch_pairwise_hist_binary_handle"
      via: "pair handles in -> 4-channel binSums handle out -> read back ONCE"
      pattern: "launch_pairwise_hist_binary"
---

<objective>
Add the **binary** (1-bit, 2-bin) pairwise histogram family — upstream's `ComputePairwiseHistogramBinary` (`pairwise_hist_binary.cu`) — as a separate `#[cube]` kernel symbol + launch arm with a FIXED 2-bin line (bit-masked bins), reusing the FROZEN 4-channel layout + seam + self-oracle harness from Plan A. The binary path's 2×2 `(i leq/ge)×(j leq/ge)` channel decomposition (`(invBin1&invBin2)|...` in upstream) maps to the SAME `Compare(bin1,bin2,flag)->histId` predicate the other families use. Self-oracle on rocm with `n_bins = 2`.

Purpose: Closes the binary slice of SC-1's variant set — the last bit-width family. Binary takes NO one-hot overlay upstream (no `pairwise_hist_binary_one_hot.cu`). The MVP body mirrors the non-binary kernel with `n_bins` fixed at 2.
Output: `pairwise_hist_binary_kernel`, `launch_pairwise_hist_binary_handle` + its `_into` arm, the `binary` self-oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md

@.planning/phases/07.4-pairwise-histogram-family/07.4-CONTEXT.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-RESEARCH.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-PATTERNS.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-VALIDATION.md

# The seam this plan extends (Plan A froze the 4-channel layout; B/C added 8-bit/half-byte)
@.planning/phases/07.4-pairwise-histogram-family/07.4-01-SUMMARY.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-03-SUMMARY.md

# Upstream parity target (the binary family — D-01; the 2x2 channel-decomposition proof)
@catboost-master/catboost/cuda/methods/kernel/pairwise_hist_binary.cu

# In-repo analogs (Plan A's pairwise kernel + the shipped 7.3 binary precedent)
@crates/cb-backend/src/kernels.rs
@crates/cb-backend/src/gpu_runtime.rs
@crates/cb-backend/src/kernels/pairwise_hist.rs

# CubeCL authoring rules (MANDATORY per AGENTS.md)
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (load on ANY CubeCL build error before fixing)
</context>

## Phase Goal

**As a** Rust developer embedding the GPU pairwise/ranking training path, **I want to** fill a device-resident 4-channel pairwise histogram for binary (1-bit, 2-bin) features on rocm, **so that** the binary variant runs device-resident with verifiable parity against the CPU pairwise path.

## Artifacts this phase produces (new symbols — exclude from drift verification)

This plan creates: `pairwise_hist_binary_kernel` (`#[cube(launch)]`, fixed 2-bin, bit-masked, no one-hot); `launch_pairwise_hist_binary_handle` + its private `_into` arm in `gpu_runtime.rs`; the `binary` test in `kernels/pairwise_hist.rs`. It REUSES the Plan A `PAIR_HIST_CHANNELS`, `pair_hist_binsums_len`, `read_pair_binsums_f64`, `host_reference_pairwise_hist`, `make_pair_fixture`, and the backend-dispatched channel.

## Acceptance Criteria
- [ ] `cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::binary -- --nocapture` passes on gfx1100 (n_bins=2) and prints the REPORTED max abs/rel divergence.
- [ ] `pairwise_hist_binary_kernel` is a SEPARATE `#[cube]` symbol with a SEPARATE launch arm; the 2-bin line is fixed (a bin count, NOT a warp literal), no runtime `bits` branch, no one-hot arg.
- [ ] `cargo build -p cb-backend --no-default-features --features wgpu` + run `binary` on host; `cargo build -p cb-backend --no-default-features --features cuda` compile-only.
- [ ] `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `crates/cb-compute`, `crates/cb-core`, `crates/cb-model` byte-unchanged.
- [ ] No warp/tile literal in any stride/slice offset; kernel is `<F: Float>`; binSums indexing is `* 4` (reuses the FROZEN layout — never `* 2`).

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The binary distinct #[cube] family + its launch arm + the binary self-oracle</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs, crates/cb-backend/src/kernels/pairwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs lines 880-950 (the shipped 7.3 `pointwise_hist2_binary_kernel` — the fixed 2-bin line + bit-mask `& 1` precedent; ADAPT to pairs/4-channel), the Plan A `pairwise_hist_nonbinary_kernel` (the per-pair/two-bin/4-channel structure to mirror with fixed 2-bin)
    - crates/cb-backend/src/gpu_runtime.rs (the Plan A `launch_pairwise_hist_handle`/`_into` + `pair_hist_binsums_len` + the backend-dispatched channel — clone an arm with n_bins=2, no one_hot)
    - crates/cb-backend/src/kernels/pairwise_hist.rs (the Plan A `host_reference_pairwise_hist`, `make_pair_fixture`, `max_divergence`, `read_pair_handle`, `nonbinary_bits` body — clone for `binary` with n_bins=2)
    - catboost-master/catboost/cuda/methods/kernel/pairwise_hist_binary.cu (`TPairBinaryHist` + the `(invBin1&invBin2)|(invBin1&bin2)<<8|...` 2x2 channel decomposition + the `4 * bfStart + 4*fold + histId` merge — confirms the 4-channel histId selection equals the Compare predicate)
    - .planning/phases/07.4-pairwise-histogram-family/07.4-PATTERNS.md § "Half-byte / binary families" + § "the 4-channel layout"
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (before writing kernel code); load cubecl_error_guideline.md on ANY build error before fixing
  </read_first>
  <behavior>
    - `binary` oracle test goes GREEN with n_bins=2: device histogram matches `host_reference_pairwise_hist` (n_bins=2) within reported bounds for n_pairs=1, n_pairs=37, large-N; empty short-circuits with NO launch and NO read-back.
    - The binary kernel is a distinct symbol with a FIXED 2-bin line (bit-masked bins), NO one-hot arg; the 2x2 channel decomposition equals the `Compare(bin1,bin2,flag)->histId` predicate the other families use.
    - The launch arm reuses the FROZEN 4-channel layout, the backend-dispatched channel, and the full guard block.
  </behavior>
  <action>Add `pairwise_hist_binary_kernel<F: Float>` to `kernels.rs` as a SEPARATE `#[cube(launch)]` fn (D-7.4-02). Bind `pair_i`, `pair_j`, `pair_weight`, `cindex`, `bin_sums: &Array<Atomic<F>>`, `n_features: u32`, `n_objects: u32` (NO one_hot arg — binary takes no one-hot overlay upstream). Fix `n_bins = comptime!(2usize)` and mask read bins to the bit (`& comptime!(1u32)` — the shipped binary precedent). Grid-stride over PAIRS; per feature read `bin1`/`bin2` (cindex stride over OBJECTS — Pitfall 3), compute `hist_id` from the non-one-hot Compare predicate `(bin1 >= bin2) == flag` (equivalent to upstream's `(invBin1&invBin2)|...` 2x2 OR decomposition — reduce that bit math to the clean per-pair histId; the self-oracle validates it), write `bin_sums[(feature * 2usize + bin) * 4usize + hist_id].fetch_add(w)` (4 channels — Pitfall 2). All index arithmetic in `usize`; NO warp/tile literal (Pitfall 4). Add `launch_pairwise_hist_binary_handle(pair_i, pair_j, pair_weight, cindex, n_objects, n_features) -> CbResult<Handle>` + its private `_into` to `gpu_runtime.rs` cloning the Plan A `launch_pairwise_hist_into` with `n_bins = 2` and dispatching `pairwise_hist_binary_kernel::launch` (reuse `pair_hist_binsums_len`, the guard block, the backend-dispatched channel, the empty short-circuit, the no-readback seam). Add the `binary` test to `kernels/pairwise_hist.rs` cloning `nonbinary_bits` with `n_bins=2`, calling `launch_pairwise_hist_binary_handle`, asserting against `host_reference_pairwise_hist(..., n_bins=2, one_hot=false)`, REPORT divergence, edge cases empty/n_pairs=1/n_pairs=37/large-N. Test code MAY `.unwrap()`/index; production may not. On ANY CubeCL build error, load `cubecl_error_guideline.md` BEFORE fixing.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::binary -- --nocapture</automated>
  </verify>
  <done>`binary` passes on rocm (n_bins=2), REPORTING divergence; `pairwise_hist_binary_kernel` is a distinct `#[cube]` symbol with a distinct launch arm and a fixed 2-bin line (no runtime bits, no one-hot); the FROZEN 4-channel layout is reused (`* 4`); empty fixture does not launch or read back; no warp/tile literal.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-backend build gate + SC-4 boundary assertion + all-families wave check</name>
  <files>crates/cb-backend/src/kernels/pairwise_hist.rs</files>
  <read_first>
    - .planning/phases/07.4-pairwise-histogram-family/07.4-VALIDATION.md § Sampling Rate ("After every plan wave")
    - crates/cb-backend/src/kernels/pairwise_hist.rs (the SelectedRuntime cross-backend property + the now-complete bit-width family set)
  </read_first>
  <action>Confirm the `binary` test builds + runs under `wgpu` on host (f32 channel) and compiles under `cuda` (fix any gap inside `cb-backend` only). Run the full pairwise suite (`kernels::pairwise_hist`) on rocm to confirm all four bit-width families (nonbinary_bits, eightbit_atomics, half_byte, binary) stay green together. Run the SC-4 boundary gate: `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `cb-compute`/`cb-core`/`cb-model` byte-unchanged. Grep all four kernels for warp/tile literals and `* 2` binSums indexing and confirm none.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features wgpu && cargo test -p cb-backend --no-default-features --features wgpu kernels::pairwise_hist::binary -- --nocapture && cargo build -p cb-backend --no-default-features --features cuda && cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist -- --nocapture && test "$(cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl)" -eq 0</automated>
  </verify>
  <done>wgpu builds and the binary oracle runs on host; cuda compiles; the full rocm pairwise suite (all four bit-width families) is green; `cb-compute` cubecl count is 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged; no warp/tile literal or `* 2` indexing in any of the four kernels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch helper → device kernel | In-process buffer lengths/shapes cross here; no external/untrusted input |
| device kernel → device binSums buffer | 2-bin × 4-channel index derived from bit-masked `cindex[feature*n_objects + oi/oj]` |

Pure in-process numeric GPU compute kernel — no network/auth/secrets/file/untrusted input. Threat surface is buffer-bounds correctness + the bit mask keeping bins in `0..2`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-15 | Tampering / DoS (UB) | mismatched lengths or OOB pair index → OOB device read/write | mitigate | Reuse the Plan A guard block: `CbError::LengthMismatch`, value-range `pair_i/j < n_objects` + `cindex < 2`, `checked_mul` overflow BEFORE launch; device `p < n_pairs` idle-guard; bit mask `& 1` keeps the bin in range |
| T-07.4-16 | DoS | `read_one` on empty/zero-length HIP buffer | mitigate | Empty-input short-circuit to `client.empty(0)`, NO launch / NO read-back (Pitfall 5) |
| T-07.4-17 | Spoofing (of a correct result) | silent all-zero histogram on wgpu (f64 atomics absent) | mitigate | Reuse the backend-dispatched f32 channel on wgpu; oracle REPORTS divergence |
| T-07.4-18 | DoS | `unwrap`/`panic`/indexing in the production binary launch helper | mitigate | D-13 lints ban them in production `gpu_runtime.rs`; tests use the `#[cfg(test)]` allow |
| T-07.4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); only a `#[cube]` kernel + launch arm + a test added to existing `cb-backend` deps |

**Block-on-high:** none expected (in-process compute kernel, no external input surface).
</threat_model>

<verification>
- rocm: `kernels::pairwise_hist::binary` green on gfx1100 (n_bins=2); the full `kernels::pairwise_hist` suite green (all four bit-width families).
- wgpu: build + run on host (f32 channel). cuda: compile-only.
- SC-4: `cargo tree` cubecl count 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged.
- D-09 / Pitfall 2: no warp/tile literal; binSums indexing `* 4` (reuses FROZEN layout).
</verification>

<success_criteria>
- Binary pairwise fill runs device-resident on rocm and matches the ordered host reference within reported tolerance across empty/n_pairs=1/n_pairs=37/large-N (SC-1).
- The binary family is a distinct kernel + launch arm with a fixed 2-bin line (no one-hot, structural parity, D-7.4-02), reusing the FROZEN 4-channel layout + seam; the four bit-width families run together green.
- In-kernel atomic merge (D-03); no warp/tile literal; generics-float (SC-3).
- cb-compute cubecl-free; cb-core/cb-model byte-unchanged (SC-4).
</success_criteria>

<output>
Create `.planning/phases/07.4-pairwise-histogram-family/07.4-04-SUMMARY.md` when done.
</output>
