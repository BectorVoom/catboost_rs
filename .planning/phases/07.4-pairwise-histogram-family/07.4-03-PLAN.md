---
phase: 07.4-pairwise-histogram-family
plan: 03
type: execute
wave: 3
depends_on: ["07.4-02"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/pairwise_hist.rs
autonomous: true
requirements: [GPU-01]
must_haves:
  truths:
    - "The half-byte (4-bit, 16-bin) pairwise histogram family (upstream pairwise_hist_half_byte) runs device-resident on rocm and matches the ordered host-reference 4-channel histogram within the REPORTED divergence across empty/n_pairs=1/n_pairs=37/large-N fixtures (D-7.4-02, SC-1)."
    - "The half-byte family is a SEPARATE #[cube] kernel symbol + launch arm with a FIXED 16-bin line (the comptime HALF_BYTE_BINS precedent, NOT a runtime bits arg); it takes NO one-hot overlay (upstream has no half_byte_one_hot.cu)."
    - "The half-byte family reuses the FROZEN 4-channel weight-only binSums layout from Plan A unchanged (histLineSize = 4 * totalBinFeatures, (feature*16+bin)*4 + histId)."
    - "cb-compute stays cubecl-free and cb-core/cb-model byte-unchanged; all new code lives in cb-backend (SC-4 / D-7.4-08)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "pairwise_hist_half_byte_kernel<F> (#[cube(launch)], fixed 16-bin via comptime HALF_BYTE_BINS, nibble-masked bins, no one-hot)"
      contains: "pairwise_hist_half_byte_kernel"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_pairwise_hist_half_byte_handle + _into arm (16-bin, reuses the FROZEN 4-channel layout + guard block + backend-dispatched channel)"
      contains: "pairwise_hist_half_byte_kernel::launch"
    - path: "crates/cb-backend/src/kernels/pairwise_hist.rs"
      provides: "half_byte test over SelectedRuntime (edge cases + REPORT divergence against host_reference_pairwise_hist with n_bins=16)"
      contains: "fn half_byte"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_pairwise_hist_half_byte_handle"
      to: "crates/cb-backend/src/kernels.rs::pairwise_hist_half_byte_kernel"
      via: "cube launch, fixed 16-bin"
      pattern: "pairwise_hist_half_byte_kernel::launch"
    - from: "crates/cb-backend/src/kernels/pairwise_hist.rs::half_byte"
      to: "crates/cb-backend/src/gpu_runtime.rs::launch_pairwise_hist_half_byte_handle"
      via: "pair handles in -> 4-channel binSums handle out -> read back ONCE"
      pattern: "launch_pairwise_hist_half_byte"
---

<objective>
Add the **half-byte** (4-bit, 16-bin) pairwise histogram family — upstream's `ComputePairwiseHistogramHalfByte` (`pairwise_hist_half_byte.cu`) — as a separate `#[cube]` kernel symbol + launch arm with a FIXED 16-bin line (the comptime `HALF_BYTE_BINS` precedent from the shipped 7.3 half-byte kernel, NOT a runtime `bits` arg), reusing the FROZEN 4-channel layout + seam + self-oracle harness from Plan A. Self-oracle on rocm with `n_bins = 16`.

Purpose: Closes the half-byte slice of SC-1's variant set. Half-byte is structurally distinct (fixed 16-bin line, nibble-masked bins) and takes NO one-hot overlay upstream (no `pairwise_hist_half_byte_one_hot.cu` exists). The MVP body mirrors the non-binary kernel structure with `n_bins` fixed at the comptime `HALF_BYTE_BINS = 16`.
Output: `pairwise_hist_half_byte_kernel`, `launch_pairwise_hist_half_byte_handle` + its `_into` arm, the `half_byte` self-oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md

@.planning/phases/07.4-pairwise-histogram-family/07.4-CONTEXT.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-RESEARCH.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-PATTERNS.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-VALIDATION.md

# The seam this plan extends (Plan A froze the 4-channel layout; B added 8-bit)
@.planning/phases/07.4-pairwise-histogram-family/07.4-01-SUMMARY.md
@.planning/phases/07.4-pairwise-histogram-family/07.4-02-SUMMARY.md

# Upstream parity target (the half-byte family — D-01)
@catboost-master/catboost/cuda/methods/kernel/pairwise_hist_half_byte.cu

# In-repo analogs (Plan A's pairwise kernel + the shipped 7.3 half-byte precedent + HALF_BYTE_BINS)
@crates/cb-backend/src/kernels.rs
@crates/cb-backend/src/gpu_runtime.rs
@crates/cb-backend/src/kernels/pairwise_hist.rs

# CubeCL authoring rules (MANDATORY per AGENTS.md)
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md
# /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (load on ANY CubeCL build error before fixing)
</context>

## Phase Goal

**As a** Rust developer embedding the GPU pairwise/ranking training path, **I want to** fill a device-resident 4-channel pairwise histogram for half-byte (4-bit, 16-bin) features on rocm, **so that** the half-byte variant runs device-resident with verifiable parity against the CPU pairwise path.

## Artifacts this phase produces (new symbols — exclude from drift verification)

This plan creates: `pairwise_hist_half_byte_kernel` (`#[cube(launch)]`, fixed 16-bin via the comptime `HALF_BYTE_BINS`, no one-hot); `launch_pairwise_hist_half_byte_handle` + its private `_into` arm in `gpu_runtime.rs`; the `half_byte` test in `kernels/pairwise_hist.rs`. It REUSES the Plan A `PAIR_HIST_CHANNELS`, `pair_hist_binsums_len`, `read_pair_binsums_f64`, `host_reference_pairwise_hist`, `make_pair_fixture`, the backend-dispatched channel, and the existing `HALF_BYTE_BINS` const.

## Acceptance Criteria
- [ ] `cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::half_byte -- --nocapture` passes on gfx1100 (n_bins=16) and prints the REPORTED max abs/rel divergence.
- [ ] `pairwise_hist_half_byte_kernel` is a SEPARATE `#[cube]` symbol with a SEPARATE launch arm; the 16-bin line is the comptime `HALF_BYTE_BINS` (a bin count, NOT a warp literal), no runtime `bits` branch, no one-hot arg.
- [ ] `cargo build -p cb-backend --no-default-features --features wgpu` + run `half_byte` on host; `cargo build -p cb-backend --no-default-features --features cuda` compile-only.
- [ ] `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `crates/cb-compute`, `crates/cb-core`, `crates/cb-model` byte-unchanged.
- [ ] No warp/tile literal in any stride/slice offset; kernel is `<F: Float>`; binSums indexing is `* 4` (reuses the FROZEN layout — never `* 2`).

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The half-byte distinct #[cube] family + its launch arm + the half_byte self-oracle</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs, crates/cb-backend/src/kernels/pairwise_hist.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs line 689 (`HALF_BYTE_BINS = 16` const — REUSE it), lines 758-810 (the shipped 7.3 `pointwise_hist2_half_byte_kernel` — the comptime `HALF_BYTE_BINS` line size + nibble-mask `& 15` precedent; ADAPT to pairs/4-channel), the Plan A `pairwise_hist_nonbinary_kernel` (the per-pair/two-bin/4-channel structure to mirror with fixed 16-bin)
    - crates/cb-backend/src/gpu_runtime.rs (the Plan A `launch_pairwise_hist_handle`/`_into` + `pair_hist_binsums_len` + the backend-dispatched channel — clone an arm with n_bins=16, no one_hot)
    - crates/cb-backend/src/kernels/pairwise_hist.rs (the Plan A `host_reference_pairwise_hist`, `make_pair_fixture`, `max_divergence`, `read_pair_handle`, `nonbinary_bits` body — clone for `half_byte` with n_bins=16)
    - catboost-master/catboost/cuda/methods/kernel/pairwise_hist_half_byte.cu (`TPairHist` 4-bit; the `4 * (16*fid+fold) + histId` merge — confirms 4-channel, 16-bin)
    - .planning/phases/07.4-pairwise-histogram-family/07.4-PATTERNS.md § "Half-byte / binary families"
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (before writing kernel code); load cubecl_error_guideline.md on ANY build error before fixing
  </read_first>
  <behavior>
    - `half_byte` oracle test goes GREEN with n_bins=16: device histogram matches `host_reference_pairwise_hist` (n_bins=16) within reported bounds for n_pairs=1, n_pairs=37, large-N; empty short-circuits with NO launch and NO read-back.
    - The half-byte kernel is a distinct symbol with a FIXED 16-bin line (the comptime `HALF_BYTE_BINS`), nibble-masked bins, NO one-hot arg.
    - The launch arm reuses the FROZEN 4-channel layout, the backend-dispatched channel, and the full guard block.
  </behavior>
  <action>Add `pairwise_hist_half_byte_kernel<F: Float>` to `kernels.rs` as a SEPARATE `#[cube(launch)]` fn (D-7.4-02). Bind `pair_i`, `pair_j`, `pair_weight`, `cindex`, `bin_sums: &Array<Atomic<F>>`, `n_features: u32`, `n_objects: u32` (NO one_hot arg — half-byte takes no one-hot overlay upstream). Fix `n_bins = comptime!(HALF_BYTE_BINS)` (16; REUSE the existing const at line 689) and mask read bins to the nibble (`& comptime!((HALF_BYTE_BINS as u32) - 1)` — the shipped half-byte precedent). Grid-stride over PAIRS; per feature read `bin1`/`bin2` (cindex stride over OBJECTS — Pitfall 3), compute `hist_id` from the non-one-hot Compare predicate `(bin1 >= bin2) == flag`, write `bin_sums[(feature * comptime!(HALF_BYTE_BINS) + bin) * 4usize + hist_id].fetch_add(w)` (4 channels — Pitfall 2). All index arithmetic in `usize`; NO warp/tile literal (Pitfall 4). Add `launch_pairwise_hist_half_byte_handle(pair_i, pair_j, pair_weight, cindex, n_objects, n_features) -> CbResult<Handle>` + its private `_into` to `gpu_runtime.rs` cloning the Plan A `launch_pairwise_hist_into` with `n_bins = HALF_BYTE_BINS` (16) and dispatching `pairwise_hist_half_byte_kernel::launch` (reuse `pair_hist_binsums_len`, the guard block, the backend-dispatched channel, the empty short-circuit, the no-readback seam). Add the `half_byte` test to `kernels/pairwise_hist.rs` cloning `nonbinary_bits` with `n_bins=16`, calling `launch_pairwise_hist_half_byte_handle`, asserting against `host_reference_pairwise_hist(..., n_bins=16, one_hot=false)`, REPORT divergence, edge cases empty/n_pairs=1/n_pairs=37/large-N. Test code MAY `.unwrap()`/index; production may not. On ANY CubeCL build error, load `cubecl_error_guideline.md` BEFORE fixing.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm kernels::pairwise_hist::half_byte -- --nocapture</automated>
  </verify>
  <done>`half_byte` passes on rocm (n_bins=16), REPORTING divergence; `pairwise_hist_half_byte_kernel` is a distinct `#[cube]` symbol with a distinct launch arm and a fixed 16-bin comptime line (no runtime bits, no one-hot); the FROZEN 4-channel layout is reused (`* 4`); empty fixture does not launch or read back; no warp/tile literal.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-backend build gate + SC-4 boundary assertion</name>
  <files>crates/cb-backend/src/kernels/pairwise_hist.rs</files>
  <read_first>
    - .planning/phases/07.4-pairwise-histogram-family/07.4-VALIDATION.md § Sampling Rate ("After every plan wave")
    - crates/cb-backend/src/kernels/pairwise_hist.rs (the SelectedRuntime cross-backend property)
  </read_first>
  <action>Confirm the `half_byte` test builds + runs under `wgpu` on host (f32 channel) and compiles under `cuda` (fix any gap inside `cb-backend` only). Run the SC-4 boundary gate: `cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl` == 0 and `git diff --stat` shows `cb-compute`/`cb-core`/`cb-model` byte-unchanged. Grep the new kernel for warp/tile literals and `* 2` binSums indexing and confirm none.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features wgpu && cargo test -p cb-backend --no-default-features --features wgpu kernels::pairwise_hist::half_byte -- --nocapture && cargo build -p cb-backend --no-default-features --features cuda && test "$(cargo tree -e features -p cb-compute | grep -v '^#' | grep -ci cubecl)" -eq 0</automated>
  </verify>
  <done>wgpu builds and the half-byte oracle runs on host; cuda compiles; `cb-compute` cubecl count is 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged; no warp/tile literal or `* 2` indexing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch helper → device kernel | In-process buffer lengths/shapes cross here; no external/untrusted input |
| device kernel → device binSums buffer | 16-bin × 4-channel index derived from nibble-masked `cindex[feature*n_objects + oi/oj]` |

Pure in-process numeric GPU compute kernel — no network/auth/secrets/file/untrusted input. Threat surface is buffer-bounds correctness + the nibble mask keeping bins in `0..16`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.4-11 | Tampering / DoS (UB) | mismatched lengths or OOB pair index → OOB device read/write | mitigate | Reuse the Plan A guard block: `CbError::LengthMismatch`, value-range `pair_i/j < n_objects` + `cindex < 16`, `checked_mul` overflow BEFORE launch; device `p < n_pairs` idle-guard; nibble mask `& 15` keeps the bin in range |
| T-07.4-12 | DoS | `read_one` on empty/zero-length HIP buffer | mitigate | Empty-input short-circuit to `client.empty(0)`, NO launch / NO read-back (Pitfall 5) |
| T-07.4-13 | Spoofing (of a correct result) | silent all-zero histogram on wgpu (f64 atomics absent) | mitigate | Reuse the backend-dispatched f32 channel on wgpu; oracle REPORTS divergence |
| T-07.4-14 | DoS | `unwrap`/`panic`/indexing in the production half-byte launch helper | mitigate | D-13 lints ban them in production `gpu_runtime.rs`; tests use the `#[cfg(test)]` allow |
| T-07.4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); only a `#[cube]` kernel + launch arm + a test added to existing `cb-backend` deps |

**Block-on-high:** none expected (in-process compute kernel, no external input surface).
</threat_model>

<verification>
- rocm: `kernels::pairwise_hist::half_byte` green on gfx1100 (n_bins=16), REPORTING divergence.
- wgpu: build + run on host (f32 channel). cuda: compile-only.
- SC-4: `cargo tree` cubecl count 0; `cb-compute`/`cb-core`/`cb-model` byte-unchanged.
- D-09 / Pitfall 2: no warp/tile literal; binSums indexing `* 4` (reuses FROZEN layout).
</verification>

<success_criteria>
- Half-byte pairwise fill runs device-resident on rocm and matches the ordered host reference within reported tolerance across empty/n_pairs=1/n_pairs=37/large-N (SC-1).
- The half-byte family is a distinct kernel + launch arm with a fixed 16-bin comptime line (no one-hot, structural parity, D-7.4-02), reusing the FROZEN 4-channel layout + seam.
- In-kernel atomic merge (D-03); no warp/tile literal; generics-float (SC-3).
- cb-compute cubecl-free; cb-core/cb-model byte-unchanged (SC-4).
</success_criteria>

<output>
Create `.planning/phases/07.4-pairwise-histogram-family/07.4-03-SUMMARY.md` when done.
</output>
