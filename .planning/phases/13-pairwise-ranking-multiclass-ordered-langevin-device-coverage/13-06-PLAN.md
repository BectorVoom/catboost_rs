---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 06
type: execute
wave: 6
depends_on: [13-05]
files_modified:
  - crates/cb-compute/src/runtime.rs
  - crates/cb-backend/src/kernels/multi_newton.rs
  - crates/cb-backend/src/kernels/multi_newton_test.rs
  - crates/cb-backend/src/kernels/apply_leaf_delta.rs
  - crates/cb-backend/src/kernels.rs
autonomous: true
requirements: [GPUT-12]
must_haves:
  truths:
    - "DeviceGrownTree carries approx_dim and leaf_values is reinterpreted as a leaf_count × approx_dim row-major block; at approx_dim==1 the scalar bytes are IDENTICAL (GPUT-14/D-04 no-regression)"
    - "A K-dim Newton der2 block leaf solve runs on device — COUPLED full-block for MultiClass softmax, DIAGONAL for the separable losses — matching cb_compute::leaf::solve_symmetric_newton at ε=1e-4"
    - "The block apply is approx[d*n + i] += lr * leaf_block[leaf_of[i]*K + d], routed through the existing multi-output CPU apply"
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "DeviceGrownTree.approx_dim + block-leaf reinterpretation (plain host struct, no backend dep)"
    - path: "crates/cb-backend/src/kernels/multi_newton.rs"
      provides: "K-dim block der2 solve (coupled + diagonal), inline Cholesky block"
    - path: "crates/cb-backend/src/kernels/apply_leaf_delta.rs"
      provides: "block-leaf apply (leaf_count × approx_dim)"
  key_links:
    - from: "crates/cb-backend/src/kernels/multi_newton.rs"
      to: "crates/cb-compute/src/leaf.rs::solve_symmetric_newton"
      via: "device K-dim block solve transcribes the CPU packed-hessian Newton solve"
      pattern: "solve_symmetric_newton|cholesky"
    - from: "crates/cb-compute/src/runtime.rs (DeviceGrownTree)"
      to: "multi-output CPU apply (runtime.rs:1145)"
      via: "block leaves route through approx[d*n+i]"
      pattern: "approx_dim|leaf_values"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Land the shared multi-output prerequisite sub-wave (D-03/D-04) BEFORE the multiclass family (Plan 07): extend the `DeviceGrownTree` seam struct to carry `approx_dim` and reinterpret `leaf_values` as a `leaf_count × approx_dim` block, and build the K-dim Newton der2 block-leaf solve kernel (`multi_newton.rs`) — coupled full-block for `MultiClass` softmax, diagonal for the separable losses — extending the Phase-11 Newton der2 machinery. Block leaves route through the existing multi-output CPU apply. The scalar path stays byte-unchanged at `approx_dim==1` (GPUT-14/D-04).

Purpose: One representation extension + one block-solve kernel amortized across all five multi-output losses.
Output: DeviceGrownTree block extension + multi_newton.rs + block apply + self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-compute/src/leaf.rs
</context>

## Artifacts this phase produces (Plan 06)

- `DeviceGrownTree.approx_dim: usize` (NEW field, plain host type) + `leaf_values` reinterpreted as `leaf_count × approx_dim` row-major per leaf (`crates/cb-compute/src/runtime.rs`).
- `crates/cb-backend/src/kernels/multi_newton.rs` (NEW) — K-dim block der2 solve: packed lower-triangular hessian reconstruction, `maxTrace` f32 regularization, `M = -(H − adjustedL2·I)`, inline Cholesky block solve; coupled vs diagonal dispatch.
- Block-leaf apply extension in `crates/cb-backend/src/kernels/apply_leaf_delta.rs` (`approx[d*n+i] += lr * leaf_block[leaf_of[i]*K + d]`).
- `crates/cb-backend/src/kernels/multi_newton_test.rs` (NEW) — self-oracle.

<tasks>

<task type="auto">
  <name>Task 1: DeviceGrownTree block-leaf extension + block apply (byte-unchanged scalar)</name>
  <files>crates/cb-compute/src/runtime.rs, crates/cb-backend/src/kernels/apply_leaf_delta.rs</files>
  <read_first>
    - MODIFY targets: `crates/cb-compute/src/runtime.rs` (`DeviceGrownTree` :927; multi-output CPU apply :1145 `approx[d*n+i]`); `crates/cb-backend/src/kernels/apply_leaf_delta.rs` (scalar gather+FMA, Phase 11).
    - PATTERNS.md analog: "runtime.rs DeviceGrownTree (seam struct, block-leaf extension) — D-03" — the SAME carrier-extension idiom used for `step_nodes`/`node_id_to_leaf_id` (non-sym) and `region_path` (Region): PLAIN HOST fields, empty/1 for paths that don't use them, byte-unchanged for the oblivious/scalar path. Landmine T-10-04: no cubecl/cb-backend type may appear in this struct (feature-unification).
    - GPUT-14/D-04: at `approx_dim==1` the scalar `leaf_values` bytes are IDENTICAL — the extension must not perturb the scalar path.
  </read_first>
  <action>
    Add `pub approx_dim: usize` to `DeviceGrownTree` (plain host type only — no cubecl/cb-backend dep) and document `leaf_values` as a `leaf_count × approx_dim` row-major block (per-leaf contiguous). Set `approx_dim = 1` for every existing scalar construction site so the bytes are unchanged. Extend `apply_leaf_delta.rs` so the block apply is `approx[d*n + i] += lr * leaf_block[leaf_of[i]*K + d]` for `d in 0..K`, routed through the existing multi-output CPU apply layout (`approx[d*n+i]`). Keep the scalar (`K==1`) path on the exact prior code path (no behavioral change). Landmines: no infinity literal in the `#[cube]` body (finite float-min sentinel); bind Handles to their client; no `unwrap`/`expect`/`panic`/indexing in production; source/test separation (no inline test module).
  </action>
  <verify>
    <automated>cargo test -p cb-compute --lib device_grown_tree 2>&1 | tail -20; cargo test -p cb-backend --lib apply_leaf_delta 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `DeviceGrownTree` has `approx_dim: usize` (plain host type); `grep -c 'approx_dim' crates/cb-compute/src/runtime.rs` ≥ 1.
    - No cubecl/cb-backend type appears in the struct: the struct fields remain `Vec`/primitive host types.
    - Scalar no-regression: `cargo test -p cb-train -p cb-compute` green (byte-unchanged at `approx_dim==1`).
    - Block apply indexes `approx[d*n + i]` and `leaf_block[leaf_of[i]*K + d]` (grep apply_leaf_delta.rs).
  </acceptance_criteria>
  <done>DeviceGrownTree carries approx_dim + block leaves; block apply routes through the multi-output CPU apply; scalar path byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: K-dim Newton der2 block solve kernel (coupled + diagonal)</name>
  <files>crates/cb-backend/src/kernels/multi_newton.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test-first (pinned in Task 3): a coupled K=3 softmax block solve == `solve_symmetric_newton` within ε=1e-4; a diagonal K=2 block solve == the CPU diagonal path; a non-PD block returns zeros.
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/multi_newton.rs`; register in `kernels.rs` (mvs idiom :2869-2877).
    - PATTERNS.md analog: "multi_newton.rs (kernel, K-dim block leaf solve) — GPUT-12" — extend `apply_leaf_delta.rs` Newton machinery to K-dim; the packed lower-triangular hessian order `[(0,0),(0,1),…,(0,k-1),(1,1),…]` is load-bearing.
    - CPU oracle to transcribe (block solve): `crates/cb-compute/src/leaf.rs::solve_symmetric_newton` (:201-260) — reconstruct H from packed order, `max_trace = max(scaled_l2 as f32, (-diag) as f32...)`, `adjusted_l2 = max(scaled_l2, max_trace*f32::EPS)`, `M = -(H - adjusted_l2·I)`, solve `M·x = -sum_der` via `cholesky_solve`, `res = -x`.
    - Dense Cholesky building block (transcribe inline, shares Plan-02 numerics): `crates/cb-compute/src/leaf.rs::cholesky_solve` (:282-324) — non-positive pivot → None → zeros.
    - Coupled vs diagonal (Pitfall 3, VERIFIED): full-block coupled solve ONLY for `MultiClass` softmax (off-diagonal `−w·p_k·p_row`); DIAGONAL for `MultiClassOneVsAll`, `MultiCrossEntropy`/multilabel, `MultiRMSE`, `RMSEWithUncertainty`. `multilogit.cu` emits der2 one row at a time — mirror that. Der functions ALREADY exist in `cb-compute` (do not re-derive).
    - D-07: the block solve is f64 non-atomic per-matrix arithmetic (allowed); der/weight SUMS still use the fixed-point reduction.
    - AGENTS.md: read the CubeCL manual before writing; on any build error load the error guideline first.
  </read_first>
  <action>
    Create `multi_newton.rs` with a `#[cube]` K-dim block der2 solve: reconstruct the K×K hessian from the packed lower-triangular order, apply the `maxTrace`/`adjustedL2` f32 regularization exactly as `solve_symmetric_newton`, form `M = -(H - adjustedL2·I)`, and solve `M·x = -sum_der` via the inline Cholesky block (transcribed from `leaf.rs::cholesky_solve`, f64 accumulation, non-positive pivot → zeros), returning `res = -x` as the K-vector leaf block. Dispatch coupled (full K×K solve) for `MultiClass` softmax vs diagonal (per-component 1×1 solve) for the separable losses — a comptime/param flag chooses the path. Batched over leaves, one logical warp/matrix. Landmines: no infinity literal in a kernel body (finite float-min sentinel); bind Handles to their client; `#[cfg(feature="wgpu")]` typed reject; transcribe constants inline (no `cb-train`/`cb-compute` from `#[cube]`); no `unwrap`/`expect`/`panic`/indexing in production.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib multi_newton 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `multi_newton.rs` defines a K-dim block solve with a coupled/diagonal dispatch flag.
    - Packed hessian reconstruction + `maxTrace`/`adjustedL2` regularization match `solve_symmetric_newton` (grep for the packing + `adjusted_l2`/`max_trace` derivations).
    - Non-PD block returns zeros (no NaN/panic).
    - No infinity literal in the `#[cube]` body; `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
  </acceptance_criteria>
  <done>K-dim Newton der2 block solve exists, coupled + diagonal, transcribing the CPU packed-hessian Newton solve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Self-oracle — K-dim block solve vs solve_symmetric_newton</name>
  <files>crates/cb-backend/src/kernels/multi_newton_test.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test 1: coupled K=3 softmax block == `solve_symmetric_newton` ≤1e-4.
    - Test 2: diagonal K=2 block == CPU diagonal path ≤1e-4.
    - Test 3: non-PD block → zeros on both paths; scalar K=1 == the prior scalar Newton result (no-regression).
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/multi_newton_test.rs`; register in `kernels.rs`.
    - PATTERNS.md analog: `apply_leaf_delta.rs` device-vs-serial-CPU harness (`run_apply`/`cpu_apply`, `F64_TOL`, non-wgpu gate); off-device record-only (WR-01).
    - CPU oracle: `crates/cb-compute/src/leaf.rs::solve_symmetric_newton` / `cholesky_solve`.
  </read_first>
  <action>
    Create the dedicated test file with a coupled K=3 softmax hessian fixture, a diagonal K=2 fixture, a non-PD fixture, and a scalar K=1 fixture. Assert the device block solve matches `solve_symmetric_newton` within `F64_TOL` (ε=1e-4) over equal-length buffers when a device backend is active (record-only otherwise), and that K=1 reproduces the prior scalar Newton result. Gate `#![cfg(not(feature = "wgpu"))]`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib multi_newton_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `multi_newton_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - Coupled + diagonal + K=1 fixtures assert ≤1e-4 vs CPU when a device backend is active.
    - Non-PD fixture asserts the zeros fallback on both paths.
  </acceptance_criteria>
  <done>K-dim block solve self-oracled against solve_symmetric_newton (coupled + diagonal + scalar).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | `DeviceGrownTree` is a plain host struct; no backend type crosses into cb-train (T-10-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Availability | non-PD K-dim block pivot | mitigate | non-positive pivot → zeros (matches `cholesky_solve`→None→zeros); no NaN/panic |
| T-13-12 | Denial of Service | block-solve launch grid sizing | mitigate | checked casts + bounded grid; batched over leaves, one warp/matrix |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib multi_newton` + `... multi_newton_test` + `... apply_leaf_delta` green.
- Scalar no-regression: `cargo test -p cb-train -p cb-compute` green (byte-unchanged at approx_dim==1).
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
</verification>

<success_criteria>
DeviceGrownTree carries approx_dim + block leaves (scalar byte-unchanged); the K-dim Newton der2 block solve (coupled + diagonal) self-oracles ≤1e-4 vs solve_symmetric_newton. Amortized by Plan 07. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-06-SUMMARY.md` when done.
</output>
