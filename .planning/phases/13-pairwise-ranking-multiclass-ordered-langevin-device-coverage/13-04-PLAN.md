---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 04
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - crates/cb-backend/src/gpu_runtime/ranking.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-backend/src/gpu_runtime/ranking_det_test.rs
autonomous: true
requirements: [GPUT-22]
must_haves:
  truths:
    - "QueryRMSE and QuerySoftMax train on the device query path (der over resident approx + grouping infra) and match cb_compute::ranking_der at ε=1e-4"
    - "QueryCrossEntropy's per-query bisection/Newton shift search runs on device behind its OWN Ok(None) gate (independently deferrable, Open Q3) and matches the CPU der when covered"
    - "Uncovered ranking configs return Ok(None)→CPU (per-fit all-or-nothing)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/ranking.rs"
      provides: "deterministic query objective device driver over the Plan-03 grouping infra"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "ranking coverage gate (Option<RankingState>) + per-objective Ok(None) arms"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/ranking.rs"
      to: "crates/cb-backend/src/kernels/query_helper.rs"
      via: "consumes group means/bias-removal/masks from the Plan-03 shared infra"
      pattern: "query_helper|group"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Land the three deterministic query/listwise objectives on device (GPUT-22): QueryRMSE, QuerySoftMax, and QueryCrossEntropy — transcribing the existing `cb_compute::ranking_der` der math into the device path over the Plan-03 shared grouping infra. QueryCrossEntropy (the most complex arm: per-query bisection/Newton shift search — RESEARCH Open Q3) lands behind its OWN independent `Ok(None)` gate so QueryRMSE/QuerySoftMax ship even if it over-runs.

Purpose: Deterministic ranking device coverage; the amortization payoff of the Plan-03 infra.
Output: ranking driver + coverage gates + self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-compute/src/ranking_der.rs
</context>

## Artifacts this phase produces (Plan 04)

- `crates/cb-backend/src/gpu_runtime/ranking.rs` (NEW) — deterministic query-objective device driver (QueryRMSE, QuerySoftMax, QueryCrossEntropy) over the Plan-03 grouping infra + resident der seam.
- `Option<RankingState>` coverage-gate field on `GpuTrainSession` with per-objective `Ok(None)` arms (QueryCrossEntropy independently gated).
- `crates/cb-backend/src/gpu_runtime/ranking_det_test.rs` (NEW) — self-oracle.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QueryRMSE + QuerySoftMax device der path + ranking coverage gate</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <behavior>
    - Test-first (pinned in Task 3): QueryRMSE der over a 3-query fixture equals `calc_ders_for_queries` within ε=1e-4; QuerySoftMax likewise.
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/gpu_runtime/ranking.rs`; `crates/cb-backend/src/gpu_runtime/session.rs` (`Option<*State>` gate idiom :361-380, :470-547).
    - PATTERNS.md analog: "query_helper.rs — GPUT-22" (grouping infra consumed here); the resident der seam pattern (`session.rs:335-338` resident `approx_h`/`der1_h`, updated on-device, no n-length readback).
    - Reuse: Plan-03 `query_helper.rs` (group means, RemoveGroupMeans residual feed) + Plan-03 deterministic reduction.
    - CPU der oracle (transcribe into `#[cube]`, do NOT call cb-compute from a kernel): `crates/cb-compute/src/ranking_der.rs::calc_ders_for_queries` (:139) — QueryRMSE = per-query mean-removed residual; QuerySoftMax per-query softmax der.
    - AGENTS.md: read the CubeCL manual before writing the kernel; on any build error load the error guideline first.
  </read_first>
  <action>
    Create `ranking.rs` driving QueryRMSE and QuerySoftMax der computation on device over the Plan-03 grouping infra: QueryRMSE uses `RemoveGroupMeans` on the resident residual then the pointwise der; QuerySoftMax computes the per-query softmax der using the group max/sum from `query_helper`. Add a `ranking: Option<RankingState>` gate field to `GpuTrainSession`, populated in `begin(...)` only for covered query configs (QueryRMSE/QuerySoftMax here), else `Ok(None)`→host. Der/weight sums use the fixed-point reduction; only O(1) group descriptors cross the seam. Transcribe the der constants inline (no `cb-train`/`cb-compute` dep from `#[cube]`); no infinity literal in a kernel body (finite float-min sentinel); bind Handles to their client; `#[cfg(feature="wgpu")]` typed reject; no `unwrap`/`expect`/`panic`/indexing in production.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ranking 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GpuTrainSession` exposes `ranking: Option<RankingState>`; `begin(...)` returns `Ok(None)` for uncovered ranking configs.
    - QueryRMSE + QuerySoftMax der paths exist and consume `query_helper` group outputs (grep ranking.rs for `query_helper`/`group`).
    - `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0; no infinity literal in the kernel body.
  </acceptance_criteria>
  <done>QueryRMSE + QuerySoftMax reach the device ranking path behind the ranking gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QueryCrossEntropy per-query shift search (independent Ok(None) gate, Open Q3)</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <behavior>
    - Test-first (pinned in Task 3): QueryCrossEntropy der after the per-query shift search equals `calc_ders_for_queries` within ε=1e-4 when covered; an uncovered/over-budget config returns `Ok(None)`.
  </behavior>
  <read_first>
    - MODIFY targets: `crates/cb-backend/src/gpu_runtime/ranking.rs`, `session.rs`.
    - PATTERNS.md analog: "Open Q3 (research)" note under query_helper — the `mvs_device.rs` bisection loop (`MVS_BISECTION_ITERS = 100`, monotone bracket) is the structural precedent for the per-query bisection/Newton shift search; if it over-runs, gate QueryCrossEntropy independently (`Ok(None)`).
    - RESEARCH Open Q3: upstream `query_cross_entropy.cu` runs 8 bisection + 5 Newton iterations per query; transcribe against `cb_compute::ranking_der` QueryCrossEntropy der.
    - CPU der oracle: `crates/cb-compute/src/ranking_der.rs` QueryCrossEntropy path.
  </read_first>
  <action>
    Add the QueryCrossEntropy arm to `ranking.rs`: a per-query bisection + Newton shift search (bounded iteration count, monotone bracket — mirror the `mvs_device.rs` bisection structure) producing the per-query shift, then the der. Gate QueryCrossEntropy independently in `session.begin(...)` — if it is uncovered or the shift search is not landed within budget, return `Ok(None)` for THAT objective only, leaving QueryRMSE/QuerySoftMax covered. Bounded iteration count guards the DoS threat (no unbounded loop). Same landmines as Task 1.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ranking 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - QueryCrossEntropy shift search uses a bounded iteration count (grep shows a fixed bisection/Newton iter constant).
    - QueryCrossEntropy is gated independently: its uncovered branch returns `Ok(None)` without disabling QueryRMSE/QuerySoftMax.
    - When covered + a device backend active, its der matches the CPU der ≤1e-4.
  </acceptance_criteria>
  <done>QueryCrossEntropy lands behind its own Ok(None) gate; QueryRMSE/QuerySoftMax unaffected if it defers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Self-oracle — deterministic ranking objectives vs CPU ranking_der</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking_det_test.rs, crates/cb-backend/src/gpu_runtime.rs</files>
  <behavior>
    - Test 1: QueryRMSE der == CPU within ε=1e-4 over a 3-query uneven fixture.
    - Test 2: QuerySoftMax der == CPU within ε=1e-4.
    - Test 3: QueryCrossEntropy der == CPU within ε=1e-4 when covered; `Ok(None)` when gated off.
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/gpu_runtime/ranking_det_test.rs`; register the module in `crates/cb-backend/src/gpu_runtime.rs` (module list) as a `#[cfg(test)] mod`.
    - PATTERNS.md analog: `mvs_device_test.rs` self-oracle harness; non-wgpu gate; off-device record-only (WR-01).
    - CPU oracle: `crates/cb-compute/src/ranking_der.rs::calc_ders_for_queries`.
  </read_first>
  <action>
    Create the dedicated test file with frozen 3-query uneven fixtures. Assert device QueryRMSE/QuerySoftMax/QueryCrossEntropy der match `calc_ders_for_queries` within `F64_TOL` (ε=1e-4) over equal-length buffers when a device backend is active (record-only otherwise). Assert QueryCrossEntropy's gated-off path returns `Ok(None)`. Gate `#![cfg(not(feature = "wgpu"))]`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ranking_det_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ranking_det_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - All three objectives assert ≤1e-4 vs CPU when a device backend is active.
    - QueryCrossEntropy `Ok(None)` gated-off path asserted.
  </acceptance_criteria>
  <done>Deterministic ranking objectives self-oracled at ε=1e-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs; O(1) group descriptors + resident der buffer only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Denial of Service | QueryCrossEntropy per-query shift search loop | mitigate | bounded bisection+Newton iteration count (fixed constant), monotone bracket — no unbounded loop |
| T-13-08 | Tampering | ranking coverage gate | accept | per-objective `Ok(None)` never fabricates; CPU der is the reference |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib ranking` + `... ranking_det_test` green.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
</verification>

<success_criteria>
QueryRMSE/QuerySoftMax/QueryCrossEntropy reach the device ranking path over the shared grouping infra, self-oracled ≤1e-4, each independently gated. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-04-SUMMARY.md` when done.
</output>
