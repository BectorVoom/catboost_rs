---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - crates/cb-backend/src/kernels/cholesky_solve.rs
  - crates/cb-backend/src/kernels/cholesky_solve_test.rs
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime/pairwise.rs
  - crates/cb-backend/src/kernels/score_split.rs
autonomous: false
requirements: [GPUT-21]
must_haves:
  truths:
    - "A batched f64 device Cholesky solver (decomposition + forward/back substitution + ridge + score-from-decomposition) runs on device, one logical warp per matrix, and matches the Rust CPU path at ε=1e-4"
    - "Pairwise LEAF values match cb_train::pairwise_leaves::calculate_pairwise_leaf_values (drop-last-row → (n-1)×(n-1), non_diag_reg=-prior/n, diag_reg=prior*(1-1/n)+l2, make_zero_average) — NOT upstream RegularizeImpl"
    - "Pairwise SPLIT scoring matches cb_compute::calculate_pairwise_score"
    - "If the on-device solve over-runs, the correctness-safe interim (Plan 01 host bounded solve behind Ok(Some)) is retained and the residency gap is logged for Phase 14 — pairwise correctness still lands"
  artifacts:
    - path: "crates/cb-backend/src/kernels/cholesky_solve.rs"
      provides: "batched f64 SPD solve #[cube] kernel (decomp + fwd/back subst + ridge + score), REMOVE_LAST + zero-average"
    - path: "crates/cb-backend/src/kernels/cholesky_solve_test.rs"
      provides: "self-oracle vs cb_compute::pairwise_cholesky_solve / calculate_pairwise_leaf_values / calculate_pairwise_score"
  key_links:
    - from: "crates/cb-backend/src/kernels/score_split.rs"
      to: "crates/cb-backend/src/kernels/cholesky_solve.rs"
      via: "launch_cholesky_solve replaces the 7.5 host calculate_pairwise_score call site (when covered)"
      pattern: "launch_cholesky_solve|cholesky"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Land the one genuinely-new device kernel of the phase and its schedule risk: a batched f64 SPD Cholesky solver (`#[cube]`, one logical warp per matrix) that replaces Phase 7.5's host-side small solve (the documented "RESEARCH Open Q3" at `score_split.rs:853`). It performs decomposition + forward/back substitution + ridge regularization + score-from-decomposition on device, batched over leaves, matching the Rust CPU path at ε=1e-4 (GPUT-21). Because this is the highest-risk item, the plan includes an explicit fallback checkpoint: if the on-device solve over-runs, keep the Plan-01 host bounded solve behind `Ok(Some(...))` (correctness-safe, ε unaffected) and log the residency gap for Phase 14.

Purpose: True host-light pairwise residency; the pivotal new `#[cube]` kernel.
Output: `cholesky_solve.rs` + self-oracle + wired scoring path (or logged residency-gap fallback).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-compute/src/leaf.rs
@crates/cb-train/src/pairwise_leaves.rs
</context>

## Artifacts this phase produces (Plan 02)

- `crates/cb-backend/src/kernels/cholesky_solve.rs` (NEW) — `launch_cholesky_solve(...)` batched f64 SPD solver: in-place lower-triangular decomposition, forward `L·y=b`, back `Lᵀ·x=y`, ridge on the diagonal, `CalcScoresCholesky` score path, `REMOVE_LAST` drop-last-row + zero-average; one parameterized kernel serving both the leaf-value system (`leaf_count`) and the split-score system (`2*PartCount`).
- `crates/cb-backend/src/kernels/cholesky_solve_test.rs` (NEW) — self-oracle.
- Wired `launch_cholesky_solve` call site in `score_split.rs`/`pairwise.rs` (or retained host solve + logged residency gap per the checkpoint).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batched f64 device Cholesky solver kernel</name>
  <files>crates/cb-backend/src/kernels/cholesky_solve.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test-first (behavior pinned in Task 2): a 2×2 closed-form leaf system solves to the CPU `calculate_pairwise_leaf_values` values; a general (n-1)×(n-1) system solves within ε=1e-4; a non-positive-definite pivot returns zeros (no NaN/panic).
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/cholesky_solve.rs`; register in `crates/cb-backend/src/kernels.rs` (mvs registration idiom :2869-2877).
    - PATTERNS.md analog + "No Analog Found": no device SPD-solve precedent — use the `mvs_device.rs` serial `#[cube]` skeleton (per-matrix serial/warp work, f64 accumulation, resident handle out) as the structural analog; transcribe upstream `RunCholeskySolver<128,256,REMOVE_LAST>` shape (NO cuSOLVER dep).
    - CPU oracle to transcribe INLINE (dense solve): `crates/cb-compute/src/leaf.rs::cholesky_solve` (:282-324) — in-place `a=L·Lᵀ`, forward, back; non-positive pivot → None → zeros.
    - CPU oracle for CONSTANTS (ε=1e-4 parity target, Pitfall 2 — NOT upstream RegularizeImpl): `crates/cb-train/src/pairwise_leaves.rs::calculate_pairwise_leaf_values` (:123-179): `cell_prior = 1/system_size`, `non_diag_reg = -pairwise_bucket_weight_prior_reg*cell_prior`, `diag_reg = pairwise_bucket_weight_prior_reg*(1-cell_prior)+l2_diag_reg`; system_size==2 closed form; general: build (n-1)×(n-1) both triangles, solve, `res.push(0.0)`, `make_zero_average` (mean via cb_core::sum_f64).
    - RESEARCH Pattern 1 / D-07: f64 non-atomic per-matrix arithmetic is explicitly allowed here (it is NOT an atomic reduction — the histogram reduction stays fixed-point). Pivot floor 1e-7, tiny-pivot 1e-4 fallback.
    - AGENTS.md: read the CubeCL manual (INDEX.md, and "Switching Between f32 and f64") before writing the kernel; on any CubeCL build error load `cubecl_error_guideline.md` before fixing.
  </read_first>
  <action>
    Create `cholesky_solve.rs` with a `#[cube]` batched SPD solver parameterized by system size and a `REMOVE_LAST` flag (one kernel serves both the leaf-value system of size `leaf_count` and the split-score system of size `2*PartCount`, per Open Q1). Transcribe the `cb-compute::leaf::cholesky_solve` numerics inline: in-place lower-triangular decomposition with a positive-pivot floor, forward substitution `L·y=b`, back substitution `Lᵀ·x=y`; a non-positive pivot yields the zeros fallback (matches the CPU `None`→zeros; prevents NaN). Apply the ridge exactly per `calculate_pairwise_leaf_values`: `non_diag_reg = -prior*cell_prior` off-diagonal, `diag_reg = prior*(1-cell_prior)+l2` on-diagonal, `cell_prior = 1/system_size`; for the leaf-value path drop the last row (`REMOVE_LAST` → `(n-1)×(n-1)`), push a trailing zero, and recenter to zero mean using an in-order fixed sum (mirror `make_zero_average` / `cb_core::sum_f64`). Add a `CalcScoresCholesky` score path (`score = max(β·y − ½·β·A·β)`) for the split-score system. Use f64 accumulation throughout the solve; return a resident handle. Landmines: no infinity literal inside the `#[cube]` body — use the finite float-min sentinel (HIP JIT rejects a negative-infinity literal); bind each `Handle` to its allocating client; guard entry with the `#[cfg(feature = "wgpu")]` typed reject (f64/u64 unsupported on WGSL); no `unwrap`/`expect`/`panic`/indexing in production (workspace lints); transcribe all constants inline (no `cb-train` dep).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib cholesky 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `launch_cholesky_solve(...)` exists, parameterized by system size + `REMOVE_LAST`; the solve uses f64 accumulation (grep shows the f64 generic on the solve path).
    - Ridge constants match `calculate_pairwise_leaf_values`: source contains `non_diag_reg`/`diag_reg`/`cell_prior` derivations equal to `-prior/n`, `prior*(1-1/n)+l2`, `1/system_size`.
    - Non-positive pivot path returns zeros, not NaN/panic (test asserts a finite zero vector).
    - No infinity literal appears in a `#[cube]` body: `grep -v '^\s*//' crates/cb-backend/src/kernels/cholesky_solve.rs | grep -c 'NEG_INF'` returns 0.
    - `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` returns 0.
  </acceptance_criteria>
  <done>A batched f64 SPD Cholesky solver kernel exists with the Rust-CPU-path ridge constants, drop-last-row + zero-average, and a NaN-safe pivot fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-oracle — device Cholesky vs CPU leaf-value + split-score solvers</name>
  <files>crates/cb-backend/src/kernels/cholesky_solve_test.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test 1 (leaf values): device solve of a frozen (n-1)×(n-1) leaf system == `cb_train::pairwise_leaves::calculate_pairwise_leaf_values` (via `cb_compute::pairwise_cholesky_solve`) within ε=1e-4, including the trailing-zero + zero-average steps.
    - Test 2 (split score): device `CalcScoresCholesky` score == `cb_compute::calculate_pairwise_score` within ε=1e-4.
    - Test 3 (degenerate): a non-PD system returns the zeros fallback on both device and CPU.
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/cholesky_solve_test.rs`; register in `kernels.rs`.
    - PATTERNS.md analog: `apply_leaf_delta.rs` + `score_split.rs` pairwise oracle harness (device `run_*` vs `cpu_*`, `F64_TOL`, non-wgpu gate, off-device record-only).
    - CPU oracles: `crates/cb-compute/src/leaf.rs::pairwise_cholesky_solve` (:272), `calculate_pairwise_score`; `crates/cb-train/src/pairwise_leaves.rs::calculate_pairwise_leaf_values`.
  </read_first>
  <action>
    Create the dedicated test file (source/test separation). Freeze small SPD fixtures (a 2×2 closed-form case, a general (n-1)×(n-1) case, a non-PD case). Assert the device solve matches the CPU leaf-value solver and the split-score path within `F64_TOL` (ε=1e-4) over equal-length buffers. Gate `#![cfg(not(feature = "wgpu"))]`; skip the numeric assertion when neither rocm nor cuda is active (record-only, WR-01). Register the module in `kernels.rs`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib cholesky_solve 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cholesky_solve_test.rs` exists as a dedicated file; passes on the CPU/serial path.
    - Leaf-value and split-score fixtures both assert max abs divergence ≤ 1e-4 vs their CPU references when a device backend is active.
    - Degenerate non-PD fixture asserts the zeros fallback on both paths.
  </acceptance_criteria>
  <done>Device Cholesky is self-oracled against both the CPU leaf-value and split-score solvers at ε=1e-4.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: GPUT-21 residency fallback decision (wire device solve vs retain host bounded solve)</name>
  <action>Decide, based on whether Tasks 1–2 pass the self-oracle within the wave's time budget, whether to wire the on-device Cholesky solve into pairwise scoring (replace the 7.5 host call site in score_split.rs) for full residency (D-05), OR retain the Plan-01 host bounded solve behind Ok(Some) and log the residency gap for Phase 14. Correctness (ε=1e-4) is unaffected either way — the host solve is already O(1)-per-level and correctness-safe; this decides only residency purity, so the pairwise family lands regardless. Record the choice and (if retain-host) the residency-gap note in the SUMMARY.</action>
  <what-built>A batched f64 device Cholesky solver (Task 1) with a self-oracle vs the CPU leaf-value + split-score solvers (Task 2). The pairwise per-leaf systems are assembled resident on device (Plan 01). The only open question is whether the SMALL bounded solve runs on-device (full residency) or on the host (Plan 01 path) — correctness is identical.</what-built>
  <how-to-verify>
    1. Confirm Tasks 1–2 self-oracles are green (device solve ≤1e-4 vs `calculate_pairwise_leaf_values` and `calculate_pairwise_score`).
    2. Option wire-device: replace the host `calculate_pairwise_score` call site with `launch_cholesky_solve(...)`; re-run `cargo test -p cb-backend --lib cholesky_solve` and the pairwise scorer oracle.
    3. Option retain-host: keep the Plan-01 host bounded solve behind `Ok(Some(...))`; log the residency gap for Phase 14. Pairwise correctness (GPUT-11/GPUT-21 leaf+score ≤1e-4) still lands.
  </how-to-verify>
  <resume-signal>State the choice: "wire-device" (full residency) or "retain-host" (residency gap logged for Phase 14). Type the choice + rationale.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs only; O(1) BestSplit descriptor + per-leaf/2^depth stats cross per level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Availability | non-PD Cholesky pivot | mitigate | non-positive pivot → zeros fallback (matches `pairwise_cholesky_solve`→None→zeros), pivot floor 1e-7 / tiny-pivot 1e-4 — no NaN/panic |
| T-13-04 | Denial of Service | solver launch grid/buffer sizing | mitigate | checked casts + bounded grid; batched over leaves with per-matrix serial/warp work |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib cholesky` + `... cholesky_solve` green on CPU/serial path.
- Ridge constants + drop-last-row + zero-average verified against `pairwise_leaves.rs`.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
- Checkpoint decision recorded in SUMMARY (wire-device vs retain-host + residency-gap note).
</verification>

<success_criteria>
A batched f64 device Cholesky solver matches the Rust CPU leaf-value and split-score paths at ε=1e-4 with a NaN-safe pivot fallback; the pairwise family lands (wired device solve, or host bounded solve behind `Ok(Some)` with a logged residency gap). Kaggle CUDA ε=1e-4 + BENCH-02 sign-off is Plan 10 (human-gated).
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-02-SUMMARY.md` when done.
</output>
