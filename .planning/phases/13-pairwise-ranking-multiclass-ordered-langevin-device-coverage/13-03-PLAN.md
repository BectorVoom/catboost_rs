---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - crates/cb-backend/src/kernels/query_helper.rs
  - crates/cb-backend/src/kernels/query_helper_test.rs
  - crates/cb-backend/src/kernels.rs
autonomous: true
requirements: [GPUT-22]
must_haves:
  truths:
    - "Shared device query-grouping infra (group ids, group means/max, group-bias removal, sort keys, taken-docs masks) runs on device and matches the CPU ranking_der group reductions at ε=1e-4"
    - "Der/weight group reductions use the fixed-point Atomic<u64> k=30 path (deterministic), not f64 atomic-add"
    - "In-query sort keys reuse the existing segmented_radix_sort (no second sort hand-rolled)"
  artifacts:
    - path: "crates/cb-backend/src/kernels/query_helper.rs"
      provides: "ComputeGroupIds/Means/Max, RemoveGroupMeans, CreateSortKeys, FillTakenDocsMask/SampledQuerySize"
    - path: "crates/cb-backend/src/kernels/query_helper_test.rs"
      provides: "self-oracle vs cb_compute::ranking_der group reductions"
  key_links:
    - from: "crates/cb-backend/src/kernels/query_helper.rs"
      to: "crates/cb-backend/src/kernels/exact_quantile.rs::segmented_radix_sort"
      via: "CreateSortKeys feeds the existing segmented radix sort for in-query sampling"
      pattern: "segmented_radix_sort"
    - from: "crates/cb-backend/src/kernels/query_helper.rs"
      to: "crates/cb-backend/src/kernels/reduce.rs"
      via: "der/weight group sums via fixed-point Atomic<u64> k=30"
      pattern: "REDUCE_FIXEDPOINT_SCALE_F64|Atomic"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Build the shared device query-grouping infrastructure ONCE (D-08), before any ranking objective, so all five query/listwise objectives (Plans 04–05) amortize it (GPUT-22 prerequisite sub-wave). This is the `query_helper` kernel surface: group ids, group means/max, group-bias removal, in-query sort keys (feeding the existing segmented radix sort), and taken-docs masks — each matching the CPU `ranking_der` group reductions at ε=1e-4.

Purpose: One resident grouping substrate amortized across QueryRMSE/QuerySoftMax/QueryCrossEntropy/YetiRank/PFound-F.
Output: `query_helper.rs` + self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-compute/src/ranking_der.rs
</context>

## Artifacts this phase produces (Plan 03)

- `crates/cb-backend/src/kernels/query_helper.rs` (NEW) — `ComputeGroupIds`, `ComputeGroupMeans` (both overloads), `ComputeGroupMax`, `RemoveGroupMeans`, `CreateSortKeys` (`key = (qid<<32)|random_low_32` via inline PCG), `FillQueryEndMask`, `FillTakenDocsMask` + `SampledQuerySize(sampleRate,qSize)` (≥2 floor).
- `crates/cb-backend/src/kernels/query_helper_test.rs` (NEW) — self-oracle.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device query-grouping kernels (group ids/means/max, bias removal, sort keys, masks)</name>
  <files>crates/cb-backend/src/kernels/query_helper.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test-first (pinned in Task 2): group means/max over a fixture with 3 uneven queries equal the CPU per-group reductions within ε=1e-4; RemoveGroupMeans subtracts the right per-query mean per doc; SampledQuerySize floors at 2.
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/query_helper.rs`; register in `crates/cb-backend/src/kernels.rs` (mvs registration idiom :2869-2877).
    - PATTERNS.md analog: "query_helper.rs (kernel, segmented group reduction + sort) — GPUT-22" — one warp (32 lanes) per query, `queriesPerBlock = BLOCK/32`; lanes stride the query's docs accumulating sumTarget/sumWeight (or max); WarpReduce → lane 0 writes mean/max. Kernels named `ComputeGroupIds`, `ComputeGroupMeans`, `ComputeGroupMax`, `RemoveGroupMeans` (`dst[d] -= queryMeans[qids[d]]`), `FillTakenDocsMask` + `SampledQuerySize(sampleRate,qSize)` (≥2 floor), `CreateSortKeys` (`key=(qid<<32)|random_low_32`).
    - Reuse (DO NOT hand-roll): `crates/cb-backend/src/kernels/exact_quantile.rs::segmented_radix_sort` (:162) for in-query sampling; inline PCG RNG from `mvs_device.rs:46-222` for CreateSortKeys.
    - Deterministic reduction: `crates/cb-backend/src/kernels/reduce.rs` (`REDUCE_FIXEDPOINT_SCALE_F64`, k=30 `Atomic<u64>` + fixed-order tree-reduce fallback) — der/weight group sums use THIS, not f64 atomic-add (gfx1100 has none).
    - CPU der oracle: `crates/cb-compute/src/ranking_der.rs::calc_ders_for_queries` (:139) over `GroupSpan {begin,end,weight,competitors}` (:78-95), `group_reduce_weighted` (:104-118, folds via cb_core::sum_f64 in object order — no raw float fold).
    - AGENTS.md: read the CubeCL manual (INDEX.md, plane/warp reduction, shared memory) before writing; on any CubeCL build error load `cubecl_error_guideline.md` first.
  </read_first>
  <action>
    Create `query_helper.rs` with the `#[cube]` grouping kernels named above. `ComputeGroupIds` scatters `qid` to each doc; `ComputeGroupMeans` (two overloads: qSizes+offsetsBias, and qOffsets) and `ComputeGroupMax` use one warp per query with a warp reduction, lane 0 writing `sumTarget/totalWeight` (or query max); `RemoveGroupMeans` is doc-parallel `dst[d] -= queryMeans[qids[d]]`; `CreateSortKeys` composes `(qid<<32)|random_low_32` from per-thread inline-PCG seeds so a radix sort keeps queries contiguous yet shuffles within a query; `FillTakenDocsMask` + `SampledQuerySize(sampleRate,qSize)` floor sampled size at 2. Route der/weight group SUMS through the fixed-point `Atomic<u64>` k=30 reduction (determinism mandatory); use `segmented_radix_sort` for the in-query sort (do not write a second sort). Only O(1) group descriptors + the resident der buffer cross the seam. Landmines: transcribe RNG/consts inline (no `cb-train`/`cb-core` call from `#[cube]`); no infinity literal in a kernel body (finite float-min sentinel); bind each `Handle` to its allocating client; `#[cfg(feature = "wgpu")]` typed reject on f64/u64 entry points; no `unwrap`/`expect`/`panic`/indexing in production.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib query_helper 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `query_helper.rs` defines the named kernels (`ComputeGroupIds`, `ComputeGroupMeans`, `ComputeGroupMax`, `RemoveGroupMeans`, `CreateSortKeys`, `FillTakenDocsMask`).
    - `SampledQuerySize` floors at 2 (test asserts `qSize=1`/small yields ≥2).
    - Group der/weight sums use the fixed-point path: `grep -c 'REDUCE_FIXEDPOINT_SCALE_F64\|Atomic' crates/cb-backend/src/kernels/query_helper.rs` ≥ 1.
    - In-query sort delegates to the existing sort: `grep -c 'segmented_radix_sort' crates/cb-backend/src/kernels/query_helper.rs` ≥ 1.
    - No infinity literal in a `#[cube]` body: `grep -v '^\s*//' crates/cb-backend/src/kernels/query_helper.rs | grep -c 'NEG_INF'` == 0.
  </acceptance_criteria>
  <done>Shared device query-grouping infra exists, reusing the segmented radix sort and the deterministic fixed-point reduction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-oracle — grouping infra vs CPU ranking_der group reductions</name>
  <files>crates/cb-backend/src/kernels/query_helper_test.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test 1: group means/max over a fixture with uneven query sizes equal the CPU `group_reduce_weighted` results within ε=1e-4.
    - Test 2: RemoveGroupMeans yields residuals matching the CPU per-query bias removal.
    - Test 3: CreateSortKeys + segmented sort keep queries contiguous (qid non-decreasing across the sorted output).
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/query_helper_test.rs`; register in `kernels.rs`.
    - PATTERNS.md analog: `mvs_device_test.rs` / `segmented_sort_test.rs` self-oracle structure; non-wgpu gate; off-device record-only (WR-01).
    - CPU oracle: `crates/cb-compute/src/ranking_der.rs::group_reduce_weighted` / `calc_ders_for_queries`.
  </read_first>
  <action>
    Create the dedicated test file. Build a serial-CPU reference over `GroupSpan`s and assert the device group ids/means/max/bias-removal match within `F64_TOL` (ε=1e-4). Assert `CreateSortKeys` + `segmented_radix_sort` produces a query-contiguous ordering (qids non-decreasing) with intra-query shuffle. Gate `#![cfg(not(feature = "wgpu"))]`; skip numeric assertion off rocm/cuda (record-only). Register in `kernels.rs`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib query_helper_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `query_helper_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - Group means/max/bias-removal assert max abs divergence ≤ 1e-4 vs CPU when a device backend is active.
    - Query-contiguity assertion passes on the sorted output.
  </acceptance_criteria>
  <done>Grouping infra self-oracled against the CPU ranking_der reductions at ε=1e-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs; only O(1) group descriptors + resident der buffer cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Denial of Service | grouping kernel launch grid sizing | mitigate | checked casts + bounded grid; warp-per-query fixed geometry |
| T-13-06 | Tampering | non-deterministic group reduction | mitigate | fixed-point `Atomic<u64>` k=30 + fixed-order fallback (SPIKE-REDUCTION) — holds ε=1e-4 across trees |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib query_helper` + `... query_helper_test` green on CPU/serial path.
- Fixed-point reduction + segmented-sort reuse verified by grep.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
</verification>

<success_criteria>
Shared device query-grouping infra exists, reuses the segmented radix sort + deterministic fixed-point reduction, and self-oracles against the CPU group reductions at ε=1e-4. Amortized by Plans 04–05. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-03-SUMMARY.md` when done.
</output>
