---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 10
type: execute
wave: 10
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06, 13-07, 13-08, 13-09]
files_modified:
  - .planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-COVERAGE-MATRIX.md
  - bench/kaggle_cuda_phase13.ipynb
autonomous: false
requirements: [GPUT-11, GPUT-21, GPUT-22, GPUT-12, GPUT-13, GPUT-20]
must_haves:
  truths:
    - "Each of the five families (pairwise / ranking / multiclass / ordered / langevin) has a recorded Kaggle CUDA ε=1e-4 correctness sign-off vs the Rust CPU path (GPUT-14 standing gate)"
    - "Each family has a recorded BENCH-02 Kaggle CUDA wall-clock speed measurement (device vs host-CPU baseline, and vs official CatBoost GPU where comparable; warm-run/JIT-excluded, train-only)"
    - "The GPU coverage matrix (per-family correctness + speed + covered/Ok(None) status) is a documented artifact (SC-5)"
    - "The CPU/host path is byte-unchanged (GPUT-14/D-04 no-regression) across the full phase"
  artifacts:
    - path: ".planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-COVERAGE-MATRIX.md"
      provides: "per-family correctness + BENCH-02 speed + coverage status matrix (SC-4/SC-5)"
    - path: "bench/kaggle_cuda_phase13.ipynb"
      provides: "Phase-13 Kaggle CUDA notebook cells per family (reuse Phase-10/12 harness)"
  key_links:
    - from: "bench/kaggle_cuda_phase13.ipynb"
      to: ".planning/phases/13-.../13-COVERAGE-MATRIX.md"
      via: "notebook results transcribed into the coverage matrix"
      pattern: "coverage|BENCH-02"
---

<objective>
Close the phase with the human-gated Kaggle CUDA authority: for EACH of the five families landed in Plans 01–09, run the Phase-10 Kaggle CUDA `--features cuda` notebook to record (a) the ε=1e-4 correctness sign-off vs the Rust CPU path (GPUT-14 standing gate) and (b) the BENCH-02 wall-clock speed measurement (device vs host-CPU baseline, and vs official CatBoost GPU where comparable; warm-run/JIT-excluded, train-only). Assemble the GPU coverage matrix (SC-4/SC-5). This is NOT an autonomous in-env task — CUDA sign-off is a human-gated external step (no NVIDIA hardware in-env; ROCm in-env is a smoke convenience only, not the gate).

Purpose: The documented per-family correctness + speed sign-off feeding the Phase-14 aggregate.
Output: coverage matrix artifact + Phase-13 Kaggle notebook cells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 10)

- `bench/kaggle_cuda_phase13.ipynb` (NEW/extend Phase-10/12 harness) — per-family Kaggle CUDA cells: correctness oracle (ε=1e-4 vs Rust CPU) + BENCH-02 timing.
- `.planning/phases/13-.../13-COVERAGE-MATRIX.md` (NEW) — the SC-5 coverage matrix: per family, covered-vs-`Ok(None)` status, recorded correctness ε, and BENCH-02 device-vs-baseline speed.

<tasks>

<task type="auto">
  <name>Task 1: Assemble Phase-13 Kaggle CUDA notebook cells + coverage matrix scaffold</name>
  <files>bench/kaggle_cuda_phase13.ipynb, .planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-COVERAGE-MATRIX.md</files>
  <read_first>
    - Reuse: the Phase-10/12 Kaggle CUDA harness (`bench/` notebook from Phase 10; Phase-12 per-family cells) — memory: the Kaggle pipeline is PROVEN & reusable via the `kaggle` CLI (Phase 12).
    - 13-VALIDATION.md "Manual-Only Verifications" + "Phase Requirements → Test Map": per-family device ε=1e-4 correctness + BENCH-02 speed are Kaggle-CUDA human-gated; the in-env CPU self-oracle (Plans 01–09) is the automated portion.
    - ROADMAP SC-4/SC-5: each family timed as it lands; coverage matrix documented.
  </read_first>
  <action>
    Build `bench/kaggle_cuda_phase13.ipynb` by extending the Phase-10/12 harness with one cell group per family (pairwise, ranking [5 objectives], multiclass [coupled + diagonal fixtures], ordered, langevin): each cell builds the `--features cuda` wheel, runs the family's correctness oracle (ε=1e-4 vs the Rust CPU path, correctness as a blocking gate), then a warm-run/JIT-excluded train-only BENCH-02 timing (device vs host-CPU baseline, and vs official CatBoost GPU where a comparable config exists). Create `13-COVERAGE-MATRIX.md` with a row per family and columns: covered (`Ok(Some)`) vs `Ok(None)` fallback status, recorded correctness ε, BENCH-02 device-vs-baseline speedup, and notes (e.g. GPUT-21 residency-gap if Plan 02 chose the host-solve fallback; QueryCrossEntropy/stochastic deferrals if any). Leave the numeric result cells to be filled by the human-gated run (Task 2). Do NOT fabricate CUDA numbers.
  </action>
  <verify>
    <automated>test -f bench/kaggle_cuda_phase13.ipynb && test -f .planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-COVERAGE-MATRIX.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bench/kaggle_cuda_phase13.ipynb` exists with one correctness+BENCH-02 cell group per family (5 families).
    - `13-COVERAGE-MATRIX.md` exists with a row per family and the covered/ε/speed/notes columns.
    - No fabricated CUDA numbers — result cells/rows are placeholders pending the human-gated run.
  </acceptance_criteria>
  <done>Phase-13 Kaggle notebook + coverage-matrix scaffold ready for the human-gated run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Kaggle CUDA per-family ε=1e-4 sign-off + BENCH-02 speed (human-gated)</name>
  <action>Human builds the `--features cuda` wheel and runs `bench/kaggle_cuda_phase13.ipynb` on a Kaggle CUDA notebook; for each family records the ε=1e-4 correctness result (blocking gate) and the BENCH-02 warm-run/JIT-excluded train-only speed, then transcribes them into `13-COVERAGE-MATRIX.md`. Do NOT fabricate CUDA numbers — record only measured results.</action>
  <what-built>Per-family device paths (pairwise/ranking/multiclass/ordered/langevin) with CPU self-oracles green in-env, plus a Phase-13 Kaggle CUDA notebook (`bench/kaggle_cuda_phase13.ipynb`) that runs the ε=1e-4 correctness oracle + BENCH-02 speed timing per family, and a `13-COVERAGE-MATRIX.md` scaffold.</what-built>
  <how-to-verify>
    1. Build the `--features cuda` wheel and run `bench/kaggle_cuda_phase13.ipynb` on a Kaggle CUDA notebook (confirm CUDA active via `nvidia-smi`).
    2. For EACH family, confirm the correctness oracle reports ε ≤ 1e-4 vs the Rust CPU path (correctness is the blocking gate — a fast-but-wrong result is never accepted).
    3. For EACH family, record the BENCH-02 warm-run/JIT-excluded train-only wall-clock: device vs host-CPU baseline, and vs official CatBoost GPU where a comparable config exists.
    4. Transcribe the per-family correctness ε + BENCH-02 speed + covered/`Ok(None)` status into `13-COVERAGE-MATRIX.md`.
    5. Confirm the CPU/host path is byte-unchanged (GPUT-14/D-04): full `cargo test -p cb-train -p cb-compute` green.
  </how-to-verify>
  <resume-signal>Type "approved" with the per-family ε + BENCH-02 numbers recorded in 13-COVERAGE-MATRIX.md, or describe which families need fixes / remain `Ok(None)`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle notebook execution | human-gated external CUDA run; no untrusted input; results transcribed by the human |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-19 | Tampering | fabricated CUDA correctness/speed numbers | mitigate | correctness is a blocking gate before any speed number; numbers only from the human-gated run (no fabrication, T-10-05) |
| T-13-20 | Repudiation | undocumented coverage status | mitigate | per-family covered/`Ok(None)` + ε + speed recorded in the coverage matrix (SC-5) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `bench/kaggle_cuda_phase13.ipynb` + `13-COVERAGE-MATRIX.md` exist.
- Human-gated Kaggle CUDA run records per-family ε ≤ 1e-4 + BENCH-02 speed (correctness blocking before speed).
- CPU/host byte-unchanged: `cargo test -p cb-train -p cb-compute` green.
</verification>

<success_criteria>
Every landed family has a recorded Kaggle CUDA ε=1e-4 correctness sign-off + BENCH-02 speed measurement; the GPU coverage matrix (SC-4/SC-5) is documented; CPU/host path byte-unchanged (GPUT-14/D-04). Completes the device-coverage surface feeding the Phase-14 aggregate sign-off.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-10-SUMMARY.md` when done.
</output>
