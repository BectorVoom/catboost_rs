---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 08
type: execute
wave: 8
depends_on: [13-07]
files_modified:
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-backend/src/gpu_runtime/ordered.rs
  - crates/cb-backend/src/gpu_runtime/ordered_test.rs
autonomous: true
requirements: [GPUT-13]
must_haves:
  truths:
    - "Ordered boosting (EBoostingType::Ordered) trains on device with the per-permutation approx trajectory kept device-resident ACROSS iterations — only O(1) descriptors cross the seam per level (no n-length readback)"
    - "The device ordered path reproduces the frozen CPU permutation + per-permutation approx trajectory bit-for-bit at ε=1e-4 (pinned seed, D-06)"
    - "The anti-leakage body/tail approximant matches ordered_approx_delta_simple (body rows keep delta 0)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "OrderedState (Option) — resident per-permutation approx trajectory across iterations"
    - path: "crates/cb-backend/src/gpu_runtime/ordered.rs"
      provides: "ordered device driver: resident trajectory update via apply_leaf_delta, O(1) descriptors"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/ordered.rs"
      to: "crates/cb-train/src/boosting.rs::ordered_approx_delta_simple"
      via: "device body/tail approximant reproduces the frozen CPU trajectory"
      pattern: "ordered_approx_delta|trajectory"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Land ordered boosting on device (GPUT-13) — the heaviest residency item (D-05). Keep the per-permutation ordered-boosting approx trajectory device-resident ACROSS boosting iterations; only O(1) descriptors cross the seam per level (Pitfall 5: no n-length readback). Reproduce the frozen CPU permutation + per-permutation approx trajectory bit-for-bit at ε=1e-4 (pinned seed, D-06). Pin `permutation_count` in the fixture and cover the single learning-fold case first (Open Q2).

Purpose: Full device residency for ordered boosting — the milestone's residency keystone for this family.
Output: OrderedState session field + ordered driver + frozen-fixture self-oracle + residency (no-readback) check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-train/src/boosting.rs
</context>

## Artifacts this phase produces (Plan 08)

- `ordered: Option<OrderedState>` coverage-gate field on `GpuTrainSession` — the per-permutation approx trajectory, RESIDENT across iterations (mirrors the resident `approx_h`/`der1_h` handles at session.rs:335-338).
- `crates/cb-backend/src/gpu_runtime/ordered.rs` (NEW) — ordered device driver: body/tail approximant, resident trajectory update via `apply_leaf_delta`, only O(1) descriptors cross the seam.
- `crates/cb-backend/src/gpu_runtime/ordered_test.rs` (NEW) — frozen permutation+trajectory self-oracle + residency check.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrderedState resident trajectory + ordered device driver</name>
  <files>crates/cb-backend/src/gpu_runtime/session.rs, crates/cb-backend/src/gpu_runtime/ordered.rs</files>
  <behavior>
    - Test-first (pinned in Task 2): with a frozen permutation, the device per-permutation approx trajectory after N iterations reproduces the frozen CPU trajectory ≤1e-4; body rows keep delta 0.
  </behavior>
  <read_first>
    - MODIFY targets: `crates/cb-backend/src/gpu_runtime/session.rs` (per-family `Option<*State>` fields :361-380; resident `approx_h`/`der1_h` handles :335-338, updated on-device via `apply_leaf_delta`, NO n-length readback); new `crates/cb-backend/src/gpu_runtime/ordered.rs`.
    - PATTERNS.md analog: "gpu_runtime/session.rs (store, ordered resident approx state) — GPUT-13" — add `ordered: Option<OrderedState>` the SAME way as `bootstrap`/`mvs`/`region`; residency discipline: trajectory stays resident across iterations, only O(1) descriptors cross per level; validate no `n`-length readback as the wave's success check.
    - CPU ref to reproduce (frozen fixture, D-06): `crates/cb-train/src/boosting.rs::ordered_approx_delta_simple` (:687) — the anti-leakage body/tail approximant (body rows keep delta 0, estimation prefix). Fold machinery `create_folds`/`Fold`, `permutation_count` (default 4 → `max(1,pc−1)` learning + 1 averaging fold); pin `permutation_count`, cover single learning-fold first (Open Q2).
    - AGENTS.md: read the CubeCL manual before writing; on any build error load the error guideline first.
  </read_first>
  <action>
    Add an `ordered: Option<OrderedState>` field to `GpuTrainSession` (mirroring `mvs`/`region`), holding the per-permutation approx trajectory as resident device handles across iterations. Create `ordered.rs` driving the device body/tail approximant: for each level, update the resident trajectory via `apply_leaf_delta` (body rows keep delta 0 — the estimation prefix), keeping the trajectory device-resident (no `n`-length readback); only the O(1) BestSplit descriptor + per-leaf stats cross the seam. Gate in `begin(...)` only for the covered ordered config (pinned `permutation_count`, single learning-fold first), else `Ok(None)`→host. Landmines: transcribe the ordered constants inline (no `cb-train` dep — the CPU ref is the frozen oracle, NOT a runtime call); no infinity literal in a kernel body (finite float-min sentinel); bind Handles to their client; `#[cfg(feature="wgpu")]` typed reject; no `unwrap`/`expect`/`panic`/indexing in production.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ordered 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GpuTrainSession` exposes `ordered: Option<OrderedState>`; `begin(...)` returns `Ok(None)` for uncovered ordered configs.
    - Trajectory updates go through `apply_leaf_delta` on resident handles (grep ordered.rs for `apply_leaf_delta`); no `n`-length host readback in the per-iteration loop.
    - Body rows keep delta 0 (the anti-leakage estimation prefix is preserved).
    - `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0; no infinity literal in the kernel body.
  </acceptance_criteria>
  <done>Ordered boosting trains on device with a resident per-permutation approx trajectory; O(1) descriptors only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frozen permutation+trajectory self-oracle + residency (no-readback) check</name>
  <files>crates/cb-backend/src/gpu_runtime/ordered_test.rs, crates/cb-backend/src/gpu_runtime.rs</files>
  <behavior>
    - Test 1: device trajectory after N iterations == frozen CPU `ordered_approx_delta_simple` trajectory ≤1e-4 (pinned permutation).
    - Test 2: residency — no `n`-length readback occurs in the per-iteration loop (the trajectory handle is not read back each iteration).
    - Test 3: uncovered config → `Ok(None)`.
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/gpu_runtime/ordered_test.rs`; register in `crates/cb-backend/src/gpu_runtime.rs`.
    - PATTERNS.md analog: Phase 12 frozen-fixture harness; non-wgpu gate; off-device record-only (WR-01). Residency check idiom: assert the trajectory handle is read back only once at the end (Pitfall 5).
    - CPU ref: `crates/cb-train/src/boosting.rs::ordered_approx_delta_simple` frozen permutation + trajectory (D-06).
  </read_first>
  <action>
    Create the dedicated test file. Freeze the CPU permutation + per-permutation approx trajectory for a pinned seed and small fixture. Assert the device trajectory reproduces the frozen CPU trajectory within `F64_TOL` (ε=1e-4) when a device backend is active (record-only otherwise). Add a residency assertion that the trajectory handle is NOT read back per iteration (only a single final readback for the oracle comparison). Assert the uncovered-config `Ok(None)` path. Gate `#![cfg(not(feature = "wgpu"))]`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ordered_test 2>&1 | tail -20; cargo test -p cb-train ordered 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ordered_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - Device trajectory asserts ≤1e-4 vs the frozen CPU trajectory when a device backend is active.
    - Residency assertion: only one final trajectory readback (no per-iteration `n`-length readback).
  </acceptance_criteria>
  <done>Ordered device trajectory self-oracled against the frozen CPU reference; residency verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs; O(1) descriptors per level; trajectory stays resident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Denial of Service | ordered per-iteration launch geometry | mitigate | checked casts + bounded grid; resident trajectory (no per-iter realloc) |
| T-13-16 | Tampering | ordered coverage gate | accept | `Ok(None)` never fabricates; frozen CPU trajectory is the reference |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib ordered` + `... ordered_test` + `cargo test -p cb-train ordered` green.
- Residency (no per-iteration readback) asserted.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
</verification>

<success_criteria>
GPUT-13: ordered boosting trains on device with a resident per-permutation approx trajectory (O(1) descriptors only), reproducing the frozen CPU trajectory at ε=1e-4. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-08-SUMMARY.md` when done.
</output>
