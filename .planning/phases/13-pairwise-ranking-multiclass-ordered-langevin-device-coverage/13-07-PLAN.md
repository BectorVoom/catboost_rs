---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 07
type: execute
wave: 7
depends_on: [13-06]
files_modified:
  - crates/cb-backend/src/gpu_runtime/multiclass.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-backend/src/gpu_runtime/multiclass_test.rs
autonomous: true
requirements: [GPUT-12]
must_haves:
  truths:
    - "MultiClass (coupled softmax), MultiClassOneVsAll, MultiCrossEntropy, MultiRMSE, and RMSEWithUncertainty each train on device via ONE shared tree with leaf_count × approx_dim block leaves and the K-dim Newton block solve, matching the CPU path at ε=1e-4"
    - "Coupled full-block solve is used ONLY for MultiClass softmax; the separable losses use the diagonal path (Pitfall 3)"
    - "Uncovered multi-output configs return Ok(None)→CPU (per-fit all-or-nothing)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/multiclass.rs"
      provides: "multi-output device driver: shared tree + block-leaf emission routed through multi-output CPU apply"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "multiclass coverage gate (Option<MulticlassState>)"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/multiclass.rs"
      to: "crates/cb-backend/src/kernels/multi_newton.rs"
      via: "per-leaf K-dim Newton block solve emits the leaf_count × approx_dim block"
      pattern: "multi_newton|block"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Wire the multiclass / multi-target / uncertainty family onto the Plan-06 block-leaf + K-dim Newton machinery (GPUT-12, D-04): grow ONE shared tree structure per boosting step, solve a K-dim der2 block per leaf (coupled softmax vs diagonal separable), emit the `leaf_count × approx_dim` block, and route it through the existing multi-output CPU apply. The der functions already exist in `cb-compute`; the device work is the block emission + coverage gate.

Purpose: Complete GPUT-12 device coverage across all five multi-output losses.
Output: multiclass driver + coverage gate + frozen fixtures + self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-compute/src/runtime.rs
</context>

## Artifacts this phase produces (Plan 07)

- `crates/cb-backend/src/gpu_runtime/multiclass.rs` (NEW) — multi-output device driver: one shared tree per step, per-leaf K-dim Newton block solve (Plan 06), `leaf_count × approx_dim` block emission routed through the multi-output CPU apply.
- `Option<MulticlassState>` coverage-gate field on `GpuTrainSession`.
- Frozen multiclass fixtures (coupled softmax K=3, diagonal RMSEWithUncertainty K=2, MultiRMSE) + `crates/cb-backend/src/gpu_runtime/multiclass_test.rs` (NEW).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-output device driver + coverage gate (coupled + diagonal dispatch)</name>
  <files>crates/cb-backend/src/gpu_runtime/multiclass.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <behavior>
    - Test-first (pinned in Task 2): a coupled softmax K=3 fit emits a block leaf matching the CPU multi-output leaf values ≤1e-4; a diagonal RMSEWithUncertainty K=2 fit matches the CPU path ≤1e-4.
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/gpu_runtime/multiclass.rs`; `crates/cb-backend/src/gpu_runtime/session.rs` (`Option<*State>` gate idiom).
    - PATTERNS.md analog: "runtime.rs DeviceGrownTree — D-03" (block leaves route through the existing multi-output CPU apply `runtime.rs:1145`, `approx[d*n+i]`); "multi_newton.rs — GPUT-12" (Plan-06 K-dim block solve consumed here).
    - RESEARCH Pattern 3 / Pitfall 3 (coupled vs diagonal): coupled full-block for `MultiClass` softmax; diagonal for `MultiClassOneVsAll`, `MultiCrossEntropy`/multilabel, `MultiRMSE`, `RMSEWithUncertainty`. Predictions column-major stride (`predictions[idx + k*alignSize]`), effective class count `numClasses−1` (last class implicit logit 0), max-subtracted softmax.
    - Der functions ALREADY in `cb-compute` (`MultiClass`/`OneVsAll`/`MultiCrossEntropy`/`MultiRMSE`/`RMSEWithUncertainty`, `runtime.rs:171-283`) — do NOT re-derive; the device transcribes the emission, not the der.
    - AGENTS.md: read the CubeCL manual before writing; on any build error load the error guideline first.
  </read_first>
  <action>
    Create `multiclass.rs` driving the multi-output grow path: grow ONE shared tree per boosting step, compute per-leaf K-dim der2 blocks, dispatch the Plan-06 `multi_newton` solve (coupled for `MultiClass` softmax, diagonal for the four separable losses), emit the `leaf_count × approx_dim` block into `DeviceGrownTree.leaf_values` with `approx_dim = K`, and route it through the existing multi-output CPU apply. Add a `multiclass: Option<MulticlassState>` gate field to `GpuTrainSession`, populated in `begin(...)` only for covered multi-output configs, else `Ok(None)`→host (per-fit all-or-nothing). Der/weight SUMS use the fixed-point reduction; the block solve is f64 (Plan 06). Landmines: transcribe constants inline (no `cb-train` dep); no infinity literal in a kernel body (finite float-min sentinel); bind Handles to their client; `#[cfg(feature="wgpu")]` typed reject; no `unwrap`/`expect`/`panic`/indexing in production.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib multiclass 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GpuTrainSession` exposes `multiclass: Option<MulticlassState>`; `begin(...)` returns `Ok(None)` for uncovered multi-output configs.
    - Coupled dispatch is used ONLY for `MultiClass`; the four separable losses use the diagonal path (grep multiclass.rs for the dispatch arms).
    - Block emission sets `approx_dim = K` and writes `leaf_count × approx_dim` values.
    - `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0; no infinity literal in the kernel body.
  </acceptance_criteria>
  <done>All five multi-output losses reach the device path via one shared tree + block leaves behind the multiclass gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frozen multiclass fixtures + end-to-end self-oracle</name>
  <files>crates/cb-backend/src/gpu_runtime/multiclass_test.rs, crates/cb-backend/src/gpu_runtime.rs</files>
  <behavior>
    - Test 1: coupled softmax MultiClass K=3 block leaves == CPU multi-output leaf values ≤1e-4.
    - Test 2: diagonal RMSEWithUncertainty K=2 (distinct row-0/row-1 hessian) == CPU ≤1e-4.
    - Test 3: diagonal MultiRMSE (or MultiClassOneVsAll) K≥2 == CPU ≤1e-4; uncovered config → `Ok(None)`.
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/gpu_runtime/multiclass_test.rs`; register in `crates/cb-backend/src/gpu_runtime.rs`.
    - PATTERNS.md analog: Phase 12 frozen-fixture harness; `apply_leaf_delta.rs` device-vs-CPU structure; non-wgpu gate; off-device record-only (WR-01).
    - RESEARCH Validation Arch "Multiclass fixture recommendation" (A2): coupled softmax K=3 + diagonal RMSEWithUncertainty K=2 + one MultiRMSE/OneVsAll covers both hessian structures with minimal fixtures.
    - CPU oracle: `cb-compute` multi-output leaf values via `solve_symmetric_newton` + the multi-output apply.
  </read_first>
  <action>
    Create the dedicated test file with the three frozen fixtures above (coupled K=3, diagonal RMSEWithUncertainty K=2, diagonal MultiRMSE/OneVsAll). Assert the device block leaves match the CPU multi-output leaf values within `F64_TOL` (ε=1e-4) over equal-length buffers when a device backend is active (record-only otherwise). Assert an uncovered config returns `Ok(None)`. Gate `#![cfg(not(feature = "wgpu"))]`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib multiclass_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `multiclass_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - Coupled + both diagonal fixtures assert ≤1e-4 vs CPU when a device backend is active.
    - Uncovered-config `Ok(None)` path asserted.
  </acceptance_criteria>
  <done>Multi-output family self-oracled with coupled + diagonal fixtures at ε=1e-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs; block leaves cross as `Vec<f64>` + `approx_dim` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Denial of Service | multi-output grow launch grid sizing | mitigate | checked casts + bounded grid; K bounded (~≤10 softmax block) |
| T-13-14 | Tampering | multiclass coverage gate | accept | `Ok(None)` never fabricates; CPU multi-output path is the reference |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib multiclass` + `... multiclass_test` green.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
- Scalar no-regression: `cargo test -p cb-train -p cb-compute` green.
</verification>

<success_criteria>
GPUT-12 device coverage complete (5 multi-output losses via one shared tree + block leaves); coupled/diagonal dispatch correct; self-oracled ≤1e-4. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-07-SUMMARY.md` when done.
</output>
