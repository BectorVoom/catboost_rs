---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 05
type: execute
wave: 5
depends_on: [13-04]
files_modified:
  - crates/cb-backend/src/gpu_runtime/ranking.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs
autonomous: true
requirements: [GPUT-22]
must_haves:
  truths:
    - "YetiRank and PFound-F train on the device query path with in-query bootstrap sampling (segmented radix sort + inline PCG), matching the frozen pinned-seed CPU reference bit-for-bit at ε=1e-4"
    - "The stochastic pair reproduces the CPU RNG draw ORDER and COUNT exactly (pinned seed, frozen fixture) — a wrong draw order shifts every value beyond ε=1e-4 (Pitfall 4)"
    - "GPUT-22 device coverage is complete (all 5 objectives) with the deterministic-only subset as the documented lower-risk fallback if stochastic sampling over-runs"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/ranking.rs"
      provides: "YetiRank + PFound-F stochastic in-query sampling arms (pinned-seed)"
    - path: "crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs"
      provides: "frozen-fixture self-oracle for the stochastic pair"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/ranking.rs"
      to: "crates/cb-backend/src/kernels/exact_quantile.rs::segmented_radix_sort"
      via: "in-query bootstrap perturbation + sort per sampling iteration"
      pattern: "segmented_radix_sort"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Complete GPUT-22 with the two stochastic ranking objectives (D-08): YetiRank and PFound-F — in-query bootstrap sampling (perturb + segmented radix sort per iteration) under the pinned-seed / frozen-fixture discipline (D-06). The RNG draw order and count are load-bearing (Pitfall 4): the device must reproduce the CPU stream bit-for-bit. If in-query sampling over-runs, the documented fallback is the deterministic-only ranking subset (Plans 03–04) — correctness-safe.

Purpose: Full ranking device coverage including the stochastic pair.
Output: YetiRank/PFound-F arms + frozen-fixture self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-compute/src/ranking_der.rs
</context>

## Artifacts this phase produces (Plan 05)

- YetiRank + PFound-F stochastic arms in `crates/cb-backend/src/gpu_runtime/ranking.rs` — in-query bootstrap perturbation (`uni/(1.000001−uni)`) + `segmented_radix_sort` per sampling iteration, inline PCG RNG (pinned seed).
- `Option<RankingState>` extended with the stochastic objectives (independently gated).
- `crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs` (NEW) — frozen-fixture self-oracle.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: YetiRank + PFound-F in-query bootstrap sampling (pinned seed)</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <behavior>
    - Test-first (pinned in Task 2): with a pinned seed and a frozen CPU draw/permutation trajectory, YetiRank der reproduces the frozen CPU der bit-for-bit at ε=1e-4; PFound-F likewise.
  </behavior>
  <read_first>
    - MODIFY targets: `crates/cb-backend/src/gpu_runtime/ranking.rs`, `session.rs`.
    - PATTERNS.md analog: "Inline RNG transcription" + "Frozen pinned-seed fixture (RNG-driven paths, D-06)" — host advances the continuous stream and hands the device the O(1) base state (bootstrap_device precedent); reproduce frozen CPU draws bit-for-bit.
    - Reuse: `segmented_radix_sort` (exact_quantile.rs:162) for the intra-query sort per bootstrap iteration; inline PCG from `mvs_device.rs:46-222`.
    - RESEARCH Pitfall 4: YetiRank/PFound-F perturb with `uni/(1.000001−uni)` and radix-sort per bootstrap iteration; a wrong draw order/count silently shifts every value beyond ε=1e-4. `std_normal` is NOT used here (that is Langevin) — this uses uniform draws.
    - CPU der oracle: `crates/cb-compute/src/ranking_der.rs` YetiRank(/Pairwise) + PFound-F der.
    - AGENTS.md: read the CubeCL manual before writing; on any build error load the error guideline first.
  </read_first>
  <action>
    Add the YetiRank and PFound-F arms to `ranking.rs`: per bootstrap iteration, perturb each doc score with `uni/(1.000001−uni)` from the inline PCG stream, `segmented_radix_sort` within each query, accumulate the sampled der via the fixed-point reduction, and average over iterations — matching the CPU draw order and count exactly. Pin the seed and derive the device base RNG state from the host-advanced stream (O(1) crossing). Extend the `ranking: Option<RankingState>` gate with YetiRank/PFound-F, each independently gated (`Ok(None)` if uncovered). Landmines: transcribe the RNG + perturbation constants inline (no `cb-train`/`cb-core` from `#[cube]`); no infinity literal in a kernel body (finite float-min sentinel); bind Handles to their client; `#[cfg(feature="wgpu")]` typed reject; no `unwrap`/`expect`/`panic`/indexing in production.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ranking 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - YetiRank + PFound-F arms exist; each independently gated in `session.begin(...)` (uncovered → `Ok(None)`).
    - In-query perturbation + sort reuse `segmented_radix_sort`: `grep -c 'segmented_radix_sort' crates/cb-backend/src/gpu_runtime/ranking.rs` ≥ 1.
    - Device RNG base state derived from the host-advanced stream (O(1) crossing; no per-iteration host RNG readback).
    - `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0; no infinity literal in the kernel body.
  </acceptance_criteria>
  <done>YetiRank + PFound-F reach the device ranking path under pinned-seed sampling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frozen-fixture self-oracle for the stochastic pair</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs, crates/cb-backend/src/gpu_runtime.rs</files>
  <behavior>
    - Test 1: YetiRank der with pinned seed == frozen CPU der bit-for-bit at ε=1e-4.
    - Test 2: PFound-F der with pinned seed == frozen CPU der at ε=1e-4.
    - Test 3: draw COUNT matches (a divergent count is detected, not silently absorbed).
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs`; register in `crates/cb-backend/src/gpu_runtime.rs`.
    - PATTERNS.md analog: Phase 12 D-07 mvs/ctr frozen fixtures (`mvs_device_test.rs`) — freeze the exact CPU draws/permutation/trajectory, reproduce bit-for-bit; non-wgpu gate; off-device record-only (WR-01).
    - CPU oracle: `crates/cb-compute/src/ranking_der.rs` YetiRank/PFound-F der with the SAME pinned seed.
  </read_first>
  <action>
    Create the dedicated test file. Freeze the CPU reference draws + der trajectory for a pinned seed and a small multi-query fixture. Assert the device der reproduces the frozen CPU der within `F64_TOL` (ε=1e-4) AND that the consumed draw count matches (record a mismatch as a failure). Gate `#![cfg(not(feature = "wgpu"))]`; off-device record-only.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib ranking_stoch_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ranking_stoch_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - YetiRank + PFound-F assert bit-for-bit reproduction (≤1e-4) of the frozen CPU der when a device backend is active.
    - Draw-count mismatch is asserted (not absorbed).
  </acceptance_criteria>
  <done>Stochastic ranking pair self-oracled against frozen pinned-seed CPU references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs; O(1) RNG base state + group descriptors + resident der buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Denial of Service | per-iteration in-query sampling loop | mitigate | fixed bootstrap-iteration count; bounded segmented sort geometry |
| T-13-10 | Tampering | RNG draw order/count divergence | mitigate | pinned seed + frozen fixture; draw-count asserted; O(1) base-state crossing (bootstrap_device precedent) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib ranking` + `... ranking_stoch_test` green.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
</verification>

<success_criteria>
GPUT-22 device coverage complete (all 5 query objectives); YetiRank/PFound-F reproduce frozen pinned-seed CPU references at ε=1e-4; deterministic-only subset documented as the fallback. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-05-SUMMARY.md` when done.
</output>
