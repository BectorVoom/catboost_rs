---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 09
type: execute
wave: 9
depends_on: [13-08]
files_modified:
  - crates/cb-backend/src/kernels/langevin.rs
  - crates/cb-backend/src/kernels/langevin_test.rs
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
autonomous: true
requirements: [GPUT-20]
must_haves:
  truths:
    - "AddLangevinNoise adds a per-element seeded Gaussian (coefficient · std_normal(seed_i)) to the resident reduced-derivative buffer on device, matching the pinned-seed CPU reference at ε=1e-4"
    - "The device std_normal transcribes the Marsaglia-polar rejection loop inline (VARIABLE even draw count per sample) — draw order and count reproduce the CPU stream bit-for-bit (Pitfall 4)"
    - "A PairLogit+Langevin config falls back to CPU (Ok(None)) — Langevin is NOT supported on the pairwise oracle (A4/upstream CB_ENSURE)"
  artifacts:
    - path: "crates/cb-backend/src/kernels/langevin.rs"
      provides: "AddLangevinNoise seeded-Gaussian kernel over the resident reduced der buffer (std_normal inline)"
    - path: "crates/cb-backend/src/kernels/langevin_test.rs"
      provides: "frozen pinned-seed draw-sequence self-oracle"
  key_links:
    - from: "crates/cb-backend/src/kernels/langevin.rs"
      to: "crates/cb-core/src/normal.rs::std_normal"
      via: "device transcribes the Marsaglia-polar draw loop inline (no cb-core call)"
      pattern: "std_normal|marsaglia|polar"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Land the smallest family last (D-09): Langevin/SGLB noise (GPUT-20). `AddLangevinNoise` adds a per-element seeded Gaussian to the resident reduced-derivative buffer on device, layering on the existing der residency. The device `std_normal` transcribes the Marsaglia-polar rejection loop inline (variable even draw count — Pitfall 4); pinned-seed / frozen-fixture reproduction at ε=1e-4. A PairLogit+Langevin config falls back to CPU (A4: upstream `CB_ENSURE`s Langevin is not supported on the pairwise oracle).

Purpose: Complete the five-family device-coverage surface for the Phase-14 aggregate sign-off.
Output: langevin.rs + langevin gate + frozen draw-sequence self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
@crates/cb-core/src/normal.rs
</context>

## Artifacts this phase produces (Plan 09)

- `crates/cb-backend/src/kernels/langevin.rs` (NEW) — `launch_langevin_resident(...)` AddLangevinNoise kernel: per-element reseed + `advance(10)` + inline Marsaglia-polar `std_normal`, `der[i] += coefficient * std_normal(seed_i)`, resident handle out (no readback).
- `langevin: Option<LangevinState>` coverage-gate field on `GpuTrainSession` (PairLogit+Langevin → `Ok(None)`).
- `crates/cb-backend/src/kernels/langevin_test.rs` (NEW) — frozen draw-sequence self-oracle.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AddLangevinNoise seeded-Gaussian kernel + langevin gate</name>
  <files>crates/cb-backend/src/kernels/langevin.rs, crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <behavior>
    - Test-first (pinned in Task 2): with a pinned seed, the device noised der reproduces the frozen CPU `coefficient · std_normal` sequence ≤1e-4; a PairLogit+Langevin config returns `Ok(None)`.
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/kernels/langevin.rs`; register in `crates/cb-backend/src/kernels.rs` (mvs idiom :2869-2877); `crates/cb-backend/src/gpu_runtime/session.rs` (`Option<*State>` gate idiom).
    - PATTERNS.md analog: "langevin.rs (kernel, seeded-Gaussian transform) — GPUT-20" — closest device precedent `mvs_device.rs`: serial `#[cube]` over the resident der buffer, inline PCG, resident handle out (no readback), host-readback oracle wrapper, wgpu reject. Copy the inline RNG primitives (`LCG_MULTIPLIER`, `REAL1_INV`, `rotate_right_u32`, `pcg_mix` at :46-92), the per-block reseed + `advance(10)` (:183-222), and the launch + resident-handle-out + wgpu-reject (:264-345) VERBATIM. Langevin is SIMPLER (no threshold bisection) — the body is `der[i] += coefficient * std_normal(seed_i)`.
    - CPU oracle to transcribe INLINE (do NOT call cb-core from a kernel): `crates/cb-core/src/normal.rs::std_normal` (:50) — Marsaglia-polar rejection loop over `gen_rand_real1`, VARIABLE even draw count per sample, `x*sqrt(-2*ln(r)/r)`.
    - CAVEAT (A4, VERIFIED): upstream `pairwise_oracle.h` `CB_ENSURE`s Langevin NOT supported for the pairwise oracle — a PairLogit+Langevin fixture must fall back to CPU (`Ok(None)`).
    - RESEARCH Pitfall 4: draw order/count is load-bearing; a wrong order shifts every subsequent value beyond ε=1e-4.
    - AGENTS.md: read the CubeCL manual before writing; on any build error load the error guideline first.
  </read_first>
  <action>
    Create `langevin.rs` with `launch_langevin_resident(client, der_h, rand_seed, coefficient, n)`: copy the `mvs_device.rs` inline PCG primitives + two-stream seed derivation + `advance(10)` verbatim, transcribe the Marsaglia-polar `std_normal` rejection loop inline (matching the CPU draw order and even-count consumption), and for each element add `coefficient * std_normal(seed_i)` to the resident reduced-derivative buffer, returning the resident handle WITHOUT `read_one` (D-09 residency). Add a `langevin: Option<LangevinState>` gate to `GpuTrainSession`; a PairLogit+Langevin config returns `Ok(None)` (A4). Provide a `draw_langevin_host(...)` readback wrapper for the self-oracle only. Landmines: no infinity literal in the `#[cube]` body (finite float-min sentinel); bind Handles to their client; `#[cfg(feature="wgpu")]` typed reject (`CbError::OutOfRange`, f64/u64 unsupported on WGSL); no `unwrap`/`expect`/`panic`/indexing in production; transcribe RNG/Gaussian inline (no `cb-core`/`cb-train` call from `#[cube]`); the RNG rejection loop is bounded in expectation (accept-region has positive measure — DoS-safe).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib langevin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `launch_langevin_resident(...)` exists; returns a resident handle without a `read_one` on the noised buffer.
    - `GpuTrainSession` exposes `langevin: Option<LangevinState>`; a PairLogit+Langevin config returns `Ok(None)` (A4).
    - Marsaglia-polar loop transcribed inline (grep langevin.rs for the `x*x + y*y` rejection condition / `std_normal` shape); no `cb-core` call inside the kernel body.
    - No infinity literal in the `#[cube]` body: `grep -v '^\s*//' crates/cb-backend/src/kernels/langevin.rs | grep -c 'NEG_INF'` == 0. `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0.
  </acceptance_criteria>
  <done>AddLangevinNoise adds a per-element seeded Gaussian to the resident der buffer behind the langevin gate; PairLogit+Langevin falls back to CPU.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frozen pinned-seed draw-sequence self-oracle</name>
  <files>crates/cb-backend/src/kernels/langevin_test.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test 1: with a pinned seed, the device noised der == frozen CPU `coefficient · std_normal` sequence ≤1e-4.
    - Test 2: draw COUNT per element matches the CPU rejection-loop consumption (a divergent count is detected).
    - Test 3: empty-`n` constructs the no-op launcher without a 0-length `read_one`; PairLogit+Langevin → `Ok(None)`.
  </behavior>
  <read_first>
    - MODIFY target: new `crates/cb-backend/src/kernels/langevin_test.rs`; register in `kernels.rs`.
    - PATTERNS.md analog: `mvs_device_test.rs` frozen-fixture harness; "Frozen pinned-seed fixture (D-06)"; non-wgpu gate; off-device record-only (WR-01); do NOT `read_one` a 0-len handle (HIP fault) — assert the no-op launcher.
    - CPU oracle: `crates/cb-core/src/normal.rs::std_normal` with the SAME pinned seed.
  </read_first>
  <action>
    Create the dedicated test file. Freeze the CPU `coefficient · std_normal` sequence for a pinned seed and small `n`. Assert the device noised der reproduces it within `F64_TOL` (ε=1e-4) when a device backend is active (record-only otherwise), AND that the consumed draw count per element matches the CPU rejection loop (record a mismatch as failure). Assert the empty-`n` no-op launcher and the PairLogit+Langevin `Ok(None)` path. Gate `#![cfg(not(feature = "wgpu"))]`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib langevin_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `langevin_test.rs` exists as a dedicated file; passes on CPU/serial path.
    - Device noised der asserts bit-for-bit reproduction (≤1e-4) of the frozen CPU sequence when a device backend is active.
    - Draw-count mismatch asserted; empty-`n` no-op and PairLogit+Langevin `Ok(None)` asserted.
  </acceptance_criteria>
  <done>Langevin device kernel self-oracled against the frozen pinned-seed CPU draw sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | plain host structs; O(1) RNG base state crosses; noised der stays resident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | Denial of Service | unbounded RNG rejection loop (`std_normal`) | mitigate | bounded expected iterations (≈1.27), positive-measure accept region (threat T-03-04-02 precedent) |
| T-13-18 | Tampering | RNG draw order/count divergence | mitigate | pinned seed + frozen fixture; draw-count asserted; O(1) base-state crossing |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package; no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib langevin` + `... langevin_test` green.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0; no infinity literal in the kernel body.
</verification>

<success_criteria>
GPUT-20: AddLangevinNoise adds a per-element seeded Gaussian to the resident der buffer, reproducing the frozen pinned-seed CPU sequence at ε=1e-4; PairLogit+Langevin falls back to CPU. Five-family device-coverage surface complete. Kaggle CUDA sign-off deferred to Plan 10.
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-09-SUMMARY.md` when done.
</output>
