---
phase: 13-pairwise-ranking-multiclass-ordered-langevin-device-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-backend/src/gpu_runtime/pairwise.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-backend/src/kernels/score_split.rs
  - crates/cb-backend/src/kernels/pairwise_deriv_test.rs
autonomous: true
requirements: [GPUT-11]
must_haves:
  truths:
    - "A PairLogit fit reaches the device grow path (pairwise gate returns Some) instead of Ok(None)→CPU for a covered config"
    - "Per-leaf pairwise-derivative matrix assembly (MakePairwiseDerivatives/MakePointwiseDerivatives) runs device-side and matches the CPU pair-stat reference at ε=1e-4"
    - "At approx_dim==1 the scalar CPU/host path stays byte-unchanged (GPUT-14/D-04 no-regression)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/pairwise.rs"
      provides: "PairLogit device path wiring + per-leaf matrix assembly driver"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "pairwise coverage gate (Option<PairwiseState>), Ok(None) fallback for uncovered configs"
    - path: "crates/cb-backend/src/kernels/pairwise_deriv_test.rs"
      provides: "self-oracle for per-leaf matrix assembly vs CPU pair-stat reference"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/session.rs"
      to: "crates/cb-backend/src/gpu_runtime/pairwise.rs"
      via: "pairwise gate dispatches to the device pairwise histogram + der-assembly path"
      pattern: "PairwiseState|pairwise"
    - from: "crates/cb-backend/src/gpu_runtime/pairwise.rs"
      to: "Phase 7.4 4-channel pairwise_hist kernels"
      via: "reuse existing (f*n_bins+bin)*4+histId histograms unchanged"
      pattern: "pairwise_hist"
---

<!-- planner-discipline-allow: cb-train -->

<objective>
Flip the PairLogit pairwise-loss training path from `Ok(None)`→CPU-fallback to a device path (GPUT-11), reusing the Phase 7.4 4-channel pairwise histograms and the Phase 7.5 pairwise scorer, and land the per-leaf pairwise-derivative matrix assembly (`MakePairwiseDerivatives`/`MakePointwiseDerivatives`) device-side. This wave assembles the SPD systems on device but still routes the small solve through the existing 7.5 host bounded solve — the on-device batched Cholesky is Plan 02 (GPUT-21). Landing pairwise first (D-02) front-loads the histogram reuse and de-risks the solver wave.

Purpose: Complete the pairwise device histogram + der-assembly surface so the Cholesky solver (Plan 02) has resident per-leaf systems to consume.
Output: Pairwise coverage gate + device per-leaf matrix assembly + self-oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-CONTEXT.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-RESEARCH.md
@.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01)

- `Option<PairwiseState>` coverage-gate field on `GpuTrainSession` (session.rs) — Some iff a fit commits to the device pairwise path; `Ok(None)` otherwise (per-fit all-or-nothing, D-10-01).
- Device per-leaf pairwise-derivative matrix assembly driver in `gpu_runtime/pairwise.rs` (`MakePairwiseDerivatives`/`MakePointwiseDerivatives`), consuming the resident 7.4 pairwise histograms.
- `crates/cb-backend/src/kernels/pairwise_deriv_test.rs` — self-oracle harness (new).

<tasks>

<task type="auto">
  <name>Task 1: Pairwise coverage gate + device per-leaf matrix assembly</name>
  <files>crates/cb-backend/src/gpu_runtime/session.rs, crates/cb-backend/src/gpu_runtime/pairwise.rs, crates/cb-backend/src/kernels/score_split.rs</files>
  <read_first>
    - MODIFY targets: `crates/cb-backend/src/gpu_runtime/session.rs` (per-family `Option<*State>` fields at :361-380; `begin(...) -> CbResult<Option<Self>>` gate at :470-547), `crates/cb-backend/src/gpu_runtime/pairwise.rs` (Phase 7.5 pairwise stat assembly), `crates/cb-backend/src/kernels/score_split.rs` (current host solve call site :864-992).
    - PATTERNS.md analog: "gpu_runtime/session.rs (store) — GPUT-13" section — the `Option<XxxState>` coverage-gate idiom (nonsym/exact_leaf/bootstrap/mvs/region), each `Some` iff the fit committed, `Ok(None)`-safe. Add a `pairwise` variant the SAME way.
    - PATTERNS.md analog: "gpu_runtime/pairwise.rs + kernels/score_split.rs (wire device Cholesky) — GPUT-21" — the pairwise-stat assembly + der-sum scatter already run device-side; this plan assembles the per-leaf systems, leaving the solve host-side until Plan 02.
    - CPU oracle reference (ε=1e-4 target, do NOT match upstream RegularizeImpl — Pitfall 2): `crates/cb-train/src/pairwise_leaves.rs::calculate_pairwise_leaf_values` (:113) and `crates/cb-compute/src/leaf.rs::calculate_pairwise_score` for scoring stats.
    - AGENTS.md: read `/home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md` before any `#[cube]` reasoning; on any CubeCL build error load `cubecl_error_guideline.md` before fixing.
  </read_first>
  <action>
    Add a `pairwise: Option<PairwiseState>` field to `GpuTrainSession` mirroring the existing `mvs`/`region` coverage-gate fields; populate it in `begin(...)` only when the fit config is a covered PairLogit config (single approx dim, supported bootstrap), else leave `None` so the seam returns `Ok(None)`→host CPU grower (D-10-01 per-fit all-or-nothing). In `gpu_runtime/pairwise.rs`, drive per-leaf pairwise-derivative matrix assembly (`MakePairwiseDerivatives` for the pair systems, `MakePointwiseDerivatives` for the pointwise diagonal contribution) over the resident Phase 7.4 4-channel `pairwise_hist` histograms — reuse those histograms unchanged (do NOT write a new histogram kernel). Assemble the packed lower-triangular per-leaf `linearSystem` (matrix triangle then RHS) resident on device, keyed by leaf. Leave the SPD solve as the existing 7.5 host bounded solve for now (`score_split.rs`) — Plan 02 replaces it. Reductions of der/weight sums use the fixed-point `Atomic<u64>` k=30 path (reduce.rs), NOT f64 atomic-add (gfx1100 has none). Landmine: no `cb-train` dep in `cb-backend` — transcribe any CPU constants inline as `const`; bind each `Handle` to its allocating client; guard f64/u64 entry points with the `#[cfg(feature = "wgpu")]` typed reject (mvs_device.rs precatedent).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib pairwise 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GpuTrainSession` exposes a `pairwise: Option<PairwiseState>` field; `begin(...)` returns `Ok(None)` for any uncovered PairLogit config (grep session.rs shows the pairwise gate arm returning None on the uncovered branch).
    - Per-leaf assembled system layout is packed lower-triangular (`rowSize*(rowSize+1)/2` matrix cells then `rowSize` RHS), keyed by leaf, resident on device (no `n`-length host readback of pair stats).
    - `cargo test -p cb-backend --lib pairwise` compiles and passes on the CPU/serial path; wgpu entry points return the typed `CbError::OutOfRange` reject rather than JIT-crashing.
    - No `cb-train` entry appears in `crates/cb-backend/Cargo.toml` dependencies: `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` returns 0.
  </acceptance_criteria>
  <done>PairLogit reaches the device pairwise path behind a coverage gate; per-leaf SPD systems are assembled resident on device; solve stays host-side pending Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-oracle — device per-leaf matrix assembly vs CPU pair-stat reference</name>
  <files>crates/cb-backend/src/kernels/pairwise_deriv_test.rs, crates/cb-backend/src/kernels.rs</files>
  <behavior>
    - Test 1: for a small fixture (2 leaves, a handful of pairs), the device-assembled per-leaf matrix + RHS equal the CPU reference assembly within F64_TOL (ε=1e-4) over equal-length buffers.
    - Test 2: an uncovered config path yields `Ok(None)` (assembly not attempted), not a fabricated result.
    - Test 3: empty-`n` input constructs the no-op launcher without reading a 0-length handle (HIP fault guard).
  </behavior>
  <read_first>
    - MODIFY targets: new `crates/cb-backend/src/kernels/pairwise_deriv_test.rs`; register in `crates/cb-backend/src/kernels.rs` (mvs registration block idiom at :2869-2877: `pub(crate) mod` + `#[cfg(test)] mod *_test`).
    - PATTERNS.md analog: "Serial-CPU self-oracle test harness" — copy the `apply_leaf_delta.rs` device `run_*` + `cpu_*` reference structure, `F64_TOL`, empty-`n` no-launch guard; non-wgpu gate `#![cfg(not(feature = "wgpu"))]`; assertions skip off rocm/cuda to avoid a CPU-vs-CPU false-pass (WR-01).
    - CPU oracle: `crates/cb-compute/src/leaf.rs::calculate_pairwise_score` / `crates/cb-train/src/pairwise_leaves.rs` pair-stat assembly (the frozen ≤1e-5 reference).
  </read_first>
  <action>
    Create `pairwise_deriv_test.rs` in dedicated test file form (source/test separation — NO `#[cfg(test)] mod tests` inside production kernels). Build a serial-CPU reference that assembles the same packed per-leaf system from raw pair der/weight stats, run the device assembly, and assert max abs/rel divergence ≤ `F64_TOL` over equal-length buffers. Register the module in `kernels.rs` using the `pub(crate) mod` + `#[cfg(test)] mod pairwise_deriv_test` idiom. Gate the whole test file `#![cfg(not(feature = "wgpu"))]` and skip the numeric assertion when neither rocm nor cuda is active (record-only), per the WR-01 CPU-vs-CPU false-pass guard.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --lib pairwise_deriv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pairwise_deriv_test.rs` exists as a dedicated test file; no `#[cfg(test)] mod tests` block is added to any production kernel file.
    - `cargo test -p cb-backend --lib pairwise_deriv` passes; max abs divergence printed ≤ 1e-4 when a device backend is active, record-only otherwise.
    - The empty-`n` case asserts the launcher is constructed without a 0-length `read_one`.
  </acceptance_criteria>
  <done>Device per-leaf matrix assembly is self-oracled against the CPU pair-stat reference at ε=1e-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host↔device seam | `Runtime::grow_tree_on_device` crosses plain host structs only; training params/seeds pre-validated at the `cb-train` seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Denial of Service | pairwise kernel launch grid/buffer sizing | mitigate | checked casts + bounded grid (existing `pair_hist_binsums_len_checked` pattern) applied to the assembly driver |
| T-13-02 | Tampering | pairwise coverage gate | accept | `Ok(None)` fallback never fabricates a device result; CPU path is the correctness reference |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | phase adds NO new external package (RESEARCH Package Legitimacy: N/A); no install task |
</threat_model>

<verification>
- `cargo test -p cb-backend --lib pairwise` and `... pairwise_deriv` green on the CPU/serial path.
- `grep -v '^#' crates/cb-backend/Cargo.toml | grep -c 'cb-train'` == 0 (landmine).
- Scalar path byte-unchanged: `cargo test -p cb-train -p cb-compute` (no-regression, GPUT-14/D-04).
</verification>

<success_criteria>
PairLogit reaches the device pairwise path behind a coverage gate; per-leaf SPD systems assembled resident on device; assembly self-oracled ≤1e-4; no `cb-train` dep leaks into `cb-backend`; scalar host path byte-unchanged. Kaggle CUDA ε=1e-4 + BENCH-02 sign-off is deferred to Plan 10 (human-gated).
</success_criteria>

<output>
Create `.planning/phases/13-pairwise-ranking-multiclass-ordered-langevin-device-coverage/13-01-SUMMARY.md` when done.
</output>
