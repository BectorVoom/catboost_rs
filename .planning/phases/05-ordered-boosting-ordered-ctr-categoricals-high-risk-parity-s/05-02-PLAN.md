---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/cb-train/src/candidates.rs
  - crates/cb-train/src/candidates_test.rs
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/tree_test.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-train/tests/one_hot_oracle_test.rs
autonomous: true
requirements: [ORD-04]
must_haves:
  truths:
    - "A categorical column with 1 < cardinality <= one_hot_max_size routes to the one-hot encoding path"
    - "A categorical column with cardinality > one_hot_max_size does NOT route to one-hot (CTR path, built in later waves)"
    - "A one-hot-only model trains and predicts ≤1e-5 vs upstream with NO permutation present (D-04 isolation)"
    - "Threshold is inclusive for one-hot at count == one_hot_max_size, exclusive above (no off-by-one)"
  artifacts:
    - path: "crates/cb-train/src/candidates.rs"
      provides: "one-hot vs CTR path selection on learn-set cardinality"
      contains: "one_hot_max_size"
    - path: "crates/cb-train/tests/one_hot_oracle_test.rs"
      provides: "one_hot_threshold unit + one_hot_predict oracle ≤1e-5"
      contains: "one_hot"
  key_links:
    - from: "crates/cb-train/src/candidates.rs"
      to: "crates/cb-train/src/tree.rs"
      via: "OneHot candidate emitted into split enumeration"
      pattern: "OneHot"
    - from: "crates/cb-train/src/tree.rs"
      to: "cb-data::calc_cat_feature_hash"
      via: "categorical value hashing for one-hot bucket identity"
      pattern: "calc_cat_feature_hash"
---

<objective>
The narrowest first slice (D-04): one-hot encoding for low-cardinality categoricals (ORD-04), riding the EXISTING plain boosting + oblivious trees with NO permutation and NO CTR math. This isolates the encoding-path selection (`one_hot_max_size`) before any ordering math enters the phase.

Purpose: Per D-04/D-05 the additive isolation ladder starts here — a one-hot-only model has genuinely no permutation (RESEARCH Pitfall 2: `IsPermutationNeeded` is false when all cats ≤ one_hot_max_size). Locking it now means a later per-object divergence can never be entangled with the encoding-selection logic.
Output: `candidates.rs` path-selection, categorical one-hot splits in `tree.rs`, and the threshold + predict oracles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-PATTERNS.md

@crates/cb-train/src/tree.rs
@crates/cb-train/src/boosting.rs
@crates/cb-data/src/cat_hash.rs

<interfaces>
From crates/cb-train/src/tree.rs (extend Candidate enumeration; strict first-wins tie-break):
```rust
pub struct Candidate { pub feature: usize, pub border: f64, pub score: f64 }
// SelectBestCandidate uses strict `if (gain > bestGain)` over FIXED order (feature asc, border asc); FIRST max wins. Do NOT use >=.
```

From crates/cb-data/src/cat_hash.rs (categorical hashing single source — D Carried-Forward, NEVER a model ctr_data hash_map):
```rust
pub fn calc_cat_feature_hash(s: &str) -> u32;     // CityHash64 & 0xffffffff
pub struct PerfectHash { /* first-seen perfect-hash bins */ }
```

From crates/cb-train/src/boosting.rs (the train driver the one-hot slice rides unchanged for the no-permutation path):
```rust
pub struct BoostParams { /* extend with one_hot_max_size; per-field doc-comment-with-upstream-citation style */ }
pub fn train<R: Runtime>(/* ... */) -> CbResult<...>;
```
</interfaces>
</context>

<artifacts_this_phase_produces>
- `cb-train::candidates` module (new) — one-hot vs CTR path selection; `OneHot` candidate variant
- `BoostParams.one_hot_max_size` field (new)
- categorical one-hot split support in `cb-train::tree`
- `crates/cb-train/tests/one_hot_oracle_test.rs` (new)
- consumes `one_hot_cat/` fixture from 05-01
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One-hot vs CTR path selection on learn-set cardinality</name>
  <read_first>crates/cb-train/src/tree.rs, crates/cb-train/src/candidates_test.rs, crates/cb-train/src/lib.rs, crates/cb-data/src/cat_hash.rs</read_first>
  <files>crates/cb-train/src/candidates.rs, crates/cb-train/src/candidates_test.rs, crates/cb-train/src/lib.rs</files>
  <behavior>
    - count == one_hot_max_size → OneHot path (inclusive boundary, greedy_tensor_search.cpp:182 skip-if-`>`)
    - count == one_hot_max_size + 1 → NOT one-hot (CTR path, deferred to later waves)
    - count <= 1 → neither (skip-if-`<=1`)
    - cardinality is the LEARN-SET-only unique count (RESEARCH Pitfall 3)
  </behavior>
  <action>Create `crates/cb-train/src/candidates.rs` (new module; declare `mod candidates;` + `pub use` in lib.rs per existing module style). Implement one-hot vs CTR routing: one-hot when `1 < count <= one_hot_max_size` (inclusive), CTR when `count > one_hot_max_size` (exclusive) — RESEARCH Pitfall 3 / Pattern §candidates.rs. Cardinality = learn-set-only unique categorical-value count via `cb_data::calc_cat_feature_hash` + first-seen perfect-hash bins (NEVER a model ctr_data hash_map — Anti-Pattern). Emit a `OneHot` candidate the tree growth consumes. Add `one_hot_max_size` to `BoostParams` (RESEARCH default 2; pin explicitly, NOT auto — Pitfall 6). Checked access only (`.get`/`try_from`), no unwrap/expect/panic/raw-index (INFRA-02); no anyhow. Tests in candidates_test.rs (source/test separation): exact boundary cases above.</action>
  <verify>
    <automated>cargo test -p cb-train one_hot_threshold</automated>
  </verify>
  <done>candidates.rs routes count==one_hot_max_size to one-hot (inclusive), +1 to CTR (exclusive), <=1 to neither; cardinality is learn-set unique via calc_cat_feature_hash; one_hot_max_size in BoostParams; candidates_test.rs covers all boundaries; no unwrap/anyhow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Categorical one-hot splits + one-hot-only predict oracle ≤1e-5</name>
  <read_first>crates/cb-train/src/tree.rs, crates/cb-train/src/tree_test.rs, crates/cb-train/src/boosting.rs, crates/cb-oracle/fixtures/one_hot_cat/config.json</read_first>
  <files>crates/cb-train/src/tree.rs, crates/cb-train/src/tree_test.rs, crates/cb-train/tests/one_hot_oracle_test.rs</files>
  <behavior>
    - A one-hot candidate produces an oblivious split on "cat value == bucket k" (forward-bit leaf index, consistent with existing leaf_index)
    - Training the one_hot_cat fixture (NO permutation present) yields splits/leaf-values/staged-approx matching upstream
    - Final prediction over the fixture matches upstream model.json ≤1e-5
  </behavior>
  <action>Extend `crates/cb-train/src/tree.rs` to support categorical one-hot splits (split on `cat == bucket k`), feeding the `OneHot` candidate from Task 1 into the existing oblivious split enumeration + strict first-wins tie-break (tree.rs header discipline — do NOT use `>=`). Forward-bit leaf indexing consistent with the existing `leaf_index`. The train loop rides the EXISTING plain boosting unchanged — assert NO permutation is generated for a one-hot-only fixture (D-04 isolation; RESEARCH Pitfall 2: warning sign is a permutation present in a one-hot-only fixture). Create `crates/cb-train/tests/one_hot_oracle_test.rs`: train over the 05-01 `one_hot_cat/` fixture and assert splits (Stage::Splits), leaf values (Stage::LeafValues), staged approx (Stage::StagedApprox) and final prediction ≤1e-5 via `cb_oracle::compare_stage`. All float sums via `cb_core::sum_f64` (D-08); checked access, no unwrap/anyhow. tree_test.rs gets unit coverage for a single categorical one-hot split.</action>
  <verify>
    <automated>cargo test -p cb-train one_hot</automated>
  </verify>
  <done>tree.rs grows categorical one-hot splits with forward-bit leaf index + strict first-wins; one_hot_oracle_test trains the one_hot_cat fixture with NO permutation present and locks splits/leaves/staged/predict ≤1e-5 vs upstream; sums via sum_f64; no unwrap/anyhow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pool categorical column → cb-train one-hot routing | learn-set categorical values (already validated at ingestion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | candidates.rs cardinality count | mitigate | learn-set unique count via calc_cat_feature_hash; checked `.get`, u32::try_from on uniq bound (reuse Plan 02-04 u32::MAX guard); no raw index |
| T-05-02-02 | Denial of Service | tree.rs one-hot split enumeration | mitigate | candidate count bounded by cardinality ≤ one_hot_max_size (small); existing DepthExceeded guard applies; no unbounded allocation |
| T-05-02-SC | Tampering | no package installs | accept | RESEARCH Package Legitimacy Audit: N/A (no external crates added this phase) |
</threat_model>

<verification>
- `cargo test -p cb-train one_hot` green (threshold unit + predict oracle).
- No permutation generated for the one-hot-only fixture (D-04 isolation assertion).
- All float sums via sum_f64.
</verification>

<success_criteria>
- ORD-04 success criterion (one_hot_max_size threshold selects the correct encoding path) oracle-locked.
- One-hot-only model trains+predicts ≤1e-5 with no ordering math present.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-02-SUMMARY.md` when done.
</output>
