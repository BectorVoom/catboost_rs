---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 18
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [ORD-01, ORD-05, ORD-03]
files_modified:
  - crates/cb-train/src/fold.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/ctr/ctr_feature.rs
  - crates/cb-train/src/ctr/online.rs
  - crates/cb-train/tests/multi_permutation_fold_oracle_test.rs
  - crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs
  - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
  - crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs
  - crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs

must_haves:
  truths:
    - "The pc=4 (production-default permutation_count=4) train→predict path reproduces catboost 1.2.10 RawFormulaVal predictions ≤1e-5 across all objects and all 5 trees (SC-1 / ORD-01 closure, bar (c))"
    - "create_folds produces the bit-exact upstream AveragingFold permutation: pc=4 == [11,18,15,29,16,12,0,7,19,27,4,3,5,17,14,25,9,20,8,23,6,28,26,24,2,13,21,22,10,1]; pc=1 == [10,17,25,3,6,...]"
    - "The per-iteration STRUCTURE fold cycles takenFold = Folds[Rand.GenRand() % learning_folds]; the pc=4 fold sequence is [0,2,0,2,2] producing per-tree structures [A,B,A,B,B] = [6,0,10,14],[8,8,0,14],[6,0,10,14],[8,8,0,14],[8,8,0,14]; leaf VALUES stay on the FIXED AveragingFold"
    - "materialize_ctr_feature reproduces ComputeOnlineCTRs(AveragingFold) bit-exact: the averaging-order ui8 bins == [7,7,7,11,3,7,2,11,7,12,9,12,11,10,3,2,11,1,7,13,5,7,8,11,1,9,1,10,1,13]"
    - "Every blast-radius CTR oracle re-pinned to the CORRECT (non-compensating) upstream value remains green: pc=1/pc=2/pc=4 partition locks integer-exact, tensor_ctr_e2e ≤1e-5, ordered_boost_e2e ≤1e-5"
    - "No green oracle regressed: cb-train lib 130/130, cb-model 3/3, full cb-train integration suite has 0 FAILED"
  artifacts:
    - path: "crates/cb-train/src/fold.rs"
      provides: "create_folds AveragingFold permutation rule corrected to learning_folds FULL Fisher-Yates passes (shuffle starts at cc=29 pc=1 / cc=87 pc=4)"
      contains: "create_folds"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "per-iteration structure-fold cycling (Folds[Rand.GenRand() % learning_folds]) over the CTR structure search; leaf values on the fixed AveragingFold"
      contains: "train_inner"
    - path: "crates/cb-train/src/ctr/ctr_feature.rs"
      provides: "materialize_ctr_feature permutation-order bucket reindex matching ComputeOnlineCTRs"
      contains: "materialize_ctr_feature"
    - path: "crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs"
      provides: "committed bar-(c) pc=4 e2e prediction oracle ≤1e-5"
      contains: "permutation_count_four_predictions_match_upstream"
  key_links:
    - from: "crates/cb-train/src/fold.rs::create_folds"
      to: "crates/cb-train/src/permutation.rs::shuffle_in_place"
      via: "averaging shuffle begins after learning_folds full Fisher-Yates passes on the persistent TFastRng64"
      pattern: "shuffle_in_place"
    - from: "crates/cb-train/src/boosting.rs::train_inner"
      to: "crates/cb-train/src/tree.rs::greedy_tensor_search_oblivious_with_ctr"
      via: "per-iteration structure fold selection over materialized CTR columns"
      pattern: "greedy_tensor_search_oblivious_with_ctr"
    - from: "crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs"
      to: "crates/cb-train/tests/fixtures/multi_permutation_fold/predictions_pc4.npy"
      via: "compare_stage(Stage::Predictions) ≤1e-5 over predict_raw_cat"
      pattern: "predictions_pc4"
---

<objective>
Close the single remaining Phase-5 blocking gap (SC-1 / ORD-01, developer-escalated):
the production-default `permutation_count=4` AveragingFold path does not reproduce
catboost 1.2.10 end-to-end. Bars (a) pc=4 partition counts, (b) hard-equality
oracle, (d) pc=1/pc=2 locks + tensor_ctr_e2e + ordered_boost_e2e, (e)
fold-creation ground truth are currently GREEN, but on a COMPENSATING-ERROR basis
(a wrong averaging permutation + wrong online-CTR bins that coincidentally yield
the right partition COUNTS, see 05-17-SUMMARY and `live_trainer_ctr_bins_blocker.json`).

This plan transcribes the committed live-instrumented catboost 1.2.10 ground
truth (05-17, authorized by the 2026-06-15 CONTEXT revision) into production
cb-train code, fixing THREE COUPLED errors SIMULTANEOUSLY (fixing any one in
isolation regresses the green locks), re-pinning every blast-radius oracle to the
CORRECT upstream value, and committing the bar-(c) pc=4 e2e prediction oracle as
the SC-1 closure gate.

Reconciliation with P1/D-08 ("Python-reachable floor, no C++ instrumentation"):
the C++ instrumentation was ALREADY performed and user-authorized (CONTEXT.md
2026-06-15 revision; 05-17 built the instrumented trainer). D-08 is SATISFIED —
CI reads only the committed frozen fixtures (`live_trainer_structure_fold.json`,
`live_trainer_ctr_bins_blocker.json`, `rng_draw_accounting.json`,
`predictions_pc4.npy`); no live build runs in CI. This plan is NOT "do C++
instrumentation" — it is "transcribe the committed ground truth."

Purpose: SC-1 / ORD-01 says "reproduces upstream permutations exactly" with no
permutation_count carve-out, and pc=4 is the production default. This is the last
open Phase-5 gap.
Output: corrected create_folds + structure-fold cycling + CTR materialization;
re-pinned blast-radius oracles; committed pc=4 e2e oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase-5 gap source + authorization
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-17-SUMMARY.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/deferred-items.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CTR-LEAF-VALUE-RESEARCH.md

# Committed ground-truth fixtures (the EXACT targets — read these before coding)
@crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json
@crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_structure_fold.json
@crates/cb-train/tests/fixtures/multi_permutation_fold/rng_draw_accounting.json
@crates/cb-train/tests/fixtures/multi_permutation_fold/leaf_weights.json

# Production source under change
@crates/cb-train/src/fold.rs
@crates/cb-train/src/boosting.rs
@crates/cb-train/src/ctr/ctr_feature.rs
@crates/cb-train/src/ctr/online.rs

# Tests in the blast radius (re-pin) + the anchor (commit)
@crates/cb-train/tests/multi_permutation_fold_oracle_test.rs
@crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs
@crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs

# Upstream oracle source (read line-by-line for the CTR + fold mechanism)
@catboost-master/catboost/private/libs/algo/online_ctr.cpp
@catboost-master/catboost/private/libs/algo/online_ctr.h
@catboost-master/catboost/private/libs/algo/train.cpp
@catboost-master/catboost/private/libs/algo/fold.cpp
@catboost-master/catboost/private/libs/algo/learn_context.cpp
</context>

<critical_constraint>
## The Three Coupled Errors Must Be Fixed Together (NON-NEGOTIABLE)

Per 05-17-SUMMARY and `live_trainer_ctr_bins_blocker.json`: the current pc=1/pc=2/pc=4
partition locks and tensor_ctr_e2e pass on a COMPENSATING combination of a wrong
averaging permutation AND wrong online-CTR bins. Correcting ONLY the permutation
regresses `[6,0,7,17]`→`[6,0,8,16]` (05-17 confirmed). Therefore Tasks 1 and 2
land in the SAME wave (this single plan); a mid-wave RED state where intermediate
locks are red is EXPECTED. The FINAL state must be all-green with the CORRECT
(non-compensating) upstream values.

## Re-pinning Means Asserting the CORRECT Upstream Value — Never Weakening

Re-pinning is re-deriving each oracle's "expected" from the COMMITTED upstream
catboost 1.2.10 fixtures (integer-exact partitions, ≤1e-5 predictions). It is NOT
loosening tolerance, NOT `#[ignore]`, NOT `assert_ne`, NOT a pinned-delta record,
NOT fabricating a pass. Any oracle whose "expected" was a compensating cb-train
value MUST be re-derived from the committed upstream fixtures. If after the three
production fixes an oracle still cannot reach its committed upstream target,
STOP and surface it — do not weaken the oracle.

## CLAUDE.md Discipline

Tests live ONLY in dedicated `_test.rs` files (no `#[cfg(test)]` in production
source — the existing `#[cfg(test)] #[path=...] mod tests` sibling-mount pattern in
fold.rs is permitted and unchanged). No `unwrap()`/`expect()`/panic/raw-index in
production code (thiserror via CbError/CbResult; tests keep their existing
`#![allow(clippy::unwrap_used, ...)]`). All parity-critical float summation routes
through `cb_core::sum_f64` (D-08). Categorical hashing via
`cb_data::calc_cat_feature_hash`, never a model's ctr_data hash_map.
</critical_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Correct the create_folds averaging permutation rule AND wire per-iteration structure-fold cycling</name>
  <files>crates/cb-train/src/fold.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-train/src/fold.rs (the WHOLE file — the create_folds learning-permutation-needed path at lines 282-330 is the rule under change; the current rule consumes ONE gen_rand per learning-fold position then one Fisher-Yates pass — WRONG)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json (`upstream_avg_perm` = the EXACT pc=4 target [11,18,15,29,16,12,0,7,19,27,4,3,5,17,14,25,9,20,8,23,6,28,26,24,2,13,21,22,10,1]; the findings array documents shuffle-start cc=29 pc=1 / cc=87 pc=4 = learning_folds FULL Fisher-Yates passes)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_structure_fold.json (`discovered_rule`: takenFold = Folds[Rand.GenRand() % foldCount], foldCount==learning_folds; pc=4 sequence [0,2,0,2,2]; per-iteration callcount_before 116/140/166/190/212 and sum_leaf_weights [6,0,10,14]/[8,8,0,14]/...)
    - crates/cb-train/src/boosting.rs (the train_inner tree loop 1295-1453: the CTR structure search at 1380-1404 currently uses a SINGLE fixed `materialized_ctr_features` column built under cat_learn_permutation (the identity Folds[0]) for EVERY iteration; PRE_TREE_DRAWS=2 / draws_active at 1290,1310 — note draws_active is FALSE for the bootstrap=No/random_strength=0 tensor_ctr config, so the structure-fold-pick gen_rand is NOT currently firing)
    - crates/cb-train/src/permutation.rs (shuffle_in_place — the Fisher-Yates pass that consumes one uniform() draw per position i in 1..n)
    - catboost-master/catboost/private/libs/algo/learn_context.cpp (fold-creation loop, shuffle = foldIdx != 0; per-fold pre-averaging RNG consumption) and fold.cpp (Shuffle())
    - catboost-master/catboost/private/libs/algo/train.cpp (line ~208: takenFold = Folds[Rand.GenRand() % foldCount]; the per-iteration structure-fold selection)
  </read_first>
  <action>
PART A — fold.rs create_folds averaging permutation rule (REPLACES the 05-17 rule).
The committed 05-17 rule advances the averaging Fisher-Yates shuffle at RNG
call-count == learning_folds (the cc=1/cc=3 rule in rng_draw_accounting.json). The
LATER live-trainer instrumentation (05-17-SUMMARY §4, live_trainer_ctr_bins_blocker.json
`findings`) PROVED that rule wrong: the averaging shuffle begins after `learning_folds`
FULL Fisher-Yates passes on the persistent TFastRng64 — RNG GetCallCount 29 at pc=1
and 87 at pc=4. (One Fisher-Yates pass over N=30 consumes 29 uniform() draws: i runs
1..30, drawing rng.uniform(i+1) at each step. learning_folds=1 → 29 = 1×29; pc=4
learning_folds=3 → 87 = 3×29.) Rewrite the learning-permutation-needed branch so:
  * Each of the `learning_folds` fold positions (including the identity Folds[0])
    performs ONE FULL Fisher-Yates pass on the persistent rng (advancing it by 29
    draws for N=30) BEFORE the averaging fold's pass. Folds[0] keeps the IDENTITY
    permutation as its stored `permutation` (shuffle = foldIdx != 0,
    learn_context.cpp:524) — but the RNG must still advance by one full pass so the
    averaging shuffle starts at the correct call-count. Use shuffle_in_place to
    advance the rng (discard the result for the identity fold and for the unconsumed
    learning folds 1..learning_folds, whose permutations no oracle reads). Document
    that the discovered cc=29/87 = learning_folds full Fisher-Yates passes supersedes
    the 05-17 cc==learning_folds rule, citing live_trainer_ctr_bins_blocker.json.
  * The AveragingFold's shuffle (shuffle_in_place) is then the (learning_folds+1)-th
    full pass and MUST equal `upstream_avg_perm` ([11,18,15,29,16,...] at pc=4;
    [10,17,25,3,6,...] at pc=1) bit-exact.
Preserve the `!permutation_needed_for_learning` numeric path EXACTLY (byte-identical
regression anchor — do not touch it). Update the create_folds doc comment block
(lines 208-305) to describe the learning_folds-full-passes rule and cite the
superseding fixture; remove the now-incorrect cc==learning_folds prose.

PART B — boosting.rs per-iteration structure-fold cycling. Today train_inner
materializes ONE structure CTR column under the identity Folds[0] and reuses it for
every tree (a FIXED structure fold). Upstream selects a DIFFERENT structure fold per
iteration: takenFold = Folds[Rand.GenRand() % foldCount] with foldCount ==
learning_folds (train.cpp:208), producing the pc=4 structure-fold sequence [0,2,0,2,2]
and the per-tree structure pattern [A,B,A,B,B]. Implement:
  * Build ALL `learning_folds` learning-fold permutations from create_folds (not just
    the identity Folds[0]) so each can serve as a structure permutation. The
    create_folds learning folds 1..learning_folds currently carry the identity
    placeholder; PART A must instead store their actual shuffled permutations (each is
    one full Fisher-Yates pass, fold idx 1..learning_folds), since the structure-fold
    cycling now READS them. Fold[0] stays identity. Confirm against
    live_trainer_structure_fold.json that fold 2's permutation (pc=4) yields structure
    B [8,8,0,14] with borders {3.999,7.999}.
  * Materialize a structure CTR column PER learning fold (one materialize_ctr_feature
    per fold permutation, reusing absolute_projections) so the per-iteration selected
    fold has its column ready. Keep the SINGLE averaging-fold leaf-value column
    (cat_averaging_permutation) unchanged — leaf VALUES always use the FIXED
    AveragingFold (approx_calcer.cpp:1082).
  * Inside the `for iter in 0..params.iterations` loop, on the has_ctr branch, draw
    the structure-fold index `taken = (rng.gen_rand() % learning_folds as u64) as usize`
    EACH iteration BEFORE the structure search, and pass that fold's materialized CTR
    column into greedy_tensor_search_oblivious_with_ctr. The gen_rand draw must fire
    UNCONDITIONALLY on the has_ctr path (independent of draws_active — upstream draws
    the fold pick every iteration even at bootstrap=No). For learning_folds==1
    (pc=1/pc=2) the modulo is always 0 so the structure fold is always Folds[0]
    IDENTITY — byte-identical to today (no pc=1/pc=2 regression; this is the key
    invariant). Reconcile this new per-iteration gen_rand with the existing
    PRE_TREE_DRAWS / draws_active accounting (1290-1314) so the bootstrap RNG phase
    stays aligned for the numeric path; the numeric path has no CTR candidates so the
    new draw never fires there.
  * Bake leaf_weights from the AVERAGING-fold partition (unchanged — research Open-Q5)
    so model leaf_weights remain the AveragingFold counts.
Verify against live_trainer_structure_fold.json per-iteration sum_leaf_weights that
the pc=4 tree structures are [6,0,10,14],[8,8,0,14],[6,0,10,14],[8,8,0,14],[8,8,0,14].
NOTE this task ALONE will not make the e2e green (the CTR bins are still wrong until
Task 2); intermediate locks may be RED here — that is the expected coupled mid-wave state.
  </action>
  <verify>
    <automated>cargo build -p cb-train 2>&1 | tail -5 && cargo test -p cb-train --lib 2>&1 | tail -5</automated>
  </verify>
  <done>cb-train compiles; the numeric/one-hot/ordered lib unit tests still pass (the cat-CTR e2e oracles may be RED pending Task 2 — that is expected). create_folds, when driven with N=30/seed=0/learning_folds-full-passes, produces upstream_avg_perm bit-exact (provable by a scratch assertion or the Task-2 re-pinned oracle). The structure-fold cycling draws Folds[Rand.GenRand() % learning_folds] per iteration and is a no-op (always fold 0) at learning_folds==1.</done>
</task>

<task type="auto">
  <name>Task 2: Fix materialize_ctr_feature to reproduce ComputeOnlineCTRs(AveragingFold) bit-exact AND re-pin every blast-radius oracle</name>
  <files>crates/cb-train/src/ctr/ctr_feature.rs, crates/cb-train/src/ctr/online.rs, crates/cb-train/tests/multi_permutation_fold_oracle_test.rs, crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/src/ctr/ctr_feature.rs (the WHOLE file — `materialize_ctr_feature`: step 2 remaps combined keys to dense first-seen bins in OBJECT order, step 3 runs online_ctr_prefix_binclf, step 4 quantizes via calc_ctr_online_bin. The OBJECT-order remap is the divergence root.)
    - crates/cb-train/src/ctr/online.rs (online_ctr_prefix_binclf — the read-before-increment prefix; reused, do not change its math)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json (`upstream_avg_ctr_bins_avg_order` = the EXACT target ui8 bins [7,7,7,11,3,7,2,11,7,12,9,12,11,10,3,2,11,1,7,13,5,7,8,11,1,9,1,10,1,13]; `upstream_leaf_indices_treeB_avg_order` the per-doc leaf indices; the `findings` array: first 4 bins match then diverge at position 4 — ours 7, upstream 3 — because the bucket reindex order differs)
    - catboost-master/catboost/private/libs/algo/online_ctr.cpp (ComputeOnlineCTRs 600-770: CopyCatColumnToHash reindexes the cat column UNDER foldLearnPermutationFeaturesSubset — the fold permutation — THEN ComputeReindexHash assigns dense bucket IDs in PERMUTATION order; CalcOnlineCTRSimple/CalcQuantizedCtrs 271-440 walk permutedTargetClass in permutation order, read-before-increment, write featureData in permutation order; CalcCTR online_ctr.h:128-131 = (ui8)((ctr+shift)/norm*borderCount), ctr=(good+prior)/(total+1), Borders prior 0.5 → shift 0 norm 1, borderCount 15)
    - crates/cb-train/tests/multi_permutation_fold_oracle_test.rs (averaging_partition() at 185-199 rebuilds the partition via feature0_bins_and_classes() OBJECT-order remap + online_ctr_prefix_binclf + calc_ctr_online_bin; self_derived_averaging_permutation() at 211-225 bakes in the OLD cc==learning_folds rule. BOTH must be re-derived.)
    - crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs (the standalone fold-permutation oracle — read to see whether its assertions still hold under the corrected rule; re-pin if it asserts the old permutation)
  </read_first>
  <action>
PART A — materialize_ctr_feature bucket reindex (the architectural CTR fix). The
position-4 divergence (ours bin 7, upstream bin 3) is the bucket ENUMERATION order:
materialize_ctr_feature step 2 assigns dense first-seen bin IDs walking documents in
OBJECT order, but upstream ComputeReindexHash (online_ctr.cpp ~700) assigns dense
bucket IDs walking the cat column reindexed UNDER the fold permutation — i.e. in
PERMUTATION order. Because the online read-before-increment then keys on those bucket
IDs, a different first-seen assignment shifts which prefix counts a document reads,
moving its quantized bin. Fix step 2 so the dense first-seen remap is built by walking
the combined keys in PERMUTATION order (iterate `permutation` positions, assign the
next bin ID the FIRST time each combined key is seen in permutation order), then map
every document's combined key through that permutation-order remap to its bucket. Keep
steps 3 (online_ctr_prefix_binclf over the now permutation-order bucket IDs) and 4
(calc_ctr_online_bin truncate-and-clamp) unchanged — the prefix math and CalcCTR
quantizer are already bit-correct (the blocker fixture confirms the first 4 bins match,
isolating the fault to the reindex order). Do NOT change online.rs's prefix loop; if a
shared permutation-order-reindex helper is cleaner, add it to ctr_feature.rs (or a new
pub(crate) fn in online.rs) without altering online_ctr_prefix_binclf's signature or
math. Confirm bit-exact: materialize_ctr_feature over the single feature-0 projection
under the corrected averaging permutation must yield bins ==
`upstream_avg_ctr_bins_avg_order` [7,7,7,11,3,7,2,11,7,12,9,...] (the blocker fixture's
averaging-ORDER sequence; the column stores bins in OBJECT order — convert via the
permutation when comparing, matching the fixture's `_avg_order` labeling).

PART B — re-pin multi_permutation_fold_oracle_test.rs. With Task 1 + PART A the
averaging permutation is now [11,18,15,29,...] (pc=4) / [10,17,25,3,6,...] (pc=1) and
the CTR bins are bit-exact. Re-derive every assertion from the CORRECT committed
upstream fixtures:
  * averaging_partition(): rebuild it on the CORRECTED materialize_ctr_feature
    permutation-order reindex (or assert directly that the production
    materialize_ctr_feature bins == upstream_avg_ctr_bins_avg_order and that the
    tree-0 leaf partition over the {2.999,7.999} borders == upstream leaf_weights). It
    MUST still produce the committed leaf_weights.json values: pc=1 [6,0,7,17], pc=2
    [6,0,7,17], pc=4 [6,0,10,14] — but now via the CORRECT permutation+bins (not the
    compensating pair). The integer-exact compare_permutation gate is preserved.
  * self_derived_averaging_permutation(): re-derive to the learning_folds-FULL-passes
    rule (learning_folds full Fisher-Yates passes, then the averaging pass) so the
    secondary cross-check agrees with the corrected create_folds. Update its doc
    comment (currently says "cc == learning_folds" — correct to "learning_folds full
    Fisher-Yates passes, cc=29/87"; the existing anchor test doc framing is stale per
    the planning context).
  * multi_permutation_count_four_averaging_matches_catboost_1_2_10: keep the HARD
    integer-exact equality vs upstream [6,0,10,14] (already present; just confirm it
    passes on the corrected path).
  * Add an integer-exact assertion that create_folds' pc=4 averaging permutation ==
    upstream_avg_perm [11,18,15,29,16,12,0,7,...] (read from
    live_trainer_ctr_bins_blocker.json) via compare_permutation — this is the new
    bit-exact permutation gate the 05-17 ground truth enables (previously only the
    partition COUNTS were checked).
  * Fix the stale module doc-comment (the "averaging shuffle starts at call-count ==
    learning_folds" prose at lines 43-64) to the learning_folds-full-passes rule.

PART C — re-pin averaging_fold_permutation_oracle_test.rs only IF it asserts the old
permutation/draw rule (it locks AveragingFold == fisher_yates_permutation(30,0) for
pc=1 per 05-12). Under the corrected rule pc=1 averaging == [10,17,25,3,6,...]; if the
test's expectation no longer holds, re-pin it to the corrected pc=1 permutation
(derive from create_folds / the committed fixture), preserving integer-exact
compare_permutation, NO weakening. If it still holds (pc=1 unchanged because
learning_folds==1 → one full pass == fisher_yates_permutation(30,0)), leave it.
Determine which by reading the test and the cc=29 pc=1 accounting.

All re-pinned "expected" values come from the COMMITTED upstream fixtures
(leaf_weights.json, live_trainer_ctr_bins_blocker.json) — never from a cb-train
computed value. No #[ignore], no assert_ne, no pinned-delta, no loosened tolerance.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test multi_permutation_fold_oracle_test --test averaging_fold_permutation_oracle_test 2>&1 | tail -20</automated>
  </verify>
  <done>materialize_ctr_feature over feature-0 under the corrected averaging permutation produces bins == upstream_avg_ctr_bins_avg_order bit-exact. multi_permutation_fold_oracle_test passes ALL tests integer-exact against the CORRECT upstream fixtures: pc=1/pc=2 [6,0,7,17], pc=4 [6,0,10,14], AND the new pc=4 averaging permutation == upstream_avg_perm [11,18,15,29,...] gate. averaging_fold_permutation_oracle_test green (re-pinned if needed). No oracle weakened.</done>
</task>

<task type="auto">
  <name>Task 3: Commit/wire the bar-(c) pc=4 e2e oracle, re-pin remaining CTR oracles, and prove no regression</name>
  <files>crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs, crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs, crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs (the UNCOMMITTED anchor — `permutation_count_four_predictions_match_upstream` trains tensor_ctr_params_pc4() via train_cat and asserts predict_raw_cat ≤1e-5 vs predictions_pc4.npy. Its module doc (lines 1-18) cites the OLD "AveragingFold shuffle begins at call-count == learning_folds" framing and `rng_draw_accounting.json` — correct it to the learning_folds-FULL-passes rule + the live-trainer ground truth.)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/predictions_pc4.npy (the ≤1e-5 target — the committed upstream pc=4 RawFormulaVal; 05-17 proved the instrumented trainer reproduced it bit-identical)
    - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs (the pc=1 tensor-CTR e2e ≤1e-5 — verify it stays green; its expected is the committed tensor_ctr_e2e/predictions.npy, unchanged. pc=1 path is byte-identical post-fix because learning_folds==1, so this should pass without re-pinning — CONFIRM.)
    - crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs (the ORD-02 ordered e2e ≤1e-5 — Plain-path / ordered-path, no cat CTRs; should be untouched by the CTR fix. CONFIRM green.)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_structure_fold.json (the [A,B,A,B,B] per-tree structure the pc=4 e2e must reproduce end-to-end)
  </read_first>
  <action>
PART A — commit + correct the bar-(c) anchor. The anchor test
multi_permutation_e2e_oracle_test.rs is already authored (untracked). Correct its
stale module doc-comment: replace "the AveragingFold shuffle begins at call-count ==
learning_folds" framing with "the averaging Fisher-Yates shuffle begins after
learning_folds FULL Fisher-Yates passes (RNG call-count 29 pc=1 / 87 pc=4), recovered
by the 05-17 live-trainer instrumentation (live_trainer_ctr_bins_blocker.json) and
the per-iteration structure-fold cycling Folds[Rand.GenRand() % learning_folds] (pc=4
sequence [0,2,0,2,2], live_trainer_structure_fold.json)". Keep the test body's
≤1e-5 assertion over predict_raw_cat vs predictions_pc4.npy. This file is committed by
the plan's git step (it is currently untracked — `git add` it). It is the SC-1 / ORD-01
closure gate.

PART B — re-pin the remaining blast-radius CTR oracles ONLY if they regressed.
tensor_ctr_e2e_oracle_test (pc=1) and ordered_boost_e2e_oracle_test should both stay
green because the pc=1 path is byte-identical after the fix (learning_folds==1 → the
structure-fold cycle is a no-op, the averaging permutation at pc=1 is one full pass).
Run them. If EITHER regressed, re-derive its expected from its COMMITTED upstream
fixture (tensor_ctr_e2e/predictions.npy, ordered_boost_e2e/predictions.npy) — these
fixtures are upstream truth and do not change; a regression means the production fix
shifted the pc=1 path, which must be reconciled (the pc=1 path MUST remain
byte-identical — if it did not, the fix is wrong, STOP and surface it rather than
re-pin to a cb-train value). Do NOT weaken either oracle.

PART C — full-suite regression gate. Run the COMPLETE cb-train integration suite +
lib + cb-model suite and confirm: 0 FAILED across all integration oracles (the
pre-existing Phase-3 #[ignore]d Bayesian/overfit/random_strength residuals are not
counted), cb-train lib 130/130, cb-model 3/3. The bar-(c) pc=4 e2e ≤1e-5 passes; every
re-pinned lock is integer-exact / ≤1e-5 against the CORRECT upstream value; no green
oracle regressed. This proves the coupled fix landed in a final all-green state with
non-compensating values.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test multi_permutation_e2e_oracle_test --test tensor_ctr_e2e_oracle_test --test ordered_boost_e2e_oracle_test --test multi_permutation_fold_oracle_test 2>&1 | tail -25 && cargo test -p cb-train --lib 2>&1 | tail -3 && cargo test -p cb-model 2>&1 | tail -3</automated>
  </verify>
  <done>multi_permutation_e2e_oracle_test::permutation_count_four_predictions_match_upstream PASSES ≤1e-5 vs predictions_pc4.npy (bar (c) / SC-1 closed). tensor_ctr_e2e 3/3, ordered_boost_e2e 2/2, multi_permutation_fold 5/5 (or more — the new permutation gate), cb-train lib 130/130, cb-model 3/3, full cb-train integration suite 0 FAILED. The anchor test is git-committed with corrected doc framing. No oracle #[ignore]'d/weakened/assert_ne'd/pinned-delta.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Symbols / tests / fixtures this plan CREATES or NEWLY COMMITS

**Newly committed test (was untracked):**
- `crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs` —
  `permutation_count_four_predictions_match_upstream` (the bar-(c) pc=4 e2e ≤1e-5
  closure gate, doc-framing corrected).

**New / changed production symbols:**
- `crates/cb-train/src/fold.rs::create_folds` — corrected averaging permutation rule
  (learning_folds full Fisher-Yates passes; averaging shuffle starts at cc=29 pc=1 /
  cc=87 pc=4); learning folds 1..learning_folds now carry their actual shuffled
  permutations (read by structure-fold cycling) while Folds[0] stays identity.
- `crates/cb-train/src/boosting.rs::train_inner` — per-iteration structure-fold
  cycling `taken = rng.gen_rand() % learning_folds` over per-fold materialized CTR
  columns; unconditional structure-fold gen_rand on the has_ctr path; leaf VALUES
  remain on the fixed AveragingFold column.
- `crates/cb-train/src/ctr/ctr_feature.rs::materialize_ctr_feature` —
  PERMUTATION-ORDER bucket reindex (replaces the object-order first-seen remap) so the
  online-CTR bins match ComputeOnlineCTRs(AveragingFold) bit-exact. (Possibly a new
  pub(crate) permutation-order-reindex helper in ctr_feature.rs or online.rs, with
  online_ctr_prefix_binclf's signature/math UNCHANGED.)

**New test assertions (re-pinned to CORRECT upstream values):**
- `multi_permutation_fold_oracle_test.rs` — new integer-exact pc=4 averaging
  permutation gate (== upstream_avg_perm [11,18,15,29,...]); re-derived
  averaging_partition / self_derived_averaging_permutation; corrected module doc.
- `averaging_fold_permutation_oracle_test.rs` — re-pinned to the corrected pc=1
  permutation IF its prior expectation no longer holds (else unchanged).

**No new fixtures generated** — all targets are the already-committed live-trainer
ground truth (`live_trainer_*.json`, `rng_draw_accounting.json`, `leaf_weights.json`,
`predictions_pc4.npy`). CI reads only these frozen fixtures (D-08/D-09 satisfied).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → CI test reads | The live-trainer ground-truth JSON/npy are read at test time; they are committed, frozen, offline-generated (no live build in CI). |
| (none new) | This plan touches only the in-process training library + test oracles. No untrusted input, network, auth, deserialization-of-external-data, or package-install surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18-01 | Tampering | committed pc=4 ground-truth fixtures (predictions_pc4.npy, live_trainer_*.json) | accept | Frozen committed fixtures, version-pinned catboost 1.2.10, offline-generated (D-09); CI never runs the live trainer. Low-value local test data, no PII. |
| T-05-18-02 | Information disclosure | training library (cb-train) | accept | Pure in-process numeric computation; no I/O of secrets, no network. No new surface. |
| T-05-18-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this plan (no Cargo.toml dependency changes). If any dependency is added, halt with a blocking human-verify checkpoint per the Package Legitimacy Gate — but this plan adds none. |
</threat_model>

<verification>
## Phase-level checks (SC-1 / ORD-01 closure)

- `cargo test -p cb-train --test multi_permutation_e2e_oracle_test` →
  `permutation_count_four_predictions_match_upstream` PASS (≤1e-5 vs predictions_pc4.npy).
- `cargo test -p cb-train --test multi_permutation_fold_oracle_test` → all integer-exact
  (pc=1/pc=2 [6,0,7,17], pc=4 [6,0,10,14], pc=4 averaging permutation == upstream_avg_perm).
- `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` → 3/3 ≤1e-5 (pc=1 unchanged).
- `cargo test -p cb-train --test ordered_boost_e2e_oracle_test` → 2/2 ≤1e-5 (untouched).
- `cargo test -p cb-train --lib` → 130 passed / 0 failed.
- `cargo test -p cb-model` → 3 passed / 0 failed.
- `cargo test -p cb-train` (full integration suite) → 0 FAILED (pre-existing Phase-3
  #[ignore]d residuals excluded).
- `cargo check --tests -p cb-train` → 0 errors, 0 warnings.
- Production discipline: `grep -rn "unwrap()\|\.expect(\|panic!\|unreachable!" crates/cb-train/src/fold.rs crates/cb-train/src/boosting.rs crates/cb-train/src/ctr/ctr_feature.rs crates/cb-train/src/ctr/online.rs | grep -v '^.*//'` returns no production-code hits; no `#[cfg(test)]` test bodies added to production source (the existing sibling-mount in fold.rs is the only permitted `#[cfg(test)]`).
- No oracle weakened: `grep -rn "#\[ignore\]\|assert_ne\|pinned.delta\|pinned_delta" crates/cb-train/tests/multi_permutation_fold_oracle_test.rs crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` shows no NEW weakening on the re-pinned/committed CTR oracles.
</verification>

<success_criteria>
- The pc=4 production-default train→predict reproduces catboost 1.2.10 ≤1e-5 across
  all objects and all 5 trees (bar (c) / SC-1 / ORD-01 CLOSED).
- create_folds produces the bit-exact upstream AveragingFold permutation
  ([11,18,15,29,...] pc=4) — integer-exact gate green.
- Structure-fold cycling produces the pc=4 [A,B,A,B,B] per-tree structure pattern.
- materialize_ctr_feature reproduces ComputeOnlineCTRs(AveragingFold) bins bit-exact.
- Every re-pinned blast-radius lock is integer-exact / ≤1e-5 against the CORRECT
  (non-compensating) upstream value; no green oracle regressed.
- cb-train lib 130/130, cb-model 3/3, full cb-train integration suite 0 FAILED.
- No oracle #[ignore]'d, weakened, assert_ne'd, or pinned-delta'd; the bar-(c) anchor
  is git-committed with corrected doc framing.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-18-SUMMARY.md` when done.
</output>
