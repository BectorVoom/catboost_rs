---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 18
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: false
requirements: [ORD-01, ORD-03, ORD-05]
files_modified:
  - crates/cb-oracle/generator/instrument_live_trainer_README.md
  - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json
  - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json
  - crates/cb-train/src/fold.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/multi_permutation_fold_oracle_test.rs
  - crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs
  - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
  - crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs
  - crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs
  - crates/cb-train/tests/self_consistency_oracle_test.rs

must_haves:
  truths:
    - "A SELF-CONSISTENT live-trainer oracle exists: for each pc=4 tree iteration the captured (effective LearnPermutation, LearnTargetClass, AveragingFold permutation, per-object ui8 CTR bins) tuple is correct-BY-CONSTRUCTION for the SAME fold state — replaying the captured permutation through the already-correct materialize_ctr_feature reproduces the captured bins bit-exact (the Spike-001 inconsistency is RESOLVED)"
    - "The pc=4 (production-default permutation_count=4) train→predict path reproduces catboost 1.2.10 RawFormulaVal predictions ≤1e-5 across all objects and all 5 trees (SC-1 / ORD-01 closure, bar (c)) — per D-01 / the 2026-06-15 RE-AUTHORIZED live-trainer instrumentation"
    - "create_folds produces the bit-exact upstream AveragingFold permutation via learning_folds FULL Fisher-Yates passes (shuffle starts at RNG call-count 29 pc=1 / 87 pc=4): pc=4 == [11,18,15,29,16,12,0,7,19,27,4,3,5,17,14,25,9,20,8,23,6,28,26,24,2,13,21,22,10,1]; pc=1 == [10,17,25,3,6,...] (proven-correct fix #1, re-applied)"
    - "The per-iteration STRUCTURE fold cycles takenFold = Folds[Rand.GenRand() % learning_folds]; the pc=4 fold sequence is [0,2,0,2,2] producing per-tree structures [A,B,A,B,B]; leaf VALUES stay on the FIXED AveragingFold (proven-correct fix #2, re-applied)"
    - "The AveragingFold / structure-fold THREADING in boosting.rs feeds the (already-bit-exact, UNMODIFIED) materialize_ctr_feature the correct per-tree fold/permutation state derived from the self-consistent oracle; the CTR math (materialize_ctr_feature / online_ctr_prefix_binclf / calc_ctr_online_bin) is NOT touched"
    - "Every blast-radius oracle is re-pinned to the NEW self-consistent upstream value: pc=1/pc=2/pc=4 partition locks integer-exact, tensor_ctr_e2e ≤1e-5, ordered_boost_e2e ≤1e-5, leaf-value oracle — none weakened (no #[ignore]/assert_ne/pinned-delta/loosened tolerance)"
    - "pc=1 is NOT regressed: if any pc=1 oracle regresses after the port the fix is WRONG → STOP, do not re-pin pc=1 to a cb-train value"
    - "No green oracle regressed: cb-train lib 130/130, cb-model 18/18, full cb-train integration suite 0 FAILED"
  artifacts:
    - path: "crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json"
      provides: "NEW self-consistent per-iteration (perm, LearnTargetClass, avg-perm, ui8 CTR bins) tuples captured atomically from the re-instrumented trainer; supersedes the internally-inconsistent live_trainer_ctr_bins_blocker.json"
      contains: "self_consistent"
    - path: "crates/cb-train/src/fold.rs"
      provides: "create_folds averaging permutation rule = learning_folds FULL Fisher-Yates passes (shuffle starts at cc=29 pc=1 / cc=87 pc=4) — proven-correct fix #1"
      contains: "create_folds"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "per-iteration structure-fold cycling Folds[Rand.GenRand() % learning_folds] + corrected AveragingFold/structure-fold THREADING into the UNMODIFIED materialize_ctr_feature; leaf values on the fixed AveragingFold"
      contains: "train_inner"
    - path: "crates/cb-train/tests/self_consistency_oracle_test.rs"
      provides: "offline gate proving replaying the captured permutation through materialize_ctr_feature reproduces the captured bins bit-exact (the Wave-1 de-risk gate)"
      contains: "self_consistent"
    - path: "crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs"
      provides: "committed bar-(c) pc=4 e2e prediction oracle ≤1e-5 vs predictions_pc4.npy"
      contains: "permutation_count_four_predictions_match_upstream"
  key_links:
    - from: "crates/cb-train/src/fold.rs::create_folds"
      to: "crates/cb-train/src/permutation.rs::shuffle_in_place"
      via: "averaging shuffle begins after learning_folds full Fisher-Yates passes on the persistent TFastRng64 (cc=29/87)"
      pattern: "shuffle_in_place"
    - from: "crates/cb-train/src/boosting.rs::train_inner"
      to: "crates/cb-train/src/ctr/ctr_feature.rs::materialize_ctr_feature"
      via: "per-iteration structure-fold permutation threaded into the UNMODIFIED materialization (CTR math unchanged)"
      pattern: "materialize_ctr_feature"
    - from: "crates/cb-train/tests/self_consistency_oracle_test.rs"
      to: "crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json"
      via: "replay captured perm through materialize_ctr_feature == captured bins bit-exact"
      pattern: "self_consistent"
    - from: "crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs"
      to: "crates/cb-train/tests/fixtures/multi_permutation_fold/predictions_pc4.npy"
      via: "compare_stage(Stage::Predictions) ≤1e-5 over predict_raw_cat"
      pattern: "predictions_pc4"
---

<objective>
Close the single remaining Phase-5 blocking gap (SC-1 / ORD-01, developer-escalated,
bar (c)): the production-default `permutation_count=4` AveragingFold path does not
reproduce catboost 1.2.10 end-to-end. This is a **RE-PLAN of 05-18**: the prior
05-18 plan (3 coupled fixes including a `materialize_ctr_feature` permutation-order
reindex) is INVALIDATED by Spike 001 and is replaced wholesale by this plan.

Spike 001 (`3cee12a`, `.planning/spikes/001-online-ctr-averaging-fold-offline/`) is a
mathematical impossibility proof that REFRAMES bar (c). Two load-bearing findings:

1. **cb-train's online-CTR materialization is ALREADY bit-exact to upstream C++.**
   `materialize_ctr_feature` / `ctr/online.rs::online_ctr_prefix_binclf` /
   `ctr/calc_ctr.rs::calc_ctr_online_bin` correctly implement
   `CalcOnlineCTRSimple → CalcQuantizedCtrs` with `CalcCTR = (good+0.5)/(total+1)`,
   shift 0 / norm 1 for prior 0.5, ui8 truncation. The old 05-18 "fix #3" targeted
   CORRECT code. **This plan MUST NOT modify the CTR math.** The bug is in WHAT
   permutation/fold state cb-train feeds the (correct) materialization — i.e. in the
   `boosting.rs`/`fold.rs` AveragingFold construction & threading and the per-tree
   structure-fold state — NOT in the CTR formula.

2. **The committed pair (`upstream_avg_perm`, `upstream_avg_ctr_bins_avg_order`) in
   `live_trainer_ctr_bins_blocker.json` is INTERNALLY INCONSISTENT** under the upstream
   algorithm — instrumented from different effective fold states. It is NOT a valid
   offline oracle and CANNOT be re-pinned against. The plan must derive a NEW,
   SELF-CONSISTENT oracle by re-instrumenting the live trainer to dump, ATOMICALLY per
   tree iteration, a self-consistent `(LearnPermutation, LearnTargetClass, AveragingFold
   permutation, per-object ui8 CTR bins)` tuple, then VERIFY self-consistency offline
   before porting.

Authorization: D-01 + the 2026-06-15 CONTEXT decision revision (user-approved
live-trainer instrumentation, scoped to the pc=4 boosting gap), **RE-AUTHORIZED by the
user 2026-06-15 for a second instrumentation cycle**. D-08/D-09: CI reads only the
committed frozen fixtures; no live build runs in CI.

Purpose: SC-1 / ORD-01 says "reproduces upstream permutations exactly" with no
permutation_count carve-out, and pc=4 is the production default. This is the last open
Phase-5 gap.
Output: a NEW self-consistent live-trainer oracle; the proven-correct create_folds +
structure-fold cycling fixes re-applied; corrected AveragingFold/structure-fold
threading (CTR math UNTOUCHED); re-pinned blast-radius oracles; the committed pc=4 e2e
oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# THE NEW DECISIVE INPUT — read in full before anything else
@.planning/spikes/001-online-ctr-averaging-fold-offline/README.md

# Phase-5 gap source + authorization + build recipe
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-17-SUMMARY.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/deferred-items.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-PATTERNS.md

# The live-trainer BUILD RECIPE to reuse (conan/ninja/cython via uv tool; clang-18/lld-18
# via apt-get download + dpkg -x; build_native.py; FindPython→.venv 3.13; CB_INSTRUMENT_LOG)
@crates/cb-oracle/generator/instrument_live_trainer_README.md

# Committed ground-truth fixtures (read; the blocker one is SUPERSEDED, not re-pinned)
@crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_structure_fold.json
@crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json
@crates/cb-train/tests/fixtures/multi_permutation_fold/leaf_weights.json

# Production source under change
@crates/cb-train/src/fold.rs
@crates/cb-train/src/boosting.rs

# Production source that is ALREADY CORRECT — read to confirm, MUST NOT modify
@crates/cb-train/src/ctr/ctr_feature.rs
@crates/cb-train/src/ctr/online.rs
@crates/cb-train/src/ctr/calc_ctr.rs

# Tests in the blast radius (re-pin) + the anchor (commit)
@crates/cb-train/tests/multi_permutation_fold_oracle_test.rs
@crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs
@crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
@crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs
@crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs

# Upstream oracle source (read for the fold/threading mechanism — NOT the CTR formula)
@catboost-master/catboost/private/libs/algo/train.cpp
@catboost-master/catboost/private/libs/algo/fold.cpp
@catboost-master/catboost/private/libs/algo/learn_context.cpp
@catboost-master/catboost/private/libs/algo/online_ctr.cpp
</context>

<critical_constraint>
## The CTR Math Is OUT OF SCOPE For Modification (Spike-001 proved it correct)

`materialize_ctr_feature` (`ctr/ctr_feature.rs`), `online_ctr_prefix_binclf`
(`ctr/online.rs`), and `calc_ctr_online_bin` (`ctr/calc_ctr.rs`) are bit-exact to
upstream `CalcOnlineCTRSimple`/`CalcQuantizedCtrs` (Spike 001 §"The Proof"). ANY task
that proposes changing the CTR formula, the online read-before-increment prefix, the
border quantization, or the bucket-reindex order is a **PLANNING ERROR** — flag it and
STOP. The bug is exclusively in WHICH permutation/fold state is fed to these
(already-correct) functions: `create_folds` (fix #1), the per-tree structure-fold
selection (fix #2), and the AveragingFold/structure-fold threading in `boosting.rs`.

## Re-pin ONLY To Self-Consistent Upstream Values — Never To A cb-train Value

Re-pinning means re-deriving each oracle's "expected" from the NEW self-consistent
live-trainer fixtures (integer-exact partitions, ≤1e-5 predictions). The committed
`live_trainer_ctr_bins_blocker.json` `(upstream_avg_perm, upstream_avg_ctr_bins_avg_order)`
pair is internally INCONSISTENT (Spike 001 §VERDICT) — DO NOT re-pin against it. It is
NOT loosening tolerance, NOT `#[ignore]`, NOT `assert_ne`, NOT a pinned-delta record,
NOT fabricating a pass. NEVER re-pin to a cb-train-produced value to force green.

## HARD INVARIANTS (carry from the prior 05-18, still binding)

1. If pc=1 regresses after the port, the fix is WRONG — STOP, do not re-pin pc=1 to a
   cb-train value (this is the Rule-3 STOP that fired in the prior 05-18 execution).
2. Re-pin only to upstream-derived, self-consistent values.
3. The CTR math is out of scope (constraint above).

## FALLBACK (feasibility risk — first-class)

Wave 1 depends on a multi-hour instrumented-trainer rebuild that may not reproduce
cleanly, AND on adequate disk. Include the disk NO-GO gate (deferred-items.md). If the
self-consistent oracle CANNOT be captured (build infeasible, disk NO-GO, or the
self-consistency offline gate fails), STOP and checkpoint to the user: defer bar (c)
with the Spike-001 proof, leave production UNTOUCHED, commit NO weakened state, leave
the pc=4 e2e oracle UNCOMMITTED. Bars (a),(b),(d),(e) stay green (05-17).

## CLAUDE.md Discipline

Tests live ONLY in dedicated `_test.rs` files (the existing `#[cfg(test)] #[path=...]
mod tests` sibling-mount in fold.rs is permitted, unchanged). No
`unwrap()`/`expect()`/panic/raw-index in production (thiserror via CbError/CbResult;
tests keep their `#![allow(clippy::unwrap_used, ...)]`). All parity-critical float
summation routes through `cb_core::sum_f64` (D-08). Categorical hashing via
`cb_data::calc_cat_feature_hash`, never a model's ctr_data hash_map.
</critical_constraint>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (Wave 1): Disk NO-GO gate + re-provision the instrumented catboost 1.2.10 toolchain</name>
  <files>crates/cb-oracle/generator/instrument_live_trainer_README.md</files>
  <read_first>
    - crates/cb-oracle/generator/instrument_live_trainer_README.md (the EXACT build recipe: conan 2.29 / ninja 1.13 / cython 3.2.5 via `uv tool install`; clang-18 + lld-18 via `apt-get download` + `dpkg -x` into a local prefix; `build_native.py --targets _catboost` with `-DPython3_INCLUDE_DIR/.../_EXECUTABLE` overrides to the project `.venv` Python 3.13; the `CbInstrumentLog` env-gated sink; the RUN-ONCE invocation)
    - .planning/phases/05-.../deferred-items.md (the disk NO-GO gate: 05-17 ran with ~58G free; the full instrumented build needs ample headroom — measure `df -h /` and reclaim target/debug FIRST)
  </read_first>
  <action>
AUTOMATE FIRST, then this blocking checkpoint gates the human-only / environment-provision steps.

PART A — disk NO-GO gate (HARD STOP condition). Run `df -h /` and record free space. The
05-17 build ran with ~58G free; the full catboost 1.2.10 instrumented build footprint is
large, and CURRENT free space is known-tight (the env snapshot showed ~6.6G free / 98%
used). If free space is below the headroom the README/deferred-items require (treat
&lt;40G free as NO-GO unless reclaim brings it above), reclaim what is safely reclaimable
(`cargo clean` on stale target dirs / remove `target/debug/incremental`) and re-measure.
If still NO-GO after reclaim → take the FALLBACK: STOP, checkpoint to the user (defer bar
(c), production UNTOUCHED, no weakened state, pc=4 e2e oracle UNCOMMITTED), do NOT proceed
to Task 2 capture.

PART B — re-provision the toolchain per the README recipe (clang-18/lld-18 were
provisioned to an ephemeral /tmp prefix in 05-17 and are now ABSENT; conan/ninja/uv
persist). `apt-get download` (no sudo) clang-18 + lld-18 debs, `dpkg -x` into a local
prefix; confirm the `uv tool` conan/ninja/cython are present and the `.venv` Python 3.13
resolves. This is the human-action gate because the toolchain re-provision involves
interactive package fetch / network steps that are environment-specific and may fail. Do
NOT modify any production source in this task — only the README (if documenting the
re-provision) is touched.
  </action>
  <what-built>
    The disk NO-GO measurement + the re-provisioned offline toolchain (clang-18/lld-18 via
    apt-get download + dpkg -x; uv-tool conan/ninja/cython; .venv Python 3.13) needed for
    the Task-2 instrumented build. No production code changes.
  </what-built>
  <how-to-verify>
    1. `df -h /` reports adequate free space AFTER any reclaim (the disk NO-GO gate is
       NOT tripped) — record the number in the SUMMARY.
    2. `clang-18 --version` and `ld.lld-18 --version` resolve from the local prefix;
       `conan --version`, `ninja --version`, and the `.venv` Python 3.13 are present.
    3. A trial `build_native.py --targets _catboost` configure step (FindPython override
       to .venv 3.13) reaches the compile phase without a missing-toolchain error.
    If ANY of these fails irrecoverably → FALLBACK STOP (defer, no production change).
  </how-to-verify>
  <verify>
    <automated>df -h / | tail -1 && command -v conan ninja && (command -v clang-18 || ls */clang-18 2>/dev/null || echo "clang-18 must be provisioned per README before Task 2") && test -x .venv/bin/python && .venv/bin/python -c "import catboost; print('catboost', catboost.__version__)"</automated>
  </verify>
  <resume-signal>Type "toolchain-ready" (disk OK + clang-18/lld-18/conan/ninja resolved + configure reaches compile) to proceed to Task 2, or "fallback-defer" to take the documented FALLBACK and stop.</resume-signal>
  <done>The disk NO-GO gate is NOT tripped (free space recorded ≥ required headroom after reclaim), and the offline toolchain (clang-18/lld-18, conan/ninja/cython, .venv Python 3.13) resolves and a trial build_native.py configure reaches the compile phase. OR: the disk NO-GO / toolchain provision failed irrecoverably → the documented FALLBACK STOP was taken (production untouched, bar (c) deferred, user checkpointed) and Task 2 capture did NOT proceed.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Re-instrument the trainer to dump ATOMIC self-consistent per-iteration tuples + commit the new oracle</name>
  <files>crates/cb-oracle/generator/instrument_live_trainer_README.md, crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json, crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json</files>
  <read_first>
    - crates/cb-oracle/generator/instrument_live_trainer_README.md (the existing instrumentation patch: env-gated `CB_INSTRUMENT_LOG`, logs structure_fold / tree_struct / leaf_partition per iteration; extend it to ALSO log the atomic self-consistent tuple)
    - .planning/spikes/001-online-ctr-averaging-fold-offline/README.md (§VERDICT finding #2 + §"How to proceed": the re-instrumentation must capture, for the SAME fold state in force when ComputeOnlineCTRs ran for each tree, the effective LearnPermutation, LearnTargetClass, the AveragingFold permutation, AND the per-object ui8 CTR bins THOSE produce — so (perm, bins) are correct-by-construction)
    - catboost-master/catboost/private/libs/algo/online_ctr.cpp (ComputeOnlineCTRs 600-790, CalcOnlineCTRSimple 344, CalcQuantizedCtrs 271 — the call sites whose live fold state + emitted ui8 bins must be logged together)
    - catboost-master/catboost/private/libs/algo/train.cpp (the per-iteration TrainOneIteration body; the structure-fold pick train.cpp:208; the ComputeOnlineCTRs call sites greedy_tensor_search.cpp:675/809/1104)
    - catboost-master/catboost/private/libs/algo/fold.cpp (fold.cpp:322-353 AssignTarget: LearnTargetClass is fixed at fold construction on raw y — log it per fold so the consumer knows which target-class vector keyed each bin sequence)
  </read_first>
  <action>
Extend the env-gated (`CB_INSTRUMENT_LOG`) instrumentation in `train.cpp` /
`online_ctr.cpp` so that, ATOMICALLY for EACH tree iteration of the pc=4
`tensor_ctr_e2e` run (and also a pc=1 run, for the no-regression anchor), it dumps ONE
self-consistent JSONL record containing, all captured from the SAME fold state in force
when `ComputeOnlineCTRs` ran for that tree:
  * `iter` — the boosting iteration index.
  * `effective_learn_permutation` — the structure fold's permutation actually selected
    that iteration (`Folds[Rand.GenRand() % foldCount]`, train.cpp:208).
  * `learn_target_class` — `fold.LearnTargetClass` for that fold (fold.cpp:322-353),
    the binclf class vector the online prefix keyed on.
  * `averaging_permutation` — the AveragingFold permutation (the fixed leaf-value fold,
    approx_calcer.cpp:1082).
  * `ctr_bins_object_order` AND `ctr_bins_avg_order` — the per-object ui8 CTR bins THAT
    THIS fold state produced for the single feature-0 Borders CTR, in BOTH object order
    and averaging-permutation order (so the offline consumer can compare in whichever
    order materialize_ctr_feature emits, matching the existing `_avg_order` labeling).
This is the FIX for the Spike-001 inconsistency: the `(perm, target_class, bins)` triple
is now captured for ONE coherent fold state, not stitched from different effective
states.

Build the instrumented `_catboost` via the README recipe and run the pc=4 (and pc=1)
fit RUN-ONCE. Confirm the trainer still reproduces upstream tree leaf_weights
`[6,0,10,14],[8,8,0,14],[6,0,10,14],[8,8,0,14],[8,8,0,14]` and predictions bit-identical
(max abs diff 0.0) to the committed `predictions_pc4.npy` (the faithfulness check the
05-17 build passed — if it does not reproduce bit-identically, the instrumentation is
unfaithful → FALLBACK STOP).

Commit the new self-consistent ground truth as
`crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json`
(the atomic per-iteration tuples for pc=1 and pc=4). SUPERSEDE the inconsistent
`live_trainer_ctr_bins_blocker.json`: annotate it in place (add a top-level
`"superseded_by": "live_trainer_self_consistent.json"` and a `"superseded_reason"`
citing Spike 001 §VERDICT that its `(perm, bins)` pair is internally inconsistent) —
keep the file so the Spike-001 evidence trail is preserved; do NOT delete it. Update
`instrument_live_trainer_README.md` to document the second instrumentation cycle and the
atomic-tuple schema.

CI reads ONLY the committed JSON (D-08/D-09); the live trainer is never wired into CI.
  </action>
  <verify>
    <automated>test -f crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json && python3 -c "import json,sys; d=json.load(open('crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json')); recs=d if isinstance(d,list) else d.get('pc4') or d.get('iterations') or d.get('records'); assert recs, 'no per-iteration records'; r=recs[0]; [r[k] for k in ('effective_learn_permutation','learn_target_class','averaging_permutation','ctr_bins_object_order')]; print('self-consistent oracle schema OK', len(recs),'records')" && python3 -c "import json; d=json.load(open('crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_ctr_bins_blocker.json')); assert d.get('superseded_by'), 'blocker not annotated superseded'; print('blocker annotated superseded')"</automated>
  </verify>
  <done>live_trainer_self_consistent.json exists and committed, with per-iteration records each carrying the atomic (effective_learn_permutation, learn_target_class, averaging_permutation, ctr_bins_object_order, ctr_bins_avg_order) tuple for pc=1 and pc=4. The instrumented trainer reproduced predictions_pc4.npy bit-identically (faithfulness confirmed). live_trainer_ctr_bins_blocker.json is annotated as superseded (not deleted). README updated. If the build/capture was infeasible → FALLBACK STOP was taken and this task records the deferral instead.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1 de-risk gate): Offline self-consistency gate — replay captured perm through materialize_ctr_feature == captured bins (BEFORE any port)</name>
  <files>crates/cb-train/tests/self_consistency_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json (the NEW oracle from Task 2 — the per-iteration tuples to replay)
    - crates/cb-train/src/ctr/ctr_feature.rs (materialize_ctr_feature — the UNMODIFIED, already-bit-exact function to drive; note it returns `bins` in OBJECT order and `ctr_value`; convert to avg-order via the permutation to compare against `ctr_bins_avg_order`)
    - crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs (load_cat_columns / oracle_fixture helpers — reuse the same fixture-loading shape; X_cat.npy, y.npy)
    - .planning/spikes/001-online-ctr-averaging-fold-offline/README.md (the per-bucket determinism proof — the self-consistency replay either matches bit-exact under the captured fold state, or the instrumentation is still wrong and Wave 2 must NOT start)
  </read_first>
  <action>
Author a NEW dedicated integration test `self_consistency_oracle_test.rs` (CLAUDE.md
source/test separation; `#![allow(clippy::unwrap_used, ...)]` test header). For EACH
per-iteration record in `live_trainer_self_consistent.json` (pc=4 and the pc=1 anchor):
  * Load the feature-0 cat column (tensor_ctr_e2e/X_cat.npy) and the captured
    `learn_target_class` and `effective_learn_permutation` (or `averaging_permutation`
    for the leaf-value materialization, matching which fold state the record labels).
  * Call the production `materialize_ctr_feature` UNMODIFIED — the same single-feature
    Borders projection, prior 0.5/1.0, ctr_border_count 15 — passing the captured
    permutation + captured target_class.
  * Assert the resulting ui8 bins (converted to the same order the fixture stores)
    equal the captured `ctr_bins_*` bit-exact (integer-exact `assert_eq` over the bin
    vector).
This proves the captured `(perm, target_class, bins)` is correct-BY-CONSTRUCTION for the
SAME fold state — the Spike-001 inconsistency is resolved. This is the DE-RISK GATE: it
MUST pass before Wave 2 (the port) begins. If it does NOT pass, the instrumentation is
still wrong → STOP, do not port; return to Task 2 or take the FALLBACK.

Do NOT modify materialize_ctr_feature (the CTR math is out of scope). This task drives
the already-correct function and asserts the NEW oracle is self-consistent with it.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test self_consistency_oracle_test 2>&1 | tail -15</automated>
  </verify>
  <done>self_consistency_oracle_test PASSES: replaying each captured per-iteration permutation + target_class through the UNMODIFIED production materialize_ctr_feature reproduces the captured ui8 bins bit-exact, for both pc=1 and pc=4 records. materialize_ctr_feature is byte-unchanged (`git diff crates/cb-train/src/ctr/ctr_feature.rs` empty). The Spike-001 inconsistency is RESOLVED and Wave 2 is unblocked. If the gate FAILED → Wave 2 is NOT started and the FALLBACK STOP is recorded.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 2): Re-apply proven fix #1 (create_folds avg permutation) + fix #2 (structure-fold cycling) + correct the AveragingFold/structure-fold THREADING (CTR math UNTOUCHED)</name>
  <files>crates/cb-train/src/fold.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-train/src/fold.rs (the WHOLE file — the LEARNING-PERMUTATION-NEEDED branch at lines 283-330 currently encodes the SUPERSEDED "one gen_rand per fold position then shuffle" cc==learning_folds rule; replace it with the proven-correct learning_folds-FULL-Fisher-Yates-passes rule per deferred-items.md §"05-18 execution finding" #1 and live_trainer_self_consistent.json)
    - crates/cb-train/src/boosting.rs (the train_inner CTR threading region lines ~1037-1233: the Ordered/cat-CTR fold construction, the SINGLE fixed structure CTR column under cat_learn_permutation reused for every tree, the averaging leaf-value column, and the for iter loop at ~1295; PRE_TREE_DRAWS / draws_active at ~1283-1290)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json (the per-iteration effective_learn_permutation + structure-fold sequence [0,2,0,2,2] the threading must reproduce)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_structure_fold.json (the committed structure-fold call-count schedule + per-iteration sum_leaf_weights [6,0,10,14]/[8,8,0,14]/... to verify against)
    - catboost-master/catboost/private/libs/algo/learn_context.cpp (fold creation; shuffle = foldIdx != 0; IsAverageFoldPermuted) and fold.cpp (Shuffle Fisher-Yates) and train.cpp:208 (takenFold = Folds[Rand.GenRand() % foldCount])
    - crates/cb-train/src/permutation.rs (shuffle_in_place — one uniform() draw per position i in 1..n; one full pass over N=30 consumes 29 draws)
  </read_first>
  <action>
PART A — fold.rs create_folds (proven-correct fix #1, re-applied). Replace the
LEARNING-PERMUTATION-NEEDED branch (lines ~306-329) so the averaging Fisher-Yates
shuffle begins after `learning_folds` FULL Fisher-Yates passes on the persistent
TFastRng64 (RNG GetCallCount 29 at pc=1, 87 at pc=4 — one full pass over N=30 consumes
29 uniform() draws; learning_folds=1 → 29 = 1×29; pc=4 learning_folds=3 → 87 = 3×29):
  * Each of the `learning_folds` fold positions (including identity Folds[0]) performs
    ONE FULL Fisher-Yates pass on the persistent rng BEFORE the averaging fold's pass.
    Folds[0] keeps the IDENTITY permutation as its stored `permutation` (shuffle =
    foldIdx != 0, learn_context.cpp:524) — but the rng STILL advances one full pass.
    Store the ACTUAL shuffled permutation for learning folds 1..learning_folds (the
    structure-fold cycling in PART C now READS them); Folds[0] stays identity.
  * The AveragingFold's shuffle is the (learning_folds+1)-th full pass and MUST equal
    `upstream_avg_perm` ([11,18,15,29,16,...] at pc=4; [10,17,25,3,6,...] at pc=1)
    bit-exact. Verify against live_trainer_self_consistent.json.
  * Preserve the `!permutation_needed_for_learning` numeric path EXACTLY (byte-identical
    regression anchor — do not touch it).
  * Update the create_folds doc block to describe the learning_folds-full-passes rule
    and cite live_trainer_self_consistent.json; REMOVE the now-superseded
    "cc == learning_folds" prose (lines 208-305).

PART B — boosting.rs per-iteration structure-fold cycling (proven-correct fix #2).
Build ALL `learning_folds` learning-fold permutations from create_folds (PART A now
stores their actual shuffled permutations). Inside `for iter in 0..params.iterations`,
on the has_ctr branch, draw `taken = (rng.gen_rand() % learning_folds as u64) as usize`
EACH iteration BEFORE the structure search; the draw fires UNCONDITIONALLY on the has_ctr
path. Reproduce the pc=4 structure-fold sequence [0,2,0,2,2] against
live_trainer_structure_fold.json (transcribe the committed call-count schedule
[116,140,166,190,212] if the intermediate per-iteration draw counts are
config/data-coupled and not derivable, per deferred-items.md #2). For learning_folds==1
(pc=1/pc=2) the modulo is always 0 → structure fold always Folds[0] IDENTITY,
byte-identical to today (the key no-regression invariant).

PART C — correct the AveragingFold/structure-fold THREADING (the LIKELY true bug site;
CTR MATH UNTOUCHED). Today boosting.rs materializes ONE structure CTR column under the
identity Folds[0] and reuses it for every tree. Materialize a structure CTR column PER
learning fold (one `materialize_ctr_feature` per fold permutation, reusing
absolute_projections — the SAME UNMODIFIED function), so the per-iteration selected fold
has its column ready, and pass the iteration's selected fold's column into the structure
search. Keep the SINGLE averaging-fold leaf-value column unchanged (leaf VALUES always
use the FIXED AveragingFold, approx_calcer.cpp:1082). Bake leaf_weights from the
AveragingFold partition (unchanged). Do NOT change materialize_ctr_feature,
online_ctr_prefix_binclf, or calc_ctr_online_bin — only WHICH permutation is threaded in
(Spike 001: the math is already correct; the bug is the fed fold state).

Verify against live_trainer_structure_fold.json that the pc=4 tree structures are
[6,0,10,14],[8,8,0,14],[6,0,10,14],[8,8,0,14],[8,8,0,14]. NOTE: fix #1 + #2 together
REGRESS pc=1 if the threading (PART C) does not feed the correct per-tree fold state —
they only land green TOGETHER with PART C. Intermediate RED states across PART A/B/C are
expected; the FINAL state (Task 5/6) must be all-green. If after PART C pc=1 still
regresses, the threading is wrong — STOP (hard invariant #1), do not proceed to re-pin.
  </action>
  <verify>
    <automated>git diff --exit-code crates/cb-train/src/ctr/ctr_feature.rs crates/cb-train/src/ctr/online.rs crates/cb-train/src/ctr/calc_ctr.rs && cargo build -p cb-train 2>&1 | tail -5 && cargo test -p cb-train --lib 2>&1 | tail -5</automated>
  </verify>
  <done>cb-train compiles; the CTR math files (ctr_feature.rs/online.rs/calc_ctr.rs) have ZERO diff (the `git diff --exit-code` gate passes). create_folds, driven with N=30/seed=0/learning_folds-full-passes, produces upstream_avg_perm bit-exact (pc=4 [11,18,15,29,...], pc=1 [10,17,25,3,6,...]). The structure-fold cycling draws Folds[Rand.GenRand() % learning_folds] per iteration (no-op at learning_folds==1) and the per-tree structure CTR column is threaded from the selected fold. The numeric/one-hot/ordered lib unit tests still pass (cat-CTR e2e oracles may be RED pending Task 5/6 re-pin — expected coupled mid-state).</done>
</task>

<task type="auto">
  <name>Task 5 (Wave 2): Re-pin the partition + permutation oracles to the NEW self-consistent upstream values</name>
  <files>crates/cb-train/tests/multi_permutation_fold_oracle_test.rs, crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/tests/multi_permutation_fold_oracle_test.rs (averaging_partition / self_derived_averaging_permutation / the pc=4 hard-equality test / the module doc — all bake the OLD cc==learning_folds rule and must be re-derived to the learning_folds-full-passes rule + the NEW self-consistent oracle)
    - crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs (locks AveragingFold == fisher_yates_permutation(30,0) for pc=1 per 05-12 — under the corrected rule check whether pc=1 averaging == [10,17,25,3,6,...]; re-pin only if its expectation no longer holds)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json (the NEW self-consistent oracle — the source of every re-pinned "expected")
    - crates/cb-train/tests/fixtures/multi_permutation_fold/leaf_weights.json (the committed partition counts: pc=1/pc=2 [6,0,7,17], pc=4 [6,0,10,14])
  </read_first>
  <action>
Re-derive EVERY assertion in `multi_permutation_fold_oracle_test.rs` from the NEW
self-consistent upstream fixtures (live_trainer_self_consistent.json + leaf_weights.json)
— NEVER from a cb-train value, NEVER from the superseded
live_trainer_ctr_bins_blocker.json:
  * averaging_partition(): rebuild on the corrected create_folds + the UNMODIFIED
    materialize_ctr_feature; it MUST still produce the committed leaf_weights.json
    partitions (pc=1/pc=2 [6,0,7,17], pc=4 [6,0,10,14]) — now via the CORRECT,
    self-consistent permutation+bins (not the compensating pair). Integer-exact
    compare_permutation gate preserved.
  * self_derived_averaging_permutation(): re-derive to the learning_folds-FULL-passes
    rule; correct its doc comment ("cc == learning_folds" → "learning_folds full
    Fisher-Yates passes, cc=29/87").
  * Keep multi_permutation_count_four_averaging_matches_catboost_1_2_10 HARD
    integer-exact == [6,0,10,14].
  * ADD an integer-exact gate that create_folds' pc=4 averaging permutation ==
    upstream_avg_perm [11,18,15,29,16,12,0,7,...] (from live_trainer_self_consistent.json)
    via compare_permutation — the new bit-exact permutation gate the self-consistent
    oracle enables.
  * Fix the stale module doc-comment to the learning_folds-full-passes rule.
averaging_fold_permutation_oracle_test.rs: re-pin to the corrected pc=1 averaging
permutation ONLY if its prior `fisher_yates_permutation(30,0)` expectation no longer
holds; determine by reading the test + the cc=29 pc=1 accounting. If it still holds
(learning_folds==1 → one full pass == fisher_yates_permutation(30,0)), leave it.
NO #[ignore], no assert_ne, no pinned-delta, no loosened tolerance. If an oracle cannot
reach its committed self-consistent target after the Task-4 port, STOP and surface it —
do not weaken.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test multi_permutation_fold_oracle_test --test averaging_fold_permutation_oracle_test 2>&1 | tail -20</automated>
  </verify>
  <done>multi_permutation_fold_oracle_test passes ALL tests integer-exact against the SELF-CONSISTENT upstream fixtures: pc=1/pc=2 [6,0,7,17], pc=4 [6,0,10,14], AND the new pc=4 averaging permutation == upstream_avg_perm [11,18,15,29,...] gate. averaging_fold_permutation_oracle_test green (re-pinned only if needed). No oracle weakened; no re-pin to a cb-train value or to the superseded blocker fixture.</done>
</task>

<task type="auto">
  <name>Task 6 (Wave 3): Commit/wire the pc=4 e2e oracle, re-pin remaining e2e/leaf oracles, full-suite + self-consistency final gate</name>
  <files>crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs, crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs, crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs (the UNTRACKED anchor — permutation_count_four_predictions_match_upstream asserts predict_raw_cat ≤1e-5 vs predictions_pc4.npy; its module doc cites the OLD "shuffle begins at call-count == learning_folds" framing — correct it to learning_folds-FULL-passes + the self-consistent oracle)
    - crates/cb-train/tests/fixtures/multi_permutation_fold/predictions_pc4.npy (the ≤1e-5 target — the committed upstream pc=4 RawFormulaVal)
    - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs (pc=1 tensor-CTR e2e ≤1e-5 — must stay green; pc=1 path byte-identical post-fix because learning_folds==1, so it should pass WITHOUT re-pinning — CONFIRM, do not weaken)
    - crates/cb-train/tests/ordered_boost_e2e_oracle_test.rs (ORD-02 ordered e2e ≤1e-5 — no cat CTRs; untouched by the threading fix — CONFIRM green)
  </read_first>
  <action>
PART A — commit + correct the bar-(c) anchor. Correct multi_permutation_e2e_oracle_test.rs
module doc: replace the "shuffle begins at call-count == learning_folds" framing with
"the averaging Fisher-Yates shuffle begins after learning_folds FULL Fisher-Yates passes
(RNG call-count 29 pc=1 / 87 pc=4), recovered by the re-instrumented live trainer
(live_trainer_self_consistent.json, the Spike-001-driven second instrumentation cycle)
and the per-iteration structure-fold cycling Folds[Rand.GenRand() % learning_folds]
(pc=4 sequence [0,2,0,2,2])". Keep the body's ≤1e-5 assertion over predict_raw_cat vs
predictions_pc4.npy. `git add` this currently-untracked file — it is the SC-1/ORD-01
closure gate.

PART B — re-pin the remaining blast-radius e2e/leaf oracles ONLY if they regressed.
tensor_ctr_e2e_oracle_test (pc=1) and ordered_boost_e2e_oracle_test should stay green
(pc=1 path byte-identical: learning_folds==1 → structure-fold cycle is a no-op, averaging
permutation at pc=1 is one full pass). Run them. If EITHER regressed, the pc=1 path
shifted → hard invariant #1 fired: the fix is WRONG, STOP and surface it; do NOT re-pin
pc=1 to a cb-train value, do NOT weaken. (The committed predictions.npy fixtures are
upstream truth and do not change.)

PART C — full-suite regression + self-consistency final gate. Run: the pc=4 e2e oracle,
the self_consistency_oracle_test (Task 3), the COMPLETE cb-train integration suite, the
cb-train lib, and cb-model. Confirm: bar-(c) pc=4 e2e ≤1e-5 PASSES; self_consistency
gate still green; every re-pinned lock integer-exact/≤1e-5 against the SELF-CONSISTENT
upstream value; cb-train lib 130/130, cb-model 18/18, full cb-train integration suite 0
FAILED (pre-existing Phase-3 #[ignore]d Bayesian/overfit/random_strength residuals not
counted); pc=1 NOT regressed. No oracle #[ignore]'d / weakened / assert_ne'd /
pinned-delta'd. This proves the coupled fix landed all-green on NON-compensating,
self-consistent values.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test multi_permutation_e2e_oracle_test --test self_consistency_oracle_test --test tensor_ctr_e2e_oracle_test --test ordered_boost_e2e_oracle_test --test multi_permutation_fold_oracle_test 2>&1 | tail -25 && cargo test -p cb-train --lib 2>&1 | tail -3 && cargo test -p cb-model 2>&1 | tail -3</automated>
  </verify>
  <done>multi_permutation_e2e_oracle_test::permutation_count_four_predictions_match_upstream PASSES ≤1e-5 vs predictions_pc4.npy (bar (c) / SC-1 closed). self_consistency_oracle_test green, tensor_ctr_e2e 3/3, ordered_boost_e2e 2/2, multi_permutation_fold all-green (incl. the new permutation gate), cb-train lib 130/130, cb-model 18/18, full cb-train integration suite 0 FAILED. The anchor test is git-committed with corrected doc framing. No oracle weakened. pc=1 NOT regressed. (If pc=1 regressed → STOP per invariant #1, do not re-pin.)</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Symbols / tests / fixtures this plan CREATES or NEWLY COMMITS

**New self-consistent fixtures (the Spike-001 fix):**
- `crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json`
  — atomic per-iteration `(effective_learn_permutation, learn_target_class,
  averaging_permutation, ctr_bins_object_order, ctr_bins_avg_order)` tuples for pc=1 +
  pc=4, captured for ONE coherent fold state (resolves the internally-inconsistent
  committed pair). `live_trainer_ctr_bins_blocker.json` annotated `superseded_by` (kept,
  not deleted).

**New tests (NEWLY COMMITTED):**
- `crates/cb-train/tests/self_consistency_oracle_test.rs` — the offline de-risk gate:
  replaying the captured permutation through the UNMODIFIED materialize_ctr_feature
  reproduces the captured bins bit-exact.
- `crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs` — was UNTRACKED;
  `permutation_count_four_predictions_match_upstream` (the bar-(c) pc=4 e2e ≤1e-5 closure
  gate), doc-framing corrected, now committed.

**Changed production symbols (CTR math UNCHANGED):**
- `crates/cb-train/src/fold.rs::create_folds` — averaging permutation rule = learning_folds
  FULL Fisher-Yates passes (cc=29 pc=1 / cc=87 pc=4); learning folds 1..learning_folds now
  store their actual shuffled permutations (read by structure-fold cycling); Folds[0]
  identity.
- `crates/cb-train/src/boosting.rs::train_inner` — per-iteration structure-fold cycling
  `taken = rng.gen_rand() % learning_folds`; per-fold materialized structure CTR columns
  threaded from the selected fold; leaf VALUES on the fixed AveragingFold.
- (NO change to `ctr/ctr_feature.rs`, `ctr/online.rs`, `ctr/calc_ctr.rs` — the CTR math
  is bit-exact per Spike 001 and is out of scope; enforced by `git diff --exit-code`.)

**Re-pinned test assertions (to the SELF-CONSISTENT upstream values):**
- `multi_permutation_fold_oracle_test.rs` — new integer-exact pc=4 averaging permutation
  gate (== upstream_avg_perm [11,18,15,29,...]); re-derived
  averaging_partition / self_derived_averaging_permutation; corrected module doc.
- `averaging_fold_permutation_oracle_test.rs` — re-pinned to the corrected pc=1
  permutation only if its prior expectation no longer holds.

**No NEW oracle weakened.** Re-pins derive "expected" from the committed
self-consistent fixtures only (never a cb-train value, never the superseded blocker).
CI reads only frozen committed fixtures (D-08/D-09); no live build in CI.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → CI test reads | The live-trainer ground-truth JSON/npy are read at test time; committed, frozen, offline-generated (no live build in CI). |
| instrumented trainer build (offline, Wave 1) → committed fixture | The RE-INSTRUMENTED catboost 1.2.10 trainer is built and run by hand OFFLINE; only its committed JSON output enters the repo. Never wired to CI. |
| (none new in-process) | This plan touches only the in-process training library + test oracles. No untrusted input, network-at-runtime, auth, or external-data deserialization surface is added to the library. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18-01 | Tampering | committed ground-truth fixtures (predictions_pc4.npy, live_trainer_self_consistent.json) | accept | Frozen committed fixtures, version-pinned catboost 1.2.10, offline-generated (D-09); CI never runs the live trainer; the self_consistency_oracle_test cross-checks the new fixture against the production CTR code so a corrupt fixture is caught. Low-value local test data, no PII. |
| T-05-18-02 | Tampering | the offline instrumented trainer build (clang-18/lld-18/conan fetched over network) | mitigate | The build is a deliberate, user-authorized (D-01 + 2026-06-15 RE-AUTH) offline RUN-ONCE step; faithfulness is gated (the trainer must reproduce predictions_pc4.npy bit-identically) and the captured oracle is independently re-validated offline by self_consistency_oracle_test before any port. Build never enters CI. |
| T-05-18-03 | Information disclosure | training library (cb-train) | accept | Pure in-process numeric computation; no I/O of secrets, no runtime network. No new surface. |
| T-05-18-SC | Tampering | npm/pip/cargo installs | mitigate | No new Cargo dependency changes in this plan. The Wave-1 toolchain provision (`uv tool` conan/ninja/cython; clang-18/lld-18 debs) is the SAME user-authorized offline toolchain as 05-17 (instrument_live_trainer_README.md), gated behind the Task-1 blocking checkpoint; it adds no runtime/library dependency. If any cb-train Cargo dependency were added, halt with a blocking human-verify checkpoint per the Package Legitimacy Gate — but this plan adds none. |
</threat_model>

<verification>
## Phase-level checks (SC-1 / ORD-01 closure, bar (c))

- `cargo test -p cb-train --test self_consistency_oracle_test` → PASS (the NEW oracle is
  self-consistent with the UNMODIFIED production CTR code; the Spike-001 inconsistency is
  resolved). This is the gate that must pass BEFORE the port.
- `cargo test -p cb-train --test multi_permutation_e2e_oracle_test` →
  permutation_count_four_predictions_match_upstream PASS (≤1e-5 vs predictions_pc4.npy).
- `cargo test -p cb-train --test multi_permutation_fold_oracle_test` → all integer-exact
  (pc=1/pc=2 [6,0,7,17], pc=4 [6,0,10,14], pc=4 averaging permutation == upstream_avg_perm).
- `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` → 3/3 ≤1e-5 (pc=1 unchanged).
- `cargo test -p cb-train --test ordered_boost_e2e_oracle_test` → 2/2 ≤1e-5 (untouched).
- `cargo test -p cb-train --lib` → 130 passed / 0 failed.
- `cargo test -p cb-model` → 18 passed / 0 failed.
- `cargo test -p cb-train` (full integration suite) → 0 FAILED (pre-existing Phase-3
  #[ignore]d residuals excluded).
- `cargo check --tests -p cb-train` → 0 errors, 0 warnings.
- CTR math untouched: `git diff --exit-code crates/cb-train/src/ctr/ctr_feature.rs crates/cb-train/src/ctr/online.rs crates/cb-train/src/ctr/calc_ctr.rs` exits 0 (zero diff).
- Production discipline: `grep -nE "unwrap\(\)|\.expect\(|panic!|unreachable!" crates/cb-train/src/fold.rs crates/cb-train/src/boosting.rs | grep -v '^[^:]*:[0-9]*: *//'` returns no production-code hits; no new `#[cfg(test)]` test bodies in production source (the existing sibling-mount in fold.rs is the only permitted `#[cfg(test)]`).
- No oracle weakened: `grep -rnE "#\[ignore\]|assert_ne|pinned.delta|pinned_delta" crates/cb-train/tests/multi_permutation_fold_oracle_test.rs crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs crates/cb-train/tests/self_consistency_oracle_test.rs` shows no NEW weakening.
- Superseded blocker annotated (not deleted): the JSON has `superseded_by`.
</verification>

<success_criteria>
- A SELF-CONSISTENT live-trainer oracle exists and is proven self-consistent with the
  UNMODIFIED production materialize_ctr_feature (the Spike-001 inconsistency RESOLVED).
- The pc=4 production-default train→predict reproduces catboost 1.2.10 ≤1e-5 across all
  objects and all 5 trees (bar (c) / SC-1 / ORD-01 CLOSED).
- create_folds produces the bit-exact upstream AveragingFold permutation
  ([11,18,15,29,...] pc=4) via learning_folds full Fisher-Yates passes — integer-exact
  gate green.
- Structure-fold cycling produces the pc=4 [A,B,A,B,B] per-tree structure pattern; the
  AveragingFold/structure-fold threading feeds the UNMODIFIED CTR materialization the
  correct per-tree fold state.
- The CTR math (ctr_feature.rs/online.rs/calc_ctr.rs) is byte-unchanged (git diff empty).
- Every re-pinned blast-radius lock is integer-exact / ≤1e-5 against the SELF-CONSISTENT
  (non-compensating) upstream value; pc=1 NOT regressed; no green oracle regressed.
- cb-train lib 130/130, cb-model 18/18, full cb-train integration suite 0 FAILED.
- No oracle #[ignore]'d, weakened, assert_ne'd, or pinned-delta'd; the bar-(c) anchor is
  git-committed with corrected doc framing.
- OR: if Wave 1 was infeasible (build / disk NO-GO / self-consistency gate failure), the
  FALLBACK was taken cleanly: production UNTOUCHED, e2e oracle UNCOMMITTED, no weakened
  state, bar (c) deferred with the Spike-001 proof, user checkpointed.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-18-SUMMARY.md` when done.
</output>
