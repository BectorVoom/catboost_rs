# Continue Here — Phase 05, Plan 05-19 (bar (c) / SC-1 / ORD-01), Task A in progress

**Paused:** 2026-06-15 (context exhaustion at ~75%). **Branch:** main. **Tree:** CLEAN + compiles + green.

## One-line state
Closing bar (c) = pc=4 categorical train→predict ≤1e-5. Root cause = THREE mechanisms: (1) initial learn-set shuffle **S**, (2) **Cosine** split-score function (catboost default; cb-train hardcodes L2), (3) per-iteration **structure-fold cycling** `[0,2,0,2,2]`. Currently executing **Task A (Cosine)**; A.1 done, A.2 wiring is next.

## Committed on main (all green)
- `51ad2f8` **T1** — `create_shuffled_indices` = `S` = `fisher_yates(30,0)` (instrument-derived, zero pre-draws) + integer-exact oracle.
- `b8d6455` **T2** — de-risk gate: S-order through UNMODIFIED `materialize_ctr_feature` reproduces the averaging CTR bins (pc4+pc1); CTR math zero-diff.
- `c38391c` — re-plan 05-19 with Cosine (Task A foundational).
- `135d4d8` **Task A.1** — `cb_compute::cosine_split_score` (= `DP/sqrt(D2)`, score_calcers.h:47-72) + unit tests (reverses L2's pc=1 border choice → border 2 = upstream).
- `dcc0bba` — A.2 probe finding (see below).

## NEXT: Task A.2 — wire `score_function` into the tree search (LOW uncertainty, just broad)
EMPIRICAL PROBE (commit `dcc0bba`, reverted): swapping tree.rs plain/ctr/any scoring to Cosine regresses ONLY `eval_metrics_oracle` (2) + `leaf_methods_oracle` (2) — both explicit `score_function: L2`. Everything else (lib 130/130, slice_first, one_hot, tensor_ctr_e2e, ordered_boost_e2e, bootstrap, autolr, ctr_split_scoring, …) STAYS GREEN on Cosine.

Do A.2 in ONE focused pass (it must compile atomically — the field breaks ~25 literals until all are updated):
1. cb-compute `crates/cb-compute/src/runtime.rs`: add `#[derive(Debug,Clone,Copy,PartialEq,Eq,Default)] pub enum EScoreFunction { #[default] Cosine, L2 }` (after the `Loss` enum, before `Derivatives`); export it from `lib.rs` (`pub use runtime::{… EScoreFunction …}`). *(was drafted, reverted)*
2. cb-train `boosting.rs`: add `pub score_function: cb_compute::EScoreFunction` as the LAST field of `BoostParams` (after `combinations_ctr_priors`) + `pub fn score_function_default() -> cb_compute::EScoreFunction { cb_compute::EScoreFunction::Cosine }`.
3. Add `score_function: cb_compute::EScoreFunction::Cosine,` to ALL ~25 `BoostParams { … }` literals (sed-insert after each `BoostParams {` line, EXCLUDING `pub struct BoostParams {`). Files: builder.rs (catboost-rs) + cb-train/tests/{slice_first,one_hot,ordered_boost_e2e,ordered_boost_wiring,overfit,leaf_methods,loss,multi_permutation_e2e,regularization,tensor_ctr_e2e,leaf_weights,bootstrap,eval_metrics,autolr_e2e}_oracle_test.rs + boosting.rs's own literal. Tests already `use cb_compute::{…}` so the path resolves. SET `::L2` on the `eval_metrics` (2 literals), `leaf_methods` (1), and `regression_skeleton`-using `slice_first` literal — verify which exactly by running them after defaulting all to Cosine.
4. tree.rs: thread `score_function: cb_compute::EScoreFunction` into `score_candidate` (363), `score_candidate_ctr_aware` (977), `score_candidate_any` (1265) and their callers `greedy_tensor_search_oblivious` (379), `_perturbed` (435), `_with_ctr` (1126), `select_level_plain` (479), `select_level_perturbed` (519); match → `cosine_split_score` vs `l2_split_score`. The ORDERED path `score_candidate_ordered` (722, per-segment) needs Cosine accumulation across segments then one sqrt — but ordered_boost_e2e is green on L2, so leave ordered on L2 unless its e2e regresses. Update boosting.rs callers to pass `params.score_function`.
5. `cargo test -p cb-train` + `cargo test -p cb-compute` ALL GREEN (re-verify the full structure-search oracle set; STOP if any DEFAULT-Cosine fixture regresses — that means a Cosine-math bug).

## THEN: T3 → T4 → T5 (the S + structure-cycling re-apply)
- **T3** (was prototyped + reverted): in `train_inner` (boosting.rs), `need_shuffle = !cat_columns.is_empty() || boosting_type==Ordered`; `S = create_shuffled_indices(n, random_seed)`; gather feature_values/cat_columns/target/weights into S-order (deferred-init owned bufs, zero-copy on no-shuffle path); invert S on `staged_out`. In `fold.rs` `create_folds` (learning-needed branch): replace the cc-hack with `perms = permutations(n, learning_folds+1, seed)`; Folds[0]=identity, Folds[k]=perms[k] for k in 1..learning_folds, averaging=perms[learning_folds]. (S=perms[0]=the data shuffle.) Remove the now-unused `shuffle_in_place`/`TFastRng64` imports in fold.rs.
- **T4**: per-iteration structure-fold cycling `takenFold = Folds[rng.gen_rand() % learning_folds]` ([0,2,0,2,2] pc4); pc1/pc2 (lf==1) byte-identical.
- **T5**: commit pc=4 e2e oracle (`crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs`, currently untracked) + re-pin `multi_permutation_fold_oracle_test` to self-consistent partitions ([6,0,10,14],[8,8,0,14],…); full-suite green; pc=1 must stay green FOR THE RIGHT REASON (data actually S-shuffled).

## Key files / oracles
- Self-consistent oracle (parity target, NEVER re-pin to a cb-train value): `crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json` (S, P_avg per pc, ctr_bins, structure_fold_cycle [0,2,0,2,2], partitions).
- Instrumented trainer persists at `/tmp/cb_build313` + `/tmp/cb_instr_pkg` + `/tmp/run_pc4_instr.py` (CB_INSTRUMENT_LOG); README `crates/cb-oracle/generator/instrument_live_trainer_README.md`.
- Memory: `cb-train-uses-l2-but-catboost-defaults-cosine.md`, `bar-c-rng-model-derived.md`, `spike001-resolved-ctr-order-vs-leaf-index-order.md`, `instrumented-trainer-toolchain-persists.md`.

## Critical Anti-Patterns

| Anti-pattern | Severity | Why |
|---|---|---|
| Adding the `BoostParams.score_function` field without updating ALL ~25 literals in the SAME change | blocking | Rust requires every field in each struct literal; a partial update leaves the whole workspace non-compiling. Do field + all literals + tree.rs wiring as ONE atomic, build-verified unit. |
| Re-pinning any oracle to a cb-train-produced value (instead of `live_trainer_self_consistent.json` / upstream) to force green | blocking | Violates the parity bar; the whole point of bar (c) is upstream parity. Never weaken (#[ignore]/assert_ne/loosened tolerance). |
| Letting a DEFAULT-Cosine fixture regress when wiring score_function | blocking | Means the Cosine math is wrong, not the config — STOP and fix the math, don't pin around it. |
| Defaulting score_function to L2 (or leaving cb-train on L2) | blocking | catboost CPU default is Cosine (`oblivious_tree_options.cpp:22`); L2 is the latent bug. Default MUST be Cosine. |
| Regressing pc=1 `tensor_ctr_e2e` after re-applying S (T3) | blocking | Hard STOP invariant — the fix is wrong/buggy; debug, do not re-pin. (pc=1 must stay green because the data is genuinely S-shuffled, not via compensation.) |
