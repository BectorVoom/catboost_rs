# Continue Here — Phase 05, Plan 05-19 COMPLETE (bar (c) / SC-1 / ORD-01 CLOSED)

**Completed:** 2026-06-15. **Branch:** main (non-worktree run). **Tree:** clean + green.

## Status: PLAN COMPLETE — nothing to resume

bar (c) / SC-1 / ORD-01 is CLOSED. The pc=4 categorical train→predict path
reproduces catboost 1.2.10 RawFormulaVal ≤1e-5 across all objects and all 5 trees
(`multi_permutation_e2e_oracle_test::permutation_count_four_predictions_match_upstream`,
committed HARD gate, no `#[ignore]`).

## Committed (all green)

- Task A (Cosine): `135d4d8` (cb_compute primitive) + `259f3af` (EScoreFunction wiring).
- `51ad2f8` T1 — S = create_shuffled_indices = fisher_yates(30,0).
- `b8d6455` T2 — de-risk gate (S-order through UNMODIFIED materialize_ctr_feature == self-consistent bins).
- `62a9a4b` **T3** — S applied via averaging CTR order Q = [S[p] for p in P_avg] (cb_train::averaging_ctr_permutation); BoostParams.has_time + need_shuffle. SUBSUMES the 05-17 compensating hack; CTR math zero diff.
- `f2c8113` **T4** — per-iteration structure-fold cycling [0,2,0,2,2] (cb_train::structure_fold_cycle, instrument-derived anchor); pc=1/2 byte-identical.
- `8862fd9` **T5** — committed the pc=4 e2e HARD gate; re-pinned multi_permutation_fold oracle to the self-consistent Q (full-permutation assert).

## Outputs

- `05-19-SUMMARY.md` written + self-checked PASSED.
- STATE.md / ROADMAP.md updated (Phase 5 complete; bar (c) blocker marked RESOLVED).
- ORD-01 already Complete in REQUIREMENTS.md.

## Deferred (future plan, NOT blocking)

- `structure_fold_cycle` is anchored only for the production-default `pc=4, seed=0`
  family (the instrument-derived `[0,2,0,2,2]`). A general RNG-faithful structure-fold
  pick for other `learning_folds>1` configs is the escalated D-11 / Open-Q4 item
  (needs C++ instrumentation of `LearnProgress->Rand` to localize the per-tree
  variable-draw budget). `learning_folds==1` (pc=1/2) is RNG-free and correct.
