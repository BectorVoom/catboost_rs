# Continue Here — Phase 05, Plan 05-19 (bar (c) / SC-1 / ORD-01), Task A DONE, T3 next

**Paused:** 2026-06-15 (after Task A.2 ship; T3 is the research-grade S-subsystem). **Branch:** main (non-worktree run). **Tree:** CLEAN + compiles + green (one expected-pending failure: pc4 e2e).

## One-line state
Closing bar (c) = pc=4 categorical train→predict ≤1e-5. Root cause = THREE mechanisms: (1) initial learn-set shuffle **S**, (2) **Cosine** split-score function (DONE — Task A), (3) per-iteration **structure-fold cycling** `[0,2,0,2,2]`. Task A (Cosine) is COMPLETE & committed. NEXT = T3 (apply S) → T4 (structure-fold cycling) → T5 (commit pc4 e2e + re-pin fold oracle).

## Committed on main (all green)
- `51ad2f8` **T1** — `create_shuffled_indices` = `S` = `fisher_yates(30,0)` (instrument-derived) + integer-exact oracle.
- `b8d6455` **T2** — de-risk gate: S-order through UNMODIFIED `materialize_ctr_feature` reproduces averaging CTR bins (pc4+pc1); CTR math zero-diff.
- `135d4d8` **Task A.1** — `cb_compute::cosine_split_score`.
- `259f3af` **Task A.2** — EScoreFunction { #[default] Cosine, L2 } in cb-compute; `BoostParams.score_function` + `score_function_default()` (Cosine); `split_score()` dispatch threaded through tree.rs score_candidate / _ctr_aware / _any + plain/perturbed/with_ctr/one_hot greedy entry points + select_level_*; ordered path left on L2. Per-fixture score_function PINNED from each model.json `tree_learner_options.score_function`: **Cosine** = tensor_ctr_e2e / one_hot / multi_permutation; **explicit L2** = ALL other numeric/loss/regularization/overfit/bootstrap/eval/leaf/skeleton fixtures. cb-train lib 130/130, cb-compute 47/47, all structure oracles GREEN. (NOTE: the dcc0bba probe under-counted the L2 blast radius — leaf_weights + loss + bootstrap + overfit + regularization + autolr + ordered_* are ALSO L2 fixtures; the authoritative source is each fixture model.json's `score_function`, surveyed below.)

## Authoritative fixture score_function map (from model.json `tree_learner_options.score_function`)
- **Cosine:** `tensor_ctr_e2e`, `multi_permutation_fold/model_pc{1,2,4}`.
- **L2 (everything else):** binclf_skeleton, regression_skeleton, bootstrap/*, eval_metrics/*, leaf_methods/*, loss_extra/*, model_serde/*, ordered_boost_e2e, overfit/*, regularization/*.
- one_hot has NO model.json (D-04 isolation) → left on Cosine default (matches production cat path).

## NEXT: T3 — apply S in train_inner before create_folds (HIGH risk; research-grade)
The self-consistent oracle (`live_trainer_self_consistent.json`) is the parity target. Key facts:
- `S = create_shuffled_indices(30,0) = fisher_yates(30,0)` (DONE, in permutation.rs). `S[k]` = original object at shuffled position k.
- `Q = [S[p] for p in P_avg]` — the averaging-fold CTR order. `P_avg` = `averaging_permutation_over_shuffled` (the averaging perm over the S-SHUFFLED data). The current cc-hack in `fold.rs::create_folds` draws `P_avg` directly over UNSHUFFLED data → it reproduces the pc4 PARTITION `[6,0,10,14]` but the WRONG per-mixed-bucket bin→object assignment (the "compensating error" memory warns about).
- pc4 per-iter (oracle): fold cycle `[0,2,0,2,2]`, borders `[7,2]`(fold0)/`[3,7]`(fold2), partitions `[6,0,10,14]`(fold0)/`[8,8,0,14]`(fold2).
- pc1: cycle all-0, partition `[6,0,7,17]` every tree.

### T3 plan (per the PLAN body + memory `bar-c-rng-model-derived.md`)
1. Add `has_time: bool` to `BoostParams` (default false) — **another ~25-literal broad change like A.2** (sed-insert `has_time: false,` into every literal; build atomically). All current fixtures are has_time=false.
2. `need_shuffle = (!cat_columns.is_empty() || boosting_type==Ordered) && !has_time` (transcribe NeedShuffle, preprocess.cpp:161).
3. In `train_inner`: when need_shuffle, seed ONE persistent `TFastRng64::from_seed(random_seed)`, draw `S = shuffle_in_place(n, &mut rng)` FIRST (zero pre-draws), gather feature_values/cat_columns/target/weights/target_class into S-order (deferred-init owned bufs; zero-copy on no-shuffle path), pass the HELD rng into create_folds so the averaging shuffle continues from post-S phase. Invert S on `staged_out` + final predictions (original object order).
4. `fold.rs::create_folds`: replace the cc-hack (one gen_rand per learning-fold position then shuffle) with the **permutations model**: `perms = permutations(n, learning_folds+1, seed)`; but note S already consumed perms[0] off the SHARED stream — so the averaging shuffle is the NEXT draw on the held rng. Validate the resulting averaging partition == oracle (`[6,0,10,14]` pc4 / `[6,0,7,17]` pc1) AND that Q = [S[p] for p in P_avg] reproduces `ctr_bins_object_order` — NOT just the partition. Remove now-unused `shuffle_in_place`/`TFastRng64` imports if they become dead.
5. **HARD STOP invariants:** pc=1 `tensor_ctr_e2e` MUST stay green for the RIGHT reason (data genuinely S-shuffled). Numeric/one-hot/Plain-no-CTR (need_shuffle=false) BYTE-IDENTICAL. CTR math zero diff. No re-pin to a cb-train value.

## THEN: T4 → T5
- **T4**: per-iteration structure-fold cycling `takenFold = Folds[rng.gen_rand() % learning_folds]` → pc4 `[0,2,0,2,2]`; materialize the structure CTR under the SELECTED fold each iter; leaf VALUES stay on the fixed AveragingFold. pc1/pc2 (lf==1) byte-identical (modulo always 0). New oracle `structure_fold_cycle_oracle_test.rs` pinning `[0,2,0,2,2]` + per-tree borders [7,2]/[3,7].
- **T5**: finalize/commit `crates/cb-train/tests/multi_permutation_e2e_oracle_test.rs` (currently UNTRACKED; the `permutation_count_four_predictions_match_upstream` HARD gate ≤1e-5 — has the score_function field added, currently FAILING by ~0.009 pending T3/T4). Re-pin `multi_permutation_fold_oracle_test.rs` to self-consistent partitions. Full cb-train + cb-model green.

## Key files / oracles
- Parity target (NEVER re-pin to a cb-train value): `crates/cb-train/tests/fixtures/multi_permutation_fold/live_trainer_self_consistent.json` (S, P_avg=averaging_permutation_over_shuffled, Q=object_permutation_Q, ctr_bins_object_order, structure_fold_cycle [0,2,0,2,2], per-iter partitions).
- Instrumented trainer persists at `/tmp/cb_build313` + `/tmp/cb_instr_pkg` + `/tmp/run_pc4_instr.py`.
- Memory: `cb-train-uses-l2-but-catboost-defaults-cosine.md` (Cosine — DONE), `bar-c-rng-model-derived.md` (averaging fold = permutations(n,learning_folds+1,seed)[learning_folds], S=[0]), `pc4-partition-compensating-error.md`, `spike001-resolved-ctr-order-vs-leaf-index-order.md`, `instrumented-trainer-toolchain-persists.md`.

## Runtime note (this run)
Non-worktree mode (on main, `.git` is a directory). The executor owns STATE.md/ROADMAP.md updates. The pc4 e2e oracle file `multi_permutation_e2e_oracle_test.rs` is UNTRACKED on main and stays untracked until T5.

## Critical Anti-Patterns
| Anti-pattern | Severity | Why |
|---|---|---|
| Re-pinning any oracle to a cb-train value (not live_trainer_self_consistent.json / upstream) | blocking | Violates the parity bar. No #[ignore]/assert_ne/loosened tolerance. |
| Adding `BoostParams.has_time` (T3) or any field without updating ALL ~25 literals in the SAME change | blocking | Partial struct-literal update breaks the whole workspace compile. |
| Regressing pc=1 `tensor_ctr_e2e` after applying S (T3) | blocking | Must stay green because data is genuinely S-shuffled, not via compensation. Debug, never re-pin. |
| Closing pc4 partition WITHOUT fixing Q (the per-bucket bin→object assignment) | blocking | The cc-hack reproduces the partition on a COMPENSATING wrong-perm+wrong-CTR-bins. T3 must make Q = [S[p] for p in P_avg] correct, validated against ctr_bins_object_order — not just the partition counts. |
| Letting any DEFAULT-Cosine fixture regress | blocking | Means a Cosine-math bug. STOP and fix the math. |
