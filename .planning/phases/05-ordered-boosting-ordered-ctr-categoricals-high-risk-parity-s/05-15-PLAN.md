---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-train/src/fold.rs
  - crates/cb-train/tests/multi_permutation_fold_oracle_test.rs
autonomous: true
requirements: [ORD-01]
gap_closure: true

must_haves:
  truths:
    - "For permutation_count=1 the AveragingFold permutation is byte-identical to the current 05-14-validated draw (the existing tensor_ctr_e2e + averaging_fold_permutation oracles stay green — zero regression on the gated config)."
    - "For permutation_count>1 the AveragingFold pre-averaging GenRand draw fires immediately before the averaging-fold shuffle (idx == learning_folds), NOT before the first learning shuffle (idx == 1)."
    - "For permutation_count>=2 the multi-fold draw order (each shuffled learning fold then the averaging fold, all off one persistent TFastRng64, with exactly one pre-averaging GenRand) is locked integer-exact against an in-env catboost 1.2.10 fixture."
    - "cb-train lib unit tests stay 130/130; the tensor_ctr_e2e and ordered_boost_e2e e2e gates stay green (no parity regression)."
  artifacts:
    - path: "crates/cb-train/src/fold.rs"
      provides: "create_folds pre-averaging draw guarded on the averaging fold position (idx == learning_folds) for all permutation_count values"
      contains: "idx == learning_folds"
    - path: "crates/cb-train/tests/multi_permutation_fold_oracle_test.rs"
      provides: "Integer-exact multi-permutation (permutation_count>=2) AveragingFold draw-order oracle vs catboost 1.2.10"
      contains: "compare_permutation"
  key_links:
    - from: "crates/cb-train/src/fold.rs::create_folds"
      to: "crates/cb-train/src/permutation.rs::shuffle_in_place"
      via: "one persistent TFastRng64; pre-averaging gen_rand() fires only at idx == learning_folds"
      pattern: "idx == learning_folds"
    - from: "crates/cb-train/tests/multi_permutation_fold_oracle_test.rs"
      to: "crates/cb-train/src/fold.rs::create_folds"
      via: "create_folds(... permutation_count>=2 ...) compared integer-exact to upstream draw order"
      pattern: "create_folds\\("
---

<objective>
Close GAP 2 (WR-01 / ORD-01): the pre-averaging `GenRand` draw in `create_folds`
currently fires before the FIRST learning shuffle (`idx == 1`) via the
`first_real_shuffle` flag. That is correct ONLY for `permutation_count=1`, where
`learning_folds == 0` so `idx == 1` IS the averaging fold. For the production
default `permutation_count=4` (`learning_folds == 3`), the pre-draw fires before
the first of three learning shuffles; the averaging fold then draws at an
RNG call-count that was never validated against upstream. Every CTR and Ordered
train path under the default config is therefore unvalidated.

Move the pre-averaging draw to fire immediately before the AVERAGING fold's
shuffle (`idx == learning_folds`) for ALL `permutation_count` values, leaving the
`permutation_count=1` draw order byte-identical (because at `permutation_count=1`,
`idx == learning_folds == 1` is already where the draw fires today — both the old
`first_real_shuffle` guard and the new `idx == learning_folds` guard fire on the
same fold). Add an integer-exact `permutation_count>=2` draw-order oracle keyed to
an in-env catboost 1.2.10 fixture so the multi-permutation path is locked.

Purpose: a user training with default params (permutation_count=4) currently gets
an unvalidated permutation draw that may silently diverge from upstream parity.
This pins the correct upstream position.

Output: corrected `create_folds` pre-draw guard + a new multi-permutation
draw-order oracle test.
</objective>

<upstream_findings>
The pre-averaging draw position was settled by reading the vendored upstream
fold-creation loop and the Shuffle/RNG-consumption helpers:

- `catboost/private/libs/algo/learn_context.cpp:524` and `:529` — each learning
  fold `foldIdx` is built with `shuffle = foldIdx != 0`, so `Folds[0]` is the
  IDENTITY (no shuffle) and learning folds `1..LearningFoldCount` are shuffled.
- `catboost/private/libs/algo/learn_context.cpp:575-578` — the AveragingFold is
  built AFTER all learning folds with `shuffle = foldsCreationParams.IsAverageFoldPermuted`
  (`= hasCtrs`), so it is the LAST fold created and its shuffle is the last draw
  in the fold-creation sequence.
- `catboost/private/libs/algo/fold.cpp:43-95` (`InitPermutationData`) — when
  `shuffle == false` the function does a plain `std::iota` and consumes ZERO
  draws on `rand`; when `shuffle == true` it calls `Shuffle(..., rand)`.
- `catboost/libs/data/objects_grouping.cpp:191-224` (`NCB::Shuffle`) →
  `catboost/libs/helpers/permutation.h:83-89` (`CreateShuffledIndices`) →
  `util/random/shuffle.h:24-32` — each shuffled fold consumes exactly one
  Fisher-Yates pass (`n-1` draws) on the SHARED persistent `Rand`; there is no
  extra inter-fold `GenRand` inside this fold-creation loop itself.

CONCLUSION: upstream draw order is the learning shuffles in order (fold 0 = zero
draws, folds 1..learning_folds-1 = one Fisher-Yates each) THEN the averaging
shuffle. The single extra `GenRand` that 05-14 empirically validated against
catboost 1.2.10 (the "RNG call-count 1" pre-averaging advance that produces the
`[6,0,7,17]` partition at permutation_count=1) is tied to the AVERAGING fold's
construction, so it belongs immediately before the averaging shuffle — i.e. it
must be guarded on `idx == learning_folds`, NOT on the first learning shuffle.
At `permutation_count=1` (`learning_folds == 1`) these two positions coincide,
which is why the gated e2e gate stayed green while the multi-permutation path was
silently wrong. This plan generalizes the guard correctly and ADDS an oracle that
catches the multi-permutation case the existing gates never exercise.
</upstream_findings>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CTR-LEAF-VALUE-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the pre-averaging GenRand draw to fire before the averaging fold (idx == learning_folds) for all permutation_count</name>
  <read_first>
    - crates/cb-train/src/fold.rs (the `create_folds` learning-permutation-needed branch, lines ~254-287; the `first_real_shuffle` flag at lines ~270-281 and its doc comment at lines ~257-268; `learning_fold_count` at ~167-174; `build_fold` `is_averaging = idx == learning_folds` at ~301)
    - crates/cb-train/src/permutation.rs (`shuffle_in_place` at ~127-143 — the Fisher-Yates pass driven off the shared rng; do NOT change it)
    - catboost-master/catboost/private/libs/algo/learn_context.cpp (lines 515-592 — the learning-fold loop `shuffle = foldIdx != 0` and the AveragingFold `BuildPlainFold` with `shuffle = IsAverageFoldPermuted` built AFTER all learning folds; this pins that the averaging shuffle is the LAST draw)
    - catboost-master/catboost/private/libs/algo/fold.cpp (lines 43-95 `InitPermutationData` — `shuffle == false` consumes ZERO draws; `shuffle == true` calls `Shuffle(..., rand)`)
    - crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs (the existing permutation_count=1 draw-order oracle this change must NOT regress)
  </read_first>
  <action>
    In `create_folds`, replace the `first_real_shuffle` boolean-flag mechanism in
    the learning-permutation-needed branch with a position-based guard. The single
    pre-averaging `rng.gen_rand()` MUST fire exactly once, immediately before the
    AVERAGING fold's `shuffle_in_place`, i.e. when `idx == learning_folds` (the
    averaging fold position, which matches `build_fold`'s existing
    `is_averaging = idx == learning_folds`). Concretely: inside the `idx != 0`
    branch, before calling `shuffle_in_place(n, &mut rng)`, gate the single
    `rng.gen_rand()` on `idx == learning_folds` instead of on the
    `first_real_shuffle` flag. Delete the `first_real_shuffle` mutable variable
    entirely. The learning folds `1..learning_folds` each take exactly one
    `shuffle_in_place` with NO pre-draw; the averaging fold (`idx ==
    learning_folds`) takes the one pre-averaging `gen_rand()` then its
    `shuffle_in_place`. For `permutation_count=1` (`learning_folds == 1`) the
    draw still fires before `idx == 1`, so the byte-stream is unchanged — verify
    this by running the existing averaging_fold_permutation_oracle_test. Update the
    doc comment (the PRE-AVERAGING DRAW paragraph at ~260-268 and the draw-order
    contract at ~195-216) to state the corrected position: "the single
    pre-averaging GenRand fires immediately before the AVERAGING fold's shuffle
    (idx == learning_folds), regardless of how many learning shuffles precede it;
    at permutation_count=1 this coincides with idx==1." Cite the upstream
    locations (`learn_context.cpp:524/575-578`, `fold.cpp:43-95`) in the comment.
    No fenced code blocks in the comment beyond the existing transcribed snippets.
    Keep the numeric / Plain-no-CTR branch (the `!permutation_needed_for_learning`
    path) byte-identical — do NOT touch it.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test averaging_fold_permutation_oracle_test 2>&1 | tail -5; cargo test -p cb-train --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train --test averaging_fold_permutation_oracle_test` exits 0 (3/3 PASS — permutation_count=1 draw order byte-unchanged).
    - `cargo test -p cb-train --lib` exits 0 with 130/130 PASS (no fold/unit regression).
    - `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` exits 0 (3/3, the permutation_count=1 CTR e2e gate ≤1e-5 still green).
    - `cargo test -p cb-train --test ordered_boost_e2e_oracle_test` exits 0 (2/2 ≤1e-5).
    - `grep -n "first_real_shuffle" crates/cb-train/src/fold.rs` returns ZERO matches (the flag is removed).
    - `grep -c "idx == learning_folds" crates/cb-train/src/fold.rs` is >= 2 (the new pre-draw guard plus the existing `build_fold` is_averaging assignment).
    - `cargo check --tests -p cb-train` reports 0 errors, 0 warnings.
  </acceptance_criteria>
  <done>
    The pre-averaging `gen_rand()` fires at `idx == learning_folds` for every
    `permutation_count`; the `first_real_shuffle` flag is gone; the
    permutation_count=1 draw order is byte-identical (existing oracles green); the
    numeric path is untouched; the doc comment documents the corrected position
    with upstream citations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add an integer-exact permutation_count>=2 AveragingFold draw-order oracle vs catboost 1.2.10</name>
  <read_first>
    - crates/cb-train/tests/averaging_fold_permutation_oracle_test.rs (the analog to mirror: `Stage::Permutation` integer-exact comparison via `cb_oracle::compare_permutation`, `create_folds(...)` driver, `find(|f| f.is_averaging)`, NO #[ignore])
    - crates/cb-train/src/fold.rs (`create_folds` signature and the corrected pre-draw position from Task 1)
    - crates/cb-train/src/permutation.rs (`fisher_yates_permutation`, `shuffle_in_place`, `permutations` — how draws compose off a persistent TFastRng64)
    - crates/cb-core/src/... TFastRng64 (`from_seed`, `gen_rand`, `uniform` — used to construct the expected draw order)
    - .planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CTR-LEAF-VALUE-RESEARCH.md (Open Q4 — the multi-fold structure: fold0 identity, folds 1..pc-1 shuffled, then averaging fold; the pre-averaging draw)
    - catboost-master/catboost/private/libs/algo/learn_context.cpp (lines 515-592 — fold-creation order pinning the draw sequence)
  </read_first>
  <action>
    Create `crates/cb-train/tests/multi_permutation_fold_oracle_test.rs`. Generate
    the upstream fixture OFFLINE using catboost 1.2.10 from the project `.venv`
    (per project memory catboost==1.2.10 is importable there): write a small
    generator invocation (or inline the validated draw order as a frozen constant
    with a comment citing the catboost 1.2.10 dump) for `permutation_count=2`
    (`learning_folds == 1`) and, if cheaply obtainable, `permutation_count=4`
    (`learning_folds == 3`) on the tensor_ctr_e2e config (N=30, seed=0, hasCtrs,
    Plain so dynamic_body_tail=false). Prefer the SELF-CONSISTENT approach used by
    the analog oracle: derive the EXPECTED draw order directly from the production
    `TFastRng64` primitives so no `.npy` fixture is committed — for
    permutation_count=2, the expected sequence is: learning fold0 = identity
    (zero draws); learning fold1 = the first `shuffle_in_place` off
    `TFastRng64::from_seed(0)`; then one pre-averaging `gen_rand()`; then the
    averaging fold = the next `shuffle_in_place` off the SAME rng. Assert each
    fold's permutation integer-exact via `cb_oracle::compare_permutation`
    (`Stage::Permutation`), and assert the averaging fold is DISTINCT from the
    learning fold1. Add test fns: `multi_permutation_count_two_draw_order_matches_upstream`,
    `multi_permutation_averaging_fires_after_all_learning_shuffles`,
    `multi_permutation_learning_fold_zero_is_identity`. If a true upstream
    catboost 1.2.10 cross-check is run, assert the cb-train averaging permutation
    equals the upstream `model`/dump draw order integer-exact (NOT a 1e-5 value
    check). NO `#[ignore]`. The header doc comment must explain WHY this gate
    exists: the production default permutation_count=4 was previously unvalidated
    (WR-01), and the pre-averaging draw must fire after ALL learning shuffles, not
    before the first. Allow the standard test-file clippy exemptions
    (`unwrap_used`/`expect_used`/`panic`/`indexing_slicing`).
  </action>
  <verify>
    <automated>cargo test -p cb-train --test multi_permutation_fold_oracle_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train --test multi_permutation_fold_oracle_test` exits 0 with all tests PASS (integer-exact draw-order, no 1e-5 fuzz).
    - The test exercises `create_folds(30, 2, true, false, 2.0, 0)` (permutation_count=2) at minimum; the averaging fold permutation is asserted to equal the `shuffle_in_place` draw taken AFTER the learning fold1 shuffle AND after exactly one pre-averaging `gen_rand()`.
    - `grep -c "#\[ignore\]" crates/cb-train/tests/multi_permutation_fold_oracle_test.rs` (filtered for non-comment) is 0 — no ignored tests.
    - `grep -c "compare_permutation" crates/cb-train/tests/multi_permutation_fold_oracle_test.rs` is >= 1 (uses the D-03 integer-exact comparator).
    - `cargo check --tests -p cb-train` reports 0 errors, 0 warnings.
    - `cargo test -p cb-train --lib` still 130/130.
  </acceptance_criteria>
  <done>
    A non-ignored integer-exact oracle locks the permutation_count>=2 multi-fold
    draw order (learning fold0 identity, learning folds shuffled, one
    pre-averaging gen_rand, averaging fold shuffled), proving the corrected
    `create_folds` matches the upstream fold-creation draw sequence for the
    production default config family.
  </done>
</task>

</tasks>

<artifacts_produced>
This plan creates or modifies the following symbols:

- `crates/cb-train/src/fold.rs::create_folds` — the learning-permutation-needed
  branch loses the `first_real_shuffle: bool` local; the single pre-averaging
  `rng.gen_rand()` is now guarded on `idx == learning_folds` (fires before the
  averaging fold's `shuffle_in_place` for ALL permutation_count). Signature
  UNCHANGED: `create_folds(n: usize, permutation_count: usize,
  permutation_needed_for_learning: bool, dynamic_body_tail: bool,
  fold_len_multiplier: f64, random_seed: u64) -> Vec<Fold>`. Doc comment updated
  (draw-order contract + PRE-AVERAGING DRAW paragraph) with upstream citations.
- `crates/cb-train/tests/multi_permutation_fold_oracle_test.rs` — NEW test file.
  New test fns: `multi_permutation_count_two_draw_order_matches_upstream`,
  `multi_permutation_averaging_fires_after_all_learning_shuffles`,
  `multi_permutation_learning_fold_zero_is_identity`. Uses
  `cb_oracle::compare_permutation` (`Stage::Permutation`) and `cb_train::create_folds`.

No production function signatures change. The numeric / Plain-no-CTR branch of
`create_folds` is byte-identical.
</artifacts_produced>

<threat_model>
## Trust Boundaries

No new trust boundary. This is an internal RNG draw-order correctness change in a
gradient-boosting training library. No untrusted input, network, auth, or
deserialization path is introduced or touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15-01 | Tampering | RNG draw order in `create_folds` | mitigate | Integer-exact oracle (`multi_permutation_fold_oracle_test`) locks the multi-permutation draw sequence; existing permutation_count=1 oracles guard the gated config. |

No new attack surface — internal RNG draw-order correctness and test fixtures
only; no untrusted input, network, or auth paths touched.
</threat_model>

<verification>
- `cargo test -p cb-train --test averaging_fold_permutation_oracle_test` — 3/3 (permutation_count=1 byte-unchanged).
- `cargo test -p cb-train --test multi_permutation_fold_oracle_test` — all PASS (new, permutation_count>=2 locked).
- `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` — 3/3 ≤1e-5.
- `cargo test -p cb-train --test ordered_boost_e2e_oracle_test` — 2/2 ≤1e-5.
- `cargo test -p cb-train --lib` — 130/130.
- `cargo check --tests -p cb-train` — 0 errors, 0 warnings.
- `grep -n "first_real_shuffle" crates/cb-train/src/fold.rs` — 0 matches.
</verification>

<success_criteria>
The pre-averaging draw fires at the upstream-correct position (`idx ==
learning_folds`) for ALL permutation_count values; the permutation_count=1 draw
order is byte-identical (every existing oracle green); a new non-ignored
integer-exact oracle locks the permutation_count>=2 multi-fold draw order; no lib
or e2e regression.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-15-SUMMARY.md` when done.
</output>
