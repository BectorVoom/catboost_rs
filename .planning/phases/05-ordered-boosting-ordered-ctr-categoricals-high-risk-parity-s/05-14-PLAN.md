---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 14
type: execute
wave: 3
depends_on: ["05-13"]
gap_closure: true
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-model/src/model.rs
  - crates/cb-model/src/apply.rs
  - crates/cb-train/tests/ctr_split_scoring_test.rs
  - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
autonomous: true
requirements: [ORD-05]

must_haves:
  truths:
    - "After training, each chosen CTR split's WHOLE-SET inference CtrValueTable (build_final_ctr over the whole learn set, NOT the prefix) is baked into Model.ctr_data with the correct per-CTR Scale/Shift derived from the prior PAIR (Borders prior 0.5/1 → calc_normalization gives shift=0, norm=1 → Shift=0, Scale=ctr_border_count/norm=15), under the canonical ctr_table_key the apply path reconstructs."
    - "The apply path threads each CtrSplit's Scale/Shift into the inference Calc on BOTH the table-FOUND AND the NOT-FOUND branches (NOT the hardcoded shift=0/scale=1) so the CTR-value split test (ctr_value > border) is in the same scaled space as the baked borders — passes_ctr_split uses split-carried shift/scale on every path."
    - "The FULL multi-tree `tensor_ctr_e2e_oracle_predictions_match_upstream` passes ≤1e-5 vs upstream catboost 1.2.10 through cb_model::predict_raw_cat, driven by train_cat, with NO #[ignore] / NO weakened tolerance / NO fabricated fixtures — the user's hard gate (STATE.md 2026-06-14)."
    - "The numeric / one-hot / ordered-boosting / leaf-methods apply + train oracles remain byte-identical (regression guard) — Scale/Shift default 0.0/1.0 for non-CTR splits, ctr_data None for numeric models."
  artifacts:
    - path: "crates/cb-model/src/apply.rs"
      provides: "passes_ctr_split threads split.shift/split.scale into ctr_value_for_combined_projection on the FOUND branch AND into calc_inference on the NOT-FOUND branch"
      contains: "split.scale"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "train_cat bakes each chosen CTR split's whole-set CtrValueTable into a CtrData with derived Scale/Shift and exposes it for the e2e call site"
      contains: "build_final_ctr"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/ctr/final_ctr.rs"
      via: "build_final_ctr bakes the chosen CTR split's whole-set CtrValueTable into Model.ctr_data"
      pattern: "build_final_ctr"
    - from: "crates/cb-model/src/apply.rs"
      to: "crates/cb-model/src/ctr_data.rs"
      via: "calc_inference / ctr_value_for_combined_projection with split-carried shift/scale on both branches"
      pattern: "split.scale"
    - from: "crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs"
      to: "cb_train::train_cat"
      via: "the e2e oracle drives training through the cat-aware entry point + with_ctr_data"
      pattern: "train_cat"
---

<objective>
Complete ORD-05: bake the chosen CTR splits' whole-set inference CtrValueTables
into Model.ctr_data with the correct Scale/Shift, thread Scale/Shift through the
apply path on BOTH branches, and close the FULL multi-tree e2e hard gate through
train_cat + predict_raw_cat. This carries forward the CORRECT parts of the
original (blocked) 05-12 — its bake (Task 2) and e2e wiring (Task 3) were always
right; only its leaf-value assumption was wrong, and that is fixed by Plans 05-12
(draw order) + 05-13 (two-materialization leaf values).

By this plan the structure search (05-13 Task 1), the averaging-fold leaf values
(05-13 Task 2), and now the apply-side Scale/Shift all align, so the production
apply path reaches the upstream scaled-border space ([2.999, 7.999] in the
Scale=15 quantized space) and the predictions match upstream bit-for-bit within
1e-5 across all 5 trees.

Purpose: The upstream fixture model (verified: 5 trees, 2 OnlineCtr splits/tree
on cat feature 0, Borders prior 0.5/1, Scale=15 Shift=0, borders [2.999, 7.999])
cannot be reproduced at apply time until (a) the chosen splits' ctr_data is baked
(whole-set inference totals — research "Summary" apply partition [10,0,0,20]),
and (b) the apply path applies the model's Scale/Shift instead of the current
hardcoded 1.0/0.0 (which never reaches the scaled-border space).

Output: ctr_data baking in train_cat, Scale/Shift threaded into apply.rs on both
branches, and a GREEN unweakened `tensor_ctr_e2e_oracle_predictions_match_upstream`.
Closes ORD-05 / Roadmap SC-5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CTR-LEAF-VALUE-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-11-SUMMARY.md

# Source-of-truth code this plan extends
@crates/cb-train/src/boosting.rs
@crates/cb-train/src/tree.rs
@crates/cb-train/src/ctr/final_ctr.rs
@crates/cb-train/src/ctr/online.rs
@crates/cb-train/src/ctr/calc_ctr.rs
@crates/cb-model/src/model.rs
@crates/cb-model/src/apply.rs
@crates/cb-model/src/ctr_data.rs
@crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bake ctr_data (Scale/Shift) into the Model + thread Scale/Shift through apply on BOTH branches</name>
  <read_first>
    - crates/cb-train/src/ctr/final_ctr.rs (build_final_ctr — the whole-set inference CtrValueTable producer over an OnlineCtrAccumulator: per-bucket class counts for Borders)
    - crates/cb-train/src/ctr/online.rs (accumulate_online — the whole-set per-bucket accumulation over a SINGLE `&[&str]` column; for a combined projection the bake must accumulate over the COMBINED-key string form OR a combined-key accumulation mirroring it — do NOT reinvent the histogram)
    - crates/cb-train/src/ctr/calc_ctr.rs (calc_normalization(prior) → (shift, norm); calc_ctr_inference((cic+PriorNum)/(tot+PriorDenom) then (ctr+Shift)·Scale) — the Scale=border_count/norm, Shift=shift derivation; the scalar prior arg is the NUMERATOR)
    - crates/cb-model/src/ctr_data.rs (CtrData { tables }, CtrValueTable shape int_counts/hashes/target_classes_count, calc_inference, ctr_value_for_combined_projection, the ctr_table_key contract; Prior { num, denom })
    - crates/cb-model/src/apply.rs (passes_ctr_split — the hardcoded `/* shift = */ 0.0, /* scale = */ 1.0` at the table-FOUND ctr_value_for_combined_projection call (apply.rs:177-184) AND `calc_inference(0.0, 0.0, split.prior, 0.0, 1.0)` at the NOT-FOUND branch (apply.rs:187); ctr_table_key)
    - crates/cb-model/src/model.rs (CtrSplit { projection, ctr_type, prior: Prior{num,denom}, target_border_idx, border }; from_trained lifting CtrSplitSpec → CtrSplit; with_ctr_data)
    - crates/cb-train/src/tree.rs (CtrSplitSpec — prior_num/prior_denom PAIR + border from 05-13; add shift/scale fields here)
    - .planning/phases/05-…/05-CTR-LEAF-VALUE-RESEARCH.md ("Q2" the apply scaled CTR ((good+0.5)/(tot+1))·15 tested >{2.999,7.999}; "Summary" apply partition whole-set totals [10,0,0,20]→leaves {0,3})
    - crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json (ctrs[0]: Scale=15, Shift=0, prior_numerator=0.5, prior_denomerator=1, borders [2.999,7.999])
  </read_first>
  <behavior>
    - Test (Scale/Shift derivation): for a Borders CTR with prior_num=0.5, prior_denom=1 and ctr_border_count=15, the derived (Shift, Scale) equal (0.0, 15.0) — calc_normalization(0.5) gives shift=0, norm=1, Scale=15/1=15 — matching the fixture ctrs[0].{shift,scale}. The bake sets BOTH prior_num and prior_denom on the chosen CtrSplitSpec (a non-1 denominator divides correctly rather than collapsing).
    - Test (apply uses split Scale/Shift on the FOUND branch): passes_ctr_split passes split.shift/split.scale into ctr_value_for_combined_projection (a CTR split with Scale=15 vs Scale=1 yields different split outcomes — proving the found-branch hardcode is removed).
    - Test (apply uses split Scale/Shift on the NOT-FOUND branch): for a split whose combined bucket is ABSENT from the baked table, passes_ctr_split feeds split.shift/split.scale into calc_inference (NOT literal 0.0/1.0) — a not-found split with Scale=15 vs Scale=1 yields a different empty-CTR scaled value (different `ctr_value > border` outcome), proving the not-found hardcode is removed and absent-bucket objects are scaled into the same border space.
    - Test (bake round-trips to apply): a baked CtrValueTable for the chosen projection keyed by ctr_table_key, looked up through passes_ctr_split with the split's Scale/Shift, reproduces the upstream inference Calc value for a known document (the apply whole-set partition [10,0,0,20] for {0}).
  </behavior>
  <action>
    Two changes that together make the apply path reach the upstream scaled-border space.

    A) Add Shift/Scale to the model CTR split + thread through apply ON BOTH BRANCHES. Extend `cb_model::CtrSplit` with `shift: f64` and `scale: f64` (default 0.0/1.0 for back-compat, set from the trainer). In `crates/cb-model/src/apply.rs::passes_ctr_split`, replace the hardcoded shift/scale in BOTH branches:
      - the table-FOUND branch (apply.rs:177-184): replace `/* shift = */ 0.0, /* scale = */ 1.0` passed to `ctr_value_for_combined_projection` with `split.shift, split.scale`;
      - the NOT-FOUND branch (apply.rs:187): replace `calc_inference(0.0, 0.0, split.prior, 0.0, 1.0)` so the trailing shift/scale args become `split.shift, split.scale` (i.e. `calc_inference(0.0, 0.0, split.prior, split.shift, split.scale)`) — the empty-CTR value for an absent combined bucket MUST be scaled into the same border space, otherwise absent-bucket objects are silently mis-scaled and compared against the wrong border.
    Update `cb_train::CtrSplitSpec` (tree.rs) with `shift: f64, scale: f64`, and `cb_model::Model::from_trained` (model.rs) to carry them into `CtrSplit`. Update the 05-09-migrated cb-model test constructions of `CtrSplit` to include the two new fields (default 0.0/1.0).

    B) Bake ctr_data in train_cat. After the boosting loop, for each DISTINCT chosen CTR split `(projection, ctr_type, prior_num, prior_denom)`:
    1. Accumulate the WHOLE learn set into per-bucket buckets keyed on the COMBINED projection hash (the SAME combined_hash + first-seen remap Plan 05-11/05-13 used), via `accumulate_online` — feed it the combined-key string form OR factor a combined-key accumulation that mirrors it (do NOT reinvent the per-bucket histogram). This is the WHOLE-SET totals (NOT the read-before-increment prefix — the inference table is the completed totals, research "Summary" apply partition [10,0,0,20], online_ctr.cpp:916-939).
    2. Build the inference `FinalCtrTable` via `build_final_ctr(acc, ECtrType::Borders)`, convert it into a `cb_model::CtrValueTable` (same int_counts/hashes/target_classes_count shape; the per-bucket projection HASH is the combined_hash so the apply-time fold reproduces the lookup key), and insert it into a `CtrData` under `ctr_table_key((projection, ctr_type))` — the SAME canonical key apply.rs reconstructs. Use checked i64 accumulation for the counters (WR-02 — counts bounded by N).
    3. Derive `(Shift, Scale)` from `calc_normalization(prior_num)` and `ctr_border_count`: `Shift = shift`, `Scale = ctr_border_count as f64 / norm` (Borders → Shift=0, Scale=15 for the fixture). Set `shift`/`scale` AND copy BOTH `prior_num`/`prior_denom` onto each chosen `CtrSplitSpec` (so they flow to `CtrSplit.prior {num,denom}` and shift/scale via from_trained — the denominator is never dropped).
    4. Expose the baked `CtrData` from `train_cat` (e.g. `train_cat` returns `(Model, Option<CtrData>)` OR sets a trainer-side field the oracle reads) so the e2e call site (Task 2) attaches it via `cb_model::Model::with_ctr_data`. Keep `train`'s return type UNCHANGED. Every bucket lookup is checked `.get`; a missing combined bucket is the not-found→empty path (now correctly scaled per change A).

    Checked `.get` only; checked i64 accumulation; no `unwrap`/`expect`/`panic`/`anyhow`; source/test separation (the bake/derivation tests live in `crates/cb-train/tests/ctr_split_scoring_test.rs`). All float reductions via cb_core::sum_f64.
  </action>
  <verify>
    <automated>cargo test -p cb-model --test apply_oracle_test --test predict_oracle_test --test ctr_data_roundtrip_test && cargo test -p cb-train --test ctr_split_scoring_test</automated>
  </verify>
  <acceptance_criteria>
    - CtrSplit carries Shift/Scale and apply uses them on BOTH branches (source assertion: `grep -c "split.scale\|split.shift" crates/cb-model/src/apply.rs` >= 2 — at least once in the FOUND ctr_value_for_combined_projection call AND once in the NOT-FOUND calc_inference call).
    - The FOUND-branch hardcode is removed (source assertion: `grep -c "scale = \*/ 1.0" crates/cb-model/src/apply.rs` == 0 — the `/* scale = */ 1.0` literal is gone).
    - The NOT-FOUND-branch hardcode is removed (source assertion: `grep -c "calc_inference(0.0, 0.0, split.prior, 0.0, 1.0)" crates/cb-model/src/apply.rs` == 0 AND `grep -c "calc_inference(0.0, 0.0, split.prior, split.shift, split.scale)" crates/cb-model/src/apply.rs` >= 1 — the absent-bucket path threads the split's shift/scale; this guards the silent-mis-scaling gap).
    - The bake uses the locked whole-set producer (source assertion: `grep -c "build_final_ctr" crates/cb-train/src/boosting.rs` >= 1; `grep -c "accumulate_online\|combined" crates/cb-train/src/boosting.rs` >= 1).
    - The bake key matches the apply key (source assertion: the trainer's table key uses the SAME ctr_table_key form as apply.rs — documented + grep-cross-checked).
    - Scale/Shift derivation cited with the prior PAIR (source assertion: `grep -c "calc_normalization" crates/cb-train/src/boosting.rs` >= 1; `grep -c "prior_num" crates/cb-train/src/boosting.rs` >= 1 AND `grep -c "prior_denom" crates/cb-train/src/boosting.rs` >= 1; Scale = ctr_border_count/norm documented).
    - Test-command assertion: `cargo test -p cb-model --test apply_oracle_test --test predict_oracle_test --test ctr_data_roundtrip_test` AND `cargo test -p cb-train --test ctr_split_scoring_test` ALL pass (including the FOUND + NOT-FOUND scale tests + the Scale/Shift derivation).
    - No new banned patterns; checked i64 accumulation (WR-02).
  </acceptance_criteria>
  <done>Chosen CTR splits' whole-set inference CtrValueTables are baked into Model.ctr_data with the correct (Shift, Scale) from the prior num/denom PAIR; the apply path threads split.shift/split.scale into the inference Calc on BOTH the table-found AND not-found branches (no hardcoded 1.0/0.0 on either path); the bake key matches the apply key; CTR + numeric apply oracles stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Close the FULL multi-tree e2e oracle through train_cat (unweakened)</name>
  <read_first>
    - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs (the existing oracle — tensor_ctr_e2e_oracle_predictions_match_upstream currently calls `train(&CpuBackend, &[], &borders, &target, &[], &tensor_ctr_params(), None)` then predict_raw_cat; the cat columns NEVER reach training — THIS is the bug to fix; the two sibling sub-tests must stay unchanged + green)
    - crates/cb-train/src/boosting.rs (train_cat from Plan 05-11/05-13 + the ctr_data bake out-path from Task 1)
    - crates/cb-model/src/model.rs (from_trained + with_ctr_data)
    - crates/cb-model/src/apply.rs (predict_raw_cat)
    - crates/cb-oracle/fixtures/tensor_ctr_e2e/{X_cat,y,predictions}.npy + model.json (the committed offline fixtures from catboost==1.2.10 — READ-ONLY; DO NOT regenerate / fabricate)
  </read_first>
  <action>
    Update `tensor_ctr_e2e_oracle_predictions_match_upstream` in `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` so the categorical columns ACTUALLY drive training: replace the `train(&CpuBackend, &[], &borders, &target, &[], &tensor_ctr_params(), None)` call with the cat-aware entry point `train_cat(&CpuBackend, &[] /* float cols */, &borders, &cat_cols, &target, &[], &tensor_ctr_params(), None)`. Lift via `cb_model::Model::from_trained(&trained, borders.clone())` and attach the baked `ctr_data` from the trainer via `.with_ctr_data(baked_ctr_data)` (the CtrData the Task-1 bake produced). Predict via the PRODUCTION `cb_model::predict_raw_cat(&model, &[], &cat_cols)`.

    Keep the assertion EXACTLY as-is: `compare_stage(Stage::Predictions, &expected_predictions, &actual)` ≤1e-5 over ALL objects / ALL 5 trees. DO NOT add `#[ignore]`, DO NOT loosen the tolerance, DO NOT touch the committed fixtures, DO NOT add a comparison-skipping fallback. The two existing sub-tests (`tensor_ctr_e2e_permutation_is_deterministic_first`, `tensor_ctr_e2e_combined_ctr_within_tolerance`) stay unchanged + green. If predictions still diverge, the fix belongs in Plan 05-12/05-13 (draw order / leaf values) or Task 1 (bake / Scale-Shift) — NOT in the test; escalate per Rule 2, never weaken the gate (the user's hard gate, STATE.md 2026-06-14).

    Note the upstream fixture model (verified): 5 trees, 2 OnlineCtr splits/tree on cat feature 0 only (the {0,1} combination was a candidate but never won — the trainer must reproduce that selection), Borders prior 0.5/1, Scale=15 Shift=0, borders [2.999, 7.999]. If the Rust trainer selects a DIFFERENT split set, the scoring tie-break / candidate ordering in Plan 05-13 Task 1 is the divergence point; if predictions diverge with the RIGHT splits, the leaf-value partition (05-13 Task 2) or the apply Scale/Shift (Task 1) is the divergence point.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test tensor_ctr_e2e_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - The oracle drives training through the cat-aware entry point (source assertion: `grep -c "train_cat" crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` >= 1; the bare `train(&CpuBackend, &[], &borders, &target, &[]` call into `train` is gone from the e2e prediction test).
    - The gate is NOT weakened (source assertion: `grep -c "#\[ignore" crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` == 0; the `compare_stage(Stage::Predictions, ...)` ≤1e-5 assertion is unchanged — `grep -c "compare_stage(Stage::Predictions" crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` == 1; no tolerance literal other than the shared 1e-5).
    - The fixtures are untouched (source assertion: `git status --short crates/cb-oracle/fixtures/tensor_ctr_e2e/` shows NO modification to X_cat/y/predictions/model.json).
    - Test-command assertion: `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` → 3/3 pass, INCLUDING `tensor_ctr_e2e_oracle_predictions_match_upstream` (FULL multi-tree ≤1e-5).
    - Regression guard: `cargo test -p cb-train --test slice_first_oracle_test --test one_hot_oracle_test --test ordered_boost_e2e_oracle_test --test leaf_methods_oracle_test --test averaging_fold_permutation_oracle_test --test ctr_split_scoring_test` ALL pass.
  </acceptance_criteria>
  <done>tensor_ctr_e2e_oracle_predictions_match_upstream passes ≤1e-5 vs upstream catboost 1.2.10 through cb_model::predict_raw_cat, driven by train_cat + with_ctr_data, with NO #[ignore] / NO weakened tolerance / NO fabricated fixtures; all regression oracles stay green. ORD-05 / SC-5 CLOSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baked ctr_data → apply lookup | Per-bucket integer counts + combined-projection hashes drive inference Calc and split tests |
| split Scale/Shift → border-space comparison | A wrong Scale/Shift compares the CTR value against the wrong border (the apply never reaches [2.999,7.999]) |
| CtrValueTable (de)serialize | Untrusted .cbm/model.json CTR tables cross into bounds-before-slice decode (existing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14-01 | Tampering | i64 CTR counters during whole-set accumulate_online bake (WR-02) | mitigate | Counters bounded by N (small dataset); reuse the EXISTING accumulate_online + build_final_ctr (checked `.get` accumulation). No parallel unchecked counter loop. |
| T-05-14-02 | Information disclosure / DoS | bounds-before-slice on baked CTR tables at apply (CR-02/CR-03) | mitigate | The apply lookup uses CtrValueTable bucket_for_hash → counts_at (checked `.get`, not-found→empty); the bake inserts complete tables only. A missing combined bucket → empty CTR value, NOW scaled by split.shift/split.scale (change A), never OOB. |
| T-05-14-03 | Tampering | malformed/oversized CTR table on decode | accept (already mitigated) | decode_ctr_data already validates every declared length before slicing (Security V5, T-05-04-V5); this plan adds no new decode path. |
| T-05-14-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs (pure cb-train/cb-model source over existing crates); the committed tensor_ctr_e2e/ fixtures are READ-ONLY (generated offline from pinned catboost==1.2.10, never regenerated). |
</threat_model>

<verification>
- `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` — 3/3 pass, including the FULL multi-tree predictions oracle ≤1e-5, NO #[ignore].
- `cargo test -p cb-train --test ctr_split_scoring_test` — Scale/Shift derivation + bake round-trip + FOUND-branch scale + NOT-FOUND-branch scale green (on top of the 05-13 scoring/partition tests).
- `cargo test -p cb-model --test apply_oracle_test --test predict_oracle_test --test ctr_data_roundtrip_test` — CTR apply (Scale/Shift threaded both branches) + numeric apply unchanged green.
- `cargo test -p cb-train --test slice_first_oracle_test --test one_hot_oracle_test --test ordered_boost_e2e_oracle_test --test leaf_methods_oracle_test --test averaging_fold_permutation_oracle_test` — full regression guard green.
- `grep -c "#\[ignore" crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` == 0; fixtures unmodified (`git status` clean under tensor_ctr_e2e/).
- `grep -E "unwrap\(|expect\(|panic!|anyhow" crates/cb-train/src/boosting.rs crates/cb-train/src/tree.rs crates/cb-model/src/apply.rs crates/cb-model/src/model.rs` — only pre-existing/allowed (no NEW banned production patterns on this plan's hunks).
- Disk-pressure constraint (STATE.md): verify PER-CRATE, never `cargo test --workspace`.
</verification>

<success_criteria>
- Each chosen CTR split's whole-set inference CtrValueTable is baked into Model.ctr_data with the correct Scale/Shift derived from the prior num/denom PAIR; the apply path threads split.shift/split.scale on BOTH the table-found AND not-found branches (no hardcoded 1.0/0.0 on either path); the bake key matches the apply key.
- `tensor_ctr_e2e_oracle_predictions_match_upstream` passes ≤1e-5 vs upstream catboost 1.2.10 through cb_model::predict_raw_cat, driven by train_cat, unweakened — ORD-05 / Roadmap SC-5 CLOSED.
- The numeric-only / one-hot / ordered-boosting / leaf-methods oracles remain byte-identical (regression guard).
</success_criteria>

<artifacts>
## Artifacts this plan produces (NEW symbols)

- `cb_train::CtrSplitSpec.shift: f64`, `cb_train::CtrSplitSpec.scale: f64` — NEW fields (Borders → 0.0 / ctr_border_count/norm); populated on chosen CTR splits at the bake
- `cb_model::CtrSplit.shift: f64`, `cb_model::CtrSplit.scale: f64` — NEW fields, threaded into passes_ctr_split's inference Calc on BOTH the found and not-found branches
- ctr_data bake in train_cat — whole-set accumulate_online over the combined projection → build_final_ctr → cb_model::CtrValueTable → CtrData keyed by ctr_table_key; Scale/Shift from calc_normalization(prior_num) + ctr_border_count
- `train_cat` extended return / out-path exposing the baked `cb_model::CtrData` (e.g. `(Model, Option<CtrData>)`) — `train`'s return type UNCHANGED
- (updated) `crates/cb-train/tests/ctr_split_scoring_test.rs` — adds Scale/Shift bake derivation + FOUND/NOT-FOUND-branch scale tests
- (updated) `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` — drives training via train_cat + with_ctr_data (assertion unchanged)
</artifacts>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-14-SUMMARY.md` when done
</output>
