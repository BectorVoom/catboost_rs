---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 06
type: execute
wave: 6
depends_on: ["05-01", "05-04", "05-05"]
files_modified:
  - crates/cb-train/src/projection.rs
  - crates/cb-train/src/projection_test.rs
  - crates/cb-train/src/candidates.rs
  - crates/cb-train/src/candidates_test.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-model/src/apply.rs
  - crates/cb-train/tests/tensor_ctr_oracle_test.rs
autonomous: true
requirements: [ORD-05]
must_haves:
  truths:
    - "Feature combinations (tensor CTRs) enumerate projections up to max_ctr_complexity"
    - "A projection's combined hash folds per-feature hashes via the upstream CalcHash/MultiHash, consuming calc_cat_feature_hash (never a model ctr_data hash_map)"
    - "SimpleCtrs (single feature) and CombinationCtrs (tensor) both produce models matching upstream ≤1e-5 on categorical data"
    - "max_ctr_complexity gate bounds projection length (GetFullProjectionLength)"
  artifacts:
    - path: "crates/cb-train/src/projection.rs"
      provides: "TProjection combination enumeration + combined hash fold + max_ctr_complexity gate"
      contains: "max_ctr_complexity"
    - path: "crates/cb-train/tests/tensor_ctr_oracle_test.rs"
      provides: "tensor CTR enumeration + ≤1e-5 oracle"
      contains: "tensor"
  key_links:
    - from: "crates/cb-train/src/projection.rs"
      to: "cb-data::calc_cat_feature_hash"
      via: "per-feature hash folded into the projection combined hash"
      pattern: "calc_cat_feature_hash"
    - from: "crates/cb-train/src/candidates.rs"
      to: "crates/cb-train/src/projection.rs"
      via: "tensor CTR candidates emitted under max_ctr_complexity"
      pattern: "projection"
---

<objective>
Feature combinations / tensor CTRs (ORD-05) — `SimpleCtrs`/`CombinationCtrs` under `max_ctr_complexity`. Per D-05 these are added LAST because they build directly on the single-feature CTR machinery locked in 05-04 (plain) and 05-05 (ordered): a tensor CTR is the same online/ordered accumulation over a COMBINED projection hash instead of a single feature's hash.

Purpose: Closes ORD-05 and the final phase success criterion. By landing after single-feature CTR is oracle-locked, a tensor-CTR divergence isolates to the projection-enumeration / combined-hash logic, not the CTR math underneath it.
Output: `projection.rs` (TProjection enumeration + combined hash + complexity gate), tensor CTR candidates in `candidates.rs`, projection apply in `apply.rs`, and the tensor_ctr oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-PATTERNS.md

@crates/cb-data/src/cat_hash.rs
@crates/cb-train/src/tree.rs
@crates/cb-model/src/apply.rs

<interfaces>
From crates/cb-data/src/cat_hash.rs (reuse, NEVER re-port; projection folds per-feature hashes):
```rust
pub fn calc_cat_feature_hash(s: &str) -> u32;     // CityHash64 & 0xffffffff
pub fn city_hash_64(/* ... */) -> u64;
pub struct PerfectHash { /* first-seen perfect-hash bins */ }
```

From crates/cb-train (built in 05-02/04/05 — consume):
```rust
// candidates.rs (05-02): one-hot vs CTR routing + Candidate enumeration — EXTEND with tensor CTR candidates
// ctr/online.rs + ctr/calc_ctr.rs (05-04/05): single-feature CTR accumulation — reuse over the projection hash
pub struct BoostParams { /* + max_ctr_complexity, combinations_ctr (this plan pins explicitly) */ }
```

RESEARCH § "Tensor / Combination CTRs": AddTreeCtrs (greedy_tensor_search.cpp:491-551); TProjection.GetFullProjectionLength (projection.h:138-144); max_ctr_complexity gate (:532-533); projection CalcHash/MultiHash fold (ctr_provider.h:65-78). Default max_ctr_complexity=4 (cat_feature_options.cpp:231-232) — pin explicitly per fixture.
</interfaces>
</context>

<artifacts_this_phase_produces>
- `cb-train::projection` module (new) — TProjection enumeration, combined hash fold, max_ctr_complexity gate
- tensor CTR candidate emission in `cb-train::candidates`
- projection-hash apply path in `cb-model::apply`
- `BoostParams.max_ctr_complexity`, `BoostParams.combinations_ctr` (new fields)
- `crates/cb-train/tests/tensor_ctr_oracle_test.rs` (new)
- consumes `tensor_ctr/` fixture from 05-01
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TProjection enumeration + combined hash + max_ctr_complexity gate</name>
  <read_first>crates/cb-data/src/cat_hash.rs, crates/cb-train/src/candidates.rs, crates/cb-train/src/lib.rs, .planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md</read_first>
  <files>crates/cb-train/src/projection.rs, crates/cb-train/src/projection_test.rs, crates/cb-train/src/candidates.rs, crates/cb-train/src/candidates_test.rs, crates/cb-train/src/lib.rs</files>
  <behavior>
    - projections enumerated up to GetFullProjectionLength <= max_ctr_complexity (projection.h:138-144, gate :532-533)
    - combined projection hash folds per-feature calc_cat_feature_hash via CalcHash/MultiHash (ctr_provider.h:65-78)
    - SimpleCtrs (length 1) and CombinationCtrs (length >= 2) both enumerated
    - max_ctr_complexity=1 → only simple CTRs; =2 → pairs included
  </behavior>
  <action>Create `crates/cb-train/src/projection.rs` (declare module + pub use in lib.rs). Implement TProjection (RESEARCH § "Tensor / Combination CTRs", projection.h:61-145): combination enumeration with `GetFullProjectionLength` bounded by `max_ctr_complexity` (gate greedy_tensor_search.cpp:532-533). Combined projection hash via the CalcHash/MultiHash fold (ctr_provider.h:65-78) over per-feature `cb_data::calc_cat_feature_hash` — reuse city_hash_64 / PerfectHash, NEVER re-port and NEVER a model's ctr_data hash_map (Anti-Pattern). Extend candidates.rs (05-02) to emit tensor CTR candidates (AddTreeCtrs, greedy_tensor_search.cpp:491-551) when complexity allows. Add `max_ctr_complexity` (default 4) + `combinations_ctr` (single type + explicit priors per D-07) to BoostParams — pin explicitly, NOT auto (Pitfall 6). Any float fold via sum_f64 (D-08); checked access; no unwrap/expect/panic; no anyhow. projection_test.rs: enumeration counts for max_ctr_complexity in {1,2,3}; a known 2-feature combined hash value.</action>
  <verify>
    <automated>cargo test -p cb-train projection</automated>
  </verify>
  <done>projection.rs enumerates combinations bounded by max_ctr_complexity; combined hash folds calc_cat_feature_hash via CalcHash/MultiHash; SimpleCtrs+CombinationCtrs emitted in candidates.rs; max_ctr_complexity+combinations_ctr in BoostParams; projection_test.rs covers enumeration + a known combined hash; no unwrap/anyhow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tensor CTR train→predict oracle ≤1e-5 (closes ORD-05)</name>
  <read_first>crates/cb-train/src/projection.rs, crates/cb-train/src/ctr/online.rs, crates/cb-model/src/apply.rs, crates/cb-oracle/fixtures/tensor_ctr/config.json</read_first>
  <files>crates/cb-model/src/apply.rs, crates/cb-train/tests/tensor_ctr_oracle_test.rs</files>
  <behavior>
    - a tensor CTR computes the same online/ordered accumulation (05-04/05) keyed on the combined projection hash
    - model-side apply computes the projection hash at inference and looks up the baked ctr_data table
    - per-object num/denom exact + value ≤1e-5; final tensor-CTR prediction ≤1e-5 vs upstream
  </behavior>
  <action>Extend `crates/cb-model/src/apply.rs` so inference computes the projection's combined hash (the Task-1 CalcHash/MultiHash fold over calc_cat_feature_hash, NEVER the model's stored ctr_data hash_map) and applies the baked per-type Calc with the not-found→empty path established in 05-04 (V5 bounds-check, never OOB). Reuse the single-feature online/ordered accumulation (05-04/05) keyed on the combined projection hash — a tensor CTR is the same math over a combined key. Create `crates/cb-train/tests/tensor_ctr_oracle_test.rs` over the 05-01 `tensor_ctr/` fixture (pinned max_ctr_complexity + combinations_ctr type/priors, thread_count=1): FIRST Stage::Permutation (D-03 gate, if permutation present), THEN per-object num/denom exact + ctr_value Stage::OnlineCtr ≤1e-5 for the combined projection, THEN final prediction ≤1e-5 vs upstream. Float sums via sum_f64; checked access; no unwrap/anyhow.</action>
  <verify>
    <automated>cargo test -p cb-train tensor_ctr_oracle</automated>
  </verify>
  <done>tensor CTR trains+predicts over a combined projection ≤1e-5 vs upstream; inference computes the projection hash via calc_cat_feature_hash (not the model hash_map) with not-found→empty bounds-safe lookup; D-03 gate + per-object num/denom exact + value ≤1e-5; ORD-05 closed; no unwrap/anyhow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection combined hash → ctr_data table lookup at inference | combined-key bucket index must be bounded before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-V5 | Tampering | apply.rs projection-hash ctr_data lookup | mitigate | bounds-check combined-key bucket `< leafCount` before indexing; not-found→emptyVal path (reuse 05-04); no OOB/raw index |
| T-05-06-01 | Denial of Service | projection.rs combination enumeration | mitigate | GetFullProjectionLength bounded by max_ctr_complexity (config, default 4); checked arithmetic; no unbounded combinatorial blowup |
| T-05-06-SC | Tampering | no package installs | accept | RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cargo test -p cb-train projection` + `cargo test -p cb-train tensor_ctr_oracle` green.
- max_ctr_complexity gate bounds enumeration.
- Projection hash via calc_cat_feature_hash (not the model hash_map); bounds-safe lookup.
- All float sums via sum_f64.
</verification>

<success_criteria>
- ORD-05 success criterion: feature combinations (SimpleCtrs/CombinationCtrs, max_ctr_complexity control) produce models matching upstream ≤1e-5 on categorical datasets.
- Phase 5 additive ladder complete: one-hot → permutation → Plain CTR → Ordered CTR → Ordered boosting → tensor CTR all oracle-locked.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-06-SUMMARY.md` when done.
</output>
