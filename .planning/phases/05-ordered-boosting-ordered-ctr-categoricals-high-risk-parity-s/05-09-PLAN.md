---
phase: 05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s
plan: 09
type: execute
wave: 8
depends_on: ["05-08"]
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-model/src/apply.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/config.json
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/X_cat.npy
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/y.npy
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json
  - crates/cb-oracle/fixtures/tensor_ctr_e2e/predictions.npy
  - crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs
autonomous: true
gap_closure: true
requirements: [ORD-05]
must_haves:
  truths:
    - "The train() driver's candidate generation emits tensor_ctr_candidates (SimpleCtrs + CombinationCtrs) during tree growth, respecting the max_ctr_complexity gate — tensor CTR candidates are no longer orphaned from training"
    - "A categorical dataset trains end-to-end with tensor CTRs and the resulting model (trees with CTR splits + baked ctr_data) predicts ≤1e-5 vs upstream CatBoost 1.2.10"
    - "The cb-model apply path evaluates CTR-based splits by computing the projection's combined hash via calc_cat_feature_hash (never the model's ctr_data hash_map) and looking up the baked CtrValueTable with a bounds-safe not-found→empty path"
    - "The combined projection CTR math (per-object num/denom, oracle-locked in 05-06) is reused over the combined key inside the training candidate path — not re-derived"
    - "Existing standalone tensor_ctr + one-hot + ordered oracles remain green (no regression)"
  artifacts:
    - path: "crates/cb-train/src/boosting.rs"
      provides: "tensor CTR candidate emission wired into the train() candidate-generation / tree-growth path under max_ctr_complexity"
      contains: "tensor_ctr_candidates"
    - path: "crates/cb-model/src/apply.rs"
      provides: "CTR-split evaluation at inference via the combined projection hash + baked ctr_data lookup"
      contains: "ctr_value_for_combined_projection"
    - path: "crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs"
      provides: "end-to-end tensor-CTR train→predict ≤1e-5 vs upstream"
      contains: "tensor"
    - path: "crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json"
      provides: "upstream catboost 1.2.10 tensor-CTR trained model + predictions reference"
  key_links:
    - from: "crates/cb-train/src/boosting.rs (candidate generation)"
      to: "crates/cb-train/src/candidates.rs::tensor_ctr_candidates"
      via: "train() emits tensor CTR candidates under max_ctr_complexity during tree growth"
      pattern: "tensor_ctr_candidates"
    - from: "crates/cb-model/src/apply.rs (CTR split eval)"
      to: "crates/cb-data::calc_cat_feature_hash"
      via: "projection combined hash folds per-feature calc_cat_feature_hash (never the ctr_data hash_map)"
      pattern: "calc_cat_feature_hash"
---

<objective>
GAP 2 (ORD-05, FAILED) — Wire tensor CTRs into the `train()` driver and produce an end-to-end categorical train→predict model matching upstream ≤1e-5. Today `tensor_ctr_candidates` (candidates.rs:179) enumerates SimpleCtrs/CombinationCtrs under `max_ctr_complexity` and the combined-projection per-object CTR math is oracle-locked ≤1e-5 (05-06), BUT the candidates are NEVER emitted into the `train()` candidate-generation / tree-growth path (05-VERIFICATION.md Gap 2, data-flow trace "HOLLOW — no tensor CTR splits are generated during training"). No full categorical-dataset train→predict model with tensor CTRs is produced, so roadmap SC-5 ("produce models matching upstream ≤1e-5 on categorical datasets") is undemonstrated end-to-end.

Purpose: Closes ORD-05 end-to-end. The training candidate generation emits tensor CTR candidates (reusing the 05-04/05/06 combined-projection CTR math over the combined key), and the cb-model apply path gains CTR-split evaluation (combined hash via `calc_cat_feature_hash` + baked `CtrValueTable` lookup). A new end-to-end tensor-CTR fixture (categorical X + labels + upstream model.json with ctr_data + predictions) locks final predictions ≤1e-5 — resolving the D-09 residual via the established offline generator.
Output: tensor CTR candidate wiring in `boosting.rs`, CTR-split apply in `apply.rs`, the `tensor_ctr_e2e/` fixture, and the end-to-end tensor oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-CONTEXT.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-RESEARCH.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-VERIFICATION.md
@.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-06-PLAN.md

@crates/cb-train/src/boosting.rs
@crates/cb-train/src/candidates.rs
@crates/cb-train/src/projection.rs
@crates/cb-model/src/apply.rs
@crates/cb-train/tests/tensor_ctr_oracle_test.rs

<interfaces>
From crates/cb-train/src/candidates.rs + projection.rs (enumeration + combined hash — already locked in 05-06; consume, do NOT re-port):
```rust
pub struct CtrCandidate { pub projection: TProjection, pub is_simple: bool }
// Emit SimpleCtrs (len 1) + CombinationCtrs (len >= 2) over CTR-eligible cat
// features (cardinality > one_hot_max_size), bounded by max_ctr_complexity.
pub fn tensor_ctr_candidates(cat_cardinalities: &[u32], one_hot_max_size: u32,
                             max_ctr_complexity: usize) -> Vec<CtrCandidate>;
pub fn enumerate_projections(eligible_count, max_ctr_complexity) -> Vec<TProjection>;
// TProjection::combined_hash folds per-feature calc_cat_feature_hash via CalcHash/MultiHash.
```

From crates/cb-model/src/apply.rs (the apply path — predict_raw_one currently handles ONLY float splits; EXTEND for CTR splits):
```rust
pub fn ctr_value_for_projection(/* single-feature CTR at inference */) -> f64;
pub fn ctr_value_for_combined_projection(/* combined projection CTR at inference */) -> f64; // 05-06
pub fn predict_raw(model: &Model, feature_values: &[Vec<f32>]) -> Vec<f64>;
fn predict_raw_one(model: &Model, features: &[f32]) -> f64; // forward-bit leaf index over tree.splits
```

From crates/cb-model (ctr_data serde — already committed):
```rust
pub use ctr_data::{calc_inference, decode_ctr_data, CtrData, CtrValueTable, ECtrType};
```

RESEARCH § "Tensor / Combination CTRs" (greedy_tensor_search.cpp:491-551 AddTreeCtrs; :532-533 max_ctr_complexity gate; projection.h:138-144 GetFullProjectionLength; ctr_provider.h:65-78 CalcHash/MultiHash fold). A tensor CTR is the SAME online/ordered accumulation (05-04/05) keyed on the COMBINED projection hash. At inference the combined hash is computed from raw cat values via `cb_data::calc_cat_feature_hash` (NEVER the model's stored ctr_data hash_map — RESEARCH Anti-Pattern, 05-06 V5 discipline) and the baked per-type `CtrValueTable` is looked up with a bounds-checked not-found→empty path.

ORACLE-SOURCING (D-09, the A2 residual this gap closes): the prior `tensor_ctr/` fixture committed only per-object CTR internals (no input cat features/labels, no trained model). The end-to-end fixture MUST commit the FULL stack — `X_cat.npy` (categorical features), `y.npy` (labels), `model.json` (upstream catboost 1.2.10 trained with simple_ctr + combinations_ctr + max_ctr_complexity, INCLUDING the baked `ctr_data` section), `predictions.npy` — generated offline by gen_fixtures.py (pinned catboost==1.2.10, thread_count=1, never in CI). model_json.rs already parses the `ctr_data` hash_map (CtrTableJson); the Rust apply consumes the baked tables.

DEPENDENCY: This plan modifies `crates/cb-train/src/boosting.rs` (the train() candidate-generation path), which is ALSO modified by 05-08 (the Ordered branch). They MUST NOT share a wave — this plan is wave 8, depends_on 05-08, so the boosting.rs edits stack cleanly (no merge conflict; the tensor candidate wiring lands on top of the Ordered branch).
</interfaces>
</context>

<artifacts_this_phase_produces>
- `crates/cb-train/src/boosting.rs`: tensor CTR candidate emission wired into the candidate-generation / tree-growth path (calls `tensor_ctr_candidates` under `params.max_ctr_complexity`, feeding the combined-projection CTR feature into the split search). Reuses the 05-04/05 online/ordered accumulation over the combined key (no re-derivation).
- `crates/cb-model/src/apply.rs`: CTR-split evaluation in `predict_raw_one` — a CTR split computes the projection's combined hash via `calc_cat_feature_hash` and looks up the baked `CtrValueTable` (`ctr_value_for_combined_projection`) with a bounds-safe not-found→empty path.
- `crates/cb-oracle/generator/gen_fixtures.py`: a new `gen_tensor_ctr_e2e()` generator (upstream catboost 1.2.10 categorical train with simple_ctr + combinations_ctr + max_ctr_complexity; save model.json WITH ctr_data; dump predictions).
- `crates/cb-oracle/fixtures/tensor_ctr_e2e/{config.json, X_cat.npy, y.npy, model.json, predictions.npy}` (new end-to-end fixture).
- `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` (new) — end-to-end tensor-CTR train→predict ≤1e-5 vs upstream, gated by D-03 permutation (if present) then per-object combined CTR ≤1e-5 then final prediction ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire tensor_ctr_candidates into train() candidate generation + CTR-split apply in cb-model</name>
  <read_first>crates/cb-train/src/boosting.rs, crates/cb-train/src/candidates.rs, crates/cb-train/src/projection.rs, crates/cb-model/src/apply.rs, crates/cb-model/src/ctr_data.rs, catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp</read_first>
  <files>crates/cb-train/src/boosting.rs, crates/cb-model/src/apply.rs</files>
  <behavior>
    - With CTR-eligible cat features present (cardinality > one_hot_max_size), the train() candidate generation emits tensor_ctr_candidates: SimpleCtrs (max_ctr_complexity>=1) and CombinationCtrs (max_ctr_complexity>=2), respecting the gate (no candidate exceeds max_ctr_complexity).
    - A tensor CTR feature's per-object value is the 05-04/05 online/ordered accumulation keyed on the combined projection hash (combined_hash over calc_cat_feature_hash) — reused, not re-derived.
    - cb-model predict_raw_one evaluates a CTR split: compute the projection combined hash for the object's raw cat values via calc_cat_feature_hash, look up the baked CtrValueTable (ctr_value_for_combined_projection), binarize against the split border, and route the forward-bit leaf index — with a not-found→empty bounds-safe lookup (never OOB).
  </behavior>
  <action>In `boosting.rs` candidate generation (the tree-growth split-search path), call `crate::candidates::tensor_ctr_candidates(&cat_cardinalities, params.one_hot_max_size, params.max_ctr_complexity)` and feed each `CtrCandidate` as a candidate CTR feature into the oblivious split search (`AddTreeCtrs`, greedy_tensor_search.cpp:491-551): materialize the candidate's per-object CTR value via the 05-04/05 online/ordered accumulation keyed on `projection.combined_hash` (the combined-projection math locked in 05-06 — REUSE it; do NOT re-port the hash fold or the accumulation). Bound enumeration by `params.max_ctr_complexity` (the gate, :532-533). In `cb-model/src/apply.rs` extend `predict_raw_one` so a CTR split (a tree split keyed on a projection + CTR border) is evaluated by computing the object's combined projection hash via `cb_data::calc_cat_feature_hash` (NEVER the model's ctr_data hash_map — RESEARCH Anti-Pattern), looking up the baked `CtrValueTable` via `ctr_value_for_combined_projection` with the not-found→empty path (V5 bounds-check: combined-key bucket index `< leafCount` before indexing), binarizing against the split border, and folding into the forward-bit leaf index exactly as a float split. All float sums via `cb_core::sum_f64` (D-08); checked `.get` only; no `unwrap`/`expect`/`panic`/raw index; no `anyhow`.</action>
  <verify>
    <automated>cargo test -p cb-train --test tensor_ctr_oracle_test && cargo test -p cb-model --test ctr_data_roundtrip_test</automated>
  </verify>
  <read_first>crates/cb-train/src/boosting.rs</read_first>
  <acceptance_criteria>
    - `boosting.rs` calls `tensor_ctr_candidates` from the train candidate-generation path — `grep -c "tensor_ctr_candidates" crates/cb-train/src/boosting.rs` ≥ 1.
    - The wiring references `params.max_ctr_complexity` and the combined-projection hash (`combined_hash` / `ctr_value_for_combined_projection`) — no re-ported hash fold.
    - `apply.rs` `predict_raw_one` evaluates CTR splits via `calc_cat_feature_hash` + `ctr_value_for_combined_projection` with a bounds-safe not-found→empty lookup (`grep -E "calc_cat_feature_hash|ctr_value_for_combined_projection" crates/cb-model/src/apply.rs` matches; no raw indexing on the ctr_data blob).
    - `cargo test -p cb-train --test tensor_ctr_oracle_test` (3/3, standalone per-object combined CTR) still passes — no regression.
    - `cargo test -p cb-model --test ctr_data_roundtrip_test` (5/5) still passes.
    - No NEW `unwrap`/`expect`/`panic`/`anyhow` in production paths of boosting.rs / apply.rs.
  </acceptance_criteria>
  <done>train() emits tensor CTR candidates under max_ctr_complexity; cb-model apply evaluates CTR splits via the combined projection hash + baked ctr_data with bounds-safe lookup; standalone tensor + ctr_data oracles unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the tensor_ctr_e2e fixture and lock the end-to-end tensor-CTR train→predict oracle ≤1e-5</name>
  <read_first>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-train/tests/tensor_ctr_oracle_test.rs, crates/cb-train/tests/one_hot_oracle_test.rs, crates/cb-oracle/fixtures/tensor_ctr/config.json, crates/cb-oracle/src/model_json.rs</read_first>
  <files>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/fixtures/tensor_ctr_e2e/config.json, crates/cb-oracle/fixtures/tensor_ctr_e2e/X_cat.npy, crates/cb-oracle/fixtures/tensor_ctr_e2e/y.npy, crates/cb-oracle/fixtures/tensor_ctr_e2e/model.json, crates/cb-oracle/fixtures/tensor_ctr_e2e/predictions.npy, crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs</files>
  <action>Add `gen_tensor_ctr_e2e()` to gen_fixtures.py (the binclf/regression + cat_hash precedent): synthesize a small categorical dataset (N=30, two cat columns each with cardinality > one_hot_max_size so a genuine combination is formed — mirror `tensor_ctr/config.json`: one_hot_max_size=1, max_ctr_complexity=2, simple_ctr=["Borders:Prior=0.5"], combinations_ctr=["Borders:Prior=0.5"], permutation_count=1, fold_len_multiplier=2.0, depth=2, iterations=5, learning_rate=0.1, l2_leaf_reg=3.0, leaf_estimation_method=Gradient, bootstrap_type=No, random_strength=0, random_seed=0, thread_count=1, loss_function=Logloss); build a `Pool(X_cat, y, cat_features=[0,1])`; `model.fit(pool)`; `model.save_model(..., format="json")` → `model.json` (carries the baked `ctr_data` section); `model.predict(X_cat, prediction_type="RawFormulaVal")` → `predictions.npy` (f64); commit `X_cat.npy` (the raw categorical columns, stringify integer categories per A4), `y.npy`, and a `config.json` mirroring `tensor_ctr/config.json` with `"scenario": "tensor_ctr_e2e"` and a note that it carries the FULL train→predict stack + baked ctr_data (closing the D-09 omission). Then create `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs`: load `X_cat.npy`/`y.npy`/`predictions.npy` + `model.json` (via `cb_oracle::model_json` / `cb_model::load_json` for the ctr_data); train via `cb_train::train` with the pinned tensor-CTR config (max_ctr_complexity=2, combinations_ctr + simple_ctr), predict via `cb_model::predict_raw` over the categorical apply path; gate FIRST `Stage::Permutation` (D-03, if a permutation is present in the fixture), THEN per-object combined CTR `Stage::OnlineCtr` ≤1e-5 (reusing the 05-06 combined-projection anchor), THEN final prediction `compare_stage(Stage::Approx, &expected_predictions, &actual)` ≤1e-5 vs the committed upstream `predictions.npy`. Float sums via sum_f64; the test may `#![allow(clippy::unwrap_used, ...)]` for fixture loads. Document offline-generator (D-09), pinned catboost==1.2.10, never in CI.</action>
  <verify>
    <automated>cargo test -p cb-train --test tensor_ctr_e2e_oracle_test</automated>
  </verify>
  <read_first>crates/cb-oracle/fixtures/tensor_ctr/config.json</read_first>
  <acceptance_criteria>
    - `gen_fixtures.py` contains `gen_tensor_ctr_e2e` that trains a categorical model with `simple_ctr` + `combinations_ctr` + `max_ctr_complexity=2` and saves `model.json` (with ctr_data) + `predictions.npy` + `X_cat.npy` + `y.npy`.
    - `crates/cb-oracle/fixtures/tensor_ctr_e2e/` contains `config.json`, `X_cat.npy`, `y.npy`, `model.json`, `predictions.npy` (all committed).
    - `crates/cb-train/tests/tensor_ctr_e2e_oracle_test.rs` trains with the pinned tensor-CTR config and asserts final predictions ≤1e-5 vs the committed upstream `predictions.npy` via `compare_stage` (NOT looser than 1e-5).
    - The test gates D-03 permutation (if present) and per-object combined CTR ≤1e-5 BEFORE the final prediction assertion.
    - `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` passes; standalone `tensor_ctr_oracle_test` (3/3) and `one_hot_oracle_test` (3/3) remain green.
  </acceptance_criteria>
  <done>An end-to-end tensor-CTR categorical train→predict model matches upstream catboost 1.2.10 ≤1e-5, with D-03 + per-object combined CTR gated first; ORD-05 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection combined hash → baked ctr_data CtrValueTable lookup at inference | the combined-key bucket index must be bounds-checked `< leafCount` before slicing the blob (CR-02/CR-03 hazard zone) |
| committed fixture (model.json with ctr_data blob, X_cat/y/predictions .npy) → loader | malformed/oversized ctr_data blob or arrays must surface typed ModelError, never panic / OOB-slice |
| projection enumeration ← max_ctr_complexity | unbounded combinatorial blow-up if the gate is bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-V5 | Tampering | apply.rs CTR-split combined-hash ctr_data lookup | mitigate | Bounds-check the combined-key bucket index `< leafCount` before indexing the baked CtrValueTable; not-found→emptyVal path (reuse 05-04/06 V5 discipline); checked `.get`, no raw index on the blob. Note CR-02 (present-but-empty bucket) / CR-03 (first-bucket stride) hazards — the lookup must not assume a present bucket; treat absent/empty as the empty CTR value. |
| T-05-09-01 | Denial of Service | candidates.rs / projection.rs combination enumeration in train() | mitigate | `GetFullProjectionLength` bounded by `max_ctr_complexity` (config, default 4) at the train() call site; checked arithmetic; no unbounded combinatorial enumeration. |
| T-05-09-02 | Tampering / DoS | model.json ctr_data blob parse (oversized blob, bad bucket index) | mitigate | Parse via the Phase-4 panic-free serde (`load_json` / model_json `CtrTableJson`); VERIFY flatbuffers accessor + bounds-check bucket index before indexing; malformed → typed `ModelError`. No `unwrap`/raw-index on untrusted model bytes (RESEARCH V5). |
| T-05-09-SC | Tampering | no package installs (Rust test + offline Python generator on pinned catboost==1.2.10) | accept | RESEARCH Package Legitimacy Audit: N/A — catboost==1.2.10 already pinned (D-09); no new npm/pip/cargo install. |
</threat_model>

<verification>
- `cargo test -p cb-train --test tensor_ctr_oracle_test` (standalone combined CTR, 3/3) still green — no regression.
- `cargo test -p cb-model --test ctr_data_roundtrip_test` (5/5) still green.
- `cargo test -p cb-train --test one_hot_oracle_test` (3/3) still green.
- `cargo test -p cb-train --test tensor_ctr_e2e_oracle_test` green — end-to-end tensor CTR ≤1e-5 vs upstream.
- `boosting.rs` emits `tensor_ctr_candidates` under max_ctr_complexity; `apply.rs` evaluates CTR splits via `calc_cat_feature_hash` + baked ctr_data with bounds-safe lookup.
- All float sums via `sum_f64`; no `unwrap`/`anyhow` in production.
</verification>

<success_criteria>
- ORD-05 success criterion: feature combinations (SimpleCtrs/CombinationCtrs, max_ctr_complexity control) produce models matching upstream ≤1e-5 on categorical datasets — now demonstrable end-to-end because tensor CTR candidates are emitted during training and the apply path evaluates CTR splits (closes 05-VERIFICATION.md Gap 2).
- Phase 5 additive ladder is fully validated end-to-end: one-hot → permutation → Plain CTR → Ordered CTR → Ordered boosting (05-08) → tensor CTR (this plan), each oracle-locked AND wired into train→predict.
</success_criteria>

<output>
Create `.planning/phases/05-ordered-boosting-ordered-ctr-categoricals-high-risk-parity-s/05-09-SUMMARY.md` when done.
</output>
