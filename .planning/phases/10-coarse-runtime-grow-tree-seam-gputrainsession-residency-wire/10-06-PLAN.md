---
phase: 10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-backend/src/kernels/spike_reduction.rs
  - crates/cb-backend/src/kernels.rs
  - .planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/SPIKE-REDUCTION.md
autonomous: true
requirements: [GPUT-04]
must_haves:
  truths:
    - "Three reduction-determinism candidate kernels (i64 fixed-point atomics, private-histogram merge, two-pass segmented reduce) exist as runnable on-device #[cube] kernels"
    - "Each candidate is measured for determinism error vs CPU f64 AND wall-clock, producing a real err+ms comparison table"
    - "SPIKE-REDUCTION.md records the table, per-backend (CUDA vs gfx1100) viability, and a recommendation that feeds Phase 11's histogram kernel"
  artifacts:
    - path: "crates/cb-backend/src/kernels/spike_reduction.rs"
      provides: "3 candidate reduction #[cube] kernels + a test-gated micro-benchmark harness measuring err+ms"
      contains: "fn"
    - path: ".planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/SPIKE-REDUCTION.md"
      provides: "err+ms comparison table + recommendation for Phase 11"
      contains: "Recommendation"
  key_links:
    - from: "crates/cb-backend/src/kernels.rs"
      to: "crates/cb-backend/src/kernels/spike_reduction.rs"
      via: "#[cfg(test)] mod spike_reduction; include alongside grow_loop"
      pattern: "mod spike_reduction"
---

<objective>
Run the reduction-determinism spike (ROADMAP Phase-10 Success Criterion 5, D-10-06/07).
Prototype ALL THREE candidate parallel-reduction strategies as small runnable on-device
`#[cube]` kernels — (a) fixed-point i64 atomics, (b) private-histogram merge, (c) two-pass
segmented reduce — and measure BOTH determinism error (vs a CPU f64 reference sum) AND
wall-clock for each. Produce `SPIKE-REDUCTION.md` with a real err+ms comparison table, the
per-backend viability (CUDA has f64 atomic-add; gfx1100 does NOT — HostSumFallback per Phase
7.6), and a recommendation that feeds Phase 11's production histogram kernel. This is a
runnable comparison, not a memo.

Purpose: De-risk Phase 11 by choosing the reduction strategy that holds the ε=1e-4 bar across
hundreds of trees before the histogram kernel is built. Independent of the seam/wiring work —
parallelizable in Wave 1.
Output: 3 candidate kernels + micro-benchmark + SPIKE-REDUCTION.md recommendation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-RESEARCH.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-PATTERNS.md
@.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-CONTEXT.md
</context>

## Artifacts this plan produces

- New test-gated kernel file `crates/cb-backend/src/kernels/spike_reduction.rs` containing
  three candidate `#[cube]` reduction kernels (generic-float / integer where required) +
  a `#[cfg(test)]` micro-benchmark that runs each over a fixed problem shape and records
  determinism error vs `cb_compute` f64 reference + wall-clock.
- New `#[cfg(test)] mod spike_reduction;` include in `crates/cb-backend/src/kernels.rs`.
- New analysis doc `SPIKE-REDUCTION.md` (err+ms table, per-backend viability, Phase-11 recommendation).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Three candidate reduction kernels + micro-benchmark harness</name>
  <files>crates/cb-backend/src/kernels/spike_reduction.rs, crates/cb-backend/src/kernels.rs</files>
  <read_first>
    - /home/user/Documents/workspace/cubecl_manual/manual/cubecl/INDEX.md (MANDATORY before any kernel work; lowercase path per RESEARCH A6)
    - /home/user/Documents/workspace/cubecl_manual/manual/cubecl/08_atomic_contention.md (atomic non-determinism; three-tier reduction strategy)
    - crates/cb-backend/src/kernels/grow_loop.rs (sibling test-module shape; make_fixture deterministic-gen :101-156; LEAF_BOUND tolerance :53-56; inline-transcription discipline :24-39)
    - crates/cb-backend/src/kernels.rs (the `#[cfg(test)] mod grow_loop;` include at :2775 — add `mod spike_reduction;` the same way)
    - crates/cb-backend/src/gpu_runtime/mod.rs (existing `#[cube]` kernel patterns + one-client/upload-once block :1626-1708 for ComputeClient + Handle usage)
    - crates/cb-compute/src/lib.rs (read-only import of a CPU f64 reference sum, e.g. a sum/reduce helper; do NOT import cb-train — feature-unification landmine)
  </read_first>
  <action>
    Create `crates/cb-backend/src/kernels/spike_reduction.rs` and include it via
    `#[cfg(test)] mod spike_reduction;` in kernels.rs. Implement three candidate reduction
    kernels over a single contiguous f-array → scalar sum: (a) fixed-point i64 atomics — scale
    each float to a fixed-point i64 and atomically add into one i64 accumulator (deterministic
    because integer add is associative/exact); (b) private-histogram merge — per-block partial
    sums into a scratch buffer, then a deterministic ordered host or single-block merge; (c)
    two-pass segmented reduce — block-local reduce then a second pass over partials. Kernels use
    generic-float where they carry floats; the i64-atomic accumulator path uses integer atomics.
    HARD: no negative-infinity float literal anywhere (`f32::MIN` sentinel only — HIP/gfx1100 JIT
    rejects negative-infinity); bounds-guard every kernel index (`if i < arr.len()`); one `ComputeClient` per run,
    never read a 0-len handle (residency rule). Add a `#[cfg(test)]` micro-benchmark that, for a
    fixed problem shape (choose sizes — Claude's discretion, e.g. n in {1e4, 1e5, 1e6} with
    adversarial magnitude spread so float-order error is visible), runs each candidate, computes
    |device_sum − cpu_f64_sum| (the cb-compute f64 reference, transcribed/imported read-only),
    and records wall-clock via `std::time::Instant` with a read-back to drain the lazy CubeCL
    queue before stopping the clock. Print an err+ms line per candidate per size. Tests may use
    `unwrap`/`assert`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm spike_reduction -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --no-default-features --features rocm spike_reduction -- --nocapture` builds and runs all three candidates in-env, printing an err+ms line for each candidate at each problem size. Tier: ROCm in-env smoke (non-gating; authoritative numbers come from Kaggle CUDA).
    - The i64 fixed-point candidate's run-to-run determinism error is exactly 0.0 across repeated runs (integer add is exact); the float-atomic-style candidate's error is recorded (may be nonzero — that is the finding).
    - No negative-infinity literal in the file: `grep -c 'NEG_INFINITY' crates/cb-backend/src/kernels/spike_reduction.rs` is 0 (use the `f32::MIN` sentinel).
    - Per-backend viability note captured for gfx1100 (lacks f64 atomic-add → record which candidate needs the HostSumFallback path).
  </acceptance_criteria>
  <done>Three runnable reduction candidates produce a real err+ms measurement in-env on ROCm; the i64 candidate is bit-deterministic; per-backend viability is captured.</done>
</task>

<task type="auto">
  <name>Task 2: SPIKE-REDUCTION.md — err+ms table + Phase-11 recommendation</name>
  <files>.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/SPIKE-REDUCTION.md</files>
  <read_first>
    - crates/cb-backend/src/kernels/spike_reduction.rs (the harness output produced in Task 1)
    - .planning/phases/10-.../10-RESEARCH.md (Open Question 2 — multi-block scan carry; D-10-06/07; Pitfall 5)
    - .planning/notes/gpu-training-host-light-root-cause.md (ε=1e-4 bar context)
  </read_first>
  <action>
    Write `SPIKE-REDUCTION.md` capturing: (1) an err+ms comparison table — one row per candidate
    (i64 fixed-point atomics / private-histogram merge / two-pass segmented reduce) × problem
    size, columns = determinism error vs CPU f64, wall-clock ms, run-to-run variance; (2) a
    per-backend viability column/section (CUDA: f64 atomic-add available but atomicAdd ordering
    still non-deterministic; gfx1100: no f64 atomic-add → which candidate needs HostSumFallback);
    (3) the multi-block scan-carry observation (Open Q2) verified against the CubeCL manual
    scan/reduce docs (lowercase path); (4) a single firm RECOMMENDATION of the strategy Phase 11
    should adopt for the partition-aware histogram kernel, with the rationale (which one holds
    ε=1e-4 across hundreds of trees at the lowest cost). Mark the in-env ROCm numbers as smoke
    and flag that the authoritative err+ms must be re-recorded on Kaggle CUDA (D-10-07).
  </action>
  <verify>
    <automated>test -f .planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/SPIKE-REDUCTION.md && grep -q "Recommendation" .planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/SPIKE-REDUCTION.md</automated>
  </verify>
  <acceptance_criteria>
    - SPIKE-REDUCTION.md exists and contains an err+ms table with one row per candidate × problem size, a per-backend viability section, and a single "Recommendation" heading naming the Phase-11 strategy.
    - The recommendation cites real measured numbers from Task 1 (not placeholders).
    - The doc flags that authoritative numbers are pending a Kaggle CUDA run (D-10-07). Tier: doc artifact (automated existence/content check); CUDA numbers human-gated.
  </acceptance_criteria>
  <done>SPIKE-REDUCTION.md records the runnable err+ms comparison + per-backend viability + a firm Phase-11 recommendation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → device buffer | the reduction input array crosses into a #[cube] kernel; malformed length/index is the only untrusted vector (internal numeric compute, no network/auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering / DoS | out-of-bounds kernel index in a candidate reduction kernel | mitigate | bounds-guard every kernel (`if i < arr.len()`); host shape guard before launch |
| T-10-06-02 | Tampering (UB-adjacent) | reading a Handle through the wrong client | mitigate | one ComputeClient per spike run; never read a 0-len handle (residency rule) |
| T-10-06-03 | Repudiation (wrong result looks valid) | silent zero on read-back failure skews the err table | mitigate | surface a hard error on read-back failure, never a silent zero |
| T-10-06-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed (RESEARCH Package Legitimacy Audit — zero new deps) |
</threat_model>

<verification>
- `cargo test -p cb-backend --no-default-features --features rocm spike_reduction -- --nocapture` runs all three candidates in-env and prints err+ms.
- No `-inf` literal in spike_reduction.rs (`f32::MIN` sentinel only).
- SPIKE-REDUCTION.md exists with the err+ms table + a "Recommendation" section.
- D-04: this plan adds only test-gated code + a doc; no production path changes — CPU suites unaffected.
</verification>

<success_criteria>
Three runnable reduction-determinism candidates are measured (err + ms) in-env; the i64
fixed-point candidate is bit-deterministic; per-backend viability (CUDA vs gfx1100) is
recorded; SPIKE-REDUCTION.md delivers a firm recommendation feeding Phase 11's histogram
kernel (ROADMAP Phase-10 SC5). Authoritative numbers flagged pending Kaggle CUDA.
</success_criteria>

<output>
Create `.planning/phases/10-coarse-runtime-grow-tree-seam-gputrainsession-residency-wire/10-06-SUMMARY.md` when done.
</output>