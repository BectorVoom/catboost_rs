---
phase: 10-gpu-foundations-runtime-seam-session-residency-device-primit
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/kernels/reduce.rs
  - .planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/SPIKE-REDUCTION.md
autonomous: true
requirements: [GPUT-16]
must_haves:
  truths:
    - "Segmented-reduce (per-segment sum) and reduce-by-key (key-run group sums) run on device and match serial per-segment / group-by references."
    - "The reduce path exposes 2-3 selectable deterministic finalize strategies whose run-to-run spread is zero (measured over repeated launches), and reports WHICH strategy ran (no silent atomic->deterministic switch)."
    - "SPIKE-REDUCTION.md records an err+ms table + per-backend viability + a recommended winner strategy feeding Phase 11's histogram kernel."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "segmented-reduce + reduce-by-key #[cube] kernels + selectable deterministic finalize strategies (f64 accumulation)"
      contains: "reduce_by_key"
    - path: "crates/cb-backend/src/kernels/reduce.rs"
      provides: "extended self-oracle + run-to-run variance harness for each candidate strategy"
    - path: ".planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/SPIKE-REDUCTION.md"
      provides: "reduction-determinism spike report (candidates, err+ms, per-backend viability, recommendation)"
      contains: "Recommendation"
  key_links:
    - from: "crates/cb-backend/src/kernels/reduce.rs"
      to: "crates/cb-backend/src/kernels.rs"
      via: "variance harness launches each finalize strategy over SelectedRuntime and asserts zero run-to-run spread + correct AtomicFinalizePath"
      pattern: "AtomicFinalizePath"
---

<objective>
Deliver the reduce family of GPUT-16 primitives — segmented-reduce and reduce-by-key — AND run the reduction-determinism spike (D-03/D-04) in the SAME work: the measured-best deterministic strategy IS the reduce that ships (no throwaway). Prototype the top 2-3 candidate strategies, measure run-to-run variance in-env, and record the recommendation that feeds Phase 11's ε=1e-4 gate.

Purpose: Deterministic reduction governs the ε=1e-4 bar across hundreds of trees. Standing it up now (the depth-1 histogram + update_part_props exercise it immediately) hard-de-risks Phase 11.
Output: segmented-reduce + reduce-by-key + selectable deterministic finalize in kernels.rs/reduce.rs; SPIKE-REDUCTION.md.

## Artifacts this phase produces (this plan)
- `segmented_reduce` and `reduce_by_key` `#[cube]` kernels added to `crates/cb-backend/src/kernels.rs`
- 2-3 selectable deterministic finalize strategies wired onto the reduce path (extending the existing `AtomicFinalizePath` enum at `reduce.rs:271`)
- Extended run-to-run variance harness in `crates/cb-backend/src/kernels/reduce.rs`
- `SPIKE-REDUCTION.md` (err+ms table, per-backend viability, recommended winner)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@CATBOOST_CUDA_KERNELS_DESIGN.md
@.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Segmented-reduce + reduce-by-key kernels</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels/reduce.rs</files>
  <read_first>
    - `crates/cb-backend/src/kernels/reduce.rs:250-352` — `block_reduce` + `HostSumFallback`/`AtomicFinalizePath` deterministic finalize + variance harness; `cb_core::sum_f64` baseline (the single sanctioned ordered reduction — never a naive `.sum()`).
    - `crates/cb-backend/src/kernels.rs:1424-1590` — `block_reduce_kernel` and `block_reduce_atomic_kernel` bodies (the #[cube] shapes to extend for segmented/by-key).
    - `crates/cb-backend/src/kernels/pointwise_hist.rs:87-128` — `host_reference_hist2` ordered per-cell reference (generalize to per-segment / per-key ordered Σ).
    - `CATBOOST_CUDA_KERNELS_DESIGN.md` §6.1 `reduce.cu` (`SegmentedReduce*PerSegment` meanSize<600 fast path, `ReduceByKey`).
  </read_first>
  <behavior>
    - Segmented-reduce of values [1,2,3,4] with segment offsets {0,2,4} → [3, 7].
    - reduce-by-key of keys [a,a,b,b,b] values [1,1,1,1,1] → keys [a,b] sums [2,3].
    - Accumulation is in f64 regardless of channel float type (bounds float error, mirrors upstream M=8 double re-accumulate).
  </behavior>
  <action>
    Add `segmented_reduce` (warp/block-per-segment, using the segment offsets produced by the partitions/scan primitives) and `reduce_by_key` (key-run detection via flag scan reusing the 10-01 full/segmented scan, then per-segment sums) to kernels.rs, generic over `F: Float`, bounds-guarded, grid-stride. Accumulate partial sums in f64. Extend kernels/reduce.rs with self-oracles: an inline serial per-segment sum and an inline serial group-by-sum (transcribed inline, no cb-train reach — D-02); baseline sums via `cb_core::sum_f64`. Integer/offset inputs use u32. No `-inf` literal.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm reduce</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --no-default-features --features rocm reduce` passes; segmented-reduce and reduce-by-key match their serial references.
    - Kernel bodies in kernels.rs; all tests in kernels/reduce.rs (source/test separation).
    - Kaggle CUDA authoritative segmented-reduce + reduce-by-key ≤1e-4 — human-gated (10-09).
  </acceptance_criteria>
  <done>Segmented-reduce and reduce-by-key land with serial self-oracles green on the rocm smoke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic finalize strategies + run-to-run variance harness</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels/reduce.rs</files>
  <read_first>
    - `crates/cb-backend/src/kernels/reduce.rs:258-342` — the `advertises_atomic` capability gate, `AtomicFinalizePath` enum, and the variance-reporting oracle (assert a silent atomic->deterministic switch FAILS, not passes).
    - `/home/user/Documents/workspace/cubecl_manual/manual/cubecl/09_fixedpoint_atomics.md` and `.../08_atomic_contention.md` — fixed-point i64/u64 deterministic atomics vs order-nondeterministic float atomics.
    - `CATBOOST_CUDA_KERNELS_DESIGN.md` §6.1 `update_part_props` (M=8 double re-accumulate) — the f64-accumulation precedent.
  </read_first>
  <behavior>
    - Over 32 repeated launches on the same input, each deterministic strategy produces a byte-identical result (zero run-to-run spread).
    - The reported finalize path equals the capability-derived expected path (InKernelAtomicF64 where advertised, else HostSumFallback); a mismatch fails the test.
  </behavior>
  <action>
    Implement the top 2-3 candidate deterministic-reduction strategies as selectable finalize paths extending the existing AtomicFinalizePath enum: (a) fixed-order tree reduce (fixed pairing in shared memory), (b) block-then-host-final-sum (the in-tree HostSumFallback, Phase 7.6 precedent), and (c) fixed-point i64/u64 atomics (`round(v*2^k)` -> integer add, exact order-independent) guarded by the `advertises_atomic` capability check. Accumulate in f64. Extend the reduce.rs variance harness to launch each strategy `runs=32` times, assert zero run-to-run spread per strategy, and assert the reported path matches the capability-derived expected path (gfx1100 lacks f64 atomic-add -> HostSumFallback; CUDA advertises it). No `-inf` literal; run the rocm smoke after the change.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm reduce</automated>
  </verify>
  <acceptance_criteria>
    - Each candidate strategy shows zero run-to-run spread across 32 launches in-env.
    - A silent atomic->deterministic path switch causes the test to FAIL (path assertion), not silently pass.
    - Per-backend viability recorded (gfx1100 exercises HostSumFallback; CUDA exercises the atomic path) — measured on Kaggle for the final numbers.
  </acceptance_criteria>
  <done>2-3 deterministic strategies selectable + variance-verified; the winner is ready to ship as the library reduce.</done>
</task>

<task type="auto">
  <name>Task 3: SPIKE-REDUCTION.md report + recommendation</name>
  <files>.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/SPIKE-REDUCTION.md</files>
  <read_first>
    - `crates/cb-backend/src/kernels/reduce.rs:258-342` — the variance-reporting oracle is the evidence source (err + ms + finalize path).
    - `10-RESEARCH.md:336-348` — the candidate table (fixed-order tree reduce, HostSumFallback, fixed-point atomics, Kahan, sorted-index) and the "winner ships as the primitive" contract (D-04).
    - `10-ROADMAP` Phase 11 note — the spike recommendation is consumed as step 0 before Phase 11's histogram kernel.
  </read_first>
  <action>
    Author SPIKE-REDUCTION.md with: the candidate set prototyped, an err+ms comparison table (populated from the in-env variance harness for the rocm-viable strategies; a clearly-marked TBD row for the CUDA-only fixed-point-atomic numbers to be filled by the Kaggle run in 10-09), per-backend viability (gfx1100 no f64 atomic-add), and a firm Recommendation naming the winning strategy that ships as the library reduce/segmented-reduce/reduce-by-key (D-04) and is consumed by Phase 11. Do NOT invent Kaggle numbers — mark them as awaiting the human-gated CUDA run.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/SPIKE-REDUCTION.md && grep -q 'Recommendation' .planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/SPIKE-REDUCTION.md</automated>
  </verify>
  <acceptance_criteria>
    - SPIKE-REDUCTION.md exists with a candidate err+ms table, per-backend viability, and a named recommended winner.
    - CUDA-only numbers are explicitly marked as awaiting the Kaggle run (not fabricated).
  </acceptance_criteria>
  <done>SPIKE-REDUCTION.md documents the measured candidates and recommends the winner feeding Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host → device reduce | array/segment/key lengths cross into kernel indexing; finalize path depends on device atomic capability |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Repudiation (nondeterministic wrong result) | reduce finalize | mitigate | Deterministic strategies only; zero run-to-run spread asserted over 32 launches; f64 accumulation |
| T-10-07 | Tampering (silent capability downgrade) | AtomicFinalizePath selection | mitigate | Capability-gated path assertion fails on a silent atomic->deterministic switch |
| T-10-08 | Tampering (portability UB) | new #[cube] bodies | mitigate | No -inf literal; rocm smoke after change |
</threat_model>

<verification>
- `cargo test -p cb-backend --no-default-features --features rocm reduce` (segmented/by-key correctness + determinism).
- SPIKE-REDUCTION.md present with recommendation.
- Manual/authoritative: Kaggle CUDA per-candidate err+ms (fills the SPIKE table, executed via 10-09).
</verification>

<success_criteria>
Segmented-reduce and reduce-by-key match serial references; 2-3 deterministic finalize strategies show zero run-to-run spread with correct capability-gated path reporting; SPIKE-REDUCTION.md recommends the winner that ships as the library reduce and feeds Phase 11.
</success_criteria>

<output>
Create `.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-03-SUMMARY.md` when done
</output>
