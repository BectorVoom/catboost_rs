---
phase: 10-gpu-foundations-runtime-seam-session-residency-device-primit
plan: 09
type: execute
wave: 8
depends_on: [10-08]
files_modified:
  - bench/generator.py
  - bench/cuda_oracle.ipynb
  - bench/fixtures/README.md
  - bench/RESULTS.md
autonomous: true
requirements: [BENCH-01, BENCH-02]
must_haves:
  truths:
    - "A committed .ipynb builds the --features cuda wheel and, on a Kaggle CUDA notebook, runs the correctness oracle FIRST (blocking gate) then a warm-run/JIT-excluded train-only wall-clock speed measurement."
    - "One seeded synthetic generator produces BOTH the depth-1 <=1e-5 correctness fixture AND the large-n (~1e6x50) speed workload (D-06)."
    - "The harness order is: nvidia-smi -> standalone primitive oracles (<=1e-4 / bit-exact) -> cindex oracle (bit-exact) -> depth-1 RMSE+Logloss (<=1e-5) -> ALL BLOCKING -> warm untimed fit -> time train-only draining the lazy queue -> structured report."
    - "The depth-1 device>=CPU speed bar is pinned to the large-n workload and the small-n infeasibility (D-10-09) is surfaced explicitly, not silently assumed."
  artifacts:
    - path: "bench/generator.py"
      provides: "seeded synthetic generator (n_rows/n_features) — single source for correctness fixture + speed workload"
      contains: "np.random.seed"
    - path: "bench/cuda_oracle.ipynb"
      provides: "committed Kaggle notebook: wheel build, correctness gate, warm-run speed, structured report"
    - path: "bench/RESULTS.md"
      provides: "human sign-off log for the per-phase Kaggle CUDA run (correctness + speed)"
  key_links:
    - from: "bench/cuda_oracle.ipynb"
      to: "bench/generator.py"
      via: "the notebook imports the generator to build both the correctness fixture and the large-n speed workload"
      pattern: "generator"
---

<objective>
Establish the reproducible Kaggle CUDA harness (BENCH-01) as a foundational deliverable measuring BOTH correctness (blocking gate) AND wall-clock speed from day one, and stand up the standing per-phase speed-check discipline (BENCH-02) every later phase reuses. This is the authoritative GPU oracle of record — a human-gated external run; ROCm in-env is smoke-only, not a gate.

Purpose: Every GPU kernel from depth-1 onward is both correctness-tested and speed-measured on CUDA, not merely benchmarked at the end. This plan wires the notebook to the full fit path (10-08) + the primitive/cindex oracles (10-01..06).
Output: bench/generator.py, bench/cuda_oracle.ipynb, bench/fixtures/README.md, bench/RESULTS.md.

## Artifacts this phase produces (this plan)
- `bench/generator.py` — seeded synthetic generator (D-06), single source for the depth-1 <=1e-5 correctness fixture AND the ~1e6x50 speed workload
- `bench/cuda_oracle.ipynb` — committed notebook: `maturin build --release --no-default-features --features cuda` -> nvidia-smi -> primitive oracles -> cindex oracle -> depth-1 RMSE+Logloss oracle (ALL BLOCKING) -> warm-run/JIT-excluded train-only speed -> structured report
- `bench/fixtures/README.md` — fixture format + regeneration/commit discipline
- `bench/RESULTS.md` — per-phase Kaggle CUDA sign-off log (correctness + speed)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded synthetic generator + committed fixtures</name>
  <files>bench/generator.py, bench/fixtures/README.md</files>
  <read_first>
    - `benchmark.py:12-52` — the seeded numpy generation pattern (`np.random.seed(42)`, `randn`, linear target) + train + wall-clock timing to build on.
    - `10-PATTERNS.md:216-229` — the generator/notebook analog and the D-06 single-source rule.
    - `10-RESEARCH.md:320-334` — BENCH-01/02: committed .ipynb form (D-05), one generator for both (D-06), harness order.
    - `10-CONTEXT.md:44` (D-06) — configurable n_rows/n_features, ~1e6x50 tunable above the launch-overhead break-even; no external download.
  </read_first>
  <behavior>
    - generator(n_rows, n_features, seed) is deterministic: same seed -> same X/y bytes.
    - A small-n config yields the depth-1 correctness fixture; a large-n (~1e6x50) config yields the speed workload — from the SAME generator.
  </behavior>
  <action>
    Author bench/generator.py parameterizing n_rows/n_features/seed (numpy, mirroring benchmark.py's seeded randn + linear target with noise), exposing one function that produces BOTH the depth-1 correctness fixture inputs and the large-n (~1e6x50, tunable above break-even) speed workload (D-06). Emit committed deterministic fixtures (random-seeded inputs + the CPU-path expected values for the depth-1 tree and the primitive/cindex references) into bench/fixtures/, and document the fixture format + regeneration/commit discipline in bench/fixtures/README.md. Keep the generator dependency-light (numpy only, already in benchmark.py). No secrets/env needed.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; ast.parse(open('bench/generator.py').read()); print('generator.py parses')" && test -f bench/fixtures/README.md</automated>
  </verify>
  <acceptance_criteria>
    - bench/generator.py parses and produces deterministic output for a fixed seed (same bytes on re-run).
    - One generator is the documented single source for both the correctness fixture and the speed workload (D-06).
    - bench/fixtures/README.md documents the fixture format + commit discipline.
  </acceptance_criteria>
  <done>The seeded generator + committed fixtures land; correctness and speed share one reproducible source.</done>
</task>

<task type="auto">
  <name>Task 2: cuda_oracle.ipynb — correctness gate then warm-run speed + RESULTS log + D-10-09 escalation</name>
  <files>bench/cuda_oracle.ipynb, bench/RESULTS.md</files>
  <read_first>
    - `10-RESEARCH.md:320-334` — harness order (nvidia-smi -> primitive oracles -> cindex oracle -> depth-1 -> ALL BLOCKING -> warm untimed fit -> time train-only with a drain read-back -> structured report -> RESULTS.md).
    - `10-RESEARCH.md:615-624` — the D-10-09 ESCALATION: depth-1 device>=CPU is achievable ONLY at large n (~1e5-1e6+), physically infeasible at small n; pin BENCH-02's depth-1 bar to the large-n workload and surface the assumption explicitly.
    - `/home/user/Documents/workspace/cubecl_manual/manual/cubecl/05_lazy_execution.md`, `.../11_launch_overhead_and_transfers.md` — lazy-queue drain / warm-run caveat.
    - `benchmark.py:31-41` — the `time.time()` around .fit() shape (to warm + drain before timing).
    - The SPIKE-REDUCTION.md from 10-03 — the CUDA-only fixed-point-atomic numbers to fill.
  </read_first>
  <behavior>
    - Running the notebook on a Kaggle CUDA instance builds the wheel, confirms CUDA via nvidia-smi, runs all correctness oracles as a BLOCKING gate, and only then reports a warm-run train-only wall-clock number.
    - A correctness failure blocks the speed cells (no fast-but-wrong number).
  </behavior>
  <action>
    Author bench/cuda_oracle.ipynb with ordered cells: (1) `maturin build --release --no-default-features --features cuda` + pip install + `nvidia-smi`; (2) standalone primitive oracles (scan, segmented scan, sort/reorder, reduce-by-key, segmented-reduce, update_part_props) vs their serial references (<=1e-4; integer primitives bit-exact); (3) bit-packed cindex oracle vs CPU quantized bins (bit-exact); (4) depth-1 RMSE + Logloss device-vs-CPU oracle (<=1e-5, Cosine score + first-order calc_average leaves); (5) mark (2)-(4) as a BLOCKING gate that halts on failure; (6) warm one untimed fit, then time train-only on the large-n workload draining CubeCL's lazy queue with a read-back/predict before stopping the clock — device vs host-CPU baseline, and vs official CatBoost GPU (`task_type='GPU'`) where a comparable depth-1 config exists; (7) print a structured report (per-primitive/per-tree pass/fail at the epsilon bar, then speed). Fill the SPIKE-REDUCTION.md CUDA-only rows from the notebook's reduction-candidate cells. Create bench/RESULTS.md as the human sign-off log (a template with correctness pass/fail + speed rows to be filled on the actual Kaggle run) and record the D-10-09 escalation prominently: "Depth-1 device>=CPU is achievable only at large n; at small n it is physically infeasible regardless of optimization — BENCH-02's depth-1 speed bar is pinned to the large-n workload; the Kaggle run is the arbiter of the crossover." Do NOT fabricate Kaggle numbers — the run is human-gated.
  </action>
  <verify>
    <automated>python3 -c "import json; nb=json.load(open('bench/cuda_oracle.ipynb')); assert nb.get('cells'); print('notebook JSON valid,', len(nb['cells']), 'cells')" && grep -q "large n" bench/RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - cuda_oracle.ipynb is valid notebook JSON with the ordered correctness-gate-then-speed cells; correctness is a blocking gate before any speed cell.
    - bench/RESULTS.md exists and records the D-10-09 escalation (depth-1 device>=CPU only at large n) explicitly.
    - MANUAL / Kaggle-oracle acceptance (human-gated, recorded in RESULTS.md on the actual run): primitive oracles + cindex (bit-exact) + depth-1 RMSE/Logloss (<=1e-5) ALL PASS, then the warm-run train-only device-vs-CPU speed at large n is recorded. This is the authoritative BENCH-01/02 sign-off; ROCm in-env is smoke-only, not a gate.
  </acceptance_criteria>
  <done>The committed Kaggle CUDA notebook runs correctness-first-then-speed with a structured report and RESULTS log; the D-10-09 large-n pinning + small-n infeasibility is surfaced explicitly — BENCH-01 established and the BENCH-02 standing discipline set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kaggle notebook → wheel build / device run | human-gated external execution; no untrusted input (inputs are the seeded synthetic generator); the risk is a wrong-but-fast number, not a security surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-25 | Repudiation (fast-but-wrong number) | speed reported before oracle passes | mitigate | Correctness is a BLOCKING gate before any speed cell; warm untimed fit + lazy-queue drain before timing |
| T-10-26 | Repudiation (misleading speed claim) | small-n depth-1 device>=CPU claim | mitigate | D-10-09 escalation recorded in RESULTS.md; BENCH-02 bar pinned to the large-n workload; no fabricated Kaggle numbers |
| T-10-SC | Tampering | maturin/numpy/catboost installs | accept | All already-present, audited packages (RESEARCH Package Legitimacy Audit: maturin/numpy/catboost all OK); no new external package introduced |
</threat_model>

<verification>
- Notebook JSON validity + RESULTS.md escalation present (in-repo, automated).
- MANUAL/authoritative: the human-gated Kaggle CUDA run — primitive + cindex + depth-1 oracles ALL PASS (blocking) then warm-run large-n speed recorded in RESULTS.md. ROCm in-env smoke is not a gate.
</verification>

<success_criteria>
A committed, reproducible Kaggle CUDA harness builds the --features cuda wheel and runs correctness-first-then-speed with a structured report and human sign-off log; one seeded generator sources both the depth-1 <=1e-5 correctness fixture and the large-n speed workload; the D-10-09 small-n infeasibility is surfaced explicitly — BENCH-01 established and BENCH-02's standing per-phase speed-check discipline set.
</success_criteria>

<output>
Create `.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-09-SUMMARY.md` when done
</output>
