---
phase: 10-gpu-foundations-runtime-seam-session-residency-device-primit
plan: 05
type: execute
wave: 4
depends_on: [10-04]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/kernels/compression.rs
  - crates/cb-backend/src/kernels/update_part_props.rs
autonomous: true
requirements: [GPUT-16]
must_haves:
  truths:
    - "Bit-compression pack/unpack round-trips ui8 bins through shared ui32 words bit-exactly (integer oracle, even tighter than 1e-4)."
    - "Per-partition stat aggregation (update_part_props) sums a stat per partition matching an ordered serial per-partition reference, accumulating in f64."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "compression pack/unpack + update_part_props #[cube] kernels"
      contains: "update_part_props"
    - path: "crates/cb-backend/src/kernels/compression.rs"
      provides: "bit-exact pack/unpack self-oracle"
    - path: "crates/cb-backend/src/kernels/update_part_props.rs"
      provides: "per-partition Σstat self-oracle vs ordered serial reference"
  key_links:
    - from: "crates/cb-backend/src/kernels/update_part_props.rs"
      to: "crates/cb-backend/src/kernels.rs"
      via: "update_part_props reuses the 10-03 deterministic reduce/segmented-reduce finalize; f64 accumulation"
      pattern: "sum_f64"
---

<objective>
Deliver the final two GPUT-16 primitives: bit-compression (pack/unpack — the packing math the bit-packed cindex consumes in 10-06) and per-partition stat aggregation (`update_part_props` — the 2^depth partition-stat read-back the depth-1 leaf step needs). update_part_props reuses the 10-03 deterministic reduce; compression is a bit-exact integer primitive.

Purpose: Compression feeds the GPUT-15 cindex directly; update_part_props feeds the leaf-value step (Σder1, Σweight per partition). These close the primitive library.
Output: compression + update_part_props in kernels.rs with self-oracles.

## Artifacts this phase produces (this plan)
- `pack` / `unpack` bit-compression `#[cube]` kernels (BLOCK_SIZE-strided, `word |= (bins[i] & Mask) << Shift`) in `kernels.rs`
- `update_part_props` `#[cube]` (ComputeSum -> block reduce -> SaveResults, f64 accumulate) in `kernels.rs`
- `kernels/compression.rs` (bit-exact oracle) + `kernels/update_part_props.rs` (ordered Σstat oracle) + `mod` decls
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@CATBOOST_CUDA_KERNELS_DESIGN.md
@.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bit-compression pack/unpack (bit-exact)</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels/compression.rs</files>
  <read_first>
    - `crates/cb-backend/src/kernels.rs:554-563` — histogram_scatter_kernel (per-lane write shape; compression is u32-key, not Float, but keeps the guard + grid-stride).
    - `10-PATTERNS.md:93-113` — the pack/unpack pattern (`word |= (bins[i] & Mask) << Shift`; unpack `(word >> Shift) & Mask`; bit-exact integer oracle).
    - `CATBOOST_CUDA_KERNELS_DESIGN.md` §6.1 `compression.cu` (`TCompressionHelper`, BLOCK_SIZE-strided packing; `bitsPerKey = ceil(log2(n_bins+1))`, `keysPerWord = 32/bitsPerKey`, `Mask = (1<<bitsPerKey)-1`).
  </read_first>
  <behavior>
    - pack then unpack of a random ui8 bin column reproduces the source bins exactly (bit-exact).
    - Multiple features sharing a ui32 word each extract their own bin field via (word >> Shift) & Mask.
  </behavior>
  <action>
    Add `pack` (each thread ORs `(bins[i] & Mask) << Shift` into the shared word at its Offset) and `unpack` (`(word >> Shift) & Mask`) to kernels.rs, u32 keys, bounds-guarded, grid-stride (BLOCK_SIZE-strided per upstream). Host-side helper computes `bitsPerKey`/`keysPerWord`/`Mask` from a feature's `n_bins`. Create kernels/compression.rs with an inline serial pack/unpack reference (D-02) asserting BIT-EXACT round-trip (integer equality — tighter than 1e-4, D-07). Guard word-count / Offset arithmetic with `checked_mul`/`checked_shl` -> `CbError::OutOfRange`. Add `mod compression;`. No -inf literal.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm compression</automated>
  </verify>
  <acceptance_criteria>
    - pack/unpack round-trips bit-exactly across random seeds and multiple features per word; rocm smoke green.
    - Overflow arithmetic is checked (no unguarded shift/mul).
    - Kaggle CUDA authoritative pack/unpack bit-exact — human-gated (10-09).
  </acceptance_criteria>
  <done>Bit-compression pack/unpack lands with a bit-exact self-oracle green; the packing math the cindex consumes is proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-partition stat aggregation (update_part_props)</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels/update_part_props.rs</files>
  <read_first>
    - `crates/cb-backend/src/kernels/reduce.rs:250-342` — the deterministic finalize + f64-accumulation pattern (update_part_props reuses the 10-03 reduce/segmented-reduce).
    - `crates/cb-backend/src/kernels/pointwise_hist.rs:87-128` — `host_reference_hist2` ordered reference (generalize to per-partition Σstat; assert every bin/partition in range rather than indexing raw).
    - `/home/user/Documents/workspace/cubecl_manual/manual/cubecl/10_grid_stride_occupancy.md` — 2-D/3-D grid partitioning a shared accumulator (mirrors upstream blockIdx.y-per-column).
    - `CATBOOST_CUDA_KERNELS_DESIGN.md` §6.1 `update_part_props.cu` (ComputeSum -> FastInBlockReduce -> SaveResults, M=8 double re-accumulate).
  </read_first>
  <behavior>
    - Stat values grouped by partition-id sum to a per-partition total matching an ordered serial per-partition sum.
    - Accumulation is in f64 (M=8 double re-accumulate precedent) to bound float error.
  </behavior>
  <action>
    Add `update_part_props` to kernels.rs: a 2-D/3-D grid computing per-partition partial sums of a stat channel (e.g. Σder1, Σweight) into a shared accumulator, then a deterministic block-reduce finalize (reuse the 10-03 winner strategy) and SaveResults writing the 2^depth partition totals. Accumulate in f64 regardless of channel float type. Bounds-guard; grid-stride; generic-float channels; no -inf literal. Create kernels/update_part_props.rs generalizing the pointwise_hist ordered host reference to per-partition Σstat (gather members in ascending object order, fold through `cb_core::sum_f64`, assert every partition id in range). Add `mod update_part_props;`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm update_part_props</automated>
  </verify>
  <acceptance_criteria>
    - Per-partition Σstat matches the ordered serial reference within a generous run-stable bound; rocm smoke green.
    - Accumulation is f64; the finalize path is the deterministic 10-03 winner.
    - Kaggle CUDA authoritative update_part_props ≤1e-4 — human-gated (10-09).
  </acceptance_criteria>
  <done>update_part_props lands with an ordered serial self-oracle green; the 2^depth partition-stat read-back the leaf step needs is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host → device pack / part-props | bin widths, Offsets, partition counts cross into shift-mask and accumulator indexing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Tampering | pack Offset / word-count arithmetic | mitigate | checked_mul/checked_shl -> CbError::OutOfRange on Offset and word count |
| T-10-13 | Repudiation (nondeterministic stat) | update_part_props finalize | mitigate | Deterministic reduce (10-03 winner) + f64 accumulation |
| T-10-14 | Tampering (portability UB) | new #[cube] bodies | mitigate | No -inf literal; rocm smoke after change |
</threat_model>

<verification>
- `cargo test -p cb-backend --no-default-features --features rocm compression update_part_props`.
- Manual/authoritative: Kaggle CUDA pack (bit-exact) + update_part_props (≤1e-4) via 10-09.
</verification>

<success_criteria>
Bit-compression pack/unpack round-trips bit-exactly and per-partition stat aggregation matches an ordered serial reference (rocm smoke green) — the primitive library is complete and feeds the cindex (10-06) and the depth-1 leaf step (10-07).
</success_criteria>

<output>
Create `.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-05-SUMMARY.md` when done
</output>
