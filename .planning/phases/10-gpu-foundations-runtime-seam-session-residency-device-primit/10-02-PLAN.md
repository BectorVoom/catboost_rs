---
phase: 10-gpu-foundations-runtime-seam-session-residency-device-primit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-compute/src/runtime.rs
autonomous: true
requirements: [GPUT-01]
must_haves:
  truths:
    - "The Runtime trait exposes begin_device_training / grow_tree_on_device / end_device_training with CubeCL-free host-typed signatures."
    - "All three methods have default implementations returning Ok(false)/Ok(None)/Ok(()) so every existing Runtime impl (CpuBackend, test runtimes) compiles unchanged and transparently falls back to CPU (D-04)."
    - "grow_tree_on_device returns CbResult<Option<DeviceGrownTree>> where DeviceGrownTree carries splits as (feature_index, bin_id), per-leaf values, and an oracle-only leaf_of."
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "DeviceGrownTree host struct + 3 default-impl Runtime seam methods"
      contains: "DeviceGrownTree"
  key_links:
    - from: "crates/cb-compute/src/runtime.rs"
      to: "crates/cb-compute/src/runtime.rs"
      via: "the 3 seam methods mirror the compute_gradients_grouped default-impl pattern; no cubecl/cb-backend dependency introduced"
      pattern: "fn grow_tree_on_device"
---

<objective>
Land the GPUT-01 grow-tree seam contract on the `Runtime` trait: three default-impl methods (`begin_device_training`, `grow_tree_on_device`, `end_device_training`) plus the host-typed `DeviceGrownTree` struct. The trait stays CubeCL-free so cb-train never pulls a backend dependency (the feature-unification landmine). Default implementations preserve the CPU path byte-unchanged (D-04) for free.

Purpose: Establish the seam every later plan wires into, independent of any GPU work — this can build in Wave 1 with zero primitive dependencies.
Output: DeviceGrownTree + 3 seam methods in cb-compute/src/runtime.rs.

## Artifacts this phase produces (this plan)
- `pub struct DeviceGrownTree { splits: Vec<(u32,u32)>, leaf_values: Vec<f64>, leaf_of: Vec<u32> }` (host-typed) in `crates/cb-compute/src/runtime.rs`
- `Runtime::begin_device_training(...) -> CbResult<bool>` default `Ok(false)`
- `Runtime::grow_tree_on_device(...) -> CbResult<Option<DeviceGrownTree>>` default `Ok(None)`
- `Runtime::end_device_training(&self) -> CbResult<()>` default `Ok(())`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeviceGrownTree host struct + 3 default-impl seam methods</name>
  <files>crates/cb-compute/src/runtime.rs</files>
  <read_first>
    - `crates/cb-compute/src/runtime.rs:944-970` — `compute_gradients_grouped` default-impl (the EXACT default-impl-on-Runtime pattern to copy: a trait method with a body that every impl inherits unless overridden).
    - `crates/cb-compute/src/runtime.rs:823-824` — `EScoreFunction::Cosine` is CatBoost's default score function (the score_function param default for begin_device_training, GPUT-08).
    - `10-RESEARCH.md:414-467` — the illustrative seam signatures and the load-bearing rules (host-typed only, Ok(None)/Ok(false) defaults, enough params for the D-10-02 gate + the once-per-fit cindex build).
    - `crates/cb-train/src/tree.rs:307-327` — `FeatureMatrix`/`feature_borders` to confirm the `(feature, bin_id)` split shape the caller will resolve to a border.
  </read_first>
  <action>
    Add the host-typed `DeviceGrownTree` struct to runtime.rs: `splits: Vec<(u32, u32)>` (per-level chosen split as feature_index + bin_id; the pass test is `quantized_bin[feature] > bin_id`, the caller resolves `border = feature_borders[feature][bin_id]`), `leaf_values: Vec<f64>` (per-leaf, length 2^depth, UN-scaled by learning_rate — cb-train scales), `leaf_of: Vec<u32>` (per-object leaf index, length 0 in the production hot path and length n only for the oracle structure check so the n-length buffer never crosses per tree — D-05). Derive Debug/Clone/PartialEq. Add three methods to the `Runtime` trait with default bodies mirroring compute_gradients_grouped: `begin_device_training(&self, loss, depth, boosting_type_is_plain, fold_count, score_function: EScoreFunction, bins_feature_major: &[u32], weight: &[f64], n, n_features, n_bins, learning_rate, scaled_l2) -> CbResult<bool>` default `Ok(false)`; `grow_tree_on_device(&self, approx: &[f64], target: &[f64]) -> CbResult<Option<DeviceGrownTree>>` default `Ok(None)`; `end_device_training(&self) -> CbResult<()>` default `Ok(())`. Bind unused default params with `let _ = (...)` to avoid warnings (copy the compute_gradients_grouped precedent). Introduce NO cubecl or cb-backend dependency — only host types (slices, Vecs, existing Loss/EScoreFunction/CbResult).
  </action>
  <verify>
    <automated>cargo build -p cb-compute && cargo test -p cb-train</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-compute` succeeds; `cargo test -p cb-train` stays green (D-04 no-regression — every existing Runtime impl inherits the CPU-fallback defaults unchanged).
    - `grep -n 'cubecl' crates/cb-compute/Cargo.toml` shows cb-compute did NOT gain a cubecl/cb-backend dependency (trait stays CubeCL-free).
    - The three method signatures accept only host types; `DeviceGrownTree.leaf_of` is documented as empty in the hot path.
  </acceptance_criteria>
  <done>DeviceGrownTree + the 3 default-impl seam methods exist on Runtime; cb-compute builds with no backend dep; cb-train suite unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| cb-train ↔ Runtime trait | host-typed structs cross; must not leak a CubeCL/backend dependency into cb-train via feature unification |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering (build-time landmine) | cb-compute dependency graph | mitigate | Seam uses host types only; add no cubecl/cb-backend dep to cb-compute — keeps SelectedRuntime feature resolution correct on rocm |
| T-10-05 | Repudiation (silent wrong path) | default seam impls | accept | Defaults return Ok(false)/Ok(None) → CPU fallback (the oracle + safety net); never a fabricated device result |
</threat_model>

<verification>
- `cargo build -p cb-compute` + `cargo test -p cb-train` (CPU suite green = D-04).
- Confirm no new dependency edge cb-compute → cb-backend/cubecl.
</verification>

<success_criteria>
The GPUT-01 seam contract exists on Runtime with CubeCL-free host-typed signatures and Ok(None)→CPU-fallback defaults; the CPU path is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-02-SUMMARY.md` when done
</output>
