---
phase: 10-gpu-foundations-runtime-seam-session-residency-device-primit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/kernels/scan.rs
  - crates/cb-backend/src/kernels/segmented_scan.rs
autonomous: true
requirements: [GPUT-16]
must_haves:
  truths:
    - "A full prefix scan (inclusive + exclusive) over an array larger than one cube returns the same result as a serial CPU prefix sum (D-02 self-oracle)."
    - "A flag-array segmented scan resets the running sum at each segment boundary and matches a serial segmented-prefix reference."
    - "The single-cube block_scan_kernel oracle still passes (no regression) and the new cross-cube oracle exercises n far larger than CUBE_DIM."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "Cross-cube two-level full-scan launcher + segmented_scan #[cube] kernel (generic-float, bounds-guarded)"
      contains: "segmented_scan"
    - path: "crates/cb-backend/src/kernels/scan.rs"
      provides: "Extended self-oracle covering n >> CUBE_DIM against inline serial prefix reference"
    - path: "crates/cb-backend/src/kernels/segmented_scan.rs"
      provides: "New self-oracle for the flag-array segmented scan"
  key_links:
    - from: "crates/cb-backend/src/kernels/scan.rs"
      to: "crates/cb-backend/src/kernels.rs"
      via: "launches the two-level scan over SelectedRuntime and compares to an inline serial prefix sum"
      pattern: "launch::<F, crate::SelectedRuntime>"
---

<objective>
Deliver the first two GPUT-16 device primitives every later histogram/sort/partition kernel stands on: a full multi-block prefix scan (the existing block_scan_kernel is single-cube only — RESEARCH Open Q2) and a flag-array segmented scan. Each ships with a serial-CPU self-oracle in a separate test file (D-01/D-02).

Purpose: A broken scan found in isolation is far cheaper than debugging it through a depth-6 histogram in Phase 11. This is the foundation of the from-scratch primitive substrate (CubeCL has no CUB).
Output: cross-cube full scan + segmented scan in kernels.rs, oracles in kernels/scan.rs (extended) and kernels/segmented_scan.rs (new).

## Artifacts this phase produces (this plan)
- `full_scan` two-level launcher (per-block scan → scan of block-sums → add block-offset) added to `crates/cb-backend/src/kernels.rs`
- `segmented_scan_kernel<F: Float>` `#[cube]` (flag-carried segment reset) in `kernels.rs` + `mod segmented_scan;` declaration
- `crates/cb-backend/src/kernels/segmented_scan.rs` self-oracle (new)
- Extended `crates/cb-backend/src/kernels/scan.rs` oracle covering `n >> CUBE_DIM`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@CATBOOST_CUDA_KERNELS_DESIGN.md
@.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-cube full prefix scan (inclusive + exclusive)</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels/scan.rs</files>
  <read_first>
    - `crates/cb-backend/src/kernels.rs:1611-1699` — `block_scan_kernel<F: Float>` (the single-cube Hillis-Steele body, SharedMemory + sync_cube(), inclusive/exclusive #[comptime] flag, zero-pad idle lanes) — the exact #[cube] shape to reuse.
    - `crates/cb-backend/src/kernels/scan.rs:1-120` — the SCOPE note (Open Q2: single-cube only, oracle exercises n<=CUBE_DIM=32), `run_scan` launch wrapper (`client.create(cubecl::bytes::Bytes::from_elems(..))`, `launch::<F, crate::SelectedRuntime>`, `client.read_one`), inline `cpu_inclusive_scan`/`cpu_exclusive_scan`, `max_divergence` helper.
    - `/home/user/Documents/workspace/cubecl_manual/manual/cubecl/Cubecl_shared_memory.md` and `.../cubecl_reduce_sum.md` — LDS block-scan + block-sum carry mechanics (AGENTS.md: read before writing kernel code).
    - `CATBOOST_CUDA_KERNELS_DESIGN.md` §6.1 `scan.cu` (`inplace_scan.cuh`, Hillis-Steele) — the upstream algorithm authority.
  </read_first>
  <behavior>
    - Inclusive scan of [3,1,4,1,5] → [3,4,8,9,14]; exclusive → [0,3,4,8,9].
    - At n = 100_000 (far above CUBE_DIM), device full-scan equals the inline serial reference within a generous run-stable f32/f64 bound (NOT the signed-off epsilon).
    - n=1 and empty-input edge cases short-circuit unchanged.
  </behavior>
  <action>
    Add a two-level full-scan launcher to kernels.rs implementing the standard multi-block scan over SelectedRuntime: (1) per-block scan using the existing block_scan_kernel geometry writing each block's total to a block_sums buffer; (2) an exclusive scan of block_sums (single cube suffices since n_blocks is small at bench scale); (3) an add-block-offset pass that adds block_sums[block_id] to each element of block block_id. Keep the inclusive/exclusive selection as a #[comptime] flag threaded through. All device reads/writes stay under an `ABSOLUTE_POS < arr.len()` bounds guard; generic over `F: Float`; grid-stride by `CUBE_COUNT_X * CUBE_DIM_X` (never a literal 32/64). Do NOT introduce any `-inf`/`NEG_INFINITY` float literal in the kernel body (use the `f32::MIN` sentinel if a sentinel is ever needed; host code may keep f64 sentinels). Extend kernels/scan.rs: keep the existing single-cube tests, add a large-n case (n=100_000) that compares the two-level launcher against the inline serial prefix reference via max_divergence with generous asserted bounds.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm scan</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --no-default-features --features rocm scan` passes in-env (rocm smoke catches -inf/atomic issues that cpu/cuda cargo check hide).
    - The scan oracle asserts equality at n >> CUBE_DIM, not only n<=32 (grep the test file shows a case with n at least 4x CUBE_DIM).
    - Kaggle CUDA authoritative oracle (manual, via bench harness plan 10-09): full-scan ≤1e-4 vs serial reference — recorded as a human-gated acceptance item, not in-CI.
  </acceptance_criteria>
  <done>Two-level full prefix scan lands in kernels.rs; kernels/scan.rs oracle exercises n >> CUBE_DIM and passes the rocm smoke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flag-array segmented scan</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/kernels/segmented_scan.rs</files>
  <read_first>
    - `crates/cb-backend/src/kernels.rs:1611-1699` — block_scan_kernel body (the base to add segment-reset logic onto).
    - `crates/cb-backend/src/kernels/scan.rs:1-120` — the self-oracle harness shape to mirror verbatim in the new segmented_scan.rs test file (launch wrapper, inline serial reference, max_divergence, generous bounds).
    - `CATBOOST_CUDA_KERNELS_DESIGN.md` §6.1 `segmented_scan.cu` (`TSegmentedSum` pair combiner — value+flag pair, reset-on-flag).
  </read_first>
  <behavior>
    - Values [1,1,1,1] with segment flags [1,0,1,0] (1 = segment start): inclusive segmented scan → [1,2,1,2].
    - Segment boundary resets the running sum; interior positions accumulate within the segment only.
  </behavior>
  <action>
    Add `segmented_scan_kernel<F: Float>` to kernels.rs using the TSegmentedSum pair-combiner semantics: carry a (value, flag) pair through the block scan where the combine operator resets the accumulated value when the right operand's flag marks a segment start. Thread a #[comptime] inclusive flag. Bounds-guard every access; generic-float; grid-stride. Add `mod segmented_scan;` to the module declarations near kernels.rs:1700-1722. Create kernels/segmented_scan.rs mirroring the scan.rs oracle structure: a launch wrapper over SelectedRuntime, an inline serial segmented-prefix reference (transcribed inline — no cb-train reach, D-02), and tests including a multi-segment case and a case where n exceeds one cube (deferring cross-cube segment carry to the same two-level pattern if needed, else documenting the single-cube scope explicitly as scan.rs does).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm segmented_scan</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --no-default-features --features rocm segmented_scan` passes.
    - Test file is separate from the kernel body (CLAUDE.md source/test separation): the `#[cube]` kernel is in kernels.rs, all `#[test]`/`.unwrap()` in kernels/segmented_scan.rs.
    - Kaggle CUDA authoritative segmented-scan oracle ≤1e-4 — human-gated acceptance item (recorded via 10-09).
  </acceptance_criteria>
  <done>segmented_scan_kernel + its self-oracle land and pass the rocm smoke; segment-boundary reset verified against a serial reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host → device kernel launch | caller-supplied array lengths / grid dims cross into `#[cube]` index arithmetic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering/DoS | scan/segmented_scan kernel indexing | mitigate | Every device access under `ABSOLUTE_POS < arr.len()` bounds guard; grid-stride by CUBE_COUNT_X*CUBE_DIM_X, never a literal wave width |
| T-10-02 | Tampering (portability UB) | new `#[cube]` bodies | mitigate | No `-inf` float literal (gfx1100 JIT reject); f32::MIN sentinel only; rocm smoke run after every kernel change |
| T-10-03 | Repudiation (wrong-looks-valid) | block-sum carry read-back | mitigate | Compare full-scan vs inline serial reference at n >> CUBE_DIM; never read a 0-len handle |
</threat_model>

<verification>
- `cargo test -p cb-backend --no-default-features --features rocm scan segmented_scan` (in-env rocm smoke).
- No regression to existing single-cube scan tests.
- Manual/authoritative: Kaggle CUDA full-scan + segmented-scan ≤1e-4 vs serial reference (executed via 10-09 harness).
</verification>

<success_criteria>
Full multi-block prefix scan and flag-array segmented scan run on device, match inline serial CPU references at n >> CUBE_DIM (rocm smoke green), with test/source separation and no -inf literals — the scan foundation for sort/partitions/reduce-by-key.
</success_criteria>

<output>
Create `.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/10-01-SUMMARY.md` when done
</output>
