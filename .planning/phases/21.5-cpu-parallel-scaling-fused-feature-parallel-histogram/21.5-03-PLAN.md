---
phase: 21.5-cpu-parallel-scaling-fused-feature-parallel-histogram
plan: 03
type: execute
wave: 3
depends_on: ["21.5-02"]
files_modified:
  - crates/cb-train/src/tree.rs
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "select_level_perturbed fuses per-feature accumulate + score using the same per-feature build/derive path as the plain search (D-02)"
    - "The Pitfall-3 RNG draw contract is byte-for-byte unchanged: one gen_rand per level, per-feature from_seed(rand_seed+task_idx).advance(10), exactly one std_normal per border in ascending order, per-feature feature_best bookkeeping, one main-RNG GetInstance per feature"
    - "The unperturbed AND perturbed oracle fixtures stay bit-exact; perturbed determinism holds"
  artifacts:
    - crates/cb-train/src/tree.rs
  key_links:
    - "select_level_perturbed reuses the Plan 02 per-feature fused build/derive helper as the SOURCE of each raw score, changing nothing about the draw sequence"
---

<objective>
Apply the same fused per-feature accumulate+score restructuring to the PERTURBED oblivious
split search (`select_level_perturbed`) — moving the accumulation into the per-feature region
— WITHOUT perturbing its documented Pitfall-3 random-draw order, count, or bookkeeping.

Purpose: D-02 requires the perturbed path to get the same Amdahl-fix as the plain path, but
the perturbed search reproduces upstream's `SetBestScore`→`SelectBestCandidate` two-pass draw
sequence exactly; only the SOURCE of each candidate's raw score may change (histogram
prefix-scan instead of a rescan), never the RNG. This plan changes where the per-feature stats
are produced (fused, reusable scratch) while holding the draw contract byte-for-byte.

Output: fused `select_level_perturbed` in `crates/cb-train/src/tree.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-CONTEXT.md
@.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-02-SUMMARY.md
@crates/cb-train/src/tree.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fuse accumulate+score in select_level_perturbed, preserving the draw contract (D-02)</name>
  <files>crates/cb-train/src/tree.rs</files>
  <read_first>
    - crates/cb-train/src/tree.rs :889-993 `select_level_perturbed` (the whole two-pass draw contract: :916 `rand_seed = gen_rand()`, :925-965 per-feature `from_seed(rand_seed+task_idx).advance(10)` + one `std_normal` per border + `feature_best`, :967-988 per-feature main-RNG `GetInstance` SelectBestCandidate)
    - crates/cb-train/src/tree.rs Plan 02 `select_level_plain` (the fused per-feature build/derive + scratch pattern to reuse as the raw-score SOURCE)
    - crates/cb-compute/src/histogram.rs Plan 01 `fused_feature_scan_and_score` + Plan 02 per-feature subtraction helper
    - .planning/phases/21.5-.../21.5-CONTEXT.md D-02 (parallelize accumulation WITHOUT perturbing the Pitfall-3 draw contract)
  </read_first>
  <action>
    Restructure `select_level_perturbed` so each feature's raw border scores come from the
    fused per-feature build/derive path (Plan 02's helper + `fused_feature_scan_and_score` for
    level 0, subtraction derivation for level > 0), reusing per-task `FusedFeatureScratch`,
    instead of scoring a pre-built whole-partition histogram. CRITICAL — leave the random-draw
    structure byte-for-byte identical:
    - Keep exactly ONE `perturb.rng.gen_rand()` per level (:916) producing `rand_seed`.
    - Keep the per-candidate-FEATURE loop with `task_idx` ascending, each feature reseeding a
      FRESH `TFastRng64::from_seed(rand_seed.wrapping_add(task_idx))` then `advance(10)`
      (:932-934) — the reseed count/order and `task_idx` skip-on-empty-borders behavior are
      unchanged.
    - Keep exactly ONE `std_normal` draw per border in ascending border order via
      `random_score_instance(raw, std_dev, &mut feat_rng)` (:953-963), the strict `>`
      first-wins per-feature `feature_best`, and the per-feature main-RNG `GetInstance` in the
      SelectBestCandidate pass (:967-988). Only the SOURCE of each `raw` changes.
    - Note the constraint: the perturbed SetBestScore pass draws per-feature RNG in a defined
      order, so the accumulation may be fused per feature but the per-feature RNG bookkeeping
      must remain in its current deterministic feature-ascending sequence. If Plan 02's fused
      helper is used inside a parallel region here, the RNG draws MUST NOT move into that
      region unless the draw order/count is provably preserved (feature-ordered); otherwise
      keep the accumulation fused (per feature, reusable scratch) while the RNG-bearing
      SetBestScore loop stays feature-ascending — Claude's discretion, but the guard is the
      perturbed oracle fixture + determinism. Do NOT reorder or change the number of draws.
    Do not add a `cb-train` → `cb-backend` dependency. `unwrap()` prohibited in production.
  </action>
  <acceptance_criteria>
    - `select_level_perturbed` sources each feature's raw border scores from the fused
      per-feature build/derive path (not a pre-built whole-partition histogram).
    - The draw contract markers are all still present exactly once each in their original
      order: `grep -c 'gen_rand' ...` unchanged for this fn; `from_seed`, `advance(10)`,
      `random_score_instance` per border, and per-feature `GetInstance` bookkeeping intact.
    - `cargo build -p cb-train` exits 0.
    - `cargo test -p cb-train --test rayon_determinism_test` exits 0 (all cases, incl. any perturbed/leaf-wise determinism guards).
    - No `#[cfg(test)]`/`mod tests` added to tree.rs.
  </acceptance_criteria>
  <verify>
    <automated>set -o pipefail && cargo test -p cb-train --test rayon_determinism_test 2>&1 | tail -20</automated>
  </verify>
  <done>The perturbed search's accumulation is fused per feature with reusable scratch; the Pitfall-3 RNG draw order, count, and per-feature bookkeeping are byte-for-byte unchanged; the determinism suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Pure-internal CPU compute refactor of the perturbed in-process split search over already-
quantized in-memory training data. No external input surface, no network / auth /
serialization / deserialization of untrusted data, no new dependency, no package install. No
trust boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21.5-03 | (informational) | select_level_perturbed fused pass | low | accept | No applicable threat above informational — in-process compute refactor; the RNG-contract + byte-identity risk is a correctness concern guarded by the perturbed oracle fixtures + determinism suite (Plan 04), not a security boundary. |
</threat_model>

<verification>
- `cargo build -p cb-train` exits 0.
- `cargo test -p cb-train --test rayon_determinism_test` exits 0.
- (Full oracle sweep + scaling curve are Plan 04.)
</verification>

<success_criteria>
The perturbed oblivious split search fuses per-feature accumulate+score with reusable scratch
while its Pitfall-3 random-draw order, count, and per-feature bookkeeping stay byte-for-byte
identical; the determinism suite is green.
</success_criteria>

<output>
Create `.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-03-SUMMARY.md` when done.
</output>
