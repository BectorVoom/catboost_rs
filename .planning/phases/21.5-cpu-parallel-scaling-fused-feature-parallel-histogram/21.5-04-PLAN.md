---
phase: 21.5-cpu-parallel-scaling-fused-feature-parallel-histogram
plan: 04
type: execute
wave: 4
depends_on: ["21.5-02", "21.5-03"]
files_modified:
  - crates/cb-train/tests/fused_scaling_test.rs
  - .planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-EVIDENCE.md
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "The full ≤10⁻⁵ CPU oracle suite (cb-train + cb-compute) is 100% green after the fusion — every shipped fixture stays bit-exact, NO oracle re-baseline (D-04)"
    - "rayon_determinism_test is green: single- vs multi-threaded runs produce byte-identical trained models (D-08)"
    - "A documented 1→16-thread scaling curve on the Spike-002 shape shows the fused per-level pass recovered from the ~1.7× two-pass ceiling to ≥3× at 16 threads (Success Criterion 4)"
  artifacts:
    - crates/cb-train/tests/fused_scaling_test.rs
    - .planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-EVIDENCE.md
  key_links:
    - "The scaling test drives the production fused path inside sized rayon ThreadPools (1/2/4/8/16) and records the recovered speedup"
---

<objective>
Prove the goal-backward truths of Phase 21.5: the fusion preserved parity byte-for-byte (full
oracle suite + determinism green with NO re-baseline), and recovered multi-core scaling
(documented 1→16-thread curve ≥3× at 16 threads on the Spike-002 shape).

Purpose: PERF-04's headline claim is "byte-for-byte identical, ≥3× at 16 threads." This plan
is the verification: it runs the entire ≤10⁻⁵ CPU oracle suite (the regression guard — no
fixtures change), the determinism guard, and a new CB_PERF-gated thread-sweep that measures
the production fused per-level pass and records the recovered speedup in an EVIDENCE doc.

Output: `crates/cb-train/tests/fused_scaling_test.rs` (CB_PERF-gated) + `21.5-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-CONTEXT.md
@.planning/spikes/005-parallel-scaling-root-cause/README.md
@.planning/spikes/006-fused-feature-parallel-histogram/README.md
@.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-02-SUMMARY.md
@.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-03-SUMMARY.md
@crates/cb-train/tests/spike005_parallel_scaling_test.rs
@crates/cb-train/tests/spike006_fused_parallel_test.rs
@crates/cb-train/tests/perf_baseline_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity + determinism regression sweep (full ≤10⁻⁵ oracle suite green, no re-baseline) (D-04, D-08)</name>
  <files>crates/cb-train/tests/fused_scaling_test.rs</files>
  <read_first>
    - crates/cb-train/tests/rayon_determinism_test.rs (the single- vs multi-thread byte-identity guard covering symmetric + leaf-wise)
    - crates/cb-compute/tests/fused_feature_parity_test.rs (Plan 01 byte-identity guard)
    - crates/cb-compute/tests/depth6_reference_test.rs, score_functions_test.rs (oracle-style fixtures in cb-compute)
    - .planning/phases/21.5-.../21.5-CONTEXT.md D-04 (byte-identity, NO oracle re-baseline; existing suite is the regression guard)
  </read_first>
  <action>
    Run the entire CPU oracle + determinism suite unchanged and confirm 100% green with NO
    fixture edits (D-04: no re-baseline). This task does NOT modify any fixture or oracle
    tolerance. Concretely:
    - `cargo test -p cb-train -p cb-compute` (default profile) — every shipped ≤10⁻⁵ oracle
      fixture MUST still pass bit-exact; the Plan 01 `fused_feature_parity_test` and
      `rayon_determinism_test` MUST be green.
    - If ANY oracle fixture regresses, STOP — that is a parity break in the fusion (Plan 02/03),
      not a fixture to update. Record the failing fixture and route back; do not touch fixtures.
    - Confirm no `git diff` touches any `*fixture*` / oracle `.json` / expected-output asset.
    Record the exact commands run and their pass/fail in the SUMMARY and (Task 2) EVIDENCE doc.
    (This task authors the shared harness scaffold in `fused_scaling_test.rs` that Task 2's
    scaling sweep also uses — the input generator + sized-ThreadPool helper, lifted from
    `spike005_parallel_scaling_test.rs` / `spike006_fused_parallel_test.rs`; keep it
    CB_PERF-gated so `cargo test` stays fast.)
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-train -p cb-compute` exits 0 (full CPU oracle + determinism suite green).
    - `cargo test -p cb-compute --test fused_feature_parity_test` exits 0.
    - `cargo test -p cb-train --test rayon_determinism_test` exits 0.
    - `git status --porcelain` shows NO modification to any oracle fixture / expected-output
      asset (D-04: no re-baseline).
    - `crates/cb-train/tests/fused_scaling_test.rs` exists and is CB_PERF-gated (prints SKIP when unset).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-train -p cb-compute 2>&1 | tail -25</automated>
  </verify>
  <done>The full ≤10⁻⁵ CPU oracle suite + both byte-identity/determinism guards pass with zero fixture changes; the scaling-test scaffold is in place and CB_PERF-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Documented 1→16-thread scaling curve ≥3× at 16 threads (Success Criterion 4)</name>
  <files>crates/cb-train/tests/fused_scaling_test.rs, .planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-EVIDENCE.md</files>
  <read_first>
    - crates/cb-train/tests/spike005_parallel_scaling_test.rs :127 `pool`, :137 `part_a` (train inside a sized rayon::ThreadPool, report per_tree_ms + speedup vs 1 thread across a depth sweep)
    - crates/cb-train/tests/spike006_fused_parallel_test.rs :128 `bench_shape` (the 1/2/4/8/16-thread sweep + speedup print pattern)
    - .planning/spikes/006-fused-feature-parallel-histogram/README.md "Scaling — baseline" (fused 5.0× @16t vs two-pass 1.66×; the isolated per-level target)
    - .planning/phases/21.5-.../21.5-CONTEXT.md §specifics ("recover per-level/per-tree 16-thread speedup from ~1.7× to ≥3×; document the 1→16-thread curve at depth 2/4/6 on n=10000, nf=20, nbins=128")
  </read_first>
  <action>
    Complete `crates/cb-train/tests/fused_scaling_test.rs` (CB_PERF-gated, `--release`) to
    measure the PRODUCTION fused path across sized rayon ThreadPools (1, 2, 4, 8, 16 threads)
    and prove the scaling recovery, then write the evidence doc:
    - Primary (hard gate): measure the isolated per-level fused pass — the accumulate+score
      work select_level_plain now performs — on the Spike-002 baseline shape (n=10000, nf=20,
      nbins=128, mid-tree n_leaves), at 1/2/4/8/16 threads inside local `rayon::ThreadPool`s
      (median-of-N, warmup first), print greppable `RSBENCH215 ...` records with
      `speedup = t1 / tN`, and `assert!` the 16-thread speedup is `>= 3.0` (Spike 006 measured
      ~5.0×, so ≥3× is a safe, honest bar). This is the direct proof the serial fraction
      collapsed in production.
    - Secondary (documented, no hard gate): drive end-to-end `train()` per-tree time (the
      spike005 part-A shape) inside the same sized pools at depth 2/4/6, print the
      1→16-thread curve, and document it as the whole-tree-grow view (it includes remaining
      out-of-scope serial phases — binning setup, leaf values — so it is reported, not gated).
    - Write `21.5-EVIDENCE.md`: the measured per-level 1→16-thread speedup table (with the
      before = ~1.7× two-pass ceiling from Spike 005/006 for contrast), the end-to-end
      per-tree curve at depth 2/4/6, the box's core count, the parity statement (full oracle
      suite green, NO re-baseline, determinism green), and an explicit note that low-nf /
      >8-thread within-feature row-block scaling is DEFERRED (out of scope per CONTEXT).
    Tests may use `unwrap()` + standard clippy allows. Keep the file gated so `cargo test`
    without CB_PERF just prints SKIP.
  </action>
  <acceptance_criteria>
    - `CB_PERF=1 cargo test -p cb-train --release --test fused_scaling_test -- --nocapture`
      exits 0 and prints `RSBENCH215` records for threads 1/2/4/8/16.
    - The isolated per-level 16-thread `speedup >= 3.0` assertion passes (the test fails loudly if not).
    - `21.5-EVIDENCE.md` exists and contains: the per-level speedup table (1→16t), the
      end-to-end per-tree curve at depth 2/4/6, the parity-preserved / no-re-baseline
      statement, and the deferred low-nf note.
    - Without CB_PERF, `cargo test -p cb-train --test fused_scaling_test` prints SKIP and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>CB_PERF=1 cargo test -p cb-train --release --test fused_scaling_test -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>The scaling test proves ≥3× per-level speedup at 16 threads on the Spike-002 shape and documents the full 1→16-thread curve (per-level + end-to-end depth 2/4/6) plus the parity statement in 21.5-EVIDENCE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Verification-only plan: runs existing tests + a new CB_PERF-gated benchmark over in-memory
synthetic data, and writes a planning-doc. No external input surface, no network / auth /
serialization / deserialization of untrusted data, no new dependency, no package install. No
trust boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21.5-04 | (informational) | scaling/parity verification | low | accept | No applicable threat above informational — measurement + regression-suite execution over trusted synthetic data; no security-relevant surface. |
</threat_model>

<verification>
- `cargo test -p cb-train -p cb-compute` exits 0 (full oracle + determinism suite green, no fixture changes).
- `CB_PERF=1 cargo test -p cb-train --release --test fused_scaling_test -- --nocapture` exits 0 with per-level 16t speedup ≥ 3.0.
- `21.5-EVIDENCE.md` documents the 1→16-thread curve + parity statement.
</verification>

<success_criteria>
Every shipped ≤10⁻⁵ CPU oracle fixture stays bit-exact (no re-baseline); single- vs
multi-thread determinism holds; and a documented 1→16-thread scaling curve on the Spike-002
shape shows the fused per-level pass recovered from the ~1.7× two-pass ceiling to ≥3× at 16
threads — closing PERF-04.
</success_criteria>

<output>
Create `.planning/phases/21.5-cpu-parallel-scaling-fused-feature-parallel-histogram/21.5-04-SUMMARY.md` when done.
</output>
