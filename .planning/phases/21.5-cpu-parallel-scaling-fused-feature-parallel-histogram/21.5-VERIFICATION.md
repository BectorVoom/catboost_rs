---
phase: 21.5-cpu-parallel-scaling-fused-feature-parallel-histogram
verified: 2026-07-07T00:00:00Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 21.5: CPU Parallel-Scaling — Fused Feature-Parallel Histogram Verification Report

**Phase Goal:** CPU tree-grow recovers CatBoost-class multi-core scaling by moving the per-level bucket-histogram accumulation INTO the parallel region — fusing per-feature accumulate + score into one `rayon` parallel-over-features pass — replacing Phase 21's serial-accumulate → parallel-score two-pass structure, while every shipped ≤10⁻⁵ CPU oracle fixture stays BIT-EXACT.
**Verified:** 2026-07-07
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The per-level accumulation is no longer a serial phase preceding the parallel scorer — plain AND perturbed split search fuse per-feature accumulate+score into ONE rayon parallel-over-features pass; serial whole-partition build removed from the per-level hot path | ✓ VERIFIED | `crates/cb-train/src/tree.rs:882-989` (`select_level_plain`) and `:1007-1076+` (`select_level_perturbed`) both build/derive each feature's histogram *inside* `(0..matrix.n_features()).into_par_iter().map_init(ScanScoreScratch::new, ...)` (2 call sites at lines 913, 1045) and score it via `scan_and_score_borders_into` in the same closure. `GrowScratch::advance_leaf_only` (line 753) is the only per-level state transition on the hot path and does not touch histograms. No whole-partition `build_bucket_histogram(... all features ...)` call remains on either grow path (`grep` confirms `build_bucket_histogram` calls inside `derive_feature_level_hist`/level-0 branches are single-feature, `n_features=1`). |
| 2 | Parity preserved byte-for-byte: fused per-feature scores bit-identical to pre-change serial-build + parallel-score path; every shipped ≤10⁻⁵ CPU oracle fixture stays bit-exact; NO fixed-point, NO oracle re-baseline | ✓ VERIFIED | `crates/cb-compute/tests/fused_feature_parity_test.rs` — both `fused_equals_two_pass_level0` and `fused_subtraction_trick_equals_full_build` independently re-run: **2 passed**, asserting `f64::to_bits()` equality. Full `cargo test -p cb-compute` independently re-run: **197 lib tests + all integration guards green, 0 failed**. `cargo test -p cb-train --test rayon_determinism_test`: **2 passed** (symmetric + leaf-wise byte-identical single- vs multi-thread). `cargo test -p cb-train --test regularization_oracle_test`: **4 passed / 2 pre-existing-ignored** (perturbed `random_strength` oracle byte-exact vs upstream catboost 1.2.10), matching SUMMARY claims exactly. `git status --porcelain` shows no fixture/oracle asset modified in the phase's commit range. |
| 3 | The subtraction trick (child = parent − sibling) preserved within the fused per-feature path; per-task scratch reused (rayon `map_init`/per-thread pool), not allocated inside the `.map` closure | ✓ VERIFIED (with disclosed caveat) | `derive_feature_level_hist` (tree.rs:800-856) performs the identical smaller-sibling-build + `relocate_sub` + `add_relocated` sequence as the pre-21.5 whole-partition path, restricted to one feature; bit-exactness is proven by `fused_subtraction_trick_equals_full_build` (independently re-run, passing). Per-task reuse: `ScanScoreScratch` (the dominant O(n_bins) scan/score buffer set) IS allocated once per rayon task via `map_init` and reused across every feature that task handles (2 call sites, tree.rs:913,1045) — not allocated inside the `.map` closure. **Caveat (disclosed in 21.5-02-SUMMARY.md "Deviations"):** the per-feature `BucketHistogram` itself (built via `build_bucket_histogram`/`relocate_sub`/`add_relocated`, each internally allocating a fresh `Vec<f64>`) is *not* pooled via `FusedFeatureScratch` as originally specified — `BucketHistogram`'s fields are private to `cb-compute`, so `tree.rs` cannot wrap a raw buffer into one. This was a fixed-API constraint, honestly disclosed as an auto-fixed deviation, and its cost is visible in the EVIDENCE.md's own analysis (16t point 3.85× sits below the 8t peak 4.59×, attributed partly to ">8-thread allocator effect", explicitly deferred as out-of-scope future work). It does not prevent the ≥3× gate from passing (independently reproduced at 5.32× in this verification run, see truth 4). |
| 4 | Documented 1→16-thread scaling curve on the Spike-002 grid shows per-level/per-tree speedup recovered from ~1.7× ceiling to ≥3× at 16 threads, determinism test still green | ✓ VERIFIED | `.planning/phases/21.5-.../21.5-EVIDENCE.md` documents fused per-level speedup 3.85×@16t (peak 4.59×@8t) vs two-pass 1.97×@16t ceiling, plus end-to-end per-tree 2.68–2.81×@16t at depth 2/4/6. **Independently re-run** in this verification (`CB_PERF=1 cargo test -p cb-train --release --test fused_scaling_test -- --nocapture`): reproduced `RSBENCH215_DONE fused_per_level_speedup_16t=5.32` (even higher than the committed 3.85×, run-to-run variance expected for a wall-clock benchmark, both comfortably clear the `>= 3.0` hard-gate `assert!`), `part=per_level_parity byte_identical=true`, test passed. `rayon_determinism_test` independently re-run green (see truth 2). |

**Score:** 4/4 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-compute/src/histogram.rs` | `build_bucket_histogram_into`, `scan_and_score_borders_into`, `FusedFeatureScratch`, `fused_feature_scan_and_score` exist and exported | ✓ VERIFIED | All 4 symbols found at lines 609, 973, 1124 (struct), 1156; exported from `crates/cb-compute/src/lib.rs` (confirmed via grep, lines 41-44); `multi_dim_split_score_into` reused (5 occurrences, not forked); no `#[cfg(test)]`/`mod tests` in the file. |
| `crates/cb-compute/tests/fused_feature_parity_test.rs` | RED-first byte-identity guard on 2 shapes + subtraction-trick decomposition | ✓ VERIFIED | File exists, 240 lines, `to_bits()` assertions throughout, both required shapes (nf=20/nbins=128, nf=8/nbins=254) present; independently re-run, 2/2 passed. |
| `crates/cb-train/src/tree.rs` | Fused `select_level_plain`/`select_level_perturbed`, `GrowScratch` restructured, subtraction trick preserved | ✓ VERIFIED | Confirmed via direct read of lines 646-1120 (see Observable Truths 1 and 3); `cargo build -p cb-train` exits 0. |
| `crates/cb-train/tests/fused_scaling_test.rs` | CB_PERF-gated 1→16-thread scaling proof | ✓ VERIFIED | File exists; without `CB_PERF`, `cargo test -p cb-train --test fused_scaling_test` prints SKIP and passes (independently re-run); with `CB_PERF=1 --release`, independently re-run and passed with `fused_16t_speedup >= 3.0` assertion holding. |
| `.planning/phases/21.5-.../21.5-EVIDENCE.md` | Documented 1→16-thread curve + parity statement | ✓ VERIFIED | Exists, contains per-level speedup table, e2e per-tree curve depth 2/4/6, parity/no-re-baseline statement, deferred low-nf note — matches template acceptance criteria. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `select_level_plain` (tree.rs) | `scan_and_score_borders_into` / `build_bucket_histogram` (cb-compute) | direct call inside `map_init` closure | ✓ WIRED | Confirmed at tree.rs:918-950. |
| `select_level_perturbed` (tree.rs) | same cb-compute primitives, RNG contract preserved | fused build/derive precedes untouched serial RNG passes | ✓ WIRED | Confirmed at tree.rs:1043-1120+; `gen_rand()`, `from_seed`+`advance(10)`, `random_score_instance` per border, per-feature `GetInstance` all present exactly once each in original order (grep-confirmed, matches SUMMARY 03 claims); `regularization_oracle_test` random_strength cases independently re-run and pass. |
| `fused_scaling_test.rs` | production `fused_feature_scan_and_score` + `FusedFeatureScratch` | sized `rayon::ThreadPool`s (1/2/4/8/16) | ✓ WIRED | Test drives the shipped primitive (not a reimplementation) per SUMMARY 04 and source inspection; independently re-run, produces the greppable `RSBENCH215` records and passes the hard-gate assertion. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| PERF-04 | 21.5-01, 21.5-02, 21.5-03, 21.5-04 | Fuse per-level histogram accumulation into the parallel region, byte-identical, ≥3× at 16 threads | ✓ SATISFIED | All 4 plans landed the required symbols/wiring; REQUIREMENTS.md marks PERF-04 "Complete" mapped to Phase 21.5; independently re-verified via direct test execution (not just SUMMARY claims). No orphaned requirements — REQUIREMENTS.md maps only PERF-04 to Phase 21.5 and all 4 plans declare `requirements: [PERF-04]`. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` grep across all 5 phase-modified files returned zero matches; no bare `unwrap()` in production code (`histogram.rs`, `tree.rs`). |

### Behavioral Spot-Checks / Independent Re-Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| cb-compute build | `cargo build -p cb-compute` | exit 0 | ✓ PASS |
| cb-train build | `cargo build -p cb-train` | exit 0 | ✓ PASS |
| Byte-identity guard | `cargo test -p cb-compute --test fused_feature_parity_test` | 2 passed | ✓ PASS |
| Full cb-compute suite | `cargo test -p cb-compute` | 197 lib + all integration guards green, 0 failed | ✓ PASS |
| Determinism guard | `cargo test -p cb-train --test rayon_determinism_test` | 2 passed | ✓ PASS |
| Perturbed oracle | `cargo test -p cb-train --test regularization_oracle_test` | 4 passed / 2 pre-existing-ignored | ✓ PASS |
| Disclosed pre-existing failure | `cargo test -p cb-train --test monotone_oracle_test` | 2 passed / 1 failed (`monotone_non_symmetric_and_region_are_typed_errors`) | ✓ CONFIRMED — matches deferred-items.md exactly, unrelated to fusion (stale Phase-6.6 Region-rejection assertion; Region grower never routes through the fused path) |
| Scaling test, no CB_PERF | `cargo test -p cb-train --test fused_scaling_test` | SKIP, 1 passed | ✓ PASS |
| Scaling test, CB_PERF=1 release | `CB_PERF=1 cargo test -p cb-train --release --test fused_scaling_test -- --nocapture` | `fused_per_level_speedup_16t=5.32` (assert `>= 3.0` held), `byte_identical=true` | ✓ PASS (independently reproduced, exceeds the committed 3.85× — run-to-run wall-clock variance, both clear the gate) |

### Human Verification Required

None. All must-haves were independently re-executed against the live codebase (not taken from SUMMARY claims) and passed.

### Gaps Summary

No blocking gaps. One disclosed, non-blocking deviation: the literal "per-task scratch reused... not allocated inside the .map closure" (D-07) is fully satisfied for the dominant `ScanScoreScratch` scan/score buffers but not for the smaller per-feature `BucketHistogram` data buffers (blocked by `BucketHistogram`'s private fields — a real `cb-compute` API constraint, honestly disclosed in 21.5-02-SUMMARY.md's "Deviations from Plan" section, and reflected in the EVIDENCE.md's own analysis of the 16t/8t scaling dip). This does not block the phase goal: the ≥3× at 16 threads gate is met with margin (documented 3.85×, independently reproduced at 5.32×), byte-for-byte parity holds, and the deviation is explicitly scoped as future work (low-nf/row-block parallelism, deferred).

---

_Verified: 2026-07-07_
_Verifier: Claude (gsd-verifier)_
