---
phase: 12-grow-policy-leaf-method-sampling-categorical-device-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-compute/src/runtime.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-backend/src/gpu_runtime/session_depth_gt1_test.rs
autonomous: true
requirements: [GPUT-18]
user_setup: []

must_haves:
  truths:
    - "A depth>1 Plain/fold_count==1 RMSE config is reachable from GpuTrainSession::begin and grows on device via the Phase-11 substrate (no longer force-declined by depth != 1)."
    - "The seam carries a non-symmetric node graph as plain host structs (step_nodes/node_id_to_leaf_id) with no cubecl/cb-backend type crossing it."
    - "A single plain host-typed DeviceTrainConfig struct carries grow-policy/sampling/exact/CTR config so later waves widen config without growing the arg list per wave."
    - "DeviceGrownTree exposes the non-symmetric node-graph carrier (step_nodes/node_id_to_leaf_id) that the Plan-03/04 device-fold arms consume to fold NonSymmetricTree/RegionTree (NOT always ObliviousTree) — the oblivious-shape path stays byte-unchanged."
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "DeviceGrownTree.step_nodes + node_id_to_leaf_id fields; DeviceTrainConfig host struct; begin_device_training config surface"
      contains: "step_nodes"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "depth>1 gate relaxation routed through Phase-11 grow substrate; all-or-nothing Ok(None) preserved for still-uncovered configs"
      contains: "DeviceTrainConfig"
    - path: "crates/cb-backend/src/gpu_runtime/session_depth_gt1_test.rs"
      provides: "self-oracle: depth>1 device tree == CPU oblivious depth>1 within ε=1e-4"
      contains: "max_divergence"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/session.rs"
      to: "crates/cb-backend/src/kernels/grow_loop.rs"
      via: "grow_oblivious_tree_resident dispatched for depth>1 (Phase-11 partition-aware substrate)"
      pattern: "grow_oblivious_tree_resident"
    - from: "crates/cb-compute/src/runtime.rs"
      to: "crates/cb-model/src/model.rs"
      via: "DeviceGrownTree host structs feed Model::from_trained (non-sym node graph)"
      pattern: "step_nodes"
  prohibitions:
    - "MUST NOT add a cb-train dependency to cb-backend (feature unification breaks the rocm runtime) — transcribe any CPU reference inline."
    - "MUST NOT let a cubecl/cb-backend type cross the Runtime seam — DeviceGrownTree and DeviceTrainConfig are plain host structs only."
    - "MUST NOT flip a still-unsigned-off config to a device result: any config not covered returns Ok(None)→byte-unchanged CPU grower (D-04, D-10-01 all-or-nothing)."
    - "MUST NOT emit a `-inf` float literal inside any `#[cube]` kernel — use the finite f32::MIN/f32::MAX sentinel."
---

<objective>
Lay the Phase-12 device-coverage foundation the grow-policy waves stand on: (1) verify and close the A3 gap — depth>1 device grow is currently force-declined by `session.rs:154` (`depth != 1`), so relax the gate to route a depth>1 Plain/fold_count==1 config through the already-shipped Phase-11 partition-aware substrate (`grow_oblivious_tree_resident` / `grow_loop.rs`); (2) extend `DeviceGrownTree` with the non-symmetric node graph as plain host structs (`step_nodes`, `node_id_to_leaf_id`) per D-04 — the carrier the Plan-03 non-sym fold arm and Plan-04 Region fold arm consume in `boosting.rs`; (3) introduce a single plain host-typed `DeviceTrainConfig` struct (Open Q2) so W2–W6 widen config without growing the `begin_device_training` arg list.

NOTE (BLOCKER handoff): the `boosting.rs` device-grow branch (~:2997-3108) that folds a `DeviceGrownTree` into the `Model` currently ALWAYS pushes `ObliviousTree` and ignores `step_nodes`/`node_id_to_leaf_id`. That fold branch is made shape-aware IN THE SAME PLAN AS THE GATE-WIDENING it depends on — Plan 03 adds the non-sym fold arm atomically with the Depthwise/Lossguide gate arm, and Plan 04 adds the Region fold arm atomically with the Region gate arm — so no window ever exists where the gate returns `Ok(Some(_))` for a non-oblivious config while the fold still degenerates to `ObliviousTree`. Plan 01 only exposes the seam carrier fields (2); it deliberately does NOT touch `boosting.rs` (that file is owned by Plan 02 in wave 1 — editing it here would be a same-wave file conflict).

Purpose: every later family (non-sym policies, Exact, sampling, MVS, CTR) widens THIS seam + THIS gate; getting the host-structs-only boundary and the config surface right once keeps the rest of the phase landmine-safe.
Output: extended `DeviceGrownTree` (incl. the non-sym carrier fields), `DeviceTrainConfig`, a depth>1-reachable gate, and a depth>1 self-oracle test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-CONTEXT.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-RESEARCH.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-PATTERNS.md
@crates/cb-compute/src/runtime.rs
@crates/cb-backend/src/gpu_runtime/session.rs
@crates/cb-backend/src/kernels/grow_loop.rs
@crates/cb-train/src/tree.rs
</context>

<artifacts_produced>
Full Phase-12 symbol set (✎ = delivered by THIS plan):
- ✎ `DeviceTrainConfig` plain host struct (cb-compute/src/runtime.rs) — grow_policy/max_leaves/min_data_in_leaf, bootstrap_type/sample_rate/mvs_lambda/pinned-seed, exact-leaf flag+alpha/delta, CTR config
- ✎ `DeviceGrownTree.step_nodes: Vec<(u16,u16)>` + `.node_id_to_leaf_id: Vec<u32>` (cb-compute/src/runtime.rs)
- ✎ session `begin` `depth != 1` relaxation routed through Phase-11 grow substrate (cb-backend/.../session.rs)
- `nonsym_grow` kernel + test + the boosting.rs device-fold **non-sym arm** (Plan 03); `TreeVariant::Region(RegionTree)` + Region grower + `validate_grow_policy` lift + Region apply/json (Plan 02); `region_device` kernel + the boosting.rs device-fold **Region arm** (Plan 04)
- segmented-radix-sort primitive + `exact_quantile` kernel (Plan 05); `bootstrap_device` (Plan 06); `mvs_device` (Plan 07); `ctr_device` + CTR→cindex join (Plan 08)
- per-family `Ok(None)` gate arms; SC-5 COVERAGE-MATRIX.md + per-family frozen ε=1e-4 fixtures (Plan 09)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Audit A3 depth>1 reachability, then relax the session gate</name>
  <files>crates/cb-backend/src/gpu_runtime/session.rs, crates/cb-backend/src/gpu_runtime/session_depth_gt1_test.rs</files>
  <read_first>
    - crates/cb-backend/src/gpu_runtime/session.rs:120-200 (the `begin` coverage gate; :154 `if depth != 1 || !boosting_type_is_plain || fold_count != 1 { return Ok(None); }`; `map_der_kernel`/`map_score_fn` at :98/:114; `grow_oblivious_tree_resident` call at :325)
    - crates/cb-backend/src/kernels/grow_loop.rs:766-795 (GPUT-05 comment: `depth > 1` is DEVICE-COVERED by `grow_oblivious_tree`) and the Newton path at :903-952
    - 12-RESEARCH.md Assumptions Log A3 + Open Questions; 12-PATTERNS.md "cb-backend/src/gpu_runtime/session.rs — widen coverage gate"
    - CPU reference oracle: crates/cb-backend/src/kernels/grow_loop.rs::max_divergence (:61) for the ε self-check
  </read_first>
  <action>
    Confirm whether `grow_oblivious_tree_resident` (called at session.rs:325 per `grow_one`) already dispatches the Phase-11 partition-aware depth>1 histogram path when `depth > 1`; document the finding in the SUMMARY. Then relax the `depth != 1` term in the `begin` gate (:154) to permit `depth >= 1` for the Plain/fold_count==1/covered-loss/covered-score/covered-n_bins regime, so a depth>1 config flows through the resident grow instead of being force-declined to CPU. Keep `!boosting_type_is_plain`, `fold_count != 1`, the `map_der_kernel`/`map_score_fn` `None => Ok(None)` arms, the degenerate-dimension guard, and the `n_bins` dispatch-set guard UNCHANGED (still return `Ok(None)` — all-or-nothing D-10-01). Do NOT touch the CPU/host grower. If the audit shows the resident path does NOT yet handle depth>1, scope this task to adding ONLY the missing dispatch wiring in `grow_oblivious_tree_resident`, transcribing from `grow_loop.rs` (no `cb-train` dep). Add `crates/cb-backend/src/gpu_runtime/session_depth_gt1_test.rs` (sibling `#[cfg(test)] #[path]` mount, source/test separation) that grows a depth-6 RMSE tree via the session path and asserts structure equality + score divergence ≤1e-4 vs the direct `grow_oblivious_tree` call using `max_divergence`.
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-backend session_depth_gt1` passes: a depth-6 Plain/fold_count==1 RMSE config returns `Ok(Some(_))` from `begin` and its grown structure matches the direct depth>1 `grow_oblivious_tree` bit-for-bit on integer `(feature,bin)` splits, with score max-abs divergence ≤1e-4.
    - A depth-6 config with `boosting_type_is_plain == false` OR `fold_count != 1` OR an unmapped loss/score still returns `Ok(None)` (assert in the same test file).
    - No `unwrap`/`expect`/`panic` in production paths; `grep -n "use cb_train" crates/cb-backend/src` returns nothing new.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-backend session_depth_gt1</automated>
  </verify>
  <done>Depth>1 Plain/fold==1 configs reach the device grow through the session; all still-uncovered configs return Ok(None); the self-oracle test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DeviceGrownTree with the non-symmetric node graph (host structs)</name>
  <files>crates/cb-compute/src/runtime.rs</files>
  <read_first>
    - crates/cb-compute/src/runtime.rs:900-935 (`DeviceGrownTree` at :917: `splits: Vec<(u32,u32)>`, `leaf_values: Vec<f64>`, `leaf_of: Vec<u32>`; the ":900" doc that the struct is plain host types and never gains a cubecl/cb-backend dep via feature unification — T-10-04)
    - crates/cb-train/src/tree.rs:196-232 (`GrownTree.step_nodes: Vec<(u16,u16)>` (left_subtree_diff,right_subtree_diff), `node_id_to_leaf_id: Vec<u32>`) and :1137-1204 (`u32::MAX` interior sentinel + checked `u16::try_from` child-diff encoding, CR-01/CR-02)
    - 12-PATTERNS.md "extend DeviceGrownTree (seam)" (verbatim field shapes) + Code Examples "Non-symmetric emission boundary (host structs)"
  </read_first>
  <action>
    Add two plain-host fields to `DeviceGrownTree` mirroring `GrownTree` verbatim: `pub step_nodes: Vec<(u16, u16)>` (left_subtree_diff, right_subtree_diff; `(0,0)` == terminal leaf) and `pub node_id_to_leaf_id: Vec<u32>` (`u32::MAX` == interior sentinel; else distinct leaf id). Preserve the existing oblivious fields and their doc contract. Document that for the oblivious/depth>1 path these vectors are EMPTY (oblivious emission is unchanged, D-04 no-regression); only the non-sym emission (Plan 03) fills them. Keep the "plain host types only" doc invariant explicit. No `#[cube]`/cubecl type may appear on the struct.
  </action>
  <acceptance_criteria>
    - `grep -n "step_nodes\|node_id_to_leaf_id" crates/cb-compute/src/runtime.rs` shows both fields on `DeviceGrownTree`.
    - `cargo build -p cb-compute` succeeds; existing oblivious constructors/tests still compile (empty vecs default).
    - No cubecl/cb-backend import added to cb-compute for these fields (`grep -n "cubecl\|cb_backend" crates/cb-compute/src/runtime.rs` unchanged for the struct region).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cb-compute && cargo test -p cb-compute runtime</automated>
  </verify>
  <done>DeviceGrownTree carries the non-sym node graph as plain host structs; oblivious path unchanged; boundary stays host-structs-only.</done>
</task>

<task type="auto">
  <name>Task 3: Introduce the DeviceTrainConfig host struct and route it through begin_device_training</name>
  <files>crates/cb-compute/src/runtime.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <read_first>
    - crates/cb-compute/src/runtime.rs:1012-1090 (`begin_device_training` at :1012, current 12-arg surface; `grow_tree_on_device` at :1059 default `Ok(None)`; `end_device_training` at :1076)
    - crates/cb-backend/src/gpu_runtime/session.rs:137-200 (`begin` signature it must consume the config from)
    - 12-RESEARCH.md Open Question 2 (config surface) + 12-PATTERNS.md "Also extend begin_device_training config surface (Open Q2): introduce a small plain host-typed DeviceTrainConfig struct"
  </read_first>
  <action>
    Define `pub struct DeviceTrainConfig` in cb-compute/src/runtime.rs as a plain host struct (NO cubecl) with fields the later waves need: `grow_policy` (an enum value or a small local host enum mirroring `EGrowPolicy` by value — do NOT `use cb_train`), `max_leaves: Option<usize>`, `min_data_in_leaf: usize`, `bootstrap_type` (local host enum), `sample_rate: f32`, `mvs_lambda: Option<f64>`, `rng_seed: u64`, `exact_leaf: bool`, `quantile_alpha: f64`, `quantile_delta: f64`, and a `ctr: Option<DeviceCtrConfig>` placeholder (define `DeviceCtrConfig` as an empty-for-now plain struct to be filled in Plan 08). Give it a `Default` that reproduces today's covered regime (SymmetricTree, Newton/uniform, no CTR) so existing callers are byte-unchanged. Thread a `&DeviceTrainConfig` (or an owned copy) into `begin_device_training` as a single new parameter rather than growing the arg list per wave, and have `GpuTrainSession::begin` accept and store it. For THIS plan every non-default config value still routes to `Ok(None)` (later waves flip each arm); assert that invariant in code comments.
  </action>
  <acceptance_criteria>
    - `grep -n "struct DeviceTrainConfig" crates/cb-compute/src/runtime.rs` returns the definition; it has no cubecl import.
    - `DeviceTrainConfig::default()` drives the existing covered regime; `cargo test -p cb-backend` depth-1 device tests still pass unchanged (no behavior regression, D-04).
    - A `DeviceTrainConfig` with `grow_policy != SymmetricTree` (or any non-default family flag) still yields `Ok(None)` from `begin` (assert in `session_depth_gt1_test.rs` or a sibling test).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-backend gpu_runtime && cargo test -p cb-compute runtime</automated>
  </verify>
  <done>A single plain host DeviceTrainConfig carries all later-wave config; default preserves today's device path; non-default family flags still decline to CPU.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase adds numerical GPU training kernels only — NO network, auth, session, file-upload, or untrusted-input surface. The one boundary is the host↔device seam, which crosses plain host structs (`DeviceGrownTree`, `DeviceTrainConfig`, quantized bins, leaf indices, partition offsets) — all internally generated, not attacker-controlled.

| Boundary | Description |
|----------|-------------|
| host→device (Runtime seam) | Internally-generated quantized indices + config cross into `#[cube]` kernels; bounds-checked before device indexing (V5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering/DoS | device index math (bin/leaf/partition) | mitigate | length + `checked_*` validation at `begin` (V5); typed `CbError`, no `unwrap()`. |
| T-12-02 | Tampering (integrity) | seam config regression | mitigate | `DeviceTrainConfig::default()` preserves the byte-unchanged covered regime (D-04). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N-A — phase installs no external packages (RESEARCH Package Legitimacy Audit); no supply-chain surface. |

The load-bearing correctness gate is the ε=1e-4 device-vs-CPU oracle, NOT a security boundary. No auth/network/PII threats apply.
</threat_model>

<verification>
- `cargo test -p cb-backend session_depth_gt1` green (depth>1 reachable + declined-config invariants).
- `cargo build -p cb-compute && cargo build -p cb-backend` green.
- ROCm smoke after any `#[cube]` touch: `cargo build -p cb-backend --features rocm` (local convenience only, NOT a gate).
- `grep -rn "use cb_train" crates/cb-backend/src` adds nothing (landmine).
</verification>

<success_criteria>
- Depth>1 Plain/fold==1/covered configs reach the device grow through `GpuTrainSession::begin`; all still-uncovered configs return `Ok(None)`.
- `DeviceGrownTree` carries `step_nodes`/`node_id_to_leaf_id` as plain host structs; oblivious path byte-unchanged.
- `DeviceTrainConfig` exists as the single config surface; default preserves today's device path.
- `DeviceGrownTree` exposes the non-sym carrier (`step_nodes`/`node_id_to_leaf_id`) that Plans 03/04 consume in the `boosting.rs` device-fold arms; Plan 01 does not itself edit `boosting.rs` (avoids the wave-1 file conflict with Plan 02).
</success_criteria>

<output>
Create `.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-01-SUMMARY.md` when done. Record the A3 audit finding (was depth>1 already reachable, or what wiring was added).
</output>
