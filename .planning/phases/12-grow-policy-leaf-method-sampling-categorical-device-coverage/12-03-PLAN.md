---
phase: 12-grow-policy-leaf-method-sampling-categorical-device-coverage
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - crates/cb-backend/src/kernels/nonsym_grow.rs
  - crates/cb-backend/src/kernels/nonsym_grow_test.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-compute/src/runtime.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/boosting_device_fold_test.rs
  - crates/cb-train/src/device_nonsym_fit_test.rs
autonomous: true
requirements: [GPUT-18]
user_setup: []

must_haves:
  truths:
    - "A Depthwise (level-order) and a Lossguide (best-gain-priority) tree grow on device, producing a non-symmetric node graph as plain host structs."
    - "The device non-sym structure + leaf values match the CPU leaf_wise_grower within ε=1e-4 (structure integer-exact, scores ≤1e-4)."
    - "The device path fills DeviceGrownTree.step_nodes/node_id_to_leaf_id and routes through Model::from_trained → TreeVariant::NonSymmetric (no device-native tree type)."
    - "The boosting.rs device-grow branch is made shape-aware IN THIS PLAN (a non-sym fold arm keyed on step_nodes non-empty) so it folds a NonSymmetricTree into non_symmetric_trees instead of always ObliviousTree; the oblivious arm stays byte-identical."
    - "An end-to-end cb-train::train() Depthwise/Lossguide fit that commits to the device path folds through this plan's device-fold non-sym arm into non_symmetric_trees (NonSymmetricTree), reproducing the CPU leaf_wise_grower-grown model within ε=1e-4 — NOT a degenerate ObliviousTree."
  artifacts:
    - path: "crates/cb-backend/src/kernels/nonsym_grow.rs"
      provides: "device Depthwise/Lossguide per-policy split scoring + leaf-selection order + node-graph emission"
      contains: "nonsym"
    - path: "crates/cb-backend/src/kernels/nonsym_grow_test.rs"
      provides: "serial CPU self-oracle: device non-sym == leaf_wise_grower ≤1e-4"
      contains: "max_divergence"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "Depthwise/Lossguide gate arm (map_grow_policy) flipping Ok(None)→device"
      contains: "grow_policy"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "device-grow branch shape dispatch: oblivious splits → ObliviousTree (byte-unchanged); non-empty step_nodes → NonSymmetricTree (node-graph pointer-walk leaf assignment) into non_symmetric_trees"
      contains: "step_nodes"
    - path: "crates/cb-train/src/boosting_device_fold_test.rs"
      provides: "unit self-oracle: a non-sym DeviceGrownTree folds to a NonSymmetricTree whose per-object leaf assignment matches the cb-model non-sym pointer-walk"
      contains: "step_nodes"
    - path: "crates/cb-train/src/device_nonsym_fit_test.rs"
      provides: "end-to-end: cb-train::train() device Depthwise/Lossguide fit reproduces the CPU leaf_wise_grower model ≤1e-4 (non_symmetric_trees non-empty, oblivious_trees empty)"
      contains: "non_symmetric_trees"
  key_links:
    - from: "crates/cb-backend/src/kernels/nonsym_grow.rs"
      to: "crates/cb-backend/src/kernels/score_split.rs"
      via: "reuses launch_find_optimal_split_pointwise per candidate leaf (argmin with finite sentinel)"
      pattern: "find_optimal_split"
    - from: "crates/cb-backend/src/kernels/nonsym_grow.rs"
      to: "crates/cb-compute/src/runtime.rs"
      via: "fills DeviceGrownTree.step_nodes/node_id_to_leaf_id (host structs)"
      pattern: "step_nodes"
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-compute/src/runtime.rs"
      via: "this plan's device-fold non-sym arm consumes the filled step_nodes/node_id_to_leaf_id → NonSymmetricTree; the same plan's gate arm makes it fire end-to-end"
      pattern: "non_symmetric_trees"
  prohibitions:
    - "MUST NOT create a device-native non-symmetric tree type — emit plain host structs only (D-04)."
    - "MUST NOT use a `-inf` literal for the score ARGMAX sentinel in the `#[cube]` kernel — use finite f32::MIN/f32::MAX (Pitfall 7)."
    - "MUST NOT add a cb-train dependency to cb-backend — transcribe leaf_wise_grower semantics inline."
    - "MUST NOT use a non-deterministic device SUM — every parity-critical reduction uses the fixed-point Atomic<u64> k=30 accumulator."
    - "MUST NOT flip Lossguide/Depthwise to a device result before its gate arm is covered — uncovered stays Ok(None), all-or-nothing."
    - "MUST NOT push an ObliviousTree on the non-sym fold arm — a step_nodes-populated DeviceGrownTree folds to a NonSymmetricTree into non_symmetric_trees only."
    - "MUST NOT widen the Depthwise/Lossguide gate arm WITHOUT the boosting.rs non-sym fold arm landing in the SAME plan — otherwise the device branch folds a degenerate ObliviousTree (the checker BLOCKER)."
    - "MUST NOT add a cb-backend dependency to cb-train — transcribe the cb-model non-sym pointer-walk (leaf_index_nonsym) inline for per-object leaf assignment."
---

<objective>
W1: grow Depthwise and Lossguide non-symmetric trees on device (GPUT-18). The device searches STRUCTURE + leaf values only and emits the non-symmetric node graph as plain host structs (`step_nodes`/`node_id_to_leaf_id`, from Plan 01), which host `Model::from_trained` builds into `TreeVariant::NonSymmetric` — mirroring upstream's generic `TGreedyTreeLikeStructureSearcher<TTreeModel>` → one `BuildTreeLikeModel<TModel>` step (§5.1–5.3). The grow driver reuses the Phase-11 depth>1 partition/histogram spine; only the leaf-selection ORDER changes (level-order for Depthwise, best-gain-priority for Lossguide) and it emits a node graph instead of a depth-length split list.

Purpose: flip the first non-symmetric family from `Ok(None)`→device against the existing `leaf_wise_grower` ≤1e-5 CPU reference.
Output: `nonsym_grow` device kernel + self-oracle + Depthwise/Lossguide gate arm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-CONTEXT.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-RESEARCH.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-PATTERNS.md
@crates/cb-backend/src/kernels/grow_loop.rs
@crates/cb-backend/src/kernels/score_split.rs
@crates/cb-backend/src/gpu_runtime/session.rs
@crates/cb-compute/src/runtime.rs
@crates/cb-train/src/tree.rs
@CATBOOST_CUDA_KERNELS_DESIGN.md
</context>

<artifacts_produced>
Full Phase-12 symbol set (✎ = delivered by THIS plan):
- `DeviceTrainConfig` + `DeviceGrownTree` non-sym carrier fields + depth relaxation (Plan 01); `TreeVariant::Region` + Region CPU path + `Model.non_symmetric_trees`/`region_trees` (Plan 02)
- ✎ `nonsym_grow` device kernel + `nonsym_grow_test.rs` (cb-backend/src/kernels)
- ✎ Depthwise/Lossguide `map_grow_policy` gate arm in session `begin` (cb-backend/.../session.rs)
- ✎ non-sym node-graph fill of `DeviceGrownTree` (cb-compute/src/runtime.rs seam usage)
- ✎ boosting.rs device-fold **non-sym arm** (step_nodes → NonSymmetricTree into non_symmetric_trees) + `boosting_device_fold_test.rs` (unit) + `device_nonsym_fit_test.rs` (end-to-end) (cb-train/src)
- `region_device` kernel + boosting.rs device-fold Region arm (Plan 04); Exact/bootstrap/MVS/CTR kernels (Plans 05-08); COVERAGE-MATRIX.md (Plan 09)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Device Depthwise/Lossguide structure search + node-graph emission</name>
  <files>crates/cb-backend/src/kernels/nonsym_grow.rs, crates/cb-compute/src/runtime.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/grow_loop.rs (host-light greedy oblivious grow driver — reads back the O(1) BestSplit descriptor + per-partition stats per level, `launch_partition_split_into`; the depth>1 partition-aware substrate; `max_divergence` :61) and score_split.rs (`launch_find_optimal_split_pointwise`, argmin with FINITE sentinel — Pattern D)
    - crates/cb-train/src/tree.rs:909-1204 (`leaf_wise_grower` :909, `LeafWisePolicy` enum, node-graph finalization `step_nodes`/`node_id_to_leaf_id` :1137-1204, `u32::MAX` interior sentinel + checked `u16::try_from` — the CPU parity oracle)
    - crates/cb-compute/src/runtime.rs:917 (`DeviceGrownTree` with the Plan-01 `step_nodes`/`node_id_to_leaf_id` carrier fields)
    - CATBOOST_CUDA_KERNELS_DESIGN.md §5.4 `SelectLeavesToSplit`, §6.4 `ComputeOptimalSplit` (Depthwise/Lossguide)
    - 12-PATTERNS.md "cb-backend/src/kernels/nonsym_grow.rs" (reuse spine, change selection order, `-inf` sentinel landmine, emission boundary)
  </read_first>
  <action>
    Add `nonsym_grow.rs` implementing a device non-symmetric grow driver that reuses the Phase-11 partition-aware histogram + `launch_find_optimal_split_pointwise` scoring spine but changes the leaf-selection order: level-order frontier expansion for `LeafWisePolicy::Depthwise`, best-gain-priority (max-score leaf next) for `LeafWisePolicy::Lossguide`, both bounded by `max_leaves`/`min_data_in_leaf` from `DeviceTrainConfig`. Transcribe the `leaf_wise_grower` node-registration bookkeeping inline (no `cb-train` dep): as splits are chosen, build the `step_nodes: Vec<(u16,u16)>` (left/right subtree diffs, `(0,0)` terminal) and `node_id_to_leaf_id: Vec<u32>` (`u32::MAX` interior sentinel), using checked `u16::try_from` for child diffs (CR-01/CR-02). Fill these onto `DeviceGrownTree` as plain host structs; do NOT create a device-native tree type. Any device min/max/ARGMAX init uses a FINITE `f32::MIN`/`f32::MAX` sentinel inside `#[cube]` (never `-inf` — Pitfall 7). Every parity-critical SUM goes through the deterministic fixed-point `Atomic<u64>` k=30 reduction.
  </action>
  <acceptance_criteria>
    - `nonsym_grow.rs` produces `DeviceGrownTree` with non-empty `step_nodes`/`node_id_to_leaf_id` and PER-NODE `splits` (one `(feature,bin)` per interior node, NOT the depth-length per-level oblivious list) for a Depthwise and a Lossguide config; the node graph is valid (interior nodes `u32::MAX`, leaves distinct, child diffs `u16`). This plan's Task 2 boosting.rs device-fold branch keys on `step_nodes` non-empty (NOT on `splits` emptiness) to route this into `NonSymmetricTree`.
    - `grep -n "NEG_INFINITY\|-inf\|f32::MIN\|f32::MAX" crates/cb-backend/src/kernels/nonsym_grow.rs` shows only finite sentinels inside `#[cube]` regions.
    - `grep -n "use cb_train" crates/cb-backend/src/kernels/nonsym_grow.rs` returns nothing.
    - `cargo build -p cb-backend` and `cargo build -p cb-backend --features rocm` succeed (ROCm smoke, no JIT-reject on `#[cube]`).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cb-backend && cargo build -p cb-backend --features rocm</automated>
  </verify>
  <done>Device Depthwise/Lossguide grow emits a valid non-sym node graph as host structs using finite sentinels + deterministic reduction; no cb-train dep.</done>
</task>

<task type="auto">
  <name>Task 2: Shape-dispatch the boosting.rs device-grow branch and fold non-sym device trees as NonSymmetricTree</name>
  <files>crates/cb-train/src/boosting.rs, crates/cb-train/src/boosting_device_fold_test.rs</files>
  <read_first>
    - crates/cb-train/src/boosting.rs:2997-3108 (the ONLY device-grow fold branch: `device_active` reads `dev_tree.splits`/`dev_tree.leaf_values`, computes `device_leaf_of` via the oblivious `leaf_index(&passes)` at :3053-3066, and ALWAYS `trees.push(ObliviousTree { .. })` at :3101 — the exact code to make shape-aware; this is the checker BLOCKER)
    - crates/cb-train/src/boosting.rs:4203-4223 (the CPU fold's shape dispatch to MIRROR: `if grown.step_nodes.is_empty() { trees.push(ObliviousTree{..}) } else { non_symmetric_trees.push(NonSymmetricTree{ splits, step_nodes, node_id_to_leaf_id, leaf_values, leaf_weights }) }`) and :2830-2833 (`let mut trees` / `let mut non_symmetric_trees`)
    - crates/cb-train/src/tree.rs:791-820 (`NonSymmetricTree` cb-train shape: per-NODE `splits`, `step_nodes`, `node_id_to_leaf_id`) — the device non-sym tree carries per-node `(feature,bin)` in `dev_tree.splits` (one per interior node) with `step_nodes`/`node_id_to_leaf_id` populated
    - crates/cb-model/src/apply.rs:234-270 (`leaf_index_nonsym` — the flat-node pointer-walk to TRANSCRIBE inline into cb-train for per-object leaf assignment: bounded walk, `left_diff`/`right_diff`, `u32::MAX` interior guard, checked `.get`; this is cb-MODEL apply logic transcribed into cb-TRAIN — the forbidden direction is a cb-train dep INSIDE cb-backend, NOT this)
    - crates/cb-compute/src/runtime.rs `DeviceGrownTree` (Plan-01 `step_nodes`/`node_id_to_leaf_id` fields) — the dispatch key is `dev_tree.step_nodes.is_empty()`, EXACTLY like the CPU path keys on `grown.step_nodes.is_empty()`
  </read_first>
  <action>
    Restructure the `device_active` branch (:3006-3108) so it dispatches on the populated `DeviceGrownTree` shape instead of unconditionally building an `ObliviousTree`:
    (a) **Oblivious arm** (`dev_tree.step_nodes.is_empty()`): keep the EXISTING behavior byte-for-byte — the `(feature,bin_id)→border` join (Pattern 4), `device_leaf_of` via `leaf_index(&passes)`, `normalize_leaf_values`, approx update, `trees.push(ObliviousTree{..})`. Depth>1 oblivious (Plan 01) flows through here unchanged.
    (b) **Non-symmetric arm** (`!dev_tree.step_nodes.is_empty()`): resolve each PER-NODE `dev_tree.splits[node]` `(feature,bin_id)` to a `Split` via the same range-checked `feature_borders[feature][bin_id]` join; compute per-object `device_leaf_of` by TRANSCRIBING `cb-model`'s `leaf_index_nonsym` pointer-walk inline (bounded walk over `step_nodes` left/right diffs, `u32::MAX` interior guard, halt on the zero side, read `node_id_to_leaf_id`; malformed → checked fallback, never panic/unwrap); apply the SAME `normalize_leaf_values` (is_pairwise=false) + approx accumulation as the oblivious arm using this leaf assignment; then `non_symmetric_trees.push(NonSymmetricTree { splits: device_splits, step_nodes: dev_tree.step_nodes.clone(), node_id_to_leaf_id: dev_tree.node_id_to_leaf_id.clone(), leaf_values, leaf_weights })` — mirroring the CPU fold at :4216. Do NOT push an `ObliviousTree` on this arm.
    Keep `continue` after each arm; keep the `Ok(None)`-after-`begin` typed-error guard (:3009-3022) unchanged. Do NOT add the Region arm here (Plan 04 owns it — Region `region_path` field/types do not exist until Plan 04). This task lands in the SAME plan as (and BEFORE) the Task 3 Depthwise/Lossguide gate arm, so the fold arm is in place the instant the gate returns `Ok(Some(_))` for a non-oblivious config (closes the checker BLOCKER — no degenerate-ObliviousTree window). Add `crates/cb-train/src/boosting_device_fold_test.rs` (sibling `#[cfg(test)] #[path]` mount, source/test separation) that constructs a hand-built non-sym `DeviceGrownTree` (populated `step_nodes`/`node_id_to_leaf_id`/per-node `splits`/`leaf_values`) and asserts the fold produces a `NonSymmetricTree` (into `non_symmetric_trees`, NOT `trees`) whose per-object `device_leaf_of` equals the cb-model `leaf_index_nonsym` walk over the same graph for a small feature matrix.
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-train boosting_device_fold` passes: a non-sym `DeviceGrownTree` folds into `non_symmetric_trees` as a `NonSymmetricTree` (assert `trees` stays empty on that fixture) and its per-object leaf assignment equals the cb-model `leaf_index_nonsym` walk over the identical node graph.
    - The oblivious arm is unchanged: a depth>1 oblivious `DeviceGrownTree` still folds to `ObliviousTree` byte-identically (existing device-oblivious tests / `session_depth_gt1` remain green).
    - No `unwrap`/`expect`/`panic` in the new fold path (malformed node graph → checked `.get` fallback, mirroring `leaf_index_nonsym`); `grep -n "use cb_backend" crates/cb-train/src/boosting.rs` returns nothing new (transcription, not a dep).
    - `boosting_device_fold_test.rs` is a separate file (`grep -n "mod tests" crates/cb-train/src/boosting.rs` unchanged — no embedded test module added).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-train boosting_device_fold</automated>
  </verify>
  <done>The device-grow branch dispatches on DeviceGrownTree shape and folds a non-sym device tree as NonSymmetricTree into non_symmetric_trees (leaf assignment via the transcribed non-sym pointer-walk); the oblivious arm stays byte-identical; the Region arm is deferred to Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: Self-oracle test + Depthwise/Lossguide session gate arm + end-to-end fit oracle</name>
  <files>crates/cb-backend/src/kernels/nonsym_grow_test.rs, crates/cb-backend/src/gpu_runtime/session.rs, crates/cb-train/src/device_nonsym_fit_test.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/grow_loop.rs::max_divergence (:61) + score_split.rs tests (:307) — the serial self-oracle pattern (structure `assert_eq!`, scores a reported ε bound); Pattern E (source/test separation, `#[cfg(test)] #[path]` mount)
    - crates/cb-backend/src/gpu_runtime/session.rs:98-178 (`map_score_fn`/`map_der_kernel` `Option`-returning template; the gate; `DeviceTrainConfig` from Plan 01)
    - crates/cb-train/src/tree.rs::leaf_wise_grower (the CPU reference to run the identical fixture through)
    - crates/cb-train/src/boosting.rs device-fold non-sym arm (THIS plan's Task 2 — the `!dev_tree.step_nodes.is_empty()` arm that folds into `non_symmetric_trees`; this task's gate arm is what makes that arm FIRE end-to-end, so the two together must reproduce the CPU-grown model)
    - 12-PATTERNS.md Pattern A (Ok(None) family-gated flip) + Pattern E
  </read_first>
  <action>
    Add `nonsym_grow_test.rs` (sibling `#[path]` mount, NEVER `mod tests` in the production file): grow a pinned depth-6 fixture through BOTH the device `nonsym_grow` path and the CPU `leaf_wise_grower` (transcribe/import the CPU reference into the test only), for Depthwise and Lossguide. Assert integer structure equality (`step_nodes`/`node_id_to_leaf_id` `assert_eq!`) and leaf-value max-abs divergence ≤1e-4 via `max_divergence` (which returns `(INF,INF)` on length mismatch so a truncated buffer fails loudly). In session.rs add a `map_grow_policy(grow_policy) -> Option<LeafWisePolicy>` config map following the `map_score_fn` template and a gate arm: `Depthwise`/`Lossguide` → covered (route to `nonsym_grow`), everything not-yet-signed-off → `Ok(None)`. Keep all-or-nothing (D-10-01): a fit is fully device or fully CPU. Additionally add an END-TO-END fold check (in `nonsym_grow_test.rs` or a `cb-train` sibling test): run a small pinned Depthwise (and Lossguide) config through `cb-train::train()` with the device path committed (`device_active`), and assert the returned `Model` has a NON-EMPTY `non_symmetric_trees` (and EMPTY `oblivious_trees`) whose predictions reproduce the CPU `leaf_wise_grower`-grown model within ε=1e-4 — proving THIS plan's Task 2 device-fold non-sym arm actually fires under the newly-widened gate (guards against the "degenerate ObliviousTree" regression the checker flagged), not just the isolated kernel self-oracle. Put this end-to-end test in `crates/cb-train/src/device_nonsym_fit_test.rs`. Document that Kaggle CUDA ε=1e-4 sign-off is deferred to Plan 09 (the in-env self-oracle + ROCm smoke is the local gate).
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-backend nonsym_grow` passes: device Depthwise and Lossguide structure == `leaf_wise_grower` structure (integer-exact) and leaf values ≤1e-4.
    - A `SymmetricTree`/unsupported grow_policy still returns `Ok(None)` (assert); a Depthwise config returns `Ok(Some(_))`.
    - End-to-end: a `cb-train::train()` device-committed Depthwise fit returns a `Model` with non-empty `non_symmetric_trees` and empty `oblivious_trees`, and its predictions match the CPU `leaf_wise_grower`-grown model ≤1e-4 (asserts THIS plan's Task 2 fold arm fires under the widened gate — no degenerate `ObliviousTree`).
    - `nonsym_grow_test.rs` exists as a separate file (no `#[cfg(test)] mod tests` inside `nonsym_grow.rs` — `grep -n "mod tests" crates/cb-backend/src/kernels/nonsym_grow.rs` empty).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-backend nonsym_grow && cargo test -p cb-train device_nonsym_fit</automated>
  </verify>
  <done>Depthwise/Lossguide are device-covered behind a gate arm, self-oracled ≤1e-4 vs leaf_wise_grower AND end-to-end reproduced through cb-train::train() → non_symmetric_trees (Plan-01 fold branch fires, no degenerate ObliviousTree); Kaggle sign-off deferred to Plan 09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Numerical GPU kernel work only — NO network, auth, session, upload, or untrusted-input surface. The one boundary is the host↔device seam (internally-generated quantized bins/partition offsets/node diffs cross into `#[cube]` kernels).

| Boundary | Description |
|----------|-------------|
| host→device (seam) | Quantized bins + partition state cross into the non-sym grow kernel; node-diff encoding uses checked `u16` conversion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering/DoS | node-diff / leaf-id integer sizing (`u16`/`u32::MAX` sentinel) | mitigate | checked `u16::try_from` child diffs; `u32::MAX` interior sentinel (CR-01/CR-02); `check_depth`. |
| T-12-02 | Tampering (integrity) | device score/histogram SUM | mitigate | fixed-point `Atomic<u64>` k=30 deterministic reduction (holds ε=1e-4). |
| T-12-06 | DoS | `-inf` JIT-reject on HIP → kernel fails to build | mitigate | finite `f32::MIN`/`f32::MAX` sentinel in `#[cube]`; ROCm smoke build gate. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N-A — no external packages (RESEARCH Package Legitimacy Audit). |

Load-bearing gate is the ε=1e-4 device-vs-CPU oracle, NOT a security boundary.
</threat_model>

<verification>
- `cargo test -p cb-backend nonsym_grow` green (structure integer-exact, scores ≤1e-4).
- `cargo build -p cb-backend --features rocm` green (ROCm smoke after `#[cube]` change).
- `grep -rn "use cb_train" crates/cb-backend/src` adds nothing; no `-inf` literal in the new `#[cube]` kernel.
</verification>

<success_criteria>
- Device Depthwise + Lossguide grow non-symmetric trees matching `leaf_wise_grower` ≤1e-4 (structure integer-exact).
- Emission is host-structs-only via `DeviceGrownTree` → `TreeVariant::NonSymmetric` (no device-native tree type).
- Gate arm flips Depthwise/Lossguide `Ok(None)`→device; all else stays `Ok(None)`.
- End-to-end: a `cb-train::train()` device-committed Depthwise/Lossguide fit folds through the Plan-01 device-fold branch into `non_symmetric_trees` and reproduces the CPU-grown model ≤1e-4 (no degenerate `ObliviousTree`).
</success_criteria>

<output>
Create `.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-03-SUMMARY.md` when done.
</output>
