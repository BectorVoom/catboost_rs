---
phase: 12-grow-policy-leaf-method-sampling-categorical-device-coverage
plan: 06
type: execute
wave: 5
depends_on: [12-01]
files_modified:
  - crates/cb-backend/src/kernels/bootstrap_device.rs
  - crates/cb-backend/src/kernels/bootstrap_device_test.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
autonomous: true
requirements: [GPUT-09]
user_setup: []

must_haves:
  truths:
    - "Bootstrap (Bayesian/Bernoulli/Poisson) sample weights + random-strength score jitter are drawn ON device from a pinned seed, matching the frozen CPU bootstrap.rs sample bit-for-bit (leaves ≤1e-4)."
    - "The RNG keep-mask/weights stay device-resident (no per-tree host round-trip) over the resident derivatives."
    - "The device RNG stream reproduces the CPU TFastRng64 stream layout (continuous for Bernoulli/main; per-block reseed + base-2 log approx for Bayesian)."
  artifacts:
    - path: "crates/cb-backend/src/kernels/bootstrap_device.rs"
      provides: "device bootstrap weight draw + random-strength jitter (MWC/LCG matching random_gen.cuh)"
      contains: "bootstrap"
    - path: "crates/cb-backend/src/kernels/bootstrap_device_test.rs"
      provides: "serial self-oracle vs frozen CPU sample (pinned seed)"
      contains: "max_divergence"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "bootstrap_type / random_strength gate arm (map_bootstrap_kernel)"
      contains: "bootstrap"
  key_links:
    - from: "crates/cb-backend/src/kernels/bootstrap_device.rs"
      to: "crates/cb-backend/src/gpu_runtime/der_seams.rs"
      via: "RNG draw runs over the resident UN-weighted der1/der2 handles (no read-back)"
      pattern: "der"
  prohibitions:
    - "MUST NOT use the `rand` crate or an ad-hoc PRNG — reproduce the exact TFastRng64 seed-advance + per-block-reseed layout (Pitfall 5)."
    - "MUST NOT round-trip the sample mask to the host per tree — keep it device-resident (D-08)."
    - "MUST NOT add a cb-train dependency to cb-backend — transcribe bootstrap.rs semantics inline."
    - "MUST NOT flip bootstrap to a device result before its gate arm is covered — uncovered stays Ok(None)."
    - "MUST NOT use a non-deterministic device SUM (e.g. ComputeTargetVariance/ScoreStdDev) — fixed-point Atomic<u64> k=30."
---

<objective>
W4: bootstrap + random-strength sampling on device (GPUT-09) — small, high-return. The RNG-drawn keep-mask / sample weights (Bayesian/Bernoulli/Poisson `bootstrap_type`) and the `RandomStrength` score jitter are computed ON device each iteration from a pinned seed, keeping derivatives resident (D-07/D-08). Parity is deterministic and checkable at ε=1e-4 by pinning the seed and freezing the exact CPU sample (`cb-train/src/bootstrap.rs`) in the oracle fixture. The device RNG (MWC `AdvanceSeed` / LCG `AdvanceSeed32` + `GenerateSeeds`, `random*.cuh`) must reproduce the CPU `TFastRng64` stream layout.

Purpose: cover non-default `bootstrap_type` + random-strength on device without a per-tree host round-trip for the sample mask.
Output: `bootstrap_device` kernel + self-oracle + bootstrap gate arm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-CONTEXT.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-RESEARCH.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-PATTERNS.md
@crates/cb-train/src/bootstrap.rs
@crates/cb-backend/src/gpu_runtime/der_seams.rs
@crates/cb-backend/src/gpu_runtime/session.rs
@CATBOOST_CUDA_KERNELS_DESIGN.md
</context>

<artifacts_produced>
Full Phase-12 symbol set (✎ = delivered by THIS plan):
- Plan 01 foundation; Plans 02-04 grow policies; Plan 05 Exact + segmented sort
- ✎ `bootstrap_device` kernel + `bootstrap_device_test.rs` (cb-backend/src/kernels)
- ✎ bootstrap/random-strength gate arm (`map_bootstrap_kernel`) in session `begin`
- `mvs_device` (Plan 07); `ctr_device` (Plan 08); COVERAGE-MATRIX.md (Plan 09)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Device bootstrap weight draw + random-strength jitter (RNG parity)</name>
  <files>crates/cb-backend/src/kernels/bootstrap_device.rs</files>
  <read_first>
    - crates/cb-train/src/bootstrap.rs:64-200 (`EBootstrapType` :69, `MVS_BLOCK_SIZE` :64, `single_probability` :193, `bayesian_weight` :145 base-2 log APPROXIMATION ~1e-5-sensitive, `generate_random_weights` per-block reseed :156 `TFastRng64::from_seed(randSeed+blockIdx).advance(10)`, `set_sampled_control` :182) — the CPU stream is CONTINUOUS across iterations for Bernoulli/main
    - crates/cb-backend/src/gpu_runtime/der_seams.rs (resident UN-weighted der1/der2 handles the RNG draw runs over; keep resident)
    - CATBOOST_CUDA_KERNELS_DESIGN.md §6.1 `bootstrap.{cu,cuh}` + `random*.cuh` (MWC `AdvanceSeed` / LCG `AdvanceSeed32`, `GenerateSeeds` decorrelation, `NextUniformF`)
    - 12-PATTERNS.md "cb-backend/src/kernels/bootstrap_device.rs" (RNG stream landmine, Pitfall 5) + Pattern F (pin-seed + freeze) + Pattern C (deterministic reduce for ScoreStdDev)
    - crates/cb-core (`TFastRng64` reference to transcribe the device MWC/LCG kernel from — NO cb-train dep)
  </read_first>
  <action>
    Add `bootstrap_device.rs` implementing the device RNG draw over the resident derivatives from `DeviceTrainConfig.rng_seed`: transcribe `TFastRng64` seed-advance into a device MWC/LCG kernel matching `random_gen.cuh` (`AdvanceSeed`/`AdvanceSeed32`, `NextUniformF`). Implement each `EBootstrapType` — Bernoulli (continuous stream, keep-with-prob `sample_rate`), Bayesian (per-block reseed `from_seed(seed+blockIdx).advance(10)` + the base-2 log approximation weight, NOT exact `log2`), Poisson — producing a device-resident keep-mask / weight buffer WITHOUT a host round-trip (D-08). Implement `RandomStrength` score jitter (`ScoreStdDev`/`ComputeTargetVariance` via the deterministic fixed-point `Atomic<u64>` k=30 reduction, folded into the device score calcer). Finite sentinels in `#[cube]`; no `rand`; no `cb-train` dep.
  </action>
  <acceptance_criteria>
    - `bootstrap_device.rs` produces a device-resident keep-mask/weight buffer for Bernoulli/Bayesian/Poisson and a random-strength jitter term; `grep -n "use rand\|use cb_train" crates/cb-backend/src/kernels/bootstrap_device.rs` empty.
    - `ScoreStdDev`/variance SUM uses the deterministic reduction (no bare atomic-f64).
    - `cargo build -p cb-backend --features rocm` succeeds (ROCm smoke; no `-inf`/JIT reject).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cb-backend && cargo build -p cb-backend --features rocm</automated>
  </verify>
  <done>Device bootstrap + random-strength draw resident from a pinned seed reproducing the TFastRng64 layout; deterministic variance; no rand/cb-train.</done>
</task>

<task type="auto">
  <name>Task 2: Frozen-sample self-oracle + bootstrap session gate arm</name>
  <files>crates/cb-backend/src/kernels/bootstrap_device_test.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <read_first>
    - crates/cb-train/src/bootstrap.rs (the CPU sample to freeze) + crates/cb-train/src/bootstrap_test.rs shape; grow_loop.rs::max_divergence (:61); Pattern E + Pattern F
    - crates/cb-backend/src/gpu_runtime/session.rs (`map_score_fn` template for a `map_bootstrap_kernel` Option map; `DeviceTrainConfig.bootstrap_type`/`sample_rate`/`rng_seed` from Plan 01)
    - 12-RESEARCH.md D-07 (pin seed, freeze exact CPU sample, reproduce bit-for-bit)
  </read_first>
  <action>
    Add `bootstrap_device_test.rs` (sibling `#[path]` mount): for a pinned seed + `sample_rate`, freeze the exact CPU `bootstrap.rs` sample (keep-mask/weights) for Bernoulli/Bayesian/Poisson in the fixture, then assert the device draw reproduces it bit-for-bit (integer keep-mask `assert_eq!`; Bayesian weights ≤1e-4 given the log-approx) and that resulting leaf values are ≤1e-4 via `max_divergence`. In session.rs add a `map_bootstrap_kernel` gate arm: a covered `bootstrap_type` + pinned seed → route to `bootstrap_device`; else `Ok(None)`. Keep all-or-nothing. Kaggle CUDA ε=1e-4 sign-off deferred to Plan 09.
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-backend bootstrap_device` passes: device keep-mask == frozen CPU sample (bit-for-bit for Bernoulli/Poisson; Bayesian weights ≤1e-4), leaves ≤1e-4.
    - A covered `bootstrap_type` returns `Ok(Some(_))`; an uncovered one returns `Ok(None)`.
    - `bootstrap_device_test.rs` is a separate file (no in-source `mod tests`).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-backend bootstrap_device</automated>
  </verify>
  <done>Device bootstrap/random-strength is covered behind a gate arm, self-oracled bit-for-bit vs the frozen CPU sample; Kaggle sign-off deferred to Plan 09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Numerical GPU kernel work only — NO network, auth, session, upload, or untrusted-input surface. The RNG is a statistical PRNG for sampling (parity, not entropy — V6 N-A). Boundary is the host↔device seam (resident derivatives + pinned seed).

| Boundary | Description |
|----------|-------------|
| host→device (seam) | Pinned seed + sample_rate cross into the RNG kernel; keep-mask indices bounds-checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering/DoS | keep-mask index over resident derivatives | mitigate | bounds-check mask length == n; clamp; typed `CbError`. |
| T-12-02 | Tampering (integrity) | `ScoreStdDev`/variance SUM | mitigate | fixed-point `Atomic<u64>` k=30 deterministic reduction. |
| T-12-10 | Info (parity, not security) | RNG stream desync | mitigate | reproduce exact `TFastRng64` seed-advance; freeze CPU sample (D-07). PRNG is not security-sensitive (V6 N-A). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N-A — no external packages (RESEARCH Package Legitimacy Audit). |

Load-bearing gate is the ε=1e-4 device-vs-frozen-CPU-sample oracle, NOT a security boundary.
</threat_model>

<verification>
- `cargo test -p cb-backend bootstrap_device` green (bit-for-bit keep-mask, leaves ≤1e-4).
- `cargo build -p cb-backend --features rocm` green (ROCm smoke).
- `grep -rn "use rand\|use cb_train" crates/cb-backend/src/kernels/bootstrap_device.rs` empty.
</verification>

<success_criteria>
- Device bootstrap (Bernoulli/Bayesian/Poisson) + random-strength reproduce the frozen CPU sample; leaves ≤1e-4.
- Keep-mask/weights stay device-resident (no per-tree host round-trip).
- Bootstrap gate arm flips `Ok(None)`→device; all-or-nothing preserved.
</success_criteria>

<output>
Create `.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-06-SUMMARY.md` when done.
</output>
