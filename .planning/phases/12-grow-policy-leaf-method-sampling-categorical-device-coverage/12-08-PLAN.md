---
phase: 12-grow-policy-leaf-method-sampling-categorical-device-coverage
plan: 08
type: execute
wave: 7
depends_on: [12-01]
files_modified:
  - crates/cb-backend/src/kernels/ctr_device.rs
  - crates/cb-backend/src/kernels/ctr_device_test.rs
  - crates/cb-backend/src/kernels/cindex.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-compute/src/runtime.rs
autonomous: true
requirements: [GPUT-10]
user_setup: []

must_haves:
  truths:
    - "Ordered target-statistic CTRs (read-before-increment, segment-reset, group-wise correction), one-hot, AND tensor/feature-combination CTRs are computed ON device, resident across the permutation, matching the CPU cb-train/src/ctr online prefix within ε=1e-4."
    - "Accumulated CTRs are binarized into ADDITIONAL cindex columns on device (NOT host-computed-then-uploaded), consumed by the existing histogram loop."
    - "Device CTR is scoped to the single-permutation covered regime (Plain/fold_count==1); multi-fold CTR defers behind Ok(None) (Open Q3)."
  artifacts:
    - path: "crates/cb-backend/src/kernels/ctr_device.rs"
      provides: "device ordered/one-hot/tensor CTR accumulation (port of ctrs/kernel) resident across the permutation"
      contains: "ctr"
    - path: "crates/cb-backend/src/kernels/ctr_device_test.rs"
      provides: "serial self-oracle: device CTR columns == CPU online_ctr_prefix_binclf / calc_ctr ≤1e-4 (incl. tensor combos)"
      contains: "max_divergence"
    - path: "crates/cb-backend/src/kernels/cindex.rs"
      provides: "CTR→cindex binarization join (extra cindex columns on device)"
      contains: "cindex"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "CTR gate arm (single-permutation regime); multi-fold → Ok(None)"
      contains: "ctr"
  key_links:
    - from: "crates/cb-backend/src/kernels/ctr_device.rs"
      to: "crates/cb-backend/src/kernels/cindex.rs"
      via: "binarized CTR values written as additional cindex columns the histogram loop reads"
      pattern: "cindex"
  prohibitions:
    - "MUST NOT host-compute CTRs then upload — target-statistics accumulate ON device, resident across the permutation (D-06)."
    - "MUST NOT leak the target: ordered CTR reads the prefix statistic BEFORE incrementing (read-before-increment), resets at segment starts (Pitfall 3)."
    - "MUST NOT use a non-deterministic device SUM for CTR prefix sums — fixed-point Atomic<u64> k=30 (Pattern 3)."
    - "MUST NOT add a cb-train dependency to cb-backend — transcribe ctr/online.rs semantics inline."
    - "MUST NOT attempt multi-fold/multi-permutation CTR this wave — defer behind Ok(None) (Open Q3); MUST NOT flip CTR to a device result before its gate arm is covered."
---

<objective>
W6: CTR / permutation-dependent categorical features on device (GPUT-10) — the headline categorical use case and the HIGHEST-uncertainty family. Port the upstream device CTR computation (`ctrs/kernel/ctr_calcers.{cu,cuh}` + `batch_binarized_ctr_calcer.h`, §6.6) so ordered target-statistic CTRs accumulate ON device, resident across the permutation, and are binarized into additional cindex columns on device (D-05/D-06) — NOT host-computed-then-uploaded. Covers ordered TS, one-hot, AND tensor/feature-combination CTRs. Oracle: `cb-train/src/ctr/online.rs::online_ctr_prefix_binclf` (read-before-increment, object-order output) + `calc_ctr.rs`/`ctr_feature.rs` (combined projection).

Purpose: remove the per-fit CTR host round-trip for true upstream residency parity, scoped (Open Q3) to the single-permutation covered device regime; multi-fold CTR defers behind `Ok(None)`.
Output: `ctr_device` kernel + CTR→cindex join + self-oracle + CTR gate arm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-CONTEXT.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-RESEARCH.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-PATTERNS.md
@crates/cb-train/src/ctr/online.rs
@crates/cb-backend/src/kernels/cindex.rs
@crates/cb-backend/src/gpu_runtime/session.rs
@crates/cb-compute/src/runtime.rs
@CATBOOST_CUDA_KERNELS_DESIGN.md
</context>

<artifacts_produced>
Full Phase-12 symbol set (✎ = delivered by THIS plan):
- Plan 01 foundation; Plans 02-04 grow policies; Plan 05 Exact; Plan 06 bootstrap; Plan 07 MVS
- ✎ `ctr_device` kernel + `ctr_device_test.rs` (cb-backend/src/kernels)
- ✎ CTR→cindex binarization join (cb-backend/src/kernels/cindex.rs) + `DeviceCtrConfig` fill (cb-compute/src/runtime.rs)
- ✎ CTR gate arm (single-permutation regime) in session `begin`
- COVERAGE-MATRIX.md + per-family Kaggle sign-off (Plan 09)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Device ordered/one-hot/tensor CTR accumulation (port ctrs/kernel), resident across the permutation</name>
  <files>crates/cb-backend/src/kernels/ctr_device.rs, crates/cb-compute/src/runtime.rs</files>
  <read_first>
    - crates/cb-train/src/ctr/online.rs:59-361 (`online_ctr_prefix_binclf` :263 read-before-increment object-order output, `ordered_ctr_per_permutation` :361, `TCtrHistory`/`TCtrMeanHistory` :59/:95) + calc_ctr.rs + ctr_feature.rs (combined/tensor projection, A5) + ctr/mod.rs:67 (`ECtrType`) — the ≤1e-5 CPU oracle to transcribe inline (NO cb-train dep)
    - CATBOOST_CUDA_KERNELS_DESIGN.md §6.6 `ctrs/kernel/ctr_calcers.{cu,cuh}` + `batch_binarized_ctr_calcer.h` (segment-start sign bit `TIndexWrapper`, `UpdateBordersMask` = prior-flag OR `bins[i]!=bins[i-1]` OR previous-layer-bin-change; `MakeGroupStarts`/`FillBinIndices`/`ApplyGroupwiseCtrFix`)
    - 12-PATTERNS.md "cb-backend/src/kernels/ctr_device.rs" (Pitfall 3 ordered-CTR landmine) + Pattern C (deterministic prefix) + Pattern F (freeze CPU CTR column)
    - crates/cb-compute/src/runtime.rs (the `DeviceCtrConfig` placeholder from Plan 01 to fill: ctr types, priors, projections, border tables)
  </read_first>
  <action>
    Add `ctr_device.rs` porting `ctrs/kernel` to CubeCL: accumulate ordered target-statistic CTRs ON device, resident across the permutation — read the prefix statistic BEFORE incrementing (read-before-increment), reset at segment starts (encode segment starts as a head-flag mirroring `UpdateBordersMask`: prior-flag OR `bins[i]!=bins[i-1]` OR previous-layer-bin-change), and apply the group-wise correction (`MakeGroupStarts`/`FillBinIndices`/`ApplyGroupwiseCtrFix`). Cover ordered TS, one-hot, and tensor/feature-combination CTRs (the combined projection/hash pre-step per `ctr_feature.rs`; reuse the same device CTR math, A5). Every CTR prefix SUM uses the deterministic fixed-point `Atomic<u64>` k=30 reduction. Fill `DeviceCtrConfig` (cb-compute) with the ctr types/priors/projections/border tables (plain host struct; border tables uploaded once per fit — quantization stays CPU, ≤1e-5 ref). Finite sentinels in `#[cube]`; no `cb-train` dep.
  </action>
  <acceptance_criteria>
    - `ctr_device.rs` produces per-object ordered CTR values matching the read-before-increment object-order semantics; `grep -n "use cb_train" crates/cb-backend/src/kernels/ctr_device.rs` empty.
    - The first doc in each segment reads the PRIOR (not its own label); identical categories in different groups share the canonical CTR per category-within-group (assert in the test).
    - CTR prefix SUM uses the deterministic reduction; `cargo build -p cb-backend --features rocm` succeeds (ROCm smoke).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cb-backend && cargo build -p cb-backend --features rocm</automated>
  </verify>
  <done>Device ordered/one-hot/tensor CTR accumulation is resident across the permutation with read-before-increment + segment reset + group-wise fix, deterministic prefix, no cb-train dep.</done>
</task>

<task type="auto">
  <name>Task 2: CTR→cindex binarization join + self-oracle + CTR session gate arm (single-permutation)</name>
  <files>crates/cb-backend/src/kernels/cindex.rs, crates/cb-backend/src/kernels/ctr_device_test.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/cindex.rs (`pack_cindex` — the bit-packed device-resident cindex the histogram loop reads; extend with ADDITIONAL columns for binarized CTRs)
    - crates/cb-train/src/ctr/online.rs (the CPU CTR column to freeze) + ctr/online_test.rs shape; grow_loop.rs::max_divergence (:61); Pattern E + Pattern F
    - crates/cb-backend/src/gpu_runtime/session.rs (add a CTR gate arm; `DeviceTrainConfig.ctr` from Plan 01; single-permutation/Plain/fold_count==1 covered regime — Open Q3)
    - 12-RESEARCH.md Open Question 3 (single-permutation first; defer multi-fold behind Ok(None)) + A6 (categorical-heavy synthetic fixture may be needed)
  </read_first>
  <action>
    Extend `cindex.rs` to binarize the accumulated device CTR values into ADDITIONAL cindex columns (border tables uploaded once per fit) that the existing histogram loop consumes — on device, no host round-trip. Add `ctr_device_test.rs` (sibling `#[path]` mount): freeze the CPU `online_ctr_prefix_binclf`/`calc_ctr` CTR column (including a tensor/feature-combination case) for a pinned categorical fixture (add a categorical-heavy synthetic fixture if the Phase-10 generator lacks cat features, A6) and assert the device CTR columns reproduce it ≤1e-4 via `max_divergence`. In session.rs add a CTR gate arm: when `DeviceTrainConfig.ctr` is present AND the fit is the single-permutation covered regime (Plain/fold_count==1) → route to `ctr_device`; multi-fold/multi-permutation CTR → `Ok(None)` (Open Q3). Keep all-or-nothing. Kaggle CUDA ε=1e-4 sign-off deferred to Plan 09.
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-backend ctr_device` passes: device CTR cindex columns == frozen CPU CTR column ≤1e-4 for ordered TS, one-hot, AND a tensor/feature-combination case.
    - A multi-fold/multi-permutation CTR config returns `Ok(None)` (assert); a single-permutation covered CTR config returns `Ok(Some(_))`.
    - `ctr_device_test.rs` is a separate file (no in-source `mod tests`); no cb-train dep in cb-backend.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-backend ctr_device</automated>
  </verify>
  <done>Device CTRs binarize into extra cindex columns matching the frozen CPU CTR column ≤1e-4 (incl. tensor combos), single-permutation regime covered behind a gate arm; multi-fold deferred; Kaggle sign-off deferred to Plan 09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Numerical GPU kernel work only — NO network, auth, session, upload, or untrusted-input surface. Boundary is the host↔device seam (categorical bin indices + labels + CTR config cross into `#[cube]` kernels).

| Boundary | Description |
|----------|-------------|
| host→device (seam) | Categorical bin indices + CTR border tables cross into the CTR/cindex kernels; CTR bucket + cindex column indices bounds-checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Tampering/DoS | CTR bucket / extra-cindex-column index sizing | mitigate | bounds-check CTR bin index + column offsets before device indexing; checked arithmetic (V5). |
| T-12-02 | Tampering (integrity) | CTR prefix SUM | mitigate | fixed-point `Atomic<u64>` k=30 deterministic reduction. |
| T-12-13 | Info (integrity) | ordered-CTR target leakage | mitigate | read-before-increment + segment reset (Pitfall 3) — a correctness invariant, oracle-frozen. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N-A — no external packages (RESEARCH Package Legitimacy Audit). |

Load-bearing gate is the ε=1e-4 device-vs-CPU-CTR-column oracle, NOT a security boundary.
</threat_model>

<verification>
- `cargo test -p cb-backend ctr_device` green (device CTR columns ≤1e-4 vs frozen CPU column, incl. tensor combos).
- `cargo build -p cb-backend --features rocm` green (ROCm smoke).
- `grep -rn "use cb_train" crates/cb-backend/src/kernels/ctr_device.rs` empty; multi-fold CTR returns `Ok(None)`.
</verification>

<success_criteria>
- Device ordered/one-hot/tensor CTRs accumulate resident across the permutation and binarize into extra cindex columns matching the CPU CTR column ≤1e-4.
- Single-permutation covered regime flips `Ok(None)`→device; multi-fold CTR deferred behind `Ok(None)` (Open Q3).
- CPU/host path byte-unchanged; no cb-train dep in cb-backend.
</success_criteria>

<output>
Create `.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-08-SUMMARY.md` when done. Record the A5 (tensor-CTR kernel sharing) and A6 (cat-heavy fixture) findings and the Open-Q3 multi-fold deferral.
</output>
