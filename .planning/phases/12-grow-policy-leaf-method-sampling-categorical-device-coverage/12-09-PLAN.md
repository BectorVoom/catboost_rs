---
phase: 12-grow-policy-leaf-method-sampling-categorical-device-coverage
plan: 09
type: execute
wave: 8
depends_on: [12-03, 12-04, 12-05, 12-06, 12-07, 12-08]
files_modified:
  - .planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/COVERAGE-MATRIX.md
autonomous: false
requirements: [GPUT-18, GPUT-19, GPUT-09, GPUT-17, GPUT-10, BENCH-02]
user_setup:
  - service: kaggle
    why: "CUDA is the sole correctness+speed authority for this milestone; there is no NVIDIA GPU in-env. Each family's ε=1e-4 correctness sign-off + BENCH-02 speed measurement is a human-gated `--features cuda` notebook run reusing the Phase-10 harness."
    dashboard_config:
      - task: "Build the --features cuda wheel and run the Phase-10 Kaggle CUDA oracle+speed harness for each Phase-12 family fixture"
        location: "Kaggle CUDA notebook (Phase-10 bench/ harness)"

must_haves:
  truths:
    - "Each Phase-12 family that lands (Depthwise/Lossguide, Region, Exact, bootstrap, MVS, CTR) carries a recorded Kaggle CUDA ε=1e-4 correctness sign-off vs the Rust CPU path."
    - "Each landed family carries a recorded BENCH-02 Kaggle CUDA speed measurement (device vs host-CPU baseline; vs official CatBoost GPU where comparable; warm-run/JIT-excluded, train-only)."
    - "The GPU coverage matrix (per-family correctness + speed) is documented (SC-5); any family not yet signed off is recorded as Ok(None)→CPU fallback, not fabricated."
  artifacts:
    - path: ".planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/COVERAGE-MATRIX.md"
      provides: "SC-5 per-family GPU coverage matrix (correctness ε result + BENCH-02 speed + Ok(None) status)"
      contains: "Coverage Matrix"
  key_links:
    - from: ".planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/COVERAGE-MATRIX.md"
      to: ".planning/ROADMAP.md"
      via: "records which Phase-12 Success Criteria 1–5 are satisfied per family"
      pattern: "GPUT-1"
  prohibitions:
    - "MUST NOT fabricate Kaggle CUDA oracle or speed results (Phase 11-05 PAUSED precedent) — record only human-reported measurements; unmeasured families stay pending/Ok(None)."
    - "MUST NOT report a speed number for a family whose ε=1e-4 correctness sign-off has not passed first (correctness is the blocking gate)."
    - "MUST NOT flip any family's gate to device in a shipped state without its recorded Kaggle sign-off — pending families remain Ok(None)→CPU fallback."
---

<objective>
Discharge the standing per-family Kaggle CUDA authority (D-01/BENCH-02) and produce the SC-5 GPU coverage matrix. There is NO NVIDIA GPU in-env: correctness (ε=1e-4 vs the Rust CPU path) and speed (BENCH-02) for every Phase-12 family are human-gated `--features cuda` Kaggle notebook runs reusing the Phase-10 harness. This plan records, per family, the ε=1e-4 correctness result (blocking gate) and the BENCH-02 speed measurement (device vs host-CPU baseline; vs official CatBoost GPU where comparable), and documents the per-family coverage matrix. Any family not yet signed off stays `Ok(None)`→CPU fallback — never fabricated (Phase 11-05 PAUSED precedent).

Purpose: close SC-4 (per-family BENCH-02 as-it-lands) and SC-5 (documented coverage matrix) for the phase.
Output: `COVERAGE-MATRIX.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-CONTEXT.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_produced>
Full Phase-12 symbol set (✎ = delivered by THIS plan):
- Plans 01-08 deliver all device kernels + CPU Region path + gate arms + self-oracles + fixtures
- ✎ SC-5 `COVERAGE-MATRIX.md` (per-family correctness ε + BENCH-02 speed + Ok(None) status)
- ✎ recorded per-family Kaggle CUDA ε=1e-4 correctness sign-offs (human-reported)
- ✎ recorded per-family BENCH-02 speed measurements (human-reported)
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Kaggle CUDA ε=1e-4 correctness sign-off (per family)</name>
  <action>Human runs the Phase-10 Kaggle CUDA `--features cuda` oracle harness for each landed family's frozen fixture and records the per-family max abs/rel divergence vs the Rust CPU path (blocking gate before any speed number).</action>
  <what-built>Six device families landed in-env with CPU self-oracles (≤1e-4) + ROCm smoke: Depthwise/Lossguide (Plan 03), Region (Plan 04, over the Plan-02 CPU Region path), Exact weighted-quantile (Plan 05), bootstrap/random-strength (Plan 06), MVS (Plan 07), CTR (Plan 08). Each is gated behind its own `Ok(None)` arm and awaits the authoritative Kaggle CUDA correctness sign-off.</what-built>
  <how-to-verify>
    1. Build the `--features cuda` wheel and open the Phase-10 Kaggle CUDA oracle harness (`bench/`).
    2. For EACH landed family, run its frozen Phase-12 fixture through the device path and the Rust CPU path on the CUDA backend; record the max abs/rel divergence.
    3. Assert ε=1e-4 vs the Rust CPU path per family (Region also ≤1e-4 vs the Plan-02 frozen CPU Region path; the depth-1 sub-case stays ≤1e-5 where applicable).
    4. Report, per family: PASS (≤1e-4) or the observed divergence. Do NOT fabricate — a family not run stays "pending / Ok(None)".
  </how-to-verify>
  <resume-signal>Paste the per-family ε result table (family → max divergence → PASS/pending). Type "recorded" when done.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BENCH-02 Kaggle CUDA speed measurement (per family)</name>
  <action>Human times train-only (warm-run, JIT-excluded, queue drained) on the Kaggle CUDA notebook for each correctness-passing family, recording device path vs host-CPU baseline and vs official CatBoost GPU where comparable.</action>
  <what-built>The same six families, each requiring a BENCH-02 Kaggle CUDA speed measurement as it lands (SC-4). Speed is reported only for families whose ε=1e-4 correctness sign-off passed first (correctness is the blocking gate).</what-built>
  <how-to-verify>
    1. On the Kaggle CUDA notebook, for EACH family that PASSED correctness, time train-only (warm-run, JIT-excluded, drain the lazy CubeCL queue with a read-back before stopping the clock).
    2. Record device path vs the host-CPU baseline, and vs official CatBoost GPU where a comparable config exists.
    3. Use the Phase-10 synthetic generator (+ the Plan-08 categorical-heavy fixture for CTR). Do NOT fabricate timings — an unmeasured family stays "pending".
  </how-to-verify>
  <resume-signal>Paste the per-family speed table (family → device time → host-CPU baseline → CatBoost-GPU where comparable). Type "recorded" when done.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the SC-5 GPU coverage matrix</name>
  <files>.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/COVERAGE-MATRIX.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 12 Success Criteria 1–5 (the per-family correctness + speed targets)
    - .planning/phases/12-.../12-VALIDATION.md "Manual-Only Verifications" (the SC-5 coverage-matrix instruction)
    - The two human-verify checkpoints' recorded results (ε table + speed table) from Tasks 1–2
    - Each Plan 03-08 SUMMARY (which family landed, its in-env self-oracle status, its gate arm)
  </read_first>
  <action>
    Write `COVERAGE-MATRIX.md` with one row per family (Depthwise/Lossguide, Region, Exact, bootstrap, MVS, CTR) and columns: requirement ID (GPUT-18/19/09/17/10), CPU self-oracle status (in-env ≤1e-4), ROCm smoke status, Kaggle CUDA correctness (ε result from Task 1, or "pending / Ok(None)→CPU fallback"), BENCH-02 speed (from Task 2, or "pending"), and the gate state (device-covered vs Ok(None)). Explicitly mark any family without a Kaggle sign-off as `Ok(None)`→CPU fallback (not fabricated). Add a footer noting the CPU/host path stays byte-unchanged (GPUT-14/D-04) and that multi-fold CTR is deferred behind `Ok(None)` (Open Q3). Reference the recorded tables verbatim.
  </action>
  <acceptance_criteria>
    - `COVERAGE-MATRIX.md` exists with a per-family row for all six families, each carrying a requirement ID, a correctness cell (ε result or pending), a BENCH-02 speed cell (or pending), and a gate-state cell.
    - No family shows a correctness/speed number that was not in the human-reported tables (no fabrication).
    - The footer records D-04 no-regression + the multi-fold-CTR `Ok(None)` deferral.
  </acceptance_criteria>
  <verify>
    <human-check>The matrix accurately reflects the recorded Kaggle CUDA results; pending families are marked Ok(None), not fabricated.</human-check>
  </verify>
  <done>SC-5 coverage matrix documents per-family correctness + BENCH-02 speed + gate state; unsigned families are recorded as Ok(None)→CPU fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation + human-gated measurement plan only — NO code, network, auth, session, upload, or untrusted-input surface. No trust boundary crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Repudiation/Integrity | fabricated Kaggle results | mitigate | record only human-reported measurements; pending families marked Ok(None) (Phase 11-05 precedent). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N-A — no external packages (RESEARCH Package Legitimacy Audit). |

The load-bearing gate is the human-gated ε=1e-4 CUDA correctness sign-off (correctness before any speed number), NOT a security boundary.
</threat_model>

<verification>
- `COVERAGE-MATRIX.md` present with all six families, correctness + speed + gate cells.
- Correctness is a blocking gate before any speed number is recorded.
- Pending/unmeasured families explicitly marked `Ok(None)`→CPU fallback (no fabrication).
</verification>

<success_criteria>
- Every landed family carries a recorded Kaggle CUDA ε=1e-4 correctness sign-off (or is marked pending/Ok(None)).
- Every correctness-passing family carries a recorded BENCH-02 speed measurement.
- SC-5 GPU coverage matrix is documented; CPU/host path byte-unchanged (D-04).
</success_criteria>

<output>
Create `.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-09-SUMMARY.md` when done. Note which families are signed off vs pending Ok(None).
</output>
