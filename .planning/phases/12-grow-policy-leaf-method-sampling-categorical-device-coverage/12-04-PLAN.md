---
phase: 12-grow-policy-leaf-method-sampling-categorical-device-coverage
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - crates/cb-backend/src/kernels/region_device.rs
  - crates/cb-backend/src/kernels/region_device_test.rs
  - crates/cb-backend/src/gpu_runtime/session.rs
  - crates/cb-compute/src/runtime.rs
autonomous: true
requirements: [GPUT-18]
user_setup: []

must_haves:
  truths:
    - "A Region grow policy grows on device as a walk-until-diverge path (MaxLeaves = MaxDepth+1), matching the CPU Region path from Plan 02 within ε=1e-4."
    - "The device Region path emits a plain host RegionTree-shaped structure across the seam (not a node graph, not a device-native type)."
    - "The Region device path checks against the frozen ≤1e-5 CPU Region oracle from Plan 02."
  artifacts:
    - path: "crates/cb-backend/src/kernels/region_device.rs"
      provides: "device ComputeOptimalSplitsRegion structure search + region path apply/bins"
      contains: "region"
    - path: "crates/cb-backend/src/kernels/region_device_test.rs"
      provides: "serial self-oracle: device Region == CPU Region ≤1e-4"
      contains: "max_divergence"
    - path: "crates/cb-backend/src/gpu_runtime/session.rs"
      provides: "Region gate arm flipping Ok(None)→device"
      contains: "Region"
  key_links:
    - from: "crates/cb-backend/src/kernels/region_device.rs"
      to: "crates/cb-backend/src/kernels/nonsym_grow.rs"
      via: "reuses the Phase-11 scoring/partition spine; region-specific selection (ComputeOptimalSplitsRegion)"
      pattern: "find_optimal_split"
    - from: "crates/cb-backend/src/kernels/region_device.rs"
      to: "crates/cb-model/src/model.rs"
      via: "emits RegionTree-shaped host structs → TreeVariant::Region"
      pattern: "Region"
  prohibitions:
    - "MUST NOT begin the device Region kernel before the CPU Region oracle (Plan 02) exists — the ε=1e-4 gate is uncheckable without it (Pitfall 1)."
    - "MUST NOT model Region as a binary node graph — path only (d+1 leaves; 2^depth is the failure signal, Pitfall 2)."
    - "MUST NOT use a `-inf` sentinel in the `#[cube]` region scorer — finite f32::MIN/f32::MAX."
    - "MUST NOT add a cb-train/cb-model dependency to cb-backend — transcribe Region path semantics inline."
    - "MUST NOT flip Region to a device result before its gate arm is covered — uncovered stays Ok(None)."
---

<objective>
W2b: grow the Region grow policy on device (GPUT-18), checked against the CPU Region oracle Plan 02 established. Region is upstream `TRegionModel` — a path walked while the computed split matches the stored direction (`ComputeOptimalSplitsRegion`, `MaxLeaves = MaxDepth+1`, §6.4/§5.4); the apply is `AddRegionImpl`/`ComputeRegionBinsImpl` (walk-until-diverge; `takeEqualAndSplitDirection` packs one-hot in bit 0, expected direction in bit 1). The device searches the single Region path and emits `RegionTree`-shaped plain host structs across the seam.

Purpose: close the second half of the Region two-step lift (D-03), the single largest item in Phase 12, now that the CPU reference exists.
Output: `region_device` kernel + self-oracle + Region gate arm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-CONTEXT.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-RESEARCH.md
@.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-PATTERNS.md
@crates/cb-backend/src/kernels/nonsym_grow.rs
@crates/cb-backend/src/kernels/grow_loop.rs
@crates/cb-backend/src/gpu_runtime/session.rs
@crates/cb-compute/src/runtime.rs
@CATBOOST_CUDA_KERNELS_DESIGN.md
</context>

<artifacts_produced>
Full Phase-12 symbol set (✎ = delivered by THIS plan):
- Plan 01 foundation; Plan 02 CPU Region path + frozen oracle; Plan 03 `nonsym_grow`
- ✎ `region_device` kernel + `region_device_test.rs` (cb-backend/src/kernels)
- ✎ Region `map_grow_policy` gate arm in session `begin`
- ✎ Region-path host-struct emission across the seam (cb-compute/src/runtime.rs usage)
- Exact/bootstrap/MVS/CTR kernels (Plans 05-08); COVERAGE-MATRIX.md (Plan 09)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Device Region structure search + path emission</name>
  <files>crates/cb-backend/src/kernels/region_device.rs, crates/cb-compute/src/runtime.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/nonsym_grow.rs (Plan 03 — the reused scoring/partition spine; the finite-sentinel + deterministic-reduction discipline to copy) + grow_loop.rs::max_divergence (:61)
    - Plan 02 outputs: crates/cb-model/src/model.rs `RegionTree` shape (per-level `(feature,bin,expected_direction,one_hot)`, `leaf_values` len `depth+1`) and crates/cb-model/src/apply.rs region walk (transcribe the walk semantics inline into the kernel; NO cb-model dep)
    - CATBOOST_CUDA_KERNELS_DESIGN.md §6.4 `ComputeOptimalSplitsRegion`, §5.4 `SelectLeavesToSplit` (Region arm, `MaxLeaves = MaxDepth+1`), §6.6 `AddRegionImpl`/`ComputeRegionBinsImpl` + `takeEqualAndSplitDirection` bit-packing
    - 12-PATTERNS.md "cb-backend/src/kernels/region_device.rs" + Pitfall 2 (path not node graph) + Pitfall 1 (needs CPU oracle first)
    - crates/cb-compute/src/runtime.rs:917 (`DeviceGrownTree`; add/decide the Region-path host carrier — reuse a plain `Vec` triple or a dedicated `region_path` field, plain host structs only)
  </read_first>
  <action>
    Add `region_device.rs` implementing `ComputeOptimalSplitsRegion`: at each level, score the single frontier and take the best split (the Region extends ONE path), recording per-level `(feature, bin, expected_direction, one_hot)` and the `depth+1` leaf values — reusing the `nonsym_grow`/Phase-11 scoring+partition spine. Do NOT emit `step_nodes`/`node_id_to_leaf_id` (that is the node graph, Pitfall 2). Cross the seam as a plain host Region carrier on `DeviceGrownTree` (add a `region_path: Vec<(u32,u32,bool,bool)>` + confirm `leaf_values` len `depth+1`, or reuse existing fields per the seam owner's choice — plain host structs only, no cubecl type). Transcribe the region-walk direction test inline; `takeEqualAndSplitDirection` semantics: split via one-hot `==` or numeric `>`, break when `split != expected_direction`. Finite `f32::MIN`/`f32::MAX` sentinel in the `#[cube]` scorer; deterministic `Atomic<u64>` k=30 reduction for every SUM. No `cb-train`/`cb-model` dep.
  </action>
  <acceptance_criteria>
    - `region_device.rs` produces a Region carrier with exactly `depth+1` leaf values and a per-level `(feature,bin,expected_direction,one_hot)` path (assert leaf count `== depth+1`, never `2^depth`).
    - `grep -n "NEG_INFINITY\|-inf" crates/cb-backend/src/kernels/region_device.rs` empty inside `#[cube]`; finite sentinels present.
    - `grep -n "use cb_train\|use cb_model" crates/cb-backend/src/kernels/region_device.rs` empty.
    - `cargo build -p cb-backend --features rocm` succeeds (ROCm smoke).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cb-backend && cargo build -p cb-backend --features rocm</automated>
  </verify>
  <done>Device Region grows one d+1-leaf path as host structs, finite sentinels + deterministic reduction, no cb-train/cb-model dep.</done>
</task>

<task type="auto">
  <name>Task 2: Region self-oracle vs the frozen CPU Region reference + gate arm</name>
  <files>crates/cb-backend/src/kernels/region_device_test.rs, crates/cb-backend/src/gpu_runtime/session.rs</files>
  <read_first>
    - Plan 02 SUMMARY: the frozen ≤1e-5 CPU Region fixture path (the oracle to load) + crates/cb-train/src/region_grow_test.rs shape
    - crates/cb-backend/src/kernels/grow_loop.rs::max_divergence (:61); Pattern E (source/test separation, `#[path]` mount)
    - crates/cb-backend/src/gpu_runtime/session.rs (the `map_grow_policy` arm added in Plan 03 — extend with `Region`)
    - 12-PATTERNS.md Pattern A + Pattern F (frozen-fixture discipline)
  </read_first>
  <action>
    Add `region_device_test.rs` (sibling `#[path]` mount): run the SAME pinned fixture used by Plan 02's `region_grow_test.rs` through the device `region_device` path and assert path structure equality (per-level `(feature,bin,expected_direction,one_hot)` `assert_eq!`) + leaf-value max-abs divergence ≤1e-4 vs the frozen CPU Region reference via `max_divergence`. Extend the `map_grow_policy` gate arm in session.rs so `EGrowPolicy::Region` → covered (route to `region_device`); keep all-or-nothing. Document Kaggle CUDA ε=1e-4 sign-off deferred to Plan 09.
  </action>
  <acceptance_criteria>
    - `cargo test -p cb-backend region_device` passes: device Region path structure == frozen CPU Region path (exact) and leaf values ≤1e-4.
    - A `Region` config now returns `Ok(Some(_))` from `begin`; `SymmetricTree` etc. behavior unchanged.
    - `region_device_test.rs` is a separate file (`grep -n "mod tests" crates/cb-backend/src/kernels/region_device.rs` empty).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cb-backend region_device</automated>
  </verify>
  <done>Device Region is covered behind a gate arm, self-oracled ≤1e-4 vs the frozen CPU Region reference; Kaggle sign-off deferred to Plan 09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Numerical GPU kernel work only — NO network, auth, session, upload, or untrusted-input surface. Boundary is the host↔device seam (internally-generated quantized bins/partition state cross into the `#[cube]` region scorer).

| Boundary | Description |
|----------|-------------|
| host→device (seam) | Quantized bins cross into the region scorer; region-path indices validated before device indexing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering/DoS | region path index / leaf sizing (`MaxLeaves = MaxDepth+1`) | mitigate | bounded loop `0..depth`; checked leaf index; `check_depth`. |
| T-12-02 | Tampering (integrity) | device region-score SUM | mitigate | fixed-point `Atomic<u64>` k=30 deterministic reduction. |
| T-12-06 | DoS | `-inf` JIT-reject on HIP | mitigate | finite `f32::MIN`/`f32::MAX`; ROCm smoke gate. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N-A — no external packages (RESEARCH Package Legitimacy Audit). |

Load-bearing gate is the ε=1e-4 device-vs-CPU-Region oracle, NOT a security boundary.
</threat_model>

<verification>
- `cargo test -p cb-backend region_device` green (path structure exact, leaf values ≤1e-4 vs frozen CPU Region).
- `cargo build -p cb-backend --features rocm` green (ROCm smoke).
- No `-inf` literal in the new `#[cube]` kernel; no cb-train/cb-model dep in cb-backend.
</verification>

<success_criteria>
- Device Region grows a d+1-leaf path matching the frozen CPU Region reference ≤1e-4.
- Region emission is host-structs-only (path, not node graph, not device-native type).
- Region gate arm flips `Ok(None)`→device; all-or-nothing preserved.
</success_criteria>

<output>
Create `.planning/phases/12-grow-policy-leaf-method-sampling-categorical-device-coverage/12-04-SUMMARY.md` when done.
</output>
