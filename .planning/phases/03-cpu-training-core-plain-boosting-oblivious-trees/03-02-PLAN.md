---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 02
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - crates/cb-compute/src/leaf.rs
  - crates/cb-compute/src/leaf_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/leaf_methods_oracle_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
autonomous: true
requirements: [TRAIN-03]
user_setup: []

must_haves:
  truths:
    - "All four leaf-estimation methods (Gradient, Newton, Exact, Simple) produce leaf values matching upstream to <=1e-5"
    - "Newton leaf delta uses sumDer/(-sumDer2 + scaledL2)"
    - "Exact leaf delta uses the quantile-style exact optimization (CalcExactLeafDeltas)"
  artifacts:
    - path: "crates/cb-compute/src/leaf.rs"
      provides: "Newton + Exact + Simple leaf-delta math alongside Gradient"
      contains: "Newton"
    - path: "crates/cb-train/tests/leaf_methods_oracle_test.rs"
      provides: "Oracle lock for all four leaf methods"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "cb_compute::leaf (method dispatch by leaf_estimation_method)"
      via: "CalcMethodDelta dispatch"
      pattern: "leaf"
    - from: "crates/cb-train/tests/leaf_methods_oracle_test.rs"
      to: "fixtures/leaf_methods/{gradient,newton,exact,simple}/model.json"
      via: "compare_stage(Stage::LeafValues, …)"
      pattern: "compare_stage"
---

<objective>
Complete TRAIN-03 by adding the remaining three leaf-estimation methods — Newton, Exact, Simple — alongside the Gradient method shipped in the first slice (D-09: all four land together), each oracle-locked on per-tree leaf values.

Purpose: The first slice (Plan 01) proved the tree/leaf surface with Gradient estimation; this slice widens leaf estimation to the full four-method set the user chose to deliver up front, keeping each method on its own oracle scenario so a divergence is attributable to a single method.
Output: Newton/Exact/Simple leaf-delta math in `cb-compute::leaf`, method dispatch in the boosting loop, and a passing `leaf_methods_oracle` test covering all four methods.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-01-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-compute::leaf`** extended: `CalcDeltaNewtonBody(sumDer, sumDer2, l2, sumAllWeights, allDocCount)` = `sumDer / (-sumDer2 + scaledL2)`; `CalcExactLeafDeltas` (quantile-style exact optimization); the `Simple` method body (confirmed from `approx_calcer.cpp` — Open Q3 / A6); a `CalcMethodDelta` dispatch over `ELeavesEstimation::{Gradient, Newton, Exact, Simple}`.
- **`cb-train::boosting`** leaf-estimation dispatch keyed by the `leaf_estimation_method` param.
- **New oracle scenarios** (generated in Plan-00's generator, extended here): `leaf_methods/{gradient,newton,exact,simple}` — each a `model.json` with the corresponding `leaf_estimation_method` pinned, all other params at the first-slice simplified values.
- **Oracle test:** `leaf_methods_oracle_test` gating `Stage::LeafValues` for all four methods at ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Read and transcribe Exact + Simple leaf bodies; generate the four leaf-method oracle scenarios</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - catboost-master/catboost/private/libs/algo/approx_calcer.cpp (`CalcLeafDeltasSimple`, `CalcExactLeafDeltas` dispatch — lines ~482-525, 712-800, 734-744; resolve Open Q3 / A6: exact `Exact` body + how `Simple` differs from Gradient)
    - catboost-master/catboost/private/libs/algo_helpers/online_predictor.h (`CalcDeltaNewtonBody`, `CalcAverage`, `ScaleL2Reg` — lines 112-178)
    - crates/cb-oracle/generator/gen_fixtures.py (Plan-00 generator — extend with the four scenarios)
    - crates/cb-oracle/fixtures/regression_skeleton/ (scenario layout to mirror)
    - 03-RESEARCH.md § Open Questions Q3 + Assumption A6 (Simple/Exact uncertainty), Pitfall 5 (L2 scaling + iterations)
  </read_first>
  <action>
    First fully READ `CalcExactLeafDeltas` and the `Simple` leaf path in `approx_calcer.cpp` (resolving Open Q3 / A6 — what `Simple` and `Exact` compute exactly) and `CalcDeltaNewtonBody` in `online_predictor.h`; record the transcribed formulas in the generator/scenario doc comments for Task 2 to implement against. Then extend `gen_fixtures.py` to emit four scenarios `leaf_methods/{gradient,newton,exact,simple}`, each a `model.json` (+ optional `staged.npy`) with `leaf_estimation_method` set to the matching value and every other param at the first-slice simplified values (`bootstrap_type=No`, `random_strength=0`, fixed `l2_leaf_reg`/`depth`/`lr`/`iterations`, explicit `boost_from_average`, `leaf_estimation_iterations=1`, `score_function=L2`, fixed seed, `thread_count=1`). Commit the fixtures. Python-reachable oracle only (D-11) — no C++ build.
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/leaf_methods/gradient/model.json && test -f crates/cb-oracle/fixtures/leaf_methods/newton/model.json && test -f crates/cb-oracle/fixtures/leaf_methods/exact/model.json && test -f crates/cb-oracle/fixtures/leaf_methods/simple/model.json</automated>
  </verify>
  <acceptance_criteria>
    - all four `leaf_methods/<method>/model.json` fixtures exist (committed)
    - each scenario config pins the matching `leaf_estimation_method` and the first-slice simplified params (grep)
    - the transcribed Exact/Simple/Newton formulas are recorded for Task 2 (doc comment or scenario note)
  </acceptance_criteria>
  <done>The Exact/Simple/Newton leaf bodies are read and transcribed, and four committed oracle scenarios pin each leaf-estimation method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Newton/Exact/Simple leaf deltas + dispatch; oracle-lock all four methods</name>
  <files>crates/cb-compute/src/leaf.rs, crates/cb-compute/src/leaf_test.rs, crates/cb-train/src/boosting.rs, crates/cb-train/tests/leaf_methods_oracle_test.rs</files>
  <read_first>
    - crates/cb-compute/src/leaf.rs (Gradient + CalcAverage + ScaleL2Reg from Plan 01 — extend in place)
    - crates/cb-train/src/boosting.rs (the leaf-estimation call site from Plan 01 — add method dispatch)
    - crates/cb-train/tests/slice_first_oracle_test.rs (oracle-test shape from Plan 01 to mirror)
    - crates/cb-oracle/src/model_json.rs + compare.rs (`Stage::LeafValues`, `compare_stage`)
    - the transcribed formulas recorded in Task 1 + 03-RESEARCH.md § Code Examples
  </read_first>
  <behavior>
    - Newton: leaf delta == `sumDer / (-sumDer2 + ScaleL2Reg(l2, sumAllW, docCount))`
    - Exact: leaf delta == `CalcExactLeafDeltas` quantile-style result (per the Task-1 transcription)
    - Simple: leaf delta == the confirmed `Simple` body (A6)
    - all sums over leaf members route through `cb_core::sum_f64` (ordered); `count>0` guarded
  </behavior>
  <action>
    Extend `cb-compute::leaf` with `CalcDeltaNewtonBody` (`sumDer/(-sumDer2+scaledL2)`), `CalcExactLeafDeltas`, and the `Simple` body, plus a `CalcMethodDelta` dispatcher over the four `ELeavesEstimation` variants. Every reduction over leaf members goes through `cb_core::sum_f64`; guard `count>0`/degenerate denominators returning 0.0 or `CbError` (never panic/div-by-zero — deny-lints). In `boosting.rs`, dispatch the leaf-value step on the `leaf_estimation_method` param (default Gradient stays the first-slice path). Write `leaf_test.rs` unit tests FIRST for each method's formula on hand-computed der/weight inputs. Add `leaf_methods_oracle_test.rs` (integration, file-scope lint allow) that trains with each `leaf_estimation_method` and gates `compare_stage(Stage::LeafValues, …)` against the four Plan-fixtures at ≤1e-5.
  </action>
  <verify>
    <automated>cargo test -p cb-compute leaf::</automated>
    <automated>cargo test -p cb-train leaf_methods_oracle</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-compute/src/leaf.rs` contains Newton + Exact + Simple deltas and a four-way `CalcMethodDelta` dispatch
    - `cargo test -p cb-compute leaf::` exits 0 (each method's formula unit-tested)
    - `cargo test -p cb-train leaf_methods_oracle` exits 0 — LeafValues match for gradient/newton/exact/simple at ≤1e-5
    - no `unwrap()`/raw float `.sum()` in `leaf.rs` (deny-lints + D-08 grep pass)
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>All four leaf-estimation methods produce leaf values matching upstream to ≤1e-5, completing TRAIN-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leaf reduction over members | Host-side ordered sum over trusted in-memory der/weight arrays; no external/network surface (ASVS web categories N/A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | Newton/Exact denominator on degenerate leaf (`-sumDer2+scaledL2 == 0`, empty leaf) | mitigate | Guard denominator/`count>0`; return 0.0 or `CbError`, never div-by-zero/panic (deny-lints). |
| T-03-02-02 | Tampering (parity integrity) | leaf-member reduction order | mitigate | All sums via `cb_core::sum_f64` in canonical order (D-02/D-05); CI-grep raw-float-sum ban (D-08). |
</threat_model>

<verification>
- `cargo test -p cb-compute leaf::` green (four method formulas).
- `cargo test -p cb-train leaf_methods_oracle` green (LeafValues ≤1e-5 for all four).
- `cargo test --workspace` green.
- D-08 grep green.
</verification>

<success_criteria>
- Leaf estimation supports Gradient, Newton, Exact, and Simple; each passes its per-tree leaf-value oracle ≤1e-5 vs C++ (TRAIN-03 complete).
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-02-SUMMARY.md` when done
</output>
