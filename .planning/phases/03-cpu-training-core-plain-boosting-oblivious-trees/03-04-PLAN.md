---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 04
type: execute
wave: 5
depends_on: ["03-03"]
files_modified:
  - crates/cb-compute/src/score.rs
  - crates/cb-compute/src/score_test.rs
  - crates/cb-core/src/normal.rs
  - crates/cb-core/src/normal_test.rs
  - crates/cb-core/src/lib.rs
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/bootstrap.rs
  - crates/cb-train/tests/regularization_oracle_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
autonomous: true
requirements: [TRAIN-05]
user_setup: []

must_haves:
  truths:
    - "random_strength perturbs split scores via the exact Box-Muller/Marsaglia-polar normal draw over TFastRng64"
    - "bagging_temperature drives the Bayesian bootstrap weight exponent"
    - "l2_leaf_reg scaling (ScaleL2Reg) is applied in score and leaf math"
    - "A model trained with non-zero random_strength / bagging_temperature / varied l2_leaf_reg matches upstream end-to-end to <=1e-5"
  artifacts:
    - path: "crates/cb-core/src/normal.rs"
      provides: "StdNormalDistribution (Marsaglia polar) + GenRandReal1 over TFastRng64"
      contains: "normal"
    - path: "crates/cb-compute/src/score.rs"
      provides: "TRandomScore::GetInstance perturbation (random_strength * scoreStDev * normal)"
    - path: "crates/cb-train/tests/regularization_oracle_test.rs"
      provides: "Oracle lock for l2 / random_strength / bagging_temp"
  key_links:
    - from: "crates/cb-compute/src/score.rs"
      to: "cb_core::normal (StdNormalDistribution)"
      via: "per-candidate normal draw in GetInstance"
      pattern: "normal"
    - from: "crates/cb-core/src/normal.rs"
      to: "cb_core::TFastRng64 (GenRandReal1)"
      via: "Marsaglia polar rejection loop over uniform draws"
      pattern: "TFastRng64"
---

<objective>
Complete the regularization surface (TRAIN-05): full `random_strength` split-score perturbation via the exact Box-Muller/Marsaglia-polar normal draw over `TFastRng64`, `bagging_temperature` (the Bayesian bootstrap exponent), and `l2_leaf_reg` scaling across score and leaf math — each oracle-locked end-to-end.

Purpose: The first slice fixed `random_strength=0` (D-07) to eliminate all normal draws. This additive slice (D-10) turns the perturbation on. The normal-draw SEQUENCE is the parity landmine (Pitfall 3): a variable-length rejection loop consuming `GenRandReal1()` draws per candidate. Since the draws aren't Python-observable, the slice is locked end-to-end on splits/leaves on a tiny dataset; if it diverges and can't be localized, escalate to instrumentation (D-11, Open Q4).
Output: `cb-core::normal` (StdNormalDistribution + GenRandReal1), the `GetInstance` perturbation in `cb-compute::score`, `bagging_temperature` wired into bootstrap, and a passing `regularization_oracle` test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-01-PLAN.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-03-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-core::normal`** — `StdNormalDistribution` (Marsaglia polar / Box-Muller rejection loop: `do { x=2u-1; y=2u-1; r=x²+y² } while (r>1 || r<=0)`) + `gen_rand_real1` helper (`(gen_rand()>>11) * (1/9007199254740991.0)` for the ui64 engine) over `TFastRng64`. Ported, NOT `rand_distr` (Don't-Hand-Roll / Pitfall 3).
- **`cb-compute::score`** — `TRandomScore::GetInstance(rand)` adding `NormalDistribution(rand, 0, scoreStDev)` where `scoreStDev = random_strength * derivativesStDevFromZero * (NormalWithModelSizeDecrease ? mult : 1)` (default `random_score_type`).
- **`cb-train::tree`** — wire the per-candidate `GetInstance` perturbation into `SelectBestCandidate` (drawn per candidate in the same order, before the strict `>` compare — Pitfall 1 + 3 together).
- **`cb-train::bootstrap`** — `bagging_temperature` exponent already plumbed in Plan 03; vary it here under oracle.
- **New oracle scenarios:** `regularization/{l2,random_strength,bagging_temp}` (generator extended), each varying one knob, others at first-slice values.
- **Oracle test:** `regularization_oracle_test` gating `Stage::Splits` + `Stage::LeafValues` per scenario at ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port StdNormalDistribution + GenRandReal1 into cb-core</name>
  <files>crates/cb-core/src/normal.rs, crates/cb-core/src/normal_test.rs, crates/cb-core/src/lib.rs</files>
  <read_first>
    - crates/cb-core/src/rng.rs (`TFastRng64` `gen_rand` surface to build `gen_rand_real1` on)
    - crates/cb-core/src/lib.rs (module-tree + `pub use` + `#[cfg(test)] mod` shape to extend)
    - catboost-master/util/random/normal.h (`StdNormalDistribution` Marsaglia polar — lines 12-28)
    - catboost-master/util/random/common_ops.h (`GenRandReal1`/`ToRandReal1` — lines 19-21: `(GenRand()>>11) * (1/9007199254740991.0)`)
    - 03-RESEARCH.md § Pitfall 3 + § Don't Hand-Roll (port the exact rejection loop; no `rand_distr`)
  </read_first>
  <behavior>
    - `gen_rand_real1(&mut TFastRng64)` == `(gen_rand() >> 11) as f64 * (1.0/9007199254740991.0)`
    - `std_normal(&mut TFastRng64)` runs the Marsaglia polar rejection loop, consuming a VARIABLE number of `gen_rand_real1` draws, returning the normal sample
    - for a fixed seed, the (uniform-draw-count, normal-value) sequence matches a hand-computed reference
  </behavior>
  <action>
    Create `cb-core::normal` porting `StdNormalDistribution` from `util/random/normal.h` (Marsaglia polar rejection loop) and a `gen_rand_real1` helper from `common_ops.h` (`(gen_rand()>>11) * (1/9007199254740991.0)` for the ui64 engine), both over `TFastRng64`. Match the draw sequence EXACTLY — the rejection loop consumes a variable number of uniform draws per normal (Pitfall 3); do NOT substitute `rand_distr::Normal`. Register `mod normal;` + `#[cfg(test)] mod normal_test;` and `pub use` the entry point. Write `normal_test.rs` FIRST asserting `gen_rand_real1` and the normal sequence for a fixed seed match hand-computed references (including the variable draw count). No `unwrap`/panic; deny-lints clean.
  </action>
  <verify>
    <automated>cargo test -p cb-core normal</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-core/src/normal.rs` exports a Marsaglia-polar `std_normal` + `gen_rand_real1` over `TFastRng64`; no `rand_distr`/`rand` import
    - `cargo test -p cb-core normal` exits 0 (draw sequence + value match fixed-seed reference)
    - `gen_rand_real1` uses the `>>11` mantissa formula with `1/9007199254740991.0` (source assertion)
  </acceptance_criteria>
  <done>The exact CatBoost normal-draw sequence is reproducible from `TFastRng64`, ported bit-for-bit (no third-party sampler).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire random_strength perturbation + l2/bagging_temp scenarios; oracle-lock</name>
  <files>crates/cb-compute/src/score.rs, crates/cb-compute/src/score_test.rs, crates/cb-train/src/tree.rs, crates/cb-train/src/bootstrap.rs, crates/cb-train/tests/regularization_oracle_test.rs, crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - crates/cb-compute/src/score.rs (L2 score from Plan 01 — add the GetInstance perturbation)
    - crates/cb-core/src/normal.rs (Task 1 — the normal draw)
    - crates/cb-train/src/tree.rs (SelectBestCandidate strict `>` loop from Plan 01 — add per-candidate draw)
    - catboost-master/catboost/private/libs/algo/rand_score.h (`TRandomScore::GetInstance`)
    - catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp (CalcScoreStDev + per-candidate GetInstance order — lines 855-867, 948-966)
    - 03-RESEARCH.md § Pitfall 3 (scoreStDev formula, NormalWithModelSizeDecrease default), § Code Examples (tie-break + GetInstance)
  </read_first>
  <behavior>
    - GetInstance: `score + std_normal(rand) * scoreStDev` where `scoreStDev = random_strength * derivativesStDevFromZero * modelSizeMult`
    - perturbation drawn PER candidate in the exact upstream candidate-iteration order, BEFORE the strict `gain > bestGain` compare
    - random_strength=0 path draws nothing (first-slice behavior preserved)
  </behavior>
  <action>
    Extend `cb-compute::score` with `TRandomScore::GetInstance(rand)` adding `std_normal(rand) * scoreStDev` (Task-1 normal), `scoreStDev = random_strength * derivativesStDevFromZero * (NormalWithModelSizeDecrease ? mult : 1)` (default `random_score_type` — transcribe `CalcScoreStDev`). In `tree.rs::SelectBestCandidate`, draw the perturbation per candidate in the EXACT upstream order (feature index ascending, border ascending) before the strict `gain > bestGain` compare — Pitfalls 1 and 3 interact, so draw order AND tie-break must both match. Preserve the `random_strength=0` no-draw path. Confirm `l2_leaf_reg` `ScaleL2Reg` is applied in both score and leaf (already from Plan 01 — vary it under oracle here) and `bagging_temperature` flows to the Bayesian bootstrap weight (Plan 03). Extend `gen_fixtures.py` with `regularization/{l2,random_strength,bagging_temp}` scenarios (each varies one knob, others at first-slice simplified values, tiny dataset for localizability per Open Q4). Add `score_test.rs` unit test for GetInstance on a fixed-seed RNG, and `regularization_oracle_test.rs` gating `Stage::Splits` + `Stage::LeafValues` per scenario at ≤1e-5. If random_strength diverges and can't be localized at tree granularity, record the escalation-to-instrumentation note (D-11) in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p cb-compute score::</automated>
    <automated>cargo test -p cb-train regularization_oracle</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-compute/src/score.rs` contains a `GetInstance` perturbation using `cb_core::normal` and the `random_strength * stDev * mult` formula
    - the per-candidate draw happens in upstream candidate order before the strict `>` compare (source assertion in `tree.rs`)
    - all three `regularization/{l2,random_strength,bagging_temp}/model.json` fixtures exist
    - `cargo test -p cb-train regularization_oracle` exits 0 — Splits + LeafValues match upstream per scenario at ≤1e-5
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>`l2_leaf_reg`, `random_strength`, and `bagging_temperature` all reproduce upstream behavior end-to-end to ≤1e-5 (TRAIN-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `TFastRng64` → normal draw → score perturbation | Host-only deterministic non-crypto RNG (RESEARCH V6); used only for parity-reproducing perturbation, never secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure (parity integrity) | normal-draw order in `GetInstance` | mitigate | Port the exact Marsaglia-polar rejection loop + per-candidate draw order (Pitfall 3); fixed-seed unit test; end-to-end oracle. |
| T-03-04-02 | Denial of Service | rejection loop non-termination on degenerate uniform draws | mitigate | Loop condition matches upstream (`r>1 || r<=0`); `gen_rand_real1` is bounded in (0,1]; `TFastRng64` advances each iteration — bounded expected draws, no infinite loop. |
| T-03-04-03 | Tampering (parity integrity) | wrong split on equal gain WITH perturbation | mitigate | Strict `gain > bestGain` first-wins preserved; perturbation drawn before compare in upstream order. |
</threat_model>

<verification>
- `cargo test -p cb-core normal` green (draw sequence).
- `cargo test -p cb-compute score::` green (GetInstance).
- `cargo test -p cb-train regularization_oracle` green (Splits/LeafValues ≤1e-5).
- `cargo test --workspace` green; D-08 grep green; no `rand_distr` import.
</verification>

<success_criteria>
- Regularization (`l2_leaf_reg`, `random_strength`, `bagging_temperature`) reproduces upstream draws/scaling and passes its oracle ≤1e-5 (TRAIN-05).
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-04-SUMMARY.md` when done
</output>
