---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 05
type: execute
wave: 6
depends_on: ["03-04"]
files_modified:
  - crates/cb-train/src/lib.rs
  - crates/cb-train/src/overfit.rs
  - crates/cb-train/src/overfit_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/overfit_oracle_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
autonomous: true
requirements: [TRAIN-06]
user_setup: []

must_haves:
  truths:
    - "Overfitting detection supports IncToDec, Iter, and Wilcoxon detector types"
    - "od_pval (stop threshold) and od_wait (wait_iterations) control the stop decision"
    - "use_best_model selects the best iteration and truncates the model to it"
    - "Stop decision + best-iteration selection match upstream on a held-out eval set"
  artifacts:
    - path: "crates/cb-train/src/overfit.rs"
      provides: "IncToDec/Iter/Wilcoxon detectors + use_best_model state machine"
      contains: "IncToDec"
    - path: "crates/cb-train/tests/overfit_oracle_test.rs"
      provides: "Stop-decision + best-iter oracle lock"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/overfit.rs"
      via: "per-iteration AddError + IsNeedStop check; use_best_model truncation"
      pattern: "overfit"
---

<objective>
Add overfitting detection and early stopping (TRAIN-06): the IncToDec (default), Iter, and Wilcoxon detector types driven by `od_pval`/`od_wait`, plus `use_best_model` best-iteration selection and model truncation — wired into the boosting loop and oracle-locked on stop decision + best iteration.

Purpose: Additive slice (D-10) after regularization. The detector is a pure host state machine over eval-metric deltas; it needs a held-out eval set per iteration (the eval-metric logging in Plan 06 deepens this, but the stop decision is locked here).
Output: `cb-train::overfit` with three detector types + `use_best_model`, the loop's per-iteration `AddError`/`IsNeedStop` integration, and a passing `overfit_oracle` test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-01-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-train::overfit`** — detectors `IncToDec` (default, `wait_iterations=20`, `stop_pvalue=0` → inactive), `Iter` (== IncToDec with threshold forced to 1.0), `Wilcoxon` (`NStatistics::Wilcoxon` over deltas after the local max); `IsActive()` iff `Threshold>0`; `IsNeedStop()` iff `!IsEmpty && CurrentPValue < Threshold`; `AddError(metric)`; `use_best_model` best-iteration tracking + model truncation.
- **`cb-train::boosting`** — per-iteration: compute eval-set metric → `detector.AddError` → `if detector.IsNeedStop() break`; on completion, if `use_best_model`, truncate `oblivious_trees` to the best iteration.
- **New oracle scenarios:** `overfit/{wilcoxon,inctodec,iter,use_best_model}` (generator extended), each with the matching `od_type`/`od_pval`/`od_wait`/`use_best_model` + an eval set.
- **Oracle test:** `overfit_oracle_test` asserting the stop iteration (tree count) and `use_best_model` best-iteration match upstream.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generate overfit oracle scenarios with eval sets</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - catboost-master/catboost/libs/overfitting_detector/overfitting_detector.cpp (IncToDec/Iter/Wilcoxon — lines 127-174; defaults type=IncToDec, wait_iterations=20, stop_pvalue=0)
    - catboost-master/catboost/private/libs/options/overfitting_detector_options.* (od_type/od_pval/od_wait defaults)
    - crates/cb-oracle/generator/gen_fixtures.py (extend with four overfit scenarios + eval-set inputs)
    - 03-RESEARCH.md § Overfitting Detection (Code Examples), Pitfall context
  </read_first>
  <action>
    Extend `gen_fixtures.py` with `overfit/{wilcoxon,inctodec,iter,use_best_model}` scenarios. Each trains with an explicit eval set (`eval_set=(X_val, y_val)`) and pins: the matching `od_type` (IncToDec/Iter/Wilcoxon), `od_pval` and `od_wait` chosen so the detector actually fires within the iteration budget, and `use_best_model=True` for the use_best_model scenario; other params at first-slice simplified values. Persist for each scenario: the eval-set inputs (`.npy`), the upstream `model.json` (whose tree count reflects the stop iteration), the per-iteration eval metric (`staged.npy` on the eval set), and `model.tree_count_` / `get_best_iteration()` from the Python API. Commit. Python-reachable oracle only (D-11).
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/overfit/inctodec/model.json && test -f crates/cb-oracle/fixtures/overfit/iter/model.json && test -f crates/cb-oracle/fixtures/overfit/wilcoxon/model.json && test -f crates/cb-oracle/fixtures/overfit/use_best_model/model.json</automated>
  </verify>
  <acceptance_criteria>
    - all four `overfit/<type>/model.json` fixtures exist (committed) with an eval set
    - each scenario pins `od_type`/`od_pval`/`od_wait` (and `use_best_model` where applicable) so the detector fires within budget (grep)
    - the best-iteration / tree-count is persisted for assertion
  </acceptance_criteria>
  <done>Four committed overfit oracle scenarios pin each detector type + use_best_model with eval sets that trigger a stop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detectors + use_best_model; wire into the loop; oracle-lock</name>
  <files>crates/cb-train/src/lib.rs, crates/cb-train/src/overfit.rs, crates/cb-train/src/overfit_test.rs, crates/cb-train/src/boosting.rs, crates/cb-train/tests/overfit_oracle_test.rs</files>
  <read_first>
    - catboost-master/catboost/libs/overfitting_detector/overfitting_detector.cpp (IsActive/IsNeedStop/AddError; Wilcoxon over deltas after local max — lines 127-174)
    - crates/cb-train/src/boosting.rs (the iteration loop to wire AddError/IsNeedStop into)
    - crates/cb-data/src/borders.rs (closest stateful-loop analog)
    - crates/cb-oracle/src/compare.rs (`Stage::StagedApprox` / `Stage::Predictions` for eval-metric comparison)
    - 03-RESEARCH.md § Code Examples (overfitting detector), § Don't Hand-Roll (port Wilcoxon, no stats crate)
  </read_first>
  <behavior>
    - IncToDec: `IsNeedStop()` iff `!IsEmpty && CurrentPValue < Threshold`; inactive when `Threshold==0`
    - Iter: == IncToDec with threshold forced to 1.0 (stops `od_wait` iters after the best)
    - Wilcoxon: `NStatistics::Wilcoxon` over deltas after the local max
    - use_best_model: tracks the best eval-metric iteration and truncates the model's tree list to it
  </behavior>
  <action>
    Create `overfit.rs` as a pure host state machine porting `overfitting_detector.cpp`: `IncToDec`, `Iter` (IncToDec with threshold 1.0), and `Wilcoxon` (port `NStatistics::Wilcoxon` over post-local-max deltas — Don't-Hand-Roll, no stats crate). Implement `IsActive()` (`Threshold>0`), `AddError(metric)`, `IsNeedStop()` (`!IsEmpty && CurrentPValue < Threshold`), and `use_best_model` best-iteration tracking. Any metric folds route through `cb_core::sum_f64`; fallible paths return `CbError`, no panic. Wire into `boosting.rs`: per iteration compute the eval-set metric (via `R::compute_gradients`/loss on the eval set or a metric fn), `detector.AddError(metric)`, `if detector.IsNeedStop() break`; after the loop, if `use_best_model`, truncate `oblivious_trees` to the best iteration. Write `overfit_test.rs` FIRST: unit-test each detector's stop decision on a synthetic metric sequence (IncToDec fires, Iter stops `od_wait` after best, Wilcoxon p-value threshold). Add `overfit_oracle_test.rs` asserting the produced tree count == upstream stop iteration and the `use_best_model` best-iteration match the fixtures.
  </action>
  <verify>
    <automated>cargo test -p cb-train overfit::</automated>
    <automated>cargo test -p cb-train overfit_oracle</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-train/src/overfit.rs` implements IncToDec, Iter, and Wilcoxon detectors + `use_best_model` (no external stats crate import)
    - `cargo test -p cb-train overfit::` exits 0 (each detector's stop decision unit-tested)
    - `cargo test -p cb-train overfit_oracle` exits 0 — produced tree count == upstream stop iteration; best-iteration matches for use_best_model
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>Overfitting detection (Wilcoxon/IncToDec/Iter, `od_pval`/`od_wait`) and `use_best_model` behave correctly and match upstream stop/best-iteration decisions (TRAIN-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval-metric sequence → detector state machine | Pure host computation over trusted in-memory metrics; no external surface (ASVS web categories N/A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Denial of Service | detector on empty/degenerate metric history (`IsEmpty`, local-max edge) | mitigate | `IsNeedStop` guards `!IsEmpty`; deny-lints (`indexing_slicing`); `CbError` on degenerate input, never panic. |
| T-03-05-02 | Tampering (parity integrity) | Wilcoxon statistic / metric accumulation order | mitigate | Port `NStatistics::Wilcoxon` semantics exactly; metric folds via `cb_core::sum_f64` (D-08). |
| T-03-05-03 | Tampering | model truncation off-by-one (use_best_model) | mitigate | Best-iteration index matched against upstream `get_best_iteration()` in the oracle test. |
</threat_model>

<verification>
- `cargo test -p cb-train overfit::` green (detector unit tests).
- `cargo test -p cb-train overfit_oracle` green (stop iter + best iter match).
- `cargo test --workspace` green; D-08 grep green.
</verification>

<success_criteria>
- Overfitting detection / early stopping (Wilcoxon/IncToDec/Iter, `od_pval`/`od_wait`, `use_best_model`) behaves correctly (TRAIN-06).
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-05-SUMMARY.md` when done
</output>
