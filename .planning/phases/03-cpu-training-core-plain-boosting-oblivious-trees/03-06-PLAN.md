---
phase: 03-cpu-training-core-plain-boosting-oblivious-trees
plan: 06
type: execute
wave: 7
depends_on: ["03-05"]
files_modified:
  - crates/cb-train/src/lib.rs
  - crates/cb-train/src/metrics.rs
  - crates/cb-train/src/metrics_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/eval_metrics_oracle_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
autonomous: true
requirements: [TRAIN-07]
user_setup: []

must_haves:
  truths:
    - "Per-iteration eval-set metric values are computed and logged for one or more eval sets"
    - "eval_metric defaults to the objective and can be overridden"
    - "Per-iteration eval metric values match upstream to <=1e-5"
  artifacts:
    - path: "crates/cb-train/src/metrics.rs"
      provides: "RMSE + Logloss eval metric computation + per-iteration logging structure"
    - path: "crates/cb-train/tests/eval_metrics_oracle_test.rs"
      provides: "Per-iteration eval-metric oracle lock (multiple eval sets)"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/metrics.rs"
      via: "per-iteration metric eval over each eval set, fed to overfit detector"
      pattern: "metric"
    - from: "crates/cb-train/src/metrics.rs (Plan 06)"
      to: "crates/cb-train/src/boosting.rs inline eval-set metric STUB (Plan 05)"
      via: "Plan 06 REPLACES the Plan 05 minimal inline eval-set loss with cb-train::metrics (multiple eval sets + eval_metric override)"
      pattern: "replaces Plan 05 STUB"
---

<objective>
Add per-iteration eval-set metric logging (TRAIN-07): compute the `eval_metric` (defaulting to the objective) over one or more eval sets each iteration, log the per-iteration values, and feed them to the overfitting detector — oracle-locked on the per-iteration metric values.

Purpose: Additive slice (D-10) after overfitting detection. Plan 05 wired a MINIMAL INLINE eval-set metric (the stop-decision loss) into the boosting loop as an explicit STUB; this slice formalizes the metric set (multiple eval sets, `eval_metric` override) in `cb-train::metrics`, REPLACES the Plan 05 inline stub with it, and locks the per-iteration values against upstream's logged history.
Output: `cb-train::metrics` (RMSE + Logloss eval metrics + per-iteration logging), the loop integration replacing the Plan 05 stub, and a passing `eval_metrics_oracle` test over multiple eval sets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-CONTEXT.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-RESEARCH.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-PATTERNS.md
@.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-05-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- **`cb-train::metrics`** — RMSE and Logloss eval-metric computation over an eval set (objective-as-default `eval_metric`), with per-iteration value logging; metric accumulation routes through `cb_core::sum_f64`.
- **`cb-train::boosting`** — per iteration, compute the `eval_metric` over EACH eval set, log the values, and feed the primary eval set to the overfit detector. This REPLACES the Plan 05 minimal inline eval-set metric stub (Plan 05 integration generalized to N eval sets).
- **New oracle scenario:** `eval_metrics` (generator extended) — trains with ≥2 eval sets and an explicit `eval_metric`; persists per-iteration metric history per eval set.
- **Oracle test:** `eval_metrics_oracle_test` gating the per-iteration eval-metric values per eval set at ≤1e-5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generate eval_metrics oracle scenario with multiple eval sets</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py</files>
  <read_first>
    - crates/cb-oracle/generator/gen_fixtures.py (extend with the eval_metrics scenario)
    - crates/cb-oracle/fixtures/regression_skeleton/ (layout to mirror)
    - 03-RESEARCH.md § Eval-Set Metric Logging (eval_metric default = objective; multiple eval sets)
  </read_first>
  <action>
    Extend `gen_fixtures.py` with an `eval_metrics` scenario training with two eval sets (`eval_set=[(X1,y1),(X2,y2)]`) and an explicit `eval_metric` (e.g. RMSE for the regression scenario; add a Logloss variant). Persist each eval set's inputs (`.npy`) and the upstream per-iteration metric history (from `model.get_evals_result()` / `eval_metrics`) as committed `.npy`. Pin first-slice simplified params otherwise; fixed seed; `thread_count=1`. Commit. Python-reachable oracle only (D-11).
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/eval_metrics/model.json && ls crates/cb-oracle/fixtures/eval_metrics/*.npy</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-oracle/fixtures/eval_metrics/` contains `model.json`, ≥2 eval-set input `.npy`, and per-iteration metric-history `.npy` (committed)
    - the scenario pins an explicit `eval_metric` and ≥2 eval sets (grep)
  </acceptance_criteria>
  <done>An eval_metrics oracle scenario with multiple eval sets and committed per-iteration metric history exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement eval metrics + per-iteration logging (replacing the Plan 05 stub); oracle-lock</name>
  <files>crates/cb-train/src/lib.rs, crates/cb-train/src/metrics.rs, crates/cb-train/src/metrics_test.rs, crates/cb-train/src/boosting.rs, crates/cb-train/tests/eval_metrics_oracle_test.rs</files>
  <read_first>
    - crates/cb-compute/src/loss.rs (RMSE/Logloss elementwise — metrics build on the same math)
    - crates/cb-core/src/reduction.rs (`sum_f64` — metric accumulation routes through it)
    - crates/cb-train/src/boosting.rs (the loop + the Plan-05 INLINE eval-set metric STUB this task replaces — find the `STUB: ... superseded by cb-train::metrics in Plan 06` comment)
    - crates/cb-train/src/overfit.rs (Plan 05 — the detector the primary eval metric feeds)
    - crates/cb-oracle/src/compare.rs (`Stage::Predictions`/`StagedApprox` for metric-history comparison)
    - 03-RESEARCH.md § Eval-Set Metric Logging
  </read_first>
  <behavior>
    - RMSE eval metric == `sqrt(sum_f64(weighted (pred-target)^2) / sum_f64(weights))`
    - Logloss eval metric == upstream weighted logloss over raw-logit approx
    - eval_metric defaults to the objective when unset; per-iteration value logged per eval set
  </behavior>
  <action>
    Create `metrics.rs` computing RMSE and Logloss eval metrics over an eval set, building on `cb-compute::loss`; default `eval_metric` to the objective; accumulate via `cb_core::sum_f64` only. Generalize `boosting.rs`: REMOVE the Plan-05 minimal inline eval-set metric stub (the line(s) carrying the `STUB: ... superseded by cb-train::metrics in Plan 06` comment) and REPLACE it by evaluating the `eval_metric` over EACH eval set per iteration via `cb-train::metrics`, logging the per-iteration values into a metric-history structure, and feeding the primary eval set's metric to the overfit detector (Plan 05 `AddError`/`IsNeedStop` path unchanged — only the metric source changes). The detector's stop/best-iteration behavior MUST remain identical for the single-eval-set case (the Plan 05 `overfit_oracle` test stays green). No `unwrap`/panic; `CbError` on degenerate eval set. Write `metrics_test.rs` FIRST: unit-test RMSE/Logloss metric values on a hand-computed pred/target/weight set. Add `eval_metrics_oracle_test.rs` gating the per-iteration metric history per eval set against the Plan fixtures at ≤1e-5.
  </action>
  <verify>
    <automated>cargo test -p cb-train metrics::</automated>
    <automated>cargo test -p cb-train eval_metrics_oracle</automated>
    <automated>cargo test -p cb-train overfit_oracle</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-train/src/metrics.rs` computes RMSE + Logloss eval metrics with accumulation via `cb_core::sum_f64` (no raw float `.sum()`)
    - the Plan-05 `STUB: ... superseded by cb-train::metrics in Plan 06` inline metric is removed from `boosting.rs` (grep finds no remaining `STUB: ... superseded` marker)
    - `cargo test -p cb-train metrics::` exits 0 (metric values on hand-computed input)
    - `cargo test -p cb-train eval_metrics_oracle` exits 0 — per-iteration metric history matches upstream per eval set at ≤1e-5
    - `cargo test -p cb-train overfit_oracle` still exits 0 (the stop/best-iteration decision is unchanged after the stub→metrics swap)
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>Per-iteration eval-set metrics (multiple eval sets, `eval_metric`) are logged and match upstream to ≤1e-5 (TRAIN-07), replacing the Plan 05 inline stub without changing the overfit stop decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval-set predictions/targets → metric accumulation | Host computation over trusted in-memory eval data; no external surface (ASVS web categories N/A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Denial of Service | metric on empty eval set / zero total weight | mitigate | Guard `sum_f64(weights) > 0` before division; return `CbError`, never div-by-zero/panic (deny-lints). |
| T-03-06-02 | Tampering (parity integrity) | metric accumulation order | mitigate | All metric folds via `cb_core::sum_f64` in canonical order (D-08 grep). |
</threat_model>

<verification>
- `cargo test -p cb-train metrics::` green.
- `cargo test -p cb-train eval_metrics_oracle` green (≤1e-5 per eval set).
- `cargo test -p cb-train overfit_oracle` green (unchanged stop decision after stub→metrics swap).
- `cargo test --workspace` green; D-08 grep green.
</verification>

<success_criteria>
- Per-iteration eval-set metric logging (multiple eval sets, `eval_metric`) behaves correctly and matches upstream ≤1e-5 (TRAIN-07), replacing the Plan 05 inline metric stub.
</success_criteria>

<output>
Create `.planning/phases/03-cpu-training-core-plain-boosting-oblivious-trees/03-06-SUMMARY.md` when done
</output>
