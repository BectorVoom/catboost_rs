---
phase: 08-python-bindings-dual-api-packaging
plan: 07
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - crates/catboost-rs-py/pyproject.toml
  - crates/catboost-rs-py/pyproject-rocm.toml
  - crates/catboost-rs-py/PACKAGING.md
  - .github/workflows/python-wheels.yml
autonomous: false
requirements: [PYAPI-01]
tags: [maturin, packaging, wheels, rocm]

must_haves:
  truths:
    - "An abi3-py312 cpu wheel builds and installs, and `import catboost_rs` works on Python 3.12 and 3.13 (GIL)"
    - "A rocm wheel builds in-env (HIP/gfx1100) under a separately-named catboost-rs-rocm distribution, both importing as catboost_rs"
    - "The two-distribution layout is documented: catboost-rs (cpu) + catboost-rs-rocm (rocm), catboost-rs[rocm] depends on catboost-rs-rocm, mutually exclusive"
    - "CI builds the cpu/abi3 wheel only; the rocm wheel build is in-env (never GitHub Actions)"
  artifacts:
    - path: "crates/catboost-rs-py/pyproject.toml"
      provides: "cpu distribution: name=catboost-rs, abi3-py312, module-name=catboost_rs, [rocm] extra -> catboost-rs-rocm"
      contains: "catboost-rs"
    - path: "crates/catboost-rs-py/pyproject-rocm.toml"
      provides: "rocm distribution: name=catboost-rs-rocm, module-name=catboost_rs, rocm features"
      contains: "catboost-rs-rocm"
    - path: "crates/catboost-rs-py/PACKAGING.md"
      provides: "the D-08/D-09 two-distribution realization + build/publish split"
      contains: "two distribution"
    - path: ".github/workflows/python-wheels.yml"
      provides: "CI cpu/abi3 wheel build only (no rocm in Actions)"
      contains: "maturin"
  key_links:
    - from: "crates/catboost-rs-py/pyproject.toml"
      to: "catboost-rs-rocm"
      via: "[project.optional-dependencies] rocm extra"
      pattern: "rocm"
    - from: "crates/catboost-rs-py/pyproject-rocm.toml"
      to: "maturin --no-default-features --features rocm"
      via: "in-env HIP build"
      pattern: "rocm"
---

<objective>
Realize PYAPI-01 packaging: an abi3-py312 CPU wheel as the primary deliverable (one wheel covers 3.12/3.13/3.14 GIL builds) plus the in-env ROCm wheel, under the two-distribution layout the research pinned for D-08 (`catboost-rs` cpu + `catboost-rs-rocm` rocm, both importing as `catboost_rs`, `catboost-rs[rocm]` depending on the rocm distribution). CI builds only the cpu/abi3 wheel; the rocm wheel is built in-env where the HIP/gfx1100 toolchain lives (never GitHub Actions — D-06 carried from Phase 7).

This is the distribution slice: the binding becomes installable.

Purpose: PYAPI-01 per-backend wheels (cpu+rocm minimum), abi3-py312, Python >=3.12, under the D-08/D-09 distribution model.
Output: cpu + rocm pyproject configs, `PACKAGING.md`, and a CI wheel workflow (cpu only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-python-bindings-dual-api-packaging/08-RESEARCH.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-01-SUMMARY.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: abi3-py312 cpu wheel build + import smoke + CI workflow</name>
  <files>crates/catboost-rs-py/pyproject.toml, crates/catboost-rs-py/PACKAGING.md, .github/workflows/python-wheels.yml</files>
  <read_first>
    - crates/catboost-rs-py/pyproject.toml (the 08-01 scaffold to finalize: name=catboost-rs, module-name=catboost_rs, requires-python>=3.12, [rocm] extra)
    - RESEARCH lines 125-141 (pyproject.toml stack), 346-350 (Pitfall 1 abi3 cpu wheel is the primary artifact), 364-368 (Pitfall 4 two-distribution, document mutual exclusivity), 492-495 (Open Q4: build cpu wheel in CI, rocm in-env)
    - 08-01-SUMMARY (the A2/A3 packaging-scope decision outcome that locked this layout)
  </read_first>
  <behavior>
    - Test: `maturin build --features cpu --release` produces an `*abi3*` wheel under `target/wheels/`.
    - Test: `pip install` that wheel into a fresh venv and `python -c "import catboost_rs; catboost_rs.CatBoostRegressor"` succeeds (on the 3.12 build venv; the abi3 tag covers 3.13 too).
    - Doc-grep: PACKAGING.md documents the two-distribution model and the mutual-exclusivity warning.
  </behavior>
  <action>Finalize `pyproject.toml` for the cpu distribution per RESEARCH 125-141: `[project] name="catboost-rs"`, `requires-python=">=3.12"`, `[project.optional-dependencies] rocm=["catboost-rs-rocm"]`, `[tool.maturin] module-name="catboost_rs"`, `features=["pyo3/extension-module"]`, ensure the abi3-py312 feature is active (via the Cargo `pyo3` `abi3-py312` feature from 08-01). Write `PACKAGING.md`: the D-08 two-distribution realization (`catboost-rs` cpu default + `catboost-rs-rocm` rocm; `catboost-rs[rocm]` extra depends on the rocm dist; both expose `catboost_rs`; NEVER install both simultaneously — Pitfall 4), the abi3-cpu-primary + free-threaded-wheel-deferred rationale (cross-link FREE_THREADING.md from 08-06), and the build/publish split (cpu/abi3 in CI, rocm in-env). Add `.github/workflows/python-wheels.yml` building ONLY the cpu/abi3 wheel via `maturin build --features cpu --release` on Python 3.12 (linux at minimum), with an import smoke step; add a comment that the rocm wheel is NEVER built in Actions (in-env only, per Phase-7 D-06). Use grep tokens: "two distribution", "catboost-rs-rocm", "mutually exclusive".</action>
  <verify>
    <automated>cd crates/catboost-rs-py && ../../.venv-py8/bin/maturin build --features cpu --release 2>&1 | tail -5 && ls ../../target/wheels/*abi3*.whl 2>/dev/null | grep -c abi3 && grep -v '^#' PACKAGING.md 2>/dev/null | grep -c -e "two distribution" -e "catboost-rs-rocm" -e "mutually exclusive"</automated>
  </verify>
  <done>The abi3-py312 cpu wheel builds; a fresh-venv install imports `catboost_rs` and exposes `CatBoostRegressor`; PACKAGING.md documents the two-distribution model + mutual exclusivity; the CI workflow builds the cpu wheel only.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Build the rocm wheel in-env (HIP/gfx1100)</name>
  <files>crates/catboost-rs-py/pyproject-rocm.toml</files>
  <read_first>
    - crates/catboost-rs-py/Cargo.toml (the `rocm` feature passthrough from 08-01 → facade → cb-backend `cubecl/hip`)
    - RESEARCH lines 60, 364-368 (D-08 realization: `catboost-rs-rocm` is a separate distribution, name differs, module-name stays `catboost_rs`), 492-495, 508 (rocm wheel built in-env where HIP/gfx1100/ROCm 7.1 lives; never in CI)
    - Memory: "rocm tests never in GitHub Actions"; "removed stale rocm CI step" — keep rocm out of Actions
  </read_first>
  <what-built>The cpu `pyproject.toml` + the rocm-distribution config. Claude creates `pyproject-rocm.toml`: `[project] name="catboost-rs-rocm"`, `requires-python=">=3.12"`, `[tool.maturin] module-name="catboost_rs"`, the rocm build invoked as `maturin build --manifest-path crates/catboost-rs-py/Cargo.toml --no-default-features --features rocm --release` (cpu-free per the 08-01 feature wiring). Claude CANNOT run the HIP-linked build to completion validation because it requires the in-env gfx1100/ROCm toolchain and a live import smoke on the GPU runtime.</what-built>
  <how-to-verify>
    In-env (gfx1100, ROCm 7.1):
    1. `cd crates/catboost-rs-py`
    2. `maturin build --no-default-features --features rocm --release` — the rocm wheel builds and links against HIP (compile/link only; no live GPU needed at build time per RESEARCH A4).
    3. In a fresh venv: `pip install target/wheels/catboost_rs_rocm-*.whl` then `python -c "import catboost_rs; m=catboost_rs.CatBoostRegressor(iterations=5); ..."` and run a smoke fit/predict on the GPU runtime.
    4. Confirm a cpu wheel and the rocm wheel are NOT installed in the same venv (mutual exclusivity).
  </how-to-verify>
  <action>Claude creates `crates/catboost-rs-py/pyproject-rocm.toml` (`[project] name="catboost-rs-rocm"`, `requires-python=">=3.12"`, `[tool.maturin] module-name="catboost_rs"`) and documents the exact in-env build command (`maturin build --no-default-features --features rocm --release`). The HIP-linked build + GPU import smoke is the human-action verification step because it requires the in-env gfx1100/ROCm 7.1 toolchain Claude cannot drive.</action>
  <resume-signal>Type "approved" with the rocm wheel filename + import-smoke result, or describe the build/import failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI / install → user environment | A wheel is installed and loaded into the user's interpreter |
| Build toolchain → wheel artifact | The compiled `.so` is produced by maturin under a backend feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Tampering | feature unification leaks cpu into the rocm wheel | mitigate | `maturin build --no-default-features --features rocm` (08-01 forwarding makes this cpu-free); verified by the rocm in-env build + the 08-01 `cargo tree` gate |
| T-08-22 | Spoofing/confusion | both wheels installed simultaneously under the same import name → undefined `.so` resolution | mitigate | PACKAGING.md documents mutual exclusivity (Pitfall 4); the `[rocm]` extra depends on the separate distribution, never co-installed |
| T-08-23 | Tampering | rocm build in untrusted CI exposes HIP toolchain | accept | rocm wheel built in-env only (never GitHub Actions, D-06); CI builds the cpu/abi3 wheel exclusively |
| T-08-SC | Tampering | maturin install | mitigate | RESEARCH Package Legitimacy Audit: maturin is the official PyO3-org tool, verdict OK |
</threat_model>

<verification>
- `maturin build --features cpu --release` produces an abi3 wheel; fresh-venv install imports `catboost_rs`.
- PACKAGING.md grep gate passes (two-distribution + mutual exclusivity).
- CI workflow builds cpu only.
- (in-env, human checkpoint) rocm wheel builds + imports `catboost_rs` + smoke fit/predict.
</verification>

<success_criteria>
- abi3-py312 cpu wheel builds and imports on 3.12/3.13 (GIL).
- rocm wheel builds in-env under `catboost-rs-rocm`, importing as `catboost_rs`.
- Two-distribution layout + mutual exclusivity documented; CI builds cpu only, rocm in-env.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings-dual-api-packaging/08-07-SUMMARY.md` when done. Record the cpu wheel filename/tag, the rocm in-env build result, and confirm the rocm-never-in-CI split.
</output>
