---
phase: 08-python-bindings-dual-api-packaging
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - crates/catboost-rs-py/src/estimator.rs
  - crates/catboost-rs-py/src/classifier.rs
  - crates/catboost-rs-py/src/regressor.rs
  - crates/catboost-rs-py/src/ranker.rs
  - crates/catboost-rs-py/src/estimator_test.rs
  - crates/catboost-rs-py/tests/test_check_estimator.py
autonomous: true
requirements: [PYAPI-02]
tags: [pyo3, sklearn, check-estimator]

must_haves:
  truths:
    - "A user can clone() a catboost-rs estimator and use it inside an sklearn Pipeline / GridSearchCV"
    - "get_params/set_params round-trip exactly (every __init__ kwarg is a readable param)"
    - "__init__ does no work or mutation beyond storing params (check_estimator no-work-in-init passes)"
    - "Calling predict before fit raises NotFittedError"
    - "check_estimator's structural checks pass; the dtype/contiguity checks are an explicit, documented, enumerated skip allowlist (D-04)"
  artifacts:
    - path: "crates/catboost-rs-py/src/estimator.rs"
      provides: "get_params/set_params/__sklearn_tags__/clone-support/NotFitted state shared across clf/reg/ranker"
      contains: "get_params"
    - path: "crates/catboost-rs-py/tests/test_check_estimator.py"
      provides: "parametrize_with_checks gate + the enumerated documented-skip allowlist (D-04)"
      contains: "check_estimator"
  key_links:
    - from: "crates/catboost-rs-py/src/estimator.rs"
      to: "sklearn clone via get_params/set_params"
      via: "verbatim params round-trip"
      pattern: "get_params|set_params"
    - from: "crates/catboost-rs-py/src/estimator.rs"
      to: "__sklearn_tags__ Tags"
      via: "estimator_type per class"
      pattern: "sklearn_tags"
---

<objective>
Make the three estimators pass scikit-learn's structural estimator contract (PYAPI-02). Implement `get_params`/`set_params` (exact round-trip over the verbatim param store from 08-01/08-02), `__sklearn_tags__` (the sklearn ≥1.6 `Tags` dataclass), clone-ability, and `NotFittedError` before fit — all in Rust on the `#[pyclass]` types (D-02). Add the `check_estimator` test gate with an explicit, enumerated, justified documented-skip allowlist for the dtype/contiguity checks (D-04/D-13), since D-12 makes input strictly float32+contiguous.

This is the contract-compliance slice: the CatBoost-native classes become drop-in sklearn estimators usable in Pipeline/GridSearchCV.

Purpose: PYAPI-02 structural `check_estimator` pass (D-03) with the documented-skip allowlist (D-04).
Output: sklearn-contract methods on the shared base, `test_check_estimator.py` with the allowlist, `estimator_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-python-bindings-dual-api-packaging/08-RESEARCH.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-PATTERNS.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sklearn contract methods — get/set_params, __sklearn_tags__, clone, NotFitted</name>
  <files>crates/catboost-rs-py/src/estimator.rs, crates/catboost-rs-py/src/classifier.rs, crates/catboost-rs-py/src/regressor.rs, crates/catboost-rs-py/src/ranker.rs, crates/catboost-rs-py/src/estimator_test.rs</files>
  <read_first>
    - crates/catboost-rs-py/src/estimator.rs (the shared base from 08-04 — extend it)
    - RESEARCH Pattern 1 lines 236-271 (get_params/set_params verbatim round-trip), Pitfall 2 lines 352-356 (round-trip must not rename/coerce), Pitfall 5 lines 370-374 (`__sklearn_tags__` returns a `Tags` dataclass for sklearn ≥1.6)
    - RESEARCH lines 483-487 (Open Question 2: Ranker→sklearn tag presentation — present CatBoostRanker with a regressor-like tag set OR exclude it from the check_estimator gate with documented justification; DECIDE here)
    - RESEARCH lines 471 (A5: NotFittedError subclassing PyValueError satisfies the not-fitted check) + 08-02-SUMMARY (the NotFittedError type already created)
  </read_first>
  <behavior>
    - Test (estimator_test.rs / pytest): `est.get_params()` returns exactly the kwargs passed to `__init__`; `est.set_params(**est.get_params())` is a no-op round-trip; `sklearn.base.clone(est)` produces an equal-params unfitted estimator.
    - Test: `est.__sklearn_tags__()` returns a `Tags` instance with the correct `estimator_type` (classifier for CatBoostClassifier, regressor for CatBoostRegressor; ranker per the decided presentation).
    - Test: `CatBoostRegressor().predict(X)` before fit raises `NotFittedError` (which is a `ValueError` subclass).
    - Test: `__init__` does no work — constructing with arbitrary stored kwargs never validates or fits (validation is at fit, D-06).
  </behavior>
  <action>On the shared base in `estimator.rs`: implement `get_params(&self, deep=None) -> Py<PyDict>` returning the verbatim params store; `set_params(&mut self, **kwargs) -> Self`/`PyResult<()>` merging kwargs into the store (return self for chaining per sklearn); ensure every `__init__` keyword is reflected in `get_params` (sklearn round-trip invariant). Implement `__sklearn_tags__` per class: import/construct `sklearn.utils.Tags` via PyO3 (call into Python to build the dataclass) setting `estimator_type="classifier"|"regressor"`; for the Ranker, DECIDE per RESEARCH Open Q2 — present a regressor-like tag set (continuous score) and document it (record the decision in the SUMMARY). Confirm `predict`/`predict_proba`/`score` raise `NotFittedError` (the 08-02 type, a `ValueError` subclass) when `model` is None. Add `score(X, y)` (R² for regressor, accuracy for classifier) to satisfy the sklearn method set. Ensure clone works: since clone uses `get_params`→`__init__(**params)`, and `__init__` stores verbatim, the round-trip is automatic — add a test asserting it. Add `estimator_test.rs` for the Rust-visible round-trip helpers.</action>
  <verify>
    <automated>cd crates/catboost-rs-py && ../../.venv-py8/bin/maturin develop --features cpu 2>&1 | tail -2 && ../../.venv-py8/bin/python -c "import catboost_rs,sklearn.base as b; e=catboost_rs.CatBoostRegressor(iterations=5); assert b.clone(e); assert e.get_params()=={'iterations':5} or 'iterations' in e.get_params(); print('ok')" 2>&1 | tail -5 && cargo test -p catboost-rs-py estimator 2>&1 | tail -5</automated>
  </verify>
  <done>get_params/set_params round-trip and clone work; __sklearn_tags__ returns a correct Tags per class; predict-before-fit raises NotFittedError; __init__ does no work.</done>
</task>

<task type="auto">
  <name>Task 2: check_estimator gate + enumerated documented-skip allowlist (D-04)</name>
  <files>crates/catboost-rs-py/tests/test_check_estimator.py</files>
  <read_first>
    - RESEARCH lines 13-14 (D-03 the MUST-pass structural checks: cloneability, get/set round-trip, no-work-in-init, NotFitted, predict-shape, Pipeline), D-04 (dtype/contiguity checks = documented skips)
    - RESEARCH Validation lines 533 (`pytest tests/test_check_estimator.py -x` with documented-skip allowlist), Pitfall 2/5 (the modern `parametrize_with_checks`/`check_estimator` API + `__sklearn_tags__`)
    - 08-04-SUMMARY (predict_proba shape convention — check_estimator asserts predict shapes)
  </read_first>
  <behavior>
    - Test: running sklearn's `check_estimator`/`parametrize_with_checks` over `CatBoostClassifier` and `CatBoostRegressor` passes ALL structural checks EXCEPT an explicit allowlist of dtype/contiguity checks, which are xfail/skip with a justification string referencing D-04/D-13.
    - Test: the allowlist is enumerated by check name (not a blanket skip) — any NEW unexpected failure outside the allowlist fails the test.
    - Test: the estimators are usable inside an `sklearn.pipeline.Pipeline` (fit→predict through the pipeline).
  </behavior>
  <action>In `test_check_estimator.py`, use `sklearn.utils.estimator_checks.check_estimator` (or `parametrize_with_checks([CatBoostClassifier(), CatBoostRegressor()])`). Define a module-level `SKIP_CHECKS` dict mapping each skipped check name → a justification string (e.g. `check_dtype_object`, the float64-feeding numeric checks that conflict with D-12's strict float32 requirement) per D-04 — enumerate them explicitly; do NOT blanket-skip. Wrap so that a failure in a non-allowlisted check fails the test, but an allowlisted check is recorded as an expected skip. Add a separate test constructing `Pipeline([("clf", CatBoostClassifier(iterations=5))])` and asserting fit→predict works. For the Ranker, follow the Task-1 decision: either include it with the regressor-like tag set or exclude it from this gate with the documented justification in a comment.</action>
  <verify>
    <automated>cd crates/catboost-rs-py && ../../.venv-py8/bin/maturin develop --features cpu 2>&1 | tail -2 && ../../.venv-py8/bin/pytest tests/test_check_estimator.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>The structural check_estimator gate passes for clf+reg with an enumerated, justified documented-skip allowlist (D-04); the estimators work in an sklearn Pipeline; any new non-allowlisted failure fails the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sklearn machinery → estimator (clone/Pipeline) | sklearn drives get_params/set_params/fit with framework-controlled inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering (correctness) | get_params/set_params round-trip drift breaks GridSearchCV (silent wrong clones) | mitigate | Verbatim store (D-06) + `check_estimator` round-trip checks pin the invariant; estimator_test.rs asserts it |
| T-08-16 | DoS | sklearn feeding float64/non-contiguous arrays panics across FFI | mitigate | Those checks are in the documented-skip allowlist AND the ingest layer (08-03) returns a typed `CatBoostValueError`, never panics |
| T-08-17 | Tampering | a silently-broadened skip allowlist hides a real contract regression | mitigate | Allowlist is enumerated by check name; any non-allowlisted failure fails the test (no blanket skip) |
</threat_model>

<verification>
- `pytest tests/test_check_estimator.py -q` green with the enumerated allowlist.
- `cargo test -p catboost-rs-py estimator` green.
- Pipeline fit→predict works.
</verification>

<success_criteria>
- Structural check_estimator passes for clf+reg; dtype/contiguity checks are an explicit, justified, enumerated skip (D-04).
- get_params/set_params round-trip + clone + Pipeline usage all work.
- predict-before-fit raises NotFittedError.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings-dual-api-packaging/08-05-SUMMARY.md` when done. Record the FULL enumerated documented-skip allowlist with justifications and the Ranker→sklearn presentation decision (RESEARCH Open Q2).
</output>
