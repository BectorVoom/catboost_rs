---
phase: 08-python-bindings-dual-api-packaging
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - crates/catboost-rs-py/src/classifier.rs
  - crates/catboost-rs-py/src/ranker.rs
  - crates/catboost-rs-py/src/estimator.rs
  - crates/catboost-rs-py/src/lib.rs
  - crates/catboost-rs-py/tests/test_native_api.py
  - crates/catboost-rs-py/tests/test_oracle_parity.py
  - crates/catboost-rs-py/tests/conftest.py
autonomous: true
requirements: [PYAPI-03]
tags: [pyo3, classifier, ranker, oracle-parity]

must_haves:
  truths:
    - "A user can fit a CatBoostClassifier and call predict / predict_proba and get correctly-shaped class labels and probabilities"
    - "A user can fit a CatBoostRanker on a group_id/pairs Pool and get ranking scores"
    - "The Python surface reproduces catboost 1.2.10 oracle predictions to within 1e-5 for regression and classification fixtures"
  artifacts:
    - path: "crates/catboost-rs-py/src/classifier.rs"
      provides: "CatBoostClassifier #[pyclass] with fit/predict/predict_proba mapping to facade Model"
      contains: "pyclass"
    - path: "crates/catboost-rs-py/src/ranker.rs"
      provides: "CatBoostRanker #[pyclass] with fit/predict over group_id/pairs Pool"
      contains: "pyclass"
    - path: "crates/catboost-rs-py/tests/test_oracle_parity.py"
      provides: "≤1e-5 parity assertions vs existing offline catboost 1.2.10 fixtures"
      contains: "1e-5"
  key_links:
    - from: "crates/catboost-rs-py/src/classifier.rs"
      to: "catboost_rs::Model::predict_proba"
      via: "predict_proba over the fitted model"
      pattern: "predict_proba"
    - from: "crates/catboost-rs-py/src/ranker.rs"
      to: "cb_data Pool group_id/pairs"
      via: "ranking Pool fit"
      pattern: "group_id|pairs"
---

<objective>
Complete the CatBoost-native estimator trio. Add `CatBoostClassifier` (`fit`/`predict`/`predict_proba`) and `CatBoostRanker` (`fit`/`predict` over a group_id/pairs Pool) as `#[pyclass]` types reusing the shared estimator base, param registry, and ingestion from earlier slices. Lock the phase's correctness bar with a Python-surface oracle-parity test (≤1e-5) against the existing offline catboost 1.2.10 fixtures.

This is the breadth slice: classification and ranking become usable end-to-end, and the ≤1e-5 oracle gate proves the Python surface preserves the Rust core's parity.

Purpose: PYAPI-03 full clf/reg/ranker surface as one unified CatBoost-mirror class hierarchy (D-01: the native classes ARE the sklearn estimators, no separate wrapper) + the ROADMAP ≤1e-5 parity criterion at the Python layer.
Output: `classifier.rs`, `ranker.rs`, `test_native_api.py`, `test_oracle_parity.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-python-bindings-dual-api-packaging/08-RESEARCH.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-PATTERNS.md
@.planning/phases/08-python-bindings-dual-api-packaging/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CatBoostClassifier + CatBoostRanker #[pyclass]</name>
  <files>crates/catboost-rs-py/src/classifier.rs, crates/catboost-rs-py/src/ranker.rs, crates/catboost-rs-py/src/estimator.rs, crates/catboost-rs-py/src/lib.rs, crates/catboost-rs-py/tests/test_native_api.py, crates/catboost-rs-py/tests/conftest.py</files>
  <read_first>
    - crates/catboost-rs-py/src/regressor.rs (the 08-01/08-02 CatBoostRegressor to mirror — same store-verbatim/validate/ingest/detach/fit structure)
    - crates/catboost-rs/src/model.rs lines 100-111 (`predict` regression, `predict_proba` classification — the exact methods clf/reg call)
    - crates/catboost-rs/src/builder.rs lines 111-167 (`loss()` SELECTS classification vs regression — Logloss/CrossEntropy/Focal = classification; the classifier sets a classification default loss)
    - crates/cb-data/src/ingest/owned.rs lines 78-99 (`with_group_id`/`with_subgroup_id`/`with_pairs` — the ranker Pool path)
    - RESEARCH Pattern 1 lines 236-271 (store-verbatim estimator), lines 483-487 (Ranker→sklearn presentation is Deferred — for THIS plan present native fit/predict only; sklearn tags for the ranker are decided in 08-05)
  </read_first>
  <behavior>
    - Test (test_native_api.py): `CatBoostClassifier(iterations=20, depth=3).fit(X,y_binary)` then `.predict(X)` returns class labels of shape (n,), `.predict_proba(X)` returns probabilities of shape (n,) or (n,2) (match upstream convention — document which).
    - Test: classifier default loss is a classification loss (Logloss) without the user passing `loss_function`.
    - Test: `CatBoostRanker(iterations=20).fit(pool_with_group_id)` then `.predict(pool)` returns finite scores of shape (n,); fitting a ranker without group_id raises a CatBoostValueError (or CatBoostParameterError) with an actionable message.
  </behavior>
  <action>Create `classifier.rs`: `#[pyclass] CatBoostClassifier` cloning the `CatBoostRegressor` structure (shared base from `estimator.rs`) but defaulting `loss_function` to a classification loss (Logloss) when the user does not set one, exposing `fit`, `predict` (class labels — threshold predict_proba at 0.5 or use `Model::predict_with(PredictionType::Class)` if available; otherwise derive from proba), and `predict_proba` → `Model::predict_proba`. Create `ranker.rs`: `#[pyclass] CatBoostRanker` requiring a group_id-bearing Pool (or `group_id=` kwarg in fit); `fit` validates group_id presence (else actionable error), `predict` → `Model::predict` returning scores. Hoist any duplicated store-verbatim/get_params/fit-skeleton logic into `estimator.rs` shared helpers so the three classes share it (prep for 08-05's sklearn contract). Register both classes in `lib.rs`. Extend `conftest.py` with a `toy_classification` (binary) fixture and a `toy_ranking` (group_id) fixture. Add `test_native_api.py`.</action>
  <verify>
    <automated>cd crates/catboost-rs-py && ../../.venv-py8/bin/maturin develop --features cpu 2>&1 | tail -2 && ../../.venv-py8/bin/pytest tests/test_native_api.py -q 2>&1 | tail -12</automated>
  </verify>
  <done>Classifier predict/predict_proba and ranker fit/predict produce correctly-shaped outputs; classifier defaults to a classification loss; ranker rejects missing group_id.</done>
</task>

<task type="auto">
  <name>Task 2: Python-surface oracle parity ≤1e-5 vs catboost 1.2.10 fixtures</name>
  <files>crates/catboost-rs-py/tests/test_oracle_parity.py, crates/catboost-rs-py/tests/conftest.py</files>
  <read_first>
    - crates/cb-oracle/ (the existing Rust oracle harness + where the offline catboost 1.2.10 fixtures live — locate the fixture files: trained .cbm/.json + reference prediction vectors)
    - RESEARCH lines 506, 536, 550-551 (reuse the EXISTING offline oracle fixtures; do NOT require a live catboost import for unit tests; ≤1e-5 bar)
    - crates/catboost-rs/src/model.rs lines 199-229 (`load_cbm`/`load_json` — load a reference model and predict for parity)
    - PATTERNS lines 26 (no Python oracle harness exists; Rust `cb-oracle` does — this test reuses the same fixture vectors at the Python layer)
  </read_first>
  <behavior>
    - Test (test_oracle_parity.py): for at least one regression and one classification fixture, load the same input the Rust oracle uses, run it through the Python `CatBoostRegressor`/`CatBoostClassifier` (or load the reference .cbm via a Python `load_model` if exposed, else fit on the fixture training data with the fixture params), and assert the Python predictions match the stored reference vector elementwise to within 1e-5.
    - Test: the parity test is hermetic — it reads offline fixture files, does not import the `catboost` package.
  </behavior>
  <action>Locate the existing offline oracle fixtures used by `cb-oracle` (trained model + reference prediction vector + input matrix). In `test_oracle_parity.py`, load the input matrix as a float32 contiguous NumPy array, drive it through the Python estimator path (prefer: if a `load_model` is exposed by `pool.rs`/an estimator wrapping `Model::load_cbm`, load the reference model and `predict`; if model-load is not yet exposed at the Python layer, fit on the fixture's training data with the fixture's locked params — whichever reproduces the stored reference vector), and `numpy.testing.assert_allclose(py_pred, ref_vec, atol=1e-5, rtol=0)`. Cover one regression + one classification fixture. Add the fixture paths to `conftest.py`. If exposing `Model::load_cbm` at the Python layer is the cleanest path, add a minimal `CatBoostRegressor.load_model(path)` classmethod-style constructor wrapping `catboost_rs::Model::load_cbm` (mirrors upstream `load_model`; RESEARCH Open Question 3 notes interop already works at the Rust layer).</action>
  <verify>
    <automated>cd crates/catboost-rs-py && ../../.venv-py8/bin/maturin develop --features cpu 2>&1 | tail -2 && ../../.venv-py8/bin/pytest tests/test_oracle_parity.py -q 2>&1 | tail -12</automated>
  </verify>
  <done>The Python surface reproduces the stored catboost 1.2.10 reference predictions to ≤1e-5 for a regression and a classification fixture, hermetically (no live catboost import).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → classifier/ranker fit | Untrusted labels/group_id cross into Rust |
| Reference model file → Python load_model | Untrusted .cbm/.json deserialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | malformed .cbm/.json loaded via `load_model` | mitigate | Facade `Model::load_cbm`/`load_json` already returns typed `CatBoostError::Deserialize`/`SchemaVersion` (never panics, Phase 4) → mapped to `CatBoostValueError` (08-02) |
| T-08-13 | Tampering (correctness) | classifier predict_proba shape/threshold deviates from upstream → silent wrong labels | mitigate | Oracle-parity ≤1e-5 test pins the numeric output against catboost 1.2.10; native-api test pins the shape convention |
| T-08-14 | DoS | ranker fit without group_id panics/indexes unchecked | mitigate | Validate group_id presence → actionable typed error; deny gate forbids unchecked indexing |
</threat_model>

<verification>
- `pytest tests/test_native_api.py tests/test_oracle_parity.py -q` green.
- Oracle parity assertions use `atol=1e-5, rtol=0`.
</verification>

<success_criteria>
- Classifier predict/predict_proba and ranker fit/predict work with correct shapes.
- Python predictions match catboost 1.2.10 fixtures to ≤1e-5 (regression + classification).
- The native-api trio (clf/reg/ranker) is complete and parity-locked.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings-dual-api-packaging/08-04-SUMMARY.md` when done. Record the predict_proba shape convention chosen and the fixtures used for parity.
</output>
