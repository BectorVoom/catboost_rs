---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 06
type: execute
wave: 6
depends_on: ["06.6-05"]
files_modified:
  - crates/cb-model/src/fstr.rs
  - crates/cb-model/tests/fstr_oracle_test.rs
  - crates/cb-oracle/tests/fixtures/fstr_loss_change/.gitkeep
autonomous: true
requirements: [MODEL-03, MODEL-05]
must_haves:
  truths:
    - "LossFunctionChange importance matches catboost 1.2.10 get_feature_importance(type='LossFunctionChange', data=pool) ≤1e-5 (D-6.6-09, completes MODEL-03/D-12)."
    - "PredictionValuesChange + Interaction are generalized to non-symmetric trees and oracle-lock ≥1 non-symmetric case (D-6.6-10)."
    - "The existing oblivious PVC/Interaction oracle (fstr_oracle_test) stays byte-identical (D-6.6-05)."
  artifacts:
    - path: "crates/cb-model/src/fstr.rs"
      provides: "loss_function_change() backend + non-symmetric PVC/Interaction recursion"
      contains: "loss_function_change"
    - path: "crates/cb-model/tests/fstr_oracle_test.rs"
      provides: "LossFunctionChange + non-symmetric PVC/Interaction oracle ≤1e-5"
  key_links:
    - from: "crates/cb-model/src/fstr.rs"
      to: "crates/cb-model/src/model.rs"
      via: "PVC/Interaction recursion branches on TreeVariant: Oblivious=bit-indexed | NonSymmetric=node-graph"
      pattern: "TreeVariant::NonSymmetric|loss_function_change"
---

<objective>
Gate-C (MODEL-03/D-12 + part of MODEL-05): add the LossFunctionChange importance backend (completing the deferred Phase-4 D-12) and generalize PredictionValuesChange + Interaction recursion to non-symmetric trees (D-6.6-10). Land BEFORE FEAT-05 (D-6.6-03 — feature selection consumes these importances). Oracle-lock ≥1 non-symmetric fstr case.

Purpose: FEAT-05 recursive elimination explicitly consumes PVC / LossFunctionChange / ShapValues — so fstr must exist first (D-6.6-03). Upstream computes fstr on non-symmetric models too; oblivious-only would leave a parity gap. The existing PVC recursion is bit-indexed (assumes perfect-binary oblivious); non-symmetric needs node-graph recursion (Pitfall 5 analog for fstr).
Output: `loss_function_change()` + a `FeatureImportanceType::LossFunctionChange` variant; non-symmetric PVC/Interaction branches; `fstr_oracle_test` extended with LossFunctionChange + ≥1 non-symmetric case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- `fstr.rs`: `FeatureImportanceType::LossFunctionChange` variant; `pub fn loss_function_change(...)` (transcribes `loss_change_fstr.cpp`); non-symmetric variant branches in `prediction_values_change` + `interaction` (node-graph leaf-pair enumeration).
- `fstr_oracle_test.rs`: LossFunctionChange test + ≥1 non-symmetric PVC/Interaction test.
- Fixtures under `crates/cb-oracle/tests/fixtures/fstr_loss_change/` (OFFLINE catboost 1.2.10).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: LossFunctionChange backend + non-symmetric PVC/Interaction recursion</name>
  <files>crates/cb-model/src/fstr.rs</files>
  <read_first>
    - crates/cb-model/src/fstr.rs:51 (`FeatureImportanceType` enum — add LossFunctionChange variant) ; fstr.rs:80 (`convert_to_percents`, Σ=100 — reuse verbatim) ; fstr.rs:94-128 (`prediction_values_change` — bit-indexed `leaf_idx ^ (1<<feature_bit)` leaf-pair walk that assumes perfect-binary oblivious) ; fstr.rs:145 (`interaction` — `CalcMostInteractingFeatures` + `CalcFeatureInteraction`)
    - catboost-master/catboost/libs/fstr/loss_change_fstr.cpp (LossFunctionChange importance algorithm — transcribe verbatim)
    - catboost-master/catboost/libs/fstr/feature_str.cpp + calc_fstr.cpp (existing oblivious PVC + Interaction to generalize)
    - crates/cb-model/src/model.rs (`TreeVariant`/`NonSymmetricTree` from 06.6-03 — `step_nodes`/`node_id_to_leaf_id` for the node-graph recursion)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Code Examples → `m.get_feature_importance(type="LossFunctionChange", data=pool)` → (n_features,))
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md (fstr.rs analog — "keep the oblivious arm byte-identical so fstr_oracle_test stays locked")
  </read_first>
  <action>
    Add `FeatureImportanceType::LossFunctionChange` (fstr.rs:51) and `pub fn loss_function_change(...)` transcribing `loss_change_fstr.cpp` verbatim (cite the source lines in the doc comment). Reuse `convert_to_percents` (fstr.rs:80) and the `sum_f64`/checked-`.get` discipline. Oracle target: `get_feature_importance(type='LossFunctionChange', data=pool)`.

    Generalize `prediction_values_change` (fstr.rs:94-128) and `interaction` (fstr.rs:145) to non-symmetric trees: branch on `TreeVariant`. Oblivious arm = the EXISTING bit-indexed loop, byte-identical (D-6.6-05 — fstr_oracle_test stays locked). NonSymmetric arm = enumerate leaf pairs by recursing over the node graph (`step_nodes` children) instead of `leaf_idx ^ (1<<feature_bit)` bit-indexing; weights attach via `node_id_to_leaf_id`. All reductions via `cb_core::sum_f64`; `.get(...)` access; NO embedded tests.
  </action>
  <verify>
    <automated>cargo test -p cb-model --lib 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-model` succeeds; `loss_function_change` + the `LossFunctionChange` variant exist transcribing the cited `loss_change_fstr.cpp` lines.
    - PVC + Interaction branch on `TreeVariant`; the oblivious arm is the literal existing bit-indexed loop.
    - All existing cb-model lib tests stay green; reductions via `sum_f64`; no `mod tests` added.
  </acceptance_criteria>
  <done>LossFunctionChange backend + non-symmetric PVC/Interaction recursion land; oblivious arm byte-identical; cb-model lib suite green.</done>
</task>

<task type="auto">
  <name>Task 2: fstr_oracle_test — LossFunctionChange + ≥1 non-symmetric PVC/Interaction case ≤1e-5</name>
  <files>crates/cb-model/tests/fstr_oracle_test.rs, crates/cb-oracle/tests/fixtures/fstr_loss_change/.gitkeep</files>
  <read_first>
    - crates/cb-model/tests/fstr_oracle_test.rs (existing PVC/Interaction oracle structure — extend, keep existing cases unchanged)
    - crates/cb-oracle/src/{fixture,compare}.rs (compare harness)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Python oracle calls; D-6.6-10 — oracle-lock ≥1 non-symmetric fstr case)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-VALIDATION.md (LossFunctionChange → `cargo test -p cb-model --test fstr_oracle_test`)
  </read_first>
  <action>
    Extend `fstr_oracle_test.rs`: add a LossFunctionChange test asserting `loss_function_change(...)` ≤1e-5 vs the committed `get_feature_importance(type='LossFunctionChange', data=pool)` fixture, and add ≥1 NON-symmetric (Depthwise or Lossguide) PVC or Interaction test asserting ≤1e-5 (D-6.6-10). Keep all existing oblivious PVC/Interaction cases UNCHANGED (no-regression lock). None `#[ignore]`d, no tolerance weakened.

    Generate fixtures OFFLINE from `.venv` catboost 1.2.10: a model + `get_feature_importance(type='LossFunctionChange', data=pool)` vector, and a non-symmetric model + its PVC/Interaction importance. Commit under `crates/cb-oracle/tests/fixtures/fstr_loss_change/`. Pinned seed, thread_count=1. No fabrication.
  </action>
  <verify>
    <automated>cargo test -p cb-model --test fstr_oracle_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-model --test fstr_oracle_test` passes, including the new LossFunctionChange case and ≥1 non-symmetric case; none `#[ignore]`d.
    - LossFunctionChange ≤1e-5 vs catboost 1.2.10; the non-symmetric PVC/Interaction case ≤1e-5.
    - Existing oblivious PVC/Interaction cases stay green (byte-identical).
  </acceptance_criteria>
  <done>LossFunctionChange importance (MODEL-03/D-12) oracle-locked ≤1e-5; non-symmetric PVC/Interaction oracle-locked ≥1 case (D-6.6-10).</done>
</task>

</tasks>

<threat_model>
No new untrusted external-input surface beyond the model files already validated in 06.6-03/05. fstr consumes an already-loaded, already-bounds-validated `Model` and an in-process `Pool`. The relevant control is bounds-checked node-graph traversal in the non-symmetric PVC/Interaction recursion.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-15 | Denial of Service | non-symmetric PVC/Interaction node-graph recursion | mitigate | Depth-bounded / iterative recursion over `step_nodes`; `.get(...)` access; reuse the load-time `index<node_count` validation from 06.6-03/05 |
| T-06.6-16 | Tampering | importance Σ / convert_to_percents | mitigate | Reductions via `cb_core::sum_f64` (D-08) |
</threat_model>

<verification>
- `cargo test -p cb-model --lib` green (oblivious fstr byte-identical).
- `cargo test -p cb-model --test fstr_oracle_test` green incl. LossFunctionChange + ≥1 non-symmetric case, none ignored.
- Grep: oblivious PVC arm is the literal existing bit-indexed loop; no `mod tests` added.
</verification>

<success_criteria>
LossFunctionChange importance completes MODEL-03/D-12 oracle-locked ≤1e-5; PVC/Interaction generalized to non-symmetric trees with ≥1 oracle-locked case (D-6.6-10). Part of SC-4; unblocks FEAT-05 (06.6-08).
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-06-SUMMARY.md` when done.
</output>
