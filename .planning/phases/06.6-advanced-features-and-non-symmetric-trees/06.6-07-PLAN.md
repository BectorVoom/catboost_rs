---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 07
type: execute
wave: 6
depends_on: ["06.6-05"]
files_modified:
  - crates/cb-model/src/shap.rs
  - crates/cb-model/tests/advanced_fstr_oracle_test.rs
  - crates/cb-model/tests/shap_oracle_test.rs
  - crates/cb-oracle/tests/fixtures/advanced_fstr/.gitkeep
autonomous: true
requirements: [MODEL-05]
must_haves:
  truths:
    - "ShapInteractionValues matches catboost 1.2.10 get_feature_importance(type='ShapInteractionValues', data=pool) ≤1e-5 (bias-slot index reverse-mapped empirically, Open Question 2)."
    - "PredictionDiff matches get_feature_importance(type='PredictionDiff', data=X[:2]) ≤1e-5."
    - "SAGE matches get_feature_importance(type='SageValues', data=pool) ≤1e-5 via seed-match strict through the Python API (D-6.6-11, no instrumentation)."
    - "TreeSHAP generalizes to non-symmetric trees (node-graph subtree weights) and ≥1 non-symmetric SHAP case oracle-locks ≤1e-5 (D-6.6-10); the oblivious shap_oracle_test stays byte-identical (D-6.6-05)."
  artifacts:
    - path: "crates/cb-model/src/shap.rs"
      provides: "non-symmetric TreeSHAP recursion + shap_interaction_values/prediction_diff/sage backends"
      contains: "shap_interaction"
    - path: "crates/cb-model/tests/advanced_fstr_oracle_test.rs"
      provides: "ShapInteraction/PredictionDiff/SAGE oracle ≤1e-5 (≥1 non-symmetric)"
  key_links:
    - from: "crates/cb-model/src/shap.rs"
      to: "crates/cb-model/src/model.rs"
      via: "calc_subtree_weights branches on TreeVariant: Oblivious=perfect-binary | NonSymmetric=node-graph"
      pattern: "TreeVariant::NonSymmetric|shap_interaction"
---

<objective>
Gate-C (MODEL-05): add the advanced SHAP-family fstr types — ShapInteractionValues, PredictionDiff, SAGE — and generalize TreeSHAP recursion to non-symmetric trees (D-6.6-10, Pitfall 5). Oracle-lock ≥1 non-symmetric SHAP case. SAGE is seed-match strict through the Python API (RESEARCH gate 2 RESOLVED — deterministic+seed-reproducible, NO C++ instrumentation, D-6.6-11).

Purpose: Completes MODEL-05 and the non-symmetric SHAP parity surface; runs in parallel with 06.6-06 (fstr.rs vs shap.rs, zero file overlap). The existing `calc_subtree_weights` walks a perfect-binary tree (`node*2`/`node*2+1` bit-indexing); a non-symmetric tree is an arbitrary binary graph — generalize to recurse on `step_nodes` children with weights via `node_id_to_leaf_id` (Pitfall 5). SHAP currently assumes `scale==1` and consumes `leaf_weights`.
Output: `shap_interaction_values()`, `prediction_diff()`, `sage_values()` backends + non-symmetric TreeSHAP; `advanced_fstr_oracle_test.rs` ≤1e-5 with ≥1 non-symmetric case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- `shap.rs`: non-symmetric TreeSHAP (`calc_subtree_weights` generalized to the node graph); `shap_interaction_values()`, `prediction_diff()`, `sage_values()` backends.
- `advanced_fstr_oracle_test.rs`: ShapInteraction/PredictionDiff/SAGE oracle (≥1 non-symmetric; SAGE pins random_seed).
- `shap_oracle_test.rs`: extended with ≥1 non-symmetric regular-SHAP case (existing oblivious cases unchanged).
- Fixtures under `crates/cb-oracle/tests/fixtures/advanced_fstr/` (OFFLINE catboost 1.2.10).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Non-symmetric TreeSHAP recursion + ShapInteractionValues / PredictionDiff / SAGE backends</name>
  <files>crates/cb-model/src/shap.rs</files>
  <read_first>
    - crates/cb-model/src/shap.rs:206-243 (`calc_subtree_weights` — perfect-binary: `subtree[depth][node]`, full-depth row = `leaf_weights`, parent = `child[2n]+child[2n+1]`; the bit-indexing to generalize) ; shap.rs:250 (`calc_mean_value`) ; shap.rs:183,267 (`sum_f64` usage)
    - crates/cb-model/src/model.rs (`TreeVariant`/`NonSymmetricTree` from 06.6-03 — `step_nodes`/`node_id_to_leaf_id` children to recurse on)
    - catboost-master/catboost/libs/fstr/shap_values.cpp (TreeSHAP recursion — the source to generalize; scale==1 / CB_ENSURE_SCALE_IDENTITY assumption)
    - catboost-master/catboost/libs/fstr/shap_interaction_values.cpp (ShapInteractionValues; output (n_obj, n_feat+1, n_feat+1) — the +1 bias slot)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Open Question 2 — reverse-map the bias-slot index empirically from a tiny oracle before coding; Pitfall 5 — node-graph subtree weights; Code Examples → PredictionDiff/SageValues calls; SAGE deterministic+seed-reproducible)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md (shap.rs analog — "generalize the recursion to walk step_nodes children rather than node*2/node*2+1; keep the oblivious recursion unchanged")
  </read_first>
  <action>
    Generalize `calc_subtree_weights` (shap.rs:206-243) to non-symmetric: branch on `TreeVariant`. Oblivious arm = the EXISTING perfect-binary recursion, byte-identical (D-6.6-05 — shap_oracle_test stays locked). NonSymmetric arm = recurse over the node graph (`step_nodes` children, leaf weights attached via `node_id_to_leaf_id`) instead of `node*2`/`node*2+1`. Keep the `scale==1` assumption (CB_ENSURE_SCALE_IDENTITY) and ensure non-symmetric SHAP satisfies local-accuracy (Σ shap + bias == prediction) — the same invariant the oblivious path holds (D-11 precedent).

    Add `pub fn shap_interaction_values(...)` transcribing `shap_interaction_values.cpp` (output `(n_obj, n_feat+1, n_feat+1)` — FIRST capture a tiny oracle and reverse-map the `+1` bias-slot index convention empirically, Open Question 2, BEFORE coding the recursion). Add `pub fn prediction_diff(...)` and `pub fn sage_values(...)`. For SAGE, the Python API output IS the spec; SAGE is deterministic+seed-reproducible (RESEARCH gate 2) — reproduce upstream's seeding via the pinned `random_seed`, NO instrumentation (D-6.6-11 fallback (a)). All reductions via `cb_core::sum_f64`; `.get(...)` access; NO embedded tests.
  </action>
  <verify>
    <automated>cargo test -p cb-model --lib 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-model` succeeds; `shap_interaction_values`/`prediction_diff`/`sage_values` exist; `calc_subtree_weights` branches on `TreeVariant`.
    - The oblivious SHAP arm is the literal existing perfect-binary recursion (shap_oracle_test will confirm).
    - SAGE uses seed-match through the Python-API-defined spec, no C++ instrumentation.
    - reductions via `sum_f64`; no `mod tests` added.
  </acceptance_criteria>
  <done>Non-symmetric TreeSHAP + ShapInteraction/PredictionDiff/SAGE backends land; oblivious SHAP byte-identical; cb-model lib green.</done>
</task>

<task type="auto">
  <name>Task 2: advanced_fstr_oracle_test + non-symmetric shap_oracle_test case ≤1e-5</name>
  <files>crates/cb-model/tests/advanced_fstr_oracle_test.rs, crates/cb-model/tests/shap_oracle_test.rs, crates/cb-oracle/tests/fixtures/advanced_fstr/.gitkeep</files>
  <read_first>
    - crates/cb-model/tests/shap_oracle_test.rs (existing regular-SHAP oracle — extend with ≥1 non-symmetric case, keep oblivious cases unchanged)
    - crates/cb-model/tests/apply_oracle_test.rs:1-40 (oracle skeleton + top-line `#![allow(...)]`)
    - crates/cb-oracle/src/{fixture,compare}.rs (compare harness)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Python oracle calls — ShapInteractionValues/PredictionDiff/SageValues shapes; SAGE pins random_seed, reproducible across seeds verified)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-VALIDATION.md (MODEL-05 → `cargo test -p cb-model --test advanced_fstr_oracle_test`; ≥1 non-symmetric fstr case)
  </read_first>
  <action>
    Author `crates/cb-model/tests/advanced_fstr_oracle_test.rs` (apply_oracle_test structure, top-line `#![allow(...)]`): tests for ShapInteractionValues, PredictionDiff, and SAGE each ≤1e-5 vs the committed catboost 1.2.10 fixture, with ≥1 of them on a NON-symmetric (Depthwise/Lossguide) model (D-6.6-10). SAGE test pins `random_seed`. Extend `shap_oracle_test.rs` with ≥1 NON-symmetric regular-SHAP case ≤1e-5 (and assert local-accuracy Σshap+bias==prediction). Keep all existing oblivious SHAP cases UNCHANGED. None `#[ignore]`d, no tolerance weakened.

    Generate fixtures OFFLINE from `.venv` catboost 1.2.10: `m.get_feature_importance(type='ShapInteractionValues', data=pool)`, `type='PredictionDiff'`, `type='SageValues'` (pinned random_seed), plus a non-symmetric model's regular ShapValues. For ShapInteractionValues capture the tiny oracle Open Question 2 needs to reverse-map the bias slot. Commit under `crates/cb-oracle/tests/fixtures/advanced_fstr/`. Pinned seed, thread_count=1. No fabrication; if SAGE proves non-reproducible at oracle time, escalate per D-6.6-11 fallback (b) (documented weakened tolerance with structural oracle) — but RESEARCH confirms determinism, so seed-match strict is expected.
  </action>
  <verify>
    <automated>cargo test -p cb-model --test advanced_fstr_oracle_test 2>&1 | tail -15; cargo test -p cb-model --test shap_oracle_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-model --test advanced_fstr_oracle_test` passes (ShapInteraction/PredictionDiff/SAGE), ≥1 on a non-symmetric model; none `#[ignore]`d.
    - `cargo test -p cb-model --test shap_oracle_test` passes incl. the new non-symmetric regular-SHAP case (local-accuracy holds); existing oblivious cases unchanged.
    - All ≤1e-5 vs catboost 1.2.10; SAGE seed-pinned; fixtures real, not fabricated.
  </acceptance_criteria>
  <done>ShapInteractionValues/PredictionDiff/SAGE + non-symmetric regular SHAP oracle-locked ≤1e-5; MODEL-05 satisfied; SC-4 complete (with LossFunctionChange from 06.6-06).</done>
</task>

</tasks>

<threat_model>
No new untrusted external-input surface beyond the model files validated in 06.6-03/05. SHAP consumes an already-loaded, bounds-validated `Model` + in-process `Pool`. The control is bounds-checked node-graph traversal in non-symmetric TreeSHAP.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-17 | Denial of Service | non-symmetric `calc_subtree_weights` recursion | mitigate | Depth-bounded / iterative recursion over `step_nodes`; `.get(...)`; reuse load-time `index<node_count` validation (06.6-03/05) |
| T-06.6-18 | Tampering | SHAP weight / interaction Σ | mitigate | Reductions via `cb_core::sum_f64` (D-08); local-accuracy invariant asserted |
</threat_model>

<verification>
- `cargo test -p cb-model --lib` green (oblivious SHAP byte-identical).
- `cargo test -p cb-model --test advanced_fstr_oracle_test` + `--test shap_oracle_test` green, none ignored, ≥1 non-symmetric case each.
- Grep: oblivious `calc_subtree_weights` arm is the literal existing perfect-binary recursion; no `mod tests` added.
</verification>

<success_criteria>
MODEL-05 complete: ShapInteractionValues, PredictionDiff, SAGE oracle-locked ≤1e-5; TreeSHAP generalized to non-symmetric with ≥1 oracle-locked case (D-6.6-10); SAGE via seed-match strict (D-6.6-11). With 06.6-06, SC-4 complete.
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-07-SUMMARY.md` when done.
</output>
