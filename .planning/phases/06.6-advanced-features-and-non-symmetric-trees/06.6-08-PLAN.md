---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 08
type: execute
wave: 7
depends_on: ["06.6-06", "06.6-07"]
files_modified:
  - crates/cb-train/src/feature_selection.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-train/tests/feature_selection_oracle_test.rs
  - crates/cb-oracle/tests/fixtures/feature_selection/.gitkeep
autonomous: true
requirements: [FEAT-05]
must_haves:
  truths:
    - "select_features by RecursiveByShapValues produces a {selected_features, eliminated_features} partition matching catboost 1.2.10 select_features(...) (FEAT-05, SC-2)."
    - "select_features by RecursiveByPredictionValuesChange and RecursiveByLossFunctionChange (the shared FeatureEffect backend) match upstream."
    - "The recursive-elimination loop wraps cb_train::train + the cb-model importance methods (D-6.6-03); no new crate (modules over crates)."
  artifacts:
    - path: "crates/cb-train/src/feature_selection.rs"
      provides: "NEW recursive-elimination module mirroring recursive_features_elimination.cpp (2 backends: Shap, FeatureEffect)"
      contains: "select_features"
    - path: "crates/cb-train/tests/feature_selection_oracle_test.rs"
      provides: "select_features selected/eliminated set oracle"
  key_links:
    - from: "crates/cb-train/src/feature_selection.rs"
      to: "crates/cb-model/src/fstr.rs"
      via: "importance backend consumes loss_function_change/prediction_values_change/shap_values"
      pattern: "shap_values|prediction_values_change|loss_function_change"
---

<objective>
Gate-D (FEAT-05, LAST — D-6.6-03): implement recursive feature selection by PredictionValuesChange / LossFunctionChange / ShapValues, consuming the importance methods built in Gate C. This is the final strand and blocks on fstr completion (06.6-06 + 06.6-07). NEW `cb-train` module (no new crate — modules over crates).

Purpose: FEAT-05's recursive elimination explicitly consumes PVC / LossFunctionChange / ShapValues importance — which is why it is sequenced LAST (D-6.6-03). Upstream `recursive_features_elimination.cpp` has only 2 backends: Shap, and FeatureEffect (which PVC + LossFunctionChange share). The loop retrains on the candidate feature set, ranks by importance, eliminates the weakest, repeats over `steps`.
Output: `feature_selection.rs` with `select_features(...)` (recursive-elimination loop over `cb_train::train` + importance), exported from `lib.rs`; `feature_selection_oracle_test.rs` comparing `{selected_features, eliminated_features}` to upstream.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- NEW `crates/cb-train/src/feature_selection.rs` module: `select_features(...)` + the `EFeaturesSelectionAlgorithm`-equivalent enum (RecursiveByShapValues | RecursiveByPredictionValuesChange | RecursiveByLossFunctionChange) + a `{selected_features, eliminated_features}` result type.
- `crates/cb-train/src/lib.rs`: `mod feature_selection;` + `pub use` of `select_features`.
- New oracle test file `crates/cb-train/tests/feature_selection_oracle_test.rs` + fixture under `crates/cb-oracle/tests/fixtures/feature_selection/`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NEW feature_selection module — recursive-elimination loop over train + importance</name>
  <files>crates/cb-train/src/feature_selection.rs, crates/cb-train/src/lib.rs</files>
  <read_first>
    - crates/cb-train/src/boosting.rs:1520 (the `train` entry / outer loop — the retrain shape the elimination loop wraps; how a fit consumes the feature set)
    - crates/cb-model/src/fstr.rs (`prediction_values_change`, `interaction`, `loss_function_change` from 06.6-06) + crates/cb-model/src/shap.rs (`shap_values` + the SHAP-based importance) — the importance backends consumed
    - crates/cb-train/src/lib.rs (module declaration + `pub use` re-export convention)
    - catboost-master/catboost/libs/features_selection/recursive_features_elimination.cpp (the elimination loop — 2 backends: Shap, FeatureEffect [PVC + LossFunctionChange share it]; the per-step retrain→rank→eliminate structure to mirror)
    - catboost-master/catboost/private/libs/options/features_select_options.h + .cpp (`EFeaturesSelectionAlgorithm` + options: features_for_select, num_features_to_select, steps, train_final_model)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Don't Hand-Roll → "Mirror recursive_features_elimination.cpp (2 backends)"; Code Examples → `m.select_features(pool, features_for_select=[...], num_features_to_select=k, algorithm="RecursiveByShapValues", steps=s, train_final_model=False)`)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md (feature_selection.rs — partial analog; NEW module, no new crate)
  </read_first>
  <action>
    Create `crates/cb-train/src/feature_selection.rs` (NEW module, no new crate — D-6.6-03). Add `select_features(pool, features_for_select, num_features_to_select, algorithm, steps, train_final_model, params) -> FeatureSelectionResult { selected_features, eliminated_features }`, mirroring `recursive_features_elimination.cpp`: per step, train a model on the current candidate set via `cb_train::train`, rank features by the chosen importance backend (RecursiveByShapValues → `cb_model::shap_values`; RecursiveByPredictionValuesChange / RecursiveByLossFunctionChange → the shared FeatureEffect path = `prediction_values_change` / `loss_function_change`), eliminate the weakest features for this step (the `steps`-based schedule from upstream), repeat until `num_features_to_select` remain. Add the `EFeaturesSelectionAlgorithm`-equivalent enum (3 variants). `train_final_model=false` returns the partition without a final fit.

    All reductions via `cb_core::sum_f64`; `.get(...)` access; typed `CbError` on invalid options (out-of-range feature indices in `features_for_select`, `num_features_to_select > features_for_select.len()`). NO embedded tests. Declare `mod feature_selection;` and `pub use feature_selection::{select_features, ...}` in lib.rs per the re-export convention.
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-train` succeeds; `feature_selection.rs` exists with `select_features` + the 3-variant algorithm enum; exported from lib.rs.
    - The loop consumes the Gate-C importance backends (grep: references `shap_values` and `prediction_values_change`/`loss_function_change`).
    - No new crate added (modules over crates, D-6.6-03); typed errors on invalid options; no `mod tests` in feature_selection.rs.
    - Existing cb-train lib tests stay green.
  </acceptance_criteria>
  <done>The recursive-elimination module wraps train + the Gate-C importances for all 3 algorithms; exported from lib.rs; cb-train lib green.</done>
</task>

<task type="auto">
  <name>Task 2: feature_selection_oracle_test — selected/eliminated set matches catboost 1.2.10</name>
  <files>crates/cb-train/tests/feature_selection_oracle_test.rs, crates/cb-oracle/tests/fixtures/feature_selection/.gitkeep</files>
  <read_first>
    - crates/cb-model/tests/apply_oracle_test.rs:1-40 (oracle skeleton + top-line `#![allow(...)]`)
    - crates/cb-oracle/src/{fixture,compare}.rs (compare harness — here the comparison is set-equality of feature-index lists, not ≤1e-5 floats)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Code Examples → `m.select_features(...)` returns `{selected_features, eliminated_features}`)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-VALIDATION.md (FEAT-05 → `cargo test -p cb-train --test feature_selection_oracle_test`)
  </read_first>
  <action>
    Author `crates/cb-train/tests/feature_selection_oracle_test.rs` (top-line `#![allow(...)]`): for RecursiveByShapValues (and ≥1 of the FeatureEffect variants), run `select_features` and assert `selected_features` + `eliminated_features` EXACTLY match the committed upstream partition (set/order equality of feature indices — this is a discrete oracle, not a float tolerance). None `#[ignore]`d.

    Generate the fixture OFFLINE from `.venv` catboost 1.2.10: `m.select_features(pool, features_for_select=[...], num_features_to_select=k, algorithm="RecursiveByShapValues", steps=s, train_final_model=False, thread_count=1, random_seed=<pinned>)`, capture `{selected_features, eliminated_features}`; likewise for a FeatureEffect algorithm. Commit under `crates/cb-oracle/tests/fixtures/feature_selection/`. Pinned seed, thread_count=1. No fabrication. If the discrete partition diverges due to RNG draw-order in the inner retrains, escalate per the oracle discipline (instrumented trainer) rather than weaken — but use the simplest isolating params (no CTR/bootstrap, random_strength=0) to keep the inner-train draw stream deterministic.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test feature_selection_oracle_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train --test feature_selection_oracle_test` passes for RecursiveByShapValues + ≥1 FeatureEffect variant; none `#[ignore]`d.
    - `selected_features` + `eliminated_features` exactly match the committed catboost 1.2.10 partition.
    - Fixture is real catboost 1.2.10 output; no fabrication; no weakening.
  </acceptance_criteria>
  <done>select_features partition oracle-locked vs catboost 1.2.10 for ShapValues + FeatureEffect algorithms; FEAT-05 / SC-2 complete.</done>
</task>

</tasks>

<threat_model>
No new untrusted external-input surface (in-process orchestration over `cb_train::train` + `Pool` + already-validated importance methods; no model-file deserialization in this plan).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-19 | Input Validation | select_features options (features_for_select / num_features_to_select) | mitigate | Typed `CbError` on out-of-range feature indices and `num > len`; `.get(...)` access |
| T-06.6-20 | Denial of Service | recursive retrain loop | mitigate | `steps`/`num_features_to_select` bound the iteration count; no unbounded loop |
</threat_model>

<verification>
- `cargo test -p cb-train --lib` green.
- `cargo test -p cb-train --test feature_selection_oracle_test` green, none ignored.
- Phase gate: `cargo test --workspace` green (per-crate fallback under disk pressure per memory note) before `/gsd-verify-work`; no-regression locks (fstr_oracle_test, shap_oracle_test, non_symmetric_oracle_test, ordered/tensor-CTR e2e) all green.
- Grep: no new crate added; no `mod tests` in feature_selection.rs.
</verification>

<success_criteria>
FEAT-05 (SC-2) complete: recursive feature selection by ShapValues / PredictionValuesChange / LossFunctionChange oracle-locked vs catboost 1.2.10; NEW module, no new crate (D-6.6-03). Phase 6.6 fully closed (SC-1 06.6-01/02, SC-3 06.6-03/04/05, SC-4 06.6-06/07, SC-2 here); Region + non-symmetric-monotone documented as escalated gaps.
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-08-SUMMARY.md` when done.
</output>
