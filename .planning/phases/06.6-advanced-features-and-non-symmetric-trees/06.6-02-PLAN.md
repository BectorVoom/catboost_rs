---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 02
type: execute
wave: 2
depends_on: ["06.6-01"]
files_modified:
  - crates/cb-compute/src/leaf.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-compute/tests/monotone_oracle_test.rs
  - crates/cb-oracle/tests/fixtures/monotone/.gitkeep
autonomous: true
requirements: [FEAT-03]
must_haves:
  truths:
    - "A model trained with monotone_constraints=[+1/-1/0,...] under SymmetricTree produces leaf values matching upstream catboost 1.2.10 ≤1e-5 (D-6.6-06)."
    - "Training with monotone_constraints AND a non-symmetric grow_policy is rejected with a typed CbError (no fabricated output) — escalated gap recorded (D-6.6-07). The Region guard assertion is self-contained here; the non-symmetric-monotone guard assertion is ENABLED and completed in Plan 06.6-04 (after grow_policy lands)."
    - "A model with empty monotone_constraints is byte-identical to the pre-6.6 leaf-estimation path (D-6.6-05)."
  artifacts:
    - path: "crates/cb-compute/src/leaf.rs"
      provides: "calc_monotonic_leaf_deltas() isotonic PAVA projection (transcribed verbatim)"
      contains: "monotonic"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "monotone_constraints BoostParams field + typed guard rejecting non-symmetric monotone + Region"
      contains: "monotone_constraints"
    - path: "crates/cb-compute/tests/monotone_oracle_test.rs"
      provides: "monotone (oblivious) leaf-value oracle ≤1e-5"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-compute/src/leaf.rs"
      via: "monotone_constraints threaded into the leaf-delta post-pass"
      pattern: "calc_monotonic_leaf_deltas|monotone_constraints"
---

<objective>
Implement FEAT-03 — monotone constraints (per-feature +1/-1/0) for the OBLIVIOUS engine only, oracle-locked ≤1e-5 vs catboost 1.2.10. This is the second Gate-A strand (D-6.6-01). Monotone enforcement is an isotonic (PAVA) projection over leaf DELTAS during leaf estimation (`approx_calcer.cpp:552`), AFTER structure is built — a leaf-value post-pass, transcribed VERBATIM per D-6.6-06.

Purpose: Complete the symmetric-features-first Gate A. RESEARCH gate 1 RESOLVED: upstream catboost 1.2.10 EXPLICITLY rejects monotone under every non-symmetric policy (`monotonic_constraint_utils.h:42`) → monotone is OBLIVIOUS-ONLY; the non-symmetric monotone variant is an ESCALATED GAP (document-and-guard, never fabricate, D-6.6-07).
Output: `calc_monotonic_leaf_deltas()` in `leaf.rs`, a `monotone_constraints` BoostParams field, a typed guard rejecting unsupported combos, and `monotone_oracle_test.rs` locking oblivious monotone leaf values ≤1e-5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- `leaf.rs` functions: `calc_monotonic_leaf_deltas(...)` (transcribes `CalcOneDimensionalIsotonicRegression`) + the linear-order builder (`BuildMonotonicLinearOrdersOnLeafs`).
- `BoostParams` field: `monotone_constraints: Vec<i8>` (+1/-1/0 per feature) with paired `*_default()`.
- A typed `CbError` guard in `boosting.rs` rejecting `monotone_constraints` + non-symmetric `grow_policy`, and `grow_policy=Region` (escalated-gap enforcement, D-6.6-07).
- New oracle test file: `crates/cb-compute/tests/monotone_oracle_test.rs`.
- New fixture directory: `crates/cb-oracle/tests/fixtures/monotone/` (OFFLINE catboost 1.2.10).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verbatim isotonic (PAVA) leaf-delta projection + monotone_constraints param + escalated-gap guard</name>
  <files>crates/cb-compute/src/leaf.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-compute/src/leaf.rs:1-58 (module doc convention: each fn transcribes a cited upstream source, uses `const DBL_EPSILON`, folds via `sum_f64`, `pub fn` returning scalar/Vec, NO embedded tests)
    - crates/cb-compute/src/leaf.rs:114-420 (`gradient_leaf_delta` ~114, `exact_leaf_delta` ~367 — the "transcribe an upstream optimum verbatim, sum via cb_core::sum_f64" shape to mirror)
    - catboost-master/catboost/private/libs/algo/monotonic_constraint_utils.cpp:4-52 (`BuildLinearOrderOnLeafsOfMonotonicSubtree` + `BuildMonotonicLinearOrdersOnLeafs` — bitmask construction, leaf-index XOR encoding; produces 2^(#non-monotone-splits) orders over 2^(#monotone-splits) leaves)
    - catboost-master/catboost/private/libs/algo/monotonic_constraint_utils.cpp:94-117 (`CalcOneDimensionalIsotonicRegression` — PAVA merge rule: `while last.average() >= new.average() { merge_left }`; weights = `SumWeights+λ` for Gradient, `-SumDer2+λ` for Newton)
    - catboost-master/catboost/private/libs/algo/approx_calcer.cpp:552 (`CalcMonotonicLeafDeltasSimple` — the leaf-estimation insertion point; projection runs on DELTAS after structure is built)
    - catboost-master/catboost/private/libs/algo/monotonic_constraint_utils.h:42 (`CB_ENSURE_INTERNAL(monotoneConstraints.empty(), "...unsupported for non-symmetric trees yet")` — the exact upstream rejection to mirror)
    - crates/cb-train/src/boosting.rs:88-340 (BoostParams field + `*_default()` convention) and boosting.rs:971 (`validate_leaf_method` — the typed-`CbResult` up-front rejection pattern to mirror for the guard)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Pattern 4 + Pitfall 2 — oblivious-only; non-symmetric throws)
  </read_first>
  <behavior>
    - calc_monotonic_leaf_deltas: given current leaf values + computed deltas + per-leaf weights + the monotone-direction map, for each monotonic linear order run PAVA so the projected (currLeafValues + deltas) are non-decreasing (+1) / non-increasing (-1) along the order. Level-set merge: `while last.average() >= new.average() { merge_left }`; averages via `sum_f64`.
    - Direction -1 handled by negating along the order (or mirroring the merge inequality) exactly as upstream.
    - Empty monotone_constraints → returns deltas unchanged (no-op), leaf path byte-identical.
    - Guard: `monotone_constraints` non-empty AND grow_policy != SymmetricTree → typed CbError; grow_policy == Region → typed CbError (escalated gaps, no fabricated output).
  </behavior>
  <action>
    In leaf.rs add `pub fn calc_monotonic_leaf_deltas(...)` transcribing `CalcOneDimensionalIsotonicRegression` (monotonic_constraint_utils.cpp:94-117) VERBATIM, plus the linear-order construction from `BuildMonotonicLinearOrdersOnLeafs` (lines 4-52) — transcribe the bitmask / leaf-index XOR encoding exactly. Use `const DBL_EPSILON` per the module convention; ALL averages/sums via `cb_core::sum_f64` (D-08, D-6.6-06). Weight per leaf = `SumWeights+λ` (Gradient) or `-SumDer2+λ` (Newton), threaded from the caller. `pub fn`, no embedded tests.

    Add `BoostParams.monotone_constraints: Vec<i8>` (default empty) with the doc-comment + `monotone_constraints_default()` convention. Thread it into the oblivious leaf-estimation post-pass: after the existing per-leaf delta computation and gated on non-empty `monotone_constraints` AND oblivious variant, apply `calc_monotonic_leaf_deltas`. The empty-constraints path MUST be numerically identical to today.

    Add the escalated-gap guard (mirror `validate_leaf_method`, boosting.rs:971; mirror upstream `monotonic_constraint_utils.h:42`): reject `monotone_constraints` non-empty with `grow_policy != SymmetricTree` via typed `CbError`, and reject `grow_policy == Region` via typed `CbError`. These guards make the no-ground-truth combinations error rather than fabricate output (D-6.6-07). (Note: `grow_policy` enum/field is added in plan 06.6-04; if it does not yet exist at execution time, gate the guard behind the field once present and add a doc-comment TODO referencing 06.6-04 — do NOT invent a partial enum here that conflicts with 04.)

    Keep monotone strictly OBLIVIOUS-ONLY. NO non-symmetric monotone code path is written (Pitfall 2 — no upstream ground truth).
  </action>
  <verify>
    <automated>cargo test -p cb-compute --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-compute` and `-p cb-train` succeed; `calc_monotonic_leaf_deltas` exists in leaf.rs transcribing the cited upstream lines.
    - `monotone_constraints` field + `*_default()` exist; empty-constraints leaf path is byte-identical (existing cb-compute + cb-train lib tests green).
    - All reductions in the new fn route through `cb_core::sum_f64` (grep: no bare `iter().sum()` / `fold(0.0` in the new code).
    - No `#[cfg(test)]`/`mod tests` added to leaf.rs or boosting.rs.
  </acceptance_criteria>
  <done>Isotonic leaf-delta projection transcribed verbatim and wired into oblivious leaf estimation; monotone_constraints param + escalated-gap guard added; default path byte-identical; cb-compute/cb-train lib suites green.</done>
</task>

<task type="auto">
  <name>Task 2: monotone_oracle_test.rs — oblivious monotone leaf values ≤1e-5 + self-contained Region guard assertion (non-symmetric-monotone guard completed in 06.6-04)</name>
  <files>crates/cb-compute/tests/monotone_oracle_test.rs, crates/cb-oracle/tests/fixtures/monotone/.gitkeep</files>
  <read_first>
    - crates/cb-model/tests/apply_oracle_test.rs:1-40 (oracle skeleton + top-line `#![allow(...)]`)
    - crates/cb-oracle/src/{fixture,compare,model_json}.rs (Stage enum + compare_stage harness + model.json leaf_values parse)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Code Examples → `m.fit(..., monotone_constraints=[1,0,-1], thread_count=1)` — SymmetricTree only; Validation §"lock SPLITS before LEAF VALUES" — here structure is unaffected so the lock target is LEAF VALUES)
  </read_first>
  <action>
    Author `crates/cb-compute/tests/monotone_oracle_test.rs` (apply_oracle_test structure, top-line `#![allow(...)]`). One test trains a small float model with `monotone_constraints` (mix of +1, 0, -1) under SymmetricTree and asserts LeafValues + StagedApprox + Predictions ≤1e-5 via `compare_stage` against the frozen catboost 1.2.10 fixture (splits are unchanged by monotone — assert them too as a sanity lock). Add a second test asserting the SELF-CONTAINED Region typed-error guard: constructing/fitting with `grow_policy=Region` returns a `CbError` (this guard is reachable now and does NOT depend on a non-symmetric grow_policy discriminator). Document the non-symmetric-monotone + Region escalated gaps in the SUMMARY (no fabricated fixture, D-6.6-07).

    IMPORTANT — deferred-assertion ownership (no silent drop): the non-symmetric-monotone typed-error guard assertion (monotone_constraints × {Lossguide,Depthwise}) is DEFERRED here because the `grow_policy` field does not exist until Plan 06.6-04. Plan 06.6-04 (Task 3) OWNS enabling and passing that assertion in THIS file once `grow_policy` lands; it is listed in 06.6-04's `files_modified` and acceptance criteria. Leave a clearly-commented stub/TODO in this file pointing to 06.6-04 so it is not lost. Do NOT `#[ignore]` a written-but-failing assertion — either write only the Region guard now, or leave the non-symmetric-monotone assertion as a documented commented stub for 06.6-04 to enable.

    Generate the fixture OFFLINE from `.venv` catboost 1.2.10: `m.fit(Pool(X,y), monotone_constraints=[1,0,-1], thread_count=1, random_seed=<pinned>)`, commit under `crates/cb-oracle/tests/fixtures/monotone/`. No tolerance weakening, no `#[ignore]`, no fabrication.
  </action>
  <verify>
    <automated>cargo test -p cb-compute --test monotone_oracle_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-compute --test monotone_oracle_test` passes, none `#[ignore]`d.
    - Monotone leaf values match catboost 1.2.10 ≤1e-5; the projected leaf values are demonstrably monotone along the constrained feature.
    - The self-contained Region escalated-gap guard test passes (typed error on `grow_policy=Region`).
    - The non-symmetric-monotone guard assertion (monotone × {Lossguide,Depthwise}) is explicitly DEFERRED to Plan 06.6-04 (Task 3) — recorded as a commented TODO/stub in this file and as a 06.6-04 acceptance criterion — so it is not silently dropped. FEAT-03 SC-1 Region portion remains self-contained here.
    - Fixture is real catboost 1.2.10 output; no fabricated ground truth.
  </acceptance_criteria>
  <done>Oblivious monotone leaf values oracle-locked ≤1e-5; the self-contained Region guard is asserted here; the non-symmetric-monotone guard assertion is explicitly owned by Plan 06.6-04 (no silent drop); Gate A (SC-1) monotone portion complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training param → leaf estimation | `monotone_constraints` / `grow_policy` combo validated up front; unsupported combos rejected (no fabricated output) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-03 | Tampering | isotonic average summation | mitigate | Reductions via `cb_core::sum_f64` (D-08) — exact level-set merge order required for ≤1e-5 parity |
| T-06.6-04 | Denial of Service | monotone_constraints feature indexing | mitigate | `.get(...)` bounds-checked access (project lint deny indexing_slicing) |
| T-06.6-05 | Tampering | unsupported monotone×non-symmetric / Region combos | mitigate | Typed `CbError` rejection mirroring upstream `CB_ENSURE_INTERNAL` — no fabricated leaf values (Region guard asserted here; non-symmetric-monotone guard asserted in 06.6-04) |

No new untrusted external-input surface (in-process training params only; no model file deserialization here).
</threat_model>

<verification>
- `cargo test -p cb-compute --lib` and `cargo test -p cb-train --lib` green (empty-constraints path byte-identical).
- `cargo test -p cb-compute --test monotone_oracle_test` green ≤1e-5, none ignored (Region guard asserted; non-symmetric-monotone guard owned by 06.6-04).
- Grep: new leaf.rs reductions use `sum_f64`; no `mod tests` in leaf.rs/boosting.rs.
</verification>

<success_criteria>
FEAT-03 monotone constraints (oblivious) oracle-locked ≤1e-5 vs catboost 1.2.10; the Region escalated gap is recorded and enforced by a self-contained typed-error guard here; the non-symmetric-monotone escalated-gap guard assertion is explicitly handed to Plan 06.6-04 (enabled once grow_policy lands) — not dropped; empty-constraints path byte-identical; ROADMAP SC-1 monotone portion satisfied (with FEAT-04 from 06.6-01, SC-1 complete).
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-02-SUMMARY.md` when done.
</output>
