---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 04
type: execute
wave: 4
depends_on: ["06.6-02", "06.6-03"]
files_modified:
  - crates/cb-train/src/tree.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-model/src/model.rs
autonomous: true
requirements: [FEAT-06]
must_haves:
  truths:
    - "grow_policy=Depthwise builds a level-order non-symmetric tree whose SPLITS match upstream catboost 1.2.10 (Open Question 1 — splits locked first)."
    - "grow_policy=Lossguide builds a best-gain priority-queue non-symmetric tree whose SPLITS match upstream, replicating the upstream queue-penalty NON-recompute (Pitfall 4)."
    - "grow_policy=SymmetricTree dispatch is the LITERAL existing oblivious call — byte-identical (D-6.6-05)."
    - "from_trained lifts the non-symmetric grower output into the TreeVariant::NonSymmetric variant; the oblivious lift path is untouched."
  artifacts:
    - path: "crates/cb-train/src/tree.rs"
      provides: "leaf_wise_grower(policy) producing a non-symmetric node graph (Depthwise + Lossguide)"
      contains: "leaf_wise_grower"
    - path: "crates/cb-train/src/boosting.rs"
      provides: "grow_policy/max_leaves/min_data_in_leaf BoostParams fields + grow_policy dispatch"
      contains: "grow_policy"
    - path: "crates/cb-model/src/model.rs"
      provides: "from_trained lift of the non-symmetric grower output"
      contains: "NonSymmetric"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/tree.rs"
      via: "grow_policy match dispatches SymmetricTree→existing | Lossguide|Depthwise→leaf_wise_grower"
      pattern: "leaf_wise_grower|grow_policy"
---

<objective>
Gate-B core (FEAT-06, D-6.6-04): build ONE policy-parameterized leaf-wise grower producing TRUE non-symmetric trees for Lossguide + Depthwise (Region OUT — CPU-unimplemented upstream, escalated gap). Mirrors upstream's shared leaf-wise scoring core with the split queue / expansion strategy parameterized by policy — NOT three separate growers. Lock SPLITS first (Open Question 1) before leaf values.

Purpose: The load-bearing structural item. Upstream uses one shared scoring core (`leafwise_scoring.cpp`) + two policy growers (`GreedyTensorSearchDepthwise`, `GreedyTensorSearchLossguide`) dispatched in `GreedyTensorSearch`. The SymmetricTree dispatch arm stays the literal existing oblivious call (byte-identical, D-6.6-05). Leaf VALUES + apply round-trip e2e land in 06.6-05; this plan locks the STRUCTURE.
Output: `leaf_wise_grower(policy)` in `tree.rs` (node graph, reuses `score_candidate`/`select_best_candidate`/candidate enumeration verbatim); `grow_policy`/`max_leaves`/`min_data_in_leaf` BoostParams + dispatch; `from_trained` lift into `TreeVariant::NonSymmetric`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-CONTEXT.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md
@.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends:
- `tree.rs`: `leaf_wise_grower(policy, ...)` producing a non-symmetric node graph; Depthwise (level-order queue) + Lossguide (best-gain priority queue) expansion strategies behind one core; new optional `GrownTree` fields (`step_nodes`/`node_id_to_leaf_id` or a `variant` enum) following the existing empty-parallel-vector pattern.
- `boosting.rs`: `grow_policy: EGrowPolicy`-equivalent (SymmetricTree | Lossguide | Depthwise), `max_leaves: usize`, `min_data_in_leaf: usize` BoostParams fields (+ paired `*_default()`); `train_inner` grower dispatch `match params.grow_policy`.
- `model.rs`: `from_trained` lift of the non-symmetric grower output into `TreeVariant::NonSymmetric`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: grow_policy BoostParams + dispatch + leaf_wise_grower(policy) producing a non-symmetric node graph</name>
  <files>crates/cb-train/src/tree.rs, crates/cb-train/src/boosting.rs</files>
  <read_first>
    - crates/cb-train/src/tree.rs:196-216 (`GrownTree` — already carries parallel optional vectors `ctr_splits`/`level_kinds` empty for searches that don't use them; add `step_nodes`/`node_id_to_leaf_id` the same way so existing callers compile unchanged)
    - crates/cb-train/src/tree.rs:526-595 (`greedy_tensor_search_oblivious_perturbed` 526 + `select_level_plain` 573 — the fixed-depth level-loop the leaf-wise grower replaces with a policy queue; candidate enumeration 582-595 = float features ascending × borders ascending through `score_candidate`, REUSE verbatim)
    - crates/cb-train/src/tree.rs:274-285 (`select_best_candidate` strict-`>` first-wins — DO NOT change; reuse for per-leaf best split)
    - crates/cb-train/src/boosting.rs:88-340 (BoostParams field + `*_default()` convention) and boosting.rs:2859 (`train_inner` grower dispatch site `greedy_tensor_search_oblivious_with_ctr(...)`) and boosting.rs:971 (`validate_leaf_method` — typed-CbResult up-front rejection pattern)
    - catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp:1509-1700 (`GreedyTensorSearchDepthwise`: `for curDepth { for leaf in level { CalculateStats; if gain<1e-9 continue; AddSplit; push left/right } }`; children appended (left,right), id%2 even/odd subtract-trick)
    - catboost-master/catboost/private/libs/algo/greedy_tensor_search.cpp:1806-1938 (`GreedyTensorSearchLossguide`: max-priority-queue of (leaf,gain,best_candidate,stats); pop best-gain, split, push children; stop on empty OR leaf_count>=max_leaves; gain<1e-9 + leafDepth<max_depth gate; LINES 1875-1882 — queued candidates NOT recomputed when a feature first becomes used: REPLICATE the non-recompute exactly, Pitfall 4)
    - catboost-master/catboost/private/libs/algo/enums.h (`EGrowPolicy { SymmetricTree, Lossguide, Depthwise, Region }`)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-PATTERNS.md (tree.rs grower skeleton + "RESEARCH-specific gotchas" + GrownTree empty-parallel-vector precedent)
  </read_first>
  <action>
    Add a `grow_policy` enum field to BoostParams (`SymmetricTree | Lossguide | Depthwise`; Region exists in the enum for parity but is REJECTED by the guard from 06.6-02 — no grower arm), plus `max_leaves: usize` (`max_leaves_default()` per upstream) and `min_data_in_leaf: usize`. Doc-comment + paired `*_default()` per the convention; cite upstream defaults; "Pinned EXPLICITLY, never auto-selected".

    In tree.rs add `leaf_wise_grower(policy, ...)` producing a non-symmetric node graph (flat node array → `step_nodes` `(left_subtree_diff,right_subtree_diff)` + `node_id_to_leaf_id`). ONE core parameterized by `policy`:
    - Depthwise: level-order queue — `for cur_depth in 0..max_depth { for leaf in current_level { compute per-leaf stats; best = select_best_candidate(per-leaf candidates); if gain < 1e-9 continue; add split; push left,right to next level } }` (greedy_tensor_search.cpp:1509-1700).
    - Lossguide: a max-priority-queue keyed on per-leaf best gain — pop best-gain leaf, split it, enqueue its two children's best candidates; stop when queue empty OR `leaf_count >= max_leaves`; gate on `gain < 1e-9` and `leaf_depth < max_depth` (greedy_tensor_search.cpp:1806-1938). REPLICATE the upstream queue-penalty NON-recompute (lines 1875-1882) exactly — penalties applied at ENQUEUE time only, never recomputed when a feature first becomes used (Pitfall 4).

    Reuse `score_candidate`, the candidate enumeration (tree.rs:582-595), and `select_best_candidate` (strict-`>`, tree.rs:274-285) VERBATIM — only the queue/expansion differs by policy. Add the new node-graph fields to `GrownTree` following the existing empty-parallel-vector precedent (tree.rs:196-216) so the oblivious grower leaves them empty and existing callers compile unchanged. All reductions via `cb_core::sum_f64` (D-08); `.get(...)` access; NO embedded tests.

    In `train_inner` (boosting.rs:2859) wrap the grower call in `match params.grow_policy { SymmetricTree => <the LITERAL existing greedy_tensor_search_oblivious_with_ctr(...) call, UNCHANGED>, Lossguide | Depthwise => leaf_wise_grower(policy, ...) }`. The SymmetricTree arm MUST be byte-identical (D-6.6-05). Lock SPLITS first per Open Question 1 — leaf VALUES + apply are 06.6-05; emit the structure (splits + node graph) here.
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-train` succeeds; `leaf_wise_grower` exists with Depthwise + Lossguide expansion; `grow_policy`/`max_leaves`/`min_data_in_leaf` fields + `*_default()` exist.
    - `train_inner` dispatches on `grow_policy`; the SymmetricTree arm is the literal existing call (grep the existing call text is preserved).
    - Lossguide enqueue-time-only penalty replication is implemented (no recompute on first-use); a comment cites greedy_tensor_search.cpp:1875-1882.
    - All existing cb-train lib tests stay green (SymmetricTree path byte-identical, D-6.6-05).
    - No `mod tests`/`#[cfg(test)]` in tree.rs/boosting.rs.
  </acceptance_criteria>
  <done>The policy-parameterized leaf-wise grower builds non-symmetric node graphs for Depthwise + Lossguide; grow_policy dispatch added with the SymmetricTree arm byte-identical; cb-train lib suite green.</done>
</task>

<task type="auto">
  <name>Task 2: from_trained lift of the non-symmetric grower output into TreeVariant::NonSymmetric + splits-first oracle lock</name>
  <files>crates/cb-model/src/model.rs</files>
  <read_first>
    - crates/cb-model/src/model.rs:165-211 (`from_trained` lifting: float splits → `ModelSplit::Float`, CTR splits pushed; the oblivious lift to leave untouched)
    - crates/cb-model/src/model.rs:106-116 (`ObliviousTree`) + the `NonSymmetricTree`/`TreeVariant` added in 06.6-03
    - crates/cb-train/src/tree.rs:196-216 (`GrownTree` non-symmetric fields produced by Task 1)
    - crates/cb-model/tests/non_symmetric_oracle_test.rs (the splits-first harness from 06.6-03 — this task turns its SPLITS assertions green)
    - .planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-RESEARCH.md (Open Question 1 — splits locked before leaf values)
  </read_first>
  <action>
    Extend `from_trained` (model.rs:165-211) to lift the non-symmetric grower output (`GrownTree` with `step_nodes`/`node_id_to_leaf_id` populated) into `TreeVariant::NonSymmetric`, mapping its splits to `ModelSplit::Float`/`ModelSplit::Ctr` exactly as the oblivious lift does, and carrying `step_nodes`/`node_id_to_leaf_id`. Branch on whether the grown tree is oblivious or non-symmetric (the grower from Task 1 sets the discriminator). The oblivious lift path stays byte-identical (D-6.6-05).

    Run the 06.6-03 `non_symmetric_oracle_test.rs` and confirm the SPLITS stage now passes ≤1e-5 / integer-exact for both Depthwise and Lossguide (Open Question 1 — splits locked first). LeafValues/StagedApprox/Predictions and the round-trip will turn green in 06.6-05; if leaf values already happen to match, that is fine, but the HARD gate for THIS plan is SPLITS. Do NOT `#[ignore]`, do NOT weaken tolerance. If splits diverge, escalate to the persistent instrumented trainer per Open Question 1 (draw-stream divergence) rather than weaken.
  </action>
  <verify>
    <automated>cargo test -p cb-model --test non_symmetric_oracle_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `from_trained` lifts the non-symmetric grower output into `TreeVariant::NonSymmetric`; oblivious lift unchanged.
    - The non_symmetric_oracle_test SPLITS assertions pass for both Depthwise and Lossguide (integer-exact / ≤1e-5 vs catboost 1.2.10).
    - No tolerance weakened, no `#[ignore]`; the existing cb-model lib + oblivious oracle tests stay green.
  </acceptance_criteria>
  <done>Non-symmetric grower output is lifted into the canonical model and its tree STRUCTURE (splits + node graph) is oracle-locked vs catboost 1.2.10 for Depthwise + Lossguide; leaf values + apply round-trip pending 06.6-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training params → grower | `max_leaves`/`min_data_in_leaf`/`grow_policy` validated; Region rejected (guard from 06.6-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-09 | Denial of Service | leaf-wise node-graph construction | mitigate | `max_leaves`/`max_depth` bound the queue; `.get(...)` access; no unbounded growth |
| T-06.6-10 | Tampering | per-leaf stat / gain summation | mitigate | Reductions via `cb_core::sum_f64` (D-08) — split-score parity demands exact order |
| T-06.6-11 | Input Validation | grow_policy=Region | mitigate | Typed `CbError` (guard from 06.6-02) — no grower arm, no fabricated structure |

No new untrusted external-input surface in this plan (in-process training; model deserialization is handled in 06.6-03/05). Region/non-symmetric-monotone remain escalated gaps enforced by typed errors.
</threat_model>

<verification>
- `cargo test -p cb-train --lib` green (SymmetricTree path byte-identical).
- `cargo test -p cb-model --test non_symmetric_oracle_test` — SPLITS stage green for Depthwise + Lossguide; none ignored.
- Grep: SymmetricTree dispatch arm is the literal existing call; Lossguide non-recompute comment cites lines 1875-1882; no `mod tests` added.
</verification>

<success_criteria>
The unified leaf-wise grower (Lossguide + Depthwise) produces true non-symmetric trees whose SPLITS match catboost 1.2.10; SymmetricTree dispatch is byte-identical; Region is rejected (escalated gap). The structural half of FEAT-06 (SC-3) is locked; leaf values + apply round-trip complete in 06.6-05.
</success_criteria>

<output>
Create `.planning/phases/06.6-advanced-features-and-non-symmetric-trees/06.6-04-SUMMARY.md` when done.
</output>
