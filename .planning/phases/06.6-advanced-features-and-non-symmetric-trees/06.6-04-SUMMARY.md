---
phase: 06.6-advanced-features-and-non-symmetric-trees
plan: 04
subsystem: training
tags: [non-symmetric-trees, depthwise, lossguide, leaf-wise-grower, grow-policy, oracle, feat-06]

# Dependency graph
requires:
  - phase: 06.6-advanced-features-and-non-symmetric-trees
    plan: 02
    provides: validate_monotone_constraints guard + the deferred grow_policy×monotone / Region guard stub now enabled here
  - phase: 06.6-advanced-features-and-non-symmetric-trees
    plan: 03
    provides: TreeVariant/NonSymmetricTree representation + non_symmetric .cbm/json serde + splits-first oracle harness this grower turns green
  - phase: 03-cpu-training-core
    provides: oblivious greedy search (score_candidate / select_best_candidate / candidate enumeration) reused verbatim by the leaf-wise core
provides:
  - "cb_train::leaf_wise_grower(policy) — ONE policy-parameterized core producing a TRUE non-symmetric node graph (Depthwise level-order + Lossguide best-gain priority queue)"
  - "EGrowPolicy {SymmetricTree, Lossguide, Depthwise, Region} + grow_policy/max_leaves/min_data_in_leaf BoostParams fields + *_default()"
  - "train_inner grow_policy dispatch (SymmetricTree→literal oblivious chain byte-identical; Lossguide|Depthwise→leaf_wise_grower)"
  - "cb_train::Model.non_symmetric_trees + cb_train::NonSymmetricTree (structure + leaf values)"
  - "cb_model::Model::from_trained lift of the non-symmetric grower output into TreeVariant::NonSymmetric"
  - "validate_grow_policy typed guard (Region + monotone×non-symmetric rejection — the escalated gaps deferred from 06.6-02, now enabled)"
  - "simplest-Depthwise SPLITS preflight + train→lift SPLITS oracle (Open Question 1 RESOLVED: leaf-wise draw stream matches catboost 1.2.10)"
affects: [06.6-05-apply-leaf-values, 06.6-06-fstr-shap]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Policy-parameterized leaf-wise grower: ONE shared scoring core (best_split_for_leaf) + a per-policy expansion strategy (level-order vs best-gain priority queue), mirroring upstream's leafwise_scoring.cpp + two policy growers"
    - "GrownTree empty-parallel-vector precedent extended: step_nodes/node_id_to_leaf_id empty for oblivious searches so existing callers compile unchanged (D-6.6-05)"
    - "grow_policy match dispatch with the SymmetricTree arm a byte-identical literal of the existing oblivious grower chain"

key-files:
  created:
    - crates/cb-oracle/generator/gen_depthwise_simplest_fixture.py
    - crates/cb-oracle/fixtures/non_symmetric/depthwise_simplest/{X,y,staged,predictions}.npy + model.{json,cbm} + meta.json
    - crates/cb-train/tests/non_symmetric_grower_oracle_test.rs
  modified:
    - crates/cb-train/src/tree.rs
    - crates/cb-train/src/boosting.rs
    - crates/cb-train/src/lib.rs
    - crates/cb-model/src/model.rs
    - crates/cb-model/tests/non_symmetric_oracle_test.rs
    - crates/cb-train/tests/monotone_oracle_test.rs
    - "38 BoostParams construction sites (tests + catboost-rs builder)"

key-decisions:
  - "The leaf-wise grower SPLITS oracle + monotone-guard test live in cb-train/tests/ (not cb-compute/tests/ as the plan's Task 3 path listed): only cb-train can TRAIN. This is the SAME Rule-3 placement 06.6-02 established for monotone_oracle_test.rs (cb-compute has no training entry point)."
  - "The simplest-Depthwise fixture pins score_function='L2' (catboost DEFAULTS to Cosine). The first grower oracle run diverged (5 vs 6 splits) purely because the fixture was generated with the Cosine default while cb-train's leaf-wise oracle pins L2 — the known 'cb-train uses L2 but catboost defaults Cosine' gap. Pinning L2 in the generator made splits match exactly: Open Question 1 RESOLVED, the draw stream MATCHES, no instrumented-trainer escalation needed."
  - "SPLITS oracle compares the SORTED MULTISET of split borders: upstream's nested-trees JSON is pre-order DFS while our AddSplit order is level-order (Depthwise); the node visitation order is a representation detail, not a draw-stream divergence (what the gate catches is a WRONG border/feature)."
  - "Leaf VALUES + the apply pointer-walk round-trip are explicitly DEFERRED to 06.6-05 (the plan's scope boundary). This plan locks the STRUCTURE (splits + node graph); the cb-model non_symmetric_oracle_test full ladder still fails at LeafValues by design (Wave-0 contract from 06.6-03)."

patterns-established:
  - "best_split_for_leaf: per-leaf 2-leaf L2 score over a document subset; gain = best_2leaf_score − unsplit_score; gated on docs ≥ min_data_in_leaf AND gain ≥ 1e-9 (shared by both policies, the upstream gain<1e-9 cutoff)"
  - "Lossguide priority queue: BinaryHeap keyed on (gain, reverse-seq) for best-gain-first with stable first-enqueued tie-break; stop on empty queue OR leaf_count ≥ max_leaves; enqueue-time-only penalties (non-recompute, Pitfall 4)"

requirements-completed: []

# Metrics
duration: ~95min
completed: 2026-06-18
---

# Phase 06.6 Plan 04: Non-Symmetric Leaf-Wise Grower (FEAT-06 structure) Summary

**ONE policy-parameterized `leaf_wise_grower` building TRUE non-symmetric trees (Depthwise level-order + Lossguide best-gain priority queue) whose SPLITS match catboost 1.2.10 ≤1e-5, dispatched behind `grow_policy` with the SymmetricTree arm byte-identical, lifted into `TreeVariant::NonSymmetric` via `from_trained`; the simplest-Depthwise SPLITS were locked as a failing-harness preflight BEFORE the grower (Open Question 1 RESOLVED — draw stream matches), and the deferred Region + monotone×non-symmetric typed-error guards are enabled and green.**

## Performance
- **Duration:** ~95 min
- **Completed:** 2026-06-18
- **Tasks:** 3
- **Files modified:** 5 src/test + 1 new test + 1 generator + 7 fixture files + 38 BoostParams construction sites

## Accomplishments
- `EGrowPolicy {SymmetricTree, Lossguide, Depthwise, Region}` + `grow_policy`/`max_leaves`/`min_data_in_leaf` BoostParams fields with paired `*_default()` (SymmetricTree / 31 / 1, pinned EXPLICITLY, never auto).
- `leaf_wise_grower(policy, …)` in `tree.rs`: ONE shared scoring core (`best_split_for_leaf` — candidate enumeration float-asc×border-asc + strict first-wins, reused verbatim) behind two policy expansion strategies — Depthwise level-order (`greedy_tensor_search.cpp:1509`) + Lossguide best-gain priority queue (`:1806`, enqueue-time-only penalty non-recompute cites `:1875-1882`). Produces the flat non-symmetric node graph (`step_nodes` `(left_diff,right_diff)` + `node_id_to_leaf_id`) + a distinct-leaf `leaf_of`.
- `GrownTree` gains `step_nodes`/`node_id_to_leaf_id` following the empty-parallel-vector precedent (oblivious callers unchanged).
- `train_inner` dispatch: `match params.grow_policy { SymmetricTree|Region => <LITERAL existing oblivious chain, byte-identical D-6.6-05>, Lossguide|Depthwise => leaf_wise_grower }`; per-tree `n_leaves` shadow for the non-symmetric leaf count.
- `cb_train::Model.non_symmetric_trees: Vec<NonSymmetricTree>` (structure + leaf values), populated on the leaf-wise path.
- `cb_model::Model::from_trained` lifts the non-symmetric grower output into `TreeVariant::NonSymmetric`-shaped `NonSymmetricTree` (per-node float splits → `ModelSplit::Float`, node graph carried verbatim); oblivious lift byte-identical.
- `validate_grow_policy` typed guard rejecting `grow_policy=Region` + `monotone_constraints × {Lossguide,Depthwise}` (the escalated gaps DEFERRED from 06.6-02, now enabled).
- Early simplest-Depthwise SPLITS preflight (`depthwise_simplest_splits`, cb-model) + train→lift SPLITS oracle (`non_symmetric_depthwise_grower_splits_match_upstream`, cb-train) — both green ≤1e-5; the two monotone-guard assertions enabled and green.

## Task Commits
1. **Task 1: simplest-Depthwise SPLITS preflight gate + fixture** — `bc44e4b` (test)
2. **Task 2: grow_policy BoostParams + dispatch + leaf_wise_grower** — `5fa5c1f` (feat)
3. **Task 3: from_trained non-symmetric lift + SPLITS oracle + monotone guards** — `64dd5bd` (feat)

## Files Created/Modified
- `crates/cb-train/src/tree.rs` — `LeafWisePolicy`, `leaf_wise_grower`, `best_split_for_leaf`, `unsplit_leaf_score`, `GrownTree` non-symmetric fields.
- `crates/cb-train/src/boosting.rs` — `EGrowPolicy` + `grow_policy`/`max_leaves`/`min_data_in_leaf` fields + defaults; `validate_grow_policy`; `train_inner` dispatch + per-tree `n_leaves`; `NonSymmetricTree` + `Model.non_symmetric_trees`.
- `crates/cb-train/src/lib.rs` — re-exports (`EGrowPolicy`, `leaf_wise_grower`, `LeafWisePolicy`, `NonSymmetricTree`, the 3 defaults).
- `crates/cb-model/src/model.rs` — `from_trained` non-symmetric lift.
- `crates/cb-model/tests/non_symmetric_oracle_test.rs` — `depthwise_simplest_splits` preflight.
- `crates/cb-train/tests/non_symmetric_grower_oracle_test.rs` — train→lift SPLITS oracle.
- `crates/cb-train/tests/monotone_oracle_test.rs` — enabled the two deferred guard assertions.
- `crates/cb-oracle/generator/gen_depthwise_simplest_fixture.py` + `crates/cb-oracle/fixtures/non_symmetric/depthwise_simplest/*` — the committed simplest fixture (L2-pinned).
- 38 `BoostParams` construction sites — appended the 3 new fields via `*_default()`.

## Decisions Made
See frontmatter `key-decisions`. The load-bearing one: the simplest-Depthwise SPLITS divergence on the FIRST grower run (5 vs 6 splits) was NOT a draw-stream bug — it was the catboost-default-Cosine vs cb-train-L2 score-function mismatch (the fixture was generated without pinning `score_function`). Pinning `score_function='L2'` in the generator made the SPLITS match exactly, **resolving RESEARCH Open Question 1 empirically**: the leaf-wise Depthwise draw stream reproduces catboost 1.2.10. No instrumented-trainer escalation was required.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Test location: cb-train/tests/ not cb-compute/tests/**
- **Found during:** Task 3
- **Issue:** The plan's Task 3 `files` listed `crates/cb-compute/tests/monotone_oracle_test.rs`, but the monotone harness (and the `// TODO(06.6-04)` stub) actually live in `crates/cb-train/tests/monotone_oracle_test.rs` (06.6-02 moved it there because cb-compute has no training entry point). The train→lift SPLITS oracle likewise needs cb-train.
- **Fix:** Enabled the deferred guard assertions in `cb-train/tests/monotone_oracle_test.rs` and authored the train→lift SPLITS oracle as `cb-train/tests/non_symmetric_grower_oracle_test.rs`. The cb-model `non_symmetric_oracle_test.rs` keeps the `.cbm`-decode SPLITS preflight (representation lock, valid there).
- **Committed in:** `bc44e4b`, `64dd5bd`

**2. [Rule 1 - Bug / non-vacuous oracle] simplest-Depthwise fixture pins score_function='L2'**
- **Found during:** Task 3 (first grower oracle run diverged 5 vs 6 splits)
- **Issue:** The Task-1 generator did not pin `score_function`, so catboost used its DEFAULT (Cosine). cb-train's leaf-wise grower oracle pins L2, producing a genuine split divergence on the 29-doc right child of tree 0 (Cosine vs L2 rank the borders differently).
- **Fix:** Pinned `score_function='L2'` in `gen_depthwise_simplest_fixture.py`, added `y.npy`, regenerated. The grower SPLITS then matched upstream exactly (≤1e-5). This is the documented Open-Question-1 empirical check working as designed (an EARLY divergence caught and root-caused to a param mismatch, not a draw-stream bug).
- **Committed in:** `64dd5bd`

### Deferred (owned by Plan 06.6-05)
**3. [Plan scope boundary] Leaf VALUES + apply pointer-walk**
- The plan scopes 06.6-04 to STRUCTURE (splits + node graph). Leaf VALUES and the non-symmetric apply pointer-walk round-trip are 06.6-05. The cb-model `non_symmetric_oracle_test` full ladder (depthwise/lossguide) still fails at LeafValues/StagedApprox by design (Wave-0 contract from 06.6-03) — NOT `#[ignore]`d.

---
**Total deviations:** 2 auto-fixed (1 blocking placement, 1 fixture-param bug) + 1 documented scope-boundary deferral handed to 06.6-05.

## Issues Encountered
The Cosine-vs-L2 fixture-param mismatch (deviation 2) was the only non-obvious blocker; the simplest-Depthwise preflight + per-leaf gain instrumentation surfaced it immediately (29-doc leaf gain=0 under L2 der), and diffing against the known memory note pinpointed the catboost default. This is exactly the early-warning value the Task-1 preflight was designed to provide.

## Known Stubs
- The cb-model `non_symmetric_oracle_test` depthwise/lossguide full-ladder tests are EXPECTED-FAIL at LeafValues/StagedApprox (Wave-0, owned by 06.6-05 leaf values + apply). The SPLITS stage of all three (simplest preflight + depthwise + lossguide) passes. Not a data stub — the leaf-value reconciliation is the next plan's explicit deliverable.

## TDD Gate Compliance
N/A — `tdd_mode: false`. Task 1 follows the plan's failing-harness-first contract (the preflight authored before the grower); in practice the `.cbm`-decode preflight passed at load and the grower's own SPLITS were locked by the Task-3 train→lift oracle.

## Next Phase Readiness
- FEAT-06 STRUCTURE half (SC-3 splits) locked: Depthwise + Lossguide leaf-wise grower produces non-symmetric node graphs; simplest-Depthwise SPLITS match upstream (Open Question 1 RESOLVED, no escalation). SymmetricTree dispatch byte-identical; Region + monotone×non-symmetric rejected (guards enabled).
- 06.6-05 must: (a) reconcile the json-nested vs cbm-flat leaf ordering into one apply-correct representation, (b) estimate non-symmetric leaf VALUES over the distinct-leaf `leaf_of`, and (c) wire the apply pointer-walk so `predict_raw` walks `non_symmetric_trees` — turning the cb-model harness's LeafValues/StagedApprox/Predictions/round-trip stages green for depthwise + lossguide.

## Self-Check: PASSED
- Created files verified present (generator, fixture, grower oracle test, SUMMARY).
- All three task commits (`bc44e4b`, `5fa5c1f`, `64dd5bd`) verified in git log.
- `cargo test -p cb-train --lib` → 228 passed; grower SPLITS oracle → 1 passed; monotone (3 incl. both guards) → passed; cb-model simplest preflight → passed; cb-model lib + oblivious oracle no-regression → passed; `cargo check --workspace --tests` → clean.

---
*Phase: 06.6-advanced-features-and-non-symmetric-trees*
*Completed: 2026-06-18*
