# Phase 7.2: On-Device Gradient/Hessian & Targets - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-20
**Phase:** 7.2-on-device-gradient-hessian-targets
**Mode:** `--auto` (all gray areas auto-selected; each question auto-resolved to the recommended default — no interactive prompts)
**Areas discussed:** Loss/derivative scope, Structural-parity target & kernel shape, Device residency & histogram hand-off, Self-oracle & tolerance

---

## Loss / derivative scope for this slice

| Option | Description | Selected |
|--------|-------------|----------|
| Pointwise family already authored (RMSE/Logloss/Quantile/Focal) + dispatch-by-loss | Mirror upstream `PointwiseTargetKernel`; make the existing `#[cube]` der1/der2 kernels device-resident; defer querywise/pairwise/multiclass | ✓ |
| Port the entire `targets/kernel/` derivative set up front | All pointwise + querywise + pairwise + multiclass der kernels now | |
| Only RMSE der1/der2 (minimal) | Single-loss proof, ignore the dispatch shape | |

**User's choice (auto):** Pointwise family already authored + dispatch-by-loss (recommended default).
**Notes:** Matches the 7.1 "start narrowest" pattern; the formulas already exist in `kernels.rs`, so the work is GPU launch + residency. Weights folded into der/der2 (upstream parity); `functionValue` deferred (histograms consume der1/der2, not the loss value).

---

## Structural-parity target & kernel shape

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror `pointwise_targets.cu(h)` + `pointwise_target_impl` | Match memory layout/blocking/accumulation; reuse existing kernel bodies as formulas | ✓ |
| Result-equivalence only | Compute correct der/der2 without mirroring CUDA structure | |

**User's choice (auto):** Mirror `pointwise_targets.cu(h)` structure (recommended default).
**Notes:** Umbrella D-01 mandates structural parity. der/der2 are elementwise, so D-03's atomic-finalize is a no-op for this slice (it bites in the histogram phases).

---

## Device residency & histogram hand-off (SC-3)

| Option | Description | Selected |
|--------|-------------|----------|
| der1/der2 as device handles, no host fold, consumable by 7.3 | Define handle-in → handles-out seam now; reuse 7.1/cpu_runtime device-memory pattern; coupling stays in cb-backend | ✓ |
| Host round-trip between derivatives and histograms | Read der back to host, re-upload for histograms | |
| Widen the `cb-compute` Runtime trait for the derivative op | Surface grad/hess through the frozen trait seam | |

**User's choice (auto):** Device handles, no host fold; backend-internal (recommended default).
**Notes:** SC-3 explicitly forbids a host fold between derivatives and histograms. Keep the frozen `cb-compute` trait unchanged (mirror D-7.1-04); all cubecl coupling inside `cb-backend`.

---

## Self-oracle & tolerance (SC-2)

| Option | Description | Selected |
|--------|-------------|----------|
| Self-oracle vs Rust CPU der path; report observed divergence | Compare GPU der1/der2 to `cb-compute/loss.rs`; report max abs/rel divergence; do NOT sign off GPU-06 epsilon (7.6's job); per-crate verify | ✓ |
| Hard-code a GPU epsilon now | Pick a fixed tolerance in 7.2 | |
| Oracle against the C++ CUDA path directly | Compare to upstream GPU output instead of the Rust CPU baseline | |

**User's choice (auto):** Self-oracle vs Rust CPU baseline, report-only tolerance (recommended default).
**Notes:** GPU-06 epsilon is signed off only in 7.6 (umbrella D-04). Tests in dedicated `_test.rs`, run on `rocm` in-env; `wgpu` build+run, `cuda` compile-only; never in GitHub Actions.

---

## Claude's Discretion

- Concrete kernel decomposition, block/launch geometry, shared-memory sizing.
- How the per-loss dispatch is expressed in CubeCL (`comptime` selector vs one kernel per loss).
- The exact device-buffer handle type handed to the 7.3 histogram seam.
- The precise representative fixture set for the self-oracle.

(All subject to umbrella D-01/D-03/D-09 and the 7.2 decisions.)

## Deferred Ideas

- Querywise / pairwise / multiclass GPU derivative kernels → 7.4 + ranking follow-up (D-7.2-01).
- Remaining pointwise regression der kernels (LogCosh/Lq/Huber/Expectile/Poisson/Tweedie/MAPE) → additive follow-ons on the same seam.
- On-device `functionValue` (loss value) computation → until a device-side metrics consumer needs it (D-7.2-02).
- Empirical GPU-06 epsilon + `rocm` oracle-gate lock-in → 7.6.
- Wave64 (CDNA/MI-series) literal validation → out of reach on gfx1100.

### Reviewed Todos (not folded)

- **Estimated-feature stored-border-VALUE quantization-grid parity** — keyword-matched (score 0.6) but is the cb-train KNN online-HNSW port, a different subsystem from GPU gradient/hessian targets. Not folded — its own focused phase, not this GPU derivative slice.
</content>
