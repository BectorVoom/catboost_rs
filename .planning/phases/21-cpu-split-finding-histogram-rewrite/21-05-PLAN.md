---
phase: 21-cpu-split-finding-histogram-rewrite
plan: 05
type: execute
wave: 5
depends_on: [21-04]
files_modified:
  - Cargo.toml
  - crates/cb-train/Cargo.toml
  - crates/cb-train/src/tree.rs
  - crates/cb-train/tests/rayon_determinism_test.rs
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "The per-level histogram build (and border scoring) is parallelized over INDEPENDENT features with rayon par_iter, collecting into a pre-sized Vec so the merge order is deterministic (Pitfall 5)"
    - "Each feature's per-bin fold stays the sequential cb_core::sum_f64 — no cross-feature float reduction, so no merge-order hazard"
    - "Running a representative oracle twice with rayon enabled produces byte-identical models (determinism test)"
    - "The full oracle suite stays bit-exact single- vs multi-threaded (PERF-02 preserved under parallelism)"
    - "A documented end-to-end speedup vs the pre-rewrite baseline on the Spike-002 grid is recorded, with single-thread per-core efficiency stated against official CatBoost 1-thread times (PERF-03)"
    - "Reusable scratch buffers eliminate the per-candidate allocation storm (no per-candidate Vec<bool>/Vec<Vec<f64>> allocation)"
  artifacts:
    - path: "Cargo.toml"
      provides: "rayon = 1.12.0 in [workspace.dependencies]"
      contains: "rayon"
    - path: "crates/cb-train/Cargo.toml"
      provides: "rayon dependency (workspace)"
      contains: "rayon"
    - path: "crates/cb-train/tests/rayon_determinism_test.rs"
      provides: "byte-identical-model determinism test with rayon enabled"
      contains: "assert"
  key_links:
    - from: "crates/cb-train/src/tree.rs"
      to: "Cargo.toml"
      via: "rayon par_iter over features in the histogram build, ordered collect"
      pattern: "par_iter|into_par_iter"
  prohibitions:
    - "MUST NOT add a cb-train dependency on cb-backend"
    - "MUST NOT add rayon or cubecl to cb-compute (parallelism lives in the cb-train grow loop; cb-compute stays pure-generic per-feature primitive)"
    - "MUST NOT introduce a cross-feature float reduction with unordered merge (use per-feature-independent work + ordered collect; fixed-point-u64 only if a global reduction is unavoidable)"
    - "MUST NOT change score.rs math, the candidate enumeration order, the strict `>` tie-break, or the leaf-VALUE path"
    - "MUST NOT touch the ordered-boosting path, pairwise path, or GPU path"
    - "MUST NOT add a `#[cfg(test)] mod tests` block in tree.rs"
---

<objective>
Parallelize the now-histogram-based CPU split search over independent features with `rayon`, finalize the reusable scratch buffers (no per-candidate allocation), prove determinism (byte-identical model single- vs multi-threaded), and record the documented end-to-end speedup on the Spike-002 grid.

Purpose: PERF-03 — with the algorithm fixed (PERF-01) the remaining win is the ~core-count factor CatBoost gets from its `LocalExecutor` (Spike 004: 3.9× at 16 threads). Features own disjoint histogram rows, so `par_iter` + ordered `collect` parallelizes cleanly while keeping each per-bin fold the sequential `sum_f64` — determinism preserved (Pitfall 5). Parallelism is SECOND, after the algorithm, per CONTEXT.

Output: `rayon = "1.12.0"` in the workspace + cb-train deps; parallel-over-features build in `tree.rs`; a determinism test; and a recorded speedup + per-core-efficiency-vs-official-CatBoost number in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-CONTEXT.md
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-RESEARCH.md
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-04-SUMMARY.md
@.planning/spikes/004-parallelism-and-allocation-audit/README.md
@crates/cb-train/src/tree.rs
@crates/cb-train/tests/bench_grow_speed_test.rs
</context>

<artifacts_this_phase_produces>
New symbols / changes introduced by THIS plan:
- `rayon = "1.12.0"` added to root `Cargo.toml [workspace.dependencies]` and to `crates/cb-train/Cargo.toml [dependencies]` as `rayon = { workspace = true }` (verdict OK — 21-RESEARCH Package Legitimacy Audit; NOT added to cb-compute).
- Parallel-over-features histogram build in tree.rs: `(0..n_features).into_par_iter().map(|f| build feature f's histogram row).collect::<Vec<_>>()` (or an equivalent ordered collect), preserving feature index order; each row folded with the sequential sum_f64.
- Finalized GrowScratch reuse: fixed-size histogram arrays + incremental leaf_of allocated once and clear-reused per level (no per-candidate allocation).
- `crates/cb-train/tests/rayon_determinism_test.rs` — trains a representative fixture twice with rayon active and asserts the two models are byte-identical (guards Pitfall 5).
- Updated `Cargo.lock` (new transitive deps rayon-core, crossbeam-*) — committed.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: add rayon + parallelize the per-feature histogram build (ordered, deterministic)</name>
  <files>Cargo.toml, crates/cb-train/Cargo.toml, crates/cb-train/src/tree.rs</files>
  <read_first>
    - Cargo.toml [workspace.dependencies] (add rayon 1.12.0), crates/cb-train/Cargo.toml [dependencies]
    - crates/cb-train/src/tree.rs (the GrowScratch build sites from 21-02/03/04 — the per-level histogram build loop over features)
    - .planning/spikes/004-parallelism-and-allocation-audit/README.md (allocation storm + single-thread cause)
    - 21-RESEARCH.md Pitfall 5 (rayon nondeterminism in the merge) + §Don't Hand-Roll (rayon over std::thread)
  </read_first>
  <action>
    Add `rayon = "1.12.0"` to the workspace [workspace.dependencies] and `rayon = { workspace = true }` to crates/cb-train/Cargo.toml (do NOT add to cb-compute — keep it pure-generic). Parallelize the per-level histogram build in tree.rs by mapping over feature indices with `into_par_iter().map(...).collect()` into a pre-sized Vec indexed by feature so rayon preserves index order — each feature owns disjoint histogram rows and its per-bin fold stays the sequential cb_core::sum_f64 (NO cross-feature reduction, NO unordered merge). Finalize GrowScratch so the histogram arrays + leaf_of are allocated once and clear-reused across levels (eliminate the per-candidate allocation storm, Spike 004). Keep the candidate enumeration order + strict `>` selection identical (parallel build, sequential deterministic selection).
  </action>
  <verify>
    <automated>cargo build -p cb-train</automated>
    <automated>cargo test -p cb-train tree::</automated>
    <automated>grep -nE 'rayon|cubecl' crates/cb-compute/Cargo.toml || echo CB-COMPUTE-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `rayon = "1.12.0"` present in root Cargo.toml [workspace.dependencies]; `rayon = { workspace = true }` in cb-train Cargo.toml.
    - `grep -nE 'rayon|cubecl' crates/cb-compute/Cargo.toml` prints CB-COMPUTE-CLEAN (rayon NOT added to cb-compute).
    - `grep -nE 'into_par_iter|par_iter' crates/cb-train/src/tree.rs` shows the parallel build; the collect is into a feature-indexed Vec (ordered).
    - `cargo test -p cb-train tree::` green.
  </acceptance_criteria>
  <done>rayon is a workspace + cb-train dep (not cb-compute); the per-level histogram build is parallel over independent features with ordered collect; GrowScratch is allocated once and reused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: determinism test + full-suite parity under rayon</name>
  <files>crates/cb-train/tests/rayon_determinism_test.rs</files>
  <read_first>
    - crates/cb-train/tests/ (an existing oracle test to model the fixture setup for the determinism test)
    - 21-RESEARCH.md Pitfall 5 + §Validation Architecture (determinism under rayon = run twice, identical output)
  </read_first>
  <behavior>
    - Test: training a representative fixture twice (rayon active) yields byte-identical model bytes / leaf values / structure.
    - Test: the full oracle suite is bit-exact with rayon enabled (identical to the single-threaded 21-04 result).
  </behavior>
  <action>
    Create crates/cb-train/tests/rayon_determinism_test.rs (integration test, source/test separated) that trains a small representative model twice with rayon active and asserts the two results are byte-identical (serialized model or leaf-value/structure equality). Then run the FULL cb-train + cb-compute oracle suites to confirm parity is preserved under parallelism (PERF-02 must still hold with rayon on). If any fixture flickers, the merge is not feature-independent — fix to per-feature-independent + ordered collect (or fixed-point-u64 for any unavoidable global reduction) before closing.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test rayon_determinism_test</automated>
    <automated>cargo test -p cb-train</automated>
    <automated>cargo test -p cb-compute</automated>
  </verify>
  <acceptance_criteria>
    - rayon_determinism_test passes: two runs produce byte-identical models.
    - `cargo test -p cb-train` + `cargo test -p cb-compute` FULL suites green with rayon enabled (PERF-02 preserved under parallelism).
    - The determinism test lives in a separate *_test.rs file (source/test separation).
  </acceptance_criteria>
  <done>A determinism test proves byte-identical models across runs with rayon on, and the full oracle suite is bit-exact under parallelism.</done>
</task>

<task type="auto">
  <name>Task 3: documented end-to-end speedup + per-core efficiency vs official CatBoost</name>
  <files>.planning/phases/21-cpu-split-finding-histogram-rewrite/21-05-SUMMARY.md (benchmark run + recorded numbers; no source edits)</files>
  <read_first>
    - crates/cb-train/tests/perf_baseline_test.rs, crates/cb-train/tests/bench_grow_speed_test.rs (CB_PERF harness)
    - .planning/spikes/002-perf-baseline-and-scaling/README.md + catboost_grid.py (the grid + official-CatBoost 1-thread baseline)
    - 21-CONTEXT.md §Specifics (PERF-03 acceptance = documented speedup + stated target factor vs official CatBoost 1-thread)
  </read_first>
  <action>
    Run the CB_PERF benchmark harness on the Spike-002 grid before/after (the pre-rewrite baseline is recorded in Spike 002; the after is the current build). Record in the SUMMARY: (a) end-to-end per-tree speedup vs the pre-rewrite baseline, (b) single-thread per-core efficiency stated as a factor of official CatBoost's 1-thread times on the same grid, and (c) confirmation the n_bins sweep stays flat (PERF-01) under the parallel build. This is a wall-clock benchmark (Manual-Only per 21-VALIDATION) — capture the raw numbers, do NOT fabricate; if the machine cannot run official CatBoost, reuse the Spike-002 recorded catboost_grid.py numbers and cite them.
  </action>
  <verify>
    <automated>CB_PERF=1 cargo test --release -p cb-train --test bench_grow_speed_test -- --nocapture</automated>
    <automated>CB_PERF=1 cargo test --release -p cb-train --test perf_baseline_test -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The bench harness runs and produces per-tree timings; the SUMMARY records before/after speedup, the stated target factor vs official CatBoost 1-thread, and n_bins flatness.
    - No fabricated numbers — official-CatBoost figures are either freshly measured or cited from Spike 002's recorded catboost_grid.py output.
  </acceptance_criteria>
  <done>PERF-03 is discharged: a documented end-to-end speedup vs the pre-rewrite baseline and a stated per-core efficiency factor vs official CatBoost 1-thread are recorded, with n_bins timing flat under the parallel build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parallel histogram build → shared scratch | Internal in-process; features write disjoint rows. No external surface. Determinism (integrity) is the correctness concern, not a classic vuln. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | Tampering (integrity) | nondeterministic parallel accumulation → parity break / run-to-run flicker | mitigate | feature-independent work + ordered collect into pre-sized Vec; per-bin fold stays sequential sum_f64; rayon_determinism_test + full suite gate. |
| T-21-14 | DoS | rayon thread-pool resource use | accept | rayon global pool bounded by core count; no unbounded spawn. |
| T-21-SC | Tampering | supply chain — new dep rayon (+ rayon-core, crossbeam-*) | accept | rayon verdict OK (crates.io, ~10yr, 7.48M/wk, github.com/rayon-rs/rayon — 21-RESEARCH Package Legitimacy Audit); not [ASSUMED]/[SUS], so no blocking-human checkpoint required. Commit updated Cargo.lock. |
</threat_model>

<verification>
- rayon_determinism_test byte-identical; full cb-train + cb-compute suites bit-exact under rayon (PERF-02 preserved).
- CB_PERF bench records speedup + per-core efficiency vs official CatBoost + flat n_bins (PERF-03/PERF-01).
- rayon in workspace + cb-train only (NOT cb-compute); no cb-backend seam; Cargo.lock committed.
</verification>

<success_criteria>
The CPU split search is parallelized over independent features with rayon and deterministic (byte-identical models across runs, full oracle suite bit-exact under parallelism), scratch buffers are reused (no per-candidate allocation storm), and a documented end-to-end speedup vs the pre-rewrite baseline plus a stated per-core efficiency factor vs official CatBoost 1-thread are recorded (PERF-03).
</success_criteria>

<output>
Create `.planning/phases/21-cpu-split-finding-histogram-rewrite/21-05-SUMMARY.md` when done. Record the speedup numbers and the per-core efficiency factor.
</output>
