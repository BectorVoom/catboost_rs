---
phase: 21-cpu-split-finding-histogram-rewrite
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
files_modified:
  - crates/cb-core/src/reduction.rs
  - crates/cb-core/src/reduction_test.rs
  - crates/cb-compute/src/histogram.rs
  - crates/cb-compute/src/histogram_test.rs
  - crates/cb-train/src/tree.rs
  - .planning/phases/21-cpu-split-finding-histogram-rewrite/21-06-SUMMARY.md

must_haves:
  truths:
    - "CB_PERF n_bins sweep per-tree time is FLAT within noise across border_count 32→254: the 32→254 per-tree-ms ratio drops from the current ~4.3× to ≤ ~2.5× (approaching official CatBoost's ~2.1× fingerprint for the same 8× bin range), and per-tree time no longer scales ~linearly with n_bins (the 64→254 ~4× bin step is near-flat, ≤ ~1.7×, because the O(n·n_features·depth) binning pass — flat in n_bins — dominates the residual O(n_features·n_bins·n_leaves·depth) scan)"
    - "The FULL CPU ≤1e-5 oracle suite passes in a SINGLE atomic (uninterrupted) run with ZERO fixtures flipped, AFTER the TRUE-side running-prefix reorder is enabled — this is the gate that authorizes the summation-order change (PERF-02 crux)"
    - "crates/cb-train/tests/rayon_determinism_test.rs stays 2/2 byte-identical (SymmetricTree + Depthwise) after the scan/scratch/advance changes"
    - "Per-level histogram allocation is eliminated: build_bucket_histogram no longer allocates a per-(leaf,feature,bin,channel) heap Vec; it scatter-adds into a single flat reused f64 accumulator via a new cb_core primitive, and scan_borders_to_leaf_stats no longer allocates per-border Vecs"
  artifacts:
    - path: "crates/cb-core/src/reduction.rs"
      provides: "scatter_add_f64 — the sanctioned object-order scatter-add primitive (D-07/D-08 honored without per-cell Vecs)"
      contains: "pub fn scatter_add_f64"
    - path: "crates/cb-compute/src/histogram.rs"
      provides: "flat-scratch build_bucket_histogram (no per-cell Vec) + running-prefix O(n_bins) scan with an explicit parity-safe TRUE-side strategy"
      contains: "scan_border_to_leaf_stats"
    - path: "crates/cb-train/src/tree.rs"
      provides: "GrowScratch::advance subtraction-trick reuse — larger sibling derived from the retained parent self.hist, not a from-scratch rebuild"
      contains: "fn advance"
  key_links:
    - from: "crates/cb-compute/src/histogram.rs::build_bucket_histogram"
      to: "crates/cb-core/src/reduction.rs::scatter_add_f64"
      via: "object-order scatter-add into a flat accumulator (bit-identical to the current gather-then-sum_f64)"
      pattern: "scatter_add_f64"
    - from: "crates/cb-train/src/tree.rs::select_level_plain / best_split_for_leaf"
      to: "crates/cb-compute/src/histogram.rs::scan_borders_to_leaf_stats"
      via: "running-prefix O(n_bins) border scan (single pass, FALSE running-prefix + TRUE = total − prefix)"
      pattern: "scan_borders_to_leaf_stats"
    - from: "crates/cb-train/src/tree.rs::GrowScratch::advance"
      to: "crates/cb-train/src/tree.rs::GrowScratch::hist (retained parent)"
      via: "relocate parent cells into forward-bit child slots, subtract the freshly-built smaller sibling"
      pattern: "self\\.hist"
---

<objective>
Close the sole failed Phase 21 roadmap Success Criterion (PERF-01: per-tree CPU
time FLAT within noise across border_count 32→254). Plans 21-01..21-05 shipped and
are VERIFIED except this one truth. The rescan is already gone; the residual
~linear-in-n_bins scaling lives entirely in the histogram DATA layer:

1. `scan_border_to_leaf_stats`/`scan_borders_to_leaf_stats` (histogram.rs:426-490)
   is O(n_bins²) per feature per level — it re-gathers per-bin rows and re-sums the
   FALSE/TRUE prefixes fresh for every border, allocating fresh Vecs per border
   (21-REVIEW WR-03).
2. `build_bucket_histogram` (histogram.rs:337-402) allocates a
   `Vec<Vec<f64>>` of length n_leaves·n_features·n_bins·n_channels — one growable
   heap Vec per cell-channel — on every call (21-REVIEW WR-02); the per-level
   allocation the verifier's `missing:` list names.
3. `GrowScratch::advance` (tree.rs:760-792) rebuilds the parent histogram it already
   holds ~3× per level transition instead of deriving the larger sibling from the
   retained parent (21-REVIEW WR-04) — defeating the subtraction trick's payoff.

The reorder is a PARITY HAZARD deferred three times (21-01→02→05) because the
TRUE-side running prefix changes f64 summation order on the ≤1e-5 bar and the
executors could only run the oracle suite in BATCHES under a then-100%-full disk,
never one atomic pass to safely gate a tie-flip. This plan bakes the atomic
full-oracle-suite gate in as a mandatory acceptance step.

Purpose: deliver the flat n_bins fingerprint (PERF-01) without regressing the
bit-exact/≤1e-5 parity crux (PERF-02) or the first-class memory constraint
(PERF-03), under an environment now at 78% disk with explicit hygiene commands.

Output: a scatter-add cb_core primitive, a flat-scratch build, a running-prefix
O(n_bins) scan with an explicit empirically-gated TRUE-side strategy, a
retained-parent subtraction advance, an atomic-suite parity proof, a re-recorded
CB_PERF 32→254 sweep, and CR-01 doc-comment honesty (bit-exact → ≤1e-5
oracle-equivalent for the TRUE-side path).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-CONTEXT.md
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-RESEARCH.md
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-VERIFICATION.md
@.planning/phases/21-cpu-split-finding-histogram-rewrite/21-REVIEW.md
@crates/cb-core/src/reduction.rs
@crates/cb-compute/src/histogram.rs
@crates/cb-train/src/tree.rs
</context>

<locked_constraints>
Violating ANY of these is a HARD FAIL (CONTEXT.md D-03, memory
backend-crate-and-rocm-ci-constraints):
- NEVER add a `cb-train` dependency on `cb-backend` (feature-unification landmine).
- `cb-compute` stays cubecl-free AND rayon-free (D-03). Do not add rayon or cubecl
  to `cb-compute/Cargo.toml`. The scatter-add primitive lives in `cb-core`.
- Do NOT touch `#[cube]` / device code (`cb-backend/src/kernels/*`). READ-ONLY
  template only.
- No change to model format, leaf-value calculation, or inference. Leaf-VALUE
  estimation (`reduce_leaf_stats`/`reduce_leaf_der2`/`collect_leaf_residuals`) stays
  on the object-order path — untouched.
- The per-bin/per-cell fold stays `cb_core::sum_f64` (ascending). The ONLY
  summation-order change permitted is the TRUE-side scan strategy (Task 2), and it
  MUST be empirically oracle-gated (Task 3).
- Source/test separation: NO embedded `#[cfg(test)] mod tests`. New tests go in the
  existing `_test.rs` sibling files (`reduction_test.rs`, `histogram_test.rs`),
  mounted via the existing `mod reduction_test;` / `mod histogram_test;`.
- `unwrap()`/`expect()`/`panic!`/raw indexing are workspace-denied in production
  (`indexing_slicing`, `unwrap_used`); use `.get()`/`.get_mut()` defensive access.
</locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cb_core scatter_add_f64 primitive + flat-scratch build_bucket_histogram (WR-02, PERF-03, parity-SAFE)</name>
  <files>crates/cb-core/src/reduction.rs, crates/cb-core/src/reduction_test.rs, crates/cb-compute/src/histogram.rs, crates/cb-compute/src/histogram_test.rs</files>
  <read_first>
    - crates/cb-core/src/reduction.rs (the WHOLE file — `sum_f64` at 25-38 is the contract; this is the ONLY file allowed a hand-written float loop per D-07/D-08 and `scripts/check-no-raw-float-sum.sh`)
    - crates/cb-compute/src/histogram.rs:337-402 (`build_bucket_histogram` — the current gather `members: Vec<Vec<f64>>` at 360, the object loop 362-387, the `sum_f64` fold 389-393)
    - crates/cb-compute/src/histogram.rs:180-291 (`BucketHistogram` struct + frozen layout `((leaf*n_features+feature)*n_bins+bin)*n_channels`, `channel()`, `remove()`)
    - crates/cb-compute/src/histogram_test.rs (existing equivalence tests — the pattern to extend)
    - scripts/check-no-raw-float-sum.sh (why the scatter-add MUST be a cb_core primitive, not an inline `+=` in cb-compute)
  </read_first>
  <behavior>
    - scatter_add_f64(acc, idx, value): folds `value` into `acc[idx]` as a single left-to-right `+=`; out-of-range `idx` is a no-op (defensive, no raw indexing). Doing N calls with ascending source order into the same slot reproduces `sum_f64` of those N values in that order, bit-for-bit.
    - build_bucket_histogram output BucketHistogram is bit-identical (PartialEq / every channel exactly equal) to the pre-change gather-then-sum_f64 output for the same inputs, because per cell the scatter-add sequence is the identical object-order left-to-right fold.
    - No per-cell heap Vec is allocated: the builder holds one flat `Vec<f64>` accumulator of length `total`.
  </behavior>
  <action>
    Add `pub fn scatter_add_f64(acc: &mut [f64], idx: usize, value: f64)` to
    `crates/cb-core/src/reduction.rs`, directly below `sum_f64`. Body: `if let
    Some(slot) = acc.get_mut(idx) { *slot += value; }` — a single sequential `+=`,
    defensive `get_mut` (no raw indexing), no compensation. Document it in the module
    style: it is the sanctioned SCATTER form of the same left-to-right f64 fold as
    `sum_f64` — folding a cell's members via repeated `scatter_add_f64` in ascending
    object order yields byte-identical bits to `sum_f64(&members)`; it exists so
    histogram accumulation honors the D-08 "no raw float fold outside this file" ban
    WITHOUT per-cell `Vec` allocation. Export it from `cb_core` (add to the same
    pub-use surface as `sum_f64` in `crates/cb-core/src/lib.rs` if `sum_f64` is
    re-exported there).

    Rewrite `build_bucket_histogram` (histogram.rs:337-402): DELETE the `members:
    Vec<Vec<f64>>` gather (line 360) and the final `sum_f64` loop (389-393). Instead
    allocate ONE `let mut data = vec![0.0_f64; total];` and, inside the existing
    `for obj in 0..n_objects { … for feature in 0..n_features { … } }` loop, replace
    each `members.get_mut(cell_base + d)…push(dval)` /
    `…push(w)` with `cb_core::scatter_add_f64(&mut data, cell_base + d, dval)` and
    `cb_core::scatter_add_f64(&mut data, cell_base + approx_dimension, w)`. Preserve
    the EXACT object-outer / feature-inner iteration order and the exact
    `cell_base = ((leaf*n_features+feature)*n_bins+bin)*n_channels` arithmetic so the
    per-cell fold order is byte-identical. Keep the T-21-02 checked `total`
    (`checked_mul(..).unwrap_or(0)`) and all `.get()` defensive guards. Construct the
    returned `BucketHistogram { data, n_leaves, n_features, n_bins, n_channels }`
    unchanged.

    Add a `histogram_test.rs` test `build_flat_scatter_equals_gather` that builds a
    small fixture two ways — the new scatter path and a local gather-then-`sum_f64`
    reference — and asserts `BucketHistogram` PartialEq (every channel bit-equal).
    Add a `reduction_test.rs` test `scatter_add_matches_sum_f64` asserting that
    scatter-adding a slice's values in order into a zeroed 1-elem accumulator equals
    `sum_f64(&slice)` bit-for-bit, plus an out-of-range-idx no-op case.
  </action>
  <verify>
    <automated>cargo test -p cb-core --release reduction_test::scatter_add &amp;&amp; cargo test -p cb-compute --release histogram &amp;&amp; bash scripts/check-no-raw-float-sum.sh</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-core/src/reduction.rs` contains `pub fn scatter_add_f64` with a
      `get_mut`-guarded single `+=` (grep: `grep -n 'pub fn scatter_add_f64' crates/cb-core/src/reduction.rs` returns 1 line; `grep -n 'acc\[' crates/cb-core/src/reduction.rs` returns 0 — no raw indexing).
    - `grep -c 'Vec<Vec<f64>>' crates/cb-compute/src/histogram.rs` on the
      build_bucket_histogram region is 0 (the per-cell gather Vec is gone).
    - `bash scripts/check-no-raw-float-sum.sh` exits 0 (D-08 backstop: no raw float
      fold introduced in cb-compute).
    - `cargo test -p cb-compute --release histogram` exits 0 including the new
      `build_flat_scatter_equals_gather` (bit-identical to the gather reference).
    - `cargo test -p cb-core --release reduction_test::scatter_add` exits 0.
  </acceptance_criteria>
  <done>scatter_add_f64 lands in cb_core; build_bucket_histogram scatter-adds into a single flat Vec with byte-identical output and zero per-cell heap Vecs; both new tests green; D-08 backstop clean.</done>
</task>

<task type="auto">
  <name>Task 2: Running-prefix O(n_bins) border scan + retained-parent subtraction advance (WR-03/WR-04, PERF-01, TRUE-side reorder — the parity hazard)</name>
  <files>crates/cb-compute/src/histogram.rs, crates/cb-compute/src/histogram_test.rs, crates/cb-train/src/tree.rs</files>
  <read_first>
    - crates/cb-compute/src/histogram.rs:404-490 (`scan_border_to_leaf_stats` + `scan_borders_to_leaf_stats` — the current per-border re-gather + per-border `sum_f64(bins[0..=border])` / `sum_f64(bins[border+1..n])`, the O(n_bins²) WR-03 gap)
    - crates/cb-compute/src/histogram.rs:293-308 (`bin_of` — FALSE=`bins<=b`, TRUE=`bins>b` boundary, Pitfall 4)
    - crates/cb-train/src/tree.rs:704-799 (`GrowScratch::hist_add`, `GrowScratch::advance` — the WR-04 3× rebuild: `h_base_low`/`h_base_high` are from-scratch rebuilds of the parent already resident in `self.hist`)
    - crates/cb-train/src/tree.rs:640-702 (`GrowScratch` struct fields: `bins`, `leaf_of`, `hist`, `n_features`, `n_bins`, `approx_dim`, `level`)
    - crates/cb-train/src/tree.rs:818-960, 1046-1160, 2152-2180 (the scan call sites: `select_level_plain`:848, `select_level_perturbed`:933, `best_split_for_leaf`:1138, `score_candidate_ctr_aware`:2174 — all consume the scan and MUST keep identical `[dimension]`/`[border][dimension]`/`[false,true]` shapes and canonical forward-bit leaf order)
    - 21-RESEARCH.md §"Open Questions" Q2 + §"Common Pitfalls" Pitfalls 1 &amp; 2 (the TRUE-side hazard and the fixed-point fallback)
  </read_first>
  <action>
    STATE THE TRUE-SIDE STRATEGY EXPLICITLY (record it verbatim in the
    `scan_border_to_leaf_stats` doc comment and the 21-06-SUMMARY):
    - FALSE side: a single left-to-right running prefix `acc_false += bin[b]`
      (ascending bin order). This is BIT-IDENTICAL to the current
      `sum_f64(bins[0..=border])` — zero parity risk (a running `+=` from acc=0.0 in
      ascending order IS the sum_f64 fold).
    - TRUE side (the hazard): `true = total − acc_false`, where
      `total = sum_f64(all bins ascending)` is computed ONCE per (parent, feature,
      channel). This mirrors upstream `CalcScoresForLeaf`'s running
      `trueStats = total − falseStats` complement (scoring.cpp), is O(n_bins), and is
      the PRIMARY strategy. It changes the TRUE-side f64 bits vs the current ascending
      per-border suffix `sum_f64(bins[border+1..n])` — this is the reorder Task 3's
      atomic oracle suite must authorize.
    - Documented FALLBACKS if Task 3's suite flips a fixture: (a) running DESCENDING
      suffix accumulator (`suffix += bin[n-1-k]`) instead of `total − prefix`; (b) if
      both flip, switch the histogram accumulation to fixed-point-u64 (round
      `v*2^30 → i64`, the Phase 10/11 pattern) making `total − prefix`, suffix, and
      the subtraction trick all EXACT/order-invariant. Do NOT implement a fallback
      pre-emptively; the primary is gated first in Task 3.

    Rewrite `scan_border_to_leaf_stats` + `scan_borders_to_leaf_stats`
    (histogram.rs:404-490) into a SINGLE O(n_bins) pass per (feature). Restructure so
    `scan_borders_to_leaf_stats` is the primary: for each parent leaf and each channel
    (weight + each dimension's delta), gather the per-bin row ONCE
    (`hist.channel(parent, feature, bin, ch)` for bin in 0..n_bins), compute
    `total = sum_f64(&row)` once, then walk borders b=0..n_borders maintaining
    `acc_false += row[b]` and emitting `false = acc_false`, `true = total − acc_false`
    per border. Emit the SAME return shape (`Vec<[border]><[dimension]><LeafStats;
    2*n_parent>>`, FALSE child at index `parent`, TRUE child at `parent + n_parent`)
    so all four call sites are untouched. Keep `scan_border_to_leaf_stats` (single
    border, used by `score_candidate_ctr_aware`:2174) as a thin wrapper that computes
    the same `total`/prefix for its one border — no per-border re-gather of the whole
    row. Remove the per-border `Vec` allocations (WR-03): gather each channel row at
    most once per (parent, feature). Route EVERY sum through `cb_core::sum_f64`
    (`total`) or the running `+=` (FALSE prefix); no raw `.sum()`.

    Fix `GrowScratch::advance` (tree.rs:760-792, WR-04): stop rebuilding the parent.
    In the `self.hist.n_leaves() == n_parent` branches, DELETE the `h_base_low` /
    `h_base_high` from-scratch `build(&base_low,…)` / `build(&base_high,…)` calls.
    Instead RELOCATE the retained `self.hist` (n_parent leaves) into the child slot
    space (n_next leaves) — parent leaf `p`'s cells copied into the low slots for the
    FALSE-smaller case, or high slots for the TRUE-smaller case — producing the parent
    histogram in the child layout with byte-identical cell values (same objects, same
    bins, same fold order → a copy, not a re-fold). Then derive the larger sibling as
    `relocated_parent.remove(&small_sibling)` and `hist_add` the two siblings. Build
    ONLY the smaller sibling from scratch (one pass). Keep the defensive
    `self.hist.n_leaves() != n_parent` fresh-rebuild fallback branch (T-21-05). This
    relocation is parity-SAFE (a memcpy of already-folded cells); it does NOT touch the
    TRUE-side reorder. Add a helper (e.g. `GrowScratch::relocate_parent(shift:
    usize)`) if it clarifies the forward-bit slot arithmetic.

    Add a `histogram_test.rs` test `running_prefix_scan_matches_per_border_reference`
    that, on a small benign fixture (exactly-representable values), asserts the new
    running-prefix scan's FALSE-side LeafStats are bit-equal to the old
    `sum_f64(bins[0..=border])` reference for every border (the FALSE side must NEVER
    change), and the TRUE side equals `total − false` — documenting the exact bits the
    atomic suite then validates on real fixtures.
  </action>
  <verify>
    <automated>cargo test -p cb-compute --release histogram &amp;&amp; cargo test -p cb-train --release --lib tree::</automated>
  </verify>
  <acceptance_criteria>
    - `scan_border_to_leaf_stats`'s doc comment states the TRUE-side strategy verbatim
      (grep: `grep -n 'total − acc_false\|total - acc_false\|total − prefix\|total - prefix' crates/cb-compute/src/histogram.rs` returns ≥1).
    - The scan no longer re-gathers per border: `scan_borders_to_leaf_stats` gathers
      each channel row at most once per (parent, feature) — verified by inspection that
      the `(0..n_bins).map(|bin| hist.channel(...)).collect()` gather is OUTSIDE the
      `for border` loop.
    - `cargo test -p cb-compute --release histogram` exits 0 including
      `running_prefix_scan_matches_per_border_reference` (FALSE side bit-identical).
    - `GrowScratch::advance` no longer calls `build(&base_low` / `build(&base_high`
      (grep: `grep -c 'base_low\|base_high' crates/cb-train/src/tree.rs` reflects the
      removed rebuilds; the retained-parent relocation path is present).
    - `cargo test -p cb-train --release --lib tree::` exits 0 (leaf-wise scorer
      equivalence + tie-break unit tests).
  </acceptance_criteria>
  <done>The border scan is a single O(n_bins) running-prefix pass with an explicitly documented TRUE = total − prefix strategy (FALSE side bit-identical); advance derives the larger sibling from the retained parent (one small-sibling pass, no 3× rebuild); benign-fixture equivalence test green. Parity on real fixtures is gated by Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic full-oracle parity gate + CB_PERF 32→254 re-sweep + CR-01 wording honesty (PERF-01/PERF-02 acceptance)</name>
  <files>crates/cb-compute/src/histogram.rs, crates/cb-train/src/tree.rs, .planning/phases/21-cpu-split-finding-histogram-rewrite/21-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/21-cpu-split-finding-histogram-rewrite/21-05-SUMMARY.md (Deviation #2 — the disk-hygiene recipe that made batched runs possible; Deviation #1 — the exact PERF-01 deferral this task discharges)
    - crates/cb-train/tests/perf_baseline_test.rs (the CB_PERF-gated harness; the n_bins sweep is `for &nb in &[16, 32, 64, 254]` at ~167; the reported metric is `per_tree_ms`)
    - 21-REVIEW.md CR-01 (the overstated "bit-exact"/"byte-for-byte" doc claims to downgrade; the exact locations: histogram.rs:415-421 and tree.rs:44-46,291-292,714-724)
    - crates/cb-train/tests/rayon_determinism_test.rs (the determinism gate that must stay 2/2)
  </read_first>
  <action>
    ATOMIC ORACLE GATE (this authorizes Task 2's TRUE-side reorder — it is
    NON-OPTIONAL). Before running, apply the disk-hygiene recipe (21-05 Deviation #2,
    disk is currently ~78% but the atomic run links ~56 test binaries):
    `export CARGO_INCREMENTAL=0`; remove stale incremental caches
    (`rm -rf target/debug/incremental target/release/incremental`); if space runs low
    mid-run, delete stale linked test EXECUTABLES under `target/release/deps` (the
    final `*-<hash>` binaries only — PRESERVE `.rlib`/`.rmeta`/`.so`, which are
    expensive to rebuild). Then run the FULL CPU oracle suite in ONE uninterrupted
    invocation: `CARGO_INCREMENTAL=0 cargo test -p cb-train --release --no-fail-fast`
    followed by `cargo test -p cb-compute --release`. The gate PASSES iff every
    ≤1e-5 oracle fixture across losses/policies/CTR/ranking/ordered/multiclass passes
    with ZERO fixtures flipped — EXCEPT the single known pre-existing unrelated failure
    `monotone_oracle_test::monotone_non_symmetric_and_region_are_typed_errors` (a
    stale pre-Phase-21 assertion, documented in 21-VERIFICATION.md and
    deferred-items.md; it is NOT a regression and does NOT gate this plan). Record the
    full pass/fail tally and the observed max deviation in 21-06-SUMMARY. If ANY
    OTHER fixture flips (a large, non-1e-5 delta ⇒ a tie-flip / structure divergence,
    Pitfall 1), do NOT paper over it: switch Task 2's TRUE-side to fallback (a)
    running descending suffix and re-run the atomic gate; if it still flips, adopt
    fallback (b) fixed-point-u64 histogram accumulation and re-run. Only a
    zero-flip atomic pass authorizes keeping the reorder.

    DETERMINISM GATE: `cargo test -p cb-train --release --test rayon_determinism_test`
    must stay 2/2 (SymmetricTree + Depthwise byte-identical). Record in the SUMMARY.

    CB_PERF RE-SWEEP: run
    `CB_PERF=1 cargo test --release -p cb-train --test perf_baseline_test -- --nocapture`
    and capture the `RSBENCH … per_tree_ms` lines for the n_bins axis (32, 64, 254).
    Record the new 32→254 per_tree_ms ratio in 21-06-SUMMARY and confirm it is ≤ ~2.5×
    (down from the verifier-measured ~4.3×), approaching official CatBoost's ~2.1×
    fingerprint, with the 64→254 step near-flat (≤ ~1.7×). If the ratio is still
    > ~2.5×, the flatness lever is not delivered — investigate the residual n_bins
    term before declaring PERF-01 met.

    CR-01 WORDING FIX: because the TRUE-side is now `total − prefix` (reordered vs the
    old ascending suffix), the code can no longer claim byte-identity to the prior Rust
    score math for arbitrary floats — the honest bar is the ≤1e-5 oracle. Downgrade the
    overstated claims: in histogram.rs the `scan_border_to_leaf_stats` doc
    (~415-421) change "this is bit-exact to the object-order reduce_leaf_stats fold"
    to state the FALSE side is bit-identical and the TRUE side is
    "≤1e-5 oracle-equivalent (total − prefix reorder, gated by the full oracle suite,
    21-06)". In tree.rs downgrade the "byte-for-byte"/"byte-identical" claims that
    specifically describe the SCAN/advance TRUE-side result (the `advance` subtraction
    doc ~714-724 and the scan-derived claims ~291-292) to "≤1e-5 oracle-equivalent".
    Do NOT touch byte-identity claims that remain TRUE (the FALSE-side prefix, the
    scatter-add build from Task 1, the rayon feature-ordered collect, the leaf-VALUE
    path, the forward-bit leaf ORDER) — those are still bit-exact and their wording
    stays.
  </action>
  <verify>
    <automated>cargo test -p cb-train --release --test rayon_determinism_test &amp;&amp; CB_PERF=1 cargo test --release -p cb-train --test perf_baseline_test -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - ATOMIC GATE: 21-06-SUMMARY records a SINGLE uninterrupted
      `cargo test -p cb-train --release --no-fail-fast` + `cargo test -p cb-compute
      --release` run with the full pass tally and the observed max deviation, ZERO
      fixtures flipped (only the documented pre-existing monotone test may be
      non-green), and the disk-hygiene commands actually used.
    - DETERMINISM: `cargo test -p cb-train --release --test rayon_determinism_test`
      exits 0 (2/2).
    - CB_PERF: 21-06-SUMMARY records the `RSBENCH` per_tree_ms for n_bins ∈ {32,64,254}
      with the 32→254 ratio ≤ ~2.5× and the 64→254 step ≤ ~1.7× (PERF-01 flat within
      noise); state the numeric ratio explicitly.
    - CR-01: `grep -rn 'bit-exact\|byte-for-byte\|byte-identical' crates/cb-compute/src/histogram.rs crates/cb-train/src/tree.rs`
      shows the TRUE-side scan/advance claims downgraded to "≤1e-5 oracle-equivalent";
      `grep -rn '≤1e-5 oracle-equivalent\|<=1e-5 oracle-equivalent' crates/cb-compute/src/histogram.rs`
      returns ≥1.
    - The atomic gate is stated in the SUMMARY as the explicit authorization for the
      TRUE-side reorder (traceable to PERF-02).
  </acceptance_criteria>
  <done>The TRUE-side reorder is authorized by a single atomic zero-flip oracle pass; determinism stays 2/2; the CB_PERF 32→254 ratio is recorded flat-within-noise (≤ ~2.5×, PERF-01 met); overstated bit-exact claims on the reordered path are downgraded to ≤1e-5 oracle-equivalent (CR-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process CPU numeric refactor — no network, auth, file, serialization, or trust-boundary crossing. All inputs are already-validated in-memory training buffers. |

## STRIDE Threat Register (carried forward from 21-01..05; no new L1/high threats)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering / DoS | histogram bin/leaf/object index → OOB read/write | mitigate | `.get()`/`.get_mut()` defensive access + `scatter_add_f64`'s guarded `get_mut`; workspace `indexing_slicing` deny. No raw indexing in new code. |
| T-21-02 | DoS | flat-index integer overflow (`n_leaves·n_features·n_bins·n_channels`) | mitigate | `checked_mul(..).unwrap_or(0)` bound (retained); `MAX_DEPTH=16` caps `n_leaves`. |
| T-21-13 | Tampering (integrity) | nondeterministic parallel accumulation (parity break) | mitigate | per-feature-independent rayon work + fixed-order merge (unchanged from 21-05); per-bin/per-cell fold stays sequential `cb_core::sum_f64`/`scatter_add_f64`; `rayon_determinism_test` gate (Task 3). The TRUE-side `total − prefix` reorder is itself gated by the atomic oracle suite (Task 3). |
| T-21-SC | Tampering | `rayon` supply chain (already vendored in Cargo.lock) | accept | No new dependency added by this plan; `rayon` legitimacy verdict OK (21-RESEARCH Package Legitimacy Audit). No package installs in this plan. |

No new threats introduced at ASVS L1/high: this plan adds one in-crate `cb-core`
function and rewrites existing in-crate numeric code paths; the honest threat surface
is unchanged from the shipped phase.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (new symbols / files)

- `cb_core::scatter_add_f64` — new sanctioned object-order scatter-add primitive in
  `crates/cb-core/src/reduction.rs` (the ONLY float-loop-permitted file), exported on
  the `cb_core` surface alongside `sum_f64`.
- Rewritten `build_bucket_histogram` (flat reused `Vec<f64>` accumulator, no per-cell
  `Vec<Vec<f64>>`).
- Rewritten `scan_border_to_leaf_stats` / `scan_borders_to_leaf_stats` (single
  O(n_bins) running-prefix pass; FALSE = running prefix, TRUE = total − prefix).
- Rewritten `GrowScratch::advance` (retained-parent relocation + single small-sibling
  build; optional `GrowScratch::relocate_parent` helper).
- New tests: `reduction_test::scatter_add_matches_sum_f64`,
  `histogram_test::build_flat_scatter_equals_gather`,
  `histogram_test::running_prefix_scan_matches_per_border_reference`.
- `21-06-SUMMARY.md` recording: the atomic oracle-suite pass tally + max deviation,
  the rayon determinism result, and the CB_PERF 32→254 per_tree_ms ratio.
</artifacts_produced>

<verification>
- Task 1 gate: `cargo test -p cb-core --release reduction_test::scatter_add`,
  `cargo test -p cb-compute --release histogram`, `bash scripts/check-no-raw-float-sum.sh`.
- Task 2 gate: `cargo test -p cb-compute --release histogram`,
  `cargo test -p cb-train --release --lib tree::`.
- Task 3 phase gate (the PERF-01/PERF-02 acceptance):
  1. ATOMIC: `CARGO_INCREMENTAL=0 cargo test -p cb-train --release --no-fail-fast`
     then `cargo test -p cb-compute --release` — single uninterrupted pass, zero
     fixtures flipped (except the documented pre-existing monotone test).
  2. `cargo test -p cb-train --release --test rayon_determinism_test` → 2/2.
  3. `CB_PERF=1 cargo test --release -p cb-train --test perf_baseline_test -- --nocapture`
     → 32→254 per_tree_ms ratio ≤ ~2.5×.
- Locked-constraint greps: `grep -rnE 'use +cb_backend|cb_backend::' crates/cb-train/src`
  → none; `grep -nE 'rayon|cubecl' crates/cb-compute/Cargo.toml` → none;
  no edits under `crates/cb-backend/src/kernels/`.
</verification>

<success_criteria>
- PERF-01: CB_PERF n_bins sweep per-tree time flat within noise 32→254 (ratio ≤ ~2.5×,
  64→254 step ≤ ~1.7×), recorded in 21-06-SUMMARY.
- PERF-02: full CPU ≤1e-5 oracle suite passes in a single atomic run, zero fixtures
  flipped — the gate that authorizes the TRUE-side reorder.
- PERF-03: per-level histogram allocation eliminated (flat scatter-add scratch, no
  per-cell Vec; no per-border scan Vec; advance builds one small sibling not three
  full histograms); rayon determinism stays 2/2.
- CR-01 doc honesty: reordered TRUE-side claims downgraded to ≤1e-5 oracle-equivalent.
- No locked constraint violated (no cb-backend dep, cb-compute rayon/cubecl-free, no
  device edits, no model/leaf-value/inference change, no embedded mod tests).
</success_criteria>

<output>
Create `.planning/phases/21-cpu-split-finding-histogram-rewrite/21-06-SUMMARY.md`
when done, recording the atomic oracle-suite tally + max deviation, the rayon
determinism result, the CB_PERF 32→254 per_tree_ms ratio, and the final TRUE-side
strategy that passed the gate (primary total − prefix, or a documented fallback).
</output>
