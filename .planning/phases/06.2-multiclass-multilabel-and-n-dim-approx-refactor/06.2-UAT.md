---
status: testing
phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor
source: [06.2-VERIFICATION.md, 06.2-REVIEW.md]
started: 2026-06-16T00:00:00Z
updated: 2026-06-16T00:00:00Z
---

## Current Test

number: 1
name: Acknowledge CR-01 — public multi-dim predict returns only dimension 0 (intentional deferral?)
expected: |
  Developer confirms that `Model::predict` / `predict_raw` / `predict_raw_cat` returning only
  dimension-0 output for MultiClass / MultiClassOneVsAll / MultiLogloss / MultiCrossEntropy /
  MultiQuantile models (CR-01 in 06.2-REVIEW.md) is a KNOWN, INTENTIONAL deferral — outside the
  ROADMAP SC-3/SC-4 oracle contract (which defines predictions via training-time staged approx,
  verified ≤1e-5) — and is tracked for a future phase (public N-dim model apply + class_params
  serialization round-trip).
awaiting: user response

## Tests

### 1. Acknowledge CR-01 — public multi-dim predict returns only dimension 0
expected: Developer confirms CR-01 is a known intentional deferral, tracked for a future phase (public N-dim `predict_raw_multi` + multiclass model round-trip apply). The per-stage training oracle (Splits/LeafValues/StagedApprox/Predictions-transform) is ≤1e-5 and unaffected.
result: [pending]

### 2. Acknowledge CR-02 — dim-major der passed to bootstrap/MVS/score_st_dev (latent)
expected: Developer acknowledges that passing the dimension-major `weighted_der1` (length `approx_dimension*n`) to `bootstrap()` / `mvs_lambda()` / `score_st_dev()` (which assume per-object length `n`) is LATENT under all committed fixtures (`bootstrap_type=No`, `random_strength=0`) but would corrupt sampling for a multi-output model with those knobs enabled — and is tracked for a hardening task before bootstrap/random_strength are exercised on N-dim models.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
