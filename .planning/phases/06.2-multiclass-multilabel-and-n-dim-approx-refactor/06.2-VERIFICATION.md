---
phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor
verified: 2026-06-16T00:00:00Z
status: human_needed
score: 4/4
overrides_applied: 0
human_verification:
  - test: "Verify that the public predict_raw / predict_raw_cat path on a multi-output model is understood to be out-of-scope for this phase"
    expected: "Developer confirms that Model::predict / predict_raw returning only dimension-0 for MultiClass/MultiLogloss/MultiQuantile models (CR-01 from 06.2-REVIEW.md) is a known, intentional deferral and is tracked for a future phase"
    why_human: "CR-01 is a real correctness defect in the public surface but is explicitly outside the ROADMAP SC-3/SC-4 oracle contract (which defines predictions via training-time staged approx). Whether this needs a follow-up tracking issue or a later-phase plan entry cannot be verified programmatically."
  - test: "Confirm CR-02 bootstrap/MVS/random_strength dim-major buffer corruption is tracked"
    expected: "Developer acknowledges that passing the dim-major weighted_der1 (length approx_dimension*n) to bootstrap/mvs_lambda/score_st_dev is latent under the in-scope fixture params (bootstrap_type=No, random_strength=0) but would corrupt a user enabling those knobs on a multi-output model"
    why_human: "CR-02 is masked by all committed fixtures. Whether it should block 6.3 or be addressed as a hardening task in a subsequent plan cannot be determined without developer intent."
---

# Phase 6.2: Multiclass / Multilabel + N-Dim Approx Refactor — Verification Report

**Phase Goal:** Generalize the core train loop from scalar approx to N-dim (scalar = the dim=1 degenerate case), then implement multiclass/multilabel losses on the stable N-dim foundation — each oracle-locked <=1e-5.
**Verified:** 2026-06-16
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Train loop carries approx as a dimension-major vector everywhere; scalar losses run as dim=1; single code path, no duplication (D-03) | VERIFIED | `boosting.rs::loss_approx_dimension()` returns 1 for all scalar losses; dim-major `approx[d*n+i]` buffer threaded through init/gradient/leaf-delta/approx-update/staged-record; grep confirms a single `for d in 0..approx_dimension` outer loop in `compute_leaf_deltas`, not a parallel scalar path |
| 2 | D-04 HARD CHECKPOINT: ALL scalar oracles re-run green at dim=1 AND an explicit `== 0.0` byte-identity test passes | VERIFIED | `cargo test -p cb-train --test ndim_dim1_identity_test` — 2/2 passed (`ndim_dim1_leaf_values_are_byte_identical`, `ndim_dim1_staged_approx_is_byte_identical`); `cargo test -p cb-train --tests` — 0 failures across all 35 integration test binaries (251 passed, 5 pre-existing #[ignore]) |
| 3 | MultiClass (softmax), MultiClassOneVsAll, MultiLogloss, MultiCrossEntropy each pass per-stage oracle <=1e-5 | VERIFIED | `cargo test -p cb-train --test multiclass_oracle_test --test multilabel_oracle_test` — 4/4 passed (2 multiclass + 2 multilabel), all four stages (Splits/LeafValues/StagedApprox/Predictions) <=1e-5 vs catboost 1.2.10, no #[ignore] |
| 4 | MultiQuantile produces per-quantile outputs matching upstream <=1e-5 on the N-dim approx path | VERIFIED | `cargo test -p cb-train --test multiquantile_oracle_test` — 1/1 passed (Splits/LeafValues/StagedApprox/RAW Predictions all <=1e-5 vs catboost 1.2.10, alpha=[0.1,0.5,0.9], Exact leaf, no #[ignore]) |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-compute/src/runtime.rs` | Loss::MultiClass, MultiClassOneVsAll, MultiLogloss, MultiCrossEntropy, MultiQuantile{alpha,delta}; approx_dimension on Runtime::compute_gradients; Copy removed from Loss | VERIFIED | All 5 variants present; `#[derive(Debug, Clone, PartialEq)]` (no Copy); `compute_gradients` signature carries `approx_dimension: usize` and `&Loss` |
| `crates/cb-compute/src/loss.rs` | softmax_ders (coupled der + packed symmetric Hessian), multiclass_onevsall_ders, multi_crossentropy_ders | VERIFIED | All three helpers present with upstream error_functions.h file:line doc comments; `calc_softmax` with max-subtraction |
| `crates/cb-compute/src/leaf.rs` | solve_symmetric_newton + cholesky_solve (hand-rolled, hessian.cpp:22-52 transcription); git-diff-clean for MultiQuantile (Exact leaf reused verbatim) | VERIFIED | `solve_symmetric_newton` and `cholesky_solve` present in leaf.rs; `leaf.rs` is git-diff-clean vs the MultiQuantile plan (Exact leaf reused without modification) |
| `crates/cb-compute/src/score.rs` | multi_dim_split_score with single shared accumulator (Cosine couples num/den inside sqrt, transform once after dim loop) | VERIFIED | `multi_dim_split_score` present; single `num_terms`/`den_terms` across all dims; `num/sqrt(den)` applied once after the per-dim loop |
| `crates/cb-train/src/boosting.rs` | N-dim approx buffer; loss_approx_dimension(); class remap (LabelToClass/class_to_label); compute_softmax_leaf_deltas; dim_index on compute_leaf_deltas | VERIFIED | All seams present; `approx_dimension` threaded through all 5 buffer operations; `loss_approx_dimension` dispatches per-loss; `build_class_remap`/`remap_target_to_class` for multiclass; `dim_index` selector for MultiQuantile Exact leaf |
| `crates/cb-train/src/tree.rs` | multi_dim_candidate_score routing score_candidate through the shared accumulator; approx_dimension inferred from der1.len()/n_objects | VERIFIED | `multi_dim_candidate_score` in tree.rs; dim=1 bit-identical via inferred dimension |
| `crates/cb-model/src/cbm.rs` | ApproxDimension reads the real dim (not hardcoded 1); leaf-major transpose on save, un-transpose on load | VERIFIED | `ApproxDimension: approx_dimension` (not `ApproxDimension: 1`); `transpose_dim_major_to_leaf_major`/`transpose_leaf_major_to_dim_major` helpers in json.rs; separate value/weight cursors on reconstruct |
| `crates/cb-model/src/predict.rs` | apply_multiclass_prediction (softmax/sigmoid Probability + argmax Class); MultiClassKind::MultiLabel (per-dim sigmoid); per-quantile RAW identity | VERIFIED | `apply_multiclass_prediction` present; `MultiClassKind` with Softmax, OneVsAll, MultiLabel; RAW predictions use the existing dim-agnostic `RawFormulaVal` arm |
| `crates/cb-train/tests/ndim_dim1_identity_test.rs` | `== 0.0` byte-identity gate (test-first, no #[ignore], no tolerance) | VERIFIED | 194 lines; two `== 0.0` assertions (`diff == 0.0`); no `#[ignore]`; test passes |
| `crates/cb-train/tests/multiclass_oracle_test.rs` | MultiClass + MultiClassOneVsAll per-stage oracle <=1e-5 | VERIFIED | 282 lines; Splits/LeafValues/StagedApprox/Predictions all checked; 2/2 tests pass |
| `crates/cb-train/tests/multilabel_oracle_test.rs` | MultiLogloss + MultiCrossEntropy per-stage oracle <=1e-5 | VERIFIED | 262 lines; all four stages checked; 2/2 tests pass |
| `crates/cb-train/tests/multiquantile_oracle_test.rs` | MultiQuantile per-stage oracle <=1e-5 | VERIFIED | 229 lines; Splits/LeafValues/StagedApprox/RAW Predictions checked; 1/1 test passes |
| `crates/cb-oracle/fixtures/multiclass_softmax/{model.json,staged.npy,predictions.npy,config.json}` | Frozen catboost 1.2.10 fixture | VERIFIED | All 4 files present |
| `crates/cb-oracle/fixtures/multiclass_onevsall/{model.json,staged.npy,predictions.npy,config.json}` | Frozen catboost 1.2.10 fixture | VERIFIED | All 4 files present |
| `crates/cb-oracle/fixtures/multilogloss/{model.json,staged.npy,predictions.npy,config.json}` | Frozen catboost 1.2.10 fixture | VERIFIED | All 4 files present |
| `crates/cb-oracle/fixtures/multicrossentropy/{model.json,staged.npy,predictions.npy,config.json}` | Frozen catboost 1.2.10 fixture | VERIFIED | All 4 files present |
| `crates/cb-oracle/fixtures/multiquantile/{model.json,staged.npy,predictions.npy,config.json}` | Frozen catboost 1.2.10 fixture (alpha=[0.1,0.5,0.9], Exact) | VERIFIED | All 4 files present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `crates/cb-train/src/boosting.rs` | `Runtime::compute_gradients` | `runtime.compute_gradients(&params.loss, &approx, target, approx_dimension)` call | VERIFIED | grep confirms `compute_gradients.*approx_dimension` pattern in boosting.rs:1951 |
| `crates/cb-model/src/cbm.rs` | `.cbm LeafValues` | `ApproxDimension` reads model's real dim; leaf-major transpose `leaf[d][l] -> leaf_values[l*dim+d]` | VERIFIED | `ApproxDimension: approx_dimension` (not hardcoded 1); `transpose_dim_major_to_leaf_major` helper confirmed |
| `crates/cb-train/src/boosting.rs` | `softmax symmetric Hessian solve` | `compute_softmax_leaf_deltas` branch (per-leaf SolveNewtonEquation / Cholesky) | VERIFIED | `compute_softmax_leaf_deltas` present; `solve_symmetric_newton` wired; `MultiClass` branch in compute_leaf_deltas |
| `crates/cb-model/src/predict.rs` | `class labels` | `apply_multiclass_prediction` softmax/sigmoid Probability + argmax Class + ClassToLabel mapping | VERIFIED | Function present; `class_to_label` threaded through `from_trained` into `cb_model::Model`; argmax + label recovery in Class arm |
| `crates/cb-train/src/boosting.rs` | `exact_leaf_delta` (MultiQuantile) | `compute_leaf_deltas` Exact arm passes `alpha[dim_index]` per dimension | VERIFIED | `dim_index` param on `compute_leaf_deltas`; `Loss::MultiQuantile { alpha, .. }` arm selects `alpha[dim_index]` for the unchanged `exact_leaf_delta` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `multiclass_oracle_test.rs` Predictions stage | `final_raw_approx(&staged, dim, n)` | Training-time `staged` Vec populated by `train()` boosting loop | Yes — real staged approx from dim-major buffer | FLOWING |
| `multilabel_oracle_test.rs` Predictions stage | `final_raw_approx(&staged, dim, n)` | Training-time `staged` Vec from boosting loop | Yes | FLOWING |
| `multiquantile_oracle_test.rs` Predictions stage | `final_raw_approx(&staged, dim, n)` via `RawFormulaVal` arm | Training-time staged approx | Yes | FLOWING |
| `crates/cb-model/src/apply.rs::predict_raw` | `tree.leaf_values.get(leaf)` | Scalar dim-0 only (`leaf ∈ [0, n_leaves)`) | Partial — returns dim-0 only for multi-output models | HOLLOW (known, out-of-scope — see CR-01 note below) |

**Note on CR-01 (public predict_raw scope):** The oracle Predictions stage in all three oracle tests is computed from the training-time `staged` approx buffer fed into `apply_multiclass_prediction` — NOT from `predict_raw`. The ROADMAP success criteria define the "oracle" as per-stage (Splits/LeafValues/StagedApprox/Predictions from training), consistent with the D-6.2-06 per-stage floor. The public `predict_raw` / `predict_raw_cat` path in `apply.rs` is correctly recognized as dim-0 only for multi-output models (CR-01 from 06.2-REVIEW.md) and was explicitly deferred by the executor. This is a real public-API defect but it does not block the four ROADMAP success criteria as written.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| D-04 byte-identity gate (== 0.0) | `cargo test -p cb-train --test ndim_dim1_identity_test` | 2 passed, 0 failed | PASS |
| MultiClass + MultiClassOneVsAll oracle <=1e-5 | `cargo test -p cb-train --test multiclass_oracle_test` | 2 passed, 0 failed | PASS |
| MultiLogloss + MultiCrossEntropy oracle <=1e-5 | `cargo test -p cb-train --test multilabel_oracle_test` | 2 passed, 0 failed | PASS |
| MultiQuantile oracle <=1e-5 | `cargo test -p cb-train --test multiquantile_oracle_test` | 1 passed, 0 failed | PASS |
| Full scalar oracle suite (D-04 holding through all waves) | `cargo test -p cb-train --tests` | 0 failures across all 35 integration test binaries (251 passed, 5 pre-existing #[ignore]) | PASS |

### Probe Execution

Step 7c: SKIPPED — no probe-*.sh files declared or conventional for this phase. Oracle gates are Rust integration tests run above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| LOSS-02 | 06.2-03, 06.2-04 | Multiclass (MultiClass softmax, MultiClassOneVsAll) and multilabel (MultiLogloss, MultiCrossEntropy) | SATISFIED | All four losses have variants in `Loss` enum, per-dim der implementations, frozen oracle fixtures, and passing per-stage oracle tests (<=1e-5) |
| LOSS-03 (MultiQuantile member) | 06.2-05 | MultiQuantile multi-output: K independent Quantile dimensions on the N-dim foundation | SATISFIED | `Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 }` present; per-dim quantile der reusing `launch_quantile_f64` with `alpha[d]`; per-dim Exact leaf via `alpha[dim_index]`; fixture + oracle test green |

Both requirement IDs claimed by the phase plans are accounted for and satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `crates/cb-model/src/cbm.rs` | load path | `class_to_label: Vec::new()` default on deserialization | WARNING | Intentional documented stub — per-stage train oracle builds model via `from_trained` (which carries the real map); full `.cbm`/json class_params round-trip deferred. Not a goal-blocking stub. |
| `crates/cb-oracle/generator/gen_fixtures.py` | `config["target_rule"]` | WR-03: `target_rule` string contradicts actual multilabel target code (committed string describes per-feature thresholds; actual code uses nested y-quantile thresholds) | WARNING | Anyone regenerating the fixture from `config.json`'s `target_rule` would produce a different label matrix, breaking the oracle lock. Latent — does not affect the currently committed fixture or the passing oracle test. |
| `crates/cb-train/src/boosting.rs` | 1981-2010 | CR-02: `weighted_der1` (length `approx_dimension*n`) passed unmodified to `bootstrap()`, `mvs_lambda()`, and `score_st_dev()` which assume per-object length n | WARNING | Latent under `bootstrap_type=No`, `random_strength=0` (all in-scope fixtures). Would corrupt sampling semantics and RNG phase for any multi-output model with sampling enabled. A real latent defect in the public API. |
| `crates/cb-backend/src/cpu_runtime.rs` | 714-717 | WR-02: `compute_multiquantile_gradients` maps per-dim `launch_quantile_f64` Err to `der1.extend(zeros)` (silent swallow) instead of propagating the error | WARNING | Mirrors the WR-04 pattern fixed in 6.1 for MAE. Device read-back failure masquerades as valid all-zero gradient for that quantile dimension. Error channel exists (`CbResult`). |
| `crates/cb-train/src/tree.rs` | 409-415 | WR-01: `der1.get(base..base+n_objects).unwrap_or(der1)` falls back to the entire `der1` buffer on a bad stride | WARNING | Silent wrong-dimension data instead of a typed error. Shape is validated upstream so this is a defense-in-depth gap. |

No `TBD`, `FIXME`, or `XXX` markers found in any modified files. No blocker anti-patterns (the above are all WARNINGs — latent under in-scope params, not reachable by the committed oracle fixtures).

### Human Verification Required

#### 1. CR-01: Public predict_raw multi-dim scope confirmation

**Test:** Review `crates/cb-model/src/apply.rs` — `predict_raw_one` reads only `tree.leaf_values.get(leaf)` where `leaf ∈ [0, n_leaves)`, returning a single `f64` per object. For a multi-output model with `approx_dimension > 1`, this reads only dimension 0 (leaves `[0..n_leaves-1]` of a `leaf_values` buffer of length `approx_dimension * n_leaves`). Confirm that this is a known, intentional deferral and that the oracle contract for SC-3 and SC-4 is satisfied by the training-time per-stage oracle (not the public apply path).
**Expected:** Developer confirms: (a) CR-01 is a known deferred item, not an oversight; (b) the ROADMAP oracle contract for SC-3/SC-4 is the per-stage training oracle (Splits/LeafValues/StagedApprox/Predictions from `staged` buffer), which passes; (c) a future plan entry (likely 6.4 or PYAPI) will wire the multi-dim apply path.
**Why human:** The oracle tests provably pass. Whether the public API defect blocks progression to 6.3 (which depends on Phase 6.2) is a project-policy decision requiring developer confirmation. Programmatic verification cannot determine intent.

#### 2. CR-02: Bootstrap / MVS / random_strength dim-major buffer — follow-up tracking

**Test:** Inspect `crates/cb-train/src/boosting.rs` around lines 1981-2034. `weighted_der1` is `dim * n` elements; it is passed to `bootstrap(params.bootstrap_type, &weighted_der1, …)` and `score_st_dev(params.random_strength, &weighted_der1, …)`. Confirm the developer is aware this would corrupt sampling semantics for any multi-output training run with `bootstrap_type != No` or `random_strength != 0`.
**Expected:** Developer confirms: (a) the defect is documented in 06.2-REVIEW.md CR-02 and is already known; (b) a plan entry (hardening, 6.2 gap closure, or 6.3 pre-work) will fix it before any user-facing sampling on multi-output models is advertised. All committed fixtures pin `bootstrap_type=No` / `random_strength=0` so no passing oracle tests are affected.
**Why human:** Whether this should block 6.3 (no multi-output losses in ranking losses, so arguably safe to defer) or needs immediate closure is a project-scheduling decision.

---

## Gaps Summary

All four ROADMAP success criteria are VERIFIED by passing Rust integration tests with direct codebase evidence. No truths are FAILED. The `status: human_needed` reflects two review findings (CR-01, CR-02) that require developer acknowledgment before Phase 6.2 is declared fully closed — these are real defects in the public surface, both latent under the committed fixtures, and both documented in 06.2-REVIEW.md. They do not block the per-stage oracle contract as defined in the ROADMAP.

The two human items are escalation-gate items: the developer must confirm that these are known deferred items (not oversights) and either add follow-up tracking or immediately plan closure. Once confirmed, the phase can be marked `passed`.

**Known follow-up items documented in 06.2-REVIEW.md (not blocking ROADMAP SCs):**
- CR-01: `predict_raw` / `predict_raw_cat` returns dim-0 only for multi-output models — no `predict_raw_multi` exists
- CR-02: `weighted_der1` length `dim*n` passed to bootstrap/mvs_lambda/score_st_dev — would corrupt sampling for `bootstrap_type != No` or `random_strength != 0` on a multi-output model
- WR-01: `multi_dim_candidate_score` falls back to entire `der1` buffer on bad stride (silent wrong data vs typed error)
- WR-02: MultiQuantile per-dim launch failure silently swallowed to zero gradient
- WR-03: oracle generator `config["target_rule"]` contradicts actual multilabel target code
- WR-05: `softmax_ders` / `compute_softmax_gradients` has no `target_class < k` bounds check at the backend call site
- WR-06: `build_class_remap` uses `partial_cmp().unwrap_or(Equal)` sort + `abs()==0.0` dedup (inconsistent with `total_cmp` precedent in leaf.rs)

---

_Verified: 2026-06-16_
_Verifier: Claude (gsd-verifier)_
