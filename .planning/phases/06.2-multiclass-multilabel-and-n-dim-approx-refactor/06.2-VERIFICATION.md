---
phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor
verified: 2026-06-16T12:30:00Z
status: passed
score: 4/4
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 4/4
  gaps_closed:
    - "CR-01: predict_raw / predict_raw_cat now routes approx_dimension>1 through predict_raw_multi — all 5 multi-output losses predict all dims; class_to_label round-trips on .cbm + json; 6/6 public-load oracle tests pass"
    - "CR-02: weighted_der1 reduced to per-object L2-norm der_obj[i]=sqrt(Σ_d wd[d*n+i]²) (length n) before bootstrap/mvs_lambda; score_st_dev divides by n not dim*n; WR-04 debug_assert guards length; dim=1 byte-identical (D-04)"
  gaps_remaining: []
  regressions: []
---

# Phase 6.2: Multiclass / Multilabel + N-Dim Approx Refactor — Verification Report

**Phase Goal:** Generalize the core train loop from scalar approx to N-dim (scalar = the dim=1 degenerate case), then implement multiclass/multilabel losses on the stable N-dim foundation — each oracle-locked <=1e-5.
**Verified:** 2026-06-16T12:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (06.2-06 CR-01, 06.2-07 CR-02)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Train loop carries approx as a dimension-major vector everywhere; scalar losses run as dim=1; single code path, no duplication (D-03) | VERIFIED | `boosting.rs::loss_approx_dimension()` returns 1 for all scalar losses; dim-major `approx[d*n+i]` buffer threaded through init/gradient/leaf-delta/approx-update/staged-record; single `for d in 0..approx_dimension` outer loop in `compute_leaf_deltas`, not a parallel scalar path |
| 2 | D-04 HARD CHECKPOINT: ALL scalar oracles re-run green at dim=1 AND an explicit `== 0.0` byte-identity test passes | VERIFIED | `cargo test -p cb-train --test ndim_dim1_identity_test` — 2/2 passed (`ndim_dim1_leaf_values_are_byte_identical`, `ndim_dim1_staged_approx_is_byte_identical`). Post-gap-closure re-lock: `cargo test -p cb-train --tests` — 0 failures across all integration test binaries. D-04 holds on the PUBLIC apply surface: `predict_raw_multi_test::dim1_byte_identical_to_predict_raw` uses `to_bits()` exact equality — 4/4 passed. |
| 3 | MultiClass (softmax), MultiClassOneVsAll, MultiLogloss, MultiCrossEntropy each pass per-stage oracle <=1e-5 | VERIFIED | `cargo test -p cb-train --test multiclass_oracle_test --test multilabel_oracle_test` — 4/4 passed (2 multiclass + 2 multilabel). All four stages (Splits/LeafValues/StagedApprox/Predictions) <=1e-5 vs catboost 1.2.10, no `#[ignore]`. Public load->predict oracle via `predict_raw_multi` also confirmed: `multiclass_apply_oracle_test` — 6/6 passed (5 fixtures + class-label round-trip). |
| 4 | MultiQuantile produces per-quantile outputs matching upstream <=1e-5 on the N-dim approx path | VERIFIED | `cargo test -p cb-train --test multiquantile_oracle_test` — 1/1 passed (Splits/LeafValues/StagedApprox/RAW Predictions all <=1e-5 vs catboost 1.2.10, alpha=[0.1,0.5,0.9], Exact leaf, no `#[ignore]`). Public oracle: `public_oracle_multiquantile` in `multiclass_apply_oracle_test` — passed. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-compute/src/runtime.rs` | Loss::MultiClass, MultiClassOneVsAll, MultiLogloss, MultiCrossEntropy, MultiQuantile{alpha,delta}; approx_dimension on Runtime::compute_gradients; Copy removed from Loss | VERIFIED | All 5 variants present; `#[derive(Debug, Clone, PartialEq)]` (no Copy); `compute_gradients` carries `approx_dimension: usize` |
| `crates/cb-compute/src/loss.rs` | softmax_ders (coupled der + packed symmetric Hessian), multiclass_onevsall_ders, multi_crossentropy_ders | VERIFIED | All three helpers present with upstream file:line doc comments; `calc_softmax` with max-subtraction |
| `crates/cb-compute/src/leaf.rs` | solve_symmetric_newton + cholesky_solve (hand-rolled, hessian.cpp:22-52 transcription) | VERIFIED | Both present in leaf.rs |
| `crates/cb-compute/src/score.rs` | multi_dim_split_score; `derivatives_std_dev_from_zero(weighted_der1, n)` and `score_st_dev(rs, weighted_der1, n, model_length)` with explicit per-object count (CR-02 fix) | VERIFIED | `multi_dim_split_score` present; `derivatives_std_dev_from_zero` at line 174 and `score_st_dev` at line 207 both take explicit `n: usize` — divide by `n`, not buffer length |
| `crates/cb-train/src/boosting.rs` | N-dim approx buffer; loss_approx_dimension(); class remap; compute_softmax_leaf_deltas; dim_index; per-object L2-norm der_obj aggregation feeding bootstrap/mvs_lambda (CR-02 fix); WR-04 length asserts | VERIFIED | `der_obj` Vec computed at boosting.rs:1991-2001 as `sqrt(Σ_d wd[d*n+i]²)`; `debug_assert_eq!(sampled.sample_weights.len(), n)` and `debug_assert_eq!(sampled.control.len(), n)` at lines 2018-2027; all prior seams confirmed present |
| `crates/cb-train/src/tree.rs` | multi_dim_candidate_score; WR-01 empty-slice fallback on stride mismatch (not whole-buffer substitution) | VERIFIED | `der1.get(base..base+n_objects).unwrap_or(&[])` at tree.rs:419 — empty slice on bad stride, never the whole `der1` buffer |
| `crates/cb-model/src/cbm.rs` | ApproxDimension reads real dim; leaf-major transpose; class_to_label round-trip via InfoMap (CR-01 fix) | VERIFIED | `class_to_label` read via `read_class_to_label` from TModelCore InfoMap; `class_params` written back when non-empty; confirmed at cbm.rs:244-489 |
| `crates/cb-model/src/json.rs` | class_params/multiclass_params deserialize fallback; `model_info_from_class_labels` emitter (CR-01 fix) | VERIFIED | `class_to_label_from_model_info` at json.rs:119 tries `class_params` then `multiclass_params`; `model_info_from_class_labels` at json.rs:136; `#[serde(default)]` + `skip_serializing_if` confirmed |
| `crates/cb-model/src/apply.rs` | `predict_raw_multi` public N-dim apply (dim-major out[d*n+i]); `predict_raw_cat` routes approx_dimension>1 to dim-aware accumulator; byte-identical at dim=1 (CR-01 fix) | VERIFIED | `predict_raw_multi` at apply.rs:315; `predict_raw_multi_cat` internal accumulator at apply.rs:333-383; dispatch at apply.rs:277-279: `if model.approx_dimension > 1 { return predict_raw_multi_cat(…) }`. All access via checked `.get` |
| `crates/cb-model/src/predict.rs` | apply_multiclass_prediction (softmax/sigmoid Probability + argmax Class); MultiClassKind; per-quantile RAW identity | VERIFIED | All present; now has a PUBLIC producer via predict_raw_multi (CR-01 closed) |
| `crates/cb-backend/src/cpu_runtime.rs` | softmax/OneVsAll producers return CbResult; reject target_class>=k with CbError::OutOfRange (WR-05 fix) | VERIFIED | `compute_softmax_gradients` at cpu_runtime.rs:633 returns `CbResult`; `target_class >= k` check at lines 648-650 emits `CbError::OutOfRange` |
| `crates/cb-train/tests/ndim_dim1_identity_test.rs` | `== 0.0` byte-identity gate (no tolerance, no `#[ignore]`) | VERIFIED | 2/2 tests pass |
| `crates/cb-train/tests/multiclass_oracle_test.rs` | MultiClass + MultiClassOneVsAll per-stage oracle <=1e-5 | VERIFIED | 2/2 pass |
| `crates/cb-train/tests/multilabel_oracle_test.rs` | MultiLogloss + MultiCrossEntropy per-stage oracle <=1e-5 | VERIFIED | 2/2 pass |
| `crates/cb-train/tests/multiquantile_oracle_test.rs` | MultiQuantile per-stage oracle <=1e-5 | VERIFIED | 1/1 pass |
| `crates/cb-model/tests/predict_raw_multi_test.rs` | predict_raw_multi dim-major accumulation + dim=1 byte-identity (`to_bits()`) | VERIFIED | 4/4 passed (`dim1_byte_identical_to_predict_raw`, `dim1_byte_identical_empty_and_biasonly`, `multi_dim_accumulation_dim_major_output`, `out_of_range_leaf_contributes_zero_no_panic`) |
| `crates/cb-model/tests/multiclass_apply_oracle_test.rs` | Public load->predict oracle for all 5 multi-output losses + class-label round-trip | VERIFIED | 6/6 passed (`public_oracle_multiclass_softmax`, `public_oracle_multiclass_onevsall`, `public_oracle_multilogloss`, `public_oracle_multicrossentropy`, `public_oracle_multiquantile`, `public_oracle_class_labels_round_trip`) |
| `crates/cb-model/tests/class_params_roundtrip_test.rs` | class_to_label .cbm + json round-trip | VERIFIED | File present (created by 06.2-06); 4/4 tests pass (confirmed by cb-model --tests 61 passed, 0 failed) |
| `crates/cb-train/tests/multidim_sampling_regression_test.rs` | CR-02/WR-05 regression: Bernoulli + MVS multi-output training no-corruption; std-dev divide-by-n; dim=1 reduction | VERIFIED | 5/5 passed (`std_dev_divides_by_n_not_dim_n_on_multidim_buffer`, `std_dev_dim1_reduces_to_scalar_rms`, `multiclass_gradient_producer_rejects_out_of_range_class`, `multiclass_onevsall_bernoulli_random_strength_trains_per_object`, `multiclass_onevsall_mvs_trains_per_object`). 228 lines, substantive. |
| Oracle fixtures (5 multi-output sets) | All 4 files per fixture (model.json, staged.npy, predictions.npy, config.json) | VERIFIED | Confirmed present in initial verification; unchanged by gap-closure plans |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `crates/cb-train/src/boosting.rs` | `Runtime::compute_gradients` | `runtime.compute_gradients(&params.loss, &approx, target, approx_dimension)` | VERIFIED | Confirmed in initial verification; unchanged |
| `crates/cb-model/src/apply.rs::predict_raw_cat` | `predict_raw_multi_cat` | `if model.approx_dimension > 1` dispatch at apply.rs:277 | VERIFIED | Direct code read: branch routes to dim-aware accumulator when dim>1 |
| `predict_raw_multi` | `apply_multiclass_prediction` | `multiclass_apply_oracle_test` calls `load_json` -> `predict_raw_multi` -> `apply_multiclass_prediction` | VERIFIED | 6/6 oracle tests pass; no `from_trained` in the predict path |
| `crates/cb-train/src/boosting.rs::der_obj` | `bootstrap()` / `mvs_lambda()` | `bootstrap(params.bootstrap_type, &der_obj, …)` at boosting.rs:2005-2012 | VERIFIED | `der_obj` is the per-object L2-norm vector (length n); direct code read at boosting.rs:1991-2001 |
| `crates/cb-compute/src/score.rs::derivatives_std_dev_from_zero` | explicit `n` divisor | `sum_sq / n as f64` (not `weighted_der1.len()`) | VERIFIED | `derivatives_std_dev_from_zero(weighted_der1: &[f64], n: usize)` signature at score.rs:174; divides by `n` |
| `crates/cb-backend/src/cpu_runtime.rs::compute_softmax_gradients` | `CbError::OutOfRange` on OOB class | `if target_class >= k { return Err(CbError::OutOfRange(…)) }` at cpu_runtime.rs:648-650 | VERIFIED | grep confirms pattern at line 648 |
| `crates/cb-model/src/cbm.rs` | `class_to_label` InfoMap | `read_class_to_label` parses `class_params` JSON string from TModelCore InfoMap on decode | VERIFIED | cbm.rs:353 `read_class_to_label(&model_core)` + json string write at cbm.rs:248-254 |
| `crates/cb-model/src/json.rs` | `class_params.class_to_label` | `class_to_label_from_model_info` tries `class_params` then `multiclass_params`; `model_info_from_class_labels` emits on save | VERIFIED | json.rs:119-144; `from_doc` wiring at json.rs:327 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `multiclass_oracle_test.rs` Predictions stage | `final_raw_approx(&staged, dim, n)` | Training-time `staged` Vec populated by boosting loop | Yes — real staged approx from dim-major buffer | FLOWING |
| `multilabel_oracle_test.rs` Predictions stage | `final_raw_approx(&staged, dim, n)` | Training-time `staged` Vec from boosting loop | Yes | FLOWING |
| `multiquantile_oracle_test.rs` Predictions stage | `final_raw_approx(&staged, dim, n)` via `RawFormulaVal` arm | Training-time staged approx | Yes | FLOWING |
| `multiclass_apply_oracle_test.rs` (CR-01 closure) | `predict_raw_multi(model, feature_values)` | `load_json(model.json)` -> dim-major accumulator reading `leaf_values[d*n_leaves+leaf]` for each d | Yes — reads all approx_dimension outputs from the leaf-major-on-wire / dim-major-in-memory buffer; confirmed by 6/6 oracle tests at <=1e-5 | FLOWING |
| `multidim_sampling_regression_test.rs` (CR-02 closure) | `der_obj[i]` per-object | `weighted_der1[d*n+i]` squared, summed via `sum_f64`, sqrt | Yes — real gradient norm over all dims; 5/5 tests confirm no corruption | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| D-04 byte-identity gate (== 0.0) | `cargo test -p cb-train --test ndim_dim1_identity_test` | 2 passed, 0 failed | PASS |
| predict_raw_multi dim=1 byte-identity (to_bits()) | `cargo test -p cb-model --test predict_raw_multi_test` | 4 passed, 0 failed | PASS |
| MultiClass + MultiClassOneVsAll oracle <=1e-5 | `cargo test -p cb-train --test multiclass_oracle_test` | 2 passed, 0 failed | PASS |
| MultiLogloss + MultiCrossEntropy oracle <=1e-5 | `cargo test -p cb-train --test multilabel_oracle_test` | 2 passed, 0 failed | PASS |
| MultiQuantile oracle <=1e-5 | `cargo test -p cb-train --test multiquantile_oracle_test` | 1 passed, 0 failed | PASS |
| Public load->predict oracle for 5 multi-output losses (CR-01) | `cargo test -p cb-model --test multiclass_apply_oracle_test` | 6 passed, 0 failed | PASS |
| CR-02 sampling regression: Bernoulli + MVS + std-dev divisor + WR-05 OOB class | `cargo test -p cb-train --test multidim_sampling_regression_test` | 5 passed, 0 failed | PASS |
| Scalar bootstrap oracle unchanged (CR-02 dim=1 byte-identity) | `cargo test -p cb-train --test bootstrap_oracle_test` | 4 passed, 1 pre-existing #[ignore] (Bayesian multi-tree), 0 failed | PASS |

### Probe Execution

Step 7c: SKIPPED — no probe-*.sh files declared or conventional for this phase. Oracle gates are Rust integration tests run above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| LOSS-02 | 06.2-03, 06.2-04 | Multiclass (MultiClass softmax, MultiClassOneVsAll) and multilabel (MultiLogloss, MultiCrossEntropy) | SATISFIED | All four losses: variants in `Loss` enum, per-dim der implementations, frozen oracle fixtures, per-stage oracle tests <=1e-5 (4/4), AND public load->predict oracle <=1e-5 (multiclass_apply_oracle_test 6/6) |
| LOSS-03 (MultiQuantile member) | 06.2-05 | MultiQuantile multi-output: K independent Quantile dimensions on the N-dim foundation | SATISFIED | `Loss::MultiQuantile { alpha: Vec<f64>, delta: f64 }`; per-dim quantile der reusing `launch_quantile_f64` with `alpha[d]`; per-dim Exact leaf via `alpha[dim_index]`; fixture + oracle test green; public predict oracle confirmed |

Both requirement IDs claimed by the phase plans are accounted for and satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `crates/cb-backend/src/cpu_runtime.rs` | 739 | WR-02: `compute_multiquantile_gradients` maps per-dim `launch_quantile_f64` Err to `der1.extend(zeros)` (silent swallow) | WARNING | Carried forward from initial verification. Out of scope for 06.2-07 (confirmed in 06.2-07 SUMMARY). Latent — device read-back failure masquerades as valid all-zero derivative. |
| `crates/cb-oracle/generator/gen_fixtures.py` | `config["target_rule"]` | WR-03: `target_rule` string contradicts actual multilabel target code | WARNING | Carried forward from initial verification. Out of scope for 06.2-07. Latent — does not affect committed fixtures or passing oracle tests. |
| `crates/cb-train/src/boosting.rs` | 730-735 | WR-06: `build_class_remap` uses `partial_cmp().unwrap_or(Equal)` sort + `abs()==0.0` dedup (NaN/-0.0 hazard vs `total_cmp` precedent in leaf.rs) | WARNING | Carried forward from initial verification. Out of scope for 06.2-07. Latent under integer-class labels. |

No `TBD`, `FIXME`, or `XXX` markers found in any modified files. No blocker anti-patterns.

**CR-01 and CR-02 are RESOLVED (previously WARNING/blocker blocking human sign-off — now CLOSED by code changes with passing tests):**
- CR-01: `predict_raw_multi` exists; `predict_raw_cat` dispatches on `approx_dimension > 1`; `class_to_label` round-trips on both .cbm and json; 6/6 public-load oracle tests pass at <=1e-5. CLOSED by commit b7b3f26 + a7795bf + a98261e.
- CR-02: `der_obj` per-object L2-norm reduction (length n) feeds `bootstrap`/`mvs_lambda`; `score_st_dev` divides by `n`; WR-04 asserts on sample_weights/control length; D-04 byte-identity confirmed at dim=1. CLOSED by commit 98a9b67 + 45beb79.
- WR-05: `compute_softmax_gradients`/`compute_onevsall_gradients` reject `target_class >= k` with `CbError::OutOfRange`. CLOSED by commit 86a44b8.
- WR-01: `multi_dim_candidate_score` uses `unwrap_or(&[])` (zero-score empty slice) on stride mismatch. CLOSED by commit 86a44b8.

### Human Verification Required

None. The two items from the initial `human_needed` verdict (CR-01 and CR-02) have been RESOLVED VIA CODE FIXES in plans 06.2-06 and 06.2-07. They were not deferred or overridden — the defects are closed. No new human-testable items were identified.

---

## Re-verification Summary

**Previous status:** `human_needed` (4/4 SC verified; 2 Critical review findings blocked sign-off)

**Gap closure plans executed:**
- **06.2-06** (3 commits: b7b3f26, a7795bf, a98261e) — CR-01: `predict_raw_multi` public N-dim apply + `class_to_label` .cbm/json round-trip + 6/6 public-load oracle tests
- **06.2-07** (3 commits: 98a9b67, 86a44b8, 45beb79) — CR-02: per-object derivative aggregation for sampling + correct std-dev divisor + WR-05 target_class<k bound + WR-01 stride guard + 5/5 regression tests

**Current status:** `passed` — all 4 ROADMAP success criteria VERIFIED, both Criticals RESOLVED, no human verification items remain, no blocker anti-patterns.

Remaining deferred items (WR-02, WR-03, WR-06 from 06.2-REVIEW.md) are latent WARNINGs explicitly out of scope for these gap-closure plans; they do not block the ROADMAP oracle contract and are carried forward for a future hardening phase.

---

_Verified: 2026-06-16T12:30:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — gap closure 06.2-06 (CR-01) + 06.2-07 (CR-02)_
