---
phase: 06.2-multiclass-multilabel-and-n-dim-approx-refactor
plan: 02
type: execute
wave: 2
depends_on: ["06.2-01"]
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/bootstrap.rs
  - crates/cb-train/src/tree.rs
  - crates/cb-model/src/cbm.rs
  - crates/cb-model/src/predict.rs
  - crates/cb-train/tests/ndim_dim1_identity_test.rs
autonomous: true
requirements: [LOSS-02, LOSS-03]
must_haves:
  truths:
    - "The cb-train boosting loop carries the approx as a dimension-major Vec<f64> (length approx_dimension*n) everywhere; scalar losses run as approx_dimension=1 on the SAME code path (D-03 single code path, no scalar/multi duplication)"
    - "ALL ~38 existing scalar oracle fixtures re-run green at approx_dimension=1 (D-04 hard checkpoint)"
    - "A dim=1 byte-identity assertion proves the dimension-major buffer reduces to today's scalar path with EXACTLY 0.0 diff (not merely <=1e-5)"
    - "cb-model serializes per-dimension leaf_values in LEAF-MAJOR order with the real approx_dimension (not hardcoded 1); at dim=1 the model is byte-identical to today"
  artifacts:
    - path: "crates/cb-train/src/boosting.rs"
      provides: "N-dim approx buffer init + per-dim leaf update + staged record + per-dim compute_leaf_deltas dispatch; dim=1 byte-identical"
      contains: "approx_dimension"
    - path: "crates/cb-model/src/cbm.rs"
      provides: "ApproxDimension wired to the real dim; dim-major training buffer transposed to LEAF-MAJOR leaf_values"
      contains: "ApproxDimension"
    - path: "crates/cb-train/tests/ndim_dim1_identity_test.rs"
      provides: "explicit 0.0-diff dim=1 byte-identity gate (the D-04 anchor)"
      min_lines: 20
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "Runtime::compute_gradients"
      via: "approx_dimension threaded into the gradient call (boosting.rs:1597)"
      pattern: "compute_gradients\\(.*approx_dimension"
    - from: "crates/cb-model/src/cbm.rs"
      to: ".cbm LeafValues"
      via: "leaf-major transpose leaf[d][l] -> leaf_values[l*dim+d]"
      pattern: "ApproxDimension"
---

<objective>
Wave 0, Part 2 of 2, and the **D-04 HARD CHECKPOINT** (umbrella D-04, non-negotiable). Generalize the `cb-train` boosting loop and `cb-model` serialization from a scalar per-object approx to the **dimension-major flat buffer** (`approx[d*n+i]`, D-6.2-01) introduced in Plan 06.2-01, then **re-run ALL ~38 existing scalar oracle fixtures green at approx_dimension=1**. No multiclass/multilabel/MultiQuantile math is written until this gate is green (it is the entry condition for Wave 1 / Plan 06.2-03).

The design hinge (CONTEXT "specific ideas"): the dimension-major buffer must reduce to **exactly** today's scalar `Vec<f64>` at dim=1, so the re-lock is a true no-behavior-change proof (0.0 diff), not an approximate one. This plan adds an explicit `0.0`-diff byte-identity test in addition to re-running the existing ≤1e-5 oracles.

Purpose: prove the entire scalar pipeline is behavior-neutral under the N-dim widening before any new loss is added — isolating refactor risk from new-loss risk.
Output: an `approx_dimension`-aware boosting loop, a leaf-major N-dim `cb-model` serialization (byte-identical at dim=1), the full scalar oracle suite green, and the D-04 gate test.

NOTE: gsd-tools CLI is ABSENT (verified). STATE/ROADMAP/REQUIREMENTS updates are MANUAL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-CONTEXT.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-RESEARCH.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-PATTERNS.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-VALIDATION.md
@.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the boosting loop to a dimension-major approx buffer (dim=1 byte-identical)</name>
  <read_first>
    - crates/cb-train/src/boosting.rs:666 (`starting_approx`), :730 (`compute_leaf_deltas`), :1229 (`let mut approx = vec![bias; n]`), :1597 (`runtime.compute_gradients(params.loss, &approx, target)`), :1800 (approx update loop), :1808 (staged record `out.extend_from_slice(&approx)`)
    - crates/cb-train/src/bootstrap.rs:334-341 (`derivative_l2_norms`, the MVS grad-norm sqrt(d*d))
    - 06.2-PATTERNS.md § "crates/cb-train/src/boosting.rs" (all four scalar seams with line refs) + the (P-NDIM) outer-loop invariant
    - 06.2-RESEARCH.md Pattern 1 (`approxDimension==1` short-circuit, approx_calcer.cpp:1093), Pitfall 1 (reduction-order drift), Pitfall 8 (SHAP stays dim=1 / out of train oracle)
  </read_first>
  <behavior>
    - At approx_dimension=1 every scalar-loss training run produces splits, leaf values, and staged approx byte-identical to before the refactor (existing slice_first/leaf_methods/wave1/wave2/wave3 oracles stay green).
    - The approx buffer is allocated as length approx_dimension*n with per-dimension bias; for dim=1 this is exactly vec![bias; n].
    - compute_leaf_deltas runs the existing scalar Newton/Gradient/Exact/Simple solver per dimension over approx[d*n..d*n+n]; the approx update is approx[d*n+i] += learning_rate * leaf_delta[d][leaf(i)] per dimension.
    - The MVS grad norm widens to sqrt(sum_d der1[d*n+i]^2) but is neutral for in-scope fixtures (bootstrap_type:No).
  </behavior>
  <action>
    In `crates/cb-train/src/boosting.rs`: derive `approx_dimension` at the top of the train loop (for all existing scalar losses it is `1` — there is no multi-dim loss yet; thread a local `let approx_dimension = 1;` sourced from the loss until Plans 03-05 derive it per loss, then route it everywhere below). Change the approx init (`:1229`) to allocate `vec![0.0; approx_dimension * n]` and seed each dimension's bias from `starting_approx` (at dim=1 this is exactly `vec![bias; n]` — Pitfall 1 byte-identity). Pass `approx_dimension` into `runtime.compute_gradients(&params.loss, &approx, target, approx_dimension)` (`:1597`; note `&params.loss` — `Loss` is non-Copy after 06.2-01). Widen `compute_leaf_deltas` (`:730`) to loop `for d in 0..approx_dimension` calling the EXISTING per-dimension Newton/Gradient/Exact/Simple arms over the dim-major slice `&approx[d*n..d*n+n]` and the matching der slice; CRITICAL (Pitfall 1) keep the per-dimension `cb_core::sum_f64` reduction as an OUTER loop so at dim=1 the slice and summation order are exactly today's — never fuse into a single `0..dim*n` reduction. Widen the approx update (`:1800`) and staged record (`:1808`) to per-dimension `approx[d*n+i]`. Widen `bootstrap.rs::derivative_l2_norms` (`:334`) to `sqrt(Σ_d der1[d*n+i]²)` (`mvs.cpp:187-209`) — neutral at dim=1 / bootstrap=No (RESEARCH A3) but correct for later. Keep `tree.rs` at a single score column for dim=1 (the cross-dimension split-score column is a Plan 06.2-03 transcription task — Pitfall 7 / Open Q2; do NOT implement it here, just keep tree.rs compiling at dim=1). Keep SHAP (`cb-model::shap.rs`) compiling against the model shape at dim=1 / first-dimension behavior (Pitfall 8 — full N-dim SHAP is out of scope). All accumulation routes through `cb_core::sum_f64`/`sum_f32_in_f64` (D-08). No `unwrap()` in production.
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cb-train` exits 0; `boosting.rs` `compute_gradients` call passes `approx_dimension` (grep `compute_gradients\(.*approx_dimension`).
    - `cargo test -p cb-train --lib` exits 0 — all cb-train library unit tests pass unchanged.
    - grep `crates/cb-train/src/boosting.rs` shows the per-dimension leaf-reduction is an outer `for d in 0..approx_dimension` loop (NOT a fused `0..approx_dimension * n` reduction) — the Pitfall-1 invariant.
  </acceptance_criteria>
  <done>Boosting loop, leaf deltas, approx update, staged record, and MVS norm are dimension-major; dim=1 byte-identical; cb-train lib green; tree.rs cross-dim score deferred to 06.2-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cb-model N-dim serialization (leaf-major transpose) and re-lock ALL scalar oracles + add the 0.0-diff D-04 gate</name>
  <read_first>
    - crates/cb-model/src/cbm.rs:199 (`TModelTreesArgs`), :201 (`ApproxDimension: 1` — flip to the real dim), :198 (`leaf_values` vector), `Bias: model.bias`
    - crates/cb-model/src/predict.rs:57 (`sigmoid`), :70 (`apply_prediction_type`), :73-101 (RawFormulaVal/Probability/Class/Exponent arms)
    - 06.2-PATTERNS.md § "crates/cb-model/src/cbm.rs" (leaf-major transpose `leaf[d][l] -> leaf_values[l*dim+d]`, leaf_weights stays one-per-leaf, FB ApproxDimension field exists at model_generated.rs:2853) + § "crates/cb-model/src/predict.rs"
    - 06.2-RESEARCH.md Pitfall 6 (leaf_values are LEAF-MAJOR, not dimension-major), and the empirical note (`scale_and_bias = [1,[0,0,0]]` per-dim bias vector)
    - crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs (the staged-approx / leaf-values compare_stage harness shape to reuse in the gate test)
  </read_first>
  <behavior>
    - At approx_dimension=1, cb-model serialization (.cbm + json) and predictions are byte-identical to before the refactor (existing model round-trip + apply oracles green).
    - cbm.rs writes the real approx_dimension into ApproxDimension and transposes the dim-major training buffer to LEAF-MAJOR leaf_values (leaf0_d0,leaf0_d1,...,leaf1_d0,...); leaf_weights stays one-per-leaf. At dim=1 leaf-major == dim-major (single dimension) so the wire bytes are unchanged.
    - The full existing scalar oracle suite (slice_first, leaf_methods, wave1/2/3, one_hot, ordered, ctr, regularization, overfit, eval_metrics, etc.) re-runs green at <=1e-5 (D-04).
    - A new dim=1 byte-identity test asserts an EXACTLY 0.0 diff between the dimension-major-path staged approx and the pre-refactor scalar path on at least one representative scalar fixture.
  </behavior>
  <action>
    In `crates/cb-model/src/cbm.rs`: replace the hardcoded `ApproxDimension: 1` (`:201`) with the model's real `approx_dimension` (add the field to the in-memory `Model`/`from_trained` boundary, defaulting to `1` so dim=1 models are unchanged). At the `from_trained`/`build_core_blob` boundary, transpose the dimension-major training leaf buffer to LEAF-MAJOR `leaf_values[l*dim + d]` (Pitfall 6); keep `leaf_weights` one-per-leaf (`cbm.rs:166`). Generalize `Bias` to a per-dimension bias vector via the existing `scale_and_bias` path (`[1,[b0,b1,...]]`; at dim=1 it stays `[1,[bias]]` — byte-identical). The FB `ApproxDimension` field already exists (`model_generated.rs:2853`) — no schema regen. In `crates/cb-model/src/predict.rs`: keep `apply_prediction_type` (`:70`) behavior byte-identical at dim=1; do NOT add softmax/per-dim-sigmoid transforms here (those land with their losses in Plans 03-05) — just ensure the function compiles against the per-dim model shape (e.g. accept the dim and treat dim=1 exactly as today). Store class-label metadata stub only if trivially needed for compile; full `class_params`/`multiclass_params` plumbing is Plan 06.2-03.

    Then create `crates/cb-train/tests/ndim_dim1_identity_test.rs` (separate file — source/test separation is MANDATORY per CLAUDE.md; `#![allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing)]` as the existing oracle tests do). It must: (a) train a representative scalar fixture (e.g. `slice_first`/RMSE) through the now-N-dim `train` path at approx_dimension=1 and assert the staged approx and leaf_values diff vs the committed fixture is EXACTLY `0.0` (not `<=1e-5`) — the D-04 byte-identity anchor (RESEARCH Wave-0 Gaps; Pitfall 1); reuse the `compare_stage`/`load_f64_vec` harness from `wave3_quantile_loss_oracle_test.rs`. Finally RE-RUN the full existing scalar oracle suite as the D-04 gate.
  </action>
  <verify>
    <automated>cargo test -p cb-model --lib && cargo test -p cb-train --test ndim_dim1_identity_test && cargo test -p cb-train --tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-model --lib` exits 0 — model round-trip/apply unit tests byte-identical at dim=1.
    - `cargo test -p cb-train --test ndim_dim1_identity_test` exits 0 — the staged-approx diff vs the scalar fixture is EXACTLY `0.0` (assert on `== 0.0`, not a tolerance).
    - `cargo test -p cb-train --tests` exits 0 — ALL existing scalar oracle integration tests (slice_first, leaf_methods, wave1/2/3, one_hot, ordered_boost_e2e, tensor_ctr_e2e, regularization, overfit, eval_metrics, etc.) re-run green at <=1e-5 (the D-04 hard checkpoint).
    - grep `crates/cb-model/src/cbm.rs` no longer shows `ApproxDimension: 1` hardcoded (it reads the model's real dim).
  </acceptance_criteria>
  <done>cb-model serializes N-dim leaf-major (dim=1 byte-identical); the 0.0-diff D-04 gate test passes; the full scalar oracle suite is green — Wave 0 complete, Wave 1 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training buffer → model serialization | The dim-major→leaf-major transpose is the only shape-sensitive boundary; a wrong transpose scrambles predictions across dimensions (internal correctness, not untrusted input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.2-02a | Tampering (silent bad model) | `cbm.rs` leaf-major transpose | mitigate | The dim=1 byte-identity gate (`== 0.0` diff) + the full scalar oracle re-lock catch any transpose regression at the only shape the existing fixtures exercise; multi-dim transpose correctness is gated by the per-loss prediction oracles in Plans 03-05. |
| T-6.2-02b | Denial of Service | `boosting.rs` per-dim buffer indexing | mitigate | Derive `n` from `approx.len()/approx_dimension`; use checked slicing (no `unwrap`/`indexing_slicing` in production); at dim=1 the slice is the full buffer (no new failure mode). |
| T-6.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure host refactor of existing crates; RESEARCH Package Legitimacy Audit = N/A for Wave 0). |
</threat_model>

<verification>
- Per-crate runs (disk pressure — MEMORY): `cargo test -p cb-model --lib`, `cargo test -p cb-train --tests`.
- D-04 gate: the full scalar oracle suite under `crates/cb-train/tests/` is green at <=1e-5 AND the dedicated `ndim_dim1_identity_test` is green at `== 0.0`.
- D-08 grep discipline: no new float summation outside `cb-core::sum_f64`/`sum_f32_in_f64`.
- After this plan, set `wave_0_complete: true` in 06.2-VALIDATION.md frontmatter (Wave 1 entry condition).
</verification>

<success_criteria>
- The boosting loop carries a dimension-major approx everywhere; scalar losses run as dim=1 on the single code path (D-03).
- ALL ~38 existing scalar oracles re-run green at dim=1 (D-04 hard checkpoint), PLUS an explicit `0.0`-diff byte-identity assertion.
- cb-model serializes per-dim leaf-major leaf_values with the real approx_dimension; dim=1 byte-identical.
- No new loss math written (Wave 0 mechanical refactor complete).
</success_criteria>

<output>
Create `.planning/phases/06.2-multiclass-multilabel-and-n-dim-approx-refactor/06.2-02-SUMMARY.md` when done. Update 06.2-VALIDATION.md `wave_0_complete: true`.
</output>
