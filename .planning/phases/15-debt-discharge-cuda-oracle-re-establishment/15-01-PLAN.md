---
phase: 15-debt-discharge-cuda-oracle-re-establishment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-backend/src/gpu_runtime/ranking.rs
  - crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs
autonomous: true
requirements: [HARD-03]
user_setup: []

must_haves:
  truths:
    - "descending_order_per_query preserves original-index order for tied values, matching a CPU stable descending sort (RV-13-01, per D-02)"
    - "query_softmax_ders_host seeds the per-query exp-shift from the max over weight>0 objects only, mirroring CPU ranking_der.rs:257-266 (RV-13-02, per D-02)"
    - "The RV-13-01 tie oracle and the RV-13-02 weighted-max-seed oracle exist as sibling *_test.rs tests and pass (ε=1e-4, record-only off real device)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/ranking.rs"
      provides: "descending_order_per_query reachable to sibling test (pub(crate)); weight>0-aware per-query max seed in query_softmax_ders_host"
      contains: "fn descending_order_per_query"
    - path: "crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs"
      provides: "RV-13-01 tie oracle + RV-13-02 weighted-max-seed oracle"
      contains: "tie_order"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs"
      to: "crates/cb-backend/src/gpu_runtime/ranking.rs"
      via: "direct invocation of descending_order_per_query and query_softmax_ders_host"
      pattern: "descending_order_per_query|query_softmax_ders_host"
  prohibitions:
    - "No cb-train dependency added to cb-backend (feature-unification landmine — CPU references are FROZEN literals or computed inline in the test)"
    - "No numeric ε assert that hard-fires on the default cpu backend — must be gated behind device_backend_active() (WR-01 anti-false-pass)"
    - "No #[cfg(test)] mod tests embedded in ranking.rs — all oracles live in the sibling ranking_stoch_test.rs (source/test separation MANDATORY)"
    - "RV-13-02 fix must FILTER the max by weight>0 — editing only the comment at ranking.rs:473-474 is a non-fix (Pitfall 2)"
---

<objective>
Resolve two of the four RV-13 latent ranking-der parity hazards (HARD-03) with direct unit/kernel oracles, per D-01/D-02:
- RV-13-01 (`descending_order_per_query`) — prove (and, only if the oracle reveals a flip, fix) tie-order stability against a CPU stable descending sort.
- RV-13-02 (`query_softmax_ders_host`) — seed the per-query softmax exp-shift from the max over weight>0 objects only (currently max over ALL objects), matching CPU `ranking_der.rs:257-266`.

Purpose: RV-13-01/02 are latent because the five device families decline `Ok(None)`→CPU today; the oracles exercise the divergent kernels/helpers DIRECTLY (no e2e device-grow) so a future device-grow wiring inherits a trustworthy oracle.
Output: Two new frozen-CPU-reference oracles in `ranking_stoch_test.rs`; a `pub(crate)` visibility bump on `descending_order_per_query`; a weight>0-aware per-query max seed in `query_softmax_ders_host`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-CONTEXT.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-RESEARCH.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-PATTERNS.md
@crates/cb-backend/src/gpu_runtime/ranking.rs
@crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs
@crates/cb-compute/src/ranking_der.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RV-13-01 tie-order oracle (write oracle first; confirm-or-fix descending_order_per_query)</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking.rs, crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs</files>
  <read_first>
    - 15-RESEARCH.md Pitfall 1 + Assumption A1 (RV-13-01 is plausibly already tie-correct — write oracle FIRST, do NOT churn working code).
    - 15-PATTERNS.md "ranking.rs — RV-13-01 tie order" (the current stable complemented-key radix at ranking.rs:739-769) and "Shared Pattern A" (frozen-CPU-reference self-oracle).
    - ranking_stoch_test.rs:1-45 (module doc + `#![cfg(not(feature = "wgpu"))]` gate + `const TOL: f64 = 1e-4` + `device_backend_active()`).
    - The existing `yetirank_der_matches_frozen_cpu` test in ranking_stoch_test.rs (skip/assert idiom to copy).
  </read_first>
  <behavior>
    - Given a single query of perturbed values containing exact ties (e.g. `exp(approx)` collisions plus f32-Gumbel-induced equal values), `descending_order_per_query` returns the permutation equal to a CPU stable descending sort: values in strictly-descending order, and tied values in ASCENDING original-index order.
    - Given multi-query offsets, ties never cross query boundaries.
    - Reference order is computed INLINE in the test via an independent method (std stable sort by descending value with original-index tie-break) — NOT via the radix path, so the oracle is non-tautological.
  </behavior>
  <action>
    In ranking.rs, change `fn descending_order_per_query` to `pub(crate) fn descending_order_per_query` so the sibling test module (declared in gpu_runtime/mod.rs, a sibling not a child) can invoke it directly. Do NOT otherwise modify the function body yet.
    In ranking_stoch_test.rs, import `descending_order_per_query` and add a test `tie_order_matches_cpu_stable_descending`: build a fixture with deliberate ties, compute the reference order inline with a stable descending sort (sort candidate indices by value descending, ties broken by ascending original index), invoke `descending_order_per_query`, and assert the returned permutation equals the reference. This ordering assertion is exact (index equality) and is NOT a floating-point ε compare, so it MAY run on all backends — but gate any device-resident numeric side-check behind `device_backend_active()` per the file convention.
    Run the oracle. Per Pitfall 1 / A1: if it passes as-is, the fix is confirmatory — add a doc-anchored comment on `descending_order_per_query` noting the tie-stability contract is oracle-guarded, and record the honest outcome for 15-EVIDENCE (Wave C). ONLY if the oracle reveals a tie flip, add the original index (`idx0`, already carried through both passes) as a tertiary stable radix key to break the tie deterministically. Do NOT hand-roll a second sort — reuse `segmented_radix_sort`.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features cpu tie_order_matches_cpu_stable_descending -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `tie_order_matches_cpu_stable_descending` compiles and passes under `--features cpu`.
    - `grep -n 'pub(crate) fn descending_order_per_query' crates/cb-backend/src/gpu_runtime/ranking.rs` matches.
    - The fixture contains at least one exact-tie pair AND the assertion checks the full permutation (not just "no panic").
    - Outcome (confirmatory vs defensive-fix) recorded in the SUMMARY for Wave C 15-EVIDENCE.
  </acceptance_criteria>
  <done>Tie oracle exists and passes; the tie-stability contract is either verified-as-is (confirmatory) or made stable with a deterministic tertiary key; no working code churned speculatively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RV-13-02 weight>0 max-seed fix + oracle</name>
  <files>crates/cb-backend/src/gpu_runtime/ranking.rs, crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs</files>
  <read_first>
    - 15-RESEARCH.md Pitfall 2 + Open Question 2 (prefer a host-side weight>0 max loop, NOT a new #[cube] kernel) + the RV-13-02 code example.
    - 15-PATTERNS.md "ranking.rs — RV-13-02 weight>0 max-seed" (the bug at ranking.rs:472-475 and the CPU reference at ranking_der.rs:257-266).
    - ranking.rs:452-505 (`query_softmax_ders_host` — the `compute_group_max_host(approx, q_offsets)` weight-blind call is the fix target; note the downstream `sum_weighted_targets > 0` guard the CPU path relies on).
    - crates/cb-compute/src/ranking_der.rs:230-299 (CPU `TQuerySoftMaxError` weight>0-only `max_approx` seed — the parity target; transcribe FROZEN, never a live dep).
  </read_first>
  <behavior>
    - For a weighted query whose GLOBAL-max-approx doc has weight ≤ 0, `query_softmax_ders_host` produces der1/der2 within ε=1e-4 of the frozen CPU QuerySoftMax der (which seeds the exp-shift from the max over weight>0 objects only).
    - For a uniform-weight query, behavior is unchanged (regression guard).
    - An all-weight-≤0 query short-circuits before `exp` via the existing `sum_weighted_targets > 0` guard (no NaN/inf), mirroring CPU.
  </behavior>
  <action>
    In `query_softmax_ders_host`, replace the weight-blind `compute_group_max_host(approx, q_offsets)?` seed with a per-query max computed over weight>0 objects only (a host-side loop mirroring CPU `ranking_der.rs:257-266`: scan each query's `[q_offsets[g]..q_offsets[g+1])`, track the max `approx` among objects with `weight > 0.0`, seeding `f64::MIN` when a query has no positive-weight object). Keep the downstream `sum_weighted_targets > 0` short-circuit intact so an all-w≤0 query never reaches `exp`. Prefer the host-side loop (Open Q2 recommendation) — do NOT add a new #[cube] kernel; if a kernel edit becomes unavoidable, first read the CubeCL manual + use the `f32::MIN` sentinel and re-run the rocm suite in-env. Replace the misleading "Uniform-weight covered regime" comment with an accurate description of the weight>0 seed. Use `thiserror` `CbError` for any error path; no `unwrap()` in production.
    In ranking_stoch_test.rs, add a test `softmax_weight_max_seed`: construct a weighted query where the max-approx doc has `weight <= 0` (so the weight-blind seed would diverge), invoke `query_softmax_ders_host`, and assert der1/der2 within `TOL` of FROZEN CPU der literals generated ONCE offline from the independent cb-compute QuerySoftMax path (record the generation recipe in the test module doc, matching the existing frozen-literal pattern). Gate the ε assert behind `device_backend_active()` (record-only on cpu).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features cpu softmax_weight_max_seed -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `softmax_weight_max_seed` compiles and passes under `--features cpu` (ε assert record-only; the test still exercises the code path and checks finiteness).
    - The fixture places `weight <= 0` on the global-max-approx doc (else tautological — Pitfall 2).
    - `query_softmax_ders_host` no longer calls `compute_group_max_host` on the raw `approx` for the seed; the seed is weight>0-aware.
    - Frozen CPU der literals + their offline-generation recipe are documented in the test module doc.
    - No `cb-train` dependency added to cb-backend (`grep -c 'cb-train\|cb_train' crates/cb-backend/Cargo.toml` returns 0).
  </acceptance_criteria>
  <done>The softmax exp-shift is seeded from the weight>0 max; the oracle proves ≤1e-4 der parity on the negative-weight-max fixture; uniform-weight regression holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes internal device der host-drivers + adds sibling unit oracles. No network, no auth, no untrusted input, no serialization boundary is introduced or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | frozen CPU der literals in ranking_stoch_test.rs | accept | Literals are test-only reference data generated offline from the independent cb-compute path; a wrong literal fails the oracle, it cannot corrupt production output. No external input. |
| T-15-01-02 | Information Disclosure | ranking der values | accept | Numeric der over synthetic fixtures; no PII, no secrets, no external surface. |
</threat_model>

<artifacts_produced>
Symbols/artifacts this plan CREATES (exclude from drift/parity scans as newly-created):
- `crates/cb-backend/src/gpu_runtime/ranking.rs`: `descending_order_per_query` visibility raised to `pub(crate)`; new host-side weight>0 per-query max seed inside `query_softmax_ders_host`.
- `crates/cb-backend/src/gpu_runtime/ranking_stoch_test.rs`: test fn `tie_order_matches_cpu_stable_descending` (RV-13-01); test fn `softmax_weight_max_seed` (RV-13-02); any frozen reference helper fns (e.g. `frozen_softmax_der1`/`frozen_softmax_der2`) and their offline-generation module-doc recipe.
</artifacts_produced>

<verification>
- `cargo test -p cb-backend --no-default-features --features cpu ranking_stoch` compiles and both new tests pass (CPU-skip on the ε assert; ordering assert runs).
- In-env ROCm smoke (non-gating): `cargo test -p cb-backend --no-default-features --features rocm tie_order_matches_cpu_stable_descending softmax_weight_max_seed` — orchestrator runs in-env; hard ε assert fires on the real gfx1100 device.
- `grep -c 'cb-train\|cb_train' crates/cb-backend/Cargo.toml` returns 0 (no forbidden dep).
</verification>

<success_criteria>
- RV-13-01 and RV-13-02 each have a passing, non-tautological direct oracle (HARD-03, 2 of 4).
- RV-13-02's weight>0 max-seed fix is real code (not a comment edit) and the negative-weight-max fixture exercises the divergence.
- RV-13-01 outcome (confirmatory-verified vs defensive-fixed) is recorded for Wave C 15-EVIDENCE.
- No source/test separation violation; no cb-train dep; ε asserts device-gated.
</success_criteria>

<output>
Create `.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-01-SUMMARY.md` when done. Record the RV-13-01 outcome (was the tie oracle green as-is, or did it require a tertiary key?) and the RV-13-02 frozen-der generation recipe for Wave C evidence.
</output>
