---
phase: 15-debt-discharge-cuda-oracle-re-establishment
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - bench/phase15_cuda_oracle/oracle.py
  - bench/phase15_cuda_oracle/kernel-metadata.json
  - bench/phase15_cuda_oracle/result.json
autonomous: false
requirements: [HARD-01, HARD-02]
user_setup:
  - service: kaggle
    why: "The single authoritative GPU oracle (Tesla P100 CUDA) — verifier subagent cannot run GPU; the orchestrator drives the kaggle CLI in-env"
    env_vars:
      - name: KAGGLE_ACCESS_TOKEN
        source: "~/.kaggle/access_token (already present in-env, proven Phases 12–14; NOT kaggle.json)"

must_haves:
  truths:
    - "bench/phase15_cuda_oracle/oracle.py runs, in ONE Kaggle CUDA kernel session, all 44 device self-oracles + the 4 RV-13 oracles as a BLOCKING correctness pre-gate (ε=1e-4, sys.exit(2) on any fail) BEFORE any timing (HARD-01, D-04/D-05)"
    - "The same session runs the depth-1 and depth-6 BENCH-02 device+CPU speed rows only after the correctness gate passes (HARD-02, D-07)"
    - "The session emits ONE verdict + ONE result.json with single-session provenance (one P100 / driver / seed) — no aggregate.py-style multi-session stitching (D-04)"
    - "No speed number is recorded unless Part A passes (do-not-fabricate discipline, D-05)"
  artifacts:
    - path: "bench/phase15_cuda_oracle/oracle.py"
      provides: "single-session runner: Part A correctness gate + Part B depth-1/depth-6 timing + Part C informational catboost-GPU arm"
      contains: "sys.exit(2)"
    - path: "bench/phase15_cuda_oracle/kernel-metadata.json"
      provides: "Kaggle kernel metadata (GPU enabled, new phase15 dataset source)"
      contains: "enable_gpu"
    - path: "bench/phase15_cuda_oracle/result.json"
      provides: "one verdict + one JSON from the authoritative single session"
      contains: "correctness_verdict"
  key_links:
    - from: "bench/phase15_cuda_oracle/oracle.py"
      to: "crates/cb-backend"
      via: "cargo test --release --no-default-features --features cuda -p cb-backend (Part A) including the 4 RV-13 oracle names"
      pattern: "features cuda"
    - from: "bench/phase15_cuda_oracle/result.json"
      to: "bench/phase15_cuda_oracle/oracle.py"
      via: "written by the run to /kaggle/working, committed back"
      pattern: "result.json"
  prohibitions:
    - "No aggregate.py-style multi-session stitching — supersede it; keep ONLY its >=20x verdict shape for Wave C recompute (D-04)"
    - "No speed number emitted when the correctness pre-gate failed — Part B must not run if Part A fails (D-05)"
    - "No fabricated numbers — every cell comes from the actual run; TBD means not-yet-run"
    - "No fabricated Region catboost_gpu_s — Region stays N/A (no upstream Region policy)"
    - "Depth-1 device≥CPU is NOT gated — success is 'row executed + crossover recorded', not 'device wins' (A4 / Pitfall 5)"
---

<objective>
Assemble and run the SINGLE authoritative Kaggle CUDA kernel session that discharges HARD-01 and produces the HARD-02 speed rows (D-04), per the CONTEXT/RESEARCH single-session mandate:
- Part A (BLOCKING correctness pre-gate, D-05): all 44 v1.1 device self-oracles + the 4 RV-13 oracles (from 15-01/15-02) under `--features cuda`, ε=1e-4, `sys.exit(2)` on any fail BEFORE any timing.
- Part B (timing, only if Part A ALL-PASS): the depth-1 and depth-6 BENCH-02 device+CPU grow rows, warm-run / JIT-excluded / lazy-CubeCL-queue-drained / median-of-N, 20 iters / 20 features / 32 bins (depth-1 on large-n SPEED_CONFIG per Pitfall 5).
- Part C (informational): the CatBoost-GPU timing arm (Region N/A, border_count 32, quantization caveat) for the informational `catboost_gpu_s` column.
Output: ONE `result.json` + ONE verdict with single-session provenance.

Purpose: turn the current stitched / per-family / Pending GPU evidence into one trusted authoritative record every later parity/benchmark claim (Phases 19, 21) rests on.
Output: `bench/phase15_cuda_oracle/{oracle.py, kernel-metadata.json, result.json}`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-CONTEXT.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-RESEARCH.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-PATTERNS.md
@bench/phase14_cuda_signoff/oracle.py
@bench/phase14_cuda_signoff/kernel-metadata.json
@bench/phase14_cuda_signoff/aggregate.py
@bench/RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build bench/phase15_cuda_oracle/oracle.py (clone phase14 + Part B depth rows + 4 RV-13 oracles) and kernel-metadata.json</name>
  <files>bench/phase15_cuda_oracle/oracle.py, bench/phase15_cuda_oracle/kernel-metadata.json</files>
  <read_first>
    - 15-RESEARCH.md "Pattern 2: Correctness-blocks-speed single-session harness", "Don't Hand-Roll" (reuse `gen()`, `SPEED_CONFIG`, the `>=20x` verdict shape), Pitfall 5 (depth-1 large-n crossover).
    - 15-PATTERNS.md "bench/phase15_cuda_oracle/oracle.py — single-session runner" (the exact clone-and-delta: reuse `gen()`, /tmp staging, `CARGO_TARGET_DIR=/tmp/target`, Part A FAMILIES gate → `sys.exit(2)`, Part C informational arm; ADD Part B depth-1/depth-6 timing; emit ONE result.json).
    - bench/phase14_cuda_signoff/oracle.py IN FULL (the base to clone: `gen()` at :58-84, repo staging :121-154, Part A FAMILIES + gate :156-205, Part C :207-296).
    - bench/phase14_cuda_signoff/kernel-metadata.json (metadata shape; note `dataset_sources` points at the phase14 src dataset — the phase15 kernel needs a phase15 dataset id + title).
    - bench/RESULTS.md D-10-09 escalation (:19-36) and the depth-1 (:56-72) / depth-6 (:127-141) TBD tables the Part B rows must fill.
  </read_first>
  <action>
    Clone `bench/phase14_cuda_signoff/oracle.py` to `bench/phase15_cuda_oracle/oracle.py`. Reuse VERBATIM: `gen(n, nf, nbins)` (the frozen numpy repro of bench_grow_speed_test.rs::gen — integer-binned 0..31 f32, seed 42, 20 feat / 32 bins), the /tmp repo staging + `CARGO_TARGET_DIR=/tmp/target` (keeps Kaggle output tiny), the Part A per-family FAMILIES gate that runs `cargo test --release --no-default-features --features cuda -p cb-backend -- <filters>` and computes `corr_pass = all(exit==0 and ran_any_tests)` → `sys.exit(2)` BEFORE any timing (D-05), and the Part C CatBoost-GPU informational arm (Region N/A, border_count 32, quantization caveat).
    Phase-15 deltas: (1) ADD the 4 RV-13 oracle test names to Part A's filter set so they ride the SAME cuda `cargo test` invocations (the tie-order + softmax-weight-max-seed names ride the ranking family; the empty-group + near-equal-tiebreak names ride the query_helper/pairwise families — confirm each named test is reached and counted in `ran_any_tests`). (2) ADD a Part B (BENCH-02 timing) that runs the depth-1 and depth-6 device+CPU grow rows in the SAME kernel session — warm-run / JIT-excluded / lazy-CubeCL-queue-drained / median-of-N (N=3 or N=5, your discretion; time is not a constraint), 20 iters / 20 feat / 32 bins, depth per row; run the depth-1 row on the large-n SPEED_CONFIG (~1e6×50 per Pitfall 5) and RECORD the crossover (n where device first beats CPU, or "not reached") — do NOT gate on device≥CPU for depth-1 (A4). (3) Emit exactly ONE `result.json` to /kaggle/working carrying: single-session provenance (GPU model, CUDA/driver ver, seed), the Part A per-family+RV-13 correctness verdict with ε divergences, and the Part B depth-1/depth-6 device_s / host_cpu_s / catboost_gpu_s / speedup rows + crossover note. Supersede aggregate.py — do NOT stitch multiple session files; keep only the `GE20X_GATE = 20.0` verdict shape for Wave C's recompute.
    Create `bench/phase15_cuda_oracle/kernel-metadata.json` by copying the phase14 metadata and changing the kernel `id`/`title`/`code_file` to a phase15 identity and `dataset_sources` to the phase15 source dataset id (the orchestrator creates/pushes the dataset from a `git archive` tracked-source tarball in Task 2); keep `enable_gpu: true`, `enable_internet: true`, `is_private: true`.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; ast.parse(open('bench/phase15_cuda_oracle/oracle.py').read()); print('oracle.py parses')" && python3 -c "import json; d=json.load(open('bench/phase15_cuda_oracle/kernel-metadata.json')); assert d['enable_gpu'] is True; print('metadata ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `oracle.py` parses (py_compile/ast) and contains a Part A gate (`sys.exit(2)`), the 4 RV-13 oracle test names in the Part A filter set, a Part B depth-1 AND depth-6 timing section, and a single `result.json` writer.
    - `grep -c 'features cuda' bench/phase15_cuda_oracle/oracle.py` ≥ 1; `grep -E 'depth.?1|depth.?6' bench/phase15_cuda_oracle/oracle.py` matches both depth rows.
    - `oracle.py` reuses `gen()` (frozen generator) — no second synthetic generator hand-rolled.
    - No multi-file `load_rows`-style stitching copied from aggregate.py.
    - `kernel-metadata.json` is valid JSON with `enable_gpu: true` and a phase15 `id`/`dataset_sources`.
  </acceptance_criteria>
  <done>The single-session runner + kernel metadata exist, parse, and encode the Part A blocking gate → Part B depth-1/depth-6 timing → one result.json contract; ready to dispatch to Kaggle.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Orchestrator drives the single Kaggle CUDA session; commit result.json (do-not-fabricate)</name>
  <files>bench/phase15_cuda_oracle/result.json</files>
  <read_first>
    - 15-RESEARCH.md "Runtime State Inventory" + "Environment Availability" (Kaggle P100, auth via ~/.kaggle/access_token, /tmp build, ~1.8MB git-archive tarball, background-poll KernelWorkerStatus ~15min).
    - MEMORY.md phase 12/13/14 outcome notes (the proven kaggle-CLI-drive recipe: git archive tracked-source-only → dataset → push kernel → bg-poll until COMPLETE).
  </read_first>
  <what-built>
    `bench/phase15_cuda_oracle/oracle.py` + `kernel-metadata.json` (Task 1): the single-session runner encoding the Part A blocking correctness gate (44 self-oracles + 4 RV-13 oracles, ε=1e-4) → Part B depth-1/depth-6 timing → one verdict + one result.json.
  </what-built>
  <how-to-verify>
    The verifier subagent CANNOT run GPU — the orchestrator drives the `kaggle` CLI IN-ENV (established Phases 12–14):
    1. Build the tracked-source tarball via `git archive` (NOT the 2.9G crates/ tree — ~1.8MB), create/version the phase15 Kaggle dataset from it.
    2. Push the kernel (`kaggle kernels push` with the phase15 kernel-metadata.json), enable_gpu true, internet true.
    3. Background-poll `kaggle kernels status` (KernelWorkerStatus) until COMPLETE (~15min); auth via `~/.kaggle/access_token`.
    4. Pull the output; retrieve the single `result.json`.
    5. Confirm Part A `correctness_verdict` == ALL-PASS (ε=1e-4). If Part A FAILED, Part B was skipped and NO speed number exists — that is the correct do-not-fabricate outcome; the phase does NOT advance to Wave C until Part A passes (loop back to fix + re-run).
    6. Commit the retrieved `result.json` to `bench/phase15_cuda_oracle/result.json` verbatim (no hand-editing of numbers).
  </how-to-verify>
  <resume-signal>Paste the `correctness_verdict` + the depth-1/depth-6 speed rows + crossover note from the committed result.json (or, on Part A failure, the failing family/oracle so Wave A can be revisited). Type "session recorded" when result.json is committed.</resume-signal>
  <action>Orchestrator-driven (verifier subagent cannot run GPU): build the `git archive` tracked-source tarball (~1.8MB, NOT the 2.9G crates/ tree), create/version the phase15 Kaggle dataset from it, push the kernel with `bench/phase15_cuda_oracle/kernel-metadata.json` (enable_gpu true, internet true), background-poll `kaggle kernels status` (KernelWorkerStatus) until COMPLETE (~15min, auth via `~/.kaggle/access_token`), pull the output, confirm Part A `correctness_verdict` == ALL-PASS (ε=1e-4), and commit the retrieved `result.json` to `bench/phase15_cuda_oracle/result.json` verbatim. If Part A failed, Part B was skipped and no speed number exists — loop back to Wave A/B; do NOT advance to Wave C.</action>
  <verify>The committed `bench/phase15_cuda_oracle/result.json` shows Part A `correctness_verdict` == ALL-PASS across all v1.1 families + the 4 RV-13 oracles, and Part B depth-1 + depth-6 rows present with a crossover note; single-session provenance fields (one gpu/driver/seed) confirm no stitching.</verify>
  <done>One Kaggle CUDA session run; result.json committed verbatim with Part A ALL-PASS and the depth-1/depth-6 rows (or, on failure, the failing oracle reported and Wave C blocked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Kaggle | The orchestrator authenticates to Kaggle with the operator's own `~/.kaggle/access_token` and runs first-party tracked source in an isolated Kaggle kernel. No third-party/untrusted code, no user input, no data ingress beyond the frozen synthetic `gen()` workload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Information Disclosure | `~/.kaggle/access_token` | mitigate | Auth token is read from the operator's home dir and NEVER committed; the `git archive` tarball is tracked-source-only (excludes secrets/build dirs); kernel is `is_private: true`. |
| T-15-03-02 | Tampering | result.json numbers | mitigate | result.json is committed verbatim from the actual run; do-not-fabricate discipline — no hand-editing; Part A blocking gate prevents recording a speed number on a failed correctness run. |
| T-15-03-03 | Spoofing/Supply-chain | Kaggle image packages (numpy, catboost==1.2.10) | accept | First-party pre-installed Kaggle image packages; no new external package installed by this phase (Package Legitimacy Audit: N/A). |
</threat_model>

<artifacts_produced>
Symbols/artifacts this plan CREATES (exclude from drift/parity scans as newly-created):
- `bench/phase15_cuda_oracle/oracle.py`: single-session runner (Part A gate, Part B depth-1/depth-6 timing, Part C informational arm); reuses `gen()` and the `>=20x` verdict shape.
- `bench/phase15_cuda_oracle/kernel-metadata.json`: phase15 Kaggle kernel metadata.
- `bench/phase15_cuda_oracle/result.json`: JSON keys include `correctness_verdict`, per-family + RV-13 oracle ε divergences, Part B depth-1/depth-6 rows (`device_s`, `host_cpu_s`, `catboost_gpu_s`, `speedup`), crossover note, single-session provenance (gpu, cuda_ver, driver, seed).
</artifacts_produced>

<verification>
- `oracle.py` and `kernel-metadata.json` static checks pass (Task 1 automated verify).
- The authoritative gate is the committed `bench/phase15_cuda_oracle/result.json`: Part A `correctness_verdict` == ALL-PASS (ε=1e-4) covering all v1.1 families + the 4 RV-13 oracles in ONE session (HARD-01); Part B depth-1 + depth-6 rows present with a crossover note (HARD-02).
- Provenance fields confirm a single kernel session (one gpu/driver/seed) — no stitched multi-session sources.
</verification>

<success_criteria>
- One Kaggle CUDA kernel session emits one verdict + one result.json (D-04).
- Part A ALL-PASS ε=1e-4 across all v1.1 device families + the 4 RV-13 oracles (HARD-01).
- Part B depth-1 and depth-6 BENCH-02 rows executed and recorded with the depth-1 crossover note; no speed number exists if Part A had failed (HARD-02, D-05).
- result.json committed verbatim (no fabrication); ready for Wave C recompute.
</success_criteria>

<output>
Create `.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-03-SUMMARY.md` when done. Record the single-session provenance, the Part A verdict, the depth-1/depth-6 rows + crossover, and the result.json path for Wave C.
</output>
