---
phase: 15-debt-discharge-cuda-oracle-re-establishment
plan: 04
type: execute
wave: 3
depends_on: ["15-03"]
files_modified:
  - .planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-EVIDENCE.md
  - bench/BENCH-03-SIGNOFF.md
  - bench/RESULTS.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]
user_setup: []

must_haves:
  truths:
    - "15-EVIDENCE.md has one entry per RV-13-0x recording what diverged, the oracle, the fix, and the passing result (D-10)"
    - "bench/BENCH-03-SIGNOFF.md and bench/RESULTS.md are rewritten in place with the real single-session numbers from 15-03 result.json — no TBD left where a depth-1/depth-6 row is now required, no fabrication (D-08/D-09)"
    - "REQUIREMENTS.md marks HARD-01/02/03 complete; MILESTONES.md clears the two v1.1 Known Gaps (GPUT-14 aggregate + un-run BENCH-02); STATE.md clears the corresponding Deferred Items (D-11)"
  artifacts:
    - path: ".planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-EVIDENCE.md"
      provides: "per-hazard RV-13-01..04 fix/verify evidence"
      contains: "RV-13-01"
    - path: "bench/RESULTS.md"
      provides: "depth-1 + depth-6 oracle/speed tables filled from the single session"
      contains: "depth-1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HARD-01/02/03 flipped to complete"
      contains: "HARD-01"
  key_links:
    - from: "bench/RESULTS.md"
      to: "bench/phase15_cuda_oracle/result.json"
      via: "TBD cells filled from the single-session JSON"
      pattern: "result.json"
    - from: ".planning/MILESTONES.md"
      to: "bench/BENCH-03-SIGNOFF.md"
      via: "Known-Gaps rows discharged, referencing the recomputed sign-off"
      pattern: "GPUT-14|BENCH-02"
  prohibitions:
    - "No fabricated numbers — every filled cell traces to 15-03 result.json; if Part A did not pass, do NOT flip any bookkeeping (loop back to Wave A/B)"
    - "No stitched-provenance cell — no depth row may remain sourced only from the Phase-12/13 sessions where a Phase-10/11 depth row is now required (D-08)"
    - "No fabricated Region catboost_gpu_s — Region stays N/A; catboost_gpu_s stays informational (D-08)"
    - "Do not mark a HARD requirement complete whose evidence is absent from 15-EVIDENCE.md or result.json"
---

<objective>
Close the phase (D-08/D-09/D-10/D-11): turn the single-session `result.json` from 15-03 into truthful in-place evidence and flip the requirement/gap bookkeeping.
- Write `15-EVIDENCE.md` — one entry per RV-13-0x { what diverged, the oracle, the fix, the passing result } (honest recording, esp. RV-13-01 where "verified stable + oracle added" is a valid discharge per A1).
- Rewrite `bench/BENCH-03-SIGNOFF.md` + fill `bench/RESULTS.md` TBD tables with the real un-stitched depth-1/depth-6 numbers (Region N/A, catboost_gpu_s informational; depth-1 crossover recorded, not forced to a device≥CPU pass per A4).
- Flip `.planning/REQUIREMENTS.md` (HARD-01/02/03 → complete), clear `.planning/MILESTONES.md` two v1.1 Known Gaps, clear `.planning/STATE.md` Deferred Items (GPUT-14 satisfied).

Purpose: every later parity/benchmark claim (Phases 19, 21) now rests on a trusted, un-stitched, single-session oracle of record.
Output: `15-EVIDENCE.md` (new); rewritten `BENCH-03-SIGNOFF.md` + `RESULTS.md`; flipped REQUIREMENTS/MILESTONES/STATE.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-CONTEXT.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-RESEARCH.md
@.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-PATTERNS.md
@bench/BENCH-03-SIGNOFF.md
@bench/RESULTS.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 15-EVIDENCE.md and rewrite BENCH-03-SIGNOFF.md + RESULTS.md from the single-session result.json</name>
  <files>.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-EVIDENCE.md, bench/BENCH-03-SIGNOFF.md, bench/RESULTS.md</files>
  <read_first>
    - 15-RESEARCH.md D-08/D-09 rows, Pitfall 5 (depth-1 crossover), "State of the Art" (supersede aggregate.py; recompute reads the single json), and the 15-01/15-02/15-03 SUMMARYs (for the RV-13 outcomes + REL_TOL + provenance).
    - 15-PATTERNS.md "bench/BENCH-03-SIGNOFF.md + bench/RESULTS.md — in-place evidence rewrite" (exact TBD table locations: RESULTS.md depth-1 correctness :56-66 + GATE :66, depth-1 speed :71 + crossover :72, depth-6 :129-132/:135-141) and "15-EVIDENCE.md — per-hazard evidence" (structure per D-10).
    - bench/phase15_cuda_oracle/result.json (the single-session source of every number).
    - bench/BENCH-03-SIGNOFF.md IN FULL (the "Standing debt" section to rewrite) and bench/RESULTS.md TBD tables + the D-10-09 escalation (:19-36).
  </read_first>
  <action>
    Create `15-EVIDENCE.md` with one section per hazard RV-13-01, RV-13-02, RV-13-03, RV-13-04, each recording: what diverged (or, for RV-13-01, the confirmatory finding per A1), the demonstrating oracle (test name + file), the fix (code change or "verified stable + oracle added"), and the passing result (the ε / index outcome from the in-env rocm smoke and the Kaggle Part A verdict in result.json). Pull the concrete outcomes from the 15-01/15-02 SUMMARYs and the 15-03 result.json; do not invent.
    Fill `bench/RESULTS.md` TBD tables from result.json: the depth-1 correctness oracle table (primitive scan/segmented-scan/radix/reduce-by-key/segmented-reduce/update_part_props, cindex GPUT-15, depth-1 RMSE/Logloss ≤1e-5) + its GATE line; the depth-1 large-n speed row + the crossover note (record the measured crossover, or "not reached" — do NOT force a device≥CPU pass, A4/Pitfall 5); the depth-6 Gate A/B rows + speed rows. Every filled cell must trace to a result.json field; leave nothing fabricated.
    Rewrite `bench/BENCH-03-SIGNOFF.md` in place: recompute the BENCH-03 aggregate from the SINGLE session (keep the `>=20x` verdict discipline; Region stays N/A; catboost_gpu_s stays informational per D-08); rewrite the "Standing debt" section to reflect that GPUT-14 aggregate + the Phase-10/11 BENCH-02 rows are now DISCHARGED by this single-session run — with no cell sourced only from the Phase-12/13 sessions where a depth row is now required.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-EVIDENCE.md && for h in RV-13-01 RV-13-02 RV-13-03 RV-13-04; do grep -q "$h" .planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-EVIDENCE.md || { echo "MISSING $h"; exit 1; }; done && ! grep -nE 'depth.?1|depth.?6' bench/RESULTS.md | grep -q 'TBD' && echo "evidence+results ok"</automated>
  </verify>
  <acceptance_criteria>
    - `15-EVIDENCE.md` exists and has a section for each of RV-13-01..04 with the four D-10 fields.
    - No `TBD` remains on any depth-1 or depth-6 row in `bench/RESULTS.md` (the automated check fails if one does).
    - The depth-1 speed row carries a crossover note (n-where-device-beats-CPU or "not reached"); depth-1 is NOT recorded as a failure for losing to CPU at small n (A4).
    - `bench/BENCH-03-SIGNOFF.md` "Standing debt" section reflects discharge; Region N/A and catboost_gpu_s informational preserved.
    - Every filled number is traceable to `bench/phase15_cuda_oracle/result.json` (no fabrication).
  </acceptance_criteria>
  <done>Per-hazard evidence written; RESULTS.md depth-1/depth-6 tables filled from the single session; BENCH-03-SIGNOFF recomputed and its standing-debt section rewritten to discharged.</done>
</task>

<task type="auto">
  <name>Task 2: Flip requirement/gap bookkeeping (REQUIREMENTS, MILESTONES, STATE)</name>
  <files>.planning/REQUIREMENTS.md, .planning/MILESTONES.md, .planning/STATE.md</files>
  <read_first>
    - 15-RESEARCH.md "Bookkeeping Surface (D-11 — exact locations)" table (REQUIREMENTS :16-18 checkboxes + :80-82 traceability; MILESTONES :21-22 Known Gaps; STATE :501-505 Deferred) and the "Note on GPUT-14 in REQUIREMENTS.md" (GPUT-14 lives in STATE Deferred + MILESTONES Known Gaps, discharged by clearing those + marking HARD-01 complete).
    - The current REQUIREMENTS.md HARD-01/02/03 checkboxes + traceability table, MILESTONES.md §Known Gaps (v1.1), STATE.md Deferred Items — locate by grep (line numbers are approximate).
  </read_first>
  <action>
    Precondition: only proceed if 15-03 result.json Part A == ALL-PASS and 15-EVIDENCE.md exists (Task 1) — otherwise STOP and do not flip anything.
    In `.planning/REQUIREMENTS.md`: flip the inline HARD-01/HARD-02/HARD-03 checkboxes from `[ ]` to `[x]`, and change their traceability-table Status from `Pending` to `Complete`. Use scoped Edits (locate each row by its HARD-0x id, not by line number).
    In `.planning/MILESTONES.md` §"Known Gaps" (v1.1): clear/mark-discharged the two rows — the GPUT-14 aggregate gap and the Phase-10/11 un-run BENCH-02 gap — noting they are discharged by Phase 15 (reference bench/BENCH-03-SIGNOFF.md).
    In `.planning/STATE.md` Deferred Items: mark the GPUT-14 requirement + the Phase-10/11 BENCH-02 rows resolved (Phase 15), and the optional Phase-10 depth-1 Kaggle verification/UAT item resolved if the depth-1 row ran.
    Finally, verify no stray `GPUT-14 Pending` string lingers anywhere: `grep -rn "GPUT-14" .planning/` and reconcile any remaining Pending reference.
  </action>
  <verify>
    <automated>grep -Eq '\[x\].*HARD-01' .planning/REQUIREMENTS.md && grep -Eq '\[x\].*HARD-02' .planning/REQUIREMENTS.md && grep -Eq '\[x\].*HARD-03' .planning/REQUIREMENTS.md && ! grep -nE 'HARD-0[123].*Pending' .planning/REQUIREMENTS.md && echo "bookkeeping flipped"</automated>
  </verify>
  <acceptance_criteria>
    - HARD-01/02/03 inline checkboxes are `[x]` and their traceability Status is `Complete` in REQUIREMENTS.md (automated check passes; no `HARD-0[123].*Pending` remains).
    - MILESTONES.md §Known Gaps (v1.1) no longer lists the GPUT-14 aggregate gap or the Phase-10/11 un-run BENCH-02 gap as open (marked discharged by Phase 15).
    - STATE.md Deferred Items no longer list GPUT-14 / Phase-10-11 BENCH-02 as pending.
    - `grep -rn "GPUT-14" .planning/` shows no lingering `Pending` reference.
    - Bookkeeping was flipped ONLY because 15-03 Part A passed and 15-EVIDENCE.md exists (precondition honored).
  </acceptance_criteria>
  <done>HARD-01/02/03 marked complete; both v1.1 Known Gaps cleared; STATE Deferred Items resolved; no stray GPUT-14 Pending string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits markdown evidence + planning bookkeeping only. No code, no network, no auth, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Repudiation | benchmark evidence integrity | mitigate | Every filled cell traces to the committed single-session result.json; do-not-fabricate discipline; bookkeeping flip is gated on Part A ALL-PASS + 15-EVIDENCE.md, so a claim of discharge cannot be recorded without the underlying passing run. |
| T-15-04-02 | Tampering | requirement status flip | accept | Planning-doc bookkeeping under version control; a wrong flip is caught by the traceability grep gate and code review; no external surface. |
</threat_model>

<artifacts_produced>
Symbols/artifacts this plan CREATES or REWRITES (exclude from drift/parity scans as newly-created):
- `.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-EVIDENCE.md` (new): per-hazard sections RV-13-01, RV-13-02, RV-13-03, RV-13-04.
- `bench/BENCH-03-SIGNOFF.md` (rewritten): recomputed single-session BENCH-03 aggregate + discharged "Standing debt" section.
- `bench/RESULTS.md` (rewritten): depth-1 correctness/speed tables + depth-6 Gate/speed tables filled from result.json; depth-1 crossover note.
- `.planning/REQUIREMENTS.md`: HARD-01/02/03 → `[x]` / Complete.
- `.planning/MILESTONES.md`: two v1.1 Known-Gaps rows discharged.
- `.planning/STATE.md`: GPUT-14 / Phase-10-11 BENCH-02 Deferred Items resolved.
</artifacts_produced>

<verification>
- Task 1 automated check: 15-EVIDENCE.md covers all four hazards and no depth-1/depth-6 TBD remains in RESULTS.md.
- Task 2 automated check: HARD-01/02/03 flipped to complete; no lingering `HARD-0[123].*Pending`; `grep -rn "GPUT-14" .planning/` reconciled.
- Human/orchestrator cross-check: every filled bench number matches `bench/phase15_cuda_oracle/result.json` (no fabrication).
</verification>

<success_criteria>
- 15-EVIDENCE.md records the honest per-hazard outcome for RV-13-01..04 (HARD-03 traceable).
- BENCH-03-SIGNOFF.md + RESULTS.md carry the real un-stitched single-session numbers with Region N/A + informational catboost_gpu_s and the depth-1 crossover recorded (HARD-01/HARD-02 traceable).
- REQUIREMENTS/MILESTONES/STATE reflect discharge; GPUT-14 satisfied; no stray Pending (D-11).
</success_criteria>

<output>
Create `.planning/phases/15-debt-discharge-cuda-oracle-re-establishment/15-04-SUMMARY.md` when done. Confirm all three HARD requirements are traceably discharged and the milestone Known Gaps are cleared.
</output>