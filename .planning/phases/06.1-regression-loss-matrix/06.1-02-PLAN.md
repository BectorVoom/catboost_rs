---
phase: 06.1-regression-loss-matrix
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-compute/src/loss_test.rs
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/cpu_runtime.rs
  - crates/cb-backend/src/cpu_runtime_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/metrics.rs
  - crates/cb-train/src/metrics_test.rs
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/fixtures/poisson/
  - crates/cb-oracle/fixtures/tweedie/
  - crates/cb-oracle/fixtures/mape/
  - crates/cb-oracle/fixtures/msle_metric/
  - crates/cb-train/tests/wave2_positive_loss_oracle_test.rs
  - crates/cb-train/tests/msle_metric_oracle_test.rs
autonomous: false
requirements: [LOSS-03]

must_haves:
  truths:
    - "A user can train with loss_function=Poisson: raw staged-approx matches upstream <=1e-5 AND exp-transformed predictions match upstream <=1e-5 (exp-link)"
    - "A user can train with Tweedie{variance_power} (raw approx, exp inside der) and oracle passes <=1e-5"
    - "A user can train with MAPE (der2=0, Gradient leaf) and oracle passes <=1e-5"
    - "A user can request eval_metric=MSLE and the metric value matches upstream MSLE metric <=1e-5 (MSLE is metric-only, NOT a Loss variant — D-6.1-06)"
    - "Existing scalar oracles + Wave-1 smooth-loss oracles stay green"
  artifacts:
    - path: "crates/cb-compute/src/loss.rs"
      provides: "poisson_der1/2 (inline exp), tweedie_der1/2 (exp inside der), mape_der1/2 (der2=0) with citations"
      contains: "pub fn poisson_der1"
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "Loss::Poisson, Loss::Tweedie{variance_power}, Loss::Mape variants (NO Loss::Msle)"
      contains: "Poisson"
    - path: "crates/cb-train/src/metrics.rs"
      provides: "EvalMetric::Msle (metric-only) + for_loss/eval arms"
      contains: "Msle"
    - path: "crates/cb-oracle/fixtures/poisson/config.json"
      provides: "upstream Poisson fixture incl. Predictions stage (exp-link validation, Open Q1)"
      contains: "Poisson"
    - path: "crates/cb-train/tests/wave2_positive_loss_oracle_test.rs"
      provides: "per-stage <=1e-5 oracle for Poisson/Tweedie/MAPE incl. Poisson raw-staged + exp-predictions"
      contains: "Exponent"
  key_links:
    - from: "crates/cb-compute/src/loss.rs"
      to: "f64::exp inline (Poisson)"
      via: "poisson_der1 computes target - approx.exp() on RAW approx (exp-approx equivalence, no exp-storage)"
      pattern: "approx.exp\\(\\)"
    - from: "crates/cb-train/tests/wave2_positive_loss_oracle_test.rs"
      to: "cb_model::PredictionType::Exponent"
      via: "Poisson Predictions stage applies the Exponent transform to raw approx"
      pattern: "Exponent"
---

<objective>
Land the **positive-domain / link regression losses** — Poisson (exp-link, the one genuinely-new machinery item), Tweedie{variance_power}, MAPE — end-to-end and oracle-locked <=1e-5 vs upstream catboost 1.2.10, and implement **MSLE as an eval_metric ONLY** (D-6.1-06: MSLE is metric-only upstream — `enum_helpers.cpp:200,533-549` — so it has NO training oracle and is NOT a `Loss` variant). Wave 2 of the family-wave structure (D-6.1-02).

Purpose: Adds the count/link-function losses to the LOSS-03 scalar matrix. Poisson exercises the exp-approx equivalence (inline `exp()` der + additive raw update + `Exponent` predict — same pattern as Logloss inline sigmoid) and validates Open Q1 (Poisson StagedApprox raw vs exp). MSLE closes the LOSS-03 MSLE member as the valid upstream *metric* without a training oracle that cannot exist.
Output: 3 new `Loss` variants, 6 der1/der2 fns + kernels, 1 new `EvalMetric::Msle`, exhaustive-match extensions, 3 training fixtures + 1 metric fixture, 2 passing oracle tests.

Decisions honored: D-6.1-02 (Wave 2 of the family-wave structure), D-6.1-03 (Loss::Tweedie{variance_power} enum-param pattern), D-6.1-05 (reuse Newton/Gradient leaf per fixture), D-6.1-06 (MSLE eval-metric only, not a Loss variant).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-regression-loss-matrix/06.1-CONTEXT.md
@.planning/phases/06.1-regression-loss-matrix/06.1-RESEARCH.md
@.planning/phases/06.1-regression-loss-matrix/06.1-PATTERNS.md
@crates/cb-compute/src/loss.rs
@crates/cb-compute/src/runtime.rs
@crates/cb-train/src/metrics.rs
@.planning/phases/06.1-regression-loss-matrix/06.1-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**New `Loss` enum variants (`cb-compute::runtime::Loss`):**
- `Poisson` (non-parametric, exp-link / IsStoreExpApprox upstream — cb-train computes exp inline on raw approx)
- `Tweedie { variance_power: f64 }` (variance_power MANDATORY, 1<p<2 — NOT exp-approx; exp lives inside der only)
- `Mape` (non-parametric, der2=0)

**Explicitly NOT a Loss variant:** `MSLE` — added to `EvalMetric` only (D-6.1-06 / RESEARCH A1).

**New der1/der2 fns (`cb-compute::loss`):**
- `poisson_der1` == `target - approx.exp()`, `poisson_der2` == `-approx.exp()` (inline exp, sigmoid precedent)
- `tweedie_der1`/`_der2` (param `variance_power`; exp inside formula, raw approx)
- `mape_der1` == `±1/max(1.0,|target|)`, `mape_der2` == `0`

**New kernels (`cb-backend::kernels`):** `poisson_gradient_kernel`/`_hessian_kernel` (F::exp inline), `tweedie_gradient_kernel`/`_hessian_kernel` (param Array<F>), `mape_gradient_kernel` (der2=0 → constant-vec, no hessian kernel).

**New eval metric (`cb-train::metrics`):** `EvalMetric::Msle` + `for_loss`/`eval` arms (log-space domain guard).

**Dispatch-site extensions:** `cb-backend::cpu_runtime::compute_gradients`, `cb-train::metrics::for_loss`, `cb-train::boosting::autolr_target_type`.

**New oracle fixtures:** `fixtures/{poisson,tweedie,mape}/{config.json,model.json,staged.npy,predictions.npy}` (Poisson+Tweedie carry predictions.npy for the Predictions stage; Tweedie pins variance_power=1.5, Newton iter=1; Poisson pins Newton iter=1 overriding upstream default 10) + `fixtures/msle_metric/{config.json,metric_values.npy}` (eval_metric only — NO training model.json).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 — generate Poisson/Tweedie/MAPE training fixtures + MSLE metric fixture (failing oracle scaffolds)</name>
  <files>crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/fixtures/poisson/, crates/cb-oracle/fixtures/tweedie/, crates/cb-oracle/fixtures/mape/, crates/cb-oracle/fixtures/msle_metric/, crates/cb-train/tests/wave2_positive_loss_oracle_test.rs, crates/cb-train/tests/msle_metric_oracle_test.rs</files>
  <read_first>
    - crates/cb-oracle/generator/gen_fixtures.py (numeric_tiny loader + save_model(json)/staged_predict/predict pattern; add predict(prediction_type) for the Poisson exp-predictions and a staged_predict('RawFormulaVal') for raw staged)
    - crates/cb-oracle/fixtures/leaf_methods/exact/config.json (config schema; pin leaf_estimation_method + iterations:1 + thread_count:1 + bootstrap_type:No + score_function:L2 + boost_from_average:false)
    - crates/cb-train/tests/loss_oracle_test.rs (oracle-test scaffold + compare_stage usage)
    - .planning/phases/06.1-regression-loss-matrix/06.1-RESEARCH.md (Exp-Link section, Open Q1, Pitfall 1/2/3/4/7; MSLE-not-an-objective A1)
  </read_first>
  <what-built>
    Four scenarios generated via `.venv/bin/python` (catboost==1.2.10, thread_count=1), all on a POSITIVE-target variant of the numeric_tiny corpus (Poisson/MAPE/Tweedie need positive targets; shift/abs y to a positive range and record the transform in config.json):
    - **poisson**: `loss_function="Poisson"`, `leaf_estimation_method="Newton"`, `leaf_estimation_iterations:1` PINNED (override upstream default 10 — Pitfall 3). Saves staged.npy (RawFormulaVal raw approx — Open Q1: inspect whether it is raw or exp and record the layout note) AND predictions.npy (default predict — the exp-transformed values for the Exponent stage). stages=[Splits, LeafValues, StagedApprox, Predictions].
    - **tweedie**: `loss_function="Tweedie:variance_power=1.5"`, `leaf_estimation_method="Newton"`, iterations:1. staged.npy (raw) + predictions.npy (RawFormulaVal — confirm Tweedie prediction is RAW, A4). stages incl. Predictions.
    - **mape**: `loss_function="MAPE"`, `leaf_estimation_method="Gradient"` (catboost_options.cpp:113-124; der2=0 so Newton is undefined — Pitfall 5), iterations:1. stages=[Splits, LeafValues, StagedApprox].
    - **msle_metric**: train a regression model with a VALID training objective (RMSE) but `eval_metric="MSLE"`; capture catboost's per-iteration MSLE metric values (eval history) into metric_values.npy. NO MSLE training model — MSLE is metric-only (A1/Pitfall 1). config.json records `eval_metric:"MSLE"` and the target transform.
    The two oracle tests (`wave2_positive_loss_oracle_test.rs`, `msle_metric_oracle_test.rs`) load these and compare_stage / compare metric values (RED until Tasks 2-4 land).
  </what-built>
  <action>
    Binary fixtures MUST be generated offline with `.venv/bin/python crates/cb-oracle/generator/gen_fixtures.py` (only `.venv` has catboost==1.2.10). **Open Q1 (RESEARCH):** inspect the Poisson staged.npy vs `exp(model raw approx)` and pin `staged_layout`: StagedApprox is RAW approx, Predictions = `exp(raw)` (A2) — record the empirical finding in poisson/config.json. **Pitfall 1:** do NOT author a Poisson/Tweedie/MAPE fixture that throws; do NOT author an MSLE *training* fixture (loss_function=MSLE throws upstream) — MSLE is metric-only. This is a `checkpoint:human-verify` (runs the .venv interpreter to produce committed frozen artifacts). If `.venv` catboost cannot run in-env, STOP and surface — do not fabricate fixture bytes, do not #[ignore] or weaken any oracle.
  </action>
  <how-to-verify>
    1. Run `.venv/bin/python crates/cb-oracle/generator/gen_fixtures.py`.
    2. Confirm `fixtures/{poisson,tweedie,mape}/model.json` parse + carry 5 trees; `fixtures/poisson/predictions.npy` and `fixtures/tweedie/predictions.npy` exist.
    3. Confirm `fixtures/msle_metric/metric_values.npy` exists and `fixtures/msle_metric/` has NO model.json under MSLE-as-objective (it may carry an RMSE-trained model only for the eval history — document which).
    4. Confirm poisson/config.json records the Open-Q1 finding (staged = raw, predictions = exp).
    5. `cargo test -p cb-train --test wave2_positive_loss_oracle_test` and `--test msle_metric_oracle_test` COMPILE and FAIL RED.
  </how-to-verify>
  <verify>
    <automated>test -f crates/cb-oracle/fixtures/poisson/predictions.npy && test -f crates/cb-oracle/fixtures/tweedie/model.json && test -f crates/cb-oracle/fixtures/mape/model.json && test -f crates/cb-oracle/fixtures/msle_metric/metric_values.npy</automated>
  </verify>
  <acceptance_criteria>
    - Poisson/Tweedie/MAPE training fixtures + MSLE metric fixture exist and committed; each training config.json pins leaf_estimation_method + iterations:1 (Poisson Newton/1, Tweedie Newton/1, MAPE Gradient/1) + thread_count:1 + isolating params.
    - Poisson + Tweedie carry predictions.npy; poisson/config.json records the Open-Q1 finding (StagedApprox raw / Predictions exp).
    - msle_metric fixture has metric_values.npy and NO MSLE-as-objective model (MSLE metric-only — A1).
    - Both oracle tests compile + fail RED with NO #[ignore] and standard 1e-5 tolerance.
  </acceptance_criteria>
  <done>Poisson/Tweedie/MAPE training fixtures + MSLE metric fixture generated and committed; Open-Q1 layout recorded; both wave2 oracle tests compile and fail RED.</done>
  <resume-signal>Type "approved" once the four fixtures are generated, Open-Q1 layout is recorded, and both oracle tests compile+fail RED, or describe the blocker.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poisson/Tweedie/MAPE der1/der2 + Loss variants + kernels + dispatch</name>
  <files>crates/cb-compute/src/runtime.rs, crates/cb-compute/src/loss.rs, crates/cb-compute/src/loss_test.rs, crates/cb-backend/src/kernels.rs, crates/cb-backend/src/cpu_runtime.rs, crates/cb-backend/src/cpu_runtime_test.rs</files>
  <read_first>
    - crates/cb-compute/src/loss.rs (sigmoid inline-link loss.rs:54 — Poisson copies the exp-inside-der shape; mae_der1/der2 loss.rs:161 der2=0 deadzone — MAPE copies; focal_der1 loss.rs:117 parametric-arg signature — Tweedie copies; FOCAL_P_MIN clamp loss.rs:103 domain-guard precedent)
    - crates/cb-compute/src/runtime.rs (Loss enum runtime.rs:47; Focal{..} parametric precedent runtime.rs:63; PartialEq-only derive runtime.rs:46)
    - crates/cb-backend/src/kernels.rs (logloss_gradient_kernel kernels.rs:40 F::exp inline — Poisson copies; mae_gradient_kernel kernels.rs:60 der2=0 — MAPE; focal_gradient_kernel kernels.rs:104 length-1 Array<F> param — Tweedie)
    - crates/cb-backend/src/cpu_runtime.rs (compute_gradients match cpu_runtime.rs:212; two-kernel Focal arm cpu_runtime.rs:226; constant-der2 Mae arm cpu_runtime.rs:231 — MAPE copies)
    - catboost-master/catboost/private/libs/algo_helpers/error_functions.h:657-676 (Poisson), :1634-1665 (Tweedie), :607-630 (MAPE)
    - catboost-master/catboost/private/libs/algo_helpers/approx_updater_helpers.h:60-72 (IsStoreExpApprox list — Poisson YES, Tweedie NO)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (MANDATORY before kernel code)
  </read_first>
  <behavior>
    - `poisson_der1(approx,target)` == `target - approx.exp()`; `poisson_der2(approx,_)` == `-approx.exp()` (RAW approx; cb-train stores raw, exp computed inline — error_functions.h:657-676, sigmoid precedent).
    - `tweedie_der1(approx,target,p)` == `target*e^((1-p)*approx) - e^((2-p)*approx)`; `tweedie_der2` == `target*(1-p)*e^((1-p)*approx) - (2-p)*e^((2-p)*approx)` (raw approx, exp inside — error_functions.h:1634-1665).
    - `mape_der1(approx,target)` == `sign(target-approx)/max(1.0,|target|)` (f32-domain `1.f`, Pitfall 7); `mape_der2` == `0` (error_functions.h:607-630).
    - Unit tests pin each at >=2 known points; Poisson test confirms the exp on a known approx; Tweedie at p=1.5; MAPE near the |target|<1 vs >1 boundary.
  </behavior>
  <action>
    Add `Poisson` (non-param), `Tweedie{variance_power:f64}` (param), `Mape` (non-param) to `Loss` (runtime.rs) copying Focal/Mae shapes; cite error_functions.h lines in doc-comments; keep PartialEq-only (NO Eq). Transcribe the 6 der1/der2 into loss.rs in the rmse_der1 citation style. **Poisson: compute `approx.exp()` INLINE inside the der (RESEARCH Pattern 2 — do NOT implement exp-approx storage; the inline-exp + additive-raw-update + Exponent-predict equivalence matches the Logloss inline-sigmoid approach).** **Tweedie: exp lives inside the der formula only — raw approx, NO Exponent transform (A4).** MAPE der2=0 (Mae precedent). Range-guard `1<variance_power<2` (Tweedie, error_functions.h:643) -> typed CbError; guard Poisson/MAPE against domain overflow mirroring the FOCAL_P_MIN clamp discipline (Poisson exp overflow, MAPE division — never `unwrap`/panic). Add the kernels: `poisson_gradient_kernel`/`_hessian_kernel` using F::exp inline (logloss kernel precedent); `tweedie_gradient_kernel`/`_hessian_kernel` with variance_power as length-1 Array<F>; `mape_gradient_kernel` only (der2=0 — fill a constant-0 der2 vec in the dispatch arm, Mae precedent). Extend the `compute_gradients` match: Poisson+Tweedie two-kernel arms; MAPE constant-der2 arm. On ANY CubeCL build error load cubecl_error_guideline.md FIRST.
  </action>
  <verify>
    <automated>cargo test -p cb-compute loss</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-compute` and `cargo test -p cb-backend` exit 0.
    - `grep -n "pub fn poisson_der1\|pub fn tweedie_der1\|pub fn mape_der1" crates/cb-compute/src/loss.rs` returns all three.
    - `grep -n "approx.exp()" crates/cb-compute/src/loss.rs` confirms Poisson computes exp inline (no exp-approx storage added).
    - Loss enum stays PartialEq-only (no Eq derive added).
    - Tweedie der uses exp INSIDE the formula and is NOT routed to the Exponent predict transform.
    - Range guard rejects variance_power outside (1,2) with typed CbError; no `unwrap()` in new production code; tests in loss_test.rs (source/test separation).
  </acceptance_criteria>
  <done>Poisson (inline-exp), Tweedie (exp-in-der, raw), MAPE (der2=0) transcribed+cited with kernels + dispatch; cb-compute/cb-backend green.</done>
</task>

<task type="auto">
  <name>Task 3: Poisson exp-link predict wiring + leaf/metric dispatch → Wave-2 training oracle GREEN</name>
  <files>crates/cb-train/src/boosting.rs, crates/cb-train/src/metrics.rs, crates/cb-train/tests/wave2_positive_loss_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/src/boosting.rs (autolr_target_type match boosting.rs:637; compute_leaf_deltas Newton/Gradient dispatch boosting.rs:667; BoostParams.leaf_method boosting.rs:117)
    - crates/cb-train/src/metrics.rs (for_loss exhaustive match metrics.rs:59 — new variants break it)
    - crates/cb-train/tests/loss_oracle_test.rs (train->compare_stage scaffold)
    - .planning/phases/06.1-RESEARCH.md Exp-Link section + Pitfall 4 (Poisson StagedApprox raw, Predictions exp via cb_model PredictionType::Exponent — oracle-locked Plan 04-02, do NOT hand-roll)
    - crates/cb-oracle/src/compare.rs (compare_stage compare.rs:84; Stage::Predictions compare.rs:10)
  </read_first>
  <action>
    Extend the two exhaustive `match loss` sites: `autolr_target_type` (boosting.rs:637) — map `Loss::Poisson | Loss::Tweedie{..} | Loss::Mape` to `TargetType::Unknown` (Mae precedent); `metrics::for_loss` (metrics.rs:59) — map Poisson/Tweedie/MAPE to their default eval metric (RMSE-arm default unless a fixture pins eval_metric, matching `Loss::Mae => Self::Rmse`). No new leaf code: fixtures pin leaf_estimation_method (Poisson/Tweedie Newton, MAPE Gradient) and iterations:1; `compute_leaf_deltas` already dispatches them. Wire the Wave-2 oracle test: for Poisson assert `Stage::StagedApprox` against RAW staged.npy AND `Stage::Predictions` against predictions.npy AFTER applying `cb_model::predict_raw` + `PredictionType::Exponent` (the boosting raw-approx update is UNCHANGED — additive; the exp is ONLY the final predict transform, Pitfall 4). For Tweedie assert Predictions against the RAW predict (no Exponent — A4). For MAPE assert Splits/LeafValues/StagedApprox. Flip RED->GREEN at <=1e-5. Any metric sum routes through `cb_core::sum_f64`. `rg "match .*loss|Loss::" crates/*/src` to confirm exhaustive coverage before done.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test wave2_positive_loss_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train --test wave2_positive_loss_oracle_test` exits 0 — Poisson raw StagedApprox + exp Predictions both <=1e-5; Tweedie + MAPE <=1e-5 (GREEN, no #[ignore]).
    - Poisson Predictions stage routes through `PredictionType::Exponent` (not a hand-rolled exp); Tweedie Predictions are RAW (no Exponent).
    - `cargo test -p cb-train --test wave1_smooth_loss_oracle_test` and `cargo test -p cb-train` still green (Wave-1 + existing ~40 oracles).
    - All exhaustive Loss matches handle the three new variants (rg verification, no `_ =>` masking a missed site).
  </acceptance_criteria>
  <done>Poisson/Tweedie/MAPE training oracles GREEN <=1e-5 incl. Poisson exp-link Predictions via Exponent; Wave-1 + existing oracles still green.</done>
</task>

<task type="auto">
  <name>Task 4: MSLE eval-metric (metric-only) → MSLE metric oracle GREEN</name>
  <files>crates/cb-train/src/metrics.rs, crates/cb-train/src/metrics_test.rs, crates/cb-train/tests/msle_metric_oracle_test.rs</files>
  <read_first>
    - crates/cb-train/src/metrics.rs (EvalMetric enum metrics.rs:43; for_loss metrics.rs:59; Rmse weighted eval arm metrics.rs:114 — sum_f64 fold; Logloss clamp(1e-15,1-1e-15) domain-guard metrics.rs:136; total_weight>0 guard metrics.rs:107)
    - crates/cb-train/src/metrics_test.rs (the #[path] test-module convention metrics.rs:196 — source/test separation)
    - catboost-master/catboost/libs/metrics/ (the MSLE metric formula — mean squared log error: mean( (ln(1+approx) - ln(1+target))^2 ); transcribe the exact upstream form + its domain guard)
    - .planning/phases/06.1-RESEARCH.md (D-6.1-06 / A1: MSLE metric-only; Pitfall 1 — never a training fixture)
  </read_first>
  <action>
    Add `EvalMetric::Msle` (metric-only — NOT a `Loss` variant; D-6.1-06). Add the `eval` arm transcribing the upstream MSLE metric formula from catboost/libs/metrics/ with its exact `metric.cpp:<line>` citation: weighted mean of `(ln(1+approx) - ln(1+target))^2`. Guard the log domain (1+approx, 1+target must be > 0) mirroring the Logloss `clamp` precedent (metrics.rs:136) — clamp/guard to avoid `ln` of non-positive, return typed `CbError` on a genuine domain violation (never `unwrap`/panic). Route the squared-error sum through `cb_core::sum_f64` (D-08); divide guarded by `total_weight>0` (metrics.rs:107). `EvalMetric::for_loss` does NOT need an MSLE arm (no Loss maps to it; MSLE is selected via an explicit eval_metric, not a default). Wire `msle_metric_oracle_test.rs`: compute MSLE per-iteration over the fixture's staged approx and compare to upstream metric_values.npy <=1e-5. Unit-test the MSLE formula at known (approx,target) points in metrics_test.rs.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test msle_metric_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-train --test msle_metric_oracle_test` exits 0 — per-iteration MSLE matches upstream metric_values.npy <=1e-5.
    - `grep -n "Msle" crates/cb-compute/src/runtime.rs` returns NOTHING (MSLE is NOT a Loss variant — D-6.1-06); `grep -n "Msle" crates/cb-train/src/metrics.rs` returns the EvalMetric variant + eval arm.
    - MSLE squared-error sum routes through `cb_core::sum_f64`; log-domain guard returns typed CbError (no `unwrap`/panic); test in metrics_test.rs (source/test separation).
    - `cargo test -p cb-train` still green.
  </acceptance_criteria>
  <done>MSLE implemented as eval_metric only (oracle-locked as a metric <=1e-5); MSLE absent from the Loss enum; cb-train green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hyperparameter -> Loss constructor | Tweedie variance_power outside (1,2) yields NaN/Inf der |
| raw approx -> Poisson `exp()` | Large raw approx overflows `exp()` to +Inf, poisoning der/leaf |
| target/approx -> MSLE `ln(1+x)` | Non-positive 1+approx / 1+target -> ln domain violation (NaN) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1.02-01 | Tampering (DoS) | Poisson `approx.exp()` overflow on large raw approx | mitigate | Positive-target fixture keeps raw approx in range; exp overflow surfaces as an oracle failure (compare_stage NaN-rejects compare.rs:55-58); mirror the Focal-clamp domain-guard discipline — never panic |
| T-06.1.02-02 | Tampering | Tweedie variance_power outside (1,2) | mitigate | Range-check 1<p<2 in the Loss constructor (error_functions.h:643) -> typed CbError; no unwrap/panic |
| T-06.1.02-03 | Tampering | MSLE `ln(1+approx)` / `ln(1+target)` domain violation | mitigate | Log-domain guard mirroring Logloss clamp(1e-15,1-1e-15) (metrics.rs:136) -> typed CbError on non-positive argument; never silent NaN |
| T-06.1.02-04 | Tampering | MAPE division by max(1,|target|) | accept | Divisor is `max(1.0,|target|) >= 1.0` by construction — division is always safe (no zero divisor possible) |
| T-06.1.02-SC | Tampering | package installs | mitigate | No new external packages (RESEARCH Package Legitimacy Audit: N/A) — internal code + pinned .venv catboost==1.2.10 generator |
</threat_model>

<verification>
- `cargo test -p cb-compute` / `-p cb-backend` — der1/der2 + kernels green.
- `cargo test -p cb-train --test wave2_positive_loss_oracle_test` — Poisson/Tweedie/MAPE per-stage <=1e-5 (Poisson raw staged + exp predictions).
- `cargo test -p cb-train --test msle_metric_oracle_test` — MSLE metric <=1e-5.
- `cargo test -p cb-train --test wave1_smooth_loss_oracle_test` + `cargo test -p cb-train` — Wave-1 + existing ~40 oracles green.
- Verify per-crate (disk pressure, Memory).
</verification>

<success_criteria>
- Poisson, Tweedie{variance_power}, MAPE each train and match upstream <=1e-5 per-stage incl. Poisson exp-link Predictions (SC#1 partial — Wave 2 share).
- MSLE implemented as eval_metric only, oracle-locked as a metric, NOT a trainable Loss (SC#1 MSLE clause, D-6.1-06).
- der1/der2 transcribed+cited; parity sums via cb_core::sum_f64 (SC#2).
- Existing + Wave-1 oracles green; no behavior change to existing losses (SC#3).
</success_criteria>

<output>
Create `.planning/phases/06.1-regression-loss-matrix/06.1-02-SUMMARY.md` when done.
</output>
