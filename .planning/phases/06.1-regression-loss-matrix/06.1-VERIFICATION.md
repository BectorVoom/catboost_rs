---
phase: 06.1-regression-loss-matrix
verified: 2026-06-16T06:30:00Z
status: human_needed
score: 4/4 must-haves verified (all roadmap success criteria met; human items below)
overrides_applied: 0
human_verification:
  - test: "Run `cargo test -p cb-train --test wave1_smooth_loss_oracle_test` in a clean shell"
    expected: "4/4 tests pass (wave1_oracle_logcosh, wave1_oracle_lq, wave1_oracle_huber, wave1_oracle_expectile) — all ≤1e-5"
    why_human: "Orchestrator pre-confirmed 4/4 pass; verifier cannot run the test suite due to documented disk pressure (cb-compute test profile can't link). Regression check requires a human to confirm the pass is still current against the committed source."
  - test: "Run `cargo test -p cb-train --test wave2_positive_loss_oracle_test` and `--test msle_metric_oracle_test`"
    expected: "3/3 + 1/1 pass: Poisson/Tweedie/MAPE stages ≤1e-5; MSLE metric ≤1e-5"
    why_human: "Same disk-pressure constraint. Pre-confirmed 3/3 + 1/1 by orchestrator; human confirms these still hold."
  - test: "Run `cargo test -p cb-train --test wave3_quantile_loss_oracle_test`"
    expected: "2/2 pass: quantile_alpha07 ≤1e-5 AND quantile_alpha05_equals_mae (Quantile{0.5} leaf values + staged == leaf_methods/exact fixture ≤1e-5)"
    why_human: "Same disk-pressure constraint."
  - test: "Run `cargo test -p cb-train` (full cb-train suite including the existing ~40 prior oracles)"
    expected: "All prior oracles green — no behavior change to RMSE/MAE/Logloss/CrossEntropy/Focal/leaf_methods/ordered/tensor_ctr/bootstrap oracles"
    why_human: "SC#3 regression guard; pre-confirmed by orchestrator (141 lib + ~35 integration tests). Human confirms still green after all Wave 1/2/3 additions."
---

# Phase 6.1: Regression-Loss Matrix Verification Report

**Phase Goal:** LOSS-03 scalar regression-loss matrix — land the scalar losses (RMSE/MAE already existed; this phase adds Quantile, LogCosh, Huber, Poisson, Tweedie, MAPE, MSLE-metric, Lq, Expectile) riding the existing scalar boosting loop, each oracle-locked ≤1e-5 vs upstream catboost 1.2.10. MultiQuantile is explicitly out of scope (relocated to 6.2).
**Verified:** 2026-06-16T06:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | RMSE, MAE, Quantile, LogCosh, Huber, Poisson, Tweedie, MAPE, Lq, Expectile each train ≤1e-5 per-stage; MSLE implemented as eval_metric only oracle-locked as a metric | VERIFIED | `Loss` enum has all 10 variants (runtime.rs:47-160). All 9 oracle fixtures (logcosh, lq, huber, expectile, poisson, tweedie, mape, quantile_alpha07, quantile_alpha05_mae) contain `oblivious_trees` in model.json (9/9 confirmed). MSLE absent from `Loss` enum (grep returned empty). `EvalMetric::Msle` present in metrics.rs:54. `msle_metric/metric_values.npy` committed, no model.json under MSLE-as-objective. Test files wave1/2/3_*_oracle_test.rs exist and are substantive (all 4+3+2+1 oracle tests wired with compare_stage at ≤1e-5). Orchestrator pre-confirmed 4/4 + 3/3 + 1/1 + 2/2 pass. |
| SC2 | der1/der2 transcribed from error_functions.{h,cpp}; parity sums via cb_core::sum_f64 | VERIFIED | 8 new der1 functions in loss.rs (logcosh_der1, lq_der1, huber_der1, expectile_der1, poisson_der1, tweedie_der1, mape_der1, quantile_der1) at lines 166–410, each citing error_functions.h line numbers in doc-comments. No raw `.iter().sum()` or `fold(0.0,` found in production files — all metric sums route through `sum_f64`. No `unwrap()` in loss.rs, runtime.rs, or metrics.rs (0/0/0 grep hits). |
| SC3 | Existing ~40 scalar oracles stay green; no behavior change to existing losses | VERIFIED (pre-confirmed) | `autolr_target_type` and `metrics::for_loss` both use exhaustive match with all new variants explicitly handled (boosting.rs:650-659, metrics.rs:78-91) — no `_ =>` wildcard masking missed sites. `mae_der1`/`mae_der2` delegate to `quantile_der1(…, QUANTILE_ALPHA, QUANTILE_DELTA)` (loss.rs:192-202), preserving byte-identity. Exact branch in compute_leaf_deltas preserves prior behavior: `Loss::Mae` maps to `(QUANTILE_ALPHA, QUANTILE_DELTA)` fallback (boosting.rs:749-751). `cargo check --all-targets -p cb-compute -p cb-backend -p cb-train` clean. Orchestrator pre-confirmed 141 lib + ~35 integration tests all passing. |
| SC4 | "etc." losses (MultiQuantile) deferred to v2/6.2, not silently in-scope | VERIFIED | No `MultiQuantile` variant in `Loss` enum. ROADMAP.md records MultiQuantile relocated to 6.2. |

**Score:** 4/4 truths verified (automated evidence passes; human run needed per disk-pressure constraint)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-compute/src/loss.rs` | 8 new der1/der2 fns with error_functions.h citations | VERIFIED | Lines 166-410: quantile_der1/2, logcosh_der1/2, lq_der1/2, huber_der1/2, expectile_der1/2, poisson_der1/2, tweedie_der1/2, mape_der1/2 — all present with doc-comment citations |
| `crates/cb-compute/src/runtime.rs` | Loss::Quantile, LogCosh, Lq, Huber, Expectile, Poisson, Tweedie, Mape variants | VERIFIED | All 8 new variants at lines 85-159; PartialEq-only derive (line 46, no Eq on Loss) |
| `crates/cb-train/src/metrics.rs` | EvalMetric::Msle (metric-only, no Loss arm) | VERIFIED | Msle variant at line 54; for_loss has no Msle arm (metric-only); MSLE eval arm at lines 168-191 |
| `crates/cb-backend/src/cpu_runtime.rs` | compute_gradients exhaustive match for all 8 new variants | VERIFIED | All new variants have arms (lines 467-517): Quantile, LogCosh, Lq, Huber, Expectile, Poisson, Tweedie, Mape — no `_ =>` wildcard |
| `crates/cb-train/src/boosting.rs` | autolr_target_type + Exact branch threads loss (alpha,delta) | VERIFIED | autolr exhaustive at lines 650-659; Exact branch parameterizes alpha/delta from loss at lines 749-762 |
| `crates/cb-oracle/fixtures/logcosh/` | config.json + model.json + staged.npy | VERIFIED | All 3 files present; config.json pins leaf_estimation_method:Exact, leaf_estimation_iterations:1, score_function:L2, bootstrap_type:No, boost_from_average:false |
| `crates/cb-oracle/fixtures/lq/` | config.json + model.json + staged.npy | VERIFIED | Present |
| `crates/cb-oracle/fixtures/huber/` | config.json + model.json + staged.npy | VERIFIED | Present |
| `crates/cb-oracle/fixtures/expectile/` | config.json + model.json + staged.npy | VERIFIED | Present; all four Wave-1 fixtures also carry predictions.npy |
| `crates/cb-oracle/fixtures/poisson/` | config.json + model.json + staged.npy + predictions.npy | VERIFIED | All 4 files present; config.json records staged_layout = RAW approx (Open Q1 resolved) |
| `crates/cb-oracle/fixtures/tweedie/` | config.json + model.json + staged.npy + predictions.npy | VERIFIED | All 4 files present |
| `crates/cb-oracle/fixtures/mape/` | config.json + model.json + staged.npy | VERIFIED | All 3 files present |
| `crates/cb-oracle/fixtures/msle_metric/` | config.json + metric_values.npy; NO model.json under MSLE-objective | VERIFIED | Has config.json, metric_values.npy, model.json (RMSE-trained base model), staged.npy — MSLE fixture correctly holds no MSLE-as-loss model |
| `crates/cb-oracle/fixtures/quantile_alpha07/` | config.json + model.json + staged.npy | VERIFIED | All 3 files present; config records loss=Quantile:alpha=0.7 + leaf_method=Exact |
| `crates/cb-oracle/fixtures/quantile_alpha05_mae/` | config.json + model.json + staged.npy | VERIFIED | All 3 files present |
| `crates/cb-train/tests/wave1_smooth_loss_oracle_test.rs` | 4 oracle tests, no #[ignore] | VERIFIED | 4 tests (wave1_oracle_logcosh/lq/huber/expectile), substantive compare_stage calls, tolerance 1e-5 |
| `crates/cb-train/tests/wave2_positive_loss_oracle_test.rs` | Poisson/Tweedie/MAPE oracle incl. Poisson Predictions stage | VERIFIED | Substantive: loads positive y via config target_shift, uses Stage::StagedApprox + cb_model::apply_prediction_type(Exponent) for Poisson |
| `crates/cb-train/tests/msle_metric_oracle_test.rs` | MSLE per-iteration metric oracle | VERIFIED | File exists, uses EvalMetric::Msle, gates against metric_values.npy |
| `crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs` | Quantile{0.7} + Quantile{0.5}==MAE anchor | VERIFIED | Two tests: wave3_oracle_quantile_alpha07 and wave3_oracle_quantile_alpha05_equals_mae; the latter also cross-checks against leaf_methods/exact fixture |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `crates/cb-backend/src/cpu_runtime.rs` | `crates/cb-compute/src/loss.rs` (kernel dispatch) | compute_gradients match arm launches loss kernel | VERIFIED | All 8 new Loss variants have explicit arms in compute_gradients (lines 467-517); imports all new kernel functions at lines 22-29 |
| `crates/cb-train/src/boosting.rs` | `cb_compute::leaf::exact_leaf_delta` | compute_leaf_deltas Exact branch threads loss (alpha,delta) | VERIFIED | Lines 749-762: `Loss::Quantile{alpha,delta} => (alpha,delta)`, `_ => (QUANTILE_ALPHA, QUANTILE_DELTA)`, passed to `exact_leaf_delta` |
| `crates/cb-compute/src/loss.rs` | Poisson inline exp | `poisson_der1` computes `target - approx.exp()` on RAW approx | VERIFIED | loss.rs:340 `target - approx.exp()`, loss.rs:351 `-approx.exp()` |
| `crates/cb-train/tests/wave2_positive_loss_oracle_test.rs` | `cb_model::PredictionType::Exponent` | Poisson Predictions stage applies Exponent transform | VERIFIED | Test imports `apply_prediction_type, PredictionType` from cb_model; uses Exponent for Poisson predictions stage |
| `crates/cb-train/src/metrics.rs` | `cb_core::sum_f64` | MSLE squared-error sum routes through sum_f64 | VERIFIED | metrics.rs:190: `Ok(sum_f64(&weighted_sq) / total_weight)` |

### Data-Flow Trace (Level 4)

Wave-1/2/3 losses all flow through compute_gradients (kernel → cpu_runtime → boosting train loop → leaf reduction → model). The oracle tests gate the final trained model output against committed upstream fixtures, making the data-flow real end-to-end. No static/empty data sources were found.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| wave1_smooth_loss_oracle_test.rs | `staged` approx + `model` | `train()` driving CpuBackend→compute_gradients→kernels | Yes (oracle fixtures from catboost 1.2.10) | FLOWING |
| metrics.rs::Msle | `weighted_sq` | sum over approx/target pairs from staged fixture | Yes (metric_values.npy from catboost 1.2.10) | FLOWING |
| boosting.rs Exact branch | `quantile_alpha`/`quantile_delta` | destructured from `params.loss` (runtime Loss::Quantile) | Yes (user-supplied, threaded from BoostParams) | FLOWING |

### Behavioral Spot-Checks

Step 7b SKIPPED for the new oracle test invocations due to documented disk pressure (cb-compute test profile cannot link; `cargo test --workspace` exhausts disk). The orchestrator pre-confirmed all oracle test suites pass via per-crate invocation. Human verification items (below) cover the same behavioral claims.

The following checks are runnable without the test suite:

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 8 new der1 fns exist in loss.rs | `grep "pub fn.*_der1" crates/cb-compute/src/loss.rs \| wc -l` | 8 | PASS |
| Loss enum does NOT derive Eq | `grep "derive.*Eq" crates/cb-compute/src/runtime.rs \| head -3` | Line 46 has only PartialEq; Eq only on EScoreFunction and Derivatives | PASS |
| MSLE absent from Loss enum | `grep "Msle" crates/cb-compute/src/runtime.rs` | No output | PASS |
| Exact branch no longer unconditionally hardcodes QUANTILE_ALPHA | `grep "QUANTILE_ALPHA" crates/cb-train/src/boosting.rs` | Used only as fallback in `_ =>` arm (line 751), not unconditional | PASS |
| No unwrap() in production loss/metrics files | `grep -c "unwrap()" loss.rs runtime.rs metrics.rs` | 0/0/0 | PASS |
| No TBD/FIXME/XXX debt markers in modified files | `grep -rn "TBD\|FIXME\|XXX" …` | No output | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` declared for this phase. SKIPPED.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| LOSS-03 | 06.1-01, 06.1-02, 06.1-03 | Regression matrix — scalar losses oracle-locked ≤1e-5; MultiQuantile to 6.2 | SATISFIED | 10 new Loss variants (Quantile/LogCosh/Lq/Huber/Expectile/Poisson/Tweedie/Mape) + MSLE eval metric; 9 oracle fixture sets committed; REQUIREMENTS.md traceability table updated: "Phase 6.1 scalar LOSS-03 COMPLETE. MultiQuantile … lands in 6.2" |

### Anti-Patterns Found

No TBD/FIXME/XXX debt markers found in any modified production file. No `unwrap()` in production code. No stub patterns (empty returns, placeholder comments). No inline `#[cfg(test)]` in production files (source/test separation observed — tests in `*_test.rs` files).

Code review (06.1-REVIEW.md) identified 5 WARNINGs and 4 Info items. None are blockers for the phase goal. Summary:

| File | Finding | Severity | Impact on Phase Goal |
|------|---------|----------|----------------------|
| `kernels.rs:61-110` | WR-01: MAE/Quantile kernel deadzone boundary returns 0 where upstream returns alpha (measure-zero — no fixture residual hits ±delta exactly) | WARNING | Sub-tolerance; oracle passes because f64-exact tie is measure-zero at delta=1e-6. Does not block LOSS-03 completion. |
| `leaf.rs:165-310` | WR-02: Exact-leaf solvers skip upstream's final f32 narrowing (sub-f32-epsilon gap, under 1e-5 bar) | WARNING | Sub-tolerance deviation; does not block. |
| `runtime.rs:223-234` | WR-03: Quantile delta missing upstream's `<= 1e-2` upper bound in validate() | WARNING | Validation gap; no NaN results, does not affect oracle. |
| `cpu_runtime.rs` (multiple) | WR-04: kernel read-back failure silently returns all-zero gradients instead of error | WARNING | Silent corruption path in error case only; pre-existing in RMSE/Logloss launchers. |
| `loss.rs:339-383` | WR-05: Poisson/Tweedie exp-link has no overflow guard on raw approx | WARNING | Production robustness gap; positive-target fixture keeps approx bounded; oracle unaffected. |

All 5 warnings are sub-tolerance robustness/divergence issues — none cause oracle failures or block the LOSS-03 completion claim. They are appropriate targets for a follow-up hardening plan.

### Human Verification Required

**Disk pressure constraint (Memory.md):** `cb-compute test profile can't link` under full workspace build. Orchestrator pre-confirmed the test results below; human must confirm they remain current.

#### 1. Wave-1 oracle suite

**Test:** `cargo test -p cb-train --test wave1_smooth_loss_oracle_test`
**Expected:** 4/4 pass: wave1_oracle_logcosh, wave1_oracle_lq, wave1_oracle_huber, wave1_oracle_expectile — all Splits/LeafValues/StagedApprox ≤1e-5
**Why human:** Disk-pressure constraint prevents verifier from running; orchestrator reported 4/4 pass but verifier must not accept pre-run claims as sufficient evidence for `status: passed`.

#### 2. Wave-2 oracle suite

**Test:** `cargo test -p cb-train --test wave2_positive_loss_oracle_test` and `cargo test -p cb-train --test msle_metric_oracle_test`
**Expected:** 3/3 Poisson/Tweedie/MAPE pass (Poisson raw staged + Exponent predictions both ≤1e-5); 1/1 MSLE metric ≤1e-5
**Why human:** Same disk-pressure constraint.

#### 3. Wave-3 oracle suite

**Test:** `cargo test -p cb-train --test wave3_quantile_loss_oracle_test`
**Expected:** 2/2 pass — quantile_alpha07 ≤1e-5 AND Quantile{0.5} leaf/staged matches leaf_methods/exact (MAE) fixture ≤1e-5
**Why human:** Same disk-pressure constraint.

#### 4. Prior-oracle regression guard (SC#3)

**Test:** `cargo test -p cb-train` (full suite)
**Expected:** All prior ~40+ scalar oracles (slice_first/leaf_methods/ordered/tensor_ctr/loss_oracle/bootstrap/etc.) still green — no behavior change introduced by new Loss variants
**Why human:** Same disk-pressure constraint. This is the SC#3 non-regression gate.

### Gaps Summary

No gaps found. All must-haves are verified by static analysis and orchestrator-confirmed test runs. The 5 code-review warnings are robustness issues that do not block the phase goal (all are sub-tolerance or error-path only); they should be tracked in a follow-up plan.

The `human_needed` status is driven exclusively by the environment constraint (disk pressure prevents the verifier from running the test suite). Once a human confirms the four oracle suites above pass, this phase is fully PASSED.

---

_Verified: 2026-06-16T06:30:00Z_
_Verifier: Claude (gsd-verifier)_
