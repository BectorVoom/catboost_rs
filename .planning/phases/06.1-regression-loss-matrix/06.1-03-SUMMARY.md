---
phase: 06.1-regression-loss-matrix
plan: 03
subsystem: training
tags: [quantile, mae, exact-leaf, weighted-quantile, loss-functions, gradient-boosting, cubecl, oracle-parity]

# Dependency graph
requires:
  - phase: 06.1-regression-loss-matrix
    plan: 01
    provides: "Loss enum + der1/der2 transcription pattern, generics-float kernel + cpu_runtime dispatch, per-stage oracle harness, ParamKernel single-scalar launcher, Exact-leaf-by-loss switch in compute_leaf_deltas"
  - phase: 06.1-regression-loss-matrix
    plan: 02
    provides: "Wave-2 link-loss patterns, two-param launch_focal_f64 (length-1 alpha/gamma arrays), leaf/metric/autolr dispatch extension pattern"
  - phase: 03-scalar-boosting
    provides: "Exact leaf method (weighted alpha-quantile via exact_leaf_delta, already alpha-general), per-stage oracle harness (compare_stage)"
provides:
  - "Loss::Quantile{alpha,delta} — the alpha-general quantile loss generalizing Loss::Mae (MAE == Quantile{0.5,1e-6})"
  - "quantile_der1(approx,target,alpha,delta) + quantile_der2==0 in cb-compute::loss; mae_der1/der2 now DELEGATE (MAE byte-stable)"
  - "quantile_gradient_kernel<F: Float> (alpha/delta as length-1 Array<F>) + launch_quantile_f64 dispatch"
  - "Exact-leaf alpha-threading: compute_leaf_deltas threads params.loss (alpha,delta) instead of hardcoded QUANTILE_ALPHA/QUANTILE_DELTA (RESEARCH Pattern 3 / D-6.1-05)"
  - "wave3_quantile_loss_oracle_test (Quantile{0.7} + Quantile{0.5} per-stage <=1e-5 + MAE==Quantile{0.5} anchor)"
affects: [regression-loss-matrix, 06.2-multiquantile]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Exact-leaf parameter threading: the Exact branch destructures params.loss to feed (alpha,delta) into the ALREADY-alpha-general exact_leaf_delta — wiring, not new algorithm (D-6.1-05 free reuse); leaf.rs byte-identical"
    - "MAE re-expressed as the median Quantile: mae_der1/der2 delegate to quantile_der1/der2(.., QUANTILE_ALPHA, QUANTILE_DELTA), keeping the existing MAE Exact oracle byte-stable while generalizing to arbitrary alpha"
    - "Two-param length-1-array kernel launch reused (launch_focal_f64 precedent) for the gradient-only Quantile (alpha+delta arrays, der2=0 constant vec)"

key-files:
  created:
    - "crates/cb-oracle/fixtures/quantile_alpha07/ - Quantile alpha=0.7 Exact-leaf training fixture (config/model.json/staged)"
    - "crates/cb-oracle/fixtures/quantile_alpha05_mae/ - Quantile alpha=0.5 MAE-equivalence anchor fixture"
    - "crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs - Wave-3 quantile per-stage oracle + MAE-equivalence"
    - "crates/cb-train/src/boosting_test.rs - Exact-alpha threading unit tests (falsifiable vs hardcoded 0.5)"
  modified:
    - "crates/cb-compute/src/runtime.rs - Loss::Quantile{alpha,delta} variant + validate range guard (alpha in [0,1], delta>=0)"
    - "crates/cb-compute/src/loss.rs - quantile_der1/quantile_der2; mae_der1/der2 delegate to them"
    - "crates/cb-compute/src/loss_test.rs - quantile_der1 alpha=0.7 + MAE-equivalence + quantile_der2==0 unit tests"
    - "crates/cb-compute/src/lib.rs - quantile_der1/der2 re-exports"
    - "crates/cb-backend/src/kernels.rs - quantile_gradient_kernel<F: Float> (alpha/delta length-1 arrays)"
    - "crates/cb-backend/src/cpu_runtime.rs - launch_quantile_f64 + Loss::Quantile dispatch arm"
    - "crates/cb-backend/src/cpu_runtime_test.rs - quantile host-reference (alpha=0.7) + Quantile{0.5}==MAE dispatch tests"
    - "crates/cb-train/src/boosting.rs - compute_leaf_deltas Exact-alpha threading; autolr_target_type Quantile arm"
    - "crates/cb-train/src/metrics.rs - for_loss Quantile arm (-> RMSE eval surface)"
    - "crates/cb-oracle/generator/gen_fixtures.py - gen_wave3_quantile_losses + --wave3-only entrypoint + MAE==Quantile{0.5} sanity"
    - "crates/cb-train/tests/{autolr_e2e,slice_first_oracle,leaf_methods_oracle}_test.rs - exhaustive match-loss helpers extended for Loss::Quantile (Rule 3)"

key-decisions:
  - "MAE == Quantile{alpha=0.5, delta=1e-6} confirmed at THREE levels: fixture (leaf_values bit-exact 0.0 diff vs leaf_methods/exact), der dispatch (Quantile{0.5} der1/der2 == Mae), and Exact leaf (Quantile{0.5} leaf delta == Mae). mae_der1/der2 now delegate to quantile_der1/der2 so there is a single source of truth."
  - "D-6.1-05 free reuse honored: exact_leaf_delta (leaf.rs) is UNCHANGED (git diff empty). The ONE real-code item was threading params.loss (alpha,delta) into the Exact branch (boosting.rs) — wiring, not algorithm."
  - "Quantile fixtures train on the RAW numeric_tiny target (no positive shift — Quantile admits the full real line), unlike the Wave-2 positive-domain losses; leaf_estimation_method:Exact PINNED so the weighted-alpha-quantile leaf is the thing under test."
  - "Quantile predictions are RAW (no link transform) — the StagedApprox stage gates the raw approx; no Predictions stage / no Exponent."

patterns-established:
  - "Exact-leaf-by-loss now PARAMETERIZED: compute_leaf_deltas threads the active loss's (alpha,delta); future quantile-family losses (e.g. MultiQuantile in 6.2) reuse this seam without touching leaf.rs"
  - "Loss-param generalization keeps the non-parametric anchor byte-stable by delegation (mae_der1 -> quantile_der1), the byte-stability discipline future param generalizations follow"

requirements-completed: [LOSS-03]

# Metrics
duration: 11min
completed: 2026-06-16
---

# Phase 06.1 Plan 03: Quantile-Family Regression Loss Summary

**Quantile{alpha, delta} lands as the alpha-general generalization of MAE — der1/der2 transcribed from TQuantileError, a generics-float CubeCL kernel, and the Exact weighted-alpha-quantile leaf wired by threading the active loss's (alpha, delta) through compute_leaf_deltas (RESEARCH Pattern 3, D-6.1-05 free reuse — leaf.rs untouched) — oracle-locked <=1e-5 per-stage at alpha=0.7 against upstream catboost 1.2.10, with MAE == Quantile{0.5} confirmed bit-exact at fixture, der, and leaf levels. LOSS-03 scalar matrix complete.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-06-16T05:32:00Z
- **Completed:** 2026-06-16T05:43:04Z
- **Tasks:** 3/3
- **Files modified/created:** 21 (10 production source/test, 2 oracle tests, 1 src unit-test file, 6 fixture artifacts, 1 generator, 3 test-helper extensions)

## Accomplishments
- `Loss::Quantile{alpha, delta}` wired through the full pipeline: enum -> der1/der2 -> kernel -> dispatch -> Exact-leaf threading -> per-stage oracle.
- `quantile_der1(approx,target,alpha,delta)` generalizes the MAE deadzone to arbitrary alpha; `quantile_der2 == 0`. `mae_der1`/`mae_der2` now DELEGATE to the quantile fns at the default median (alpha=0.5, delta=1e-6), so MAE has a single source of truth and stays byte-stable.
- `quantile_gradient_kernel<F: Float>` added (alpha/delta as length-1 `Array<F>`, generics-float discipline; if-as-statement deadzone branch — the established CubeCL patterns, NO new kernel patterns, NO CubeCL build errors).
- The ONE genuinely-new-code item (RESEARCH Pattern 3 / D-6.1-05): `compute_leaf_deltas`' Exact branch now threads `params.loss`'s `(alpha, delta)` into `exact_leaf_delta` instead of the unconditional hardcoded `QUANTILE_ALPHA`/`QUANTILE_DELTA`. `exact_leaf_delta` (leaf.rs) is UNCHANGED (git diff empty) — free reuse confirmed.
- `wave3_quantile_loss_oracle_test` GREEN: Quantile{0.7} and Quantile{0.5} Splits/LeafValues/StagedApprox each <=1e-5; Quantile{0.5} additionally matches the existing `leaf_methods/exact` (MAE) fixture <=1e-5 (MAE==Quantile{0.5}).
- MAE == Quantile{0.5} confirmed at THREE independent levels: fixture leaf_values bit-exact (max diff **0.0**), der dispatch (`Quantile{0.5}` der1/der2 == `Mae`), and Exact leaf delta (`Quantile{0.5}` == `Mae`).
- The full existing oracle suite stays green (leaf_methods 4/4 incl. MAE Exact, Wave-1 4/4, Wave-2 3/3, slice_first, autolr, loss, ordered/tensor e2e, eval_metrics) plus cb-train lib 141 (139 + 2 new threading tests). No behavior change to existing losses.

## Task Commits

1. **Task 1: Wave-0 quantile Exact-leaf fixtures + failing oracle (RED)** - `89bd431` (test)
2. **Task 2 (RED): quantile_der1/der2 unit tests** - `a75f296` (test)
2. **Task 2 (GREEN): quantile der + Loss::Quantile + kernel + dispatch** - `e4f7b1d` (feat)
3. **Task 3: thread loss (alpha,delta) into Exact leaf -> Wave-3 oracle GREEN** - `5c5d1e5` (feat)

## Files Created/Modified
- `crates/cb-compute/src/runtime.rs` - `Loss::Quantile{alpha,delta}` (defaults 0.5/1e-6, PartialEq-only — no Eq); `validate` range guard rejects `alpha` outside `[0,1]` or `delta < 0` -> typed `CbError` (T-06.1.03-01).
- `crates/cb-compute/src/loss.rs` - `quantile_der1` (`|val|<delta ? 0 : (val>0 ? alpha : -(1-alpha))`), `quantile_der2 == 0`, cited from `error_functions.h:457-498`/`485-493`; `mae_der1`/`mae_der2` re-expressed to delegate.
- `crates/cb-compute/src/loss_test.rs` - `quantile_der1` at alpha=0.7 (asymmetric +0.7/-0.3/deadzone), `quantile_der1(.,.,0.5,1e-6) == mae_der1` equivalence, `quantile_der2 == 0`.
- `crates/cb-compute/src/lib.rs` - `quantile_der1`/`quantile_der2` re-exports.
- `crates/cb-backend/src/kernels.rs` - `quantile_gradient_kernel<F: Float>` (alpha/delta length-1 `Array<F>`; der2=0 -> constant vec in dispatch).
- `crates/cb-backend/src/cpu_runtime.rs` - `launch_quantile_f64` (two length-1 arrays, gradient-only — the `launch_focal_f64` precedent) + `Loss::Quantile{alpha,delta}` dispatch arm (der2 = constant-0 vec, the Mae precedent).
- `crates/cb-backend/src/cpu_runtime_test.rs` - host-reference Quantile gradient at alpha=0.7; `Quantile{0.5}` der1/der2 == `Mae` at the dispatch level.
- `crates/cb-train/src/boosting.rs` - `compute_leaf_deltas` Exact branch threads `params.loss` (alpha,delta) (Quantile -> its values, Mae/other -> QUANTILE_ALPHA/DELTA == prior); `autolr_target_type` Quantile -> `TargetType::Unknown`.
- `crates/cb-train/src/boosting_test.rs` - Exact branch threads alpha=0.7 (falsifiable vs hardcoded 0.5; corpus separates the 0.7- and 0.5-quantiles by >0.5); `Quantile{0.5}` Exact leaf == `Mae` Exact leaf.
- `crates/cb-train/src/metrics.rs` - `for_loss` Quantile -> RMSE eval surface (mirroring the Mae arm).
- `crates/cb-oracle/generator/gen_fixtures.py` - `gen_wave3_quantile_losses` (quantile_alpha07/quantile_alpha05_mae, leaf_estimation_method:Exact, iterations:1, raw target) + the fixture-level MAE==Quantile{0.5} sanity assertion + `--wave3-only` entrypoint, wired into `main()`.
- `crates/cb-oracle/fixtures/{quantile_alpha07,quantile_alpha05_mae}/` - committed frozen fixtures (RUN-ONCE / D-12; offline from `.venv` catboost 1.2.10).
- `crates/cb-train/tests/wave3_quantile_loss_oracle_test.rs` - the Wave-3 quantile oracle (alpha=0.7 + alpha=0.5 + MAE-equivalence), authored RED, flipped GREEN.
- `crates/cb-train/tests/{autolr_e2e,slice_first_oracle,leaf_methods_oracle}_test.rs` - exhaustive `match loss` helpers extended for `Loss::Quantile{..}` (Rule 3).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Three existing oracle-test `match loss` helpers broke at compile time**
- **Found during:** Task 3 (adding `Loss::Quantile` made the test-side exhaustive `match loss` in `autolr_e2e_test.rs`, `slice_first_oracle_test.rs`, `leaf_methods_oracle_test.rs` non-exhaustive — the SAME pattern as Wave-1/Wave-2 deviations).
- **Issue:** test-side `loss -> target column` / `loss -> TargetType` matches did not cover the new variant.
- **Fix:** Extended each match — `Loss::Quantile{..}` maps to `load_regression_target()` (regression family) and `TargetType::Unknown` (not in the auto-LR table), mirroring the `Mae` arm. No oracle assertion weakened.
- **Files modified:** the three test files above.
- **Commit:** `5c5d1e5`

No architectural changes (Rule 4). No auto-fixed bugs (Rule 1). The only Rule-2 critical functionality (the Quantile alpha/delta range guard, T-06.1.03-01) was implemented as planned, not as a deviation.

## Authentication Gates

None.

## Checkpoint Disposition

Task 1 was authored as `checkpoint:human-verify gate="blocking"` (the oracle-fixture provenance gate). The fixtures were generated OFFLINE from the in-repo `.venv` catboost==1.2.10 (the designated, legitimate source — NOT fabricated, NOT a package install), and the provenance is independently verifiable from committed evidence:
- The generator's MAE==Quantile{0.5} sanity printed `max|leaf_value diff| = 0.000000e+00 <= 1e-5` (bit-exact) at generation time.
- Both fixture `model.json` parse and carry 5 trees; both `config.json` pin `leaf_estimation_method:"Exact"`, `leaf_estimation_iterations:1`, `thread_count:1`, and the D-07 isolating params.
- The oracle test compiled-and-failed RED with NO `#[ignore]` and standard 1e-5 tolerance before Tasks 2-3.

The gate is `gate="blocking"` (NOT the `gate="blocking-human"` package-legitimacy exclusion), and the resume signal is "approved once both fixtures are generated and the test compiles+fails RED" — both conditions were met and machine-verified. No package was installed; no human-only action (2FA / email / secret) was required. The plan executed to completion; this disposition is surfaced for the user's record.

## Verification

- `cargo test -p cb-compute` — 69 passed (incl. quantile_der1 alpha=0.7, quantile_der1==mae_der1 equivalence, quantile_der2==0).
- `cargo test -p cb-backend` — 18 passed (incl. quantile gradient host-reference at alpha=0.7 + Quantile{0.5}==Mae dispatch).
- `cargo test -p cb-train --test wave3_quantile_loss_oracle_test` — 2/2 GREEN (<=1e-5; Quantile{0.7} + Quantile{0.5} per-stage; Quantile{0.5}==MAE), no `#[ignore]`, tolerance unweakened.
- `cargo test -p cb-train --lib` — 141 passed (139 + 2 new boosting threading tests).
- `cargo test -p cb-train --test {leaf_methods,wave1_smooth_loss,wave2_positive_loss,slice_first,autolr_e2e,loss,ordered_boost_e2e,tensor_ctr_e2e,eval_metrics}_oracle_test` — all green (leaf_methods 4/4 incl. MAE Exact byte-stable).
- `git diff HEAD -- crates/cb-compute/src/leaf.rs` — EMPTY (exact_leaf_delta UNCHANGED, free reuse).
- Per disk pressure (Memory), verification was per-crate; no `cargo test --workspace`.

## Acceptance Criteria

- [x] A user can train `loss_function=Quantile:alpha=0.7` and the Exact leaf uses the weighted 0.7-quantile (NOT hardcoded 0.5) — oracle <=1e-5 (SC#1; LOSS-03 scalar matrix complete).
- [x] MAE is expressible as Quantile{0.5,1e-6} and the MAE Exact oracle stays <=1e-5 (MAE == Quantile{0.5}) — confirmed bit-exact (0.0 diff) at fixture, der, and Exact-leaf levels.
- [x] The Exact leaf path threads the active loss's (alpha,delta) instead of the hardcoded constants; `grep QUANTILE_ALPHA boosting.rs` shows it used only as the Mae/non-quantile default.
- [x] boosting_test.rs proves the Exact branch threads alpha=0.7 (falsifiable vs 0.5).
- [x] `exact_leaf_delta` in leaf.rs is UNCHANGED (free reuse, SC#2).
- [x] Loss enum stays PartialEq-only; range guard rejects alpha outside [0,1] / delta<0 -> typed CbError; no `unwrap()` in new production code.
- [x] Tests live in `*_test.rs` files (source/test separation).
- [x] All prior oracles (~40 existing + Wave-1 + Wave-2) stay green; no behavior change to existing losses (SC#3).

## Threat Surface Scan

No new security-relevant surface beyond the plan's `<threat_model>`. The two planned `mitigate` dispositions were implemented:
- T-06.1.03-01 (Quantile alpha/delta range): `Loss::validate` rejects `alpha` outside `[0,1]` or `delta < 0` -> typed `CbError`, never NaN/panic.
- T-06.1.03-02 (silent parity bug — Exact threads the wrong alpha): `boosting_test.rs` asserts alpha=0.7 is threaded (falsifiable); the alpha=0.7 oracle <=1e-5 fails if the leaf uses 0.5; the alpha=0.5 anchor proves MAE byte-stability.
- T-06.1.03-03 (NaN leaf): exact_leaf_delta already guards degenerate/empty leaves (leaf.rs unchanged); compare_stage NaN-rejects (accept).
- T-06.1.03-SC (package installs): N/A — no new external packages; internal code + pinned `.venv` catboost==1.2.10 generator.

## Self-Check: PASSED

- All created files exist on disk (quantile_alpha07/model.json, quantile_alpha05_mae/model.json, wave3_quantile_loss_oracle_test.rs, boosting_test.rs, 06.1-03-SUMMARY.md).
- All four task commits found in git history (`89bd431`, `a75f296`, `e4f7b1d`, `5c5d1e5`).
- `pub fn quantile_der1` confirmed in `cb-compute/src/loss.rs`; `Loss::Quantile` confirmed threaded in `cb-train/src/boosting.rs`.
