---
phase: 11-depth-1-partition-aware-histograms-reduction-determinism-new
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - crates/cb-backend/src/gpu_runtime/mod.rs
  - crates/cb-backend/src/kernels/grow_loop.rs
autonomous: true
requirements: [GPUT-05, GPUT-06]
must_haves:
  truths:
    - "A depth-6 RMSE oblivious tree grows fully on device — the depth>1 reject is removed from both grow_oblivious_tree_into and grow_boosting_pass_into"
    - "Per level, the score step uses the partition-aware (fullPass=false) fill keyed by the resident leaf_of + subtraction trick, scoring per active leaf"
    - "The device leaf_of over a depth-6 split sequence equals the CPU leaf_index; leaf values match the CPU reference to <=1e-4"
    - "Only the O(1) BestSplit descriptor + 2^depth part-stats cross host<->device per level (D-05 boundary preserved)"
    - "The depth>1 histogram accumulator shows zero run-to-run spread over >=32 launches (GPUT-06 determinism verified)"
  artifacts:
    - path: "crates/cb-backend/src/gpu_runtime/mod.rs"
      provides: "depth>1 grow loop: per-level partition-aware score step, both depth>1 rejects removed"
    - path: "crates/cb-backend/src/kernels/grow_loop.rs"
      provides: "depth-6 grow self-oracle + reduction-determinism (zero-spread) self-oracle"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime/mod.rs"
      to: "crates/cb-backend/src/kernels.rs"
      via: "grow_oblivious_tree_into per-level score calls launch_partition_hist2_into + launch_subtract_histograms_into instead of the whole-dataset fill"
      pattern: "launch_partition_hist2_into"
    - from: "crates/cb-backend/src/kernels/grow_loop.rs"
      to: "crates/cb-compute/src/histogram.rs"
      via: "depth-6 self-oracle asserts device leaf_of == CPU leaf_index and leaf values match the CPU reference"
      pattern: "leaf_index|reduce_leaf_stats"
---

<objective>
Thread the Plan-02 partition-aware histogram + subtraction trick into the device grow loop so a real depth-6 RMSE oblivious tree grows fully on device (GPUT-05), and verify the deterministic reduction holds with zero run-to-run spread (GPUT-06). Remove BOTH depth>1 rejects (grow_oblivious_tree_into mod.rs:1740 AND grow_boosting_pass_into mod.rs:2009); the extension is surgical — the per-level loop already exists, only the score step changes.

Purpose: This flips depth>1 from `Ok(None)` (Phase 10 CPU fallback) to device-covered (all-or-nothing per fit, D-10-01), preserving the D-05 boundary (only the O(1) BestSplit + 2^depth stats cross host↔device). The zero-spread self-oracle evidences SC-3 "no split flips compounding over the boosting run".
Output: the depth>1-capable grow loop in mod.rs, a depth-6 grow self-oracle, and a reduction-determinism self-oracle in grow_loop.rs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-RESEARCH.md
@.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-PATTERNS.md
@.planning/phases/10-gpu-foundations-runtime-seam-session-residency-device-primit/SPIKE-REDUCTION.md
</context>

<artifacts_produced>
NEW symbols introduced by this plan (drift-verification exclude list):
- `crates/cb-backend/src/gpu_runtime/mod.rs`: the per-level partition-aware score helper inside `grow_oblivious_tree_into` (e.g. `score_level_partition_aware(...)` if extracted), removal of the two depth>1 rejects (lines 1740, 2009), extended `checked_shl`/`checked_mul` guards for `2^level` slot sizing.
- `crates/cb-backend/src/kernels/grow_loop.rs`: `#[test] fn depth6_rmse_grow_matches_cpu` (depth-6 grow self-oracle), `#[test] fn depth_gt_one_is_device_covered` (INVERTS the two former `depth_gt_one_is_tracked_forward_dependency` tests at lines 653 and 1153), `#[test] fn partition_hist_reduce_zero_spread` (GPUT-06 zero-run-to-run-spread self-oracle over ≥32 launches).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Remove the depth>1 rejects; wire the per-level partition-aware score step</name>
  <files>crates/cb-backend/src/gpu_runtime/mod.rs</files>
  <read_first>
    - crates/cb-backend/src/gpu_runtime/mod.rs — `grow_oblivious_tree_into` (line 1711): the depth>1 reject to REMOVE at line 1740 (`if depth > 1 { return Err(CbError::OutOfRange(...)) }`), the per-level loop at line 1785 (`for level in 0..depth`), the whole-dataset score step at ~line 1792 (`launch_find_optimal_split_pointwise_into` — the ONLY line that must change), `launch_partition_split_into` at line 1439 (forward-bit `leaf_of |= (bit << level)` — UNCHANGED), `read_part_stats_f64` at line 1391, and the existing `checked_shl` (2^depth, ~line 1753) / `checked_mul` (n_features*n, ~line 1759) guards. Also the SECOND reject in `grow_boosting_pass_into` at line 2009 (per-tree path, loop at 2025) — remove it too.
    - crates/cb-backend/src/kernels.rs — `launch_partition_hist2_into` + `launch_subtract_histograms_into` (added in Plan 02) — the calls to thread into the score step.
    - 11-PATTERNS.md Pattern 4 (per-level threading) and the D-05 boundary contract (only O(1) BestSplit + 2^depth stats cross; NEVER read the full histogram/partition buffer to host — the FORBIDDEN hybrid).
    - CATBOOST_CUDA_KERNELS_DESIGN.md §6.3/§6.4 (partition-aware scoring per active leaf; left = hist[leaf, binFeature], right = partStats[leaf] − left).
  </read_first>
  <action>Remove the depth>1 reject at mod.rs:1740 and the mirror reject at mod.rs:2009 (grow_boosting_pass_into). In the per-level loop of `grow_oblivious_tree_into`, replace the whole-dataset score step (~line 1792) with a partition-aware score: at level L, run `launch_partition_hist2_into` keyed by the resident `leaf_of` to fill the `2^L` leaf slots (compute the smaller sibling directly per pair), apply `launch_subtract_histograms_into` to derive the larger siblings from the parent-resident histogram, then score every (feature, bin) per active leaf (`left = hist[leaf, binFeature]`, `right = partStats[leaf] − left`) and argmin the gain across leaves to pick the single oblivious split for that level — matching the CPU argmin tie-break exactly (lowest index / first-wins). Keep `launch_partition_split_into` (forward-bit `leaf_of |= (bit << level)`) and the read-back UNCHANGED. Preserve the D-05 boundary: only the O(1) BestSplit descriptor per level and the final `2^depth` part-stats (`read_part_stats_f64`) may cross host↔device — never read the full histogram/partition buffer to host. Extend the `checked_shl`/`checked_mul` guards to cover `2^level` slot sizing (and, forward-compatibly, the +der2 channel that Plan 04 adds). Use a finite `f32::MIN` sentinel for any "no candidate" score inside a `#[cube]` (host-side score aggregation may keep `f64::NEG_INFINITY`). Update the doc-comments at mod.rs:1659-1669 / 1961-2012 that describe depth>1 as a forward dependency to reflect that it is now covered.</action>
  <verify>
    <automated>cargo build -p cb-backend --no-default-features --features rocm 2>&1 | tail -5; grep -n "depth > 1" crates/cb-backend/src/gpu_runtime/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - The `if depth > 1 { return Err(...) }` rejects at mod.rs:1740 AND mod.rs:2009 are gone (grep for `depth > 1` shows no returning reject in either grow fn).
    - The per-level score step calls `launch_partition_hist2_into` + `launch_subtract_histograms_into` (grep confirms) instead of the whole-dataset `launch_find_optimal_split_pointwise_into`.
    - `launch_partition_split_into` and `read_part_stats_f64` calls are unchanged; no full histogram/partition buffer is read to host (D-05 preserved).
    - `checked_shl`/`checked_mul` guards cover `2^level` slot sizing.
    - `cargo build -p cb-backend --no-default-features --features rocm` compiles.
  </acceptance_criteria>
  <done>Depth>1 oblivious trees grow on device via the per-level partition-aware score step; both depth>1 rejects are removed and the D-05 boundary is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Depth-6 RMSE grow self-oracle (structure + value)</name>
  <files>crates/cb-backend/src/kernels/grow_loop.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/grow_loop.rs — `matches_cpu_greedy_search` (line 475: grows the device tree, asserts per-object `leaf_of == cpu_leaf_index`, reports leaf-value divergence), `leaf_of_matches_cpu_leaf_index` (line 288: extend the split sequence to depth-6), `cpu_boosting_pass` (line 733: serial multi-tree reference — forward-bit leaf_of, per-leaf calc_average over ordered sum_f64), `max_divergence` (line 60), and the two `depth_gt_one_is_tracked_forward_dependency` tests at lines 653 and 1153 (these assert the OLD reject — they MUST be inverted/removed now that depth>1 is covered).
    - crates/cb-compute/src/histogram.rs `reduce_leaf_stats` (line 49) and crates/cb-compute/src/leaf.rs `calc_average` (line 83) — the RMSE leaf reference.
    - bench/fixtures/expected_depth6_tree.json (Plan 01) — optionally cross-check the device tree against the pinned fixture's RMSE arm.
    - CLAUDE.md — source/test separation; follow grow_loop.rs's existing self-oracle convention.
  </read_first>
  <action>Add `depth6_rmse_grow_matches_cpu` to grow_loop.rs, extending `matches_cpu_greedy_search` to depth 6: grow a depth-6 RMSE oblivious tree on device from a deterministic dataset, hard-assert (STRUCTURE) that the device per-object `leaf_of` equals the CPU `leaf_index` over the full depth-6 split sequence (reuse/extend `leaf_of_matches_cpu_leaf_index` for the 2^level slot addressing, Pitfall 6), and report (VALUE) that the device leaf values match the CPU `calc_average` reference (and, optionally, the Plan-01 fixture's RMSE arm) to ≤1e-4 via `max_divergence`. Invert the two `depth_gt_one_is_tracked_forward_dependency` tests (lines 653, 1153) into `depth_gt_one_is_device_covered` (assert depth>1 now grows without the typed reject) OR delete them and rely on the new depth-6 oracle — do not leave a test asserting the removed reject. Run under the rocm feature.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm depth6_rmse_grow_matches_cpu; cargo test -p cb-backend --no-default-features --features rocm leaf_of_matches_cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --features rocm depth6_rmse_grow_matches_cpu` passes on gfx1100.
    - The test hard-asserts device `leaf_of == cpu_leaf_index` over a depth-6 split sequence (structure) and reports leaf-value divergence ≤1e-4 (value).
    - No test asserting the removed depth>1 reject remains (the two `depth_gt_one_is_tracked_forward_dependency` tests are inverted or deleted).
    - `cargo test -p cb-backend --features rocm leaf_of_matches_cpu` still passes (extended to depth-6).
  </acceptance_criteria>
  <done>A full depth-6 RMSE tree grows on device with structure identical to the CPU path and leaf values within 1e-4, oracle-tested on gfx1100.</done>
</task>

<task type="auto">
  <name>Task 3: Reduction-determinism self-oracle (zero run-to-run spread, GPUT-06)</name>
  <files>crates/cb-backend/src/kernels/grow_loop.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/reduce.rs — the existing SPIKE-REDUCTION harness (`run_fixedpoint_reduce` line 731, `REDUCE_FIXEDPOINT_SCALE_F64`, `AtomicFinalizePath`) and the existing `reduce` zero-spread test the phase reuses as a pattern.
    - .planning/phases/10-.../SPIKE-REDUCTION.md §5b — the LOCKED accumulator: zero run-to-run spread measured on gfx1100; the fixed-order tree-reduce capability fallback reports the downgrade, never silently switches.
    - crates/cb-backend/src/kernels/grow_loop.rs `max_divergence` (line 60) and the Plan-02 `partition_hist2_nonbinary_kernel` accumulate path (the NEW accumulator whose determinism this test proves).
    - 11-RESEARCH.md Pitfall 4 (compounding split-flip drift) — this test is the per-run evidence that the depth>1 histogram accumulator is order-independent.
  </read_first>
  <action>Add `partition_hist_reduce_zero_spread` to grow_loop.rs: launch the depth>1 partition-aware histogram accumulate (Plan 02) ≥32 times on the SAME input on gfx1100 and assert the decoded histogram is bit-identical across all launches (zero run-to-run spread) — the deterministic fixed-point `Atomic<u64>` accumulator must produce order-independent results regardless of atomic contention ordering. Assert the finalize path reported is the fixed-point `Atomic<u64>` path (not a silent fallback), and that if a capability downgrade occurs it is surfaced via `AtomicFinalizePath` (explicit reporting, no silent switch). This is the in-env GPUT-06 evidence; the across-hundreds-of-trees compounding check is discharged by the Kaggle per-tree spread diagnostic in Plan 05.</action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm partition_hist_reduce_zero_spread; cargo test -p cb-backend --no-default-features --features rocm reduce</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-backend --features rocm partition_hist_reduce_zero_spread` passes: ≥32 launches of the depth>1 accumulator produce bit-identical histograms (zero spread).
    - The test asserts the fixed-point `Atomic<u64>` finalize path is in use (or an explicit `AtomicFinalizePath` downgrade is reported — never silent).
    - The existing `reduce` determinism harness still passes.
  </acceptance_criteria>
  <done>The depth>1 histogram accumulator is proven order-independent (zero run-to-run spread) on gfx1100, satisfying the in-env portion of GPUT-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host (grow driver) → device (per-level dispatch) | Per-level leaf counts / slot sizing / buffer lengths cross into device launches; the only attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering / DoS | Out-of-bounds device write from `2^level` slot mis-sizing across levels | mitigate | Task 1 extends `checked_shl`/`checked_mul` guards to cover per-level `2^level` slot sizing before every launch → typed `CbError::OutOfRange`. |
| T-11-03-02 | Information Disclosure / correctness | Reading the full histogram/partition buffer to host (FORBIDDEN D-05 hybrid) | mitigate | Task 1 preserves the D-05 boundary — only the O(1) BestSplit + `2^depth` stats cross; the self-oracle asserts no full-buffer read path is introduced. |
| T-11-03-03 | Tampering (silent wrong result) | Non-deterministic reduce corrupting parity across the run | mitigate | Task 3 proves zero run-to-run spread + explicit `AtomicFinalizePath` reporting (no silent downgrade). |
| T-11-SC | Tampering | package installs (npm/pip/cargo) | accept | No packages added or removed; extends first-party grow loop only. |
</threat_model>

<verification>
- Both depth>1 rejects removed (mod.rs:1740, 2009); grep confirms no returning `depth > 1` reject.
- `cargo test -p cb-backend --features rocm depth6_rmse_grow_matches_cpu` green (structure identical, value ≤1e-4).
- `cargo test -p cb-backend --features rocm partition_hist_reduce_zero_spread` green (zero spread over ≥32 launches).
- `cargo test -p cb-backend --no-default-features --features rocm` full in-env smoke green (per-wave merge gate).
</verification>

<success_criteria>
A depth-6 RMSE tree grows fully on device matching the CPU path ≤1e-4 in-env (GPUT-05), the D-05 boundary is preserved, and the deterministic reduction is proven zero-spread (GPUT-06). Kaggle CUDA remains the authoritative gate (Plan 05).
</success_criteria>

<output>
Create `.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-03-SUMMARY.md` when done.
</output>
