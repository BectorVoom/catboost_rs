---
phase: 11-depth-1-partition-aware-histograms-reduction-determinism-new
plan: 05
type: execute
wave: 5
depends_on: ["11-03", "11-04"]
files_modified:
  - bench/cuda_oracle.ipynb
  - bench/RESULTS.md
autonomous: false
requirements: [GPUT-14, GPUT-06, BENCH-02]
must_haves:
  truths:
    - "The Kaggle notebook gates on final-prediction ε=1e-4 over the full depth-6 run for BOTH RMSE and Logloss vs the Rust CPU path (blocking)"
    - "A per-tree split-agreement + run-to-run spread diagnostic pinpoints the first divergent tree, evidencing no split flips compounding (D-05)"
    - "Depth-6 RMSE + Logloss device training is timed on Kaggle CUDA: device vs host-CPU baseline AND vs official CatBoost GPU (warm-run/JIT-excluded, train-only) — BENCH-02"
    - "The CPU/host training paths remain byte-unchanged (D-04 no-regression)"
  artifacts:
    - path: "bench/cuda_oracle.ipynb"
      provides: "final-ε=1e-4 gate + per-tree diagnostic + depth-6 speed cells"
    - path: "bench/RESULTS.md"
      provides: "dated Phase-11 sign-off block (correctness gate + speed numbers)"
  key_links:
    - from: "bench/cuda_oracle.ipynb"
      to: "bench/fixtures/expected_depth6_tree.json"
      via: "the notebook loads the depth-6 fixture + speed workload from Plan 01 and runs the device path vs the Rust CPU path"
      pattern: "expected_depth6_tree"
    - from: "bench/RESULTS.md"
      to: "bench/cuda_oracle.ipynb"
      via: "RESULTS.md records the notebook's correctness gate result + speed numbers (no fabricated numbers; correctness blocks before speed)"
      pattern: "Phase 11|depth-6"
---

<objective>
Extend the Kaggle CUDA harness to close the phase on the authoritative GPU oracle: a blocking final-prediction ε=1e-4 gate over the full depth-6 run (RMSE + Logloss) vs the Rust CPU path (GPUT-14), a per-tree split-agreement + run-to-run spread diagnostic that pinpoints the first divergent tree (GPUT-06 / D-05, evidencing SC-3 "no split flips compounding"), and depth-6 device-vs-host-CPU-vs-official-CatBoost-GPU speed cells (BENCH-02). Kaggle CUDA is the sole authoritative GPU oracle for correctness AND speed — the notebook run is human-gated.

Purpose: The in-env ROCm smoke (Plans 02–04) is a fast local check, not the gate. This plan delivers the deliverable that the phase is actually verified against: the CUDA notebook. Correctness is BLOCKING before any speed number is recorded (no fabricated numbers).
Output: the extended `cuda_oracle.ipynb`, a dated Phase-11 sign-off block in `RESULTS.md`, and a human-gated Kaggle run confirming the gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-RESEARCH.md
@.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-VALIDATION.md
@bench/RESULTS.md
</context>

<artifacts_produced>
NEW symbols introduced by this plan (drift-verification exclude list):
- `bench/cuda_oracle.ipynb`: new depth-6 cells — the final-prediction ε=1e-4 gate cell (RMSE + Logloss), the per-tree split-agreement + run-to-run-spread diagnostic cell, the depth-6 speed cell (device vs host-CPU vs official CatBoost GPU, warm-run/JIT-excluded).
- `bench/RESULTS.md`: a dated `## Phase 11` sign-off block (correctness gate result + speed numbers).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend cuda_oracle.ipynb — final-ε gate + per-tree diagnostic</name>
  <files>bench/cuda_oracle.ipynb</files>
  <read_first>
    - bench/cuda_oracle.ipynb — the existing depth-1 oracle cells (device-vs-Rust-CPU prediction comparison structure) to mirror at depth-6.
    - bench/fixtures/expected_depth6_tree.json + the depth-6 speed workload (Plan 01) — the inputs the notebook loads.
    - 11-VALIDATION.md Manual-Only Verifications table (the three human-gated CUDA behaviors) + 11-RESEARCH.md D-05 (final-ε gate + per-tree diagnostic) and Pitfall 4 (compounding split-flip drift — the diagnostic must show a step-change at the originating tree, not just the final aggregate).
    - .planning/phases/11-.../11-CONTEXT.md D-04 (CPU/host paths byte-unchanged) and D-05.
  </read_first>
  <action>Add depth-6 cells to `bench/cuda_oracle.ipynb`: (a) a BLOCKING final-prediction ε=1e-4 gate that trains a full depth-6 run on device (CUDA) and on the Rust CPU path for BOTH RMSE and Logloss, and asserts `max abs(device_prediction − cpu_prediction) ≤ 1e-4` over the workload; (b) a per-tree split-agreement + run-to-run spread diagnostic that, for each tree in the run, records whether the device split matches the CPU split and the run-to-run spread of the device reduction, so a compounding drift is localized to the tree where it starts (evidences SC-3). Load the depth-6 fixture + speed workload from Plan 01; do NOT retrain the CPU reference with a changed config — the CPU/host path stays byte-unchanged (D-04). Mark the cells clearly as Kaggle-CUDA-authoritative (human-gated). Keep the epsilon bar at 1e-4 for the device path (the operative standing GPUT-14 bar); the CPU path stays the ≤1e-5 reference.</action>
  <verify>
    <automated>python -c "import json,nbformat; nb=nbformat.read('bench/cuda_oracle.ipynb', as_version=4); src='\n'.join(c.source for c in nb.cells); assert 'depth6' in src.replace('_','').replace('-','').lower() or 'depth 6' in src.lower(); assert '1e-4' in src or '1e-04' in src"</automated>
  </verify>
  <acceptance_criteria>
    - `bench/cuda_oracle.ipynb` contains a depth-6 final-prediction ε=1e-4 gate cell for BOTH RMSE and Logloss (device vs Rust CPU).
    - A per-tree split-agreement + run-to-run spread diagnostic cell exists that localizes drift to the originating tree (D-05).
    - The notebook loads the Plan-01 depth-6 fixture/workload and does not alter the byte-unchanged CPU reference config (D-04).
    - The device bar is 1e-4; the CPU reference stays ≤1e-5.
  </acceptance_criteria>
  <done>The Kaggle notebook has a blocking depth-6 ε=1e-4 correctness gate + a per-tree diagnostic ready for the human-gated run.</done>
</task>

<task type="auto">
  <name>Task 2: BENCH-02 depth-6 speed cells + RESULTS.md sign-off block</name>
  <files>bench/cuda_oracle.ipynb, bench/RESULTS.md</files>
  <read_first>
    - bench/RESULTS.md — the existing dated sign-off log structure (correctness is a BLOCKING gate before any speed number; no fabricated numbers). Append a new dated Phase-11 block in the same format.
    - bench/cuda_oracle.ipynb — the existing speed-cell structure (warm-run/JIT-excluded, train-only) to mirror for depth-6.
    - 11-RESEARCH.md BENCH-02 row + 11-CONTEXT.md (BENCH-02 standing per-phase speed check: device vs host-CPU baseline AND vs official CatBoost GPU; real named datasets deferred to Phase 14).
  </read_first>
  <action>Add depth-6 speed cells to `bench/cuda_oracle.ipynb` measuring wall-clock train time for the depth-6 RMSE and Logloss runs on: (1) the device path, (2) the host-CPU baseline, and (3) official CatBoost GPU (where a comparable config exists) — warm-run, JIT-excluded, train-only (per BENCH-02). Append a dated `## Phase 11` block to `bench/RESULTS.md` with placeholders/TODO markers for the numbers that ONLY get filled in from the actual Kaggle run (leave them explicitly marked as pending until the human-gated run in Task 3 — do NOT fabricate numbers). Record the correctness-gate result field as blocking-before-speed.</action>
  <verify>
    <automated>grep -niE "phase 11|depth-6|depth6" bench/RESULTS.md | head</automated>
  </verify>
  <acceptance_criteria>
    - `bench/cuda_oracle.ipynb` has depth-6 speed cells (device vs host-CPU vs official CatBoost GPU, warm-run/JIT-excluded, train-only).
    - `bench/RESULTS.md` has a dated `## Phase 11` block with number fields explicitly marked pending until the Kaggle run (no fabricated numbers), and correctness recorded as blocking-before-speed.
  </acceptance_criteria>
  <done>The speed cells and the RESULTS.md sign-off skeleton are ready; numbers remain pending the human-gated Kaggle run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Run the Kaggle CUDA notebook — confirm ε=1e-4 gate + log speed</name>
  <files>bench/cuda_oracle.ipynb, bench/RESULTS.md</files>
  <action>Human-gated Kaggle CUDA run (no code Claude can execute in-env — CUDA is unavailable locally). The human runs the extended notebook on a Kaggle CUDA GPU, confirms the blocking depth-6 ε=1e-4 correctness gate for RMSE + Logloss, checks the per-tree diagnostic shows no compounding split-flip drift, records the depth-6 speed numbers, and fills the pending fields in the RESULTS.md Phase-11 block. See how-to-verify for the exact steps.</action>
  <what-built>The extended `bench/cuda_oracle.ipynb` (depth-6 final-ε=1e-4 gate for RMSE + Logloss, per-tree split-agreement + run-to-run spread diagnostic, depth-6 speed cells) and the dated `## Phase 11` sign-off skeleton in `bench/RESULTS.md`. The device path (Plans 02–04) is oracle-passed in-env on gfx1100 ROCm smoke, but Kaggle CUDA is the sole authoritative GPU oracle for both correctness and speed.</what-built>
  <how-to-verify>
    1. Open `bench/cuda_oracle.ipynb` on Kaggle with a CUDA GPU runtime.
    2. Run all cells. Confirm the final-prediction ε=1e-4 gate PASSES for BOTH RMSE and Logloss depth-6 runs (max abs(device − CPU) prediction ≤ 1e-4). This is BLOCKING — if it fails, the phase is not done; use the per-tree diagnostic cell to locate the first divergent tree and report back.
    3. Confirm the per-tree split-agreement + run-to-run spread diagnostic shows no split flips compounding across the run (no step-change in spread at any tree).
    4. Run the depth-6 speed cells (warm-run/JIT-excluded, train-only) and record device vs host-CPU baseline vs official CatBoost GPU wall-clock.
    5. Fill the pending number fields in the `## Phase 11` block of `bench/RESULTS.md` with the actual Kaggle numbers (correctness result first, then speed).
  </how-to-verify>
  <acceptance_criteria>
    - Kaggle CUDA final-prediction ε=1e-4 gate PASSES for depth-6 RMSE AND Logloss (blocking).
    - The per-tree diagnostic confirms no compounding split-flip drift across the run.
    - Depth-6 speed numbers (device / host-CPU / official CatBoost GPU) are recorded in `bench/RESULTS.md` (no fabricated numbers).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the Kaggle ε result + speed numbers, or describe the divergence (and the first divergent tree from the diagnostic) if the gate fails.</resume-signal>
  <verify><human-check>Kaggle CUDA final-prediction ε=1e-4 gate PASSES for depth-6 RMSE and Logloss; per-tree diagnostic shows no compounding drift; speed numbers logged in bench/RESULTS.md.</human-check></verify>
  <done>The human-gated Kaggle CUDA run confirms the ε=1e-4 correctness gate (blocking) and the depth-6 speed numbers are recorded in bench/RESULTS.md (no fabricated numbers).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline notebook / fixture → recorded sign-off | The harness is an offline benchmark run against checked-in fixtures; no runtime/external-input attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Repudiation / Tampering | Fabricated or unverified speed/correctness numbers in RESULTS.md | mitigate | Numbers are filled ONLY from the human-gated Kaggle run (Task 3); correctness is blocking-before-speed; pending fields are explicitly marked until the run. |
| T-11-05-02 | Tampering (silent wrong result) | Non-deterministic reduce causing a passing-local / failing-aggregate ε | mitigate | The per-tree spread diagnostic localizes any compounding drift to the originating tree (D-05); the LOCKED deterministic reduce (Plans 02/03) removes run-to-run order sensitivity. |
| T-11-SC | Tampering | package installs (npm/pip/cargo) | accept | No packages added or removed; the Kaggle notebook uses official CatBoost GPU + the existing harness only. |
</threat_model>

<verification>
- `bench/cuda_oracle.ipynb` has a depth-6 final-ε=1e-4 gate (RMSE + Logloss), a per-tree split-agreement/spread diagnostic, and depth-6 speed cells.
- `bench/RESULTS.md` has a dated Phase-11 sign-off block (numbers pending until the Kaggle run).
- Human-gated Kaggle run confirms the ε=1e-4 gate PASSES (blocking) and speed is logged (BENCH-02).
</verification>

<success_criteria>
GPUT-14 (every device-covered case ≤1e-4 vs the Rust CPU path on Kaggle CUDA; CPU paths byte-unchanged), GPUT-06 (no split flips compounding, per-tree diagnostic), and BENCH-02 (recorded depth-6 CUDA speed) are all discharged on the authoritative Kaggle CUDA oracle.
</success_criteria>

<output>
Create `.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-05-SUMMARY.md` when done.
</output>
