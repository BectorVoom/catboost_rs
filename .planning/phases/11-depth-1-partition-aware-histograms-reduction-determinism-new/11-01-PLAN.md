---
phase: 11-depth-1-partition-aware-histograms-reduction-determinism-new
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/generator.py
  - bench/fixtures/
  - crates/cb-compute/tests/depth6_reference_test.rs
autonomous: true
requirements: [GPUT-05, GPUT-07]
must_haves:
  truths:
    - "A depth-6 RMSE fixture and a depth-6 Logloss fixture exist, produced by the seeded synthetic generator from one seed (D-03)"
    - "The Logloss fixture leaf values come from newton_leaf_delta with leaf_estimation_iterations pinned to 1 (A1/D-02)"
    - "A CPU-side test proves the emitted fixtures match cb-compute's reduce_leaf_stats / reduce_leaf_der2 / newton_leaf_delta to <=1e-5"
    - "The fixture also emits a large-n depth-6 speed workload (same generator, D-03) for later BENCH-02 use"
  artifacts:
    - path: "bench/generator.py"
      provides: "serial_depth6_tree recursive per-level partition reference + depth-6 write_fixtures extension"
      contains: "serial_depth6_tree"
    - path: "bench/fixtures/expected_depth6_tree.json"
      provides: "depth-6 RMSE + Logloss correctness fixture (structure + per-leaf values + config: iterations=1, score_function pinned)"
    - path: "crates/cb-compute/tests/depth6_reference_test.rs"
      provides: "CPU oracle cross-check of the emitted depth-6 fixture against reduce_leaf_stats/reduce_leaf_der2/newton_leaf_delta"
      exports: ["#[test]"]
  key_links:
    - from: "bench/generator.py"
      to: "crates/cb-compute/src/leaf.rs"
      via: "serial_depth6_tree transcribes the newton_leaf_delta / calc_average / scale_l2_reg formulas (Logloss arm uses newton)"
      pattern: "newton_leaf_delta|calc_average"
    - from: "crates/cb-compute/tests/depth6_reference_test.rs"
      to: "crates/cb-compute/src/histogram.rs"
      via: "test reduces the fixture with reduce_leaf_stats + reduce_leaf_der2 and asserts <=1e-5"
      pattern: "reduce_leaf_(stats|der2)"
---

<objective>
Extend the Phase-10 seeded synthetic generator to depth-6 RMSE + Logloss configs (D-03) and prove the emitted fixture is bit-consistent with the cb-compute CPU oracle. This is the Wave-0 test foundation every downstream device self-oracle (Plans 02–04) and the Kaggle harness (Plan 05) consume. No GPU work.

Purpose: Pin the two MEDIUM-confidence unknowns as concrete fixture facts BEFORE any kernel is written — (A1) leaf_estimation_iterations=1, (A2) histogram channel-0 semantics / score function — so the device path has an unambiguous ≤1e-4 target. RMSE's Newton step collapses to calc_average; Logloss is the genuinely-new newton_leaf_delta path.
Output: `bench/fixtures/expected_depth6_tree.json` (+ sha-manifested `.npy`), a large-n depth-6 speed workload, and a passing cb-compute cross-check test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-RESEARCH.md
@.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-PATTERNS.md
</context>

<artifacts_produced>
NEW symbols introduced by this plan (drift-verification exclude list):
- `bench/generator.py`: `serial_depth6_tree(...)` (recursive per-level Cosine-score best-split reference, RMSE calc_average leaves + Logloss newton_leaf_delta leaves), depth-6 branch in `write_fixtures`, `DEPTH6 = 6` / `LEAF_ESTIMATION_ITERATIONS = 1` config constants, `expected_depth6_tree.json` fixture + `X_depth6_speed.npy` (large-n speed workload) fixture files + sha entries in the manifest.
- `crates/cb-compute/tests/depth6_reference_test.rs`: `#[test] fn depth6_fixture_matches_cpu_oracle` (+ any helpers).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the synthetic generator to depth-6 RMSE + Logloss fixtures (D-03)</name>
  <files>bench/generator.py, bench/fixtures/</files>
  <read_first>
    - bench/generator.py — `serial_depth1_tree` (line 230: Cosine-score best split + calc_average leaves; RMSE der1 = y−approx, Logloss der1 = y−sigmoid) and `write_fixtures` (line 313; emits `expected_depth1_tree.json` + sha-manifested `.npy`, depth1 config at line 371). This is the exact analog to extend.
    - crates/cb-compute/src/leaf.rs — `newton_leaf_delta` (line 145: `sum_der / (-sum_der2 + scaled_l2)`), `calc_average` (line 83), `scale_l2_reg` (line 99). Transcribe the Logloss-arm Newton leaf into Python.
    - crates/cb-compute/src/histogram.rs — `reduce_leaf_der2` (line 100: consumes `der2·weight`, count-order Σ per leaf). The Python reduction must fold weight identically.
    - .planning/phases/11-.../11-CONTEXT.md — D-02 (pin iteration count in fixture), D-03 (reuse the Phase-10 generator). 11-RESEARCH.md Assumptions A1 (iterations=1) and A2 (channel-0 semantics / score function must be pinned, not guessed).
  </read_first>
  <action>Add `serial_depth6_tree(X, y, weights, loss, borders, ...)` to bench/generator.py, mirroring `serial_depth1_tree` but recursing per level for depth 6: at each level score every (feature, bin) candidate over the CURRENT 2^level partitions with the same Cosine score function `serial_depth1_tree` already uses, pick the single best oblivious split (same across all leaves at that level), route objects forward-bit into 2^(level+1) partitions, and at the final level compute per-leaf values. RMSE leaves use the existing calc_average path; Logloss leaves use a transcribed `newton_leaf_delta` = Σder1 / (−Σ(der2·weight) + scaled_l2) with der2 = −p(1−p), p = sigmoid(approx), and leaf_estimation_iterations pinned to 1 (single closed-form step — NO refinement loop, NO backtracking; this matches the CPU oracle per A1). Extend `write_fixtures` to emit `expected_depth6_tree.json` containing BOTH `rmse` and `logloss` sub-trees (full split sequence + per-leaf values + a `config` block recording `leaf_estimation_iterations: 1`, the pinned `score_function`, `l2_leaf_reg`, `learning_rate`, seed, and the channel-0 semantics assumption A2). Also emit a separate large-n depth-6 speed workload array (e.g. `X_depth6_speed.npy`) from the same seed for later BENCH-02 use — correctness fixture and speed workload from one generator (D-03). Add every new file to the sha manifest exactly as the depth-1 fixtures are manifested. Do NOT add real named datasets (Higgs/Epsilon) — deferred to Phase 14 per D-03.</action>
  <behavior>
    - For the RMSE arm at depth 6, each leaf value equals calc_average(Σder1, Σweight, scaled_l2) — because der2 = −1 collapses Newton to the average.
    - For the Logloss arm, each leaf value equals newton_leaf_delta(Σder1, Σ(der2·weight), scaled_l2) with a single step.
    - The emitted JSON `config.leaf_estimation_iterations` is exactly 1.
  </behavior>
  <verify>
    <automated>cd bench && python generator.py && python -c "import json; d=json.load(open('fixtures/expected_depth6_tree.json')); assert d['config']['leaf_estimation_iterations']==1; assert 'rmse' in d and 'logloss' in d; assert len(d['rmse']['splits'])==6, d['rmse'].keys()"</automated>
  </verify>
  <acceptance_criteria>
    - `bench/fixtures/expected_depth6_tree.json` exists with `rmse` and `logloss` sub-trees, a 6-level split sequence each, and a `config` block where `leaf_estimation_iterations == 1` and `score_function` is pinned (A1/A2).
    - A large-n depth-6 speed workload array (e.g. `X_depth6_speed.npy`) is emitted from the same seed and appears in the sha manifest.
    - `python generator.py` runs to completion with no external download (fully reproducible from the seed).
  </acceptance_criteria>
  <done>The generator emits reproducible depth-6 RMSE + Logloss correctness fixtures and a depth-6 speed workload, with iterations=1 and the score function pinned in the config block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CPU oracle cross-check of the depth-6 fixture (cb-compute)</name>
  <files>crates/cb-compute/tests/depth6_reference_test.rs</files>
  <read_first>
    - crates/cb-compute/src/histogram.rs — `reduce_leaf_stats` (line 49: Σder1/Σweight per leaf) and `reduce_leaf_der2` (line 100: Σ(der2·weight) per leaf). The device Σder2 channel in Plan 04 will match reduce_leaf_der2 — this test locks the CPU side.
    - crates/cb-compute/src/leaf.rs — `newton_leaf_delta` (line 145), `calc_average` (line 83), `scale_l2_reg` (line 99).
    - bench/fixtures/expected_depth6_tree.json (produced by Task 1) — the fixture this test validates.
    - CLAUDE.md — source/test separation is MANDATORY: this is a dedicated `tests/` file, never `#[cfg(test)] mod tests` in production source. No `unwrap()` in production (test code may use it, but prefer `expect`).
  </read_first>
  <action>Create `crates/cb-compute/tests/depth6_reference_test.rs`. Load `bench/fixtures/expected_depth6_tree.json` (path relative to the workspace root; if the crate cannot reach it, copy the minimal fixture arrays into the test via a small `include_str!` or a checked-in reduced fixture — do NOT add a network or python dependency to the Rust test). Reconstruct the per-leaf object assignment from the fixture's split sequence, then for BOTH arms independently reduce with `cb_compute::histogram::reduce_leaf_stats` (Σder1/Σweight) and, for the Logloss arm, `reduce_leaf_der2` (Σ(der2·weight)); recompute each leaf value via `calc_average` (RMSE) and `newton_leaf_delta` (Logloss) using `scale_l2_reg` for the regularization, and assert the recomputed leaf values match the fixture's stored leaf values to ≤1e-5. Add an explicit assertion documenting A1 (the fixture's `leaf_estimation_iterations == 1`) and A2 (the pinned score function / channel-0 semantics), so a later change to either surfaces as a test failure rather than silent drift.</action>
  <behavior>
    - RMSE recomputed leaves == fixture RMSE leaves within 1e-5.
    - Logloss recomputed leaves (newton_leaf_delta on Σder1, Σ(der2·weight)) == fixture Logloss leaves within 1e-5.
    - Test fails loudly if `config.leaf_estimation_iterations != 1`.
  </behavior>
  <verify>
    <automated>cargo test -p cb-compute --test depth6_reference_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cb-compute --test depth6_reference_test` passes.
    - The test reduces the fixture with `reduce_leaf_stats` + `reduce_leaf_der2` and recomputes leaves with `calc_average` + `newton_leaf_delta`, asserting ≤1e-5.
    - The test asserts the fixture pins `leaf_estimation_iterations == 1` (A1) and records the score-function / channel-0 assumption (A2).
    - No `mod tests` block added to any production source file.
  </acceptance_criteria>
  <done>The depth-6 fixture is proven consistent with the cb-compute CPU oracle to ≤1e-5, and the A1/A2 assumptions are locked by assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator (offline python) → checked-in fixture | Fixture data is generated offline and consumed by tests/Kaggle; no runtime/external-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | Fixture correctness (silent wrong reference) | mitigate | Task 2 cross-checks the fixture against the independent cb-compute oracle (≤1e-5); a wrong fixture fails the test. |
| T-11-SC | Tampering | package installs (npm/pip/cargo) | accept | No packages added or removed this phase (RESEARCH Package Legitimacy Audit); uses vendored numpy + existing crates only. |
</threat_model>

<verification>
- `python bench/generator.py` emits `expected_depth6_tree.json` (rmse + logloss, 6-level splits, config iterations=1, score_function pinned) + a large-n speed workload, all sha-manifested.
- `cargo test -p cb-compute --test depth6_reference_test` green.
</verification>

<success_criteria>
Depth-6 RMSE + Logloss fixtures exist, are reproducible from one seed, pin iterations=1 and the score function, and are proven consistent with the cb-compute CPU oracle to ≤1e-5. Downstream device self-oracles now have an unambiguous target.
</success_criteria>

<output>
Create `.planning/phases/11-depth-1-partition-aware-histograms-reduction-determinism-new/11-01-SUMMARY.md` when done.
</output>
