---
phase: 07.5-score-split-selection-on-device-tree-grow-loop
plan: 02
type: execute
wave: 2
depends_on: ["07.5-01"]
files_modified:
  - crates/cb-backend/src/kernels.rs
  - crates/cb-backend/src/gpu_runtime.rs
  - crates/cb-backend/src/kernels/score_split.rs
autonomous: true
requirements: [GPU-01]

must_haves:
  truths:
    - "A device scan/update kernel turns the FROZEN 7.3 2-channel per-bin (Σder, Σweight) histogram into cumulative 'left-of-border' leaf stats so a candidate at border b reads left=scan[b], right=total-scan[b], matching the CPU ordered reference."
    - "The scan/update consumes the FROZEN 7.3 device handle in place with no host round-trip inserted at the seam (D-7.5-03)."
    - "The MVP scope is ≤CUBE_DIM bins per feature (single-cube scan precondition); the cross-cube carry for >CUBE_DIM (8-bit, 256-bin) features is an EXPLICIT tracked follow-up sub-task, not a silent cut (RESEARCH A1 / Open Q1)."
  artifacts:
    - path: "crates/cb-backend/src/kernels.rs"
      provides: "scan_update_pointwise_kernel reusing block_scan_kernel over the per-feature bin axis"
      contains: "scan_update_pointwise_kernel"
    - path: "crates/cb-backend/src/gpu_runtime.rs"
      provides: "launch_scan_update_pointwise (+ private _into) handle-in/handle-out, single-client seam, typed guards"
      contains: "launch_scan_update_pointwise"
    - path: "crates/cb-backend/src/kernels/score_split.rs"
      provides: "scan self-oracle test vs the host ordered cb_core::sum_f64 cumulative reference"
      contains: "scan"
  key_links:
    - from: "crates/cb-backend/src/gpu_runtime.rs::launch_scan_update_pointwise"
      to: "crates/cb-backend/src/gpu_runtime.rs::launch_pointwise_hist2_handle"
      via: "consumes the FROZEN 7.3 binSums Handle in place"
      pattern: "scan_update_pointwise|binSums"
    - from: "crates/cb-backend/src/kernels.rs::scan_update_pointwise_kernel"
      to: "crates/cb-backend/src/kernels.rs::block_scan_kernel"
      via: "reuse the wave-agnostic single-cube prefix-sum"
      pattern: "block_scan"
---

<objective>
Land the **scan/update bridge** that 7.3 explicitly deferred: `ScanPointwiseHistograms`/`UpdatePointwiseHistograms` (D-7.5-03), turning the FROZEN 7.3 2-channel device histogram FILL handle into cumulative "left-of-border" leaf stats that the Plan A scorer consumes — device-resident, no host round-trip at the seam.

This is the second narrowest-first vertical slice: a device prefix-sum over the per-feature bin axis (reusing the existing `block_scan_kernel`), self-oracled against the host ordered `cb_core::sum_f64` cumulative reference. It bridges Plan A's scorer (which reads `left = scan[b]`, `right = total - scan[b]`, mirroring CPU `FindOptimalSplitSingleFoldImpl`) to the FILL handle, so Plan C can chain fill→scan→score device-resident.

Purpose: complete the FILL-handle → scorer data path so the histogram finally feeds a scorer without bulk data leaving the device.
Output: `scan_update_pointwise_kernel`, `launch_scan_update_pointwise` (+ `_into`), the `scan` self-oracle test, and an EXPLICIT tracked cross-cube-carry follow-up for >CUBE_DIM-bin features.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md
@.planning/phases/07.5-score-split-selection-on-device-tree-grow-loop/07.5-CONTEXT.md
@.planning/phases/07.5-score-split-selection-on-device-tree-grow-loop/07.5-RESEARCH.md
@.planning/phases/07.5-score-split-selection-on-device-tree-grow-loop/07.5-PATTERNS.md
@.planning/phases/07.3-pointwise-histogram-family/07.3-CONTEXT.md
</context>

## Phase Goal (MVP — user story)

**As a** developer training CatBoost-rs on a GPU, **I want to** have per-bin histograms turned into cumulative left/right leaf stats device-resident, **so that** the split scorer can evaluate every border without the histogram ever leaving the device.

After this plan: the device scan/update runs on rocm AND its cumulative output cross-oracles against the host ordered `sum_f64` reference — a thin working slice that connects Plan A's scorer to the FILL handle.

## Artifacts this phase produces (this plan's portion)

- `scan_update_pointwise_kernel<F: Float>` (`#[cube]`, `kernels.rs`) — per-feature bin-axis inclusive/exclusive prefix-sum over the 2-channel `(feature*n_bins+bin)*2+chan` layout, reusing `block_scan_kernel`; produces cumulative left-of-border (Σder, Σweight).
- `launch_scan_update_pointwise` + private `_into` (`gpu_runtime.rs`) — handle-in (FROZEN 7.3 binSums) / handle-out (cumulative stats), single-client seam, typed guards.
- `scan::cumulative_matches_host_ordered_reference` test in `kernels/score_split.rs`.
- An EXPLICIT tracked follow-up note for the cross-cube scan carry (>CUBE_DIM bins / 8-bit features) — recorded in the SUMMARY as a forward dependency, not silently cut.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scan self-oracle test (≤CUBE_DIM bins) — RED</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs, crates/cb-backend/src/kernels/score_split.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels/score_split.rs (the Plan-A harness: max_divergence, read_handle_f64, make_fixture, host-reference baselines, `use cb_core::sum_f64`)
    - crates/cb-backend/src/kernels.rs (block_scan_kernel:1612-1700 single-cube `n<=CUBE_DIM`; the comptime `inclusive` flag idiom)
    - crates/cb-backend/src/gpu_runtime.rs (launch_block_scan_f64:218-, the `n<=CUBE_DIM` precondition doc ~223-231; const CUBE_DIM:101; launch_pointwise_hist2_handle:971-989 to produce the FROZEN binSums input)
    - crates/cb-compute/src/score.rs (FROZEN reference for how the scorer reads left/right: left = scan[b], right = total - scan[b], the FindOptimalSplitSingleFoldImpl convention)
    - crates/cb-core (sum_f64 — the ONLY ordered host fold; NEVER naive `.sum()`, D-08)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (MANDATORY before kernel code)
  </read_first>
  <behavior>
    - Test `scan::cumulative_matches_host_ordered_reference`: produce a FROZEN 7.3 binSums handle (≤CUBE_DIM bins per feature), launch the device scan/update, read back the cumulative per-(feature,bin) (Σder, Σweight), and assert each cumulative value equals the host ordered prefix-sum (folded via `cb_core::sum_f64` over the same bin order) within a reported f64 bound. REPORT divergence; do not sign off.
    - Edge cases: n_bins=1, n_bins=CUBE_DIM (boundary), multiple features, empty (no read-back).
  </behavior>
  <action>
    In `kernels/score_split.rs`, add a `scan` test module section. Build the FROZEN binSums input via `launch_pointwise_hist2_handle` (read back for the baseline only). Add a host-reference cumulative baseline that folds the per-bin channels with `cb_core::sum_f64` in ascending bin order (the ordered reference; left = prefix, right = total - prefix). Write `scan::cumulative_matches_host_ordered_reference` referencing `launch_scan_update_pointwise` (defined Task 2) so it compiles RED. SCOPE the fixtures to ≤CUBE_DIM bins per feature (single-cube precondition, RESEARCH A1). Test code MAY unwrap/index; production may not. Do NOT modify cb-compute/cb-core (read-only, SC-4).
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm score_split::scan --no-run 2>&1 | tail -5</automated>
  </verify>
  <done>`scan::cumulative_matches_host_ordered_reference` test + host ordered-prefix baseline (via sum_f64) exist and compile RED against the not-yet-defined `launch_scan_update_pointwise`; fixtures scoped ≤CUBE_DIM bins; no cb-compute/cb-core edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scan_update_pointwise_kernel + launch_scan_update_pointwise (GREEN) + cross-cube-carry follow-up</name>
  <files>crates/cb-backend/src/kernels.rs, crates/cb-backend/src/gpu_runtime.rs, crates/cb-backend/src/kernels/score_split.rs</files>
  <read_first>
    - crates/cb-backend/src/kernels.rs (block_scan_kernel:1612-1700 — reuse VERBATIM for the per-feature bin-axis prefix-sum; the `#[comptime] inclusive` flag:1615; single-cube `n<=CUBE_DIM` constraint doc:1598-1604)
    - crates/cb-backend/src/gpu_runtime.rs (launch_block_scan_f64:218- precondition; launch_pointwise_hist2_handle:971-989 / _into:991- — CLONE the handle-in/handle-out no-readback seam + single-client `&client` + the guard block; binSums layout (feature*n_bins+bin)*2+chan; const CUBE_DIM:101)
    - catboost-master/catboost/cuda/methods/kernel/pointwise_scores.cu (ScanPointwiseHistograms / the scan reduction; FindOptimalSplitSingleFoldImpl:259-263 weightRight = part.Weight - weightLeft — the left/right convention)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md (load on ANY CubeCL build error before fixing)
  </read_first>
  <behavior>
    - `scan::cumulative_matches_host_ordered_reference` (Task 1) goes GREEN: device cumulative per-(feature,bin) (Σder, Σweight) == host ordered prefix within reported tol.
    - The output layout is consumable by Plan A's scorer (left = scan[b], right = total - scan[b]).
  </behavior>
  <action>
    In `kernels.rs`, add `#[cube] pub fn scan_update_pointwise_kernel<F: Float>(...)` generic over `F` (generics-float). Reuse the `block_scan_kernel` mechanism for the per-feature bin-axis prefix-sum over the FROZEN 2-channel `(feature*n_bins+bin)*2+chan` layout (one scan per feature over its bin axis; carry the `#[comptime] inclusive` flag). NO literal warp size (D-09; stride from CUBE_DIM_X/PLANE_DIM). In `gpu_runtime.rs`, add `launch_scan_update_pointwise(&client, binsums: &Handle, n_bins, n_features) -> CbResult<Handle>` plus a private `_into` — clone the `launch_pointwise_hist2_handle`/`_into` seam: consume the FROZEN 7.3 binSums `&Handle` in place, return the cumulative-stats `Handle`, NO `read_one` on the seam, single-client `&client`. Guard block: `CbError::LengthMismatch`, `checked_mul` overflow → `CbError::Degenerate`, empty short-circuit (never read a 0-len handle), read-back failure → `CbError::Degenerate`. No production unwrap/panic/indexing.

    SCOPE: implement the ≤CUBE_DIM-bins single-cube path this plan. Add an EXPLICIT guard that returns `CbError::OutOfRange` (or a typed `CbError`) if `n_bins > CUBE_DIM`, with a doc-comment flagging the cross-cube carry as the tracked 7.2/7.3 forward dependency (RESEARCH A1 / Open Q1) needed for 8-bit (256-bin) features. Record this follow-up explicitly in the SUMMARY (NOT a silent cut). On ANY CubeCL build error, load `cubecl_error_guideline.md` FIRST.
  </action>
  <verify>
    <automated>cargo test -p cb-backend --no-default-features --features rocm score_split::scan -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>`scan_update_pointwise_kernel` (generics-float) + `launch_scan_update_pointwise`(+`_into`) exist; `scan::cumulative_matches_host_ordered_reference` PASSES on rocm for ≤CUBE_DIM bins; output consumable by the Plan A scorer; n_bins>CUBE_DIM returns a typed error with a documented cross-cube-carry follow-up (recorded in SUMMARY); no host round-trip at the FILL→scan seam; no production unwrap/panic/indexing; `cargo build -p cb-backend --features cuda` compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host driver → device kernel | FROZEN 7.3 binSums handle + lengths/n_bins/n_features cross into launch geometry; no external/untrusted input |
| device → device | scan output stays device-resident (handle-out, no read-back at the seam) |

Pure in-process GPU compute in `cb-backend`; surface is device-buffer index correctness + the single-cube scan precondition.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-02-01 | Tampering | scan OOB read/write past the per-feature bin axis when n_bins > CUBE_DIM | mitigate | explicit `n_bins > CUBE_DIM` guard → typed `CbError`; single-cube precondition enforced; comptime shmem size guard (block_scan_kernel precedent) |
| T-07.5-02-02 | Tampering | integer overflow in (feature*n_bins+bin)*2+chan index math | mitigate | `checked_mul` on the layout products → `CbError::Degenerate` |
| T-07.5-02-03 | Tampering/DoS | reading the cumulative handle through the wrong client / 0-len handle | mitigate | single `&client` threading; empty short-circuit; never read a 0-len handle |
| T-07.5-02-04 | Spoofing | silent zero buffer on read-back failure | mitigate | read-back failure → `CbError::Degenerate`, never a zero buffer |
| T-07.5-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs (no new deps); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cargo test -p cb-backend --no-default-features --features rocm score_split::scan -- --nocapture` — scan green on gfx1100.
- `cargo build -p cb-backend --features cuda` — cuda arm compiles.
- `cargo build -p cb-backend --no-default-features --features wgpu` — wgpu arm builds.
- `cargo tree -e features -p cb-compute | grep -ci cubecl` == 0 — SC-4 intact.
- `git diff --stat crates/cb-core crates/cb-model crates/cb-compute` — byte-unchanged.
</verification>

<success_criteria>
- Device scan/update over the FROZEN 7.3 handle yields cumulative left-of-border (Σder, Σweight) matching the host ordered `sum_f64` reference within reported tol (REPORTED, not signed off).
- No host round-trip inserted at the FILL→scan seam (handle-in / handle-out).
- ≤CUBE_DIM-bins path works; >CUBE_DIM returns a typed error with the cross-cube-carry follow-up explicitly documented (RESEARCH A1).
- No production unwrap/panic/indexing; generics-float; no warp-size literals.
- SC-4 boundary intact; cb-core/cb-model/cb-compute byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/07.5-score-split-selection-on-device-tree-grow-loop/07.5-02-SUMMARY.md` when done. Record the cross-cube scan-carry follow-up explicitly.
</output>
