---
phase: 7.5
slug: score-split-selection-on-device-tree-grow-loop
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-20
---

# Phase 7.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` (workspace standard), in dedicated `kernels/score_split.rs` / `kernels/grow_loop.rs` test modules (source/test separation, CLAUDE.md) |
| **Config file** | none — `cargo test -p cb-backend` with `--features {rocm\|wgpu\|cuda}` |
| **Quick run command** | `cargo test -p cb-backend --no-default-features --features rocm score_split:: -- --nocapture` (in-env gfx1100) |
| **Full suite command** | `cargo test -p cb-backend --no-default-features --features rocm` then `--features wgpu` (host); `cargo build -p cb-backend --features cuda` (compile-only) — per-crate, never full-workspace (disk, D-7.5-07) |
| **Estimated runtime** | ~60–120 seconds (per-crate, in-env gfx1100) |

---

## Sampling Rate

- **After every task commit:** `cargo test -p cb-backend --no-default-features --features rocm <relevant module>:: -- --nocapture` (the narrowest passing module for that task).
- **After every plan wave:** `cargo test -p cb-backend --no-default-features --features rocm` (all 7.5 modules) + the SC-4 `cargo tree` guard + `cargo build -p cb-backend --features cuda`.
- **Before `/gsd-verify-work`:** full `cb-backend` rocm suite green + wgpu build/run + cuda build; per-tree structure-match EXACT on all fixtures; leaf-value divergence REPORTED (not signed off).
- **Max feedback latency:** ~120 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.5-A | A | 1 | GPU-01 (score) | — | N/A (in-process GPU compute; no external input) | unit/self-oracle | `cargo test -p cb-backend --features rocm score_split::score -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-A | A | 1 | GPU-01 (split) | — | N/A | unit/self-oracle | `cargo test -p cb-backend --features rocm score_split::argmin -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-B | B | 2 | GPU-01 (scan/update) | — | N/A | unit/self-oracle | `cargo test -p cb-backend --features rocm score_split::scan -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-C | C | 3 | GPU-01 / SC-3 (single tree) | — | N/A | integration/cross-oracle | `cargo test -p cb-backend --features rocm grow_loop::single_tree -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-D | D | 4 | GPU-01 / SC-3 / D-7.5-06 (multi-tree) | — | N/A | integration/cross-oracle | `cargo test -p cb-backend --features rocm grow_loop::multi_tree -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-E | E | 5 | GPU-01 (Cosine + Solar/LOO/Sat) | — | N/A | unit/self-oracle | `cargo test -p cb-backend --features rocm score_split::variants -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-F | F | 6 | GPU-01 / SC-3 (pairwise) | — | N/A | integration/cross-oracle | `cargo test -p cb-backend --features rocm grow_loop::pairwise -- --nocapture` | ❌ W0 | ⬜ pending |
| 07.5-* | * | * | SC-4 (boundary) | — | cb-compute cubecl-free; cb-core/cb-model/cb-compute byte-unchanged; cb-train cubecl-free | guard | `cargo tree -e features -p cb-compute \| grep -ci cubecl` == 0 | ✅ runnable | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/cb-backend/src/kernels/score_split.rs` — score + argmin + scan self-oracle vs `cb-compute/src/score.rs` (covers GPU-01 score/split/scan). Clone the `kernels/pointwise_hist.rs` `max_divergence` reporter.
- [ ] `crates/cb-backend/src/kernels/grow_loop.rs` — full-tree cross-oracle vs `cb-train/src/tree.rs` (`greedy_tensor_search_oblivious`) + `cb-compute/src/leaf.rs` (covers SC-3 single/multi/pairwise). STRUCTURE match is exact; leaf VALUES report divergence.
- [ ] `#[cfg(test)] mod score_split;` / `mod grow_loop;` mounts in `kernels.rs` (the `pointwise_hist`/`pairwise_hist` mount precedent).
- [ ] Synthetic fixtures: small multi-tree dataset (f32 + f64), **clear-gain-margin splits** (so atomic jitter can't flip structure — D-7.5-06), plus a pairwise/ranking-loss-derived pair/weight fixture (source via `cb-train`/`cb-compute` ranking der, read-only, as in 7.4).
- [ ] Framework install: none — Rust `#[test]` + the `rocm` feature already in use.

*Existing infrastructure (the shipped `pointwise_hist.rs`/`pairwise_hist.rs` report-divergence harness + the FROZEN CPU oracles `score.rs`/`leaf.rs`/`pairwise_scoring.rs` + the `cb-train` grow loop + the `cargo tree` SC-4 guard) covers the PATTERN; the two new harness files are the only new test artifacts.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `cuda` arm correctness | GPU-01 | No NVIDIA hardware in-env (D-07) | `cargo build -p cb-backend --features cuda` — compile-only; correctness deferred to a CUDA host |
| Multi-tree argmax-flip boundary | SC-3 / D-7.5-06 | Atomic accumulation order is non-deterministic; structure must stay exact, the flip boundary is REPORTED not signed off (Phase 7.6) | Re-run `grow_loop::multi_tree` N times; confirm per-tree structure is stable + observe leaf-value divergence is small/stable |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
