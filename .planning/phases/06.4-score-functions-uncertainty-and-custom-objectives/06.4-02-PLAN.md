---
phase: 06.4-score-functions-uncertainty-and-custom-objectives
plan: 02
type: execute
wave: 2
depends_on: ["06.4-01"]
files_modified:
  - crates/cb-oracle/fixtures/rmse_uncertainty
  - crates/cb-oracle/generator/rmse_uncertainty_fixture.py
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-backend/src/cpu_runtime.rs
  - crates/cb-train/tests/rmse_uncertainty_oracle_test.rs
autonomous: true
requirements: [LOSS-08]
must_haves:
  truths:
    - "Loss::RmseWithUncertainty is a 2-dim DIAGONAL-hessian variant riding the shipped 6.2 N-dim spine (D-6.4-04)"
    - "der1/der2 match error_functions.h:280-313: diff=target-approx[0], prec=exp(-2*approx[1]), der1=[w*diff, w*(diff²*prec−1)], der2-diag=[−w, −2w*diff²*prec]"
    - "Training RMSEWithUncertainty (CPU thread_count=1, leaf_estimation_method=Newton, leaf_estimation_iterations=1) reproduces upstream borders/splits/leaf-values/staged-approx (2-dim) + final RawFormulaVal ≤1e-5 (D-6.4-02 full Python-reachable oracle)"
    - "Fixtures are generated OFFLINE against .venv catboost==1.2.10 and frozen-committed (no /tmp instrumentation, D-6.4-02)"
  artifacts:
    - path: "crates/cb-compute/src/runtime.rs"
      provides: "Loss::RmseWithUncertainty variant (approx_dimension=2)"
      contains: "RmseWithUncertainty"
    - path: "crates/cb-compute/src/loss.rs"
      provides: "2-dim diagonal RMSEWithUncertainty der helper"
      contains: "rmse_with_uncertainty"
    - path: "crates/cb-oracle/fixtures/rmse_uncertainty"
      provides: "frozen offline training-stage fixture (borders/splits/leaves/staged-approx/predict)"
    - path: "crates/cb-train/tests/rmse_uncertainty_oracle_test.rs"
      provides: "per-stage ≤1e-5 training oracle"
  key_links:
    - from: "crates/cb-backend/src/cpu_runtime.rs"
      to: "crates/cb-compute/src/loss.rs"
      via: "compute_gradients dispatches Loss::RmseWithUncertainty to the 2-dim diagonal der helper"
      pattern: "RmseWithUncertainty"
    - from: "crates/cb-train/tests/rmse_uncertainty_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/rmse_uncertainty"
      via: "compare_stage ≤1e-5"
      pattern: "compare_stage"
---

<objective>
Wave B (LOSS-08, loss half): add `RMSEWithUncertainty` as a 2-dimensional diagonal-hessian `Loss`
variant riding the shipped 6.2 N-dim approx spine, oracle-locked ≤1e-5 against an OFFLINE-generated
catboost 1.2.10 training fixture. End-to-end slice: a Rust caller trains RMSEWithUncertainty on CPU
(thread_count=1, Newton/1-iter) and the per-stage approx/leaf/prediction matches upstream.

Purpose: deliver the uncertainty LOSS the prediction types (Plan 03) consume; rides the one N-dim
spine (D-6.2-03 single code path), no new pipeline.

Output: `Loss::RmseWithUncertainty`, the 2-dim diagonal der helper in `loss.rs`, the
`compute_gradients` dispatch arm, the offline fixture generator + frozen fixture, and
`crates/cb-train/tests/rmse_uncertainty_oracle_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-CONTEXT.md
@.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-RESEARCH.md
@.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-PATTERNS.md
@crates/cb-compute/src/runtime.rs
@crates/cb-compute/src/loss.rs
@crates/cb-backend/src/cpu_runtime.rs
</context>

<artifacts_this_phase_produces>
- `Loss::RmseWithUncertainty` (unit variant, `approx_dimension=2`, `runtime.rs`)
- `rmse_with_uncertainty_ders` 2-dim diagonal der helper (`loss.rs`)
- `compute_gradients` `Loss::RmseWithUncertainty` dispatch arm (`cpu_runtime.rs`)
- `crates/cb-oracle/generator/rmse_uncertainty_fixture.py` (offline generator) + frozen fixture under `crates/cb-oracle/fixtures/rmse_uncertainty`
- `crates/cb-train/tests/rmse_uncertainty_oracle_test.rs` (NEW)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 fixture — offline RMSEWithUncertainty training fixture + failing oracle test</name>
  <files>crates/cb-oracle/generator/rmse_uncertainty_fixture.py, crates/cb-oracle/fixtures/rmse_uncertainty, crates/cb-train/tests/rmse_uncertainty_oracle_test.rs</files>
  <read_first>
    - crates/cb-oracle/src/compare.rs (`compare_stage` ≤1e-5 API)
    - crates/cb-train/tests/multiquantile_oracle_test.rs (training-oracle test convention + fixture loading via CARGO_MANIFEST_DIR)
    - 06.4-RESEARCH.md Strand 2 (leaf default Newton/1, catboost_options.cpp:77-82; ≥2V+1 trees needs ≥11 trees for the Plan-03 VE reuse) + Oracle/Fixture Plan Wave B
    - 06.4-PATTERNS.md "oracle test file convention"
    - project memory `catboost-offline-fixture-blocker` (.venv catboost==1.2.10, fixtures committed frozen)
  </read_first>
  <action>
    Write `crates/cb-oracle/generator/rmse_uncertainty_fixture.py` (OFFLINE, runs against `.venv`
    catboost==1.2.10, CPU, `thread_count=1`): a small regression dataset, `loss_function='RMSEWithUncertainty'`,
    `iterations` ≥ 11 (so Plan 03's `virtual_ensembles_count=5` has ≥2V+1 trees — Pitfall 5), `depth`≈3,
    `leaf_estimation_method='Newton'`, `leaf_estimation_iterations=1`, `model_shrink_rate=0` (A2 default
    path), `random_seed` pinned, `boosting_type='Plain'`. Export per-stage ground truth: borders/splits,
    per-iteration leaf-values (2-dim), staged-approx (2-dim), and final `predict(prediction_type='RawFormulaVal')`
    (2 cols). Run it ONCE and FREEZE-COMMIT the output under `crates/cb-oracle/fixtures/rmse_uncertainty`
    (same frozen-fixture discipline as the shipped oracles). Then write the SEPARATE test file
    `crates/cb-train/tests/rmse_uncertainty_oracle_test.rs` that loads the fixture and `compare_stage`s
    each stage ≤1e-5 against a Rust RMSEWithUncertainty train run — it MUST fail/not-compile now
    (`Loss::RmseWithUncertainty` does not exist yet → RED). NO inline `#[cfg(test)]` (CLAUDE.md).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/catboost_rs && ls crates/cb-oracle/fixtures/rmse_uncertainty && cargo test -p cb-train --test rmse_uncertainty_oracle_test 2>&1 | grep -qE "cannot find|E0599|unresolved|RmseWithUncertainty" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    Frozen fixture exists under `crates/cb-oracle/fixtures/rmse_uncertainty` with ≥11 trees and 2-dim
    stages; the separate oracle test exists and fails because `Loss::RmseWithUncertainty` is undefined.
  </acceptance_criteria>
  <done>RED: fixture frozen-committed; oracle test references the not-yet-existing loss and fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loss::RmseWithUncertainty variant + 2-dim diagonal der helper</name>
  <files>crates/cb-compute/src/runtime.rs, crates/cb-compute/src/loss.rs</files>
  <read_first>
    - crates/cb-compute/src/runtime.rs (`Loss` enum `:56`, already non-Copy/non-Eq; `MultiClassOneVsAll`/`MultiLogloss` no-param cluster `:183-213`; `validate` `:556`, no-param cluster `:756-758`; `approx_dimension` resolution)
    - crates/cb-compute/src/loss.rs (`multiclass_onevsall_ders` `:517`, `multi_crossentropy_ders` `:554` — diagonal per-dim der pattern)
    - 06.4-RESEARCH.md Strand 2 RMSEWithUncertainty der1/der2 (error_functions.h:280-313) + Pitfall 4 (diagonal, not dense) + Pitfall 6/A4 (f64::exp ↔ FastExp within 1e-5)
    - 06.4-PATTERNS.md "RMSEWithUncertainty 2-dim der"
  </read_first>
  <behavior>
    - der for one object (target scalar `t`, approx `[a0, a1]`, weight `w`): `diff = t - a0`; `prec = (-2.0*a1).exp()`; der1 = `[w*diff, w*(diff*diff*prec - 1.0)]`; der2-diag = `[-w, -2.0*w*diff*diff*prec]`.
    - approx_dimension for `Loss::RmseWithUncertainty` is 2.
    - Diagonal hessian → each dim is an INDEPENDENT scalar Newton step (the 6.2 MultiClassOneVsAll/MultiLogloss per-dim path), NOT MultiClass's dense `solve_symmetric_newton` (Pitfall 4).
    - Unit-test the der helper directly against the RESEARCH formula on a couple of hand-picked (t, a0, a1, w) triples within 1e-12.
  </behavior>
  <action>
    Add `RmseWithUncertainty` as a unit variant to `Loss` (additive — `Loss` already dropped `Copy`/`Eq`).
    Wire `approx_dimension = 2` and the `validate` arm alongside the no-param N-dim cluster. In `loss.rs`,
    add `rmse_with_uncertainty_ders` returning the 2-dim `(der1[2], der2_diag[2])` per object per the
    RESEARCH formula, mirroring the unweighted-pair shape of `multi_crossentropy_ders`/`multiclass_onevsall_ders`
    (caller folds weight; here fold `w` per the formula). Use `(-2.0*a1).exp()` for `prec` (NOT exp(a1);
    `f64::exp` ↔ `FastExp` within 1e-5, A4). Any accumulation into leaf stats routes through `cb_core::sum_f64`
    (D-08). Add a separate unit test file or extend the existing loss-der test file (separate file, no inline
    `#[cfg(test)]`) asserting the der helper matches the formula.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/catboost_rs && cargo test -p cb-compute 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    `Loss::RmseWithUncertainty` exists with `approx_dimension=2` and a `validate` arm; `rmse_with_uncertainty_ders`
    matches the RESEARCH der1/der2 within 1e-12; `cargo test -p cb-compute` green.
  </acceptance_criteria>
  <done>The 2-dim diagonal der helper and loss variant exist and are unit-correct.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — compute_gradients dispatch + train-stage oracle ≤1e-5</name>
  <files>crates/cb-backend/src/cpu_runtime.rs, crates/cb-train/tests/rmse_uncertainty_oracle_test.rs</files>
  <read_first>
    - crates/cb-backend/src/cpu_runtime.rs (`Runtime::compute_gradients` body; the existing N-dim/MultiClassOneVsAll arms that assemble dimension-major `Derivatives { der1, der2 }`)
    - crates/cb-compute/src/loss.rs (the `rmse_with_uncertainty_ders` from Task 2)
    - 06.4-RESEARCH.md Strand 2 (rides the 6.2 N-dim spine; diagonal per-dim leaf path) + Pitfall 4
    - 06.4-PATTERNS.md "compute_gradients dispatch"
  </read_first>
  <action>
    Add a `Loss::RmseWithUncertainty` arm to `compute_gradients` (`cpu_runtime.rs`) that calls
    `rmse_with_uncertainty_ders` per object and assembles the dimension-major `Derivatives { der1, der2 }`
    (dim=2, diagonal), routing through the SAME N-dim assembly the MultiClassOneVsAll/MultiLogloss arms use
    (D-6.2-03 single code path) — the diagonal hessian feeds the existing 6.2 per-dim scalar Newton leaf
    estimation, NOT the dense MultiClass solve (Pitfall 4). Then make the Task-1 oracle test GREEN: train
    RMSEWithUncertainty (Newton/1-iter, thread_count=1) and `compare_stage` borders/splits/leaf-values
    (2-dim)/staged-approx (2-dim) + final RawFormulaVal (2 cols) ≤1e-5 against the frozen fixture. All Σ
    via `cb_core::sum_f64` (D-08). No `unwrap`/indexing in library code.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/catboost_rs && cargo test -p cb-train --test rmse_uncertainty_oracle_test 2>&1 | tail -5; cargo test -p cb-compute -p cb-train 2>&1 | grep -iE "test result|FAILED" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p cb-train --test rmse_uncertainty_oracle_test` PASSES all stages ≤1e-5; `cargo test -p cb-compute`
    and `cargo test -p cb-train` green (no regression on shipped scalar/N-dim loss oracles).
  </acceptance_criteria>
  <done>GREEN: RMSEWithUncertainty trains on CPU and matches upstream per-stage ≤1e-5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | 2-dim diagonal der arithmetic over already-validated Pool data + a frozen fixture set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.4B-01 | Tampering | `rmse_with_uncertainty_ders` non-finite der on extreme `approx[1]` (prec=exp underflow/overflow) | mitigate | `exp` is finite-bounded; der products guarded by the `unwrap`/indexing bans; staged-approx oracle catches any NaN/Inf drift (robustness note, not a security `high`) |

**No external attack surface:** in-process f64 arithmetic over already-validated Pool data and a
frozen offline fixture; no untrusted input, network, auth, or new serialization.
</threat_model>

<verification>
- `cargo test -p cb-train --test rmse_uncertainty_oracle_test` green (all 2-dim stages + final predict ≤1e-5).
- `cargo test -p cb-compute -p cb-train` green; shipped scalar/N-dim loss oracles not regressed.
- Fixture frozen-committed under `crates/cb-oracle/fixtures/rmse_uncertainty` (offline, catboost==1.2.10).
- der1/der2 match `error_functions.h:280-313`; diagonal per-dim leaf path (Pitfall 4); `(-2.0*a1).exp()` (Pitfall 6/A4).
- No raw `iter().sum()`/`fold(0.0,…)` in the new der/assembly (D-08).
</verification>

<success_criteria>
- `Loss::RmseWithUncertainty` is a 2-dim diagonal variant on the 6.2 N-dim spine.
- der1/der2 match RESEARCH `error_functions.h:280-313`.
- Training reproduces upstream per-stage ≤1e-5 (full Python-reachable oracle, D-6.4-02).
- Fixtures offline-generated + frozen-committed.
</success_criteria>

<output>
Create `.planning/phases/06.4-score-functions-uncertainty-and-custom-objectives/06.4-02-SUMMARY.md` when done.
</output>
