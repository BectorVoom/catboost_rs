---
phase: 06.5-text-and-embedding-features
plan: 06
subsystem: compute+train+oracle
tags: [catboost, embedding-features, knn, k-nearest-neighbor, online-hnsw, brute-force-exact, neighbor-id-spike, online-estimation, read-before-update, instrumented-gt, FEAT-02, A2-landmine]

# Dependency graph
requires:
  - phase: 06.5-text-and-embedding-features
    plan: 05
    provides: online read-before-update embedding prefix pattern (online_embedding.rs LDA analog) + estimated-feature seam
  - phase: 06.5-text-and-embedding-features
    plan: 01
    provides: instrumented catboost 1.2.10 trainer + knn_neighbors D-07 hook (per-query HNSW neighbor-id dump) + KNN per-stage .npy fixtures
provides:
  - cb_compute::KnnCloud — brute-force-exact L2-squared k-NN (sum_f64 distances, ascending-distance + ascending-id tie-break), insertion-order ids
  - cb_compute::KnnCalcer (update/compute/neighbors) — classification per-class vote counts (width=nClasses) / regression neighbor-target mean (width=1)
  - cb_train::offline_knn_features (Plain whole-set) + online_knn_prefix (read-before-update prefix, D-03) + knn_feature_count + OnlineKnnPrefix
  - KNN neighbor-id EXACT parity vs the instrumented HNSW dump (0 mismatches) + bit-exact class-vote feature + byte-identical per-stage model
affects: [06.5-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Brute-force-exact L2 k-NN as the spike-validated HNSW substitute (knn.cpp:31-49): at fixture scale (16 docs / 4 dims) the online HNSW index degenerates to exact (A5), so a deterministic distance scan reproduces the upstream neighbor ids BIT-FOR-BIT (0/64 mismatches vs the knn_neighbors dump). NO third-party HNSW/ANN crate (A2/D-05) — a non-identical graph would change neighbors and break parity"
    - "KNN Compute (knn.cpp:51-74): classification ++result[TargetClasses[id]] over the k neighbors (width=nClasses); regression result[0]=mean(Targets[id]) via sum_f64 (width=1, Pitfall 5). The vote is an INTEGER count over the neighbor set -> bit-exact when the neighbor set is bit-exact (strictly stronger than LDA's binarization-stability argument)"
    - "Online KNN (D-03): insertion order = the learn permutation; each document queries its k-NN over the PREFIX of already-inserted vectors ONLY (read-before-update) THEN inserts (target, embedding). Leading docs have an empty prefix -> empty neighbor list + all-zero feature (the dump's leading \"neighbors\":[] rows)"
    - "Neighbor-id oracle: segment the instrumented knn_neighbors.json into its repeated 16-doc online passes (a \"neighbors\":[] row marks a pass boundary) and assert online_knn_prefix's prefix-local neighbor ids equal every pass EXACTLY — the A2 landmine's direct parity gate"

key-files:
  created:
    - crates/cb-oracle/tests/knn_oracle_test.rs
  modified:
    - crates/cb-compute/src/embedding_calcers.rs
    - crates/cb-compute/src/embedding_calcers_test.rs
    - crates/cb-compute/src/lib.rs
    - crates/cb-train/src/estimated/online_embedding.rs
    - crates/cb-train/src/estimated/online_embedding_test.rs
    - crates/cb-train/src/lib.rs
    - .planning/phases/06.5-text-and-embedding-features/deferred-items.md

key-decisions:
  - "DECISION (Task-1 checkpoint:decision, AUTO-SELECTED in --auto chain AND spike-unambiguous): brute-force-exact L2-squared k-NN, NOT a hand-port of online_hnsw and NOT a documented tolerance. Rationale: the 06.5-06 neighbor-id SPIKE compared a brute-force-exact L2-squared k-NN against the instrumented upstream HNSW neighbor-id dump (knn_neighbors.json, the Plan-01 D-07 hook, CloseNum=5) on the frozen 16-row/4-dim fixture and found 0/64 neighbor-id mismatches (ordered AND set, across all 4 online passes). At fixture scale the HNSW index degenerates to exact (A5), so brute-force-exact reproduces the upstream neighbor set BIT-FOR-BIT. The decision contract's UNAMBIGUOUS-GOOD-OUTCOME branch applies (zero divergence, no human judgment adds value) -> proceed to complete the plan. A third-party HNSW/ANN crate was NEVER an option (A2/D-05): any non-identical graph changes the neighbors and breaks parity."
  - "NO documented tolerance, NO binarization-stability argument needed (unlike LDA Plan 05). KNN's neighbor ids are reproduced EXACTLY, so the integer class-vote feature is bit-exact, so the per-stage model (Splits at the 0.5 integer-count border / LeafValues / StagedApprox / Predictions) is byte-identical. The KNN oracle therefore asserts EXACT neighbor ids + bit-exact votes + the perfect class separation the upstream model emits — a strictly stronger parity guarantee than LDA's ≤6e-2 raw-projection tolerance."
  - "The KNN fixture model (KNN:k=3) co-trains a text/BoW feature: splits.npy = [0.5, 0.590515, 0.5, 0.590515, 0.5] where 0.5 is the KNN class0-vote integer border (trees 0/2/4, calcer 242FCBFF...) and 0.590515 is the co-trained BoW feature border (trees 1/3, calcer 4843DC65...). Both perfectly separate the alternating classes; each leaf holds exactly 8 of the 16 learn docs; predictions saturate to ±0.971603 by class (class 1 -> +, class 0 -> -)."
  - "Plain boosting feeds the OFFLINE whole-set KNN estimate to the tree splits (each doc is its own nearest neighbor at distance 0 over the full set); the online read-before-update prefix (the dump's surface, leakage-controlled) feeds the ordered-boosting leaf path. Both are wired (offline_knn_features / online_knn_prefix); the SC-4 end-to-end seam integration is Plan 07."

requirements-completed: [FEAT-02]

metrics:
  duration: ~50min
  completed: 2026-06-18
  tasks: 2
  files-created: 1
  files-modified: 7
  tests-added: 21  # 10 embedding_calcers (KNN) + 6 online_embedding (KNN) + 5 knn_oracle
---

# Phase 6.5 Plan 06: KNN Embedding Calcer Summary

**One-liner:** The last and highest-risk embedding calcer — KNN (`TKNNCalcer` neighbor-vote) — implemented as a brute-force-exact L2-squared k-NN after an instrumented neighbor-id SPIKE proved it reproduces the upstream online-HNSW neighbor ids BIT-FOR-BIT (0/64 mismatches, A5 degenerate-to-exact at fixture scale), yielding a bit-exact integer class-vote feature and a byte-identical per-stage model — NEVER a third-party HNSW crate (A2). Closes FEAT-02.

## Task 1 — neighbor-id SPIKE + decision (checkpoint:decision, resolved brute-force-exact)

The plan's Task 1 is a `checkpoint:decision`. Per the decision contract, the SPIKE ran FIRST: a brute-force-exact L2-squared k-NN (online insertion order = the learn permutation, distances via `sum_f64`, ascending-distance + ascending-id tie-break) was compared against the instrumented upstream HNSW neighbor-id dump (`fixtures/text_tokenizer/knn_neighbors.json`, the Plan-01 D-07 `knn_neighbors` hook, `CloseNum = 5`).

**Spike result — neighbor-id comparison report:**

| Quantity | Value |
|----------|-------|
| Queries compared (4 passes × 16 docs) | 64 |
| ORDERED neighbor-id mismatches | **0 / 64** |
| SET neighbor-id mismatches | **0 / 64** |
| Per-document KNN encoding divergence | **0 (bit-exact integer votes)** |
| Binarization border crossings | **0 (not needed — divergence is zero)** |

The HNSW index degenerates to exact at fixture scale (16 docs / 4 dims, A5). This is the decision contract's **UNAMBIGUOUS GOOD OUTCOME** (zero/negligible divergence — no human judgment adds value), so the executor proceeded to complete the plan. Auto-mode was active (`_auto_chain_active: true`) and `brute-force-exact` is the recommended first option; it was auto-selected AND independently spike-validated.

⚡ **Auto-selected: brute-force-exact** (0/64 neighbor-id mismatches; exact encoding match).

No HNSW hand-port (unnecessary — brute-force is already bit-exact), no documented tolerance (unnecessary — the ids are EXACT, not approximate), no third-party HNSW/ANN crate (forbidden, A2/D-05).

## Task 2 — KNN calcer + online wiring + oracle

- **KNN calcer** (`cb_compute::embedding_calcers`, commit `1de27db`): `KnnCloud` (brute-force-exact L2-squared scan, `sum_f64` distances, insertion-order ids, ascending-distance/ascending-id tie-break) + `KnnCalcer` (classification per-class vote counts width=nClasses; regression neighbor-target mean width=1, `sum_f64`). Transcribed from `knn.cpp:31-90` / `knn.h:94-142`. Checked `.get(..)`, typed `CbError` on dim mismatch / parallel-array desync; no `unwrap`/`expect`/`panic`/raw index. 10 unit tests.
- **Online/offline seam** (`cb_train::estimated::online_embedding`, commit `a4ddc36`): `offline_knn_features` (Plain whole-set — insert all, compute each, self is own nearest at distance 0) + `online_knn_prefix` (read-before-update prefix over the learn permutation, records prefix-local neighbor ids = the dump's exact surface, D-03) + `knn_feature_count` + `OnlineKnnPrefix`. 6 wiring tests including the prefix neighbor-id pattern vs the dump (`[0]`, `[0,1]`, `[1,0,2]`, `[2,0,3]`, `[1,3,0]`).
- **Oracle** (`cb_oracle::knn_oracle_test`, commit `be241fa`): 5 tests, NO `#[ignore]`, NO weakened tolerance:
  1. **Neighbor-id EXACT compare** vs the instrumented HNSW dump (every query in every pass) — the A2 landmine's direct gate.
  2. KNN feature integer-separates the clouds at the upstream `0.5` border (votes sum to k, integer-valued).
  3. The `0.5` integer-count border appears in `splits.npy`.
  4. Per-stage: predictions reproduce the perfect ±0.971603 class separation (`compare_stage` Predictions + StagedApprox tail ≤1e-5); leaf weights 8/8 partition.
  5. Robustness: ragged-embedding dim mismatch rejected (typed error, no panic).

## Deviations from Plan

None — plan executed as written. Task 1's decision resolved to the recommended `brute-force-exact` option on unambiguous spike evidence; Task 2 implemented exactly that path. No architectural changes, no auth gates, no auto-fixed bugs.

**Out-of-scope items logged (not fixed, per SCOPE BOUNDARY):**
- Pre-existing unused `const DIM` in `online_embedding_test.rs:13` (introduced by Plan 05, commit `4c194ae`) — warning-only, logged to `deferred-items.md`.
- Pre-existing `cb-backend/src/cpu_runtime.rs:696/:1025` clippy indexing/slicing errors (06.5-03) — block the transitive lint compile of cb-train but are not in this plan's code; already in `deferred-items.md`.

## Verification

- `cargo test -p cb-compute --lib embedding_calcers_test` — 18/18 (8 LDA + 10 KNN)
- `cargo test -p cb-train --lib estimated::online_embedding` — 14/14 (8 LDA + 6 KNN)
- `cargo test -p cb-train --lib` — 224/224 (non-embedding path byte-identical, no regression; was 217 in Plan 05 + new KNN tests)
- `cargo test -p cb-oracle --test knn_oracle_test` — 5/5 (neighbor-id EXACT + per-stage ≤1e-5; NO #[ignore])
- `cargo test -p cb-oracle` — full suite green (knn 5/5, lda 5/5, all others unchanged)
- `cargo clippy -p cb-compute --lib` / new KNN code in `-p cb-train --lib` — 0 indexing_slicing/unwrap_used/expect_used/panic in new lib files (the only 2 clippy errors are the pre-existing cb-backend lints)
- No `hnsw`/`ann`/`nmslib`/`faiss`/`kdtree`/`hora`/`instant-distance` in any `Cargo.toml` (A2/D-05 verified)
- `catboost-master/` vendored instrumentation patches remain untracked (D-09/D-12)

## Known Stubs

None. The KNN calcer is fully wired (both offline Plain and online ordered paths) and oracle-gated at the EXACT neighbor-id + bit-exact feature + byte-identical per-stage level. The SC-4 end-to-end mixed text+embedding pool integration (a separate concern) is Plan 07.

## FEAT-02 status

FEAT-02 complete: LDA (Plan 05, documented-tolerance projection + binarization-stability per-stage parity) + KNN (this plan, EXACT neighbor ids + bit-exact votes + byte-identical per-stage) both produce upstream-matching embedding encodings, computed leakage-controlled online over the TFold permutation (D-03). SC-3 closed.

## Self-Check: PASSED

- Created file `crates/cb-oracle/tests/knn_oracle_test.rs` verified present on disk.
- All 3 per-task commits verified in git history: `1de27db` (calcer), `a4ddc36` (online wiring), `be241fa` (oracle).
- No third-party HNSW/ANN crate in any Cargo.toml; `catboost-master/` patches untracked.

---
*Phase: 06.5-text-and-embedding-features*
*Completed: 2026-06-18*
