---
phase: 06.5-text-and-embedding-features
plan: 09
type: execute
wave: 9
depends_on: ["06.5-08"]
files_modified:
  - crates/cb-train/src/estimated/online_text.rs
  - crates/cb-train/src/estimated/online_text_test.rs
  - crates/cb-train/src/estimated/estimated_features.rs
  - crates/cb-train/src/estimated/estimated_features_test.rs
  - crates/cb-oracle/tests/bm25_oracle_test.rs
  - .planning/phases/06.5-text-and-embedding-features/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [FEAT-01]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "BM25 reaches the SC-2 per-stage parity bar: EITHER (PATH-A) the BM25 per-stage trained-model oracle (bm25_oracle_splits_match_upstream / _leaf_values_match_upstream / _staged_approx_match_upstream / _predictions_match_upstream) is ≤1e-5 GREEN against the frozen catboost 1.2.10 fixture crates/cb-oracle/fixtures/text_calcers/BM25/ (splits.npy ±1.24 reproduced), OR (PATH-B) the bit-exact-reproducible BM25 parity contract recorded in BM25-NORMALIZATION-DECISION.md is gated HARD ≤1e-5 (StagedApprox + Predictions, plus a necessary-and-sufficient structure/binarization-invariant gate) and the irreducible normalization artifact is documented, owned, and human-signed-off (FEAT-01, SC-2)"
    - "NO #[ignore], NO weakened tolerance on model outputs (StagedApprox/Predictions are HARD ≤1e-5 under BOTH paths); the BM25 calcer math + online read-before-update prefix remain bit-exact ≤1e-5 (no regression to the existing 3 calcer-encoding tests)"
    - "FEAT-01 is fully OWNED: REQUIREMENTS.md FEAT-01, ROADMAP SC-2, and deferred-items.md reflect the closure (PATH-A: BM25 per-stage closed; PATH-B: the residual is an owned signed-off scope decision, NOT a silent deferral) — no later phase is left silently holding the gap"
  artifacts:
    - path: "crates/cb-oracle/tests/bm25_oracle_test.rs"
      provides: "BM25 per-stage trained-model oracle gated against the frozen BM25 fixture (PATH-A: full Splits/LeafValues/StagedApprox/Predictions ≤1e-5; PATH-B: HARD StagedApprox/Predictions + the documented owned-scope gate)"
      contains: "bm25_oracle"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FEAT-01 status updated to reflect SC-2/BM25 closure"
      contains: "FEAT-01"
  key_links:
    - from: "crates/cb-oracle/tests/bm25_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/text_calcers/BM25/"
      via: "compare_stage ≤1e-5 against splits.npy / leaf_values.npy / staged.npy / predictions.npy"
      pattern: "text_calcers/BM25"
    - from: "crates/cb-oracle/tests/bm25_oracle_test.rs"
      to: "crates/cb-train/src/estimated/estimated_features.rs"
      via: "build_online_text_estimated_features → train → per-stage compare (mirror naive_bayes_oracle_test)"
      pattern: "build_online_text_estimated_features"
---

<objective>
Close the BM25 per-stage gap (SC-2 / FEAT-01) by EXECUTING the path the 06.5-08 investigation recorded in BM25-NORMALIZATION-DECISION.md:

- **PATH-A (preferred):** implement the located estimated-feature value-normalization (the transform / border-algorithm / multi-permutation averaging Task 1+2 identified) in the cb-train estimated-feature seam so the stored BM25 border scale reaches ≤1e-5 parity with splits.npy (±1.24), and add the BM25 per-stage trained-model oracle mirroring naive_bayes_oracle_test.rs — fully satisfying SC-2 as worded.
- **PATH-B (only if 06.5-08 proved the normalization upstream-irreducible):** gate the bit-exact-reproducible BM25 parity contract (StagedApprox/Predictions ≤1e-5 HARD + the necessary-and-sufficient structure/binarization-invariant gate recorded in DECISION.md), document the irreducible artifact in-file with the upstream evidence, and convert the deferral into a human-signed-off owned scope — updating REQUIREMENTS.md / ROADMAP SC-2 / deferred-items.md so the residual is OWNED, not silently deferred (mirroring the SC-3 LDA / SC-4 precedent).

Purpose: The verifier rejected a bare re-deferral as a BLOCKER. This plan converts the 06.5-08 evidence into either a reproduced normalization (A) or a documented, owned, signed-off scope (B). Under BOTH paths the model OUTPUTS (StagedApprox/Predictions) are HARD ≤1e-5 — only the path differs.
Output: BM25 per-stage oracle + (A) the normalization implementation OR (B) the documented owned-scope + doc updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
@.planning/phases/06.5-text-and-embedding-features/06.5-VERIFICATION.md
@.planning/phases/06.5-text-and-embedding-features/deferred-items.md
@.planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md
@.planning/phases/06.5-text-and-embedding-features/06.5-08-SUMMARY.md
@.planning/phases/06.5-text-and-embedding-features/06.5-04-SUMMARY.md
</context>

## Phase Goal

All six text and embedding calcers produce upstream-matching encodings ≤1e-5. This plan closes the final FEAT-01 item: the BM25 per-stage trained-model parity (SC-2).

## Acceptance Criteria
- [ ] The path recorded in BM25-NORMALIZATION-DECISION.md (A or B) is executed faithfully — no path-switching without re-investigation
- [ ] BM25 per-stage oracle gates the frozen fixture: PATH-A → Splits/LeafValues/StagedApprox/Predictions all ≤1e-5; PATH-B → StagedApprox/Predictions ≤1e-5 HARD + the documented necessary-and-sufficient gate
- [ ] FEAT-01 / SC-2 / deferred-items.md reflect the closure (owned, not silently deferred)
- [ ] NO #[ignore], NO weakened tolerance on model outputs; calcer-math + online-prefix tests still ≤1e-5

<tasks>

<task type="auto">
  <name>Task 1: Execute the recorded path — normalization impl (A) or owned-scope contract (B)</name>
  <read_first>
    - .planning/phases/06.5-text-and-embedding-features/BM25-NORMALIZATION-DECISION.md (THE GATE — read "## DECISION:" FIRST; it dictates A vs B and carries the reproduction recipe / irreducibility evidence + the necessary-and-sufficient parity contract)
    - .planning/phases/06.5-text-and-embedding-features/06.5-08-SUMMARY.md (the DECISION restated for the executor)
    - crates/cb-train/src/estimated/online_text.rs (the online/offline text-feature seam — online_text_prefix lines 188-281, offline_text_features lines 76-139; PATH-A normalization lands HERE or in estimated_features.rs, applied to the f32 columns before quantization; integer counts stay exact, float reductions via cb_core::sum_f64, D-04/D-08)
    - crates/cb-train/src/estimated/estimated_features.rs (build_online_text_estimated_features lines 239-294 — where columns flow into select_borders_greedy_logsum; PATH-A: insert the value transform / averaging here or in online_text.rs before the quantizer call; the multi-permutation averaging, if that is the recipe, iterates the fold permutations like ctr/online.rs)
    - crates/cb-train/src/ctr/online.rs (the multi-permutation ordered-averaging ANALOG if DECISION recipe is permutation averaging — read lines 284-336 for the per-permutation prefix loop + averaging shape)
    - crates/cb-train/src/fold.rs (Fold.permutation + permutation_count=4 — the SAME permutations CTRs use; PATH-A averaging must reuse these, never a fresh RNG)
    - crates/cb-data/src/borders.rs (select_borders_greedy_logsum line 209 — PATH-A: if the located mechanism is a transformed border STORAGE space, the borders are computed in that space; do NOT fork a parallel quantizer — SC-4 discipline, reuse this)
    - catboost-master/catboost/private/libs/algo/estimated_features.cpp + fold.cpp (the upstream symbols DECISION.md cites — transcribe the exact transform/averaging verbatim, D-04)
    - crates/cb-core/src/reduction.rs (sum_f64 line 32 — all float reductions in the normalization route here; D-08)
    - CLAUDE.md (source/test separation MANDATORY — normalization unit tests go in the *_test.rs siblings, NOT in the source files; no unwrap/expect/panic/index in production)
  </read_first>
  <behavior>
    PATH-A (DECISION: PATH-A):
    - the normalization transform reproduces the dumped pre-quantization estimated-feature values from the raw BM25 online/averaged columns ≤1e-5 (unit test vs the instrumented dump recorded in DECISION.md)
    - the borders selected over the normalized columns reproduce splits.npy (±1.24 / -0.550486) ≤1e-5
    - the no-text / numeric-only path stays byte-identical (D-04 inert-when-absent) — the transform is applied ONLY to estimated columns
    PATH-B (DECISION: PATH-B):
    - the bit-exact-reproducible parity contract from DECISION.md is the production behavior: the BM25 estimated column + existing quantizer produce StagedApprox/Predictions that match the fixture ≤1e-5 (these are the structure-invariant model outputs the fixture shows are clean: predictions are ±0.90702464, staged is the clean boosting trajectory)
    - the necessary-and-sufficient gate (e.g. the BM25 column's relative document ordering / binarization stability that makes the borders induce upstream's partition regardless of the stored ±1.24 scale) holds
  </behavior>
  <action>
    Read BM25-NORMALIZATION-DECISION.md "## DECISION:" and branch:

    IF DECISION: PATH-A — Implement the located normalization (the transform / border-algorithm / multi-permutation ordered averaging the investigation identified) in the estimated-feature seam, transcribed verbatim from the upstream symbols DECISION.md cites (estimated_features.cpp / fold.cpp, D-04). Apply it ONLY to the estimated (calcer-output) columns before they reach select_borders_greedy_logsum — reuse the EXISTING quantizer (SC-4, no parallel quantizer); if the located mechanism is a transformed border-storage space, compute borders in that space. If the recipe is multi-permutation averaging, iterate the fold's permutations (fold.rs, permutation_count=4) like ctr/online.rs — never a fresh RNG. All float reductions via cb_core::sum_f64; integer class counts exact (D-04/D-08). Add a unit test (online_text_test.rs / estimated_features_test.rs sibling) asserting the normalized column reproduces the instrumented dump ≤1e-5 AND the selected borders reproduce splits.npy ≤1e-5. Keep the no-text/numeric-only path byte-identical (D-04). Checked .get(..), typed CbError, no unwrap/index.

    IF DECISION: PATH-B — Do NOT implement an unrecoverable normalization. Instead implement the documented owned-scope: ensure the BM25 estimated column + existing quantizer produce the structure-invariant model outputs (StagedApprox/Predictions) that match the fixture ≤1e-5, and add the necessary-and-sufficient gate DECISION.md specifies (e.g. the BM25 column's relative document-ordering / binarization-stability gate that makes the partition upstream-identical regardless of the irreducible ±1.24 stored scale — mirror the SC-3 LDA binarization-stability gate and the SC-4 structure-invariant Splits/LeafValues gate). Document the irreducible normalization artifact in-file (module docs of online_text.rs and the oracle test) with the upstream file:line evidence from DECISION.md. No production normalization code that cannot be reproduced; no #[ignore]; no weakened tolerance on StagedApprox/Predictions.

    Under BOTH paths: do not regress the 3 existing BM25 calcer-encoding tests (bm25_oracle_columns_match_closed_form, _first_prefix_is_empty_no_leakage, _borders_selected_through_existing_quantizer).
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib estimated:: && cargo clippy -p cb-train --lib 2>&1 | grep -v '^#' | grep -Ec 'unwrap_used|expect_used|panic|indexing_slicing' | grep -qx 0</automated>
  </verify>
  <done>The recorded path is executed: PATH-A normalization reproduces the dump + splits.npy ≤1e-5 (unit-tested) with the no-text path byte-identical; OR PATH-B's owned-scope contract (StagedApprox/Predictions ≤1e-5 + the necessary-and-sufficient gate) is implemented and the irreducible artifact documented in-file. cb-train lib green, new lib code clippy-clean of the 4 restriction lints, source/test separation honored.</done>
  <acceptance_criteria>cargo test -p cb-train --lib estimated:: exits 0 (no regression + the new normalization/owned-scope unit test); cargo clippy -p cb-train --lib reports 0 of unwrap_used/expect_used/panic/indexing_slicing in the modified source files; the executed path matches DECISION.md (no silent path-switch)</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BM25 per-stage trained-model oracle (mirror naive_bayes_oracle_test) + own the requirement</name>
  <read_first>
    - crates/cb-oracle/tests/naive_bayes_oracle_test.rs (THE PATTERN TO MIRROR — full file: nb_params() pinned config lines 112-145, train_nb() end-to-end lines 149-180, canonical_stages() depth-2→distinct-split reconciliation lines 186-244, the 4 per-stage tests _splits/_leaf_values/_staged_approx/_predictions lines 320-362, the per-prefix leakage anchor lines 278-317)
    - crates/cb-oracle/tests/bm25_oracle_test.rs (the CURRENT BM25 oracle — 3 calcer-encoding tests to KEEP; this task ADDS the per-stage trained-model tests; the module docs currently say the normalized borders are "outside scope" — update them to reflect the closure path A or B)
    - crates/cb-oracle/fixtures/text_calcers/BM25/ (splits.npy ±1.24, leaf_values.npy, leaf_weights.npy [7,2,0,7], staged.npy clean trajectory, predictions.npy ±0.90702464, params.json, model.cbm)
    - crates/cb-oracle/src/compare.rs (compare_stage line 84, Stage::{Splits,LeafValues,StagedApprox,Predictions} lines 14-20, assert_abs_close line 46) + lib.rs (load_f64_vec)
    - crates/cb-train/src/estimated/estimated_features.rs (build_online_text_estimated_features — the entry the oracle trains through, now with the Task-1 normalization for PATH-A)
    - .planning/REQUIREMENTS.md (FEAT-01 line 63 — update status) and .planning/ROADMAP.md (SC-2 wording line 443; Phase 6.5 plan list + count) and deferred-items.md (the "BM25 per-stage normalized-border scale" section to resolve)
  </read_first>
  <behavior>
    - the BM25 model trains end-to-end through build_online_text_estimated_features (BM25 calcer) → existing quantizer → tree, with the pinned BM25 params (Logloss, iterations=5, depth=2, lr=0.3, Plain, bootstrap=No, seed=20260618)
    - PATH-A: bm25_oracle_splits_match_upstream / _leaf_values_match_upstream / _staged_approx_match_upstream / _predictions_match_upstream ALL ≤1e-5 vs the frozen fixture (canonical depth-2→distinct-split reconciliation as in naive_bayes_oracle_test if the tree re-selects the feature)
    - PATH-B: bm25_oracle_staged_approx_match_upstream + bm25_oracle_predictions_match_upstream ≤1e-5 HARD vs staged.npy/predictions.npy; the Splits/LeafValues gated by the documented necessary-and-sufficient structure/binarization-invariant gate from DECISION.md (orientation/scale-invariant where the ±1.24 stored scale is irreducible — magnitudes/partition exact)
    - no #[ignore], no weakened tolerance on StagedApprox/Predictions under either path
  </behavior>
  <action>
    Extend crates/cb-oracle/tests/bm25_oracle_test.rs mirroring naive_bayes_oracle_test.rs: add bm25_params() (pinned to fixtures/text_calcers/BM25/params.json), train_bm25() (build_online_text_estimated_features for BM25 → train → staged buffer), and the per-stage tests. PATH-A: add bm25_oracle_splits_match_upstream, _leaf_values_match_upstream, _staged_approx_match_upstream, _predictions_match_upstream — each compare_stage ≤1e-5 vs the frozen .npy (with the canonical_stages depth-2→distinct-split reconciliation if needed). PATH-B: add bm25_oracle_staged_approx_match_upstream + _predictions_match_upstream as HARD ≤1e-5 vs staged.npy/predictions.npy, and add the necessary-and-sufficient structure/binarization-invariant gate test DECISION.md specifies for Splits/LeafValues (document in the test why the ±1.24 stored scale is the irreducible artifact, citing DECISION.md's upstream file:line). KEEP the 3 existing calcer-encoding tests. Update the bm25_oracle_test.rs module docs to state the closure (path A: per-stage closed; path B: owned-scope with the documented irreducible artifact). Then OWN the requirement: update REQUIREMENTS.md FEAT-01 status (PATH-A → text calcers BoW/NaiveBayes/BM25 per-stage ≤1e-5 complete; PATH-B → BM25 per-stage owned with a signed-off documented-irreducible normalization scope), update ROADMAP SC-2 wording + the Phase 6.5 plan list (add 06.5-08, 06.5-09; bump count to 9) + mark FEAT-01 closure, and resolve the deferred-items.md "BM25 per-stage normalized-border scale" section (PATH-A: mark closed with the implementing plan; PATH-B: convert to an owned signed-off scope decision, NOT an open deferral). NO #[ignore], NO weakened tolerance on model outputs.
  </action>
  <verify>
    <automated>cargo test -p cb-oracle --test bm25_oracle_test 2>&1 | grep -Eq 'test result: ok' && cargo test -p cb-oracle --test bm25_oracle_test 2>&1 | grep -q '0 ignored'</automated>
  </verify>
  <done>bm25_oracle_test runs the 3 existing calcer-encoding tests + the new per-stage trained-model tests, all GREEN, 0 ignored: PATH-A → Splits/LeafValues/StagedApprox/Predictions ≤1e-5; PATH-B → StagedApprox/Predictions ≤1e-5 HARD + the documented necessary-and-sufficient gate. REQUIREMENTS.md FEAT-01, ROADMAP SC-2 + plan list, and deferred-items.md all reflect the owned closure.</done>
  <acceptance_criteria>cargo test -p cb-oracle --test bm25_oracle_test exits 0 with 0 ignored and the per-stage test names present (grep bm25_oracle_staged_approx_match_upstream / bm25_oracle_predictions_match_upstream); REQUIREMENTS.md FEAT-01 line no longer reads "In Progress … deferred"; ROADMAP SC-2 + plan list updated; deferred-items.md BM25 section resolved (closed or owned-signed-off, not open)</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the BM25 SC-2 closure (path-A full per-stage, or path-B owned signed-off scope)</name>
  <action>Pause for human verification of the BM25 SC-2 closure. Present the <what-built> summary and the <how-to-verify> steps; do NOT auto-advance (this checkpoint is a blocking SC-2/FEAT-01 closure gate equivalent to the SC-3 LDA and SC-4 signed-off scope decisions, and is never auto-approvable). Resume only on the <resume-signal>.</action>
  <what-built>
    The BM25 per-stage closure executed along the path 06.5-08 recorded:
    - PATH-A: the estimated-feature value-normalization reproduces splits.npy (±1.24) ≤1e-5 and the full BM25 per-stage oracle (Splits/LeafValues/StagedApprox/Predictions) is GREEN.
    - PATH-B: the bit-exact-reproducible parity contract (StagedApprox/Predictions ≤1e-5 HARD + the necessary-and-sufficient structure/binarization-invariant gate) is GREEN, the irreducible normalization artifact is documented in-file with upstream evidence, and REQUIREMENTS.md / ROADMAP SC-2 / deferred-items.md are updated to OWN the residual.
    This is the SC-2 / FEAT-01 closure gate — equivalent to the SC-3 LDA and SC-4 human-signed-off scope decisions.
  </what-built>
  <how-to-verify>
    1. Open BM25-NORMALIZATION-DECISION.md and confirm the executed path matches the recorded "## DECISION:" (no silent path-switch).
    2. Run `cargo test -p cb-oracle --test bm25_oracle_test` — confirm all tests pass, 0 ignored.
    3. Read the bm25_oracle_test.rs module docs + the new per-stage tests.
       - PATH-A: confirm splits.npy ±1.24 is reproduced ≤1e-5 (not papered over).
       - PATH-B: confirm StagedApprox/Predictions are HARD ≤1e-5 AND the documented irreducible-artifact justification cites DECISION.md's upstream file:line evidence (the residual is genuinely upstream-irreducible, mirroring the LDA CLAPACK precedent) — NOT a convenience deferral.
    4. Confirm REQUIREMENTS.md FEAT-01, ROADMAP SC-2, and deferred-items.md reflect the closure (owned, not silently deferred).
    5. SIGN OFF that the path-B scope (if taken) is an acceptable owned residual, or that path-A fully closes SC-2.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the BM25 SC-2 closure (path A full per-stage, or path B owned-signed-off scope), or describe what must change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estimated-feature normalization (PATH-A) → existing quantizer | a transform applied to the wrong columns / in the wrong space would corrupt the numeric-only path or fork a parallel quantizer (SC-4 violation) |
| DECISION.md path → implementation | executing the wrong path (A when B was recorded, or vice-versa) would either chase an irreducible artifact or silently under-deliver |
| owned-scope (PATH-B) → REQUIREMENTS/ROADMAP | an undocumented or unsigned scope reduction would be a silent deferral (the verifier's BLOCKER) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-09-01 | Integrity (parity) | PATH-A normalization applied to wrong columns / space | mitigate | Transform applied ONLY to estimated columns, reusing the existing select_borders_greedy_logsum (SC-4, no parallel quantizer); D-04 no-text path byte-identical, unit-tested; borders unit-tested vs splits.npy ≤1e-5 |
| T-06.5-09-02 | Integrity (false closure) | PATH-B owned-scope hiding a reproducible gap | mitigate | PATH-B is taken ONLY when 06.5-08 proved irreducibility with dump evidence; the blocking human-verify checkpoint forces a signoff that the residual is genuinely upstream-irreducible (LDA/SC-4 precedent) and StagedApprox/Predictions stay HARD ≤1e-5 |
| T-06.5-09-03 | Repudiation (silent deferral) | requirement left in limbo | mitigate | Task 2 MANDATORY-updates REQUIREMENTS.md FEAT-01 + ROADMAP SC-2 + deferred-items.md; the checkpoint verifies the residual is OWNED, not deferred |
| T-06.5-09-04 | Denial of Service (panic) | estimated-column normalization on degenerate input | mitigate | Checked .get(..); typed CbError on length/index mismatch (mirror online_text.rs); no unwrap/index; clippy 0 restriction lints |
| T-06.5-09-SC | Tampering | no package install | accept | No new crates (D-05) |
</threat_model>

<verification>
- The executed path matches BM25-NORMALIZATION-DECISION.md "## DECISION:" (no silent path-switch)
- `cargo test -p cb-oracle --test bm25_oracle_test` GREEN, 0 ignored: PATH-A full per-stage ≤1e-5 (splits.npy ±1.24 reproduced) OR PATH-B StagedApprox/Predictions ≤1e-5 HARD + the documented necessary-and-sufficient gate
- `cargo test -p cb-train --lib` GREEN (no regression); the 3 existing BM25 calcer-encoding tests still ≤1e-5; D-04 no-text path byte-identical
- NO #[ignore], NO weakened tolerance on model outputs; new lib code clippy-clean of the 4 restriction lints; source/test separation honored
- REQUIREMENTS.md FEAT-01, ROADMAP SC-2 + plan list, deferred-items.md updated to OWN the closure
- Blocking human-verify checkpoint signed off
</verification>

<success_criteria>
SC-2 / FEAT-01 is CLOSED and OWNED: BM25 reaches the per-stage parity bar via the evidence-driven path 06.5-08 recorded — either a reproduced estimated-feature normalization with the full BM25 per-stage oracle ≤1e-5 (PATH-A), or a documented, human-signed-off owned scope gating the bit-exact-reproducible parity contract (StagedApprox/Predictions ≤1e-5 HARD + a necessary-and-sufficient invariant gate) with the irreducible artifact evidenced from upstream source (PATH-B). No #[ignore], no weakened model-output tolerance, no silent re-deferral. FEAT-01 fully accounted for across REQUIREMENTS.md / ROADMAP / deferred-items.md.
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-09-SUMMARY.md` when done.
The SUMMARY MUST state which path (A or B) was executed and the resulting SC-2/FEAT-01 status.
</output>
