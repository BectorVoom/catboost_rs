---
phase: 06.5
slug: text-and-embedding-features
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-18
---

# Phase 06.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` (separate `*_test.rs` files) + cb-oracle per-stage comparator ≤1e-5 |
| **Config file** | none — Cargo workspace; oracle fixtures under `crates/cb-oracle` corpora |
| **Quick run command** | `cargo test -p cb-data -p cb-compute --tests` (calcer math + tokenizer/dictionary units) |
| **Full suite command** | `cargo test -p cb-data -p cb-compute -p cb-train -p cb-oracle --tests` |
| **Estimated runtime** | ~60–180 seconds (per-crate; cb-compute full profile may not link under disk pressure — verify per-crate) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command for the touched crate(s).
- **After every plan wave:** Run the full suite command.
- **Before `/gsd-verify-work`:** Full suite green + every calcer's per-stage oracle ≤1e-5.
- **Max feedback latency:** ~180 seconds.

---

## Per-Task Verification Map

> Filled per-plan by the planner. Each calcer slice carries a per-stage oracle (borders/splits/leaf/staged/prediction ≤1e-5) plus, where Python ground truth is thin, an instrumented `CB_INSTRUMENT_LOG` D-07 oracle (token stream / dictionary ids / per-calcer intermediate encodings / online-estimation order).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.5-01-01 | 01 | 1 | FEAT-01 | — | N/A (parity lib, no external input surface) | oracle | `cargo test -p cb-oracle tokenizer` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Validation Architecture (per Success Criterion)

| SC | What must be TRUE | Observable signal / oracle | Sampling granularity |
|----|-------------------|----------------------------|----------------------|
| SC-1 | Tokenizer/dictionary stream bit-identical (D-01 first gate) | Instrumented `CB_INSTRUMENT_LOG` token-stream + dictionary token→id dump vs Rust tokenizer; bit-exact | Per-token / per-dictionary-entry (D-07) |
| SC-2 | BoW, NaiveBayes, BM25 encodings ≤1e-5 | catboost 1.2.10 (`thread_count=1`) per-stage fixture (borders/splits/leaf/staged/prediction) + instrumented per-calcer intermediate encoding dump | Per-stage + per-calcer-feature |
| SC-3 | LDA, KNN encodings ≤1e-5 | Same per-stage oracle; LDA projection dump + KNN neighbor-id dump (instrumented, LOW-confidence — may need documented tolerance escalation) | Per-stage + per-projection/neighbor |
| SC-4 | Pool→calcer→quantize→tree flow | End-to-end trained-model per-stage oracle through the existing quantize→tree path; no Pool schema change | Per-stage end-to-end |

---

## Wave 0 Requirements

- [ ] Tokenizer/dictionary instrumented ground-truth fixture (`CB_INSTRUMENT_LOG` token stream + dict ids) — regenerated single-threaded (`thread_count=1`), NOT consumed from `-T 4` canondata.
- [ ] Per-calcer oracle corpora (BoW / NaiveBayes / BM25 / LDA / KNN) trained with catboost 1.2.10 single-threaded.
- [ ] Instrumented trainer reuse: warm `/tmp/cb_build313` + clang-18, `build_instrumented_trainer.sh` incremental rebuild with calcer-internal hooks.

*Existing cb-oracle harness (fixture/compare/model_json) covers per-stage comparison; only calcer-specific corpora + instrumentation hooks are new.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Instrumented-trainer ground-truth regeneration | FEAT-01/02 | RUN-ONCE OFFLINE build; human-gated per D-07 / 06.3 discipline | Run `build_instrumented_trainer.sh` (incremental), set `CB_INSTRUMENT_LOG`, train fixture, freeze JSONL groundtruth; no fabricated fixtures, no weakened tolerance |
| LDA/KNN tolerance escalation decision | FEAT-02 | LAPACK f32 eigen / HNSW approximate-index reproducibility is uncertain — if ≤1e-5 unreachable, a documented tolerance needs human sign-off | Spike instrumented projection/neighbor dumps first; escalate-don't-weaken |

---

## Validation Sign-Off

- [ ] All tasks have automated oracle verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (calcer corpora + instrumentation hooks)
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
