---
phase: 06.5-text-and-embedding-features
verified: 2026-06-18T00:00:00Z
status: passed
score: 4/4 success criteria verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/4
  gaps_closed:
    - "SC-2: BM25 per-stage trained-model oracle (Splits/LeafValues/StagedApprox/Predictions ≤1e-5) — closed by 06.5-08 (fixture-mislabel investigation, PATH-A) + 06.5-09 (text-only fixture regeneration + per-stage oracle). 7/7 bm25_oracle_test tests pass, 0 ignored."
  gaps_remaining: []
  regressions: []
---

# Phase 6.5: Text & Embedding Features — Verification Report (Re-verification)

**Phase Goal:** All six text and embedding calcers produce upstream-matching encodings ≤1e-5, with tokenizer parity nailed first as the load-bearing risk (D-11). Success Criteria: SC-1 tokenizer parity bit-identical; SC-2 text calcers BoW/NaiveBayes/BM25 ≤1e-5; SC-3 embedding calcers LDA/KNN ≤1e-5; SC-4 text/embedding columns flow Pool→calcer→quantized features into the existing tree path.
**Verified:** 2026-06-18
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 06.5-08 and 06.5-09 closed the SC-2/FEAT-01 BM25 per-stage gap)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Tokenizer parity — upstream text-processing token stream reproduced bit-identical | VERIFIED | `tokenizer_oracle_test`: 3 passed, 0 failed, 0 ignored (confirmed by running test) |
| SC-2 | Text calcers BoW, NaiveBayes, BM25 produce upstream-matching encodings ≤1e-5 | VERIFIED | `bow_oracle_test` 4/4; `naive_bayes_oracle_test` 5/5; `bm25_oracle_test` **7/7 including 4 new per-stage gates** (splits/leaf_values/staged_approx/predictions ≤1e-5). 0 ignored across all three. Confirmed by running all three test suites. |
| SC-3 | Embedding calcers LDA, KNN produce upstream-matching encodings ≤1e-5 | VERIFIED | `lda_oracle_test` 5/5 (binarization stability, 0/16 crossings, documented raw-projection tolerance); `knn_oracle_test` 5/5 (neighbor ids 0/64 mismatches). 0 ignored. No regression. |
| SC-4 | Text/embedding columns flow Pool→calcer→quantized features into the existing tree path ≤1e-5 | VERIFIED | `text_embedding_end_to_end_oracle_test` 5/5 (StagedApprox + Predictions ≤1e-5 HARD; structure-invariant Splits/LeafValues gating under degenerate-separating corpus). 0 ignored. No regression. |

**Score:** 4/4 success criteria verified

---

## Gap Closure Verification — SC-2 / FEAT-01 (BM25)

### The Gap (from prior VERIFICATION.md)

The prior verification found SC-2 PARTIAL: BoW and NaiveBayes per-stage oracles were green ≤1e-5, but the BM25 per-stage trained-model oracle (Splits/LeafValues/StagedApprox/Predictions) did not exist. The stored `splits.npy` contained ±1.24 borders that could not be reproduced from the raw O(1e-3) BM25 calcer column.

### Resolution Path — PATH-A

**Plan 06.5-08 (investigation gate):** Source-read the entire estimated-feature value→border chain in catboost 1.2.10 (`base_text_feature_estimator.h:74-88` → `estimated_features.cpp:204-250` → `split.cpp:45-46` → `model.cpp:209`) and ran a RUN-ONCE instrumented dump (`cb_instr_estimated_borders`). Finding: the BM25 estimated-feature borders are O(1e-3), scale-preserving with no transform. The ±1.24 / -0.550486 `splits.npy` borders all carried `calcer_id=96AE6D4D…` — the **default embedding calcer on `emb0`**, not the BM25 text calcer (`4559D4B0…`). The original BM25 fixture pool wrongly included `embedding_features=["emb0"]`; the well-separated embedding clouds dominated the split search. PATH-A declared: there is NO BM25 normalization; the original deferral rested on a fixture mislabel.

**Plan 06.5-09 (execution — PATH-A fixture-correctness fix):**
- `gen_text_embedding_fixtures.py::_make_pool(text_only=True)` drops `emb0` from the text-calcer fixture path
- BM25/BoW/NaiveBayes fixtures regenerated from the instrumented single-thread trainer; BoW/NaiveBayes `.npy` arrays returned byte-identical (zero regression)
- BM25 `splits.npy` now contains genuine O(1e-3) BM25 text-feature borders (`0.00248965, 0.00127047, …`, `calcer_id=0BDFE5…`)
- Four new per-stage tests added to `bm25_oracle_test.rs`: `bm25_oracle_splits_match_upstream`, `bm25_oracle_leaf_values_match_upstream`, `bm25_oracle_staged_approx_match_upstream`, `bm25_oracle_predictions_match_upstream`
- No production trainer code changed; the existing Rust seam already produced O(1e-3) borders

**Important deviation recorded:** The 06.5-09 PLAN.md prose assumed implementing a BM25 normalization transform (written before 06.5-08 concluded). PATH-A established there is no such transform. The DEVIATION was recorded in 06.5-09-SUMMARY.md; the binding DECISION doc (BM25-NORMALIZATION-DECISION.md) was treated as authoritative. No scope was silently dropped.

### Evidence of Closure — Actual Test Run

```
cargo test -p cb-oracle --test bm25_oracle_test

running 7 tests
test bm25_oracle_first_prefix_is_empty_no_leakage ... ok
test bm25_oracle_borders_selected_through_existing_quantizer ... ok
test bm25_oracle_columns_match_closed_form ... ok
test bm25_oracle_leaf_values_match_upstream ... ok
test bm25_oracle_predictions_match_upstream ... ok
test bm25_oracle_staged_approx_match_upstream ... ok
test bm25_oracle_splits_match_upstream ... ok

test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.22s
```

7/7 passed, 0 ignored, 0 failed. The 4 new per-stage tests (splits/leaf_values/staged_approx/predictions) gate the full BM25 trained-model oracle ≤1e-5 against the regenerated text-only fixtures.

### Regression Check — BoW and NaiveBayes (Unchanged per PATH-A)

```
cargo test -p cb-oracle --test bow_oracle_test
test result: ok. 4 passed; 0 failed; 0 ignored

cargo test -p cb-oracle --test naive_bayes_oracle_test
test result: ok. 5 passed; 0 failed; 0 ignored
```

Zero regression. BoW/NaiveBayes fixture `.npy` arrays returned byte-identical on regeneration.

---

## SC-1 / SC-3 / SC-4 Non-Regression

All previously-VERIFIED success criteria remain green with no regressions:

| Test Suite | Result | Status |
|------------|--------|--------|
| `tokenizer_oracle_test` | 3 passed, 0 failed, 0 ignored | PASS |
| `bow_oracle_test` | 4 passed, 0 failed, 0 ignored | PASS |
| `naive_bayes_oracle_test` | 5 passed, 0 failed, 0 ignored | PASS |
| `bm25_oracle_test` | **7 passed** (was 3), 0 failed, 0 ignored | PASS (gap closed) |
| `lda_oracle_test` | 5 passed, 0 failed, 0 ignored | PASS |
| `knn_oracle_test` | 5 passed, 0 failed, 0 ignored | PASS |
| `text_embedding_end_to_end_oracle_test` | 5 passed, 0 failed, 0 ignored | PASS |

**#[ignore] scan:** Grep for actual `#[ignore]` attributes across all 7 oracle test files returned zero matches (the grep matches found were all within doc comments asserting their absence, not actual attribute occurrences). All oracle tests are live.

**Tolerance verification:** `compare_stage` in `crates/cb-oracle/src/compare.rs:85` is hardwired to `1e-5` and cannot be overridden by call sites. No weakened tolerance is possible through this function.

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| FEAT-01 | 06.5-01 through 06.5-04, 06.5-08, 06.5-09 | Text features — tokenization → BoW, NaiveBayes, BM25 calcers | COMPLETE | REQUIREMENTS.md line 175 marks Complete; all three calcers per-stage ≤1e-5 confirmed by running tests |
| FEAT-02 | 06.5-01, 06.5-05, 06.5-06, 06.5-07 | Embedding features — LDA, KNN calcers, mixed end-to-end | COMPLETE | REQUIREMENTS.md line 176 marks Complete; no regression in lda/knn/e2e oracle suites |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `crates/cb-train/src/estimated/online_embedding_test.rs` | 13 | `const DIM: usize = 4;` dead constant (warning-only, not a denied lint) | INFO | Pre-existing from 06.5-05, logged in deferred-items.md; warning does not affect the build or any test; workspace `cargo check` emits exactly 1 warning |

No `TBD` / `FIXME` / `XXX` / `TODO` / `#[ignore]` / `return null` / `return []` debt markers found in any new oracle test files, fixture generator, or library files touched by plans 06.5-08 or 06.5-09.

---

## Committed Work Verification

| Commit | Content | Status |
|--------|---------|--------|
| `3426145` | PATH-A fixture-correctness fix — `_make_pool(text_only=True)`, regenerated BM25/BoW/NaiveBayes fixtures | CONFIRMED in git log |
| `0e87a1a` | BM25 per-stage oracle (7 tests) + REQUIREMENTS.md / ROADMAP.md / deferred-items.md SC-2/FEAT-01 closure | CONFIRMED in git log |

ROADMAP.md SC-2 bullet is annotated `CLOSED (06.5-09)`. REQUIREMENTS.md FEAT-01 row reads `Complete`. deferred-items.md BM25 section is annotated `CLOSED (06.5-09, PATH-A)`.

---

## Known Residuals (Not Blockers for FEAT-01/FEAT-02)

### General estimated-feature quantization-GRID parity

The SC-4 join (pool→calcer→quantize→tree pipeline) is closed. The general estimated-feature stored-border VALUE grid — KNN integer-vote border upstream=0.5 vs Rust greedy-logsum midpoint=1.5; BoW digitization grid under a non-degenerate XOR corpus — is a separate TRAINER estimated-feature-quantization concern. Predictions match ≤1e-5 under the current degenerate-separating SC-4 corpus; only the stored border VALUE differs. This does NOT affect FEAT-01 (all text calcers per-stage ≤1e-5 on their own fixtures) or FEAT-02 (all embedding calcers per-stage ≤1e-5). Documented in deferred-items.md section "General estimated-feature quantization-GRID parity". No later Phase 6.x roadmap phase currently owns this follow-on concern; it should be claimed in a future trainer-estimated-feature-grid slice.

### LDA raw-projection documented tolerance

The LDA raw projection diverges up to 3.9e-2 per document from upstream (upstream vendored-CLAPACK ssyev_ non-reference iterate). The model per-stage oracle is byte-identical because 0/16 documents cross the upstream projection split border (binarization-stability HARD gate, min margin 1.34). This is a justified, human-signed-off intermediate tolerance that does NOT weaken the model parity bar. No change from the prior verification.

---

## Human Verification Required

None. All success criteria are gated by automated oracle tests that run against frozen catboost 1.2.10 fixtures. No human testing is required to advance.

---

## Gaps Summary

No gaps remain. The single open item from the prior verification — the BM25 per-stage trained-model oracle — is closed by plans 06.5-08 and 06.5-09. The phase goal ("all six text and embedding calcers produce upstream-matching encodings ≤1e-5") is fully achieved:

- **SC-1 (CLOSED):** Tokenizer parity is bit-identical (3 tests).
- **SC-2 (CLOSED):** BoW 4/4, NaiveBayes 5/5, BM25 **7/7** per-stage ≤1e-5. The prior "normalized border" gap was a fixture mislabel resolved by 06.5-08/09.
- **SC-3 (CLOSED):** LDA binarization-stability byte-identical (5 tests, documented tolerance on raw projection only); KNN bit-exact (5 tests).
- **SC-4 (CLOSED):** Mixed text+embedding+numeric end-to-end StagedApprox + Predictions ≤1e-5 (5 tests).

FEAT-01 and FEAT-02 are both marked Complete in REQUIREMENTS.md. Phase 6.5 goal is achieved.

---

_Verified: 2026-06-18_
_Verifier: Claude (gsd-verifier) — re-verification after 06.5-08/09 gap closure_
