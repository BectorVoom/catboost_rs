---
phase: 06.5-text-and-embedding-features
plan: 07
type: execute
wave: 7
depends_on: ["06.5-04", "06.5-06"]
files_modified:
  - crates/cb-train/src/estimated/estimated_features.rs
  - crates/cb-train/src/estimated/estimated_features_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/tests/text_embedding_end_to_end_oracle_test.rs
autonomous: true
requirements: [FEAT-01, FEAT-02]
user_setup: []

must_haves:
  truths:
    - "A single trained model with BOTH text and embedding columns present flows Pool -> tokenizer/digitizer + calcers (online per-fold) -> estimated float columns -> existing borders/quantize/quantized_pool -> tree search, with the estimated-feature index/border layout matching upstream"
    - "The end-to-end per-stage oracle (Splits|LeafValues|StagedApprox|Predictions) + borders of the estimated features passes ≤1e-5 vs catboost 1.2.10 (thread_count=1) on a mixed text+embedding pool — SC-4 hard gate, no #[ignore]"
    - "A pool with NO text/embedding columns produces byte-identical training to pre-phase-6.5 (the estimated-feature path is inert when absent)"
  artifacts:
    - path: "crates/cb-train/src/estimated/estimated_features.rs"
      provides: "final mixed text+embedding feature-layout join + per-fold online orchestration"
    - path: "crates/cb-oracle/tests/text_embedding_end_to_end_oracle_test.rs"
      provides: "SC-4 mixed-pool end-to-end per-stage oracle ≤1e-5"
  key_links:
    - from: "crates/cb-train/src/boosting.rs"
      to: "crates/cb-train/src/estimated/estimated_features.rs"
      via: "estimated columns materialized per fold and appended to the float-feature layout before tree search"
      pattern: "estimated"
    - from: "crates/cb-oracle/tests/text_embedding_end_to_end_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/ (mixed text+embedding fixture)"
      via: "compare_stage ≤1e-5 across all stages"
      pattern: "compare_stage"
---

<objective>
SC-4 integration slice: prove that text AND embedding columns flow TOGETHER through the full pipeline end-to-end on a single trained model, with the estimated-feature index/border layout matching upstream, gated per-stage ≤1e-5. Plans 03-06 each landed and oracled one calcer; this plan locks the combined flow and the estimated-feature ordering into the existing quantize->tree path.

Purpose: The per-calcer oracles (03-06) validate each encoding in isolation. SC-4 requires the full Pool->calcer->quantize->tree flow with mixed feature types in one model — the estimated-feature index layout and border selection of estimated floats are only exercised when text + embedding + (existing) numeric/categorical features coexist. This is the phase's terminal hard gate.
Output: the mixed text+embedding orchestration in estimated_features.rs + boosting.rs wiring + the SC-4 end-to-end per-stage oracle ≤1e-5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-text-and-embedding-features/06.5-CONTEXT.md
@.planning/phases/06.5-text-and-embedding-features/06.5-RESEARCH.md
@.planning/phases/06.5-text-and-embedding-features/06.5-PATTERNS.md
</context>

## Phase Goal

**As a** Rust developer embedding catboost-rs, **I want to** train models with text and embedding features whose encodings match upstream catboost 1.2.10 to within 1e-5, **so that** existing CatBoost text/embedding pipelines port over with verifiable parity.

## Acceptance Criteria
- [ ] Mixed text+embedding pool trains end-to-end through the existing quantize->tree path
- [ ] SC-4 per-stage oracle ≤1e-5 (Splits|LeafValues|StagedApprox|Predictions) + estimated-feature borders
- [ ] No-text/no-embedding pool trains byte-identically (estimated path inert)

<tasks>

<task type="auto">
  <name>Task 1: Mixed text+embedding feature-layout orchestration + per-fold online materialization</name>
  <read_first>
    - crates/cb-train/src/estimated/estimated_features.rs (from Plans 03/04/05/06 — the seam to finalize)
    - crates/cb-train/src/boosting.rs (the per-fold loop where estimated columns must be materialized before tree search; mirror how ctr columns are materialized per fold)
    - crates/cb-train/src/ctr/ctr_feature.rs (ANALOG: how CTR estimated columns are appended to the feature layout + indexed)
    - crates/cb-train/src/ctr/online.rs (ANALOG: per-fold online materialization ordering)
    - crates/cb-data/src/borders.rs (select_borders_greedy_logsum ~line 209 — estimated-feature border selection, unchanged quantizer)
    - crates/cb-data/src/quantize.rs (Pool::quantize line 69)
    - catboost-master/catboost/private/libs/algo/fold.cpp (online estimated features per-fold over ff.GetLearnPermutationArray, lines 181/200) + estimated_features.cpp (lines 448-473 — feature-layout join, TRANSCRIPTION SOURCE)
  </read_first>
  <action>
    Finalize estimated/estimated_features.rs to orchestrate ALL calcers on a mixed pool: BoW (offline, target-independent) + NaiveBayes/BM25 (online text, Plan 04) + LDA/KNN (online embedding, Plans 05/06). Each calcer's estimated float columns are appended to the float-feature layout in the upstream order (transcribe the layout join from estimated_features.cpp:448-473), then the COMBINED estimated floats flow through the UNCHANGED cb_data::select_borders_greedy_logsum -> Pool::quantize -> quantized_pool -> tree search. Online calcers materialize per fold over &fold.permutation (fold.cpp:181/200); the dictionary + offline BoW build once. Wire into boosting.rs at the per-fold materialization point (mirror CTR column materialization), branching so a pool with NO text/embedding columns is byte-identical (estimated path inert — D-04 discipline). Checked .get(..), typed CbError, no unwrap/index. Unit test in estimated_features_test.rs (mixed-layout index/order + inert-when-absent).
  </action>
  <verify>
    <automated>cargo test -p cb-train estimated::estimated_features && cargo test -p cb-train --lib</automated>
  </verify>
  <done>Mixed text+embedding estimated columns join the feature layout in upstream order and flow through the existing quantizer; no-text/no-embedding pool byte-identical; lib lint clean</done>
  <acceptance_criteria>cargo test -p cb-train estimated::estimated_features exits 0; cargo test -p cb-train --lib exits 0 (no regression on the inert path); cargo clippy -p cb-train --lib 0 indexing_slicing/unwrap_used in estimated_features.rs</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SC-4 mixed text+embedding end-to-end per-stage oracle</name>
  <read_first>
    - crates/cb-oracle/tests/per_stage_oracle_test.rs (ANALOG oracle-test structure, lines 1-55)
    - crates/cb-oracle/tests/bow_oracle_test.rs (from Plan 03 — single-calcer per-stage pattern to combine)
    - crates/cb-oracle/src/compare.rs (compare_stage line 84, Stage enum, assert_abs_close line 46)
    - crates/cb-oracle/src/model_json.rs (ModelJson line 168, split_borders 190, leaf_values 200, leaf_weights 212)
    - crates/cb-oracle/fixtures/ (the mixed text+embedding fixture — if absent, the Plan-01 generator must produce it: a pool with text + embedding + numeric columns trained thread_count=1)
    - crates/cb-train/src/estimated/estimated_features.rs (from Task 1)
  </read_first>
  <action>
    Create crates/cb-oracle/tests/text_embedding_end_to_end_oracle_test.rs: train a model on a mixed pool (text + embedding + numeric columns) via the Task-1 orchestration and assert the FULL 4-stage gate (Splits|LeafValues|StagedApprox|Predictions) + the borders of the estimated text/embedding features ≤1e-5 vs the catboost 1.2.10 (thread_count=1) mixed fixture. If the mixed fixture does not exist, extend gen_text_embedding_fixtures.py (Plan 01) to produce it single-threaded and freeze model.json + per-stage .npy + config.json. NO #[ignore], NO weakened tolerance (LDA/KNN may carry their human-signed-off documented tolerance from Plans 05/06, cited in-file; everything else ≤1e-5). Run the full cross-crate suite (cb-data, cb-compute, cb-train, cb-oracle) to confirm no regression.
  </action>
  <verify>
    <automated>cargo test -p cb-oracle --test text_embedding_end_to_end_oracle_test && cargo test -p cb-data -p cb-compute -p cb-train --tests</automated>
  </verify>
  <done>SC-4 mixed-pool end-to-end per-stage oracle ≤1e-5 (no #[ignore]); full cross-crate suite green; phase parity bar met for all 6 calcers in one trained model</done>
  <acceptance_criteria>cargo test -p cb-oracle --test text_embedding_end_to_end_oracle_test exits 0 (all stages ≤1e-5 except any human-signed-off LDA/KNN tolerance, 0 ignored); cargo test -p cb-data -p cb-compute -p cb-train --tests exits 0 (no regression)</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mixed Pool (text + embedding + numeric) → combined estimated-feature layout → quantizer | Feature-index collision / misordering across calcer types could corrupt the layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-07-01 | Integrity (parity) | combined estimated-feature index/border ordering | mitigate | Transcribe the layout join from estimated_features.cpp:448-473; SC-4 per-stage oracle gates the combined ordering ≤1e-5 |
| T-06.5-07-02 | Denial of Service (panic) | mixed-layout append on empty/absent column types | mitigate | Bounds-checked .get(..); inert path when absent; typed CbError; no unwrap/index (INFRA-02, V5) |
| T-06.5-07-03 | Integrity (regression) | the inert (no-text/no-embedding) path | mitigate | Byte-identical branch when no estimated columns (D-04); full cross-crate suite re-run |
| T-06.5-07-SC | Tampering | no package install | accept | No new packages; D-05 no new crate |
</threat_model>

<verification>
- SC-4 mixed-pool end-to-end per-stage oracle ≤1e-5 (no #[ignore])
- No-text/no-embedding pool byte-identical
- Full cross-crate suite green (cb-data, cb-compute, cb-train, cb-oracle)
</verification>

<success_criteria>
SC-4 closed: text and embedding columns flow Pool -> calcer -> quantize -> tree end-to-end in one trained model, per-stage ≤1e-5. FEAT-01 + FEAT-02 complete; Phase 6.5 parity bar met.
</success_criteria>

<output>
Create `.planning/phases/06.5-text-and-embedding-features/06.5-07-SUMMARY.md` when done
</output>
