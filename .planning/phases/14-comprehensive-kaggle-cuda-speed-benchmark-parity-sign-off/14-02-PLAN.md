---
phase: 14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/phase14_cuda_signoff/oracle.py
  - bench/phase14_cuda_signoff/kernel-metadata.json
  - bench/phase14_cuda_signoff/oracle_gen_test.py
autonomous: true
requirements: [BENCH-03]
must_haves:
  truths:
    - "oracle.py passes `python -m py_compile` and imports in-env with no GPU (all Kaggle-run work is under a `main()` guarded by `if __name__ == '__main__'`)."
    - "The importable `gen(n)` reproduces the Rust `bench_grow_speed_test.rs::gen()` workload: an n x 20 float32 matrix of integer bins 0..31 and a +/-1 target, bit-stable across calls."
    - "Part A (Rust device self-oracle under --features cuda) is structured as a BLOCKING pre-flight that aborts before the Part C CatBoost-GPU arm on any failure (D-04 pre-flight-only)."
    - "kernel-metadata.json declares a fresh Phase-14 kernel id with enable_gpu:true and enable_internet:true."
  artifacts:
    - path: "bench/phase14_cuda_signoff/oracle.py"
      provides: "Kaggle CUDA driver: Part A blocking correctness pre-flight + NEW Part C official-CatBoost-GPU timing arm; emits bench03-result.json"
      contains: "def gen"
    - path: "bench/phase14_cuda_signoff/kernel-metadata.json"
      provides: "Fresh Kaggle kernel descriptor (Phase-14 id, GPU+internet enabled)"
      contains: "catboost-rs-phase14"
    - path: "bench/phase14_cuda_signoff/oracle_gen_test.py"
      provides: "Offline test of the importable gen() workload reproduction (shape + determinism), no GPU"
      contains: "def test_"
  key_links:
    - from: "bench/phase14_cuda_signoff/oracle.py"
      to: "crates/cb-train/tests/bench_grow_speed_test.rs"
      via: "Part C reproduces gen()+params() as the CatBoost-GPU workload/config (orchestrator decision 2 / A2)"
      pattern: "border_count"
    - from: "bench/phase14_cuda_signoff/oracle.py"
      to: "bench/phase13_cuda_oracle/oracle.py"
      via: "Part A correctness-gate structure reused wholesale as the blocking pre-flight"
      pattern: "features"
  prohibitions:
    - "MUST NOT use bench/generator.py's 1e6x50 regression workload for the CatBoost-GPU arm — it produced NO committed device/CPU number, so the head-to-head would be incomparable (Pitfall 3). Reproduce bench_grow_speed_test.rs::gen() instead (orchestrator decision 2; synthetic large-n only, no external datasets per D-02)."
    - "MUST NOT run Part C before Part A passes — a fast-but-wrong number is never quoted (Pitfall 5, D-04 blocking pre-flight)."
    - "MUST NOT print, echo, or commit the Kaggle access_token; the notebook writes only compact result files to /kaggle/working."
    - "MUST NOT flip GPUT-14 to satisfied or write into bench/RESULTS.md from this driver — correctness is pre-flight ONLY (D-04)."
    - "MUST NOT fabricate a CatBoost-GPU number for the Region family — it has no official grow_policy, its cell is N/A (Pitfall 4)."
---

<objective>
Author the human-gated Kaggle CUDA driver (a variant of the proven `phase13_cuda_oracle/oracle.py`) and its kernel descriptor. The delta over Phase 13 is minimal and additive: keep Part A (Rust device self-oracle under `--features cuda`) as a BLOCKING correctness pre-flight (D-04), and add Part C — a pure-Python official-CatBoost-GPU timing arm that reproduces the `bench_grow_speed_test.rs::gen()` workload (orchestrator decision 2 / research A2) and times `task_type='GPU'` on the frozen config, emitting `bench03-result.json`. This plan writes the SCRIPT and proves the offline-testable `gen()` reproduction in-env; the actual Kaggle run is Plan 03.

Purpose: The one new human-gated run (D-03) adds ONLY the missing official-CatBoost-GPU column on the SAME synthetic large-n configs the aggregated device/CPU numbers used — anything else would make the head-to-head incomparable. The correctness pre-flight preserves the do-not-fabricate discipline (D-04).
Output: `bench/phase14_cuda_signoff/oracle.py`, `kernel-metadata.json`, and an offline `oracle_gen_test.py` proving `gen()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-RESEARCH.md
@.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-PATTERNS.md
@bench/phase13_cuda_oracle/oracle.py
@bench/phase13_cuda_oracle/kernel-metadata.json
</context>

<artifacts>
## Artifacts this plan produces
- `bench/phase14_cuda_signoff/oracle.py` — NEW Kaggle CUDA driver. Clone of `phase13_cuda_oracle/oracle.py` (repo→/tmp staging, Rust install, env capture, Part A blocking correctness gate) refactored so all run-time work lives in `main()` under an `if __name__ == "__main__"` guard, plus a module-level importable `gen(n, nf=20, nbins=32)` (lazy numpy import) and a NEW Part C that times official `catboost` `task_type='GPU'` and writes `bench03-result.json`.
- `bench/phase14_cuda_signoff/kernel-metadata.json` — NEW Kaggle kernel descriptor (fresh Phase-14 id/title/code_file/dataset_sources; `enable_gpu:true`, `enable_internet:true`).
- `bench/phase14_cuda_signoff/oracle_gen_test.py` — NEW offline test of `gen()` (shape, dtype, determinism, target ±1) importing from oracle.py without triggering the Kaggle run.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Kaggle CUDA driver with Part A pre-flight + Part C CatBoost-GPU arm (oracle.py)</name>
  <files>bench/phase14_cuda_signoff/oracle.py</files>
  <read_first>
    - bench/phase13_cuda_oracle/oracle.py (clone target: log/sh/env_line helpers, repo staging into /tmp, Rust toolchain, Part A FAMILIES correctness loop, result-emit + verdict)
    - crates/cb-train/tests/bench_grow_speed_test.rs lines 43-108 (gen() hash i*2654435761 + f*40503 then % nbins; target sign on col0 + 0.5*col1 > nbins*0.75; params(): RMSE, depth 6, iters 20, lr 0.3, l2 0.0, random_strength 0.0, boost_from_average false, bootstrap No, nbins 32, seed 42, score_function L2, grow_policy Depthwise/Region)
    - .planning/phases/14-.../14-RESEARCH.md §Config-Matching (frozen CatBoostRegressor GPU-arm config + 4 documented divergences) + §Notebook Harness Delta
    - .planning/phases/14-.../14-PATTERNS.md §Shared Patterns → CatBoost-GPU config (frozen)
  </read_first>
  <action>
    Create `bench/phase14_cuda_signoff/oracle.py` by cloning `bench/phase13_cuda_oracle/oracle.py` and applying this minimal, additive delta.

    (1) Structure: put ALL Kaggle-run work (env capture, repo staging, Rust install, Part A, Part C, result emit) inside `def main():`, invoked by `if __name__ == "__main__": main()`. Define `gen(n, nf=20, nbins=32)` at MODULE level with a LAZY `import numpy as np` inside the function body so importing oracle.py in-env needs no numpy and triggers no run. Keep a module header comment describing the two-part structure and the do-not-fabricate discipline (every value comes from THIS run).

    (2) Keep the log/sh/env_line helpers and the nvidia-smi/nvcc/cuda_dirs env capture from the phase-13 clone; set the result dict to phase 14 with kind "cuda-signoff-bench03", a families map, and a bench03 map, capturing gpu/nvcc at its top level (the provenance the aggregator and sign-off doc read).

    (3) Keep Part A — the per-family Rust device self-oracle correctness gate — reused wholesale as the BLOCKING pre-flight. Per D-04 this is pre-flight ONLY: do NOT flip GPUT-14, do NOT write RESULTS.md. Reuse the phase-13 FAMILIES device self-oracle loop running cargo test release with --no-default-features --features cuda on cb-backend and the family test-name filters, collecting the test-result summaries and divergence lines. After Part A, roll up corr_pass; if it is false, write the result with a SOME-FAIL correctness verdict and sys.exit(2) BEFORE Part C runs (fast-but-wrong is never quoted — Pitfall 5).

    (4) Add Part C — the NEW official-CatBoost-GPU timing arm, pure Python, no Rust. Per D-02 (synthetic large-n only, no external named datasets), ensure catboost is importable, else `pip install -q catboost` (internet enabled). Reproduce gen() in numpy exactly as the Rust source: per feature f build h = arange(n, uint64) * uint64(2654435761) + uint64(f*40503), column = (h % uint64(nbins)) as float32; target = where(X[:,0] + 0.5*X[:,1%nf] > nbins*0.75, 1.0, -1.0). For BENCH_NS = 10000,100000,300000 and families [("depthwise","Depthwise"), ("region", None)]: when the policy is None (Region has NO official policy — Pitfall 4) append a row with catboost_gpu_s None, grow_policy_used "N/A", note "no Region policy in official CatBoost"; otherwise build the frozen CatBoostRegressor with task_type GPU, devices 0, loss_function RMSE, score_function L2, depth 6, iterations 20, learning_rate 0.3, l2_leaf_reg 0.0, random_strength 0.0, boost_from_average False, bootstrap_type No, border_count 32, random_seed 42, grow_policy Depthwise, verbose False; warm one untimed fit on a 2000-row slice, then time a single train-only fit and record the rounded elapsed seconds. Store the rows plus the catboost version under result bench03.

    (5) Emit results: json.dump the result to /kaggle/working/bench03-result.json (indent 2) plus a small human-readable bench03-result.md table. Set the correctness verdict and a catboost_gpu verdict. Do-not-fabricate: a failed Part C leaves catboost_gpu_s None, never invented.

    Document the 4 informational divergences in the header comment (Region has no official policy so its cell is N/A; the GPU border_count default is 128 and is set to 32; CatBoost fit() wall-clock includes on-device quantization while catboost-rs times only the grow loop, and this must NOT be subtracted; integer-binned features fed as float32 with border_count 32). Per D-01 the CatBoost-GPU column is informational, not a gate.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -m py_compile bench/phase14_cuda_signoff/oracle.py && python3 -c "import importlib.util as u; s=u.spec_from_file_location('o14','bench/phase14_cuda_signoff/oracle.py'); m=u.module_from_spec(s); s.loader.exec_module(m); assert hasattr(m,'gen') and hasattr(m,'main'); print('import-ok')" && grep -q "task_type" bench/phase14_cuda_signoff/oracle.py && grep -q "border_count" bench/phase14_cuda_signoff/oracle.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile bench/phase14_cuda_signoff/oracle.py` succeeds.
    - Importing oracle.py in-env (no GPU, no numpy at import time) succeeds and exposes module-level `gen` and `main` (the run body is guarded by `if __name__ == "__main__"`).
    - The frozen CatBoost-GPU config is present: `task_type='GPU'`, `border_count=32`, `bootstrap_type='No'`, `score_function='L2'`, `grow_policy='Depthwise'` (grep-verifiable).
    - Region's arm records `catboost_gpu_s` None with grow_policy_used "N/A" (no fabricated Region number).
    - Part A rolls up a correctness verdict and `sys.exit(2)` fires before Part C on failure (blocking pre-flight present).
  </acceptance_criteria>
  <done>oracle.py compiles and imports offline, keeps Part A as a blocking correctness pre-flight, and adds a Part C CatBoost-GPU arm reproducing gen() with the frozen config and Region=N/A — emitting bench03-result.json on a real run.</done>
</task>

<task type="auto">
  <name>Task 2: Kaggle kernel descriptor + offline gen() test</name>
  <files>bench/phase14_cuda_signoff/kernel-metadata.json, bench/phase14_cuda_signoff/oracle_gen_test.py</files>
  <read_first>
    - bench/phase13_cuda_oracle/kernel-metadata.json (clone: id/title/code_file/dataset_sources, enable_gpu true, enable_internet true, kernel_type script, is_private true)
    - bench/phase14_cuda_signoff/oracle.py (Task 1 output — import gen from it)
    - crates/cb-train/tests/bench_grow_speed_test.rs lines 43-65 (the exact gen() the test asserts parity of shape/determinism against)
  </read_first>
  <action>
    Create `bench/phase14_cuda_signoff/kernel-metadata.json` by cloning the phase-13 descriptor and bumping the Phase-14 identifiers: id `yensen2/catboost-rs-phase14-cuda-signoff`, title `catboost-rs phase14 cuda signoff`, code_file `catboost-rs-phase14-cuda-signoff.py`, dataset_sources `["yensen2/catboost-rs-phase14-src"]`. Keep `enable_gpu: true` and `enable_internet: true` (internet is load-bearing — the Part C `pip install -q catboost` fallback depends on it), `kernel_type: "script"`, `is_private: true`, empty kernel_sources/competition_sources/model_sources.

    Create `bench/phase14_cuda_signoff/oracle_gen_test.py` — a standalone pytest-style test file (per CLAUDE.md, kept separate from oracle.py). Import `gen` from oracle.py (resolve the sibling module via `__file__`/importlib so importing does NOT run main()). Assert: `X, y = gen(1000)` gives `X.shape == (1000, 20)` and `X.dtype == float32`; every X value is an integer in `[0, 32)` (bins); `gen(1000)` called twice returns identical arrays (determinism); `set(y.tolist()) <= {1.0, -1.0}` and both classes appear; and a couple of spot-checked cells match the hash formula `(i*2654435761 + f*40503) % 32`. Skip cleanly (pytest skip) if numpy is unavailable rather than failing the suite.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "import json;d=json.load(open('bench/phase14_cuda_signoff/kernel-metadata.json'));assert d['enable_gpu'] and d['enable_internet'] and 'phase14' in d['id'];print('meta-ok')" && python3 -m pytest bench/phase14_cuda_signoff/oracle_gen_test.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - kernel-metadata.json parses, has a Phase-14 id, and both `enable_gpu` and `enable_internet` are true.
    - `python3 -m pytest bench/phase14_cuda_signoff/oracle_gen_test.py -x -q` passes offline (or skips cleanly if numpy is absent), never fabricating GPU work.
    - The test proves gen() shape (n x 20 float32), integer bins in [0,32), determinism across calls, and a ±1 target with both classes present.
  </acceptance_criteria>
  <done>The fresh Phase-14 kernel descriptor is valid and internet-enabled, and gen() is proven offline to match the Rust workload — the driver is ready for the human-gated Kaggle run in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook → Kaggle logs/output | The driver runs on a Kaggle CUDA image; only compact result files may leave; the Kaggle access_token must never appear in the notebook body or logs |
| pip install → Kaggle image | Part C may `pip install catboost` (internet enabled) — the official Yandex library being measured, not a repo dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | Kaggle access_token | mitigate | The driver never reads/prints/commits the token; it writes only bench03-result.json / .md to /kaggle/working (existing phase-13 practice) |
| T-14-03 | Tampering (sign-off integrity) | Part C CatBoost-GPU number | mitigate | Correctness-gated-before-speed (Part A blocks Part C); a failed arm leaves cells None, never invented; Region records N/A not a proxy |
| T-14-SC | Tampering | pip install catboost | accept | `catboost` is the official Yandex PyPI library (RESEARCH Package Legitimacy Audit: OK, source reputation High), consumed at runtime on the Kaggle image only; no repo manifest changes — no legitimacy checkpoint required |
</threat_model>

<verification>
- `python3 -m py_compile bench/phase14_cuda_signoff/oracle.py` succeeds; importing it in-env exposes `gen`/`main` without running.
- The frozen CatBoost-GPU config (`task_type='GPU'`, `border_count=32`, `bootstrap_type='No'`, `score_function='L2'`, `grow_policy='Depthwise'`) is present; Region records `N/A`.
- `python3 -m pytest bench/phase14_cuda_signoff/oracle_gen_test.py -x -q` green (or clean skip without numpy).
- kernel-metadata.json is a fresh Phase-14 kernel with GPU + internet enabled.
</verification>

<success_criteria>
A minimal, additive Kaggle CUDA driver exists: Part A blocking correctness pre-flight (D-04, no GPUT-14 flip, no RESULTS.md write) + a NEW Part C official-CatBoost-GPU timing arm reproducing the exact `gen()` workload/config the aggregated numbers came from (orchestrator decision 2), with Region as N/A and no fabricated numbers — proven offline via `gen()` unit test and ready for the human-gated run.
</success_criteria>

<output>
Create `.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-02-SUMMARY.md` when done.
</output>