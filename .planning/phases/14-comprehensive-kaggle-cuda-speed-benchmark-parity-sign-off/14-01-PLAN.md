---
phase: 14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/phase14_cuda_signoff/aggregate.py
  - bench/phase14_cuda_signoff/aggregate_test.py
autonomous: true
requirements: [BENCH-03]
must_haves:
  truths:
    - "Running aggregate.py over the two committed BENCH-02 JSONs prints all 12 speed rows (6 Phase-12 depthwise/region + 6 Phase-13 depthwise/region)."
    - "Every printed row's speedup is cast from its JSON string to float and flagged >=20x (D-01 hard gate); the aggregate verdict line reads PASS."
    - "aggregate_test.py passes offline in-env with no GPU, asserting exactly 12 rows and every speedup >= 20.0."
  artifacts:
    - path: "bench/phase14_cuda_signoff/aggregate.py"
      provides: "Offline schema-branching aggregator over committed per-phase BENCH-02 JSON (D-03)"
      contains: "def load_rows"
    - path: "bench/phase14_cuda_signoff/aggregate_test.py"
      provides: "Offline unit test asserting 12 rows + every speedup >= 20x over the REAL committed JSON"
      contains: "def test_"
  key_links:
    - from: "bench/phase14_cuda_signoff/aggregate.py"
      to: "bench/phase12_cuda_oracle/bench02-result.json"
      via: "reads root .runs[] (Phase-12 schema)"
      pattern: "runs"
    - from: "bench/phase14_cuda_signoff/aggregate.py"
      to: "bench/phase13_cuda_oracle/result.json"
      via: "reads nested .bench02.runs[] (Phase-13 schema)"
      pattern: "bench02"
  prohibitions:
    - "MUST NOT assume .runs[] for both files — Phase-13 rows live under .bench02.runs[]; a single-branch loader silently drops 6 rows (Pitfall 1)."
    - "MUST NOT compare the JSON speedup as a number without float() casting it first — it is a string like \"39.987\" (Pitfall 2)."
    - "MUST NOT embed the test inside aggregate.py — tests live in the separate aggregate_test.py file (CLAUDE.md source/test separation)."
    - "MUST NOT re-run any GPU/Kaggle work — this plan only reads committed JSON offline (D-03). The aggregated workloads are the synthetic large-n gen() runs (D-02), no external datasets."
---

<objective>
Build the offline aggregator that stitches the already-committed per-phase BENCH-02 speed numbers into one matrix, honoring D-03 (aggregate committed results, do not re-run) and D-01 (>=20x-vs-host-CPU is the hard gate). This is the load-bearing new logic of Phase 14: a schema-branching reader over the ONLY two committed sources (Phase-12 `bench02-result.json` at root `.runs[]`, Phase-13 `result.json` nested `.bench02.runs[]`), producing the 12-row device/host-CPU/speedup/>=20x matrix the sign-off document consumes.

Purpose: Every device/host-CPU column of BENCH-03 comes from these committed JSONs (D-03) — no fresh GPU run produces them. Getting the schema branch and the string-speedup cast right is what makes the sign-off correct.
Output: `bench/phase14_cuda_signoff/aggregate.py` (offline stitcher, no GPU) + `bench/phase14_cuda_signoff/aggregate_test.py` (offline unit test over the real committed files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-RESEARCH.md
@.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-PATTERNS.md
</context>

<artifacts>
## Artifacts this plan produces
- `bench/phase14_cuda_signoff/aggregate.py` — NEW offline schema-branching aggregator (Python stdlib only; no numpy/GPU needed). Public function `load_rows(path, phase, gpu, date)`; a `main()` printing a markdown matrix + a `BENCH-03: PASS/FAIL` verdict line; optional `--json PATH` to write the combined matrix as JSON for the sign-off doc task.
- `bench/phase14_cuda_signoff/aggregate_test.py` — NEW offline unit test (separate file per CLAUDE.md) over the REAL committed Phase-12/Phase-13 JSON.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Schema-branching BENCH-02 aggregator (aggregate.py)</name>
  <files>bench/phase14_cuda_signoff/aggregate.py</files>
  <read_first>
    - bench/phase12_cuda_oracle/bench02-result.json (Phase-12 schema: root `runs[]`, top-level `gpu`/`nvcc`; per-row `{family,n,device_s,cpu_s,speedup(str),dev_trees,cpu_trees}`)
    - bench/phase13_cuda_oracle/result.json (Phase-13 schema: nested `bench02.runs[]`, top-level `gpu`/`nvcc`)
    - bench/generator.py (house style: stdlib `argparse`+`json`, triple-quoted module docstring, dependency-light `main()`)
    - .planning/phases/14-.../14-RESEARCH.md §Aggregation Mechanics + §Code Examples (the schema-branch load_rows template)
  </read_first>
  <action>
    Create `bench/phase14_cuda_signoff/aggregate.py`, a stdlib-only (json, argparse, sys) offline stitcher matching `bench/generator.py`'s house style (module docstring explaining it aggregates committed per-phase BENCH-02 numbers per D-03; a `main()`; no numpy, no GPU).

    Implement `load_rows(path, phase, gpu, date)` that reads the file and selects rows via the schema branch `rows = d.get("runs") or d.get("bench02", {}).get("runs", [])` so BOTH the Phase-12 root shape and the Phase-13 nested shape resolve (per D-03 mixed-session aggregation). For each row yield a dict merging the original fields with `speedup` converted via `float(r["speedup"])` (the JSON value is a string), plus `phase`, `gpu`, `date`, and `ge20x = float(r["speedup"]) >= 20.0` (the D-01 hard gate flag). Guard missing keys defensively with `.get(...)` so a malformed file raises a clear error rather than a bare KeyError.

    Wire `main()` to load Phase-12 from `bench/phase12_cuda_oracle/bench02-result.json` (label phase="P12", read `gpu`/`nvcc` from its top level, date="2026-07-04") and Phase-13 from `bench/phase13_cuda_oracle/result.json` (phase="P13", date="2026-07-04"), concatenate the rows, print a markdown table with columns `phase | family | n | device_s | cpu_s | speedup | >=20x?`, then print a final verdict line `BENCH-03: PASS` when every row's `ge20x` is true else `BENCH-03: FAIL (rows below 20x: ...)`. Support `--json PATH` to dump the combined list-of-rows + the boolean verdict to a JSON file for the sign-off doc task to consume.

    Resolve committed-JSON paths relative to the repo root (derive from `__file__` so the script runs from any cwd). Do NOT hardcode absolute paths. Do NOT read or write anything under Phase-10/Phase-11 (no committed BENCH-02 there — A4). Do NOT add any package to a manifest.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 bench/phase14_cuda_signoff/aggregate.py | tee /tmp/agg14.out && test "$(grep -cE '\| P1[23] \|' /tmp/agg14.out)" -eq 12 && grep -q 'BENCH-03: PASS' /tmp/agg14.out</automated>
  </verify>
  <acceptance_criteria>
    - `python3 bench/phase14_cuda_signoff/aggregate.py` exits 0 and prints exactly 12 data rows (6 tagged P12, 6 tagged P13).
    - The output contains the line `BENCH-03: PASS` (every aggregated speedup >= 20x — D-01).
    - `load_rows` uses the branch `d.get("runs") or d.get("bench02", {}).get("runs", [])` (schema branch present) and casts `speedup` with `float(...)`.
    - `python3 bench/phase14_cuda_signoff/aggregate.py --json /tmp/agg14.json` writes a JSON file with 12 rows and a boolean verdict.
    - No file under `bench/phase12_cuda_oracle/` or `bench/phase13_cuda_oracle/` is modified.
  </acceptance_criteria>
  <done>aggregate.py stitches all 12 committed rows via the schema branch, casts the string speedup, flags >=20x, and prints/writes a PASS verdict — all offline with no GPU.</done>
</task>

<task type="auto">
  <name>Task 2: Offline aggregator unit test (aggregate_test.py)</name>
  <files>bench/phase14_cuda_signoff/aggregate_test.py</files>
  <read_first>
    - bench/phase14_cuda_signoff/aggregate.py (Task 1 output — import load_rows from it)
    - bench/phase12_cuda_oracle/bench02-result.json + bench/phase13_cuda_oracle/result.json (the real files the test asserts over)
    - bench/generator.py `--check` self-verify idea (fixture-drift guard: offline, seconds, no GPU)
  </read_first>
  <action>
    Create `bench/phase14_cuda_signoff/aggregate_test.py` — a standalone pytest-style test file (per CLAUDE.md, tests NEVER live inside aggregate.py). Import `load_rows` from `aggregate.py` (resolve the sibling module path via `__file__`/`sys.path` so pytest can import it from repo root).

    Write `test_twelve_rows_from_both_schemas()` that loads the real committed Phase-12 file and the real committed Phase-13 file, concatenates, and asserts the total is exactly 12 — explicitly guarding Pitfall 1 (only 6 rows means the nested `.bench02.runs[]` branch is broken). Write `test_every_speedup_ge_20x()` asserting every row's `speedup` is a float and `>= 20.0` (the D-01 hard gate, and a proof the string-to-float cast happened). Write `test_phase13_nested_schema_resolves()` asserting the Phase-13 file alone yields 6 rows through `load_rows` (proving the nested branch, not the root branch, fired).

    Keep it dependency-light (stdlib + the imported aggregate module); no numpy, no GPU, no network.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -m pytest bench/phase14_cuda_signoff/aggregate_test.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest bench/phase14_cuda_signoff/aggregate_test.py -x -q` passes with all tests green, no GPU, in under 60s.
    - The test asserts a total of exactly 12 rows across both committed files (Pitfall 1 guard).
    - The test asserts every `speedup` is a float >= 20.0 (D-01) and that the Phase-13 file alone resolves 6 rows via the nested branch.
    - The test file contains no `#[cfg(test)]`-style embedding in aggregate.py and imports load_rows from the separate module.
  </acceptance_criteria>
  <done>aggregate_test.py green offline: 12 rows total, both schemas resolve, every speedup >= 20x — the aggregator is proven against the real committed sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON → aggregator | Repo-committed, trusted BENCH-02 result files parsed offline; only structural malformation is a concern, not adversarial input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Denial of Service (local) | aggregate.py JSON parse | mitigate | Defensive `.get()` on all keys + explicit `float()` casts; the unit test exercises the real committed files so a schema drift fails loudly rather than crashing silently |
| T-14-03 | Tampering (sign-off integrity) | speedup aggregation | mitigate | Numbers are read verbatim from committed JSON and each row is tagged with its source phase/gpu/date (D-03 provenance); no value is computed or invented |
| T-14-SC | Tampering | pip/cargo installs | accept | This plan adds NO package to any manifest and runs stdlib-only; nothing to install |
</threat_model>

<verification>
- `python3 bench/phase14_cuda_signoff/aggregate.py` prints 12 rows and `BENCH-03: PASS`.
- `python3 -m pytest bench/phase14_cuda_signoff/aggregate_test.py -x -q` green.
- No committed `bench/phase12_cuda_oracle/` or `bench/phase13_cuda_oracle/` file is modified (`git status` shows only the two new files under `bench/phase14_cuda_signoff/`).
</verification>

<success_criteria>
The offline aggregator deterministically reproduces the 12-row device/host-CPU/speedup matrix from the two committed BENCH-02 JSONs, correctly branching Phase-12 root vs Phase-13 nested schema, casting the string speedup, flagging the D-01 >=20x gate, and is proven by an offline unit test — all with no GPU.
</success_criteria>

<output>
Create `.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-01-SUMMARY.md` when done.
</output>
