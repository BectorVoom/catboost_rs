---
phase: 14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - bench/phase14_cuda_signoff/bench03-result.json
  - bench/BENCH-03-SIGNOFF.md
  - bench/RESULTS.md
autonomous: false
requirements: [BENCH-03]
must_haves:
  truths:
    - "The human-gated Kaggle CUDA run produces bench03-result.json only AFTER Part A correctness PASSES; each depthwise (family,n) has a real catboost_gpu_s and every Region cell is N/A."
    - "BENCH-03-SIGNOFF.md shows a verdict banner `BENCH-03: PASS` because all 12 aggregated device rows are >=20x vs the host-CPU baseline (D-01 hard gate), referencing the pre-Phase-10 host-light baseline."
    - "Every device/host-CPU/CatBoost-GPU cell in the sign-off carries a source-run provenance label (Phase-12 P100 2026-07-04, Phase-13 P100 2026-07-04, or the Phase-14 session) so mixed-session provenance is explicit (D-03)."
    - "The sign-off has a 'Standing debt — NOT closed here' section flagging GPUT-14 (still Pending) and the bench/RESULTS.md depth-1/depth-6 TBD oracle table as out of scope (D-04)."
  artifacts:
    - path: "bench/phase14_cuda_signoff/bench03-result.json"
      provides: "The one new human-gated Kaggle CUDA run output: official-CatBoost-GPU timings + correctness verdict (D-03)"
      contains: "bench03"
    - path: "bench/BENCH-03-SIGNOFF.md"
      provides: "The milestone-closing BENCH-03 speed-parity sign-off document"
      contains: "BENCH-03: PASS"
  key_links:
    - from: "bench/BENCH-03-SIGNOFF.md"
      to: "bench/phase14_cuda_signoff/aggregate.py"
      via: "device/host-CPU/speedup/>=20x columns produced by running the aggregator over committed JSON"
      pattern: "aggregate"
    - from: "bench/BENCH-03-SIGNOFF.md"
      to: "bench/phase14_cuda_signoff/bench03-result.json"
      via: "informational CatBoost-GPU column merged from the new run"
      pattern: "catboost_gpu"
    - from: "bench/BENCH-03-SIGNOFF.md"
      to: ".planning/notes/gpu-training-host-light-root-cause.md"
      via: "the pre-Phase-10 host-light baseline the >=20x sign-off reverses"
      pattern: "host-light"
  prohibitions:
    - "MUST NOT fabricate any CatBoost-GPU number — every value comes from the actual Kaggle run; a failed/absent arm stays TBD or N/A (RESULTS.md do-not-fabricate discipline)."
    - "MUST NOT flip GPUT-14 to satisfied in REQUIREMENTS.md — correctness bookkeeping is out of scope (D-04)."
    - "MUST NOT backfill the depth-1/depth-6 TBD oracle table in bench/RESULTS.md — only append a single cross-link line to the new sign-off doc (D-04)."
    - "MUST NOT force a Region CatBoost-GPU number — Region has no official policy; its cell is N/A (Pitfall 4)."
    - "MUST NOT quote any speed number unless Part A correctness passed first (blocking gate, Pitfall 5)."
---

<objective>
Run the one new human-gated Kaggle CUDA notebook (adds ONLY the official-CatBoost-GPU column per D-03) and author the milestone-closing `bench/BENCH-03-SIGNOFF.md`. The hard-gate verdict (D-01: every device row >=20x vs the pre-Phase-10 host-light CPU baseline) is fully determined by the already-committed Phase-12/13 numbers the aggregator stitches — the human run only fills the informational CatBoost-GPU head-to-head. The sign-off aggregates device/host-CPU columns (from aggregate.py over committed JSON) with the new CatBoost-GPU column, labels every number's source session (D-03), records the documented divergences (D-01 informational), and flags the standing GPUT-14 / RESULTS.md debt as explicitly out of scope (D-04).

Purpose: This is the terminal deliverable of the v1.1 GPU-performance milestone — the signed-off proof that the device-resident path reverses the original >20x host-light slowdown.
Output: `bench/phase14_cuda_signoff/bench03-result.json` (from the run) + `bench/BENCH-03-SIGNOFF.md` (the sign-off) + one cross-link line appended to `bench/RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-RESEARCH.md
@.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-PATTERNS.md
@.planning/notes/gpu-training-host-light-root-cause.md
@bench/RESULTS.md
@bench/phase13_cuda_oracle/result.md
</context>

<artifacts>
## Artifacts this plan produces
- `bench/phase14_cuda_signoff/bench03-result.json` — NEW output of the human-gated Kaggle CUDA run (Part A correctness verdict + Part C official-CatBoost-GPU timings; Region = N/A). Committed from the pulled Kaggle output.
- `bench/BENCH-03-SIGNOFF.md` — NEW milestone-closing sign-off document: verdict banner, 12-row aggregate matrix + informational CatBoost-GPU column, per-number provenance labels, divergence notes, standing-debt section.
- `bench/RESULTS.md` — MODIFIED by exactly one appended cross-link line pointing at the new sign-off (no TBD-table backfill — D-04).
</artifacts>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Human-gated Kaggle CUDA run — correctness pre-flight + CatBoost-GPU arm</name>
  <files>bench/phase14_cuda_signoff/bench03-result.json</files>
  <read_first>
    - bench/phase14_cuda_signoff/oracle.py (Plan 02 output — the driver being run)
    - bench/phase14_cuda_signoff/kernel-metadata.json (Plan 02 output — the kernel descriptor)
    - bench/phase13_cuda_oracle/ (the proven Phase-13 push/poll/pull pipeline this run mirrors)
  </read_first>
  <action>
    A human runs the Plan-02 Kaggle CUDA driver on real NVIDIA hardware (no CUDA GPU in-env — this is the ONE human-gated run per D-03). Package the repo source as the Kaggle dataset `yensen2/catboost-rs-phase14-src` (tarball `repo.tar.gz`, Phase-13 shape) and push `oracle.py` as the kernel via the `kaggle` CLI (auth `~/.kaggle/access_token`). The driver runs Part A (Rust device self-oracle correctness gate under `--features cuda`) as a BLOCKING pre-flight, then Part C (official `catboost` `task_type='GPU'` timing on the reproduced `gen()` workload). Pull the output and commit `bench/phase14_cuda_signoff/bench03-result.json`. Do NOT commit the access_token or any secret.
  </action>
  <what-built>
    Plan 02 produced the Kaggle CUDA driver (`bench/phase14_cuda_signoff/oracle.py`) and its kernel descriptor. It runs Part A (Rust device self-oracle correctness gate under `--features cuda`) as a BLOCKING pre-flight, then Part C (official `catboost` `task_type='GPU'` timing on the reproduced `gen()` workload). This step is the ONE human-gated Kaggle CUDA execution (D-03) — there is no NVIDIA GPU in-env, so a human must run it.
  </what-built>
  <how-to-verify>
    1. Package the repo source and push it as the Kaggle dataset `yensen2/catboost-rs-phase14-src` (tarball `repo.tar.gz`, same shape as the Phase-13 dataset), and push `bench/phase14_cuda_signoff/oracle.py` as the kernel `catboost-rs-phase14-cuda-signoff.py` via the `kaggle` CLI (auth via `~/.kaggle/access_token`, per prior phases). `enable_gpu`/`enable_internet` are set in kernel-metadata.json.
    2. Confirm the kernel log shows CUDA active via `nvidia-smi` (Tesla P100 expected).
    3. Confirm Part A prints a per-family correctness verdict and that it is ALL-PASS (the BLOCKING gate) BEFORE any Part C timing. If Part A fails, STOP — do not record any speed number (do-not-fabricate).
    4. Confirm Part C printed a `catboost_gpu_s` for each depthwise (n = 10000/100000/300000) and `N/A` for every Region cell.
    5. Pull the kernel output and commit `bench/phase14_cuda_signoff/bench03-result.json` (and optionally a `log-excerpt.txt`) into the repo. Do NOT commit the Kaggle access_token or any secret.
  </how-to-verify>
  <resume-signal>Type "approved" once bench03-result.json is committed with Part A = ALL-PASS and CatBoost-GPU numbers present (Region = N/A), or describe the failure.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "import json;d=json.load(open('bench/phase14_cuda_signoff/bench03-result.json'));assert d.get('correctness_verdict')=='ALL-PASS', d.get('correctness_verdict');runs=d['bench03']['runs'];assert any(r['family']=='depthwise' and r.get('catboost_gpu_s') is not None for r in runs);assert all(r.get('grow_policy_used')=='N/A' for r in runs if r['family']=='region');print('bench03-ok')"</automated>
  </verify>
  <done>bench03-result.json is committed with Part A correctness ALL-PASS (blocking gate satisfied), a real catboost_gpu_s for each depthwise (family,n), and Region cells = N/A — no fabricated numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Author bench/BENCH-03-SIGNOFF.md and cross-link RESULTS.md</name>
  <files>bench/BENCH-03-SIGNOFF.md, bench/RESULTS.md</files>
  <read_first>
    - bench/phase14_cuda_signoff/aggregate.py (run it: `python3 bench/phase14_cuda_signoff/aggregate.py --json /tmp/agg14.json` for the 12-row device/host-CPU/speedup/>=20x matrix)
    - bench/phase14_cuda_signoff/bench03-result.json (Task 1 output — the informational CatBoost-GPU column; Region N/A)
    - bench/RESULTS.md lines 1-15 (the three standing disciplines to copy verbatim) + the D-10-09 escalation pin
    - bench/phase13_cuda_oracle/result.md (markdown table shape to extend)
    - .planning/phases/14-.../14-RESEARCH.md §BENCH-03 Deliverable Format (required content + provenance conventions)
    - .planning/notes/gpu-training-host-light-root-cause.md (the pre-Phase-10 host-light baseline reference)
  </read_first>
  <action>
    Run `python3 bench/phase14_cuda_signoff/aggregate.py --json /tmp/agg14.json` to obtain the 12 committed device/host-CPU rows with the >=20x flags (D-01), then author `bench/BENCH-03-SIGNOFF.md`.

    Header: copy the three standing disciplines from `bench/RESULTS.md` verbatim (correctness is a blocking gate; ROCm in-env is smoke-only not a gate; do NOT fabricate numbers).

    Verdict banner: `BENCH-03: PASS` — because every one of the 12 aggregated device rows is >=20x vs the host-CPU baseline (D-01 hard gate). State the pre-Phase-10 host-light baseline reference (`.planning/notes/gpu-training-host-light-root-cause.md`): the original >20x device-slower-than-CPU host-light slowdown is demonstrably reversed. Note the correctness pre-flight (Part A) passed as a blocking gate this session (D-04 pre-flight-only).

    Aggregate matrix (columns): `family | n | device_cuda_s | host_cpu_s | speedup_vs_host_cpu | >=20x? | catboost_gpu_s (informational) | source run (GPU, date, session)`. Fill device/host-CPU/speedup from the aggregator output (12 rows: P12 depthwise/region + P13 depthwise/region at n = 10k/100k/300k). Merge the informational CatBoost-GPU column from bench03-result.json; every Region CatBoost-GPU cell is `N/A (no Region policy in official CatBoost)`.

    Per-number provenance labels (D-03): tag each device/host-CPU cell with its origin — Phase-12 P100 (2026-07-04) or Phase-13 P100 (2026-07-04); tag each CatBoost-GPU cell with the Phase-14 session (P100, run date). Mixed-session provenance is explicit, never hidden.

    Divergence notes (D-01 informational): Region CatBoost-GPU = N/A; GPU border_count default 128 set to 32; quantization-cost asymmetry (CatBoost fit() includes on-device quantization while catboost-rs times only the grow loop — do NOT subtract it). State plainly that the CatBoost-GPU column is informational context, not a milestone gate.

    Standing debt — NOT closed here (D-04): a short section pointing at GPUT-14 (still `Pending` in REQUIREMENTS.md) and the `bench/RESULTS.md` depth-1/depth-6 `TBD` oracle table, to be resolved at milestone-close audit or a dedicated follow-up — explicitly out of scope for this speed-only sign-off.

    Finally, append EXACTLY ONE cross-link line to `bench/RESULTS.md` (e.g. a "See `bench/BENCH-03-SIGNOFF.md` for the milestone-closing BENCH-03 aggregate" line under the Run log heading). Do NOT modify, fill, or reformat any of RESULTS.md's existing TBD tables (D-04).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f bench/BENCH-03-SIGNOFF.md && grep -q 'BENCH-03: PASS' bench/BENCH-03-SIGNOFF.md && grep -q 'host-light' bench/BENCH-03-SIGNOFF.md && grep -qi 'Standing debt' bench/BENCH-03-SIGNOFF.md && grep -qi 'GPUT-14' bench/BENCH-03-SIGNOFF.md && test "$(grep -cE '\| *(depthwise|region) *\|' bench/BENCH-03-SIGNOFF.md)" -ge 12 && grep -q 'BENCH-03-SIGNOFF' bench/RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `bench/BENCH-03-SIGNOFF.md` exists and contains the verdict banner `BENCH-03: PASS`.
    - The aggregate matrix has at least 12 data rows (P12 + P13, depthwise + region, three n each), device/host-CPU/speedup from the aggregator.
    - Every Region CatBoost-GPU cell reads `N/A`; every device/CPU/CatBoost-GPU cell carries a source-run provenance label (D-03).
    - The doc references the pre-Phase-10 host-light baseline and contains a "Standing debt — NOT closed here" section naming GPUT-14 and the RESULTS.md TBD table (D-04).
    - `bench/RESULTS.md` gains exactly one cross-link line to the sign-off and no TBD table is altered (`git diff bench/RESULTS.md` shows a single added line).
  </acceptance_criteria>
  <done>The milestone-closing BENCH-03 sign-off is authored: PASS verdict (all 12 rows >=20x vs host-CPU baseline), informational CatBoost-GPU head-to-head (Region N/A), full per-number provenance, documented divergences, and an explicit out-of-scope standing-debt flag — with RESULTS.md cross-linked, not backfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle run → repo commit | Only the compact bench03-result.json / log-excerpt leaves the Kaggle image; the access_token must never be committed |
| new run + committed JSON → sign-off doc | Numbers are transcribed verbatim with source-session labels; no value is invented or session-blended silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | Kaggle access_token | mitigate | The human commits only bench03-result.json / log-excerpt; the token stays in `~/.kaggle/access_token`, never in the repo or notebook body |
| T-14-03 | Tampering (sign-off integrity) | BENCH-03-SIGNOFF numbers | mitigate | Do-not-fabricate: device/CPU from committed JSON, CatBoost-GPU from THIS run; per-number source-session labels (D-03); correctness-gated-before-speed; Region = N/A not a proxy |
| T-14-SC | Tampering | pip/cargo installs | accept | This plan adds no package to any manifest; the Kaggle-side `pip install catboost` (official library) is covered by Plan 02's audit |
</threat_model>

<verification>
- Human-gated Kaggle run: Part A ALL-PASS before any speed number; bench03-result.json committed with depthwise timings + Region N/A.
- `bench/BENCH-03-SIGNOFF.md`: `BENCH-03: PASS` banner, >=12 matrix rows, provenance labels, host-light baseline reference, standing-debt section (GPUT-14 + RESULTS.md TBD).
- `bench/RESULTS.md`: exactly one added cross-link line; no TBD table modified.
</verification>

<success_criteria>
BENCH-03 is signed off: the device-resident path demonstrably closes the >20x gap — every aggregated device row is >=20x vs the pre-Phase-10 host-light CPU baseline (D-01), with a correctness-gated informational CatBoost-GPU head-to-head, full mixed-session provenance (D-03), and the GPUT-14 / RESULTS.md backfill debt flagged out of scope (D-04). The milestone-closing deliverable exists and is committed.
</success_criteria>

<output>
Create `.planning/phases/14-comprehensive-kaggle-cuda-speed-benchmark-parity-sign-off/14-03-SUMMARY.md` when done.
</output>
