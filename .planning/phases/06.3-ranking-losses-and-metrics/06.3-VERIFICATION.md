---
phase: 06.3-ranking-losses-and-metrics
verified: 2026-06-17T14:00:00Z
status: gaps_found
score: 7/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/9
  gaps_closed:
    - "CR-01 (VERIFICATION): StochasticRank NDCG calc_dcg_metric_diff now reads normalized pos_weights (plan 06.3-06, commit 6cbe2eb)"
    - "CR-02 (VERIFICATION): QueryRMSE/QuerySoftMax weighted_der1 branches on group_spans.is_some() — no double-weighting (plan 06.3-07, commit ff2fd8e)"
    - "WR-01 (VERIFICATION): make_zero_average now routes through cb_core::sum_f64 (plan 06.3-08, commit c9a0f8e)"
    - "WR-02 (VERIFICATION): lambdamart_ideal_ndcg routes through cb_core::sum_f64 (plan 06.3-06, commit c7d4baa)"
    - "WR-03 (VERIFICATION): stochasticrank_oracle.cpp centering aligned to sum_f64 left-to-right order (plan 06.3-08, commit 6123c60)"
    - "Truth #4 PARTIAL: pairlogit_oracle_test.rs + pairlogit_pairwise_oracle_test.rs now EXIST and are substantive (175 lines each, full compare_stage wiring) — previously MISSING; oracle tests committed and #[ignore]'d on the still-deferred leaf-der2 parity"
    - "PairLogit bt.PairwiseWeights split-scoring wiring landed (plan 06.3-09, commit de78521); Splits match index advanced 4 -> 6"
  gaps_remaining:
    - "Truth #4: PairLogit/PairLogitPairwise end-to-end ≤1e-5 oracle still deferred — oracle tests #[ignore]'d on leaf-der2 parity requiring instrumented trainer"
    - "Truth #5: YetiRank/YetiRankPairwise/StochasticRank end-to-end TRAINER fixtures still absent; model.json MISSING for all three"
    - "Truth #7: SC-3/D-07 instrumented-harness RNG validation still deferred"
    - "REVIEW CR-01: stochasticrank_oracle.cpp serializes noise/score at 6-decimal precision (std::to_string), insufficient to back a ≤1e-5 fixture gate (unfixed)"
  regressions: []
gaps:
  - truth: "PairLogit trains end-to-end on explicit Pool.pairs and predicts matching upstream ≤1e-5 (per-stage Splits|LeafValues|StagedApprox|Predictions)"
    status: failed
    reason: "Oracle test files NOW EXIST (pairlogit_oracle_test.rs / pairlogit_pairwise_oracle_test.rs, 175 lines each, full compare_stage wiring) — the previous 'MISSING' gap is closed at the artifact level. However both tests are #[ignore]'d with an explicit deferral: the PairLogit leaf-der2 reduction diverges ~6x from upstream in a way no global L2/pairwise scaling reconciles (per-leaf denominators mutually inconsistent with -sumDer2+C, diagnosed in deferred-items.md). The bt.PairwiseWeights wiring advanced the Splits match index from 4 to 6, but the residual requires the instrumented catboost 1.2.10 trainer's per-leaf SumDer2 log to pin. The end-to-end ≤1e-5 oracle gate has never executed. No tolerance was weakened; removing #[ignore] runs the full gate."
    artifacts:
      - path: "crates/cb-train/tests/pairlogit_oracle_test.rs"
        issue: "SUBSTANTIVE (175 lines, full compare_stage wiring) but #[ignore]'d — leaf-der2 parity unresolved; no executing ≤1e-5 gate exists"
      - path: "crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs"
        issue: "SUBSTANTIVE (175 lines) but #[ignore]'d — same root cause as above"
    missing:
      - "Produce the instrumented catboost 1.2.10 trainer's per-leaf SumDer/SumDer2 log for PairLogit (clang-18 recipe, CB_INSTRUMENT_LOG, instrument_live_trainer_README.md) to identify the exact leaf-der2 reduction upstream applies"
      - "Transcribe the per-leaf der2 reduction, advance the Splits match to full agreement, and un-ignore both oracle tests"

  - truth: "YetiRank/YetiRankPairwise/StochasticRank predict matching upstream ≤1e-5 (per-stage Splits|LeafValues|StagedApprox|Predictions)"
    status: failed
    reason: "All three per-stage oracle tests (yetirank_oracle_test.rs, yetirank_pairwise_oracle_test.rs, stochasticrank_oracle_test.rs) pass by asserting the deferred-fixture invariant — model.json does NOT exist so the test exercises only the RNG ground-truth presence assertion. The trained model.json is MISSING for all three losses. The instrumented catboost 1.2.10 trainer build required to generate these fixtures remains infeasible (toolchain absent, disk 97%+ / link-failure regime). RNG stream validation (yetirank_rng_draw_log_oracle, stochasticrank_rng_draw_log_oracle) are verified GREEN ≤1e-5; those are not the same as the end-to-end trained-model gate."
    artifacts:
      - path: "crates/cb-oracle/fixtures/ranking_corpus/yetirank/model.json"
        issue: "MISSING — only yetirank_rng_groundtruth.jsonl exists"
      - path: "crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/model.json"
        issue: "MISSING"
      - path: "crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json"
        issue: "MISSING"
    missing:
      - "Perform the instrumented catboost 1.2.10 trainer build (instrument_ranking_rng_README.md STATUS section) when toolchain + disk permit; train YetiRank/YetiRankPairwise/StochasticRank corpus configs, freeze model.json + staged/predictions .npy"
      - "Remove the absent-fixture invariant branches in yetirank_oracle_test.rs / stochasticrank_oracle_test.rs; wire the full compare_stage(Splits|LeafValues|StagedApprox|Predictions) gates"

  - truth: "SC-3: YetiRank/StochasticRank RNG streams validated against C++-instrumented harness (D-07)"
    status: failed
    reason: "Standalone self-oracle generators (yetirank_oracle.cpp / stochasticrank_oracle.cpp) are bit-exact vs cb-core::TFastRng64/std_normal, and the RNG draw-log ground truth is frozen and gated ≤1e-5. However D-07 specifies instrumented TRAINER harness validation (CB_INSTRUMENT_LOG from actual training), not just a dependency-free self-oracle. The instrumented trainer build has never run. Additionally, the REVIEW's CR-01 identifies a fixture serialization flaw: stochasticrank_oracle.cpp uses std::to_string(double) (~6-decimal precision) for the committed ground-truth JSONL, which cannot certify a ≤1e-5 gate for noise/score values with magnitude >= 1. This is unfixed."
    artifacts:
      - path: "crates/cb-oracle/generator/stochasticrank_oracle.cpp"
        issue: "Lines 199, 202: std::to_string(noise[d]) / std::to_string(scores[d]) truncates at ~6 decimal digits; commits a low-precision ground-truth fixture that cannot certify ≤1e-5 for values >= 1.0. Fix: serialize with %.17g via snprintf and regenerate fixture."
      - path: "crates/cb-oracle/generator/instrument_ranking_rng_README.md"
        issue: "Documents OFFLINE closure step but build has not been run; STATUS section describes the deferred state"
    missing:
      - "Fix stochasticrank_oracle.cpp serialization to use %.17g (full round-trip precision) and regenerate the committed stochasticrank_rng_groundtruth.jsonl fixture"
      - "Run the instrumented catboost 1.2.10 trainer (CB_INSTRUMENT_LOG) when toolchain + disk permit; compare per-tree RNG draw log against Rust sampler; resolve any divergence"
deferred: []
---

# Phase 06.3: Ranking Losses and Metrics Re-Verification Report

**Phase Goal:** The full ranking-loss and ranking-metric surface works over group_id/subgroup_id/pairs, each oracle-locked ≤1e-5 vs catboost 1.2.10 — with proactive C++ instrumentation for the randomized losses (D-07).
**Verified:** 2026-06-17T14:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — after gap-closure round (plans 06.3-06 through 06.3-09)

## Re-Verification Summary

Five of the nine gaps from the initial verification are **closed**:

| Gap | Plan | Status |
|-----|------|--------|
| CR-01 (VERIFICATION): calc_dcg_metric_diff scale mismatch | 06.3-06 | CLOSED |
| CR-02 (VERIFICATION): weighted_der1 double-weighting | 06.3-07 | CLOSED |
| WR-01 (VERIFICATION): make_zero_average raw accumulation | 06.3-08 | CLOSED |
| WR-02 (VERIFICATION): lambdamart_ideal_ndcg raw accumulation | 06.3-06 | CLOSED |
| WR-03 (VERIFICATION): stochasticrank_oracle.cpp raw accumulation | 06.3-08 | CLOSED |

Two truths that were previously FAILED remain FAILED (instrumented trainer required); one truth was PARTIALLY progressed (oracle test files now exist, but are #[ignore]'d on a remaining root cause). Three truths were verified in the initial verification and remain VERIFIED with no regressions.

The code review (06.3-REVIEW.md) conducted on the gap-closure delta identified one new critical finding (REVIEW CR-01: fixture serialization precision) that was NOT addressed by the gap-closure plans and represents a latent correctness concern for truth #7.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: QueryRMSE trains end-to-end ≤1e-5 on group seam | VERIFIED | queryrmse_oracle_test 1/1 PASS; CR-02 fix confirmed; grouped_weight_regression_test 2/2 PASS |
| 2 | SC-1: QuerySoftMax trains end-to-end ≤1e-5 on group seam | VERIFIED | querysoftmax_oracle_test 1/1 PASS; CR-02 fix in force (weighted_der1 branches on group_spans.is_some() at boosting.rs:2292) |
| 3 | SC-1: LambdaMart per-group lambda gradient trains ≤1e-5 | VERIFIED | lambdamart_oracle_test 1/1 PASS; WR-02 fix (sum_f64) confirmed at ranking_der.rs:619 |
| 4 | SC-1: PairLogit/PairLogitPairwise train ≤1e-5 on explicit pairs | FAILED | Oracle test files NOW EXIST (175 lines each, full compare_stage wiring, commits ae6eb67) but are #[ignore]'d — leaf-der2 parity gap confirmed unresolvable without instrumented trainer; bt.PairwiseWeights wiring landed (de78521), Splits match advanced 4->6; no ≤1e-5 executing gate |
| 5 | SC-1: YetiRank/YetiRankPairwise/StochasticRank predict ≤1e-5 (Splits/LeafValues/StagedApprox/Predictions) | FAILED | model.json MISSING for all three; oracle tests pass by absent-fixture invariant only; RNG draw-log oracles green (separate partial validation) |
| 6 | SC-2: NDCG, DCG, MAP, MRR, ERR, PFound, PrecisionAt, RecallAt, QueryAUC each match upstream ≤1e-5 | VERIFIED | ranking_metrics_oracle_test 18/18 PASS; flat eval byte-identical (D-04); no regression |
| 7 | SC-3: YetiRank/StochasticRank RNG streams validated against C++-instrumented harness (D-07) | FAILED | Self-oracle generators bit-exact vs cb-core::TFastRng64/std_normal; RNG draw-log gated ≤1e-5. But (a) the instrumented TRAINER build (D-07 requirement) was never executed, and (b) the committed stochasticrank ground-truth JSONL serializes at ~6 decimal digits (std::to_string), insufficient to certify ≤1e-5 for values with magnitude ≥ 1.0 (REVIEW CR-01, unfixed) |
| 8 | CR-02 CLOSED: QueryRMSE/QuerySoftMax gradients not double-weighted for non-uniform weights | VERIFIED | boosting.rs:2292 branches `if group_spans.is_some() { ders.der1.clone() } else { der1 * weight ... }`; grouped_weight_regression_test gates the invariant with non-uniform weights [2.0,0.5,1.5,1.0,0.25] — passes AND distinguishes from pre-fix code; commit ff2fd8e |
| 9 | CR-01 CLOSED: StochasticRank NDCG calc_dcg_metric_diff uses consistent pos_weights | VERIFIED | ranking_der.rs:884 signature includes `pos_weights: &[f64]`; lines 899-900 read via `.get().copied().unwrap_or(0.0)`; raw `1.0/ndcg_denominator(pos)` recompute removed; `calc_dcg_metric_diff_reads_normalized_pos_weights_ndcg` regression test confirms pre-fix vs post-fix diverges for ideal_dcg != 1.0; commit 6cbe2eb |

**Score:** 7/9 truths verified (up from 4/9)

### Gap-Closure Regression Check

Truths 1, 2, 3, 6 (VERIFIED in initial pass) were spot-checked for regression:

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| QueryRMSE oracle | `cargo test -p cb-train --test queryrmse_oracle_test` | 1/1 PASS | NO REGRESSION |
| QuerySoftMax oracle | `cargo test -p cb-train --test querysoftmax_oracle_test` | 1/1 PASS | NO REGRESSION |
| LambdaMart oracle | `cargo test -p cb-train --test lambdamart_oracle_test` | 1/1 PASS | NO REGRESSION |
| Ranking metrics oracle | `cargo test -p cb-train --test ranking_metrics_oracle_test` | 18/18 PASS | NO REGRESSION |
| cb-compute lib suite | `cargo test -p cb-compute --lib` | 122/122 PASS | NO REGRESSION |
| grouped_weight_regression | `cargo test -p cb-train --test grouped_weight_regression_test` | 2/2 PASS | VERIFIED CR-02 FIX |
| stochasticrank centering | `cargo test -p cb-oracle stochasticrank_centering --tests` | 2/2 PASS | VERIFIED WR-03 FIX |
| pairlogit oracle (deferred) | `cargo test -p cb-train --test pairlogit_oracle_test` | 1 ignored | PROPERLY DEFERRED |
| stochasticrank e2e (absent fixture) | `cargo test -p cb-train --test stochasticrank_oracle_test` | 2/2 (absent-fixture invariant) | DEFERRED |
| yetirank e2e (absent fixture) | `cargo test -p cb-train --test yetirank_oracle_test` | 2/2 (absent-fixture invariant) | DEFERRED |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/cb-train/src/query_info.rs` | QueryInfo/Competitor + build_query_info | VERIFIED | Present; struct QueryInfo, build_query_info |
| `crates/cb-compute/src/ranking_der.rs` | Grouped der seam; pos_weights parameter in calc_dcg_metric_diff; sum_f64 in lambdamart_ideal_ndcg | VERIFIED | 935+ lines; pos_weights at line 884; sum_f64(&terms) at line 619; 22 uses of sum_f64 confirmed |
| `crates/cb-train/src/boosting.rs` | CR-02 fix: group_spans.is_some() branch for weighted_der1; calc_pairwise_weights / uses_pairwise_weights | VERIFIED | Line 2292: if group_spans.is_some() branch; uses_pairwise_weights + calc_pairwise_weights present |
| `crates/cb-train/src/pairwise_leaves.rs` | Cholesky pairwise leaf solve; make_zero_average via sum_f64 | VERIFIED | sum_f64(res)/n at line 207; D-08 citation present |
| `crates/cb-oracle/generator/stochasticrank_oracle.cpp` | Centering via left-to-right fold (WR-03) | VERIFIED | `for (double s : shifted) avrg += s;` at line 185; std::accumulate removed from centering line |
| `crates/cb-train/tests/queryrmse_oracle_test.rs` | Per-stage ≤1e-5 oracle for QueryRMSE | VERIFIED | 1/1 PASS |
| `crates/cb-train/tests/querysoftmax_oracle_test.rs` | Per-stage ≤1e-5 oracle for QuerySoftMax | VERIFIED | 1/1 PASS |
| `crates/cb-train/tests/lambdamart_oracle_test.rs` | Per-stage ≤1e-5 oracle for LambdaMart | VERIFIED | 1/1 PASS |
| `crates/cb-train/tests/pairlogit_oracle_test.rs` | Per-stage ≤1e-5 oracle for PairLogit | PARTIAL | EXISTS (175 lines, full compare_stage wiring, commit ae6eb67) but #[ignore]'d — leaf-der2 parity deferred |
| `crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs` | Per-stage ≤1e-5 oracle for PairLogitPairwise | PARTIAL | EXISTS (175 lines) but #[ignore]'d — same root cause |
| `crates/cb-train/tests/grouped_weight_regression_test.rs` | Non-uniform-weight CR-02 regression gate | VERIFIED | 265 lines; 2/2 PASS; distinguishes pre-fix from post-fix code |
| `crates/cb-oracle/generator/stochasticrank_centering_test.rs` | >4-doc non-zero-mu accumulation-order parity test | VERIFIED | EXISTS; tests/ shim at cb-oracle/tests/stochasticrank_centering_test.rs; 2/2 PASS |
| `crates/cb-oracle/fixtures/ranking_corpus/yetirank/model.json` | Trained YetiRank fixture | MISSING | Only yetirank_rng_groundtruth.jsonl exists |
| `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json` | Trained StochasticRank fixture | MISSING | Only stochasticrank_rng_groundtruth.jsonl exists |
| `crates/cb-train/src/ranking_metrics.rs` | Per-group ranking metric formulas | VERIFIED | 460 lines; 4 uses of sum_f64 |
| `crates/cb-train/tests/ranking_metrics_oracle_test.rs` | Per-metric ≤1e-5 oracle (all 9) | VERIFIED | 18/18 PASS |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `boosting.rs:2292` | `ders.der1.clone()` | `group_spans.is_some()` branch (CR-02) | VERIFIED | Grouped ranking ders not re-multiplied by weight |
| `boosting.rs:uses_pairwise_weights` | `calc_pairwise_weights` | `eff_weights` fed to `score_weights` + `scaled_l2` | VERIFIED | PairLogit/PairLogitPairwise/YetiRank split-scoring rides bt.PairwiseWeights |
| `ranking_der.rs:calc_dcg_metric_diff` | `pos_weights` slice | `old_pos`/`new_pos` reads via `.get().unwrap_or(0.0)` | VERIFIED | Normalized pos_weights read at lines 899-900 |
| `ranking_der.rs:lambdamart_ideal_ndcg` | `cb_core::sum_f64` | `sum_f64(&terms)` at line 619 | VERIFIED | D-08 compliant |
| `pairwise_leaves.rs:make_zero_average` | `cb_core::sum_f64` | `sum_f64(res) / n` at line 207 | VERIFIED | D-08 compliant |
| `stochasticrank_oracle.cpp` | left-to-right fold | `for (double s : shifted) avrg += s;` | VERIFIED | Matches sum_f64 order |
| `crates/cb-train/src/boosting.rs` | `compute_gradients_grouped` | ranking-loss der dispatch | VERIFIED | boosting.rs:2189 |
| `crates/cb-train/src/metrics.rs` | `ranking_metrics::eval_grouped` | EvalMetric group seam | VERIFIED | eval_grouped at metrics.rs:346 |

### Data-Flow Trace (Level 4)

Applies only to artifacts rendering dynamic data. For this phase the oracle tests are the primary verification path; the key data flows are:

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `queryrmse_oracle_test.rs` | staged approx, predictions | train_ranking → cb-train boosting | YES — trains from ranking_corpus fixtures | FLOWING |
| `ranking_metrics_oracle_test.rs` | metric values | eval_grouped over fixed approx vectors | YES — loaded from .npy files | FLOWING |
| `grouped_weight_regression_test.rs` | weighted ders | calc_ders_for_queries with non-unit weights | YES — inline synthetic groups | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| QueryRMSE oracle ≤1e-5 (4 stages) | `cargo test -p cb-train --test queryrmse_oracle_test` | 1/1 PASS | PASS |
| QuerySoftMax oracle ≤1e-5 (4 stages) | `cargo test -p cb-train --test querysoftmax_oracle_test` | 1/1 PASS | PASS |
| LambdaMart oracle ≤1e-5 (4 stages) | `cargo test -p cb-train --test lambdamart_oracle_test` | 1/1 PASS | PASS |
| Ranking metrics oracle ≤1e-5 (9 metrics, 18 cases) | `cargo test -p cb-train --test ranking_metrics_oracle_test` | 18/18 PASS | PASS |
| cb-compute lib suite (CR-01 NDCG test included) | `cargo test -p cb-compute --lib` | 122/122 PASS | PASS |
| CR-02 non-uniform weight invariant | `cargo test -p cb-train --test grouped_weight_regression_test` | 2/2 PASS | PASS |
| WR-03 centering accumulation parity | `cargo test -p cb-oracle stochasticrank_centering --tests` | 2/2 PASS | PASS |
| YetiRank per-stage trained model ≤1e-5 | `cargo test -p cb-train --test yetirank_oracle_test` | 2/2 pass on absent-fixture invariant | DEFERRED (no model.json) |
| StochasticRank per-stage trained model ≤1e-5 | `cargo test -p cb-train --test stochasticrank_oracle_test` | 2/2 pass on absent-fixture invariant | DEFERRED (no model.json) |
| PairLogit ≤1e-5 oracle | `cargo test -p cb-train --test pairlogit_oracle_test` | 1 ignored (leaf-der2 deferred) | DEFERRED (#[ignore]'d) |

### Probe Execution

Step 7c: No probes declared in PLAN frontmatter. The standalone C++ oracle generators (yetirank_oracle.cpp / stochasticrank_oracle.cpp) are not conventional probe scripts but were compiled and run during 06.3-08 execution; stochasticrank_rng_groundtruth.jsonl was byte-identical to committed fixture. No new probes to run.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| LOSS-04 | Plans 02, 03, 04, 07, 08, 09 | Ranking losses — YetiRank/Pairwise, PairLogit/Pairwise, QueryRMSE, QuerySoftMax, LambdaMart, StochasticRank | PARTIAL | QueryRMSE/QuerySoftMax/LambdaMart oracle-locked ≤1e-5 (3/6 losses complete); CR-01/CR-02/WR-01/WR-02/WR-03 correctness gaps CLOSED; PairLogit/PairLogitPairwise oracle tests exist but #[ignore]'d on leaf-der2 parity; YetiRank/YetiRankPairwise/StochasticRank ≤1e-5 trainer gate never run. REQUIREMENTS.md marks LOSS-04 as `[ ]` (incomplete). |
| LOSS-05 | Plan 05 | Ranking metrics — all 9 eval-only, ≤1e-5 | SATISFIED | 18/18 oracle tests pass; REQUIREMENTS.md marks LOSS-05 as `[x]`. |

### Anti-Patterns Found (Gap-Closure Delta)

The following items are from the 06.3-REVIEW.md (gap-closure code review). Items marked CLOSED were addressed in the initial verification gap round; NEW items are introduced by the gap-closure plans.

| File | Line | Pattern | Severity | Impact | Status |
|------|------|---------|----------|--------|--------|
| `crates/cb-oracle/generator/stochasticrank_oracle.cpp` | 199, 202 | `std::to_string(noise[d])` / `std::to_string(scores[d])` — ~6-decimal precision in committed JSONL fixture; insufficient to certify ≤1e-5 for values >= 1.0 (REVIEW CR-01) | BLOCKER | Committed ground-truth cannot support the ≤1e-5 claim for the RNG draw-log gate; truth #7 evidence is structurally inadequate | UNFIXED |
| `crates/cb-train/tests/pairlogit_oracle_test.rs` | 128 | `#[ignore]`'d end-to-end oracle — headline deliverable of gap-closure round is non-executing (REVIEW WR-01) | WARNING | PairLogit/PairLogitPairwise parity remains unverified; structural gap, not a code defect | INTENTIONAL DEFERRAL — documented in deferred-items.md |
| `crates/cb-compute/src/ranking_der.rs` | 903-909 | Raw `cum_sum[old_pos]` / `cum_sum_low[old_pos]` / `cum_sum_up[new_pos+1]` indexing despite per-function bounds-checked pos_weights reads two lines above (REVIEW WR-02) | WARNING | Inconsistent with same function's own stated CLAUDE.md unchecked-index ban; latent panic on future caller index desync | UNFIXED (pre-existing, introduced by 06.3-06 CR-01 fix) |
| `crates/cb-train/src/boosting.rs` | 2659-2660 | `leaf_weights_for_deltas = if group_spans.is_some() { unit_weights } else { eff_weights }` passes unit weights for YetiRank Gradient leaf, while upstream uses bt.PairwiseWeights for Gradient `sumWeight` (REVIEW WR-03) | WARNING | YetiRank Gradient leaf denominator would mix doc-count sumWeight with pairwise-weight-scaled L2; latent (YetiRank E2E deferred) | UNFIXED |
| `crates/cb-train/tests/grouped_weight_regression_test.rs` | 211-213 | Distinguish-guard uses `p.abs() > 1e-12` threshold but inner assertion requires `(p*w - p).abs() > 1e-6`; guard does not imply assertion for `p` in `[1e-12, 1e-6]` with `w` near 1 (REVIEW WR-04) | WARNING | Latent false-negative in the regression test for ders in the `[1e-12, 1e-6]` range; today's corpus produces `p ~ O(0.1)` so currently safe | UNFIXED |
| `crates/cb-oracle/generator/stochasticrank_oracle.cpp` | 242 | `const double mu = 0.0` — committed fixture uses the exact masking case (shifted sum = 0, centering is a no-op), defeating exercising of centering behavior (REVIEW WR-05) | WARNING | The Rust compensating test uses mu != 0; but the generated fixture itself never stresses the centering path | UNFIXED |

**Debt marker audit (gap-closure files):** No TBD/FIXME/XXX markers found in any of the gap-closure modified files. Clean.

### Human Verification Required

None. All remaining gaps are code-level (observable in source) or fixture-level (verified by file absence). No UI, UX, external-service, or timing-sensitive items require human testing.

## Gaps Summary

This re-verification closes 5 of the 9 original gaps, advancing the score from 4/9 to 7/9. The three remaining open gaps all share a single root cause: the instrumented catboost 1.2.10 trainer build remains infeasible (toolchain absent, disk 97%/link-failure regime).

**Closed by this gap-closure round:**
- CR-01 (VERIFICATION) — NDCG gradient scale mismatch: CLOSED
- CR-02 (VERIFICATION) — QueryRMSE/QuerySoftMax double-weighting: CLOSED
- WR-01/WR-02/WR-03 — D-08 summation-discipline violations: CLOSED
- PairLogit oracle test files — MISSING artifacts: CLOSED (files now exist and are wired)
- PairLogit bt.PairwiseWeights split-scoring: WIRED (Splits match advanced 4→6)

**Remaining open (all require instrumented trainer build or depend on it):**

1. **PairLogit/PairLogitPairwise end-to-end oracle** (truth #4): Oracle test files are substantive and wired but #[ignore]'d. The residual is the leaf-der2 reduction — single cross-leaf-pair object needs an effective denominator of ~23 from sumDer=0.5/sumDer2=-0.25, impossible from plain per-object der2 sum. Requires the instrumented trainer's per-leaf SumDer2 log to diagnose and fix.

2. **YetiRank/YetiRankPairwise/StochasticRank trained-model fixtures** (truth #5): model.json absent for all three; oracle tests correctly assert the deferred-fixture invariant and will auto-upgrade to the full ≤1e-5 gate when fixtures land. The RNG draw-log is validated — this is specifically the full training loop that is unvalidated.

3. **SC-3/D-07 instrumented-harness RNG validation** (truth #7): Self-oracle generators validated; trainer-level CB_INSTRUMENT_LOG comparison not performed. Additionally, the committed stochasticrank ground-truth JSONL uses ~6-decimal serialization (REVIEW CR-01, unfixed), making the existing ≤1e-5 fixture gate structurally undersized for scores with magnitude >= 1.

**LOSS-04 remains incomplete.** Three of six ranking losses (QueryRMSE, QuerySoftMax, LambdaMart) are oracle-locked ≤1e-5. PairLogit, PairLogitPairwise, YetiRank, YetiRankPairwise, and StochasticRank do not yet have passing end-to-end per-stage ≤1e-5 gates.

**LOSS-05 is complete.** All nine ranking metrics are oracle-locked ≤1e-5.

**Additional latent issues from code review (not blocking current truths, not new regressions):** REVIEW WR-02 (raw `[]` indexing in calc_dcg_metric_diff), REVIEW WR-03 (YetiRank Gradient leaf unit-weight denominator mismatch), REVIEW WR-04 (distinguish-guard threshold mismatch in grouped_weight_regression_test.rs), REVIEW WR-05 (mu=0 masking in stochasticrank fixture). All are latent — they are invisible to current uniform-weight fixtures and are unmasked only when the deferred trainer fixtures land.

---

_Verified: 2026-06-17T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification after gap-closure plans 06.3-06 through 06.3-09_
