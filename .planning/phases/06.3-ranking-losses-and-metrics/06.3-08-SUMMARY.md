---
phase: 06.3-ranking-losses-and-metrics
plan: 08
subsystem: testing
tags: [ranking, loss-04, stochasticrank, pairlogit-pairwise, sum_f64, d-08, summation-discipline, oracle]

# Dependency graph
requires:
  - phase: 06.3-ranking-losses-and-metrics
    provides: "ranking_der.rs sum_f64 discipline (06.3-06 WR-02), pairwise_leaves.rs Cholesky leaf path (06.3-03), stochasticrank_oracle.cpp generator (06.3-04)"
provides:
  - "make_zero_average routes its mean through cb_core::sum_f64 (WR-01 / D-08 closed)"
  - "stochasticrank_oracle.cpp centering folds shifted[] in sum_f64 left-to-right order (WR-03 / D-08 closed)"
  - ">4-doc non-zero-mu accumulation-order parity test + order-sensitivity guard for the oracle centering"
  - "automation-verified that the frozen stochasticrank RNG groundtruth is byte-identical post-fix (empty diff)"
affects: [06.3-verify, loss-04, stochasticrank, d-08-summation-discipline]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-08 summation discipline extended to the last two ranking sites (Rust make_zero_average + C++ oracle centering)"
    - "Generator-located test discovered via a thin tests/ #[path] shim (INFRA-06 source/test separation across crate test boundary)"

key-files:
  created:
    - crates/cb-oracle/generator/stochasticrank_centering_test.rs
    - crates/cb-oracle/tests/stochasticrank_centering_test.rs
  modified:
    - crates/cb-train/src/pairwise_leaves.rs
    - crates/cb-train/src/pairwise_leaves_test.rs
    - crates/cb-oracle/generator/stochasticrank_oracle.cpp

key-decisions:
  - "Aligned BOTH sites to cb_core::sum_f64 (strict left-to-right fold) — NO summation exception claimed; loop-order doc-comments dropped (T-06.3-08-02 mitigated)"
  - "Frozen RNG groundtruth needs NO regeneration: the 3-doc mu=0 fixture has an exactly-zero shifted sum, so the centering-order change is a provable no-op (empty diff)"

patterns-established:
  - "Generator-located parity test linked through a tests/ #[path] shim so cargo test -p <crate> --tests discovers it without inlining"

requirements-completed: []  # LOSS-04 stays PARTIALLY OPEN — this plan closes WR-01/WR-03 only, not truths #5/#7

# Metrics
duration: ~14min
completed: 2026-06-17
---

# Phase 06.3 Plan 08: WR-01/WR-03 D-08 Summation-Discipline Gap Closure Summary

**`make_zero_average` and the stochasticrank oracle centering now both route through `cb_core::sum_f64`, closing the last two D-08 summation-discipline WARNINGS; the frozen RNG groundtruth is automation-verified byte-identical (no regeneration required).**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-06-17T10:35:00Z (approx)
- **Completed:** 2026-06-17T10:49:00Z (approx)
- **Tasks:** 2 of 3 executed autonomously; Task 3 is a blocking human-action checkpoint (automation-verified as no-op, pending human sign-off)
- **Files modified:** 5 (3 modified, 2 created)

## Accomplishments
- **WR-01 closed:** `make_zero_average` in `pairwise_leaves.rs` replaced its raw `average += v` loop with `cb_core::sum_f64(res) / n`; the loop-order exception language was deleted and replaced with a D-08 citation. `sum_f64` IS the strict left-to-right fold, so this is bit-identical to the prior loop — the 2x2 + general SPD unit tests pass unchanged.
- **WR-03 closed:** `stochasticrank_oracle.cpp` Stage-1 centering replaced `std::accumulate(shifted.begin(), shifted.end(), 0.0)` with an explicit `double avrg = 0.0; for (double s : shifted) avrg += s;` fold that matches `sum_f64`'s documented left-to-right order, with a comment citing `sum_f64` (reduction.rs) as the D-08 source of truth and `ranking_der.rs:726-727` as the Rust mirror.
- **Accumulation-order parity test added:** a >4-doc (7-doc) NON-ZERO-mu group exercises the centering subtraction (non-zero shift mean) and asserts the `sum_f64`-centered vs generator-order-centered `shifted[]` agree to ≤1e-5; a companion `centering_is_accumulation_order_sensitive` test proves the assertion is non-vacuous (a magnitude-reordered fold demonstrably diverges from `sum_f64` on adversarial `±1e16` magnitudes).
- **Frozen fixture verified unchanged:** the dependency-free generator was compiled (`g++ -std=c++20 -O2 -Wall`, zero catboost includes) and run; the regenerated `stochasticrank_rng_groundtruth.jsonl` is BYTE-IDENTICAL to the committed one (empty `diff`). The self-oracle reproduced `std_normal(seed=0) = [0.6337, -0.5284, -0.4408]` bit-for-bit against `cb-core::TFastRng64`.

## Task Commits

1. **Task 1: WR-01 — make_zero_average via cb_core::sum_f64** - `c9a0f8e` (fix; TDD: extended the zero-average invariant unit test to assert via `sum_f64`)
2. **Task 2: WR-03 — align stochasticrank oracle centering with sum_f64 + >4-doc non-zero-mu test** - `6123c60` (fix; TDD: new accumulation-order parity test file)
3. **Task 3: Regenerate (or confirm unchanged) the frozen RNG groundtruth** - blocking human-action checkpoint; automation completed (empty diff ⇒ no fixture change, nothing committed), awaiting human confirmation.

**Plan metadata:** committed with STATE/ROADMAP update (see final docs commit).

## Files Created/Modified
- `crates/cb-train/src/pairwise_leaves.rs` - `make_zero_average` mean via `cb_core::sum_f64`; loop-order exception doc-comment removed, D-08 citation added.
- `crates/cb-train/src/pairwise_leaves_test.rs` - imports `cb_core::sum_f64`; the zero-average invariant is now measured via `sum_f64`, plus a new end-to-end `compute_pairwise_leaf_deltas` zero-average test.
- `crates/cb-oracle/generator/stochasticrank_oracle.cpp` - centering uses an explicit left-to-right fold matching `sum_f64`; D-08 source-of-truth comment.
- `crates/cb-oracle/generator/stochasticrank_centering_test.rs` - NEW: >4-doc non-zero-mu accumulation-order parity test + order-sensitivity guard (lives next to the generator it validates).
- `crates/cb-oracle/tests/stochasticrank_centering_test.rs` - NEW: thin `#[path]` discovery shim so `cargo test -p cb-oracle stochasticrank_centering --tests` finds the generator-located test.

## Decisions Made
- **Align, do not except (T-06.3-08-02):** both sites were brought into D-08 compliance by routing through `cb_core::sum_f64`; no sequential-order exception was claimed and the "parity contract is the loop order" doc wording was removed. `sum_f64` is itself the strict left-to-right fold, so the numeric fold is unchanged.
- **No fixture regeneration required (T-06.3-08-01):** the committed 3-doc `mu=0` fixture has a shifted sum of exactly `0.0`, so the centering-order change is a provable no-op for the existing corpus. The generator was actually run and diffed — the diff is empty — so the frozen RNG groundtruth is left untouched (the checkpoint's "no regeneration required" branch).
- **Generator-located test + tests/ shim:** to honor both the plan's explicit artifact path (`generator/stochasticrank_centering_test.rs`) AND cargo's integration-test discovery (`tests/*.rs`), the body lives by the generator and a `#[path]` shim in `tests/` makes it discoverable — keeping source/test separation while satisfying the verify command.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] tests/ discovery shim for the generator-located parity test**
- **Found during:** Task 2 (WR-03 test authoring)
- **Issue:** The plan's artifact path `crates/cb-oracle/generator/stochasticrank_centering_test.rs` is outside cargo's auto-discovered `tests/` directory, so `cargo test -p cb-oracle stochasticrank_centering --tests` (the plan's verify command) would not find it.
- **Fix:** Added a thin `crates/cb-oracle/tests/stochasticrank_centering_test.rs` shim that `#[path]`-includes the generator-located body. Both the explicit artifact path and the verify command are satisfied; source/test separation preserved.
- **Files modified:** crates/cb-oracle/tests/stochasticrank_centering_test.rs (created)
- **Verification:** `cargo test -p cb-oracle stochasticrank_centering --tests` discovers and runs 2/2 tests green.
- **Committed in:** 6123c60 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking — test discovery wiring).
**Impact on plan:** No scope creep; the shim is pure test-harness plumbing required for the plan's own verify command.

## Issues Encountered
- The order-sensitivity guard test initially used a `> 1.0` divergence threshold; the chosen `±1e16` adversarial input diverges by ~0.5 between left-to-right and magnitude-ascending folds, so the threshold was lowered to `> 0.25` (still robustly proving the order is observable). Resolved within Task 2; test is green.

## TDD Gate Compliance
Both implementation tasks carried `tdd="true"`. WR-01 extended the existing zero-average invariant assertion to use `sum_f64` (discipline fix with a guarding test). WR-03 added a dedicated RED-capable accumulation-order parity test before/alongside the C++ centering change. No separate `test(...)` commit was split out from the fix commits (the tests are co-committed with their fixes, as is the convention for these gap-closure discipline fixes); the parity tests are present and green.

## Verification Results
- `cargo test -p cb-train pairwise_leaves --lib --tests` — **7 passed, 0 failed** (WR-01 + zero-average invariant via sum_f64).
- `cargo test -p cb-oracle stochasticrank_centering --tests` — **2 passed, 0 failed** (WR-03 >4-doc non-zero-mu + order-sensitivity).
- `cargo test -p cb-train --test stochasticrank_oracle_test` — **2 passed, 0 failed** (absent-fixture invariant + RNG-draw gate untouched).
- `g++ -std=c++20 -O2 -Wall stochasticrank_oracle.cpp` — compiles clean (dependency-free, zero catboost includes); self-oracle bit-exact; regenerated jsonl **byte-identical** to the committed fixture (empty diff).

Acceptance greps: `grep -c 'average += '` (non-comment, pairwise_leaves.rs) = **0**; `grep "sum_f64(res)"` matches inside `make_zero_average`; `std::accumulate` no longer appears on the oracle centering line (only in an explanatory comment).

## Checkpoint Status (Task 3 — blocking human-action)
The OFFLINE generator was compiled and run; the regenerated `stochasticrank_rng_groundtruth.jsonl` is **byte-identical** to the committed fixture (empty diff), because the only committed corpus is the 3-doc `mu=0` group whose shifted sum is exactly `0.0` (the centering-order change is a no-op there). Per the plan's scope_fence directive, the executor does NOT auto-commit/rewrite the frozen fixture — and here there is nothing to rewrite. Awaiting human confirmation: **"approved — no regeneration required"**.

## Next Phase Readiness
- WR-01 and WR-03 D-08 violations are closed; the 06.3 verifier's two D-08 summation-discipline WARNINGS are addressed (the third, WR-02, was closed in 06.3-06).
- **LOSS-04 stays PARTIALLY OPEN:** this plan does NOT close the deferred YetiRank/YetiRankPairwise/StochasticRank end-to-end TRAINER fixtures (truth #5) nor SC-3/D-07 instrumented-harness validation (truth #7); those still require the deferred instrumented catboost build. It does not touch the absent-fixture invariant guards.

## Self-Check: PASSED

All created/modified files present on disk; both task commits (`c9a0f8e`, `6123c60`) found in git history.

---
*Phase: 06.3-ranking-losses-and-metrics*
*Completed: 2026-06-17*
