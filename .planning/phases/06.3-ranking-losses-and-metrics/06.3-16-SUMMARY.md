---
phase: 06.3-ranking-losses-and-metrics
plan: 16
subsystem: testing
tags: [ranking, pairwise, pairlogit, oracle, tree-search, catboost-parity]

# Dependency graph
requires:
  - phase: 06.3-15
    provides: "cb-compute pairwise split-scorer (compute_der_sums + compute_pair_weight_statistics + calculate_pairwise_score)"
provides:
  - "Pairwise split-scoring wired into the greedy oblivious tree search in cb-train (gated on is_pairwise_scoring)"
  - "PairLogitPairwise per-stage oracle un-ignored and passing the full 4-stage <=1e-5 gate"
  - "LOSS-04 gap #1 (PairLogitPairwise, truth #4) CLOSED"
affects: [06.3-17, yetirankpairwise, ranking-losses, LOSS-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pairwise candidate-scoring branch in tree.rs gated on is_pairwise_scoring; non-pairwise path byte-identical (D-04)"

key-files:
  created: []
  modified:
    - crates/cb-train/src/tree.rs
    - crates/cb-train/src/tree_pairwise_test.rs
    - crates/cb-train/src/boosting.rs
    - crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs

key-decisions:
  - "Task 2 fixture NOT regenerated: the existing PairLogitPairwise/model.json is genuine catboost 1.2.10 output (tags/v1.2.10, model_guid 7a8f259-…, train_finish_time 2026-06-16T23:14:35Z), committed in bef767d predating this gap round; user APPROVED as genuine — no new fixture commit"

patterns-established:
  - "Pairwise losses (IsPairwiseScoring) score candidate splits through calculate_pairwise_score, not the pointwise der histogram"

requirements-completed: [LOSS-04]

# Metrics
duration: 12min
completed: 2026-06-17
---

# Phase 06.3 Plan 16: PairLogitPairwise End-to-End Oracle Closure Summary

**The pairwise split-scorer is wired into the greedy oblivious tree search behind `is_pairwise_scoring`, closing the tree-0 split-1 SPLIT-SELECTION divergence so the PairLogitPairwise per-stage oracle passes all four stages (Splits | LeafValues | StagedApprox | Predictions) at <=1e-5 with no tolerance weakened.**

## Performance

- **Duration:** ~12 min (continuation agent, resumed from blocking-human checkpoint)
- **Completed:** 2026-06-17
- **Tasks:** 3 (Task 1 pre-committed in 6aaa769; Task 2 satisfied by existing genuine fixture; Task 3 landed here)
- **Files modified:** 4 (1 in this continuation: the test un-ignore)

## Accomplishments
- Removed the `#[ignore]` + the 06.3-13 deferral comment block from `pairlogit_pairwise_oracle_test.rs`; the full 4-stage <=1e-5 gate now runs and PASSES (`1 passed; 0 ignored`).
- Confirmed the tree-0 split-1 SPLIT-SELECTION divergence (upstream f0@1.628 vs prior pointwise-histogram f1@1.816) is resolved by the Task-1 pairwise scorer routing (6aaa769).
- Verified D-04 non-regression: PairLogit (pointwise), QueryRMSE, QuerySoftMax, LambdaMart, YetiRank oracles all still pass <=1e-5 — the non-pairwise tree-search path is byte-identical.
- Verified Task-1 unit tests (`tree_pairwise_test.rs`, 5 tests) still green within the 15-test pairwise lib suite.

## Task Commits

1. **Task 1: Route candidate scoring through the pairwise scorer when is_pairwise_scoring** — `6aaa769` (feat) — *committed in the prior (pre-checkpoint) executor session; verified present.*
2. **Task 2: Generate + freeze the PairLogitPairwise fixture OFFLINE (catboost 1.2.10)** — *no commit.* The fixture (`crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/{model.json,staged.npy,predictions.npy}`) is genuine catboost 1.2.10 training output already committed in `bef767d` (06.3-03), provenance confirmed and user-APPROVED. No regeneration, no tracked changes → no commit (per checkpoint resume instruction).
3. **Task 3: Un-ignore pairlogit_pairwise_oracle_test.rs and pass the full <=1e-5 gate** — `09bd53e` (test)

**Plan metadata:** (this SUMMARY + STATE/ROADMAP/REQUIREMENTS) — final docs commit below.

## Files Created/Modified
- `crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs` — removed `#[ignore]` (line 143) + the multi-line 06.3-13 deferral comment (117-142); updated doc-header to record CLOSURE (gap #1) referencing scorer commit 6aaa769.
- *(Task 1, prior commit 6aaa769)* `crates/cb-train/src/tree.rs`, `crates/cb-train/src/tree_pairwise_test.rs`, `crates/cb-train/src/boosting.rs` — pairwise candidate-scoring branch + unit tests + boosting plumbing.

## Decisions Made
- **Fixture provenance accepted, not regenerated.** Per the blocking-human checkpoint, the user signed off that `PairLogitPairwise/model.json` is genuine catboost 1.2.10 output (Branch tags/v1.2.10, commit b1bd2a6d…, model_guid 7a8f259-1e87d60c-7feb6af4-48bb9c30, train_finish_time 2026-06-16T23:14:35Z, loss PairLogitPairwise). Its trained params match the test's `base_params` (depth 2, 5 iters, lr 0.3, l2_leaf_reg 5, boosting_type Plain). Task 2 acceptance treated as satisfied — no regeneration, no fabricated fixture.

## Deviations from Plan
None - plan executed exactly as written. (Task 2 produced no commit because the approved genuine fixture required no changes — this is the expected RUN-ONCE/COMMIT/D-08 outcome, not a deviation.)

## Issues Encountered
- The plan's Task-1 verify line names `cargo test -p cb-train --lib tree_pairwise`, but the unit tests live under test-fn names not prefixed `tree_pairwise`; filtering by `--lib pairwise` confirms all 15 pairwise lib tests (incl. the 5 in `tree_pairwise_test.rs`) pass. No code change needed — naming-filter nuance only.

## Verification Results
- `cargo test -p cb-train --test pairlogit_pairwise_oracle_test` → **1 passed; 0 ignored** (4 stages green <=1e-5).
- D-04 non-regression: `pairlogit_oracle_test` 1/1, `queryrmse_oracle_test` 1/1, `querysoftmax_oracle_test` 1/1, `lambdamart_oracle_test` 1/1, `yetirank_oracle_test` 2/2 — all <=1e-5.
- `cargo test -p cb-train --lib pairwise` → 15 passed.
- No `#[ignore]` attribute remains in the test file (grep). No `compare.rs` tolerance constant changed.

## Next Phase Readiness
- LOSS-04 gap #1 (PairLogitPairwise / truth #4) CLOSED.
- The pairwise tree-search wiring is now reusable by 06.3-17 (YetiRankPairwise fixture + un-ignore), the sibling `*Pairwise` gap.
- Remaining LOSS-04 deferrals: YetiRankPairwise end-to-end (06.3-17) and the SC-3 instrumented harness items tracked in prior 06.3 plans.

## Self-Check: PASSED
- SUMMARY.md present.
- Commits 6aaa769 (Task 1) and 09bd53e (Task 3) present in git history.
- No `#[ignore]` attribute in `pairlogit_pairwise_oracle_test.rs` (only a `//` doc-comment mention of the removed attribute); test runs `0 ignored`.

---
*Phase: 06.3-ranking-losses-and-metrics*
*Completed: 2026-06-17*
