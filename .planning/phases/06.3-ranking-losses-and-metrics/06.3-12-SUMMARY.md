---
phase: 06.3-ranking-losses-and-metrics
plan: 12
subsystem: ranking-losses-oracles
tags: [stochasticrank, rng-oracle, fixture, full-precision, centering, regression-test]
requires:
  - cb-oracle/generator/stochasticrank_oracle.cpp (OFFLINE dependency-free generator)
  - cb-core::TFastRng64 / std_normal (oracle-locked RNG stream)
provides:
  - full-precision (%.17g) StochasticRank RNG ground-truth fixture (non-masking mu!=0 / 5-doc unit)
  - scale-matched grouped_weight_regression distinguish-guard
affects:
  - cb-train::stochasticrank_oracle_test (draw-count now derived from ground truth)
  - cb-oracle::stochasticrank_centering_test
tech-stack:
  added: []
  patterns:
    - "snprintf(buf, \"%.17g\", x) for f64 ground-truth serialization (17 sig digits round-trip)"
    - "RUN-ONCE/COMMIT human-signed frozen RNG fixture discipline"
    - "derive consuming-test draw count from ground-truth length, not hardcoded"
key-files:
  created: []
  modified:
    - crates/cb-oracle/generator/stochasticrank_oracle.cpp
    - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl
    - crates/cb-train/tests/grouped_weight_regression_test.rs
    - crates/cb-train/tests/stochasticrank_oracle_test.rs
decisions:
  - "Derive the stochasticrank RNG draw count from the frozen ground truth (expected.len()) instead of hardcoding 3, so the test survives fixture regenerations (Rule 3 blocking fix for the new 5-doc unit)"
  - "Regenerated fixture is human-signed under RUN-ONCE/COMMIT; self-oracle reproduces it bit-for-bit from the committed generator source"
metrics:
  duration: ~10m (continuation: Task 3 only)
  completed: 2026-06-17
---

# Phase 06.3 Plan 12: Full-Precision StochasticRank RNG Ground Truth Summary

Closed REVIEW CR-01 (BLOCKER), WR-05, and WR-04 for the StochasticRank RNG ground truth: the committed fixture now serializes `noise`/`score` at full f64 round-trip precision (`%.17g`) over a non-masking `mu=0.5` / 5-doc centering unit, and the grouped-weight regression distinguish-guard is gated on the same scale as its assertion — all trainer-independent (the generator is dependency-free, ZERO catboost includes, RUN-ONCE/COMMIT offline).

## What Was Built

This is a CONTINUATION executor that resumed at Task 3 after a `blocking-human` checkpoint was approved. Tasks 1 and 2 were completed and committed by the prior executor:

| Task | Name | Commit |
| ---- | ---- | ------ |
| 1 | CR-01 + WR-05: `%.17g` serialization + non-masking `mu!=0` / 5-doc instrumented unit | `fbccdef` |
| 2 | WR-04: distinguish-guard gated on the assertion scale `(p*(w-1.0)).abs() > 1e-6` | `492c16f` |

### Task 3 (this executor) — RUN-ONCE regenerate + human-signed fixture

- Verified the candidate fixture `/tmp/stochasticrank_rng.jsonl` reproduces **bit-for-bit** by rebuilding the committed generator (`g++ -std=c++20 -O2 -Wall`, clean) and re-running it with `CB_INSTRUMENT_LOG`. Self-oracle stderr confirmed the `std_normal(seed=0)` cross-check vector and the `mu=0.500000` non-masking centering input.
- Copied the regenerated full-precision JSONL over the committed fixture at `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_rng_groundtruth.jsonl`. The fixture grew from a 6-decimal `mu=0` 3-doc unit to a `%.17g` `mu=0.5` 5-doc unit.
- Both consuming oracles GREEN against the regenerated ground truth:
  - `cargo test -p cb-train --test stochasticrank_oracle_test` → 2/2 PASS (RNG-draw-log gate ≤1e-5)
  - `cargo test -p cb-oracle stochasticrank_centering --tests` → 2/2 PASS (the `..._more_than_four_docs_non_zero_mu` unit now exercises the centering path)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `stochasticrank_oracle_test` hardcoded a 3-draw expectation**
- **Found during:** Task 3, first run of `stochasticrank_oracle_test` after copying the new fixture.
- **Issue:** `stochasticrank_rng_draw_log_oracle` hardcoded `let count = 3` and a stale "3 docs" doc comment, matching the OLD `mu=0` 3-doc fixture. The new non-masking WR-05 fixture has 5 docs, so the test panicked on a `COUNT` mismatch (`left: 3, right: 5`) — the regenerated full-precision ground truth (the intended deliverable) could not be consumed.
- **Fix:** Derive the draw count from the frozen ground truth (`let count = expected.len();`) and updated the doc comment to describe the 5-doc / `mu!=0` unit. The Rust `std_normal` stream (seed 5 = `random_seed` + group 0) still reproduces the noise draws ≤1e-5; only the count source changed. This makes the test robust to future RUN-ONCE regenerations.
- **Files modified:** `crates/cb-train/tests/stochasticrank_oracle_test.rs`
- **Commit:** see Self-Check.

## Known Stubs

None. The end-to-end per-stage StochasticRank trainer compare (`stochasticrank_end_to_end_per_stage`) remains a wired-but-deferred guard (D-6.3-03b, instrumented trainer build absent) — this is pre-existing and out of scope for this plan, which is explicitly the trainer-INDEPENDENT fixture/test closure. NO `#[ignore]`, NO weakened tolerance was introduced.

## Verification

- Generator: `g++ -std=c++20 -O2 -Wall` clean; serializes f64 via `%.17g`; `mu=0.5`, 5 docs.
- Self-oracle: regenerated stream is bit-for-bit identical to the candidate human-signed fixture; `std_normal(seed=0)` cross-check vector matches the README reference.
- `stochasticrank_oracle_test`: 2/2 PASS (RNG draw ≤1e-5).
- `stochasticrank_centering_test`: 2/2 PASS (non-masking centering path exercised).
- `grouped_weight_regression_test`: covered by Task 2 (commit `492c16f`).
