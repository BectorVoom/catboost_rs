---
phase: 06.3-ranking-losses-and-metrics
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-oracle/generator/build_instrumented_trainer.sh
  - crates/cb-oracle/generator/gen_ranking_fixtures.py
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/staged.npy
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/predictions.npy
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_pertree_noise_groundtruth.jsonl
  - crates/cb-train/tests/stochasticrank_oracle_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/yetirank.rs
requirements: [LOSS-04]
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The per-group StochasticRank noise seed model (randomSeed + group_index, error_functions.h:1257) is analyzed and the Rust per-tree noise stream reproduces the instrumented catboost 1.2.10 trainer's per-tree Gaussian draw log bit-for-bit"
    - "A StochasticRank model.json + staged.npy + predictions.npy fixture, trained OFFLINE by the catboost 1.2.10 trainer with an explicit metric param, is committed"
    - "stochasticrank_oracle_test.rs::stochasticrank_end_to_end_per_stage takes the present-fixture branch and passes all four per-stage compare gates at ≤1e-5 (querywise pointwise leaf)"
    - "D-07 / SC-3 is closed for StochasticRank: the trainer-level per-tree noise draw comparison (not just the standalone self-oracle) passes"
  artifacts:
    - path: "crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json"
      provides: "frozen upstream StochasticRank trained model"
    - path: "crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_pertree_noise_groundtruth.jsonl"
      provides: "instrumented per-tree per-group Gaussian noise draw log (CB_INSTRUMENT_LOG)"
    - path: "crates/cb-train/tests/stochasticrank_oracle_test.rs"
      provides: "end-to-end per-stage ≤1e-5 oracle + per-tree noise draw gate taking the present-fixture branch"
  key_links:
    - from: "crates/cb-train/tests/stochasticrank_oracle_test.rs:stochasticrank_end_to_end_per_stage"
      to: "ranking_corpus/stochasticrank/model.json"
      via: "compare_stage(Splits|LeafValues|StagedApprox|Predictions) once model.json exists"
      pattern: "compare_stage"
    - from: "StochasticRank per-tree noise stream (cb_core::std_normal over TFastRng64(random_seed+group_index))"
      to: "stochasticrank_pertree_noise_groundtruth.jsonl"
      via: "per-tree per-group draw-log compare ≤1e-5 (D-07)"
      pattern: "gauss_draw|std_normal"
---

<objective>
Close gap #3: StochasticRank end-to-end ≤1e-5 + the D-07 trainer-level RNG
validation. StochasticRank is the OTHER randomized loss, with a DISTINCT
per-group Monte-Carlo noise-seed model — `randomSeed + group_index`
(`error_functions.h:1257`), drawn fresh each `CalcDersForQueries`, NOT the
pairwise YetiRank seeder. The standalone per-draw `std_normal` self-oracle is
already GREEN; what remains is (a) mapping that per-group noise stream onto the
per-TREE trainer consumption (structure + split + leaf draws), validated against
the instrumented catboost 1.2.10 trainer's per-tree draw log, and (b) freezing a
trained `model.json` fixture (the corpus config needs an explicit `metric` param)
and activating the end-to-end per-stage gate.

This is INDEPENDENT of the pairwise split-scorer track (06.3-15/16/17) — it runs
in parallel. It requires the instrumented trainer toolchain, which may need
re-fetching/rebuilding (the persisted `/tmp` artifacts are not guaranteed to
survive); the first task is a [BLOCKING] feasibility probe that escalates rather
than silently expanding scope (D-6.3-03b / D-08).

Purpose: deliver the FULL ≤1e-5 StochasticRank oracle + the trainer-level D-07
gate (gap #3 of LOSS-04's three; also the truth-#7 StochasticRank residual).
Output: per-tree noise ground truth + frozen model.json fixture + present-branch
passing end-to-end oracle + trainer-level noise gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| StochasticRank target in `crates/cb-oracle/generator/gen_ranking_fixtures.py` | generator edit | adds explicit `metric` param (e.g. DCG/NDCG) the prior config did not pin |
| `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json` | fixture | frozen upstream StochasticRank trained model |
| `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/staged.npy` | fixture | per-iteration staged approx |
| `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/predictions.npy` | fixture | final raw predictions |
| `crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_pertree_noise_groundtruth.jsonl` | fixture | CB_INSTRUMENT_LOG per-tree per-group Gaussian noise draws |
| present-fixture branch of `stochasticrank_end_to_end_per_stage` (now LIVE) | test | full 4-stage ≤1e-5 gate |
| `stochasticrank_pertree_noise_oracle` | test (new) | trainer-level D-07 per-tree noise draw gate ≤1e-5 |
| per-group seed wiring in `crates/cb-train/src/boosting.rs` (random_seed + group_index per tree) if a desync is found | code | only if the per-tree gate exposes a mismatch |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Instrumented trainer toolchain feasibility probe (GO / escalate)</name>
  <files>crates/cb-oracle/generator/build_instrumented_trainer.sh, crates/cb-oracle/generator/instrumented_trainer_STATUS.md</files>
  <read_first>
    - crates/cb-oracle/generator/build_instrumented_trainer.sh (the sudo-free idempotent driver; disk gate ≥25GB; clang-18/lld-18 restore; CB_INSTRUMENT_LOG patch of yetirank/error_functions TUs; build_native.py)
    - crates/cb-oracle/generator/instrumented_trainer_STATUS.md (the prior GO record + /tmp artifact paths)
    - .planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md "Environment Availability" (A2: persisted /tmp toolchain NOT found in research session; disk 67G free / 71% used; rebuild recipe)
    - MEMORY: catboost-instrumented-trainer-build + instrumented-trainer-toolchain-persists (the recipe + the /tmp reuse)
  </read_first>
  <decision>Is the instrumented catboost 1.2.10 trainer available (reusable /tmp artifacts or a successful incremental/fresh rebuild) for generating the StochasticRank per-tree noise log + model.json?</decision>
  <context>
    StochasticRank's D-07 trainer-level gate + the model.json fixture both require running the instrumented trainer with CB_INSTRUMENT_LOG over the StochasticRank corpus config. The toolchain lives under /tmp and may have been cleared. The driver is disk-gated at 25GB and refuses to link below it. This decision records GO (artifacts present/rebuilt) or escalates (toolchain gone + disk in the link-failure regime) — escalate-don't-weaken, never fabricate the fixture.
  </context>
  <options>
    <option id="reuse">
      <name>Reuse persisted /tmp toolchain</name>
      <pros>Fastest; build_instrumented_trainer.sh is idempotent and reuses /tmp/clang18_prefix + /tmp/cb_build313</pros>
      <cons>Only valid if /tmp survived; verify the staged _catboost.so still loads + emits CB_INSTRUMENT_LOG</cons>
    </option>
    <option id="rebuild">
      <name>Re-run build_instrumented_trainer.sh (incremental or fresh)</name>
      <pros>Recovers the toolchain via the recorded sudo-free recipe; disk currently healthier than the memory's ~100%-full regime</pros>
      <cons>Heavier; requires ≥25GB free; the script must patch the StochasticRank error_functions.cpp noise TU for per-tree logging</cons>
    </option>
    <option id="escalate">
      <name>Escalate — toolchain unavailable + disk below floor</name>
      <pros>Honors D-08: surfaces the blocker rather than fabricating a fixture or weakening the gate</pros>
      <cons>Gap #3 stays open; planner/operator must decide a path-c fresh build or a deferral</cons>
    </option>
  </options>
  <resume-signal>Select: reuse, rebuild, or escalate (and record the GO/NO-GO in instrumented_trainer_STATUS.md).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run the instrumented trainer for the StochasticRank corpus — per-tree noise log + model.json</name>
  <files>crates/cb-oracle/generator/gen_ranking_fixtures.py, crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json, crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/staged.npy, crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/predictions.npy, crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/stochasticrank_pertree_noise_groundtruth.jsonl</files>
  <read_first>
    - crates/cb-oracle/generator/gen_ranking_fixtures.py (the StochasticRank target — it does NOT yet pin an explicit `metric`; add one, e.g. StochasticRank:metric=DCG, mirroring the test's expected loss params)
    - catboost-master/catboost/private/libs/algo_helpers/error_functions.cpp:1008-1090 + error_functions.h:1257 (CalcDersForSingleQuery: seed = randomSeed + queryIndex; TFastRng64 rng(seed); per-sample per-doc std_normal noise — the per-group draw order the log must capture)
    - crates/cb-oracle/generator/build_instrumented_trainer.sh (the CB_INSTRUMENT_LOG patch points: error_functions.cpp StochasticRank noise stream)
    - crates/cb-train/tests/stochasticrank_oracle_test.rs:73-111 (the present-fixture branch + the existing standalone draw-log oracle the per-tree log must stay consistent with)
  </read_first>
  <what-built>
    The instrumented trainer run over the StochasticRank corpus producing: (a) `stochasticrank_pertree_noise_groundtruth.jsonl` — the CB_INSTRUMENT_LOG per-tree, per-group Gaussian noise draws at %.17g (the D-07 trainer-level ground truth, distinct from the existing standalone single-group self-oracle); (b) `model.json` + `staged.npy` + `predictions.npy` for the trained StochasticRank model with an explicit metric param. RUN-ONCE/COMMIT, OFFLINE, single-thread.
  </what-built>
  <how-to-verify>
    1. Toolchain GO from Task 1.
    2. Add the explicit `metric` to the StochasticRank generator config; run it single-thread (thread_count=1) under the instrumented `_catboost.so` with `CB_INSTRUMENT_LOG` set.
    3. The noise log must record per-tree per-group draws; spot-check the FIRST tree's group-0 draws against `random_seed + 0` via `TFastRng64` + `std_normal` (consistency with the existing standalone oracle).
    4. Commit the four files; the patched catboost-master TUs stay UNCOMMITTED (D-09/D-12).
  </how-to-verify>
  <acceptance_criteria>
    `stochasticrank_pertree_noise_groundtruth.jsonl` + `model.json` + `staged.npy` + `predictions.npy` exist under `ranking_corpus/stochasticrank/`; model.json parses via `cb_oracle::load_model_json` and pins the explicit metric; the noise log is at %.17g precision and contains per-tree per-group `gauss_draw` events. Human confirms genuine instrumented catboost 1.2.10 output.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the StochasticRank fixture + per-tree noise log are generated by the instrumented trainer and committed, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Activate end-to-end gate + wire the trainer-level per-tree noise oracle (D-07)</name>
  <files>crates/cb-train/tests/stochasticrank_oracle_test.rs, crates/cb-train/src/boosting.rs, crates/cb-train/src/yetirank.rs</files>
  <read_first>
    - crates/cb-train/tests/stochasticrank_oracle_test.rs (whole file — the present-fixture branch at :96-111 currently panics if model.json exists; replace that guard with the full compare_stage gate, mirroring stochasticrank_rng_draw_log_oracle's load/compare shape)
    - crates/cb-train/tests/yetirank_oracle_test.rs (the CLOSED randomized-loss end-to-end + draw-log pattern to mirror for StochasticRank)
    - crates/cb-compute/src/ranking_der.rs:706-884 (stochastic_rank_group_der — the per-group Monte-Carlo der the per-tree noise stream feeds; querywise pointwise leaf)
    - crates/cb-train/src/boosting.rs (where per-group random_seed+group_index would thread per tree if the per-tree gate exposes a seed-advance mismatch)
    - error_functions.h:1257 (seed = randomSeed + group_index)
  </read_first>
  <behavior>
    - Once model.json exists, stochasticrank_end_to_end_per_stage takes the present-fixture branch and gates Splits/LeafValues/StagedApprox/Predictions ≤1e-5 (querywise pointwise leaf); it must PASS.
    - A NEW stochasticrank_pertree_noise_oracle test loads stochasticrank_pertree_noise_groundtruth.jsonl and asserts the Rust per-tree per-group std_normal stream (TFastRng64(random_seed + group_index) per CalcDersForQueries) reproduces the instrumented per-tree draws ≤1e-5 — the D-07 trainer-level gate (vs the prior standalone single-group self-oracle).
    - If the per-tree gate exposes a per-tree seed-advance or group-index mismatch, the per-group seeding in boosting/ranking_der is corrected to randomSeed + group_index drawn fresh each tree, and the end-to-end gate re-passes. NEVER weaken tolerance or #[ignore].
  </behavior>
  <action>
    Replace the absent-fixture `panic!`/invariant guard in `stochasticrank_end_to_end_per_stage` (stochasticrank_oracle_test.rs:96-111) with the full present-fixture per-stage compare (load model.json/staged/predictions, train_ranking under `Loss::StochasticRank{...}` with the querywise pointwise leaf, `compare_stage` all four stages ≤1e-5), mirroring `yetirank_oracle_test.rs`. Add `stochasticrank_pertree_noise_oracle` that parses the per-tree `gauss_draw` events from `stochasticrank_pertree_noise_groundtruth.jsonl` and gates them against the Rust per-group noise stream (`TFastRng64::from_seed(random_seed + group_index)` → `std_normal` per doc per sample, per `error_functions.h:1257`) at ≤1e-5. If a per-tree seed-advance mismatch surfaces, fix the per-group seeding in `boosting.rs`/`ranking_der.rs` to draw fresh `randomSeed + group_index` each `CalcDersForQueries` per tree (cite error_functions.h:1257). Keep the existing `stochasticrank_rng_draw_log_oracle` green (no regression on the standalone self-oracle). Update the test doc-header to record gap #3 + truth-#7 StochasticRank CLOSURE.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test stochasticrank_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p cb-train --test stochasticrank_oracle_test` passes ALL tests: `stochasticrank_end_to_end_per_stage` on the present-fixture branch (4 stages ≤1e-5), the new `stochasticrank_pertree_noise_oracle` (trainer-level D-07 ≤1e-5), and the pre-existing `stochasticrank_rng_draw_log_oracle` (no regression). No `#[ignore]`; no compare.rs tolerance change. LambdaMart + the standalone stochasticrank oracles unaffected.
  </acceptance_criteria>
  <done>StochasticRank end-to-end per-stage oracle passes at ≤1e-5 on the present-fixture branch AND the trainer-level per-tree noise gate (D-07) passes; gap #3 (truth #5 StochasticRank + truth #7 StochasticRank D-07) CLOSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instrumented trainer (CB_INSTRUMENT_LOG) → committed noise log + model.json | trusted D-07 ground truth; must be genuine instrumented catboost 1.2.10 output |
| per-group RNG seed (randomSeed + group_index) → noise stream | the distinct StochasticRank seed model whose per-tree advance is the parity crux |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-18-01 | Tampering | fixture + noise-log provenance | mitigate | blocking-human checkpoint confirms genuine instrumented catboost 1.2.10 single-thread output at %.17g |
| T-06.3-18-02 | Tampering | per-group seed (randomSeed+group_index) per-tree advance | mitigate | trainer-level per-tree noise gate proves the Rust stream matches; a desync is fixed in boosting/ranking_der, not masked |
| T-06.3-18-03 | Spoofing | re-ignored / tolerance-weakened oracle masking divergence | mitigate | acceptance forbids `#[ignore]` and tolerance change (D-6.3-03b) |
| T-06.3-18-04 | Tampering | uncommitted vendored catboost-master patch leaking into the tree | mitigate | instrumentation patch stays UNCOMMITTED (D-09/D-12); only the frozen fixtures are committed |
| T-06.3-18-SC | Tampering | npm/pip/cargo/apt installs (clang-18/lld-18 toolchain) | accept | sudo-free apt-get download + dpkg -x into /tmp prefix per the recorded recipe; OFFLINE/RUN-ONCE; no CI install; reuses .venv catboost |
</threat_model>

<verification>
- `cargo test -p cb-train --test stochasticrank_oracle_test` → all pass: end-to-end (4 stages ≤1e-5) + per-tree noise D-07 gate + standalone draw-log.
- LambdaMart oracle still green (no regression from any per-group seeding change).
- model.json + staged + predictions + per-tree noise log committed; vendored catboost-master patch UNCOMMITTED.
- No `#[ignore]`, no tolerance change.
</verification>

<success_criteria>
StochasticRank's distinct per-group noise-seed model is validated against the instrumented trainer's per-tree draw log (D-07); the model.json fixture is frozen with an explicit metric; the end-to-end per-stage oracle passes at ≤1e-5 on the present-fixture branch. Gap #3 + the truth-#7 StochasticRank residual closed.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-18-SUMMARY.md` when done.
</output>
