---
phase: 06.3-ranking-losses-and-metrics
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-train/src/query_info.rs
  - crates/cb-train/src/query_info_test.rs
  - crates/cb-train/tests/pairlogit_oracle_test.rs
  - crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs
autonomous: true
gap_closure: true
requirements: [LOSS-04]

must_haves:
  truths:
    - "PairLogit trains end-to-end over explicit Pool.pairs and matches upstream catboost 1.2.10 ≤1e-5 across Splits|LeafValues|StagedApprox|Predictions"
    - "PairLogitPairwise trains end-to-end via the Cholesky pairwise-leaf path and matches upstream ≤1e-5 across the four stages"
    - "build_query_info's Competitor.weight applies the upstream pair-weight normalization (GeneratePairs pair-weight construction in private/libs/target/data_providers.cpp + bt.PairwiseWeights folding in private/libs/algo/approx_calcer.cpp:444) instead of the hardcoded 1.0"
  artifacts:
    - path: "crates/cb-train/src/query_info.rs"
      provides: "pair-weight normalization in the pairs -> competitors mapping"
      contains: "weight"
    - path: "crates/cb-train/tests/pairlogit_oracle_test.rs"
      provides: "per-stage ≤1e-5 oracle for PairLogit against the frozen fixture"
      contains: "compare_stage"
    - path: "crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs"
      provides: "per-stage ≤1e-5 oracle for the Cholesky pairwise-leaf variant"
      contains: "compare_stage"
  key_links:
    - from: "crates/cb-train/src/query_info.rs"
      to: "Competitor.weight"
      via: "upstream pair-weight normalization transcription"
      pattern: "Competitor"
    - from: "crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/model.json"
      via: "compare_stage against the frozen fixture"
      pattern: "PairLogitPairwise"
---

<objective>
Close the FAILED truth #4 from 06.3-VERIFICATION.md: PairLogit / PairLogitPairwise train end-to-end on
explicit `Pool.pairs` and predict matching upstream ≤1e-5. The der, the Cholesky pairwise-leaf path,
`is_pairwise_scoring` / `is_plain_only` routing, and the frozen catboost 1.2.10 fixtures
(`ranking_corpus/PairLogit/model.json` + `.../PairLogitPairwise/model.json` + staged/predictions `.npy`,
confirmed present on disk) ALL already exist. Two pieces remain:

1. The **pair-weight normalization** fix: `build_query_info` currently hardcodes `Competitor { id,
   weight: 1.0 }` (query_info.rs:239-242). The deferred-items.md root cause shows upstream applies a
   group-relative pair-weight normalization (the per-leaf SumDer2 / sumWeight is scaled by a
   non-uniform per-pair factor — the `7` denominator == total pair count) via the `GeneratePairs`
   pair-weight construction in `private/libs/target/data_providers.cpp` (around line 241/844) and the
   `bt.PairwiseWeights` / `recalculatedPairwiseWeights` folding in
   `private/libs/algo/approx_calcer.cpp:444`. Transcribe that normalization into the `Competitor.weight`
   assignment.

2. The two **oracle test files** (`pairlogit_oracle_test.rs` / `pairlogit_pairwise_oracle_test.rs`)
   gating `compare_stage(Splits|LeafValues|StagedApprox|Predictions)` ≤1e-5 against the already-frozen
   fixtures, mirroring the existing passing oracle tests.

Purpose: Bring two of the six LOSS-04 ranking losses (PairLogit, PairLogitPairwise) to oracle-locked
≤1e-5, raising LOSS-04 from 3/6 toward completion.
Output: Pair-weight-normalized build_query_info + two per-stage oracle tests at ≤1e-5.

Deferral note (for verify-phase): this plan does NOT close the deferred YetiRank/YetiRankPairwise/
StochasticRank end-to-end TRAINER fixtures (truth #5) nor SC-3/D-07 instrumented-harness validation
(truth #7) — those still require the deferred instrumented catboost build. After this plan LOSS-04
remains partially open (YetiRank/YetiRankPairwise/StochasticRank trainer gates still deferred).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-VERIFICATION.md
@.planning/phases/06.3-ranking-losses-and-metrics/deferred-items.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-03-SUMMARY.md

# Upstream source of truth for the pair-weight normalization (read the cited functions).
# The ACTUAL normalization source per deferred-items.md:13-19 is the GeneratePairs pair-weight
# construction + the bt.PairwiseWeights group-weight folding, NOT the der-summation path alone.
@catboost-master/catboost/private/libs/target/data_providers.cpp
@catboost-master/catboost/private/libs/algo/approx_calcer.cpp
@catboost-master/catboost/private/libs/algo/approx_calcer_querywise.cpp
@catboost-master/catboost/private/libs/data_types/query.h

# Files being modified + the oracle-test template
@crates/cb-train/src/query_info.rs
@crates/cb-train/tests/queryrmse_oracle_test.rs
@crates/cb-train/tests/lambdamart_oracle_test.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pair-weight normalization in build_query_info Competitor.weight</name>
  <read_first>
    .planning/phases/06.3-ranking-losses-and-metrics/deferred-items.md (the [06.3-03] PairLogit leaf-estimation gap — the ISOLATED root cause: pair-weight normalization, the `7` == total pair count denominator, the Gradient/Newton leaf probes, and the pointer to GeneratePairs / bt.PairwiseWeights group-weight folding),
    crates/cb-train/src/query_info.rs:205-252 (the pairs -> competitors mapping where Competitor.weight is hardcoded 1.0 at line 241),
    crates/cb-train/src/query_info.rs:46-76 (Competitor / QueryInfo struct docs),
    catboost-master/catboost/private/libs/target/data_providers.cpp:241-260 and :840-850 (GeneratePairs — the explicit-Pool.pairs pair-weight construction, autoPairWeight / processedTargetData.Weights folding: the PRIMARY normalization source),
    catboost-master/catboost/private/libs/algo/approx_calcer.cpp:430-445 (the bt.PairwiseWeights / recalculatedPairwiseWeights selection at line 444 — how the folded pair weights enter the leaf der/weight sums),
    catboost-master/catboost/private/libs/algo/approx_calcer_querywise.cpp:96-160 (AddLeafDersForQueries — secondary: how pair weights enter the per-leaf der/weight sums once folded),
    catboost-master/catboost/private/libs/data_types/query.h:19-44 (TQueryInfo / TCompetitor.Weight semantics)
  </read_first>
  <behavior>
    - For the frozen PairLogit fixture corpus, build_query_info produces per-group competitors whose weights reproduce the upstream group-relative pair-weight normalization (the deferred-items.md depth-1/l2=0/lr=1 probe: Gradient leaf sumWeight [72/7, 36/7] and Newton -sumDer2 [18/7, 9/7] both scaled by the same per-pair factor, NOT the raw pair-incidence counts).
    - A unit test on a known small group with explicit pairs asserts the normalized Competitor.weight values match the transcribed upstream formula (hand-computed from the cited source), distinguishing them from the old uniform 1.0.
    - Groups with no pairs are unaffected (empty competitor rows).
  </behavior>
  <files>crates/cb-train/src/query_info.rs, crates/cb-train/src/query_info_test.rs</files>
  <action>
    Transcribe the upstream pair-weight normalization into the `Competitor.weight` assignment at
    query_info.rs:239-242 (currently `Competitor { id: loser_local, weight: 1.0 }`). Read the exact
    normalization from the `GeneratePairs` pair-weight construction in
    `catboost-master/catboost/private/libs/target/data_providers.cpp` (around line 241 and the
    autoPairWeight / processedTargetData.Weights folding at :840-850) and the `bt.PairwiseWeights` /
    `recalculatedPairwiseWeights` selection at `catboost-master/catboost/private/libs/algo/approx_calcer.cpp:444`
    referenced in deferred-items.md — the per-pair weight is scaled by the group-relative factor that
    produces the `1/(group-pair-count)`-style normalization the probes isolated. Apply it where pairs are
    mapped to competitors (do NOT change the group `weight` mean-of-members logic, the run-detection, or
    the typed-error guards). Route any per-group pair-weight reduction through `cb_core::sum_f64` (D-08 —
    the file already imports it). Update the Competitor doc-comment (query_info.rs:54-55) to cite the
    upstream normalization source (GeneratePairs in private/libs/target/data_providers.cpp +
    bt.PairwiseWeights folding in private/libs/algo/approx_calcer.cpp:444) instead of "defaults to 1.0 for
    an unweighted explicit Pair". In query_info_test.rs add a RED-first unit test on a known group with
    explicit pairs asserting the normalized Competitor.weight values against the hand-computed upstream
    formula.
  </action>
  <verify>
    <automated>cargo test -p cb-train query_info --lib --tests</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "weight: 1.0" crates/cb-train/src/query_info.rs` no longer matches inside the
    pairs->competitors mapping (the hardcode is replaced by the normalized value); the Competitor
    doc-comment cites the upstream normalization source; the new query_info pair-weight unit test passes
    and asserts non-uniform normalized weights matching the transcribed formula; existing query_info
    contiguity / out-of-range / subgroup tests stay green.
  </acceptance_criteria>
  <done>build_query_info applies the upstream pair-weight normalization to Competitor.weight; unit test green.</done>
</task>

<task type="auto">
  <name>Task 2: PairLogit + PairLogitPairwise per-stage oracle tests ≤1e-5</name>
  <read_first>
    crates/cb-train/tests/queryrmse_oracle_test.rs (the full integration-test template: fixture() helper, load_feature_columns / load_u64_vec / load_pairs / load_target, base_params, compare_stage(Splits|LeafValues|StagedApprox|Predictions), predict_raw via CbModel::from_trained),
    crates/cb-train/tests/lambdamart_oracle_test.rs (the LambdaMart variant — leaf_method + LambdaMart params pinning pattern),
    crates/cb-oracle/fixtures/ranking_corpus/PairLogit/config.json (loss_function PairLogit, iterations 5, depth 2, lr 0.3, leaf_estimation_iterations 1, boosting_type Plain, bootstrap_type No, random_seed 20260617, thread_count 1),
    crates/cb-oracle/fixtures/ranking_corpus/PairLogitPairwise/config.json (same params, loss_function PairLogitPairwise),
    crates/cb-compute/src/ranking_der.rs:49-59 (is_pairwise_scoring / is_plain_only — PairLogitPairwise rides the Cholesky leaf path, boosting_type Plain),
    crates/cb-compute/src/runtime.rs:298-325 (Loss::PairLogit / Loss::PairLogitPairwise variants)
  </read_first>
  <files>crates/cb-train/tests/pairlogit_oracle_test.rs, crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs</files>
  <action>
    Author `crates/cb-train/tests/pairlogit_oracle_test.rs` mirroring queryrmse_oracle_test.rs exactly:
    same fixture loaders and shared `ranking_corpus/inputs/{X,y,group_id,subgroup_id,pairs}.npy`, same
    `train_ranking` call with `RankingData { group_id, subgroup_id, pairs }`, but `Loss::PairLogit` and
    base_params pinned to the PairLogit/config.json values (iterations 5, depth 2, learning_rate 0.3,
    boosting_type Plain, bootstrap_type No, random_strength 0, random_seed 20260617). PairLogit rides the
    pointwise leaf path (NOT pairwise) — pin `leaf_method` to the upstream default for PairLogit (read it
    from config / the 06.3-03-SUMMARY; PairLogit uses the pointwise Newton/Gradient estimator). Gate
    `compare_stage(Stage::Splits, ...)`, `Stage::LeafValues`, `Stage::StagedApprox` (vs staged.npy), and
    `Stage::Predictions` (predict_raw via CbModel::from_trained vs predictions.npy) all ≤1e-5 against the
    frozen `ranking_corpus/PairLogit/` fixture. Then author
    `crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs` identically but with
    `Loss::PairLogitPairwise`, pinned `boosting_type=Plain` (is_plain_only) so leaf values flow through
    the Cholesky pairwise-leaf path (is_pairwise_scoring), gating the same four stages ≤1e-5 against
    `ranking_corpus/PairLogitPairwise/`. NO `#[ignore]`, NO weakened tolerance. Use the
    `#![allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing)]`
    test-edge header. The fixtures already exist on disk — do NOT regenerate them and do NOT require any
    instrumented catboost build.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test pairlogit_oracle_test --test pairlogit_pairwise_oracle_test</automated>
  </verify>
  <acceptance_criteria>
    `crates/cb-train/tests/pairlogit_oracle_test.rs` and
    `crates/cb-train/tests/pairlogit_pairwise_oracle_test.rs` both exist, contain `compare_stage` for all
    four stages (Splits|LeafValues|StagedApprox|Predictions), use Loss::PairLogit / Loss::PairLogitPairwise
    respectively with boosting_type Plain, and load the frozen ranking_corpus/PairLogit* fixtures; no
    `#[ignore]` and no tolerance > 1e-5 anywhere in the files; `cargo test -p cb-train --test
    pairlogit_oracle_test --test pairlogit_pairwise_oracle_test` passes ≤1e-5.
  </acceptance_criteria>
  <done>PairLogit + PairLogitPairwise per-stage oracles gate Splits|LeafValues|StagedApprox|Predictions ≤1e-5 against the frozen fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pool.pairs -> Competitors | Explicit pairs may reference out-of-group / out-of-range indices (already guarded in build_query_info). |
| Pairwise leaf SPD system | PairLogitPairwise Cholesky solve must not NaN on a degenerate matrix (guarded in pairwise_leaves). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-09-01 | Tampering | wrong pair-weight normalization producing wrong leaf SumDer2/sumWeight | mitigate | transcribe the cited upstream formula; query_info unit test + the two ≤1e-5 oracle gates catch deviation |
| T-06.3-09-02 | Denial of Service | competitor index arithmetic on unvalidated pairs | mitigate | existing build_query_info guards (in-group + in-range typed errors, query_info.rs:225-250) are preserved |
| T-06.3-09-03 | Denial of Service | non-SPD pairwise leaf matrix (PairLogitPairwise) | mitigate | existing cholesky_solve returns None on non-positive pivot → zero fallback (pairwise_leaves), unchanged |
| T-06.3-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs — fixtures already frozen on disk; no generator run, no trainer build |
</threat_model>

<verification>
- `cargo test -p cb-train query_info --lib --tests` green (pair-weight normalization unit test).
- `cargo test -p cb-train --test pairlogit_oracle_test --test pairlogit_pairwise_oracle_test` ≤1e-5 green.
- `cargo test -p cb-train --test queryrmse_oracle_test --test querysoftmax_oracle_test --test lambdamart_oracle_test` still green (no regression from the Competitor.weight change — QueryRMSE/QuerySoftMax/LambdaMart do NOT read explicit-pair competitor weights, confirmed in ranking_der.rs).
- Per-crate verification; free disk before any broader run.
</verification>

<success_criteria>
- build_query_info applies the upstream pair-weight normalization to Competitor.weight (truth #4 root cause closed).
- PairLogit and PairLogitPairwise per-stage oracles pass ≤1e-5 against the frozen fixtures (no #[ignore]/no weakening).
- LOSS-04 advances to 5/6 oracle-locked losses; YetiRank/YetiRankPairwise/StochasticRank trainer gates explicitly remain deferred.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-09-SUMMARY.md` when done.
</output>
</content>
</invoke>
