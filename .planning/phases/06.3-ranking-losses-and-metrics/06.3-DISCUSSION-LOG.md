# Phase 6.3: Ranking Losses & Metrics - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-17
**Phase:** 6.3-ranking-losses-and-metrics
**Mode:** `--auto` (no interactive prompts; every gray area auto-resolved to the recommended, parity-/scope-dictated option)
**Areas discussed:** Wave/plan structure, C++-instrumentation scope (D-07/D-08), Per-group gradient plumbing, Pair-source modes, Ranking-metric eval seam, Oracle floor / disk feasibility

---

## Wave / Plan Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Family-wave plans (deterministic-losses / pairwise / randomized / metrics), each with its own ≤1e-5 oracle gate | Small blast radius, unambiguous regression attribution; mirrors 6.1 D-6.1-02 / 6.2 D-6.2-02 | ✓ |
| One mega-plan for the whole ranking surface | Fewer files, but defeats per-loss regression attribution | |
| Per-loss plans (one plan per loss/metric) | Maximal isolation, excess ceremony | |

**Auto-selected:** Family-wave plans, grouped Wave A (grouped seam + QueryRMSE/QuerySoftMax) / Wave B (PairLogit/LambdaMart) / Wave C (YetiRank/StochasticRank) / Wave D (metrics).
**Notes:** D-6.3-01. Planner finalizes exact plan-to-wave mapping.

---

## C++-Instrumentation Scope (D-07 / D-08)

| Option | Description | Selected |
|--------|-------------|----------|
| Instrument ONLY the randomized losses (YetiRank(/Pairwise), StochasticRank) | RNG streams lack clean Python-reachable ground truth; deterministic losses + all metrics ride the Python-reachable per-stage oracle | ✓ |
| Instrument every ranking loss | Wastes instrumentation budget on deterministic, Python-reachable losses | |
| Instrument nothing | Leaves YetiRank/StochasticRank RNG streams unverifiable — fails SC-3 | |

**Auto-selected:** Instrument only the randomized losses (D-07 selective-not-universal).
**Notes:** D-6.3-02. Reuse persisted `/tmp` clang-18 + `_catboost` toolchain incrementally; RUN-ONCE/COMMIT fixtures.

---

## Per-Group / Per-Pair Gradient Plumbing

| Option | Description | Selected |
|--------|-------------|----------|
| New grouped der seam reading existing Pool group_id/subgroup_id/pairs; transcribed from upstream querywise/pairwise | Single code path, no duplication; consumes already-shipped Pool fields | ✓ |
| Parallel ranking-only gradient pipeline | Duplication / divergence risk; violates single-code-path discipline | |

**Auto-selected:** Grouped der seam in the gradient path (D-6.3-03).
**Notes:** All accumulation routes through `cb-core::sum_f64` (D-08 CI-grep ban).

---

## Pair-Source Modes

| Option | Description | Selected |
|--------|-------------|----------|
| Support BOTH explicit Pool.pairs (PairLogit) AND group-derived sampled pairs (YetiRank) | Shared pairwise der path; only the pair source differs | ✓ |
| Explicit pairs only | Fails YetiRank's group-derived pair generation | |
| Derived pairs only | Fails PairLogit's explicit-pair contract | |

**Auto-selected:** Both pair-source modes (D-6.3-04).
**Notes:** Pair generation transcribed from upstream, not approximated.

---

## Ranking-Metric Eval Seam

| Option | Description | Selected |
|--------|-------------|----------|
| Widen EvalMetric::eval to receive group_id/subgroup_id; metrics eval-only (no der) | Keeps the established eval/early-stopping path; @k params via the variant-with-params pattern | ✓ |
| Side-channel group structure outside EvalMetric | Brittle; breaks the established eval path | |

**Auto-selected:** Widen the EvalMetric eval seam (D-6.3-05).
**Notes:** Ranking metrics carry no gradient.

---

## Oracle Floor / Disk Feasibility (D-08)

| Option | Description | Selected |
|--------|-------------|----------|
| Smallest instrumented unit, reuse persisted /tmp toolchain incrementally, free disk before/after, feasibility-probe escalates | First-class link/disk risk per D-08; RUN-ONCE/COMMIT | ✓ |
| Fresh full instrumented build per task | Infeasible under ~100%-full disk | |

**Auto-selected:** Incremental reuse of the persisted toolchain with feasibility-probe escalation (D-6.3-03b).

---

## Claude's Discretion

- Exact der1/der2 + leaf-estimation-method defaults per ranking loss (transcribe from `error_functions.{h,cpp}` + `yetirank_helpers.*` / `pairwise_*` / `querywise_*`).
- Group-boundary slicing representation; threading the grouped der seam without perturbing existing pointwise locks.
- YetiRank/StochasticRank RNG-draw order + pair-sampling procedure (C++-instrumented ground truth).
- Exact metric formulas, default `top`/`k`, tie-handling, normalization (NDCG IDCG, PFound decay, ERR cascade, QueryAUC per-group averaging).
- Plan-to-wave mapping; oracle fixture datasets/configs.
- Whether any randomized-loss task can ride a Python-reachable approximation if research recovers one (gated by D-08).

## Deferred Ideas

None — discussion stayed within phase scope. Score functions / uncertainty / custom objectives → 6.4; text/embedding → 6.5; advanced features / non-symmetric trees → 6.6; GPU → Phase 7; Python bindings → Phase 8.
