---
phase: 06.3-ranking-losses-and-metrics
plan: 04
type: execute
wave: 4
depends_on: ["06.3-01", "06.3-02", "06.3-03"]
files_modified:
  - crates/cb-train/src/yetirank.rs
  - crates/cb-train/src/yetirank_test.rs
  - crates/cb-compute/src/loss.rs
  - crates/cb-compute/src/runtime.rs
  - crates/cb-compute/src/ranking_der.rs
  - crates/cb-compute/src/ranking_der_test.rs
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/src/lib.rs
  - crates/cb-oracle/generator/yetirank_oracle.cpp
  - crates/cb-oracle/generator/stochasticrank_oracle.cpp
  - crates/cb-oracle/generator/instrument_ranking_rng_README.md
  - crates/cb-train/tests/yetirank_oracle_test.rs
  - crates/cb-train/tests/yetirank_pairwise_oracle_test.rs
  - crates/cb-train/tests/stochasticrank_oracle_test.rs
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank/.gitkeep
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/.gitkeep
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/.gitkeep
autonomous: false
requirements: [LOSS-04]

must_haves:
  truths:
    - "YetiRank generates sampled pairs per group via the transcribed 2-level TFastRng64 seed derivation + Gumbel noise, matching the C++-instrumented RNG draw log"
    - "YetiRankPairwise rides the same sampled pairs through the Cholesky pairwise-leaf path (Plain), predicting ≤1e-5"
    - "StochasticRank computes the Monte-Carlo gradient via NumEstimations Gaussian-noise samples per group, matching the instrumented log, predicting ≤1e-5"
    - "Wave-C does NOT proceed to instrumented builds without a feasibility-probe confirming toolchain + disk (escalate, never silently expand scope)"
  artifacts:
    - path: "crates/cb-train/src/yetirank.rs"
      provides: "YetiRank RNG-stream pair sampling (2-level seed, Gumbel noise, per-permutation weights)"
      contains: "gen_rand"
      min_lines: 60
    - path: "crates/cb-oracle/generator/yetirank_oracle.cpp"
      provides: "OFFLINE instrumented standalone generator (zero catboost includes) logging RNG draws + sampled pairs"
      contains: "CB_INSTRUMENT_LOG"
    - path: "crates/cb-train/tests/yetirank_oracle_test.rs"
      provides: "Per-stage ≤1e-5 oracle for YetiRank against instrumented ground truth"
      contains: "compare_stage"
  key_links:
    - from: "crates/cb-train/src/yetirank.rs"
      to: "cb_core::TFastRng64"
      via: "instrumented-validated RNG draw order"
      pattern: "TFastRng64|gen_rand"
    - from: "crates/cb-train/src/yetirank.rs"
      to: "cb_core::sum_f64"
      via: "per-permutation pairwise-weight accumulation"
      pattern: "sum_f64"
---

<objective>
Land the two randomized ranking losses — **YetiRank/YetiRankPairwise** and **StochasticRank** —
the RNG-stream losses that have no clean Python-reachable ground truth and get C++ instrumentation
(D-6.3-02 / umbrella D-07). Each is a vertical slice: feasibility-probe → RNG-draw transcription →
instrumented ground-truth capture (OFFLINE) → grouped-seam wiring → per-stage oracle ≤1e-5. The RNG
draw order is the parity crux: YetiRank's 2-level seed derivation + Gumbel noise and StochasticRank's
Gaussian Monte-Carlo gradient must match the instrumented `CB_INSTRUMENT_LOG` sequence BEFORE the der
is checked. The plan opens with a blocking feasibility-probe checkpoint (the persisted `/tmp` toolchain
was NOT found this session; disk is 67G free) that escalates rather than silently expanding scope.

Purpose: Close Wave C of LOSS-04 (D-6.3-01) + SC-3 (randomized RNG streams validated against an
instrumented harness, D-08 feasibility-bounded).
Output: yetirank.rs RNG sampler + StochasticRank der + Loss variants + grouped-seam arms +
two instrumented OFFLINE generators + three per-stage oracles + three frozen fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-CONTEXT.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-RESEARCH.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-PATTERNS.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-03-SUMMARY.md

# Upstream transcription sources (RNG draw order — the parity crux)
@catboost-master/catboost/private/libs/algo/yetirank_helpers.cpp
@catboost-master/catboost/private/libs/algo_helpers/error_functions.cpp

# Rust seams + RNG transcription precedent
@crates/cb-core/src/rng.rs
@crates/cb-core/src/normal.rs
@crates/cb-oracle/generator/ordered_oracle.cpp
@crates/cb-train/src/pairwise_leaves.rs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Wave-C feasibility-probe: a scripted check of the instrumented-build prerequisites BEFORE any
    instrumented build. The executor runs (and reports): `ls /tmp` for the persisted clang-18 +
    lld-18 + built `_catboost` toolchain (memories: instrumented-trainer-toolchain-persists /
    catboost-instrumented-trainer-build); `df -h /` free disk (RESEARCH found 67G free / 71% used,
    healthier than the ~100% in memories); and `.venv` catboost 1.2.10 import. The probe decides the
    path per D-6.3-03b: (a) toolchain present → incremental rebuild; (b) toolchain gone but disk
    healthy → fresh sudo-free build per the catboost-instrumented-trainer-build recipe (clang-18 +
    lld-18 via apt-get download+dpkg-x, conan/ninja/cython via uv tool, CB_INSTRUMENT_LOG); (c) disk
    insufficient / build infeasible → ESCALATE (do NOT weaken the oracle, do NOT silently expand scope).
  </what-built>
  <how-to-verify>
    1. Review the executor's probe report: which of paths (a)/(b)/(c) applies, with the actual
       `ls /tmp` + `df -h /` output.
    2. If (a) or (b): approve to proceed with the instrumented OFFLINE build for Tasks 2–4.
    3. If (c): the instrumented harness is infeasible this session — instruct the executor to STOP
       Wave C, record the blocker in the SUMMARY + deferred-items, and leave the three oracles
       authored-but-unclosed (NO `#[ignore]`/NO weakened tolerance/NO fabricated fixtures), exactly
       as the 05-17 fallback precedent. Do NOT approve a weakened gate.
  </how-to-verify>
  <resume-signal>Type "approved: path a", "approved: path b", or "escalate: defer Wave C" with the probe output reviewed.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: YetiRank RNG pair sampler + StochasticRank der + Loss variants</name>
  <read_first>
    crates/cb-core/src/rng.rs:141-200 (TFastRng64, gen_rand :175, gen_rand_real1 :195),
    crates/cb-core/src/normal.rs:11-50 (std_normal line-by-line RNG-draw transcription precedent),
    crates/cb-compute/src/ranking_der.rs (PairLogit der — YetiRank rides it over sampled pairs),
    catboost-master/catboost/private/libs/algo/yetirank_helpers.cpp:146-163 (AddNoise/Gumbel), :305-393 (2-level seed: GenRandUI64Vector → per-query GenRand re-seed → per-permutation noise+sort+CalcWeights),
    catboost-master/catboost/private/libs/algo_helpers/error_functions.cpp:1008-1090 (StochasticRank CalcDersForSingleQuery: seed=randomSeed+queryIndex, NumEstimations Gaussian samples, sort, metric-position-weight grad),
    catboost-master/catboost/private/libs/options/loss_description.cpp:181-209 (permutations=10, decay defaults)
  </read_first>
  <behavior>
    - YetiRank single-thread (blockCount=1): one GenRandUI64Vector(1,seed); per-query rand.GenRand() re-seeds inner per-query rng; per permutation (default 10): Gumbel u=gen_rand_real1(); expApprox[d]*=u/(1.000001-u); stable-sort desc; CalcWeights → competitorsWeights; competitorsWeight = queryWeight·weights[w][l]/permutations.
    - StochasticRank: seed=randomSeed+queryIndex; shift approx by -Sigma·Mu·target, center; per sample (NumEstimations): noise[d]=std_normal(rng); scores=shifted+Sigma·noise; stable-sort desc; accumulate metric-position-weight grad; average.
    - YetiRank Gumbel via gen_rand_real1; StochasticRank Gaussian via std_normal. All weight accumulation via sum_f64. Draw count+order must match the instrumented log BEFORE the der is checked.
  </behavior>
  <files>crates/cb-train/src/yetirank.rs, crates/cb-train/src/yetirank_test.rs, crates/cb-compute/src/loss.rs, crates/cb-compute/src/runtime.rs, crates/cb-compute/src/ranking_der.rs, crates/cb-compute/src/ranking_der_test.rs, crates/cb-train/src/lib.rs</files>
  <action>
    Add `Loss::YetiRank { permutations, decay }`, `Loss::YetiRankPairwise { permutations, decay }`,
    and `Loss::StochasticRank { metric, sigma, mu, num_estimations }` to the enum with full
    doc-comments (RNG draw order, `yetirank_helpers.cpp`/`error_functions.cpp:line` citation,
    defaults permutations=10, leaf path, isExpApprox). Add range validation to `Loss::validate`
    (permutations>=1, decay∈[0,1], sigma>0, mu>=0, num_estimations>=1 → CbError::OutOfRange).
    Create `yetirank.rs` transcribing the 2-level seed derivation + Gumbel sampling
    (yetirank_helpers.cpp:305-393) — mirror `normal.rs`'s line-by-line RNG-draw transcription
    discipline, doc-citing every `gen_rand`/`gen_rand_real1` call. Produce per-group sampled
    `competitors` (the YetiRank pair source) the existing PairLogit der (Plan 03) then consumes —
    YetiRank/YetiRankPairwise reuse the PairLogit der over SAMPLED pairs. StochasticRank is
    querywise (no pairs): add its Monte-Carlo der arm in `ranking_der.rs`
    (error_functions.cpp:1008-1090) using `cb_core::std_normal` for the Gaussian noise. Route every
    pairwise-weight / sample accumulation through `cb_core::sum_f64`. Resolve the decay ambiguity
    (0.85 vs 0.99 — RESEARCH A3/Open-Q2) by LOGGING the effective decay in the instrumented harness
    (Task 3), NOT by assuming. Wire `mod yetirank;`. Write yetirank/ranking_der test additions as
    the RED step: the YetiRank `TFastRng64` call sequence for permutations=1 on a 2-doc group
    matches a hand-traced draw list; StochasticRank num_estimations=1 noise draw matches std_normal.
  </action>
  <verify>
    <automated>cargo test -p cb-compute stochasticrank ranking_der --tests && cargo test -p cb-train yetirank --tests</automated>
  </verify>
  <acceptance_criteria>
    YetiRank/YetiRankPairwise/StochasticRank in Loss enum with doc-cited RNG draw order; yetirank.rs
    reproduces the 2-level seed + Gumbel draws (hand-traced for permutations=1); StochasticRank uses
    std_normal Gaussian; sampled pairs feed the PairLogit der; all sums via sum_f64; validate rejects
    bad params; unit tests green.
  </acceptance_criteria>
  <done>YetiRank sampler + StochasticRank der + Loss variants land; RNG-draw unit tests pass (instrumented validation in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: OFFLINE instrumented generators (yetirank/stochasticrank) — RNG ground truth</name>
  <read_first>
    crates/cb-oracle/generator/ordered_oracle.cpp:1-45 (zero-catboost-include standalone transcription + self-oracle + file:line header precedent),
    crates/cb-oracle/generator/instrument_live_trainer_README.md (instrumented-trainer run recipe),
    crates/cb-train/src/yetirank.rs (Task 2 — the Rust stream to validate),
    catboost-master/catboost/private/libs/algo/yetirank_helpers.cpp:305-393,
    catboost-master/catboost/private/libs/algo_helpers/error_functions.cpp:1008-1090
  </read_first>
  <files>crates/cb-oracle/generator/yetirank_oracle.cpp, crates/cb-oracle/generator/stochasticrank_oracle.cpp, crates/cb-oracle/generator/instrument_ranking_rng_README.md</files>
  <action>
    Create the two OFFLINE instrumented generators following the `ordered_oracle.cpp` precedent: a
    DEPENDENCY-FREE standalone C++ file with ZERO catboost includes that transcribes the smallest
    RNG units (yetirank_helpers.cpp:305-393 / error_functions.cpp:1008-1090) verbatim, documents each
    transcribed unit with `file.cpp:line`, logs every RNG call (count + value) + the sampled
    pairs/weights via `CB_INSTRUMENT_LOG`, runs SINGLE-THREAD (blockCount=1), and SELF-ORACLEs
    against the bitstream-validated `cb-core::TFastRng64` Rust reproduction. Use the smallest
    instrumented unit (one query group, permutations/NumEstimations small) per D-6.3-03b. RUN-ONCE/
    COMMIT: run OFFLINE on the toolchain confirmed by the Task-1 probe, capture the RNG draw log +
    sampled-pair/weight ground truth, freeze it as committed fixture data. Resolve the YetiRank decay
    ambiguity here (log the effective Classic-weight decay; RESEARCH A3). Document in
    `instrument_ranking_rng_README.md`: the exact OFFLINE build+run command (incremental rebuild OR
    fresh build per the probe path), the `CB_INSTRUMENT_LOG` schema, the smallest-unit config, and
    the captured decay value. Free disk before/after the build (disk-pressure memory). If the
    Task-1 probe chose "escalate: defer Wave C", this task authors the generators + README
    compile-clean but does NOT run the build — the README records the deferred OFFLINE step.
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/generator/yetirank_oracle.cpp && test -f crates/cb-oracle/generator/stochasticrank_oracle.cpp && test -f crates/cb-oracle/generator/instrument_ranking_rng_README.md && grep -q CB_INSTRUMENT_LOG crates/cb-oracle/generator/yetirank_oracle.cpp</automated>
  </verify>
  <acceptance_criteria>
    Two standalone zero-catboost-include instrumented generators + README committed; each logs RNG
    draws + sampled pairs/weights via CB_INSTRUMENT_LOG, runs single-thread/smallest-unit, self-oracles
    vs cb-core::TFastRng64; README documents the OFFLINE build+run command + decay resolution + disk hygiene.
  </acceptance_criteria>
  <done>Instrumented OFFLINE generators + README land; RNG ground truth captured (or deferred per the Task-1 probe), decay resolved.</done>
</task>

<task type="auto">
  <name>Task 4: Boosting wiring + 3 per-stage oracles (YetiRank, YetiRankPairwise, StochasticRank)</name>
  <read_first>
    crates/cb-train/src/boosting.rs:1950-1969 (der site — recompute YetiRank pairs each iteration), :869-972 (pointwise leaf), Plan 03 pairwise leaf branch,
    crates/cb-train/src/yetirank.rs (Task 2 sampler),
    crates/cb-oracle/generator/yetirank_oracle.cpp + stochasticrank_oracle.cpp (Task 3 ground truth),
    crates/cb-train/tests/pairlogit_oracle_test.rs (Plan 03 oracle precedent),
    crates/cb-compute/src/ranking_der.rs (is_pairwise_scoring — YetiRankPairwise uses Cholesky leaf)
  </read_first>
  <files>crates/cb-train/src/boosting.rs, crates/cb-train/tests/yetirank_oracle_test.rs, crates/cb-train/tests/yetirank_pairwise_oracle_test.rs, crates/cb-train/tests/stochasticrank_oracle_test.rs, crates/cb-oracle/fixtures/ranking_corpus/yetirank/.gitkeep, crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/.gitkeep, crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/.gitkeep</files>
  <action>
    In `boosting.rs`, wire YetiRank: per boosting iteration, regenerate the sampled `competitors`
    via `yetirank::sample_pairs(...)` (the pairs are recomputed each iteration — RESEARCH
    yetirank_helpers.cpp:347-418) and feed the PairLogit der through the grouped seam.
    YetiRankPairwise routes leaf values to the Cholesky path (is_pairwise_scoring → Plan 03
    pairwise_leaves), boosting_type=Plain; YetiRank uses pointwise. StochasticRank is querywise
    pointwise. Keep the deterministic/scalar/N-dim arms byte-identical (D-04). Before checking the
    der, ASSERT the Rust `TFastRng64` call sequence matches the Task-3 instrumented log (RESEARCH
    Pitfall 1 — der matches for permutations=1 but diverges at default ⇒ seed re-derivation wrong).
    Run `gen_ranking_fixtures.py --loss YetiRank|YetiRankPairwise|StochasticRank` OFFLINE against the
    instrumented ground truth; freeze fixtures under `ranking_corpus/{yetirank,yetirank_pairwise,
    stochasticrank}/`. Author the three oracle tests gating
    `compare_stage(Stage::Splits|LeafValues|StagedApprox|Predictions, …)` ≤1e-5 PLUS an integer-exact
    RNG-draw-log compare (the sampled-pair/weight ground truth) so the randomized stream is gated
    independently of the der. If the Task-1 probe deferred Wave C, author the oracles compile-clean
    (NO `#[ignore]`/NO weakened tolerance) and record the OFFLINE closure step in the SUMMARY — never
    fabricate fixtures, never weaken the gate (05-17 fallback precedent).
  </action>
  <verify>
    <automated>cargo test -p cb-train yetirank_oracle yetirank_pairwise_oracle stochasticrank_oracle --tests</automated>
  </verify>
  <acceptance_criteria>
    YetiRank regenerates sampled pairs per iteration + rides PairLogit der; YetiRankPairwise → Cholesky
    leaf (Plain); StochasticRank querywise pointwise; the Rust RNG call sequence matches the instrumented
    log; three oracles gate splits/leaf-values/staged-approx/predictions ≤1e-5 + RNG-draw-log exact
    (no #[ignore]/no weakening, or offline closure recorded); D-04 + Wave-A/B oracles green.
  </acceptance_criteria>
  <done>YetiRank/YetiRankPairwise/StochasticRank train end-to-end matching the instrumented RNG ground truth; three per-stage oracles ≤1e-5 (or offline closure recorded); D-04 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RNG draw order ↔ instrumented log | Parity crux: a divergent draw count/order silently produces wrong pairs and fails ≤1e-5 from tree 0. |
| Instrumented C++ build under disk pressure | A failed/partial build can corrupt caches; toolchain may be absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-04-01 | Tampering | YetiRank Gumbel `u/(1.000001-u)` near u→1 | mitigate | The 1.000001 denominator guard (upstream) prevents div-by-zero/Inf; transcribe verbatim, no `+` reductions outside sum_f64 |
| T-06.3-04-02 | Denial of Service | empty group / zero-weight in sampled-pair weighting | mitigate | Guard group size>0 / queryWeight>0, skip, never divide (Security V5) |
| T-06.3-04-03 | Out-of-range loss params | permutations<1 / decay∉[0,1] / sigma<=0 / num_estimations<1 | mitigate | Loss::validate → CbError::OutOfRange, no panic |
| T-06.3-04-04 | Tampering (supply chain) | instrumented C++ build under disk pressure | mitigate | Feasibility-probe checkpoint (Task 1, blocking-human) confirms toolchain + disk before build; free disk before/after; escalate (never silently weaken) per D-08 |
| T-06.3-04-SC | Tampering | npm/pip/cargo installs | mitigate | No Rust crate installs; the instrumented build provisions clang-18/lld-18 via the audited catboost-instrumented-trainer-build recipe gated behind the Task-1 blocking-human checkpoint; .venv catboost 1.2.10 already present |
</threat_model>

<verification>
- Task-1 feasibility-probe checkpoint reviewed + approved (path a/b) or escalated (path c).
- `cargo test -p cb-compute stochasticrank ranking_der --tests` + `cargo test -p cb-train yetirank --tests` green (RNG-draw units).
- Instrumented generators + README present; CB_INSTRUMENT_LOG schema documented; decay resolved.
- `cargo test -p cb-train yetirank_oracle yetirank_pairwise_oracle stochasticrank_oracle --tests` green ≤1e-5 + RNG-log exact (or offline closure recorded).
- `cargo test -p cb-train --tests` shows D-04 + Wave-A/B oracles green.
- Per-crate verification; free disk before/after any build + full suite.
</verification>

<success_criteria>
- YetiRank/YetiRankPairwise/StochasticRank train end-to-end with the RNG draw order matching the C++-instrumented ground truth (SC-3).
- Feasibility-probe gates the instrumented build; escalates rather than silently weakening (D-08).
- Three per-stage oracles ≤1e-5 + integer-exact RNG-draw-log compare (or offline closure recorded, gate un-weakened); D-04 + Wave-A/B oracles green.
</success_criteria>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-04-SUMMARY.md` when done.
</output>
