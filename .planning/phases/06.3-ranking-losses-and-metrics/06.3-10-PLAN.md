---
phase: 06.3-ranking-losses-and-metrics
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cb-oracle/generator/instrument_live_trainer_README.md
  - crates/cb-oracle/generator/build_instrumented_trainer.sh
  - crates/cb-oracle/generator/instrumented_trainer_STATUS.md
autonomous: false
gap_closure: true
requirements: [LOSS-04]
must_haves:
  truths:
    - "The instrumented catboost 1.2.10 trainer (`_catboost.so` with `CB_INSTRUMENT_LOG` per-leaf SumDer/SumDer2 + RNG-draw events) either links + runs on this host, OR the build is escalated with a recorded NO-GO reason — never silently weakened"
    - "Disk headroom is verified above a concrete Release-C++-link safety threshold before the link step is attempted"
    - "The clang-18 + lld-18 toolchain and conan/ninja/cython tooling are restored per the documented sudo-free recipe"
  artifacts:
    - path: "crates/cb-oracle/generator/build_instrumented_trainer.sh"
      provides: "Re-runnable, sudo-free disk-prep + toolchain-fetch + build_native.py driver for the instrumented _catboost trainer"
      min_lines: 40
    - path: "crates/cb-oracle/generator/instrumented_trainer_STATUS.md"
      provides: "Recorded build outcome (GO with artifact paths, or NO-GO with the precise failing step) gating downstream plans 13/14"
  key_links:
    - from: "build_instrumented_trainer.sh"
      to: "catboost-master/build/build_native.py --targets _catboost"
      via: "clang-18 prefix + .venv Python 3.13 include/executable overrides"
      pattern: "build_native.py"
---

<objective>
Stand up the instrumented catboost 1.2.10 trainer that every remaining open truth (PairLogit leaf-der2, YetiRank/StochasticRank trainer fixtures, D-07 RNG-draw validation) depends on. This is the FEASIBILITY-FIRST centerpiece of the gap-closure round.

NEW FACT vs all prior deferrals: disk is now at 67 GB free / 71% used (`df -h /`), NOT the 95-97% / ~8-12 GB link-failure regime that forced the path-(c) escalation in 06.3-04 and the deferrals in 06.3-03/09. The operator explicitly chose ATTEMPT THE FULL INSTRUMENTED TRAINER BUILD. The disk NO-GO precondition no longer holds; the toolchain (`/tmp/clang18_prefix`, `/tmp/cb_build313`) is gone and must be re-fetched.

Purpose: produce the per-leaf SumDer/SumDer2 log (PairLogit) and the trained `model.json` + RNG draw log (YetiRank/StochasticRank) that pin the three open truths ≤1e-5. Without this build, GAP 1, GAP 2's full gates, and GAP 3's trainer-half cannot close — and must stay deferred (escalate-don't-weaken, D-6.3-03b), never fabricated.
Output: a re-runnable build driver script, a recorded GO/NO-GO STATUS artifact, and (on GO) the instrumented `_catboost.so` ready for plans 13/14.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-VERIFICATION.md
@.planning/phases/06.3-ranking-losses-and-metrics/deferred-items.md
@crates/cb-oracle/generator/instrument_live_trainer_README.md
@crates/cb-oracle/generator/instrument_ranking_rng_README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disk-prep + toolchain restore + instrumented-trainer build driver script</name>
  <read_first>
    - crates/cb-oracle/generator/instrument_live_trainer_README.md (the sudo-free clang-18 + lld-18 `apt-get download` + `dpkg -x` recipe into `/tmp/clang18_prefix`; conan/ninja/cython via `uv tool`; `build_native.py --targets _catboost` with `-DPython3_INCLUDE_DIR/_EXECUTABLE` overrides pointing at the project `.venv` Python 3.13)
    - crates/cb-oracle/generator/instrument_ranking_rng_README.md (## Disk hygiene — free `target/debug/incremental` + `~/.conan2` before/after; the per-tree RNG-draw `CB_INSTRUMENT_LOG` schema)
    - MEMORY.md entries `catboost-instrumented-trainer-build`, `instrumented-trainer-toolchain-persists`, `disk-pressure-and-full-suite-verification`
  </read_first>
  <action>
    Author `crates/cb-oracle/generator/build_instrumented_trainer.sh` (sudo-free, idempotent, re-runnable). It must, in order:
    1. Print and assert disk headroom: `df -h /`; require avail >= a concrete threshold (use 25 GB as the Release-C++-link safety floor — the README documents linking failed only at ~8-12 GB free; current free is ~67 GB). If below threshold, free `target/debug/incremental` and `~/.conan2` first, re-check, and ABORT with a NO-GO message if still below.
    2. Restore the toolchain only if absent: if `/tmp/clang18_prefix` is missing, `apt-get download` the clang-18 / lld-18 / llvm-18 noble debs (NO sudo) and `dpkg -x` them into `/tmp/clang18_prefix`; ensure `uv tool install` of conan, ninja, cython (+numpy) is present (reuse existing `uv`/`conan`/`ninja` on PATH).
    3. Apply the env-gated `CB_INSTRUMENT_LOG` instrumentation patch to the vendored trainer for the TWO logging surfaces this round needs: (a) the per-leaf SumDer/SumDer2 in the pairwise/grouped leaf-der reduction (PairLogit closure — `approx_calcer_querywise.cpp` AddLeafDersForQueries der2 path / `approx_calcer.cpp` CalcLeafValues), and (b) the per-tree YetiRank/StochasticRank RNG-draw events (`yetirank_helpers.cpp` GenerateYetiRankPairsForQuery + `error_functions.cpp` StochasticRank noise stream). Use the inert-when-unset `CbInstrumentLog` sink helper documented verbatim in instrument_live_trainer_README.md. Patch must be a no-op when `CB_INSTRUMENT_LOG` is unset (D-09/D-12: never live in CI).
    4. Invoke `catboost-master/build/build_native.py --targets _catboost` against the project `.venv` Python 3.13 with the clang-18 prefix on PATH and the `-DPython3_INCLUDE_DIR/-DPython3_EXECUTABLE` overrides (FindPython otherwise picks system 3.12 → ABI mismatch).
    5. On success, drop the built `_catboost.so` into a copy of the venv catboost package and print the path for downstream RUN-ONCE invocations.
    Do NOT run the trainer in CI; the script is OFFLINE/RUN-ONCE. unwrap()-equivalent shell failures must `set -euo pipefail` and surface the failing step.
    Then EXECUTE the script once and capture stdout/stderr to `/tmp/instr_build.log`.
  </action>
  <verify>
    <automated>bash -n crates/cb-oracle/generator/build_instrumented_trainer.sh && grep -q 'build_native.py' crates/cb-oracle/generator/build_instrumented_trainer.sh && grep -q 'CB_INSTRUMENT_LOG' crates/cb-oracle/generator/build_instrumented_trainer.sh</automated>
  </verify>
  <acceptance_criteria>
    Script parses (`bash -n` clean), drives `build_native.py --targets _catboost`, is sudo-free, gates on a >=25 GB disk floor, and the instrumentation is `CB_INSTRUMENT_LOG`-gated. The build has been run once and its full log saved to `/tmp/instr_build.log`.
  </acceptance_criteria>
  <done>
    `build_instrumented_trainer.sh` exists, parses clean, and has been executed; the build either produced an `_catboost.so` or failed at a recorded step captured in `/tmp/instr_build.log`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Build-feasibility checkpoint — GO / NO-GO (escalate-don't-weaken, D-6.3-03b)</name>
  <read_first>
    - /tmp/instr_build.log (the captured build output from Task 1)
    - crates/cb-oracle/generator/instrument_live_trainer_README.md (## RUN-ONCE invocation — the expected `_catboost.so` artifact + the bit-identical reproduction check)
  </read_first>
  <what-built>
    Task 1 authored and ran `build_instrumented_trainer.sh`: disk-prep (67 GB free, well above the 25 GB link floor), clang-18/lld-18 restore into `/tmp/clang18_prefix`, the `CB_INSTRUMENT_LOG` per-leaf-der2 + RNG-draw instrumentation patch, and `build_native.py --targets _catboost`.
  </what-built>
  <how-to-verify>
    1. Read `/tmp/instr_build.log`. Confirm whether `build_native.py --targets _catboost` LINKED and produced an `_catboost.so` (path printed by the script), or failed.
    2. If it LINKED: as a smoke check, drop the instrumented `_catboost.so` into a venv-package copy and run a tiny `.fit()` with `CB_INSTRUMENT_LOG=/tmp/instr_smoke.jsonl` set; confirm the JSONL file is written with per-leaf-der2 and/or RNG-draw events. Write `instrumented_trainer_STATUS.md` recording **GO**, the `_catboost.so` path, and the toolchain prefix.
    3. If it FAILED (link error / disk / toolchain): record **NO-GO** in `instrumented_trainer_STATUS.md` with the exact failing step and error excerpt. This triggers the ESCALATE-DON'T-WEAKEN branch: downstream plans 13 and 14 keep the PairLogit oracles `#[ignore]`'d and the YetiRank/StochasticRank `model.json` absent-fixture invariants in force, with NO weakened tolerance and NO fabricated fixtures. The trainer-INDEPENDENT plans (11, 12) still land and commit regardless.
    Approve only after `instrumented_trainer_STATUS.md` records a definitive GO (with artifact paths) or NO-GO (with the failing step).
  </how-to-verify>
  <resume-signal>Type "GO — instrumented trainer built at &lt;path&gt;" or "NO-GO — &lt;failing step&gt;; proceed trainer-independent only"</resume-signal>
  <acceptance_criteria>
    `instrumented_trainer_STATUS.md` exists and records an unambiguous GO (artifact paths) or NO-GO (precise failing step). No oracle weakened, no fixture fabricated under either branch.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → vendored C++ build | Building `_catboost.so` from the committed `catboost-master/` source via clang-18 |
| host → package mirrors | `apt-get download` of clang/lld/llvm debs; conan fetch of openssl/ragel/swig/yasm from conancenter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-10-01 | Tampering | clang/lld/llvm debs via `apt-get download` | accept | Fetched from the configured Ubuntu noble apt mirror over the distro's signed package index; no new third-party repo added; local-only build artifact, no deploy surface |
| T-06.3-10-02 | Tampering | conan dependency fetch (openssl/ragel/swig/yasm) | accept | Standard conancenter recipes, same as prior Phase-5 instrumented build; output is a local OFFLINE-only `_catboost.so`, never shipped |
| T-06.3-10-03 | Elevation | build script execution | mitigate | Script is sudo-free (`dpkg -x` extraction, `uv tool`, user-prefix install); `set -euo pipefail`; no privileged operations |
| T-06.3-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs introduced by this plan beyond the documented sudo-free toolchain restore; no `[ASSUMED]`/`[SUS]` packages added |

No network/auth/PII surface; no high-severity threats apply to a local numerical training build. Threat model kept proportionate per the phase security gate.
</threat_model>

<verification>
- `bash -n` on `build_instrumented_trainer.sh` is clean.
- `instrumented_trainer_STATUS.md` records GO or NO-GO definitively.
- On GO: a `CB_INSTRUMENT_LOG` smoke run writes a non-empty JSONL with the expected events.
- On NO-GO: the failing step is recorded; no downstream oracle is weakened.
</verification>

<success_criteria>
The build was attempted with disk no longer a blocker, the toolchain restore + instrumentation patch + `build_native.py` driver are captured in a re-runnable script, and the GO/NO-GO outcome is recorded in `instrumented_trainer_STATUS.md` to gate plans 13/14.
</success_criteria>

<artifacts_produced>
- NEW: `crates/cb-oracle/generator/build_instrumented_trainer.sh` (sudo-free build driver)
- NEW: `crates/cb-oracle/generator/instrumented_trainer_STATUS.md` (GO/NO-GO record)
- MODIFIED: `crates/cb-oracle/generator/instrument_live_trainer_README.md` (append the 06.3-10 re-run note: disk now 67 GB free, toolchain re-fetch path)
- (on GO, non-committed) instrumented `_catboost.so` under `/tmp` for RUN-ONCE invocation by plans 13/14
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-10-SUMMARY.md` when done
</output>
