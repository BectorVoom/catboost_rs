---
phase: 06.3-ranking-losses-and-metrics
plan: 14
type: execute
wave: 3
depends_on: ["06.3-10", "06.3-12", "06.3-13"]
files_modified:
  - crates/cb-train/src/boosting.rs
  - crates/cb-train/tests/yetirank_oracle_test.rs
  - crates/cb-train/tests/yetirank_pairwise_oracle_test.rs
  - crates/cb-train/tests/stochasticrank_oracle_test.rs
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank/model.json
  - crates/cb-oracle/fixtures/ranking_corpus/yetirank_pairwise/model.json
  - crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json
autonomous: false
gap_closure: true
requirements: [LOSS-04]
must_haves:
  truths:
    - "GAP 2 / Truth #5: YetiRank, YetiRankPairwise, and StochasticRank train end-to-end and match upstream ≤1e-5 per stage (Splits|LeafValues|StagedApprox|Predictions); the absent-fixture invariant branches are removed and the full compare_stage gates execute"
    - "GAP 3 / Truth #7 (trainer-half): the instrumented trainer's per-tree CB_INSTRUMENT_LOG RNG draw log matches the Rust YetiRank/StochasticRank sampler (D-07); the RNG draw-log oracles stay GREEN"
    - "REVIEW WR-03: the YetiRank Gradient leaf passes `eff_weights` (pairwise sumWeight), not unit weights — branched on the leaf method / `uses_pairwise_weights(loss)`, NOT uniformly on `group_spans.is_some()` — and this is now oracle-verified by the landed YetiRank model.json"
  artifacts:
    - path: "crates/cb-oracle/fixtures/ranking_corpus/yetirank/model.json"
      provides: "Trained YetiRank fixture for the per-stage ≤1e-5 gate"
      contains: "leaf_values"
    - path: "crates/cb-oracle/fixtures/ranking_corpus/stochasticrank/model.json"
      provides: "Trained StochasticRank fixture"
  key_links:
    - from: "boosting.rs YetiRank Gradient leaf"
      to: "eff_weights (bt.PairwiseWeights sumWeight)"
      via: "branch on uses_pairwise_weights / leaf method, not group_spans.is_some()"
      pattern: "eff_weights"
---

<objective>
Close GAP 2 (Truth #5) and the trainer-dependent half of GAP 3 (Truth #7), and land REVIEW WR-03 (now oracle-verifiable).

GAP 2: `model.json` is MISSING for YetiRank, YetiRankPairwise, and StochasticRank — the three `*_end_to_end_per_stage` oracle tests pass only by asserting the deferred-fixture invariant. The RNG draw-log oracles are GREEN (separate partial validation); the full training loop is unvalidated.

WR-03 (boosting.rs:2658-2667): on the grouped path the leaf-delta call forces unit weights whenever `group_spans.is_some()`. That is correct for the Newton leaf (der2 already folds the pair weight) but WRONG for YetiRank (non-Pairwise) which rides the Gradient leaf `sumDer/(sumWeight+scaledL2)`, where upstream `UsesPairsForCalculation` makes `sumWeight` the PAIRWISE weight `bt.PairwiseWeights`, not the doc count. With unit weights the denominator mixes a doc-count sumWeight with a pairwise-weight-scaled L2 — internally inconsistent. This becomes oracle-verifiable once the YetiRank model.json lands.

GAP 3 trainer-half: D-07 requires the instrumented TRAINER's per-tree RNG draw log (CB_INSTRUMENT_LOG from actual training) compared against the Rust sampler — not just the standalone self-oracle.

This plan requires the instrumented trainer from 06.3-10 and the regenerated full-precision RNG ground truth from 06.3-12. If 06.3-10 recorded NO-GO, this plan stays the escalate-don't-weaken branch: model.json absent-fixture invariants stay in force, no tolerance weakened, no fabricated fixture; WR-03 still lands (it is a code fix, oracle-gated when the fixture eventually arrives).
Output: three trained fixtures + executing per-stage ≤1e-5 gates + a validated trainer-level RNG draw log (GO), or an un-weakened recorded deferral (NO-GO).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.3-ranking-losses-and-metrics/06.3-REVIEW.md
@.planning/phases/06.3-ranking-losses-and-metrics/deferred-items.md
@crates/cb-oracle/generator/instrumented_trainer_STATUS.md
@crates/cb-oracle/generator/instrument_ranking_rng_README.md
@crates/cb-train/tests/yetirank_oracle_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-03 — branch the grouped leaf sumWeight on leaf method, not on group_spans.is_some()</name>
  <read_first>
    - crates/cb-train/src/boosting.rs lines 2656-2667 (`let unit_weights = vec![1.0; n]; let leaf_weights_for_deltas = if group_spans.is_some() { &unit_weights } else { &eff_weights };`) and the `uses_pairwise_weights` predicate at line 739, the `eff_weights` / `scaled_l2` wiring at 2316-2457
    - 06.3-REVIEW.md section WR-03 (Newton leaf wants unit weights — der2 folds the pair weight; Gradient leaf wants `eff_weights` — pairwise sumWeight)
    - crates/cb-train/src/boosting.rs lines 988-1010 (the `LeafMethod::Gradient` vs `LeafMethod::Newton` leaf-delta arms)
  </read_first>
  <action>
    Replace the WR-03 guard at boosting.rs ~2659-2660. The grouped leaf `sumWeight` must be `eff_weights` for the GRADIENT leaf and unit weights ONLY for the NEWTON leaf (where der2 already folds the pair weight — the der2 analogue of CR-02). Branch on the leaf method (`params.leaf_method`) — or equivalently on `uses_pairwise_weights(&params.loss) && matches!(method, LeafMethod::Gradient)` — NOT uniformly on `group_spans.is_some()`. Concretely: for a grouped loss with the Gradient leaf, pass `&eff_weights`; for a grouped loss with the Newton leaf, pass unit weights; the pointwise path is byte-identical (D-04). Document the upstream `UsesPairsForCalculation` Gradient-`sumWeight = bt.PairwiseWeights` rationale and cite REVIEW WR-03 / T-06.3-14 in a comment. Route any new accumulation via `cb_core::sum_f64`. No `unwrap`/`panic`.
    This fix lands regardless of GO/NO-GO (it is a code correction); it becomes oracle-verified by Task 3's YetiRank model.json on the GO path.
  </action>
  <verify>
    <automated>cargo test -p cb-train --lib 2>&1 | tail -5 && cargo test -p cb-train --test queryrmse_oracle_test --test querysoftmax_oracle_test --test lambdamart_oracle_test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    The grouped leaf `sumWeight` branches on the leaf method (Gradient → `eff_weights`, Newton → unit), not on `group_spans.is_some()`; cb-train --lib green; queryrmse/querysoftmax/lambdamart oracles ≤1e-5 (no regression — those are Newton/pointwise or already-correct, unaffected).
  </acceptance_criteria>
  <done>
    WR-03 closed: YetiRank Gradient leaf denominator uses the pairwise sumWeight; no parity regression on the existing oracles.
  </done>
</task>

<task type="auto">
  <name>Task 2: GAP 3 trainer-half — validate the instrumented trainer RNG draw log against the Rust sampler (D-07)</name>
  <read_first>
    - crates/cb-oracle/generator/instrumented_trainer_STATUS.md (GO/NO-GO gate)
    - crates/cb-oracle/generator/instrument_ranking_rng_README.md (## CB_INSTRUMENT_LOG schema — the per-tree `query_seed`/`gumbel_draw`/`competitor` and `gauss_draw`/`score`/`sorted_order` events)
    - crates/cb-train/src/yetirank.rs (the Rust 2-level seed + Gumbel sampler) and the StochasticRank std_normal der path in ranking_der.rs
    - crates/cb-train/tests/yetirank_oracle_test.rs + stochasticrank_oracle_test.rs (the existing GREEN RNG draw-log oracles — must STAY green)
  </read_first>
  <action>
    GATE on `instrumented_trainer_STATUS.md`. NO-GO → record the trainer-half of GAP 3 as un-weakened deferred, make no fixture; the existing standalone self-oracle RNG validation stands. GO →
    1. Run the instrumented `_catboost.so` training the three ranking_corpus configs with `CB_INSTRUMENT_LOG` set; capture the per-TREE RNG draw log (the actual training RNG stream, not just the standalone generator's smallest unit).
    2. Compare the trainer's per-tree RNG draw sequence against the Rust `yetirank.rs` sampler and the StochasticRank std_normal stream — the draw COUNT and ORDER must match (RESEARCH Pitfall 1). Resolve any divergence in the Rust sampler (transcribe the upstream draw order faithfully; do NOT weaken the compare).
    3. Keep the existing `yetirank_rng_draw_log_oracle` / `stochasticrank_rng_draw_log_oracle` ≤1e-5 GREEN; the trainer-level log is the D-07 evidence the self-oracle alone did not provide.
  </action>
  <verify>
    <automated>cargo test -p cb-train --test yetirank_oracle_test --test stochasticrank_oracle_test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    GO: the trainer-level CB_INSTRUMENT_LOG RNG draw log matches the Rust sampler (draw count + order); the RNG draw-log oracles stay GREEN ≤1e-5. NO-GO: trainer-half recorded as un-weakened deferral; self-oracle validation unchanged.
  </acceptance_criteria>
  <done>
    D-07 trainer-level RNG validation done (GO) or recorded un-weakened deferred (NO-GO); RNG draw-log oracles green throughout.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: RUN-ONCE freeze the three trained model.json fixtures + un-gate the per-stage oracles (human-signed)</name>
  <read_first>
    - crates/cb-oracle/generator/instrumented_trainer_STATUS.md (GO/NO-GO)
    - crates/cb-train/tests/yetirank_oracle_test.rs lines 149-175 (the `yetirank_end_to_end_per_stage` test: `if model_json.exists() { /* wire full compare_stage */ } else { /* assert deferred-fixture invariant */ }`) and the sibling stochasticrank/yetirank_pairwise tests
    - crates/cb-train/tests/lambdamart_oracle_test.rs (the precedent shape: load model.json/staged/predictions, train_ranking, compare_stage all four stages ≤1e-5)
  </read_first>
  <what-built>
    Tasks 1-2 landed WR-03 (Gradient-leaf eff_weights) and (GO) validated the trainer-level RNG draw log. The instrumented trainer from 06.3-10 can now train the three ranking_corpus configs to produce the missing `model.json` + staged/predictions `.npy` fixtures.
  </what-built>
  <how-to-verify>
    GO path:
    1. Train the three configs (YetiRank, YetiRankPairwise, StochasticRank) with the instrumented `_catboost.so`, single-thread (`thread_count=1`); freeze `model.json` + staged/predictions `.npy` under `ranking_corpus/{yetirank,yetirank_pairwise,stochasticrank}/` (RUN-ONCE/COMMIT, OFFLINE).
    2. Remove the absent-fixture invariant branch in each oracle test (`yetirank_oracle_test.rs`, `yetirank_pairwise_oracle_test.rs`, `stochasticrank_oracle_test.rs`) and wire the full `compare_stage(Splits|LeafValues|StagedApprox|Predictions)` ≤1e-5 gate following the `lambdamart_oracle_test.rs` precedent. NO tolerance weakening.
    3. Run all three: `cargo test -p cb-train --test yetirank_oracle_test --test yetirank_pairwise_oracle_test --test stochasticrank_oracle_test` — every stage ≤1e-5 GREEN. The RNG draw-log oracles in the same files must also stay green.
    4. Confirm WR-03 is exercised: the YetiRank Gradient-leaf eff_weights path is now on the verified per-stage path.
    NO-GO path: the three `model.json` stay absent; the tests keep the deferred-fixture invariant; nothing weakened or fabricated. Approve the recorded deferral.
    Approve only after either (GO) all three per-stage oracles pass ≤1e-5 with the frozen fixtures, or (NO-GO) the un-weakened deferral is recorded.
  </how-to-verify>
  <resume-signal>Type "approved — three model.json frozen, per-stage oracles green" or "approved — NO-GO deferral recorded, nothing weakened"</resume-signal>
  <acceptance_criteria>
    GO: `ranking_corpus/{yetirank,yetirank_pairwise,stochasticrank}/model.json` exist; the three oracle tests have NO absent-fixture invariant branch and pass all four `compare_stage` checks ≤1e-5; RNG draw-log oracles still green. NO-GO: fixtures absent, invariant in force, no tolerance changed. RUN-ONCE/COMMIT + human sign-off honored.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFFLINE instrumented trainer → committed model.json + RNG log | The instrumented `_catboost.so` writes trained fixtures + RNG draw log CI reads (never runs the trainer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-14-01 | Tampering | three committed `model.json` fixtures | mitigate | RUN-ONCE/COMMIT OFFLINE + human sign-off; the un-gated per-stage oracle compares the Rust trainer against the frozen catboost 1.2.10 model.json ≤1e-5, so a corrupted fixture fails the gate rather than passing silently |
| T-06.3-14-02 | Tampering | trainer-level RNG draw log | mitigate | Compared against the oracle-locked Rust `TFastRng64`/`std_normal` sampler; divergence is caught at compare time |
| T-06.3-14-03 | Denial of Service | WR-03 leaf-weight branch | mitigate | New accumulation via `cb_core::sum_f64`; no `unwrap`/`panic`; pointwise path byte-identical (D-04) |
| T-06.3-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; trainer artifact from 06.3-10's sudo-free build |

No network/auth/PII surface; no high-severity threats apply.
</threat_model>

<verification>
- WR-03: grouped leaf sumWeight branches on leaf method; cb-train --lib green; queryrmse/querysoftmax/lambdamart oracles ≤1e-5 unchanged.
- GO: three `model.json` exist; the three per-stage oracles have no absent-fixture branch and pass all four stages ≤1e-5; RNG draw-log oracles green; trainer-level RNG log matches the Rust sampler.
- NO-GO: fixtures absent, invariants in force, no tolerance weakened; WR-03 still landed.
</verification>

<success_criteria>
GAP 2 (Truth #5) and the trainer-half of GAP 3 (Truth #7) closed with three trained fixtures + executing ≤1e-5 per-stage gates + validated trainer-level RNG, and WR-03 landed and oracle-verified — OR an un-weakened recorded deferral on a 06.3-10 NO-GO with WR-03 still landed.
</success_criteria>

<artifacts_produced>
- MODIFIED: `crates/cb-train/src/boosting.rs` (WR-03 Gradient-leaf eff_weights branch)
- NEW (GO, frozen, human-signed): `ranking_corpus/{yetirank,yetirank_pairwise,stochasticrank}/model.json` + staged/predictions `.npy`
- MODIFIED (GO): `yetirank_oracle_test.rs`, `yetirank_pairwise_oracle_test.rs`, `stochasticrank_oracle_test.rs` (absent-fixture invariant removed, full compare_stage gates wired)
- (GO) trainer-level RNG draw log captured + validated against the Rust sampler (D-07)
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ranking-losses-and-metrics/06.3-14-SUMMARY.md` when done
</output>
