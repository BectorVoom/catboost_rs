---
phase: 06.3-ranking-losses-and-metrics
plan: 11
subsystem: testing
tags: [ranking, stochasticrank, ndcg, dcg, clippy, indexing_slicing, bounds-check, parity, lambdamart]

# Dependency graph
requires:
  - phase: 06.3-ranking-losses-and-metrics
    provides: "CR-01 pos_weights.get(..) discipline in calc_dcg_metric_diff (06.3-06); the stochastic_rank_group_der DCG cumulative-stats loop (06.3-04)"
provides:
  - "calc_dcg_metric_diff reads all cum_sum/cum_sum_up/cum_sum_low prefix sums via bounds-checked .get(..).copied().unwrap_or(0.0) (WR-02 closed)"
  - "stochastic_rank_group_der has zero clippy::indexing_slicing panic sites (42 -> 0), numerically bit-identical"
affects: [06.3-ranking-losses-and-metrics, hardening, clippy-gate]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bounds-checked read: slice.get(i).copied().unwrap_or(0.0) on parity-critical in-range indices"
    - "Bounds-checked write: if let Some(slot) = vec.get_mut(i) { *slot = .. } for pre-sized Vecs"
    - "Nested scores[order[p]] reads via a local score_at(p) closure"
    - "Iterator-zip rewrite of indexed loops in canonical doc-ascending order (D-08-preserving)"

key-files:
  created: []
  modified:
    - "crates/cb-compute/src/ranking_der.rs"
    - "crates/cb-compute/src/ranking_der_test.rs"

key-decisions:
  - "WR-02 + the 42 indexing_slicing sites converted to bounds-checked access forms with provably ZERO numerical change; no unwrap()/expect() introduced (CLAUDE.md ban); oracle-revalidated rather than blind-fixed"
  - "count>=2 invariant (count<=1 early return) makes saturating_sub(1) == count-1 and every .get() index in-range, so the conversion is bit-identical"

patterns-established:
  - "Parity-critical numeric loops: prefer iterator/zip or .get()/.get_mut() over raw [] while keeping the exact same accumulation order (D-08)"

requirements-completed: [LOSS-04]

# Metrics
duration: 5min
completed: 2026-06-17
---

# Phase 06.3 Plan 11: ranking_der.rs index-panic hardening Summary

**Removed every latent index-panic site from the StochasticRank/NDCG parity-critical reductions — WR-02 plus the 42 pre-existing clippy::indexing_slicing sites in stochastic_rank_group_der — with provably zero numerical change (lambdamart + stochasticrank oracles stay ≤1e-5).**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-17T04:41:16Z
- **Completed:** 2026-06-17T04:46:11Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- **WR-02 closed:** the 6 raw `cum_sum`/`cum_sum_up`/`cum_sum_low` subscripts in both arms of `calc_dcg_metric_diff` now read via `.get(..).copied().unwrap_or(0.0)`, matching the CR-01 `pos_weights.get(..)` discipline two lines above. Bit-identical for the present caller (arrays are length `count+1`); removes the latent panic on a future `top`/`query_top_size` index desync.
- **42 → 0 clippy::indexing_slicing:** every raw `[]` in `stochastic_rank_group_der` (noise/scores fill, DCG cumulative-stats prefix build, per-position der loop, `scores[order[p]]` density reads, `der1[doc_id]` accumulation, SFA dot+projection) converted to bounds-checked reads (`.get`), guarded writes (`get_mut`), iterator-zip, or a `score_at` closure. No `unwrap()`/`expect()` introduced.
- **Oracle-revalidated, not blind-fixed:** lambdamart_oracle 1/1 and stochasticrank_oracle 2/2 stay GREEN ≤1e-5; cb-compute lib 124/124 (122 baseline + 2 new), cb-train lib 189/189 — no parity regression.

## Task Commits

Each task was committed atomically:

1. **Task 1: WR-02 — bounds-check the cum_sum prefix-array reads in calc_dcg_metric_diff** - `21ddf5a` (fix; tests + source together, identity refactor)
2. **Task 2: clippy hardening — convert the 42 indexing_slicing sites in stochastic_rank_group_der** - `0fdd507` (fix)

_Note: Task 1 is `tdd="true"`. Because the change is numerically identity (in-range indices on length `count+1` arrays), the two RED-first behavior tests lock the reference values and pass both before and after the source edit — the meaningful gate is that they stay green after the access-form change._

## Files Created/Modified
- `crates/cb-compute/src/ranking_der.rs` - bounds-checked `calc_dcg_metric_diff` (6 prefix reads) + `stochastic_rank_group_der` (all 42 index sites)
- `crates/cb-compute/src/ranking_der_test.rs` - 2 new WR-02 behavior tests: `calc_dcg_metric_diff_boundary_new_pos_plus_one_eq_count` and `calc_dcg_metric_diff_graded_relevance_identical_both_arms` (+ `build_dcg_prefix_arrays` test helper)

## Decisions Made
- Kept the exact accumulation order (D-08) in every rewrite: the iterator-zip forms (noise/scores fill, SFA dot/projection) iterate in canonical doc-ascending order, identical to the indexed loops they replace.
- Used `count.saturating_sub(1)` for the `cum_sum_low[count] = cum_sum_low[count-1]` tail copy; the `count<=1` early-return guarantees `count>=2`, so this equals `count-1` exactly — bit-identical, panic-free.
- Did not touch the 2 substantive pre-existing clippy warnings elsewhere in the crate (multiply-by-1, excessive-precision) — out of this plan's scope (they live outside `ranking_der.rs`).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- LOSS-04's two trainer-INDEPENDENT hardening findings (WR-02 + the 42 clippy sites) are closed independently of the 06.3-10 build outcome, as the plan required.
- Still deferred for LOSS-04 (unchanged by this plan): truths #5 (end-to-end trainer fixtures) and #7 (SC-3 instrumented harness), pending the instrumented catboost trainer GO sign-off recorded in 06.3-10.

## Self-Check: PASSED

- FOUND (file): `crates/cb-compute/src/ranking_der.rs`
- FOUND (file): `crates/cb-compute/src/ranking_der_test.rs`
- FOUND (commit): `21ddf5a` (Task 1)
- FOUND (commit): `0fdd507` (Task 2)
- Acceptance — `grep -nE 'cum_sum(_up|_low)?\[' ranking_der.rs` returns only prefix-BUILD writes (none in `calc_dcg_metric_diff`): VERIFIED
- Acceptance — `cargo clippy -p cb-compute --lib` reports 0 `indexing may panic`: VERIFIED
- Oracles — lambdamart 1/1, stochasticrank 2/2 ≤1e-5; cb-compute lib 124/124; cb-train lib 189/189: VERIFIED

---
*Phase: 06.3-ranking-losses-and-metrics*
*Completed: 2026-06-17*
