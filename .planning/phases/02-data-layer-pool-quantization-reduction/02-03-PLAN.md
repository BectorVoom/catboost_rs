---
phase: 02-data-layer-pool-quantization-reduction
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - crates/cb-data/src/lib.rs
  - crates/cb-data/src/nan_mode.rs
  - crates/cb-data/src/nan_mode_test.rs
  - crates/cb-data/src/quantized_pool.rs
  - crates/cb-data/src/quantized_pool_test.rs
  - crates/cb-data/src/quantize.rs
  - crates/cb-data/tests/quantize_oracle_test.rs
autonomous: true
requirements: [DATA-02, DATA-04]
must_haves:
  truths:
    - "NanMode Min prepends f32::MIN, Max appends f32::MAX; NaN consumes one border budget; bin assignment is strict value > border"
    - "QuantizedPool stores u8/u16 (float) and u32 (cat) bin indices in immutable columnar SoA via a typed per-column width enum"
    - "pool.quantize(&params) -> QuantizedPool quantizes a float feature end-to-end and the binned column matches expected bins on the numeric_nan corpus"
  artifacts:
    - path: "crates/cb-data/src/nan_mode.rs"
      provides: "NanMode enum + sentinel insertion + strict value>border bin assignment (DATA-04)"
      contains: "enum NanMode"
    - path: "crates/cb-data/src/quantized_pool.rs"
      provides: "QuantizedPool SoA + ColumnBins width enum (DATA-02, D-10/D-11/D-12)"
      contains: "enum ColumnBins"
    - path: "crates/cb-data/src/quantize.rs"
      provides: "pool.quantize(&params) -> QuantizedPool driver (D-01)"
      contains: "fn quantize"
  key_links:
    - from: "crates/cb-data/src/quantize.rs"
      to: "crates/cb-data/src/borders.rs"
      via: "border selection during quantization"
      pattern: "borders::"
    - from: "crates/cb-data/src/quantize.rs"
      to: "crates/cb-data/src/nan_mode.rs"
      via: "sentinel insertion + bin assignment"
      pattern: "nan_mode::"
    - from: "crates/cb-data/tests/quantize_oracle_test.rs"
      to: "crates/cb-oracle/fixtures/borders_quant"
      via: "numeric_nan border + bin comparison"
      pattern: "numeric_nan"
---

<objective>
Float-feature quantization vertical slice: `NanMode` sentinel handling + strict `value > border` bin assignment, the immutable `QuantizedPool` SoA with the typed per-column width enum (`u8`/`u16`/`u32`), and the `pool.quantize(&params) -> QuantizedPool` driver that composes borders + NaN handling into binned columns — oracle-validated on the NaN-containing corpus.

Purpose: This completes the float path from raw `Pool` to immutable binned `QuantizedPool`, locking the NaN sentinel and `<`/`<=` semantics that determine which bin every boundary value lands in. Memory efficiency (first-class constraint) is realized via the zero-widening width enum.
Output: `nan_mode.rs`, `quantized_pool.rs` (+ `ColumnBins`), `quantize.rs`, and a green `quantize_oracle_test` on the `numeric_nan` dataset.
</objective>

<artifacts_this_phase_produces>
NEW symbols/paths introduced by this plan (excluded from source-drift verification):
- Enums: `NanMode { Min, Max, Forbidden }`, `ColumnBins { U8(Vec<u8>), U16(Vec<u16>), U32(Vec<u32>) }`
- Structs: `QuantizedPool`, the quantization params struct consumed by `quantize`
- Functions: `Pool::quantize` (or `quantize(...)` driver), the `bin_of` strict-`>` assignment helper, the width-selection helper (CalcHistogramWidthForBorders equivalent)
- Files: `crates/cb-data/src/{nan_mode.rs,quantized_pool.rs,quantize.rs}`, `crates/cb-data/tests/quantize_oracle_test.rs`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-data-layer-pool-quantization-reduction/02-RESEARCH.md
@.planning/phases/02-data-layer-pool-quantization-reduction/02-PATTERNS.md
@.planning/phases/02-data-layer-pool-quantization-reduction/02-01-SUMMARY.md
@.planning/phases/02-data-layer-pool-quantization-reduction/02-02-SUMMARY.md
@crates/cb-oracle/src/compare.rs
@crates/cb-oracle/tests/per_stage_oracle_test.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NanMode enum + sentinel borders + strict value>border bin assignment (DATA-04)</name>
  <files>crates/cb-data/src/nan_mode.rs, crates/cb-data/src/nan_mode_test.rs, crates/cb-data/src/lib.rs</files>
  <read_first>
    - `crates/cb-oracle/src/compare.rs:9-21` — the small `#[derive(Debug, Clone, Copy, PartialEq, Eq)] enum` with per-variant doc comments (exact shape for `NanMode`), per 02-PATTERNS.md "nan_mode.rs" assignment.
    - Source of truth: 02-RESEARCH.md Pattern 2 (bin assignment strict `value > border`; `<=64` borders → count of `value > border`, `>64` → `partition_point`/lower_bound; equal value lands in LOWER bin; utils.h:28-49) and Pattern 3 (Min prepends `f32::MIN`/`lowest()`, Max appends `f32::MAX`; NaN consumes one border budget `border_count - 1`; NaN→0 for Min/Forbidden, NaN→top for Max; no-NaN-in-learn forces Forbidden; quantization.cpp:322-345, utils.h:51-66). The A1/A3 sentinel resolution recorded in `02-01-SUMMARY.md`.
    - `enums.h:107-111` `ENanMode {Min,Max,Forbidden}` variant order.
  </read_first>
  <behavior>
    - bin_of(borders, value) counts borders strictly less than value; value == a border lands in the lower bin.
    - NanMode::Min inserts f32::MIN at borders[0]; NanMode::Max appends f32::MAX; both consume one border from the budget.
    - A NaN value quantizes to bin 0 under Min/Forbidden and to the top bin under Max.
  </behavior>
  <action>
    Create `nan_mode.rs` with `pub enum NanMode { Min, Max, Forbidden }` (`#[derive(Debug, Clone, Copy, PartialEq, Eq)]`, per-variant docs) mirroring `Stage`'s shape and matching upstream `enums.h:107-111` order. Add a `bin_of(borders: &[f32], value: f32) -> u32` strict-`>` assignment (count of `value > b` for `<=64` borders, else `partition_point(|&b| b < value)`) — equal value to the LOWER bin (RESEARCH Pattern 2, the `<`/`<=` answer). Add a sentinel-insertion function: for `Min` prepend `f32::MIN` (Rust `f32::MIN` == C++ `lowest()`), for `Max` append `f32::MAX`, both reserving one border from the budget; and a NaN-assignment helper returning `borders.len()` for `Max`, else `0`. Transcribe per-step from the cited C++ with `//` comments. No `unwrap`/`panic`/indexing. Add `mod nan_mode;` + `pub use` to `lib.rs`. Write `nan_mode_test.rs` (dedicated file) covering: the strict-`>` boundary (a value exactly equal to a border → lower bin), sentinel insertion for Min/Max, and NaN→0 (Min/Forbidden) / NaN→top (Max).
  </action>
  <verify>
    <automated>cargo test -p cb-data nan_mode 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-data/src/nan_mode.rs` contains `pub enum NanMode` with `Min`, `Max`, `Forbidden`.
    - A test asserts `value == border` lands in the lower bin (strict `>` semantics).
    - A test asserts NaN→0 under Min/Forbidden and NaN→top bin under Max.
    - A test asserts Min prepends `f32::MIN` and Max appends `f32::MAX`.
    - `cargo test -p cb-data nan_mode` exits 0; `cargo clippy -p cb-data --lib -- -D warnings` clean.
  </acceptance_criteria>
  <done>NanMode sentinel insertion and strict value>border bin assignment match upstream, with NaN placement correct per mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuantizedPool SoA + ColumnBins width enum + quantize driver (DATA-02) + oracle</name>
  <files>crates/cb-data/src/quantized_pool.rs, crates/cb-data/src/quantized_pool_test.rs, crates/cb-data/src/quantize.rs, crates/cb-data/tests/quantize_oracle_test.rs, crates/cb-data/src/lib.rs</files>
  <read_first>
    - `crates/cb-oracle/src/compare.rs:9-21` — enum-with-per-variant-docs shape for `ColumnBins` (02-PATTERNS.md "quantized_pool.rs" assignment).
    - `crates/cb-core/src/rng.rs` — orchestration analog for the `quantize.rs` driver (a type whose methods compose sub-utilities).
    - `crates/cb-oracle/tests/per_stage_oracle_test.rs` — fixture-read + compare integration-test shape.
    - Source of truth: 02-RESEARCH.md Pattern 5 / D-10/D-11 (`CalcHistogramWidthForBorders`: `<256` borders → u8, `<65536` → u16, asserts `<65536`; float NEVER u32; u32 only for categorical perfect-hash; utils.h:175-181), D-03 (QuantizedPool immutable after build; trainer reuses its OWN scratch — QuantizedPool hands out no mutable scratch), D-12 (per-feature `Vec` SoA), D-01 (`pool.quantize(&params) -> QuantizedPool`). The resolved `border_count`/sentinel from `02-01-SUMMARY.md`.
  </read_first>
  <behavior>
    - Width selection: a feature with <256 borders → ColumnBins::U8; <65536 → U16; a categorical with >65535 uniques → U32.
    - QuantizedPool build→read round-trip is lossless for each width arm.
    - pool.quantize on the numeric_nan corpus produces borders matching get_borders() (1e-5) and binned indices consistent with strict value>border + NaN placement.
  </behavior>
  <action>
    Create `quantized_pool.rs` with `pub enum ColumnBins { U8(Vec<u8>), U16(Vec<u16>), U32(Vec<u32>) }` (per-variant docs) and a `pub struct QuantizedPool` holding immutable per-feature SoA bin columns (`Vec<ColumnBins>`), per-feature borders, and `NanMode`/feature metadata (D-11/D-12; immutable after build per D-03 — no mutable-scratch accessor). Add a width-selection helper transcribed from `utils.h:175-181`: `<256` borders → `U8`, `<65536` → `U16` for float features; categorical perfect-hash uniq-count → `U8`/`U16`/`U32` (feature-kind-dependent, RESEARCH Pattern 5). Create `quantize.rs` with the `pool.quantize(&params) -> CbResult<QuantizedPool>` driver (D-01) that, per float feature: selects borders via `borders::` (Plan 02), inserts NaN sentinels + reserves NaN budget via `nan_mode::` (Task 1), assigns each value to a bin via `nan_mode::bin_of` (strict `>`), and stores into the width-selected `ColumnBins`. Route every sum through `cb_core::reduction` (D-08). Add `mod quantized_pool; mod quantize;` + `pub use` to `lib.rs`. Write `quantized_pool_test.rs` (width selection + lossless round-trip per arm) and the integration test `crates/cb-data/tests/quantize_oracle_test.rs`: quantize the `numeric_nan` corpus, compare per-feature borders to `borders_quant/` via `compare_stage(Stage::Borders, ...)` honoring the recorded sentinel handling (A3), and assert NaN values land in the expected bin per the dataset's `nan_mode`.
  </action>
  <verify>
    <automated>cargo test -p cb-data quantized_pool 2>&1 | tail -5; cargo test -p cb-data --test quantize_oracle_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cb-data/src/quantized_pool.rs` contains `pub enum ColumnBins` with `U8`/`U16`/`U32` arms and `pub struct QuantizedPool`.
    - `crates/cb-data/src/quantize.rs` exposes a `quantize` driver returning `CbResult<QuantizedPool>`; it references `borders::` and `nan_mode::`.
    - A width-selection test asserts `<256`→U8, `<65536`→U16, cat>65535→U32; a round-trip test is lossless per arm.
    - `crates/cb-data/tests/quantize_oracle_test.rs` calls `compare_stage(Stage::Borders, ...)` on `numeric_nan` and exits 0 within 1e-5, with NaN bin placement asserted.
    - `cargo test -p cb-data quantized_pool` and `--test quantize_oracle_test` exit 0; `bash scripts/check-no-raw-float-sum.sh` exits 0.
  </acceptance_criteria>
  <done>pool.quantize turns a raw float Pool into an immutable QuantizedPool whose borders match upstream and whose NaN bins are placed correctly, stored in the zero-widening width enum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw float column → quantized bins | wrong sentinel / off-by-one NaN budget / inclusive border silently shifts every boundary value |
| border_count / uniq-count → bin width | unbounded width or wrong arm selection breaks memory layout or overflows the index type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | inclusive vs strict border (`value >= border`) | mitigate | Strict `value > border` transcribed from utils.h:28-49; boundary-equality test asserts lower-bin placement |
| T-02-08 | Tampering | NaN sentinel off-by-one (border-count ±1 on NaN features) | mitigate | NaN consumes exactly one border budget (`border_count - 1`); oracle compares against numeric_nan get_borders() honoring A3 |
| T-02-09 | DoS / Tampering | integer overflow on bin index / width arm | mitigate | Width selection asserts `<65536` for float (utils.h), u32 reserved for cat; deny `indexing_slicing`; fallible paths return CbResult |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `cargo test -p cb-data nan_mode | quantized_pool | --test quantize_oracle_test` all green.
- `bash scripts/check-no-raw-float-sum.sh` exits 0.
- `cargo clippy -p cb-data --lib -- -D warnings` clean.
</verification>

<success_criteria>
- DATA-04 NanMode sentinel + strict-`>` assignment match upstream; NaN placement correct.
- DATA-02 QuantizedPool immutable SoA with u8/u16/u32 width enum; build→read lossless.
- pool.quantize float path oracle-locked on numeric_nan ≤1e-5.
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-pool-quantization-reduction/02-03-SUMMARY.md` when done.
</output>
