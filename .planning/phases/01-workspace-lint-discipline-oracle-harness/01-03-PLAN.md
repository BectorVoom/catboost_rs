---
phase: 01-workspace-lint-discipline-oracle-harness
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - crates/cb-data/Cargo.toml
  - crates/cb-data/src/lib.rs
  - crates/cb-compute/Cargo.toml
  - crates/cb-compute/src/lib.rs
  - crates/cb-backend/Cargo.toml
  - crates/cb-backend/src/lib.rs
  - crates/cb-train/Cargo.toml
  - crates/cb-train/src/lib.rs
  - crates/cb-model/Cargo.toml
  - crates/cb-model/src/lib.rs
  - crates/catboost-rs/Cargo.toml
  - crates/catboost-rs/src/lib.rs
  - crates/cb-oracle/generator/requirements.txt
  - crates/cb-oracle/generator/gen_inputs.py
  - crates/cb-oracle/generator/gen_fixtures.py
  - crates/cb-oracle/generator/README.md
  - crates/cb-oracle/fixtures/inputs/numeric_tiny/config.json
  - crates/cb-oracle/fixtures/inputs/numeric_categorical/config.json
  - crates/cb-oracle/fixtures/inputs/grouped_ranking/config.json
  - crates/cb-oracle/fixtures/regression_skeleton/config.json
  - scripts/check-source-test-separation.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [INFRA-01, INFRA-03, INFRA-04, INFRA-06]
user_setup: []

must_haves:
  truths:
    - "All six stub crates (cb-data, cb-compute, cb-backend, cb-train, cb-model, catboost-rs) build; cb-backend compiles under each of cpu/wgpu/cuda/rocm features"
    - "cb-compute has NO cubecl dependency; cb-backend is the sole feature-gated runtime-alias owner"
    - "A frozen input corpus (numeric, numeric+categorical, grouped/ranking) is generated by the pinned catboost==1.2.10 generator and committed"
    - "A per-stage borders+predictions oracle fixture is generated and compared at <=1e-5 via the cb-oracle comparator"
    - "scripts/check-source-test-separation.sh fails if any production src/*.rs (non *_test.rs) contains an inline #[cfg(test)] module"
  artifacts:
    - path: "crates/cb-backend/src/lib.rs"
      provides: "cfg-gated SelectedRuntime alias, one arm per cpu/wgpu/cuda/rocm (inert stub)"
      contains: "SelectedRuntime"
    - path: "crates/cb-compute/Cargo.toml"
      provides: "pure-generic crate, NO cubecl dependency (D-03)"
    - path: "crates/cb-oracle/generator/gen_fixtures.py"
      provides: "Python oracle generator (borders/json/staged/predict -> .npy + config.json), thread_count=1, pinned seed"
      contains: "thread_count"
    - path: "crates/cb-oracle/fixtures/inputs/numeric_tiny/config.json"
      provides: "First dataset of the frozen shared input corpus (D-11)"
    - path: "scripts/check-source-test-separation.sh"
      provides: "INFRA-06 grep gate for inline #[cfg(test)] in production modules"
  key_links:
    - from: "crates/cb-oracle/generator/gen_fixtures.py"
      to: "crates/cb-oracle/fixtures/inputs/<dataset>/"
      via: "reads frozen input corpus, writes expected-output fixtures referencing it"
      pattern: "fixtures/inputs"
    - from: "crates/cb-oracle/src/compare.rs::assert_abs_close"
      to: "crates/cb-oracle/fixtures/regression_skeleton/predictions.npy"
      via: "borders+predictions per-stage comparison at 1e-5 in an integration test"
      pattern: "assert_abs_close|Stage"
    - from: ".github/workflows/ci.yml"
      to: "crates/cb-backend"
      via: "cargo build -p cb-backend --features wgpu,cuda,rocm stub-build step"
      pattern: "cb-backend"
---

<objective>
Widen the Walking Skeleton to the full Phase-1 surface: stub all six remaining workspace crates with their feature gates (cb-backend single feature-gated runtime alias per D-02; cb-compute cubecl-free per D-03), stand up the Python-first oracle generator (pinned `catboost==1.2.10`, `thread_count=1`, fixed seed), generate and commit the frozen shared INPUT corpus plus a per-stage (borders + predictions) expected-OUTPUT fixture, prove the per-stage comparator against that real oracle fixture at <=1e-5, and add the source/test-separation grep gate.

Purpose: Completes INFRA-01 (all crates + feature gates), the generation side of INFRA-03, INFRA-04 (per-stage borders/splits/leaf/approx comparison demonstrated on real oracle data), and INFRA-06 (source/test separation enforced by script). Establishes the frozen corpus (D-11) that all 8 phases draw from.
Output: six stub crates, `crates/cb-oracle/generator/*` (Python build-time tool, never in CI), the committed `fixtures/inputs/*` corpus + `fixtures/regression_skeleton/*`, `scripts/check-source-test-separation.sh`, and the CI extension building cb-backend under all feature gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-workspace-lint-discipline-oracle-harness/01-RESEARCH.md
@.planning/phases/01-workspace-lint-discipline-oracle-harness/01-PATTERNS.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Consumes Plan 01's cb-oracle (assert_abs_close, Stage, load_f64_vec, load_config, OracleError)
     and the root Cargo.toml workspace + [workspace.lints] + check-no-anyhow.sh. -->
Stub crates each export: a minimal lib.rs with the cfg(test) clippy-allow header, [lints] workspace = true,
and NO anyhow in [dependencies] for the six core libs (cb-data/cb-compute/cb-backend/cb-train/cb-model;
catboost-rs facade treated as a library too per D-15 default — thiserror-only, no anyhow).

cb-backend/src/lib.rs (D-02 Pattern 2):
  #[cfg(feature="cpu")] pub type SelectedRuntime = ();   // placeholder; real runtime in P3/P7
  ...one cfg arm per wgpu/cuda/rocm... (compile-time selection, zero runtime dispatch)
  [features] default=["cpu"]; cpu=[]; wgpu=[]; cuda=[]; rocm=[]  (all inert in P1)

Generator (Python, build-time ONLY — never CI, D-12) writes the hybrid fixture format (D-09):
  fixtures/inputs/<dataset>/{config.json, X.npy[, cat.npy][, group_id.npy], y.npy}
  fixtures/<scenario>/{config.json, borders.npy (or per-feature), predictions.npy, staged.npy, model.json}
Rust side reads via Plan 01's load_f64_vec/load_config and compares via assert_abs_close.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Six stub crates with feature gates (cb-backend single alias, cb-compute cubecl-free)</name>
  <read_first>
    - .planning/phases/01-workspace-lint-discipline-oracle-harness/01-PATTERNS.md (Stub crates section; cb-backend Pattern 2; cb-compute D-03 no-cubecl; Lint/Test Exemption header)
    - .planning/phases/01-workspace-lint-discipline-oracle-harness/01-RESEARCH.md (Pattern 2 feature-gated alias; Anti-Patterns: per-backend crates rejected, no cubecl in cb-compute)
    - CLAUDE.md + AGENTS.md (cb- naming; CubeCL rules apply ONLY to cb-backend/cb-compute and these are STUB-ONLY this phase — do NOT write kernels)
  </read_first>
  <files>crates/cb-data/Cargo.toml, crates/cb-data/src/lib.rs, crates/cb-compute/Cargo.toml, crates/cb-compute/src/lib.rs, crates/cb-backend/Cargo.toml, crates/cb-backend/src/lib.rs, crates/cb-train/Cargo.toml, crates/cb-train/src/lib.rs, crates/cb-model/Cargo.toml, crates/cb-model/src/lib.rs, crates/catboost-rs/Cargo.toml, crates/catboost-rs/src/lib.rs</files>
  <action>
    Create six stub crates. Each `Cargo.toml`: correct `cb-`-prefixed (or `catboost-rs`) package name, edition latest, `[lints] workspace = true`, and NO `anyhow` in `[dependencies]` (D-14; the facade `catboost-rs` is treated as a thiserror-only library per D-15 default). Each `src/lib.rs`: first line `#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing))]`, a one-line module doc-comment stating which phase realizes the crate (per D-05 crate->phase map), and minimal/no public items (empty stub). NO inline `#[cfg(test)]` test bodies (D-17).
    `cb-backend` (D-02 / RESEARCH Pattern 2): `[features] default=["cpu"]`, `cpu=[]`, `wgpu=[]`, `cuda=[]`, `rocm=[]` (all inert). In `lib.rs`, one `#[cfg(feature=...)]` arm per backend defining `pub type SelectedRuntime = ();` as a placeholder, selected at compile time with no runtime match. Do NOT add a cubecl dependency yet (wired in P7). Do NOT create per-backend crates.
    `cb-compute` (D-03): pure-generic crate — MUST NOT list `cubecl` (or any backend) in `[dependencies]`. Stub `lib.rs` only; the generic `R: Runtime`/`F: Float` seam is realized in P3.
    Add all six to the workspace `members` glob (already `crates/*` from Plan 01 — confirm they are picked up).
  </action>
  <verify>
    <automated>cargo build --workspace && cargo build -p cb-backend --no-default-features --features wgpu && cargo build -p cb-backend --no-default-features --features cuda && cargo build -p cb-backend --no-default-features --features rocm && ! grep -q 'cubecl' crates/cb-compute/Cargo.toml && cargo clippy --workspace --lib -- -D warnings</automated>
  </verify>
  <done>All six stubs build; cb-backend compiles under each of cpu/wgpu/cuda/rocm; cb-compute has no cubecl dep; workspace clippy clean; no anyhow in any core-lib `[dependencies]`.</done>
</task>

<task type="auto">
  <name>Task 2: Python oracle generator + frozen input corpus + per-stage expected-output fixture</name>
  <read_first>
    - .planning/phases/01-workspace-lint-discipline-oracle-harness/01-RESEARCH.md (Oracle generator Python example; verified catboost==1.2.10 API: get_borders/save_model(json)/staged_predict/predict; D-06..D-12; Pitfall 2 thread_count=1; Pitfall 3 np.float64; Environment Availability — catboost/numpy NOT installed, needs venv; Open Question 2 corpus shapes)
    - .planning/phases/01-workspace-lint-discipline-oracle-harness/01-PATTERNS.md (generator + fixtures sections; D-10/D-11 layout)
  </read_first>
  <files>crates/cb-oracle/generator/requirements.txt, crates/cb-oracle/generator/gen_inputs.py, crates/cb-oracle/generator/gen_fixtures.py, crates/cb-oracle/generator/README.md, crates/cb-oracle/fixtures/inputs/numeric_tiny/config.json, crates/cb-oracle/fixtures/inputs/numeric_categorical/config.json, crates/cb-oracle/fixtures/inputs/grouped_ranking/config.json, crates/cb-oracle/fixtures/regression_skeleton/config.json</files>
  <action>
    Set up the build-time generator environment ONCE (never in CI, D-12): create `crates/cb-oracle/generator/requirements.txt` pinning `catboost==1.2.10` and a compatible `numpy` (D-07). Create a venv and `pip install -r requirements.txt`; if the install fails (e.g. numpy/catboost wheel unavailable for this Python), record the exact failure in the SUMMARY and proceed — but the corpus MUST end up committed, so resolve the env (try a compatible numpy pin per RESEARCH Assumption A3) rather than skipping.
    `gen_inputs.py`: deterministically synthesize the frozen INPUT corpus (D-11; RESEARCH Open Question 2 shapes) under `fixtures/inputs/<dataset>/` — (a) `numeric_tiny` ~50x4 pure numeric; (b) `numeric_categorical` ~50 rows x (3 numeric + 2 categorical); (c) `grouped_ranking` ~60 rows with `group_id`. Use a fixed seed for input synthesis; write `X.npy` (+ `cat.npy`/`group_id.npy` as applicable) as `np.float64`/appropriate dtype and `y.npy`, plus a per-dataset `config.json` recording seed, shape, column kinds. Assert `dtype==np.float64` for float arrays (Pitfall 3).
    `gen_fixtures.py`: for the `regression_skeleton` scenario, load the `numeric_tiny` input, train `CatBoostRegressor(iterations=10, learning_rate=0.1, depth=4, random_seed=0, thread_count=1, verbose=False)` (Pitfall 2), then extract per-stage values: `get_borders()` -> `borders.npy` (per-feature; document layout in README), `save_model(format='json')` -> `model.json` (splits + leaf_values, INFRA-04), `staged_predict` -> `staged.npy`, `predict` -> `predictions.npy`. Save all numeric arrays as `np.float64`. Write `fixtures/regression_skeleton/config.json` with `{"seed":0,"catboost_version":"1.2.10","thread_count":1,"input_dataset":"numeric_tiny","params":...}`.
    `README.md`: document how to (re)generate (venv + `pip install` + run scripts), the fixture layout (D-10), and the hard rule that the generator NEVER runs in CI and fixtures are committed frozen (D-12). COMMIT all generated `.npy`/`.json`/`model.json` artifacts (do not gitignore fixtures/).
  </action>
  <verify>
    <automated>test -f crates/cb-oracle/generator/gen_fixtures.py && grep -q 'thread_count' crates/cb-oracle/generator/gen_fixtures.py && grep -q '1.2.10' crates/cb-oracle/generator/requirements.txt && test -f crates/cb-oracle/fixtures/inputs/numeric_tiny/config.json && test -f crates/cb-oracle/fixtures/regression_skeleton/config.json && test -f crates/cb-oracle/fixtures/regression_skeleton/predictions.npy</automated>
  </verify>
  <done>Generator scripts exist and pin catboost==1.2.10 + thread_count=1 + fixed seed; the three input datasets and the regression_skeleton expected-output fixtures (borders, model.json, staged, predictions) are generated and committed frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Per-stage comparator proof on real oracle data + source/test-separation gate + CI extension</name>
  <read_first>
    - .planning/phases/01-workspace-lint-discipline-oracle-harness/01-RESEARCH.md (Pattern 3 per-stage comparator; Validation test map INFRA-04/INFRA-06; D-16 CI; Sampling Rate)
    - .planning/phases/01-workspace-lint-discipline-oracle-harness/01-PATTERNS.md (compare.rs; check-no-anyhow analog for the new grep script; ci.yml section)
  </read_first>
  <files>crates/cb-oracle/src/compare.rs, crates/cb-oracle/src/lib.rs, crates/cb-oracle/tests/per_stage_oracle_test.rs, crates/cb-oracle/src/compare_test.rs, scripts/check-source-test-separation.sh, .github/workflows/ci.yml</files>
  <action>
    Extend the comparator (Plan 01 created `assert_abs_close`/`Stage`): add a thin per-stage convenience `pub fn compare_stage(stage: Stage, expected: &[f64], actual: &[f64]) -> Result<(), OracleError>` that calls `assert_abs_close(expected, actual, 1e-5)` and tags `Diverged`/`LengthMismatch` errors with the `Stage` (so failures say which stage drifted, INFRA-04). Keep using `.iter().zip().enumerate()` (no indexing). Re-export from lib.rs. Extend `compare_test.rs` for the new function.
    Add `crates/cb-oracle/tests/per_stage_oracle_test.rs` (integration test; file-scope `#![allow(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::indexing_slicing)]`): load `fixtures/regression_skeleton/predictions.npy` AND `borders.npy` via `cb_oracle::load_f64_vec`, and assert `compare_stage(Stage::Predictions, &expected, &expected, ).is_ok()` and `compare_stage(Stage::Borders, &b, &b).is_ok()` (comparing the committed oracle vector to itself proves the per-stage read+compare pipeline against REAL oracle data at 1e-5; later phases supply the `actual` from cb-train/cb-model). This demonstrates INFRA-04 per-stage comparison on borders AND predictions from a real catboost==1.2.10 fixture.
    Create `scripts/check-source-test-separation.sh` (executable, `set -euo pipefail`): grep all `crates/*/src/**/*.rs` EXCLUDING `*_test.rs` for an inline `#[cfg(test)]` that introduces a test MODULE/body (INFRA-06 / D-17). Allow the `#[cfg(test)] mod <name>_test;` DECLARATION form in `lib.rs` (that points at a separate file) but FAIL on `#[cfg(test)] mod tests {` / inline test bodies. Exit 1 with a D-17 violation message on any hit, else print OK. (Distinguish declaration-with-semicolon from module-with-brace, e.g. flag `#[cfg(test)]` lines whose following `mod` line ends in `{`.)
    Extend `.github/workflows/ci.yml` (created by Plan 01): add a step `cargo build -p cb-backend --no-default-features --features wgpu` (and cuda/rocm) to prove the feature gates compile, and add `bash scripts/check-source-test-separation.sh` to the gate set. Keep the lane CPU-only; still NO GPU/ROCm job and NO generator/catboost install (D-12/D-16).
  </action>
  <verify>
    <automated>cargo test -p cb-oracle && cargo clippy -p cb-oracle --lib -- -D warnings && bash scripts/check-source-test-separation.sh && grep -q 'check-source-test-separation' .github/workflows/ci.yml && grep -q 'cb-backend' .github/workflows/ci.yml</automated>
  </verify>
  <done>`compare_stage` exists and is stage-tagged; the per-stage integration test reads real committed catboost==1.2.10 borders + predictions fixtures and passes at 1e-5 (INFRA-04); `check-source-test-separation.sh` enforces D-17 and is wired into CI alongside the cb-backend feature-gate build step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time generator → repo | Python (pinned catboost==1.2.10) runs OUTSIDE CI; produces committed frozen fixtures reviewed in PR |
| committed fixture (.npy/json/model.json) → Rust reader | parsed by cb-oracle; must fail gracefully, no panic |
| Cargo feature flags → cb-backend cfg alias | compile-time selection only; no runtime dispatch surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | non-deterministic fixtures (summation-order drift) | mitigate | `thread_count=1` + fixed `random_seed` recorded in config.json; fixtures committed frozen, generator never re-runs in CI (Pitfall 2, D-12) |
| T-01-08 | Tampering | f32 downcast in generator silently shifting the 1e-5 baseline | mitigate | explicit `np.float64` + dtype assert in generator; Rust `read_npy` into `Array1<f64>` errors on mismatch (Pitfall 3) |
| T-01-SC | Tampering | `catboost==1.2.10` / `numpy` supply chain (Python generator) | mitigate | pinned exact version (D-07), canonical packages (RESEARCH Legitimacy Audit, OK); generator runs build-time only, NEVER in CI — no CI network/install surface. No [ASSUMED]/[SUS] packages -> no blocking checkpoint |
| T-01-09 | Tampering | inline test code creeping into production modules | mitigate | `scripts/check-source-test-separation.sh` grep gate in CI (INFRA-06/D-17) |
</threat_model>

<verification>
- `cargo build --workspace` + `cargo build -p cb-backend --no-default-features --features {wgpu,cuda,rocm}` succeed (INFRA-01 feature gates).
- `cargo test -p cb-oracle` passes incl. the per-stage real-oracle-fixture test (INFRA-04).
- `cargo clippy --workspace --lib -- -D warnings` clean.
- `bash scripts/check-source-test-separation.sh` and `bash scripts/check-no-anyhow.sh` exit 0 (INFRA-06, D-14).
- Frozen input corpus + regression_skeleton fixtures committed (D-11).
</verification>

<success_criteria>
- All eight crates exist and build; cb-backend is the single feature-gated runtime-alias owner (D-02); cb-compute is cubecl-free (D-03) (INFRA-01 complete).
- The Python-first oracle generator (pinned, deterministic, build-time) produces and commits the frozen shared input corpus + per-stage expected-output fixtures (INFRA-03 generation, D-06..D-12).
- The per-stage comparator is proven against real catboost==1.2.10 borders + predictions at <=1e-5 (INFRA-04).
- Source/test separation is script-enforced and wired into CI (INFRA-06).
</success_criteria>

<output>
Create `.planning/phases/01-workspace-lint-discipline-oracle-harness/01-03-SUMMARY.md` when done.
</output>
